# Dream — 2026-06-16

First Dreaming run (dry-validated in-session before opening this PR). **Scope:** full scan. **Reviewed:** `knowledge/` (3 domains), `log.md` (243 lines, 2 resolved human-needed episodes), 10 archived `progress.md`, `lessons.md`.

**Headline:** the hypotheses→rules promotion lifecycle had not kept up — all three `hypotheses.md` were empty while 6+ patterns recurred across 5–6 changes, carried forward by copy-paste in `progress.md` (which freezes on archive). This PR promotes the highest-confidence ones and proposes the rest. Per the skill's first-run cap, only the top set is applied; the remainder is below for your call.

> **Followup — 2026-06-16 (applied on branch `dream/2026-06-16-apply-proposed`).** All "Proposed" items below were applied after re-verifying each against the archived `progress.md`: every pattern **met or exceeded** its claimed confirmation count. One re-grade — the **localized-exception** pattern was rule-grade (4 cross-change confirmations), not a hypothesis → promoted to `knowledge/laravel/rules.md`. The other three (spine-template, config-test, reflection-scope-guard) landed at **2/3** as proposed. New **`knowledge/data-model/`** domain created — it absorbed the enum-`CHECK` rule (relocated from `laravel/`), the PG index-naming rule, and the spine-template hypothesis. Separately, the scheduled-Dreaming routine ("Phase 2") was wired: ADR `decisions/2026-06-16-scheduled-memory-dreaming.md`.

## Applied (in this PR)

**Promoted to `knowledge/laravel/rules.md`:**
- **Pint `fully_qualified_strict_types` auto-imports a docblock `{@see \FQCN}` into a real `use`** — 6 changes (2026-06-12 → 2026-06-16) + already in `lessons.md`. Reference not-yet-built / namespace targets in prose; `{@see}` only existing classes.
- **Driver-guarded PG `CHECK` derived from `Enum::cases()`** — 5 changes. `if (DB::getDriverName()==='pgsql')` branch; SQLite floor = the enum cast; assert the constraint NAME on PG (never a SQLSTATE).

**Promoted to `knowledge/testing/rules.md`:**
- **Pest top-level `function`s share ONE global namespace** — 4 changes. Name per-file helpers uniquely or hit a fatal redeclare on full-suite load.
- **6th SQLite/PG portability trap added to the existing rule:** an uncast bigint reads back as a numeric STRING on PG vs an `int` on SQLite — 3 changes.

**Lessons:** cross-linked the 2026-06-13 Pint lesson → the new rule (kept as the quick-ref correction).

**INDEX:** `knowledge/INDEX.md` rule counts updated (laravel 1→3, testing 1→2).

## Proposed — your call, NOT applied (held by the first-run cap)

Also rule-grade (≥3 cross-change confirmations) but held back to keep this PR reviewable — tell me to apply any/all:
- **Full-chain integration test driven THROUGH the Actions (never factories), asserting the emergent event-SET** — 3 changes → `knowledge/testing/rules.md`.
- **`DatabaseMigrations` vs `RefreshDatabase` for txn-level-0 / `afterCommit` proofs** — 3 changes → `knowledge/testing/rules.md`.
- **Within-module relations allowed (vs the cross-module ban) + short PG index names (63-char limit)** — 3 changes → `knowledge/architecture/rules.md`.

Hypothesis-grade (2 confirmations, "1 away" → `hypotheses.md`):
- Spine create-entity template (migration+model+factory+event+action+recorder-in-txn) · `config/` is outside phpstan paths → pin with a Feature config-test · Localized domain-rejection exception via static factory + `(string) __()` cast · Reflection dir-listing as a scope-guard (not `class_exists('<literal>')`).

## Flags — report-only
- ⚠️ **All three `hypotheses.md` were empty** — the hypothesis stage was being skipped; reusable patterns died in archived `progress.md`. This PR is the first correction; a scheduled Dreaming routine keeps it from re-accreting.
- **Domain gap:** DDL / migration / FK-and-index patterns have no home. Suggest a new `knowledge/data-model` domain (would house the enum-CHECK rule + index-naming + the spine template).
- `hot.md` Open Patterns: all current, none stale.

## Reviewed, nothing to do
- **Demotions: none.** The 3 pre-existing rules all hold; the recurring `jsonb` key-reorder *confirms* the existing testing trap #3 (not a contradiction).
- **`log.md` human-needed episodes (×2)** — operator table-prefix; lifecycle block-vs-preserve children — both resolved via ADR and already captured; not lesson material.
- **`decisions/` (11 ADRs):** no contradictions surfaced.

---
*Generated by the `/dreaming` skill (propose-only). Merge to accept the Applied curations; comment to request any Proposed item.*

🤖 Generated with [Claude Code](https://claude.com/claude-code)
