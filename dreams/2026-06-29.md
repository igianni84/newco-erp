# Dream — 2026-06-29

Scope: since the last dream (`dreams/2026-06-16.md`). Reviewed: `knowledge/` (4 domains: architecture, data-model, laravel, testing), `log.md` (since 06-16), 8 archived `progress.md` `## Codebase Patterns` (06-17 → 06-21), `lessons.md` (06-16 → 06-21), `hot.md` Open Patterns (stale check).

**Note on snapshot skew.** This session was initialized with a *future* (06-24) `hot.md`/`lessons.md` snapshot, but the actual repo (origin/main, clean tree) is at the **06-21 state**: `lessons.md` ends at the 06-21 nullable-`belongsTo` lesson; `log.md` and the archive both end at `operator-console-parties-supply-side`. Curation used the **real repo files** only — the 06-22→06-24 club-credit / profile-lifecycle / RelationManager lessons do **not** exist here and were **not** cited. Every confirmation below is grounded in an archived change present on `main` and was re-verified against code.

## Applied (in this PR)

- **Promote — `knowledge/data-model/` "Spine create-entity template" → rule** (was 2/3). 3rd confirmation: **2026-06-18 `parties-holds`** stands up a fresh evented entity (`parties_holds` migration + `Hold` model `$guarded=[]`/typed `newFactory()` + `HoldFactory` + `CustomerHoldPlaced` event + `PlaceHold` Action with `Hold::create()` + `recorder->record()` in one `DB::transaction`). Verified `app/Modules/Parties/Actions/PlaceHold.php:76-98`. The promotion **refines** the template: the creation event/Action may carry a **domain verb** (`Place`/`Placed`), not literal `Create`/`Created` — the structure is the invariant, not the word.
- **Promote — `knowledge/testing/` "Prove a class ABSENT by listing the directory" → rule** (was 2/3). Now 4 cross-change confirmations: 06-15 `catalog-product-spine`, 06-16 `parties-producer-lifecycle` (verified `SupplyLifecycleChainTest.php:277-285,383-387` — `glob(...) ?: []` + `not->toContain`), 06-17 `parties-compliance` and 06-19 `parties-membership-suspension` (both extend/maintain that test's Events-glob forbidden-names set).
- **New rule — `knowledge/testing/` "A green Pest suite is necessary but NOT sufficient (PHPStan-max trap family)"** — rule-grade on arrival (≥3 cross-change). A self-identified family of test shapes that pass at runtime yet red Larastan: `$this->get()`/`travelTo()` (`method.notFound`), two chained `->not`, multi-method expectation chains, bare-`array` dataset spread, `class_exists('<literal>')` (`function.impossibleType`), `CarbonImmutable::create()` `|null`. Confirmations: 06-13 `foundations-money-i18n-flags`, 06-17 `parties-compliance`, 06-19 `parties-membership-activation`, 06-19 `parties-membership-suspension`, 06-20 `operator-console-catalog-spine`. Prescription: `phpstan analyse` at max is a mandatory gate, not the green suite alone.
- **Confirm existing rule (#5) — `knowledge/laravel/` Pint `{@see \FQCN}` rule.** Appended a dated confirmation: **2026-06-20 `operator-console-catalog-master`** (`ViewProductMaster`'s `{@see \…\Catalog\Exceptions\IllegalLifecycleTransition}` — a *concrete, existing* class — auto-imported a `use` that breached the OperatorPanel `{Models, Actions}` carve-out). Strengthens, not duplicates.
- **INDEX** updated: data-model 2→3 rules (0 hyp), testing 4→6 rules (0 hyp); laravel/architecture unchanged.

## Proposed — your call, NOT applied

- **Create a `knowledge/filament/` domain.** The operator-console work (4 archived changes, 06-20×3 + 06-21) has accreted a cluster of Filament-5-specific gotchas that currently live only in `lessons.md` / archived `progress.md`, with no knowledge home: `EloquentBuilder::value()`/`first()` apply the cast (return the enum, not the raw string); a `->numeric()` field dehydrates as a **float** → narrow with `is_numeric()` not `is_string`/`is_int`; `Filament\Forms\Components\Component` does **not** exist in Filament 5 (use `Filament\Schemas\Components\Component`); a nullable `belongsTo` read in display code is phpstan-clean only as `$x = $record->rel; $x === null ? … : $x->attr`; a `DatePicker` dehydrates as a `'Y-m-d'` string. Each is seen in 1–2 changes so far → **hypothesis-grade on arrival**. Deferred because creating a domain + grading 5 candidate entries is judgment-heavy and would push this PR past the reviewable cap. Recommend: next dream, open `knowledge/filament/{knowledge,hypotheses,rules}.md`, seed these as hypotheses, add the INDEX row.
- **`knowledge/laravel/` "config/ outside PHPStan paths" hypothesis stays at 2/3** (1 away). No 3rd config-invariant change archived since 06-16 (the operator-console slices are UI/i18n, not config). Leave as-is; the `catalog.php` governance test remains a weaker non-counted form.

## Flags — report-only

- **Snapshot skew (above).** Worth a human glance: the container's injected `hot.md`/`lessons.md` (06-24) is ahead of `origin/main` (06-21). If 06-22→06-24 work (club-credit, profile-lifecycle, premium-UI pass) is real, it lives on an un-merged/un-pushed branch — its lessons (e.g. the `class_exists`-`impossibleType` in `ClubCreditEventOwnershipTest`, the `travelTo` trap, the RelationManager-on-`ViewRecord` hidden-`CreateAction`) would add **further confirmations** to the two testing rules promoted here and seed the proposed `filament/` domain. Re-run a dream once that work reaches `main`.
- `hot.md` **Open Patterns** — checked for staleness only (never edited; volatile). The two patterns there (RelationManager `isReadOnly()`/`ViewRecord` create-button; DemoSeeder not chained into `DatabaseSeeder`) belong to the 06-24 snapshot, not this repo state — no action.
- `decisions/INDEX.md` — 21 ADR files vs 22 table rows: consistent (the extra row is the header/`active` legend, not an orphan). 11 ADRs added since 06-16, all present. No drift.

## Reviewed, nothing to do

- **Demotions: none.** No rule was contradicted by 06-17→06-21 evidence. The new operator-console boundary work *confirms* the existing within-module-relations and platform-substrate architecture rules rather than challenging them.
- **`log.md` human-needed episodes since 06-16:** one — `catalog-lifecycle-approval` 5.2 (retire block vs preserve children), resolved 06-16 via ADR `2026-06-16-catalog-retirement-reference-integrity-scope`. Already captured; not lesson material.
- **`knowledge/architecture/`** (2 rules) — both hold; no new architecture hypothesis earned a slot.
- **`lessons.md`** — the recurring mistakes since 06-16 are already individually captured (each PHPStan-max trap, the Pint `{@see}` console variant, the handoff-blast-radius grep). This dream **rolls the scattered PHPStan-max traps up** into one knowledge rule rather than adding a new lesson; no un-captured recurring mistake remained.

---
*Generated by the `dreaming` skill (propose-only). Merge to accept the Applied curations; comment to request any Proposed item.*

🤖 Generated with [Claude Code](https://claude.com/claude-code)
