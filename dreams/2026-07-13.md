# Dream — 2026-07-13

Scope: since the last dream (2026-06-16). Reviewed: `knowledge/` (6 domains), `log.md` (971 lines, 4 resolved human-needed episodes), **25 archived changes** (`2026-06-16-parties-producer-lifecycle` → `2026-07-10-parties-hero-package-residuals`) — their `progress.md` `## Codebase Patterns` are already consolidated into `knowledge/*/hypotheses.md`, `lessons.md` (250 lines), `hot.md` Open Patterns (stale check only).

**Headline.** The hypothesis lifecycle is healthy this cycle — the ralph loop has been feeding `knowledge/*/hypotheses.md` diligently (the 2026-06-16 dream's fix held). Two consequences to curate: (1) one hypothesis genuinely reached its 3rd distinct-change confirmation and is promoted; (2) two hypotheses turned out to **duplicate rules that already exist** (a later ralph iteration re-seeded a pattern the first dream had already promoted) — folded back as confirmations, not left as parallel copies. Almost every remaining 1/3 hypothesis is legitimately young: seeded from the two most recent changes (`parties-hero-package` / `-residuals`), which alone contributed ~10 new testing/laravel/filament hypotheses. Nothing else earned promotion; a few counting/index issues are flagged below.

## Applied (in this PR)

- **Promote → `knowledge/testing/rules.md`:** *"Prove a class / Action / Event is ABSENT by listing the directory at runtime — never `class_exists('<literal>')` (PHPStan max reds `function.impossibleType`)"*. Reached **3 distinct-change confirmations**: 2026-06-15 `catalog-product-spine` (`CatalogNamingCascadeTest`), 2026-06-16 `parties-producer-lifecycle` (`SupplyLifecycleChainTest`), **2026-06-23 `club-credit`** (`ClubCreditEventOwnershipTest` §15.8 — verified in the archived test: `glob(app_path('Modules/Parties/Events/*.php')) ?: []` → basename → `not->toContain`, with the `class_exists` arm removed for exactly this reason; lesson 2026-06-23). Removed from `hypotheses.md`.
- **Dedup + confirm (`knowledge/testing/rules.md`):** the hypothesis *"A Pest global test helper is declared once per PROCESS"* duplicated the existing rule *"Pest helper functions / top-level `function`s share ONE global namespace"* (already at 4 confirmations). Folded `parties-hero-package` in as a **5th dated confirmation** (2026-07-09 — `ProfileApprovalCapacityGateTest`'s probe fataled in `TestSuiteLoader::load()`; renamed to `seatClubTo()`/`freeOneSeat()`) and removed the duplicate hypothesis.
- **Dedup + confirm (`knowledge/laravel/rules.md`):** the hypothesis *"Pint promotes a docblock `{@see \FQCN}` into a real `use` … module-boundary violation"* duplicated the existing rule (promoted in the 2026-06-16 dream). Folded in **two newer confirmations** — 2026-07-06 `parties-producer-approval-sod` (the trap is **not** OperatorPanel-only; fires in any lateral `Modules\{X}`→`{Y}` docblock; lesson 2026-07-06) and 2026-07-09 `parties-hero-package` (proved `not->toUse` catches the manufactured dependency as an *unused* `use` **and** as an inline `\FQCN` with no `use`) — and removed the duplicate hypothesis.
- **`knowledge/INDEX.md`:** corrected all counts to verified values — testing `5 rules · 10 hypotheses`, laravel `3 rules · 3 hypotheses`, filament `1 rule · 5 hypotheses` (the last two were already stale before this run; laravel said 1, filament omitted its hypotheses entirely).

## Proposed — your call, NOT applied

- **`data-model` spine-template hypothesis (2/3) may have earned its 3rd.** It says "the next slice that stands up a new create-entity spine is the 3rd confirmation". `club-credit` (2026-06-23) stood up the `ClubCredit` entity — but its writer is `IssueClubCredit`, not the canonical `Create*` shape, so it is a **variant** of the template, not a clean instance. Deferred: promoting needs a judgment call on whether the `Issue*` creator counts, best made by reading `club-credit`'s `progress.md`. Leave at 2/3 ("1 away") until a textbook `Create*` spine lands, or rule that `Issue*` counts.
- **`testing` guard-ORDER negative-proof hypothesis (2/3 → 1 away).** Both confirmations are the two hero-package changes (2026-07-09, 2026-07-10) — genuinely distinct changes, so the count is honest. Needs one more distinct change (a Module A/S multi-gate transition) to promote.
- **`laravel` `config/`-outside-PHPStan hypothesis (2/3 → 1 away).** Two distinct changes (both 2026-06-13). No 3rd surfaced this cycle.
- **Consolidate the "runtime fine, static analysis (PHPStan max) is not" meta-family into a `knowledge/testing` or `knowledge/laravel` rule.** It recurs across ≥8 lessons (2026-06-12 arch-array-under-`not`, 2026-06-13 `$this->get()`, 2026-06-18 two-chained-`not` + bare-`array` spread, 2026-06-20 `toContain` chain, 2026-06-21 nullsafe-`belongsTo`, 2026-06-23 `$this->travelTo()`, 2026-07-02 collection-index). Rule-grade by count, but the *scope* and phrasing need a human call (it's a family, not one trap), so deferred rather than auto-authored.
- **Extract a `knowledge` rule: "the change's Risks/tasks list is NOT the blast radius — grep every caller (Actions AND console `callAction`)."** ≥4 distinct-change confirmations in `lessons.md` (2026-06-18 parties-holds, 2026-07-03 RM-03, 2026-07-06 RM-08 ×2). Rule-grade, but I'd want your eye on where it lives (testing vs a new `process` domain) before promoting.
- **Extract a `knowledge` rule: "a verifier's finding is a candidate, not a fact."** The 2026-07-09 F1 lesson explicitly logs its own 3rd confirmation (F8 subagent misreport; P3-sweep rejected WARNING; tracker §7), reinforced 2026-07-10. Meta/process-grade; no home domain exists yet — proposing a new `knowledge/process/` domain rather than forcing it into `testing`.

## Flags — report-only

- **⚠️ Double-counted confirmation — `filament` RelationManager hypothesis reads 2/3 but both confirmations are dated 2026-06-24 and cite the same archived change** (`operator-console-parties-membership` — an "UI pass" and a "PREMIUM pass" of one change). Per the one-change-one-date rule (`.claude/CLAUDE.md`; lesson 2026-06-16) this is really **1 distinct confirmation**. Left unedited (a count *reduction* is a judgment call I'd rather you ratify); recommend re-grading to 1/3.
- **`knowledge/INDEX.md` had drifted before this run** — laravel/filament hypothesis counts were stale. Fixed here, but the drift means nothing keeps INDEX counts honest between dreams; consider a `memory-health.sh` check or a note that Dreaming owns the reconciliation.
- **`hot.md` Open Patterns:** current, none stale (all three trace to the 2026-07-10 F10 work, freshly relevant).
- **`module-k` domain still at 2 observations, 0 hypotheses/rules** since inception — a large body of Module-K work archived this cycle (holds, membership, hero-package) yet nothing promoted into its folder (the patterns landed in `testing`/`laravel`/`filament` instead). Not necessarily wrong (they *are* cross-cutting), but worth a glance: is `module-k` a dead domain, or is domain-specific Parties knowledge being mis-filed?
- **Two protected-file doc-sync items still open** (from `hot.md`, unchanged, not Dreaming's to touch): `ownership_flag` `CRURATED→NEWCO` rename lag at `CLAUDE.md:73` + `CONTEXT.md:450/453`; and root `CLAUDE.md`'s "immutable baseline" wording flagged by the F10 ADR. Both need Giovanni (protected paths).

## Reviewed, nothing to do

- **Demotions: none.** Scanned every `rules.md` against the 25 changes' lessons/progress — the recent work uniformly *extends or confirms* existing rules (the cross-engine portability rule gained a jsonb-`toBe` refinement 2026-07-08; the enum-CHECK rule gained the widening-admission-test refinement 2026-07-08; the operator-console-slice rule gained the `isReadOnly()` corollary). No rule was contradicted.
- **`log.md` human-needed episodes (×4, all pre-06-25):** operator table-prefix; catalog lifecycle block-vs-preserve; auth guard prefix — all resolved via ADR and already captured. Not lesson material.
- **`lessons.md`:** already comprehensive and current — every recurring 2026-06-16→07-10 mistake is captured with Mistake→Correction→Rule (the loop writes them diligently). No un-captured recurring mistake found, so **0 new lessons** extracted this cycle. The consolidation opportunities are noted under *Proposed*, not missing lessons.
- **`decisions/`:** no ADR contradictions surfaced by the reviewed changes.

---
*Generated by the scheduled `dreaming` routine (propose-only). Merge to accept the Applied curations; comment to request any Proposed item.*

🤖 Generated with [Claude Code](https://claude.com/claude-code)
