# Dream — 2026-07-20

Scope: since the last dream `2026-06-16` (23 changes archived `2026-06-17` → `2026-07-10`). Reviewed: `knowledge/` (6 domains), `log.md` (July slice), 23 archived `progress.md` `## Codebase Patterns`, `lessons.md` (through `2026-07-10`), `decisions/INDEX.md`, `hot.md` Open Patterns (stale check).

**Headline:** the memory system is being maintained *inline* and well — every hypothesis I checked carries honest, self-critical confirmation counts (some even shrank their own count for rigor). Two hypotheses had genuinely earned a **verified third distinct-change confirmation** and are promoted; two others named as candidates are **true negatives** and correctly stay at 2/3. One cross-change pattern (7 changes) was rule-grade-on-arrival and is extracted. Everything judgment-heavy is below the fold.

## Applied (in this PR)

**Promoted `knowledge/testing/hypotheses.md` → `rules.md` (3/3):**
- **Prove a class/Action/Event is ABSENT by listing the directory at runtime — never `class_exists('<literal>')`** (PHPStan max flags it `function.impossibleType`). Third confirmation verified: `2026-06-23 club-credit` `ClubCreditEventOwnershipTest` 5.1 — wrote the literal `class_exists`, got redded, switched to a `glob(...)` → `basename` → `not->toContain` loop (evidence: `.../2026-06-23-club-credit/progress.md:118-124`; quick-ref already in `lessons.md 2026-06-23`). Joins `2026-06-15 catalog-product-spine` + `2026-06-16 parties-producer-lifecycle`. The `is_a`/`is_subclass_of`-literal corollary that lived in the hypothesis's "NOT counted" note was folded into the rule, so nothing was lost.

**Promoted `knowledge/laravel/hypotheses.md` → `rules.md` (3/3):**
- **`config/` is outside PHPStan's analysis paths — pin every config invariant with a Feature config-test.** Third confirmation verified: `2026-07-09 parties-hero-package` stood up a new `config/parties.php` and pinned it with `HeroPackageCapacityReaderBindingTest` (default/override/`?int`-cast) + `DemoSeederHeroPackageCapacityTest` (the `.env.example`-doc + config-key pin, mutants M5/M6 red only it) (evidence: `.../2026-07-09-parties-hero-package/progress.md:10,46,240`). Joins the two `2026-06-13` origin changes.

**Extracted to `knowledge/laravel/rules.md` (rule-grade on arrival, 7 distinct changes):**
- **An Action injects `DomainEventRecorder` + `ActorContext` IFF it records a domain event** — audit-only/eventless Actions take neither; decide by "does it record an event?", never by surface symmetry with an evented sibling. Confirmed across `parties-compliance` (06-17), `parties-holds` (06-18), `parties-membership-activation` (06-18), `parties-membership-suspension` (06-19), `club-credit` (06-23), `parties-anonymisation` (07-02), `parties-module-k-br-guards` (07-08).

**`knowledge/INDEX.md`:** recounted the drifted rows — testing `4→5 rules`, laravel `3 rules · 1 hyp → 5 rules · 3 hyp` (the "1 hypothesis" was stale; there were 4), filament `1 rule → 1 rule · 5 hypotheses`.

## Proposed — your call, NOT applied

Rule-grade-on-arrival cross-change patterns (≥3 distinct changes) found by the archived-`progress.md` scan but held back to keep this PR reviewable (skill cap). Each would be a new `rules.md` entry; tell me to apply any/all:
- **Operator-console i18n kit-key completeness test** (5-guard `<Entity>ConsoleI18nTest` shape, enumerating string-concatenated kit keys a source scan can't see) — **9 changes** → `knowledge/filament/rules.md` (or testing).
- **`SupplyLifecycleChainTest`'s exact-set non-`Create*` Action whitelist** — any new non-`Create` Action reds `toEqualCanonicalizing([...])` unless registered the same iteration; the ONLY exact-set Action guard (siblings are subset checks) — **7 changes** → `knowledge/testing/rules.md`. (Currently a single-cite `lessons.md 2026-06-23`; this would promote + backfill the recurrences.)
- **Run the full suite with `php -d memory_limit=… vendor/bin/pest`, never `php artisan test`** — `artisan test` re-spawns a child that ignores `-d` and OOMs the ~1900-test suite at 128 MB; a multi-class `--filter` OOMs too — **~11 changes** → an ops rule in `knowledge/testing/rules.md`. (In `lessons.md 2026-06-20`; would formalize.)
- **i18n exception-copy test + PII-register discipline** (sibling `*ExceptionsTest`, absent-token interpolation probe, never interpolate money-minor/DOB/email) — **7 changes** → testing/laravel.
- **Meta-pattern: "a verifier's finding is a candidate, not a fact"** — verify a runtime claim by *running* it (or reading the exact method invoked, never a sibling); a claim that reverses a decision must *quote* the decision. `lessons.md` itself tallies this at 3–4 (F8 subagent misreport · P3-sweep rejected WARNING · F1 · F10). Genuinely rule-grade, but it has **no natural domain** (verification/process) — deferred pending your call on whether to open a `knowledge/process/` domain or file it under `testing`.

## Flags — report-only (no edit)

- **Duplicate: the Pest-global-helper pattern exists as BOTH a promoted rule AND a fresh 1/3 hypothesis.** `knowledge/testing/rules.md` already carries "Pest helper functions share ONE global namespace…" (promoted first dream, 4 confirmations), yet `knowledge/testing/hypotheses.md:76` re-logs the identical mechanism as a new hypothesis citing `parties-hero-package`. The hypothesis adds richer prescription (the `TestSuiteLoader` mechanism; the "never call a sibling file's helper" corollary). Recommend folding that extra prescription into the existing rule and deleting the hypothesis — left unapplied because the prose merge is a judgment call. The pattern genuinely recurred again in `club-credit` (06-23), `membership-suspension` (06-19), `hero-package` (07-09), `residuals` (07-10) — the rule is confirmed, not in question.
- **Near-duplicate: `knowledge/laravel/hypotheses.md` "Pint `{@see}` → module-boundary violation" (1/3) restates the already-promoted rule** "Pint `fully_qualified_strict_types` rewrites `{@see \FQCN}` into a real `use`", whose confirmation list already includes the invariant-10 / `ModuleBoundariesTest` escalation (`2026-06-16 catalog-lifecycle-approval`) and which recurred again at `2026-07-06 producer-approval-sod` (`lessons.md`). Consider retiring the hypothesis into the rule as a confirmation rather than tracking it separately.
- **`hot.md` Open Patterns are current** (dated 07-10, F10 staleness-detector theme) — no stale entry to flag. Its "*a vacuous gate is worse than no gate* (4th confirmation)" line is a genuinely-recurring design maxim (RM-05 Null-adapter · F1 inferred-PG-failure · F10 empty-`ls-remote`) that lives only in volatile `hot.md` + `lessons.md 2026-07-10`; could earn a `knowledge/` home if it recurs in code rather than tooling. Noted, not acted on.

## Reviewed, nothing to do

- **Demotions: none.** No rule was contradicted by July evidence. The recurring cross-engine `jsonb`/bigint/`timestamptz` traps *confirm* the existing testing cross-engine rule (trap 3 was even refined in-place at `catalog-module-0-completeness-sweep 07-08`); the enum-`CHECK` and short-index rules held.
- **`guard-ORDER provable only NEGATIVELY` (testing, 2/3):** checked all 23 changes for a genuine 3rd — none. The nearest (`membership-activation` proves order *positively* by which exception fires; `producer-approval-sod` is a single-gate no-trace proof; `catalog-module-0 07-08` pins a skipped `$apply` closure but *also* positively) are adjacent, not the "only negatively" mechanism. Correctly stays 2/3, still concentrated in the hero-package lineage.
- **`Spine create-entity template` (data-model, 2/3):** the obvious candidate `club-credit` is a **true negative** — `IssueClubCredit` records NO `*Created` event (Club-Credit state is Module-E-owned, §11.4), so the template's defining elements are deliberately absent; `anonymisation`/`enhanced-kyc` new entities are likewise eventless. Correctly stays 2/3.
- **`decisions/INDEX.md`:** 33 ADRs on disk, all indexed; every `2026-07` ADR present. No drift.
- **`module-k` / `architecture` / `data-model` / `filament` observation & rule bodies:** no contradictions; module-k's 2 observations haven't earned a mechanism/count yet (correctly still observations).

---
*Generated by the `/dreaming` skill (propose-only). Merge to accept the Applied curations; comment to request any Proposed item.*

🤖 Generated with [Claude Code](https://claude.com/claude-code)
