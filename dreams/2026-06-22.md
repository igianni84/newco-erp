# Dream — 2026-06-22

**Scope:** since the 2026-06-16 dream. **Reviewed:** `knowledge/` (4 domains — architecture/data-model/laravel/testing), `log.md` (lines 256–457, 8 archived changes + ADR/spec-sync entries), the 8 newly-archived `progress.md` `## Codebase Patterns` (2026-06-17 `parties-compliance` → 2026-06-21 `operator-console-parties-supply-side`), `lessons.md` (15 new corrections since the last dream), `hot.md` Open Patterns, `decisions/` (11 ADRs since 06-16).

**Headline:** the period was dominated by two big arcs — the demand-side **Parties** backend (compliance / holds / membership activation+suspension) and the **operator-console** build-out (4 changes: catalog-master, catalog-spine, parties-producer, parties-supply-side). The console arc has produced a tight, **4-times-proven** family of patterns (the shared kit, the i18n five-guard test, the through-the-pages closing chain) that had no home in `knowledge/` — and a long run of Pest+PHPStan-max "runtime-fine/static-analysis-not" lessons that were never consolidated. This PR curates the three highest-confidence, lowest-risk ones into `knowledge/testing/rules.md` and proposes the rest (a hypothesis promotion + a new `filament` domain) for your call.

## Applied (in this PR)

All three land in `knowledge/testing/rules.md` (one file → reviewable):

- **CONFIRM (action 5): the closing-integration rule** — added its now-proven **operator-console *through-the-Filament-pages* variant** (drive via `Livewire::test(...)->callAction(...)`, re-instantiate the View page per action, assert the emergent set with `toEqualCanonicalizing` as a *multiset*, foreach the `module`/`actor_role`/`actor_id` envelope, grep `app/` for listeners/projectors first) + 4 dated cross-change confirmations: `operator-console-catalog-master` (06-20), `…-catalog-spine` (06-20), `…-parties-producer` (06-20), `…-parties-supply-side` (06-21). Not a new rule — the existing one strengthened, per "confirm, don't duplicate."
- **NEW RULE (action 4, rule-grade on arrival — 4 distinct changes): the operator-console FIVE-guard i18n kit-key-completeness test** (`<Entity>ConsoleI18nTest`). Enumerate every `operator_console.<entity>.*` key (kit-concat keys are invisible to a source scan), assert EN baseline + IT-differs + label/plural_label EN-fallback + IT⊆EN (trailing-dot filter load-bearing) + a hardcoded-sink scan; test count is mechanical; verify with `--filter`, never a bare path. Confirmations: catalog-master / catalog-spine / parties-producer / parties-supply-side.
- **NEW RULE (action 4, rule-grade — 5 distinct changes): "verify every non-trivial Pest expectation/dataset SHAPE against PHPStan max."** Consolidates the recurring larastan "narrows only one proxy per chain" family that was scattered across 5 standalone `lessons.md` entries: one `->not` per chain; don't chain multiple expectation methods; arrays on the dependency side of `not` (never the source side); TestCase instance methods via `Pest\Laravel\*` globals; don't `...$spread` a bare-`array` dataset param into a string-keyed literal. Confirmations: `foundations-modules-skeleton` (06-12), `foundations-money-i18n-flags` (06-13), `parties-compliance` (06-18), `parties-membership-activation` (06-19), `operator-console-catalog-spine` (06-20).

`knowledge/INDEX.md`: testing rule count 4 → 6.

## Proposed — your call, NOT applied

- **Promote `data-model` "spine create-entity template" hypothesis 2/3 → 3/3 → rule.** The hypothesis explicitly named its own trigger ("the next slice that stands up a new create-entity spine is the 3rd confirmation"). **`2026-06-18-parties-holds` stood up the `Hold` entity** as exactly that skeleton: migration + `Models\Hold` (`$guarded=[]`, typed `newFactory`) + a pure-fixture `HoldFactory` (records no event) + the creation event `CustomerHoldPlaced` + `PlaceHold` (Model::create + recorder.record in ONE `DB::transaction`). **Deferred, not applied,** for one judgment call: the naming varies (`PlaceHold`/`CustomerHoldPlaced` vs the hypothesis's `Create*`/`*Created`) because a Hold is domain-"placed", not "created" — structurally identical, lexically not. If you read that as a genuine 3rd confirmation (I do), say so and I'll promote it next pass; the structural match is complete.
- **Create a `filament` knowledge domain** (listed as a suggested domain in INDEX, now earned by 4 console changes). The console arc has produced a cluster of rule-grade patterns with no home — they live only in archived `progress.md` + `lessons.md` (the exact "patterns die in progress.md" signal the first dream flagged). Candidate rule-grade entries (each ≥3 console-change confirmations):
  - The **shared operator-console kit** under `Filament/Console/` (abstract `OperatorConsoleResource`/`ViewRecord`/`CreateRecord` + `SurfacesDomainActions`; placed outside the 3 discovered dirs so abstracts aren't auto-discovered; typed invoke-closures + `recordOf()` for a no-shared-interface PHPStan-max-clean dynamic dispatch).
  - **Write-through-actions, never `$model->save()`** — read-bind the resource read-only, route every write through `app(<Action>)->handle(...)`, enforced by the repo-local type-aware `NoEloquentWriteInOperatorPanelRule` PHPStan rule; reach-the-create affordance is a plain link, never `CreateAction::make()`.
  - The **`{Models, Actions, Enums}` cross-module carve-out** for `OperatorPanel` (operand enums only; state enums stay rendered via the cast) — ADR `2026-06-21-operator-console-operand-enum-carveout`.
  - Three Filament-5-specific PHPStan-max traps already in `lessons.md`: nullable-`belongsTo` display = local + `=== null` ternary (never `?->` nor FK-column narrowing); a `->numeric()` field dehydrates as a **float** (narrow with `is_numeric()`); the form-component base is `Filament\Schemas\Components\Component`, NOT the non-existent `Filament\Forms\Components\Component`.
- **`testing` "prove-absence-by-directory-listing" hypothesis (2/3):** likely reached 3/3 via `parties-membership-activation`'s deferred-seam proof (`glob(Events/*.php)` basename `->not->toContain('<TriggerEvent>')`), but it's bundled with a `class_exists(...)->toBeFalse()` variant — deferred pending your read on whether the seam-proof counts as the same idiom.
- **`laravel` "config-test" hypothesis (2/3):** no clean 3rd `expect(config(...))->toBe(...)` confirmation surfaced in this window — leave at 2/3.

## Flags — report-only
- **Homeless console patterns (the recurring signal):** the 4-change operator-console arc is the strongest pattern cluster in the repo with no `knowledge/` home — see the proposed `filament` domain. Until it lands, the patterns survive only in frozen `progress.md`.
- **`hot.md` Open Patterns:** all four (closing-chain thrice-proven, supersession-side-effect D8, i18n five-guard four-times-proven, rule-of-three deferred) are current as of `operator-console-parties-supply-side` (06-21) — none stale. The "i18n five-guard four-times-proven" line is now codified as a rule by this PR.
- **`decisions/INDEX.md`:** all 11 ADRs added since 06-16 are referenced — no index drift.
- **Demotions: none.** No rule was contradicted by the window's evidence; every recurrence strengthened an existing rule or extended a trap list.

## Reviewed, nothing to do
- **`architecture/rules.md`** (2 rules) + **`architecture/hypotheses.md`** (empty): the within-module-relations rule was re-confirmed (e.g. `Customer::profiles()`/`Producer::clubs()` cascade reads) but already carries ample confirmations — no edit needed. Platform-substrate rule untouched by the window.
- **`data-model/rules.md`** (enum-CHECK, short-index-names): both re-confirmed by every Parties migration (`parties_holds`, the additive nullable-CHECK widening) — already richly confirmed, no new line earned.
- **`laravel/rules.md`** (auth-config-merge, Pint `{@see}`→`use`, localized-exception): the Pint and localized-exception rules re-confirmed repeatedly (parties-compliance/holds/membership) and already cite 6+ changes — strengthened in spirit, no edit needed.
- **`log.md` human-needed / blocked:** the only two episodes (operator table-prefix; lifecycle block-vs-preserve) predate this window and were already resolved+captured by the last dream.
- **`lessons.md`:** the 15 new entries are individually well-formed; only the Pest/PHPStan-max family was cross-change-recurrent enough to consolidate into a knowledge rule (done above). The Filament-specific lessons are proposed for the new domain, not yet rule-extracted.

---
*Generated by the `dreaming` skill (propose-only). Merge to accept the Applied curations; comment to request any Proposed item.*

🤖 Generated with [Claude Code](https://claude.com/claude-code)
