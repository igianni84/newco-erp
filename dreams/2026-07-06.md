# Dream — 2026-07-06

Scope: since the last dream (`dreams/2026-06-16.md`; `log.md` dreaming markers 2026-06-16). Reviewed: `knowledge/` (4 domains → 5), `log.md` (lines 245→457, no `blocked`/`human-needed` since), the **8** changes archived since the last dream (`parties-compliance`, `parties-holds`, `parties-membership-activation`, `parties-membership-suspension`, and the four `operator-console-*` consoles) via their `## Codebase Patterns`, `lessons.md`, `hot.md` Open Patterns, `knowledge/INDEX.md`, `decisions/INDEX.md`.

**Headline:** the operator-console build-out (4 changes, 06-20→06-21) produced a dense, repeatedly-confirmed **Filament** pattern family that had no domain home — `knowledge/INDEX.md` even lists `filament` as a suggested-but-unbuilt domain. This dream opens that domain with two rule-grade patterns (each 4× cross-change confirmed), promotes one testing hypothesis that reached 3/3, and strengthens the closing-chain testing rule with its console-page variant. Everything judgment-heavy is left under *Proposed*.

## Applied (in this PR)

**Promoted `hypotheses.md` → `rules.md`:**
- **`testing`: "Prove a class / Action / Event is ABSENT by directory listing — never `class_exists('<literal>')`"** — was 2/3, now **3/3**. Third confirmation verified in code: `2026-06-17 parties-compliance` `ComplianceIndependenceTest` uses `glob(app_path('Modules/Parties/Actions/*.php')) ?: []` → basenames → `expect($actions)->not->toContain($forbidden)` (for `ActivateAccount`/`LockOriginatingClub`/`SetOriginatingClub`) with a `->not->toBeEmpty()` non-vacuity guard and `class_exists($var)` on listing-derived names. Added a note distinguishing it from the deferred-cross-module-**seam** proof (`class_exists(FQCN)->toBeFalse()` on a not-yet-built listener) so the two aren't confused as a contradiction.

**New domain `knowledge/filament/`** (`knowledge.md` + `hypotheses.md` + `rules.md`), two rules — each already rule-grade on arrival (4 dated cross-change confirmations from the console build-out, per the skill's "≥3 distinct-change confirmations → rule-grade directly"):
- **The operator-console kit** — read-only `OperatorConsoleResource` + write-through `OperatorConsoleCreateRecord` + `OperatorConsoleViewRecord`/`SurfacesDomainActions` under the never-discovered `Filament/Console/`; cross-module surface fenced to `{Models, Actions, Enums}`; `getPages()` boot-coupling; prose-not-`{@see}` for cross-module `Exceptions`/`Enums`. Confirmations: `catalog-master` (origin), `catalog-spine` (kit extracted + reused), `parties-producer` (non-catalog recipe), `parties-supply-side` (operand-enum carve-out). Anchored to ADRs `2026-06-19-...read-binding...` and `2026-06-21-...operand-enum-carveout`.
- **The kit-based console i18n-completeness test** — sink-anchored literal scan + kit-key enumeration via `Lang::has(...,'en',false)` (the concatenated keys never appear as literals) + per-key EN fallback, **run via `--filter`/full-suite never a bare path** (the shared `scanOperatorConsoleHardcodedSinks` won't load). Confirmations: `ProductMasterConsoleI18nTest`, `SpineConsoleI18nTest`, `ProducerConsoleI18nTest`, `Club`+`ProducerAgreement` i18n tests.
- Plus one **hypothesis** (1/3, consolidated) — the Filament-5 PHPStan-max trap family (nullable `belongsTo` local+null-check; `->numeric()` dehydrates a FLOAT; `Filament\Schemas\Components\Component` not `Forms\...`; multi-method `expect()` chains degrade to `mixed`). Dense within the 4 changes but tracked as one shape to confirm on the next module console.

**Confirmed an existing rule (not duplicated):**
- **`testing`: "closing integration test drives the chain and asserts the emergent event-SET"** — appended the **operator-console variant** (drive through the Filament **PAGES**, not raw Actions; grep `app/` for the event names first to rule out a projector row; loose `toEqual` for uncast PG bigint ids) with 4 new console-change confirmations.

**INDEX:** `knowledge/INDEX.md` — new `filament` row (2 rules · 1 hypothesis); `testing` 4→5 rules, hypothesis count 1→0.

## Proposed — your call, NOT applied

- **`data-model` spine-template hypothesis (2/3) — is `parties-holds`'s `Hold` the 3rd confirmation?** `Hold` is a genuinely new evented entity (migration `parties_holds` + `Models\Hold` `$guarded=[]` + `HoldFactory` + `HoldPlaced` event + `PlaceHold` action wrapping create+record in one `DB::transaction`). It matches the template *structurally* but the action/event are the domain-specific `PlaceHold`/`HoldPlaced`, not the generic `Create*`/`*Created`. Promoting would require generalizing the template's naming clause ("a `Create*`-shaped action" → "a create-via-domain-action"). Deferred as a judgment call: apply if you agree the naming variance is cosmetic (→ promote to `rules.md`, 3/3), else leave it for the next literal `Create*` spine.
- **Promote the Filament PHPStan-max trap hypothesis straight to a rule?** By raw count it is ≥3-distinct-change dense (the sub-traps span `parties-compliance`/`parties-holds` + all four consoles and are each already in `lessons.md`). I filed it as a *hypothesis* rather than auto-promoting because it's a consolidated grab-bag rather than one mechanism — tell me if you'd rather it be a rule now.
- **Extend the `laravel` Pint-`{@see}` rule + `architecture` boundary rule with the console `{Models, Actions, Enums}` carve-out specifics** rather than (as done here) capturing them inside the new filament kit rule. Left as-is to avoid editing two other domains' rules in the same PR; flagging the mild overlap.
- **`laravel` config-test hypothesis stays 2/3** — no new `expect(config(...))` Feature-test confirmation among the 8 changes (the console `Filament::getPanel(...)` discovery assertions are a looser cousin, not counted). Still "1 away".

## Flags — report-only

- **Demotions: none.** Every existing rule held across all 8 changes — in fact the console changes *reconfirm* the Pint-`{@see}` rule (escalated to a boundary breach), the within-module-relations rule, the localized-exception rule, the enum-`CHECK` and short-index rules, and testing traps 5/6. The `parties-membership-activation` deferred-seam `class_exists(...)->toBeFalse()` proof reads *near* the promoted absence rule but is a compatible sibling, not a contradiction (noted inline).
- **`hot.md` Open Patterns (updated 2026-06-21): all current, none stale.** The four (console closing-chain, supersession-as-side-effect, i18n five-guard kit, rule-of-three deferred verb-list) all describe the active console work area; the first and third are now lifted into `knowledge/filament/` rules, so the hot.md copies can retire naturally on the next overwrite (not edited here — hot.md is volatile).
- **`decisions/INDEX.md`: no drift.** All 21 ADRs on disk are indexed, including the six added since the last dream (06-18 → 06-21); no orphaned references.
- **`log.md`: clean.** No `blocked`/`human-needed`/FAILED episode since 2026-06-16 (the two 06-17 approval/KYC items resolved via ADR). Size 56K, well under the 200K rotation floor.

## Reviewed, nothing to do

- **`lessons.md`** — the 06-18→06-21 corrections (Larastan-max traps, `->numeric()`-float, task-prose-wrong-attribute, pao-OOM memory flag, handoff-blast-radius grep) are all already captured; no un-recorded recurring mistake. The durable cross-change ones are what this PR lifts into `knowledge/filament/` + the testing rules; the lessons stay as quick-ref.
- **`architecture` / `data-model` / `laravel` existing rules** — reconfirmed, no edit needed beyond the proposals above.
- **First-run cap** — applied set held to 4 curations (1 promotion, 1 domain w/ 2 rules, 1 confirmation); the rest is Proposed to keep this PR reviewable.

---
*Generated by the `/dreaming` skill (propose-only). Merge to accept the Applied curations; comment to request any Proposed item.*

🤖 Generated with [Claude Code](https://claude.com/claude-code)
