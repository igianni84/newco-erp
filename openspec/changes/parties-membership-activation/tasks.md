# Tasks — parties-membership-activation

Read `proposal.md`, `design.md` (L1–L9) and the spec delta `specs/party-registry/spec.md` before any task. **Standing rules per task:** the five Quality Commands green at the end (format · test_filter · test · type_check · lint); `openspec validate parties-membership-activation --strict` green; **no protected files touched** (the two guard tests under `tests/` are NOT protected — `spec/**`, `openspec/specs/**`, `CLAUDE.md`, `ralph.sh`, `.claude/**` are); **exactly ONE additive migration** (three nullable timestamps on `parties_customers` — no change to `parties_profiles` or any other table); **no new Composer dependency** (`git diff main -- composer.json composer.lock` stays empty). **Verify every framework/vendor API in `vendor/`/`app/` before writing it** (lessons.md) — in particular `DomainEventRecorder::record(string $name, string $module, ActorRole $actorRole, ?int $actorId, string $entityType, string $entityId, array $payload, ?string $correlationId = null, ?int $causationId = null): DomainEvent` (must run in an open transaction; omit the last two for a root event); `ActorContext::role()`/`actorId()`; the `ActorRole` cast; `Profile.state` is a `ProfileState` cast (NOT `status`) born `Applied`; `Customer.status` is a `CustomerStatus` cast born `Pending`; `Customer.originating_club_id` is a nullable FK born NULL; reuse `KycStatus::clears()` and `SanctionsStatus::Passed`; mirror the `ActivateProducer` transition-Action idiom and the `2026_06_17_000001` additive-migration idiom. **Every DB-touching task is verified on a real PostgreSQL 17 before it is marked done** — SQLite-green is necessary, never sufficient (`knowledge/testing/rules.md`). **Boundary & persistence law:** every reference is within Module K (no cross-module import/join/event-consume — Module A capacity + Module E `MembershipFeePaid` are deferred seams, NOT reads); the `Profile`/`Customer` models stay persistence-only (`$guarded = []`) — the **Action is the sole writer**. **i18n:** new copy goes in the existing `profile`/`customer` groups of `lang/en/parties.php` only (English baseline + per-key fallback — DEC-127); `:state` tokens are business enum values, never PII (omit acceptance-timestamp values from messages). **Guard tests (design L8):** the activation Actions/events trip two archived-change guard tests (`SupplyLifecycleChainTest` exact-set whitelist + event-non-existence loop; `ComplianceIndependenceTest` forbidden-Action list + `originating_club_id` write-scan) — **each task that introduces a tripping name narrows its guard slice in the SAME task** so the suite never goes red; before editing, `grep -rn` the introduced symbol across `tests/` to re-derive the blast radius (don't trust this list). **Scope guard (named seams — proposal boundary table):** NO Hero Package capacity gate (approval/activation ship uncapped — Module-A seam, L7), NO `Applied → WaitingList`, NO `MembershipFeePaid` listener (Module-E seam — invoke `ActivateProfile` directly), NO Club Credit, NO Hold→`suspended` / `Active → Suspended|Lapsed|Cancelled|Inactive` / Account transition, NO Customer segments, NO Filament/producer UI.

## 1. Foundations (migration, exceptions, events)

- [x] 1.1 **Additive migration — onboarding-acceptance timestamps on `parties_customers`** (design L1; spec — Customer Onboarding Activation) — one migration `database/migrations/2026_06_18_000002_add_onboarding_acceptance_to_parties_customers.php` adding three **nullable** `timestampTz` columns `email_verified_at`, `tc_accepted_at`, `privacy_accepted_at` (no default, no backfill; reversible `down()` drops the three). Add `immutable_datetime` casts + `@property ?CarbonImmutable` for each to `app/Modules/Parties/Models/Customer.php`. No PG-only CHECK (plain timestamps). No change to `parties_profiles`.
  - Acceptance: migrate fresh on SQLite **and PG17**; a Customer row round-trips the three typed timestamp casts (set + re-read); `down()` drops the columns and re-migrates clean; `ModulePersistenceConventionsTest` + `ModuleBoundariesTest` + the shipped `CustomerTest`/`ComplianceSchemaTest` stay green; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/CustomerOnboardingAcceptanceTest.php` (`uses(RefreshDatabase::class)`) — `Customer::factory()->create(['email_verified_at' => now(), 'tc_accepted_at' => now(), 'privacy_accepted_at' => now()])` then re-read asserts each is a `CarbonImmutable`; a default-factory Customer has all three NULL; assert the columns exist on PG17 (`Schema::hasColumn`).

- [x] 1.2 **`IllegalProfileTransition` + `IllegalCustomerTransition` exceptions + localized copy** (design L4/L6; spec — Profile Membership Approval, Profile Activation, Customer Onboarding Activation) — `App\Modules\Parties\Exceptions\IllegalProfileTransition extends RuntimeException` with named factories `::cannotApprove(ProfileState $from)`, `::cannotReject(ProfileState $from)`, `::cannotActivate(ProfileState $from)`; `App\Modules\Parties\Exceptions\IllegalCustomerTransition extends RuntimeException` with `::cannotActivate(CustomerStatus $from)` (wrong from-state) and `::gateNotMet()` (composite onboarding gate unmet). Each resolves localized copy from the existing `profile`/`customer` groups in `lang/en/parties.php` (new keys `profile.cannot_approve`/`cannot_reject`/`cannot_activate` with `:state`; `customer.cannot_activate` with `:state`, `customer.gate_not_met` with no PII). Mirror `IllegalProducerTransition`. Preserve all existing lang groups.
  - Acceptance: each factory returns its class naming the rule + (where applicable) the `:state` business token; `gateNotMet()` names the rule with **no** acceptance-value/PII interpolation; `__('parties.profile.cannot_approve', ['state' => 'approved'])` and `__('parties.customer.gate_not_met')` resolve; a pre-existing key (e.g. `parties.producer.cannot_activate`) still resolves; quality commands green.
  - Test hint: `tests/Unit/Modules/Parties/Exceptions/MembershipTransitionExceptionsTest.php` (no DB) — assert each factory returns its class with a non-empty resolved message containing the token (and `gate_not_met` contains no value); assert the new keys resolve and a pre-existing `parties.*` key still resolves.

- [ ] 1.3 **The three activation event classes + narrow the event-existence guard** (design L9; spec — Demand-Side Activation Events) — `App\Modules\Parties\Events\{CustomerActivated,ProfileActivated,OriginatingClubLocked}` — each a `final` class with untyped `const NAME` (verbatim § 15 name), `const ENTITY_TYPE` (`'Customer'` / `'Profile'` / `'Customer'`), and a static `payload(...): array`: `CustomerActivated::payload(Customer)` → `{customer_id, status}`; `ProfileActivated::payload(Profile)` → `{profile_id, state}`; `OriginatingClubLocked::payload(Customer $c, Profile $triggering)` → `{customer_id, club_id, profile_id, locked_at}` (enum values via `->value`; `locked_at` an ISO string; PII-free). Mirror the `ProducerActivated` shape. **Then narrow `SupplyLifecycleChainTest`'s event-non-existence loop** (design L8): remove `CustomerActivated`/`ProfileActivated`/`OriginatingClubLocked` from the not-recordable list, **keep** `AccountActivated`/`ProfileApproved`/`CustomerSegmentChanged`.
  - Acceptance: each `NAME` byte-equals its § 15 name (`'CustomerActivated'`/`'ProfileActivated'`/`'OriginatingClubLocked'`); `ENTITY_TYPE` correct; `payload()` returns exactly the listed keys with no PII; `SupplyLifecycleChainTest` green with the three now-shipped events admitted and `ProfileApproved`/`AccountActivated`/`CustomerSegmentChanged` still asserted-absent; quality commands green (no DB → no PG run).
  - Test hint: `tests/Unit/Modules/Parties/Events/ActivationEventsTest.php` — assert each `NAME`/`ENTITY_TYPE`; build a `Customer`+`Profile`, assert `OriginatingClubLocked::payload($c,$p)` keys are exactly `{customer_id, club_id, profile_id, locked_at}` and contain no PII; same for the other two. First `grep -rn 'ProfileActivated\|CustomerActivated\|OriginatingClubLocked' tests/` to confirm `SupplyLifecycleChainTest` is the only existence-guard touched.

## 2. Demand-side activation Actions

- [ ] 2.1 **`ApproveProfile` + `DeclineProfile` + the Originating-Club one-shot lock** (design L2/L3/L4/L7/L8; spec — Profile Membership Approval, Demand-Side Activation Events) — `app/Modules/Parties/Actions/ApproveProfile.php` (`handle(int $profileId): Profile`) and `DeclineProfile.php` (`handle(int $profileId): Profile`), each inject `DomainEventRecorder` + `ActorContext`; one `DB::transaction`: `lockForUpdate` re-read the Profile; guard `state === Applied` else `IllegalProfileTransition::cannotApprove`/`cannotReject`; `update(['state' => Approved|Rejected])`. **Approve only:** then `lockForUpdate` re-read the **Customer**; if `originating_club_id === null`, `update(['originating_club_id' => $profile->club_id])` and `record(OriginatingClubLocked::NAME, …, entityType: 'Customer', entityId: (string) $customer->id, payload: OriginatingClubLocked::payload($customer, $profile))` in the same tx (root event). **Neither records a Profile event** (audit-only — L2). Capacity gate = documented Module-A seam in the `ApproveProfile` docblock (L7). **Then narrow the guards** (L8): add `ApproveProfile`+`DeclineProfile` to `SupplyLifecycleChainTest`'s exact-set whitelist; remove them from `ComplianceIndependenceTest`'s forbidden-Action list (keep `LockOriginatingClub`/`SetOriginatingClub` etc.); update its `originating_club_id` write-scan to admit `ApproveProfile`'s conditional write.
  - Acceptance: spec scenarios "Approve a first-ever applied Profile locks the Originating Club", "A second Club's approval does not re-lock or re-fire", "Decline an applied Profile is terminal and event-silent", "Illegal approve/decline is rejected" pass; first-ever approve → `state=Approved`, `Customer.originating_club_id` set, exactly **1** `OriginatingClubLocked` (`entity_type=Customer`, payload keys, `causation_id` null, `correlation_id=event_id`), **0** `ProfileApproved`; second-Club approve → `state=Approved`, **0** events, `originating_club_id` unchanged; decline → `state=Rejected`, **0** events, re-application inserts a new `Applied` row; illegal from-state → throws, unchanged, 0 events; both guard tests green; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/ProfileMembershipApprovalTest.php` (`uses(RefreshDatabase::class)`) — `app(ApproveProfile::class)->handle($p1->id)` on a NULL-OC Customer asserts the lock + 1 `OriginatingClubLocked` (`where('name','OriginatingClubLocked')->count()===1`, `entity_type==='Customer'`, payload `toEqualCanonicalizing(['customer_id','club_id','profile_id','locked_at'])`); a second Profile on another Club → count stays 1, `originating_club_id` unchanged; `DeclineProfile` → `state=Rejected`, `DomainEvent::count()` unchanged, then `CreateProfile` for the same pair succeeds; `expect(fn()=>app(ApproveProfile::class)->handle($activeProfile->id))->toThrow(IllegalProfileTransition::class)`.

- [ ] 2.2 **`ActivateProfile` Action** (design L4/L5/L7/L8; spec — Profile Activation, Demand-Side Activation Events) — `app/Modules/Parties/Actions/ActivateProfile.php` (`handle(int $profileId): Profile`) — inject recorder+actor; one `DB::transaction`: `lockForUpdate` re-read; guard `state === Approved` else `IllegalProfileTransition::cannotActivate`; `update(['state' => Active])`; `record(ProfileActivated::NAME, …, entityType: 'Profile', entityId: (string) $profile->id, payload: ProfileActivated::payload($profile))` (root). The `MembershipFeePaid` listener + the §13 capacity gate are documented Module-E / Module-A seams in the docblock (L5/L7) — the Action is invoked directly. **Then narrow the guards** (L8): add `ActivateProfile` to the exact-set whitelist; remove it from the forbidden-Action list.
  - Acceptance: spec scenarios "Activate an approved Profile", "Illegal activation is rejected", "The membership-fee trigger is a deferred seam" pass; `Approved → Active` → `state=Active`, exactly **1** `ProfileActivated` (`entity_type=Profile`, payload `{profile_id, state}`, root); illegal from-state (e.g. `Applied` or `Active`) → throws, unchanged, 0 events; no `MembershipFeePaid`/Module-E class exists in the diff; both guard tests green; quality commands green; **verified on PG17**.
  - Test hint: extend `ProfileMembershipApprovalTest` or add `tests/Feature/Modules/Parties/ProfileActivationTest.php` — approve→activate a Profile asserts `state=Active` + 1 `ProfileActivated`; `expect(fn()=>app(ActivateProfile::class)->handle($appliedProfile->id))->toThrow(IllegalProfileTransition::class)` with 0 `ProfileActivated`.

- [ ] 2.3 **`ActivateCustomer` Action + the composite onboarding gate** (design L6/L8; spec — Customer Onboarding Activation, Demand-Side Activation Events) — `app/Modules/Parties/Actions/ActivateCustomer.php` (`handle(int $customerId): Customer`) — inject recorder+actor; one `DB::transaction`: `lockForUpdate` re-read; guard `status === Pending` else `IllegalCustomerTransition::cannotActivate`; evaluate the composite gate — `email_verified_at !== null && tc_accepted_at !== null && privacy_accepted_at !== null && sanctions_status === SanctionsStatus::Passed && (! kyc_required || kyc_status?->clears() !== false)` (NULL `kyc_status` treated as cleared — DEC-071) — else `IllegalCustomerTransition::gateNotMet()`; `update(['status' => Active])`; `record(CustomerActivated::NAME, …, entityType: 'Customer', entityId: (string) $customer->id, payload: CustomerActivated::payload($customer))` (root). **Does NOT touch the Account** and is **not** invoked from any sanctions/KYC Action (L6). **Then narrow the guards** (L8): add `ActivateCustomer` to the exact-set whitelist; remove it from the forbidden-Action list.
  - Acceptance: spec scenarios "Activate a Customer once all gates clear", "Any unmet gate blocks activation", "Activation is explicit, not auto-driven by another FSM", "Illegal from-state is rejected" pass; all-gates-met → `status=Active`, **1** `CustomerActivated` (`entity_type=Customer`, payload `{customer_id, status}`), Account `status` unchanged; each sub-gate unmet (parametrized: email/TC/privacy NULL, sanctions ≠ passed, `kyc_required` + `kyc_status` pending/rejected) → `gateNotMet`, stays `Pending`, 0 events; recording a sanctions verdict alone does not flip `status`; wrong from-state → `cannotActivate`; both guard tests green; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/CustomerOnboardingActivationTest.php` (`uses(RefreshDatabase::class)`) — a fully-gated Customer → `app(ActivateCustomer::class)->handle($c->id)` asserts `status=Active` + 1 `CustomerActivated` + the Account row `status` unchanged; a `dataset` of each single unmet gate asserts `toThrow(IllegalCustomerTransition::class)` + `status` stays `Pending` + `DomainEvent::where('name','CustomerActivated')->count()===0`; `RecordCustomerScreening`(passed) alone leaves `status=Pending`.

## 3. Chain, docs, cross-engine close

- [ ] 3.1 **Full activation chain + docs + cross-engine PG17 close** (design L2–L9; spec — all ADDED + both MODIFIED requirements) — one feature test driving the whole slice on one Customer: create (Pending, OC NULL) → set the three acceptance timestamps + `RecordCustomerScreening`(passed) (+ KYC cleared) → `ActivateCustomer` (assert `CustomerActivated`) → `CreateProfile` (Applied) → `ApproveProfile` (assert `OriginatingClubLocked` once + OC set) → a second Club's `CreateProfile`+`ApproveProfile` (assert no second lock) → `ActivateProfile` (assert `ProfileActivated`); assert the **exact** `domain_events` name-set across the chain is `{CustomerCreated, ProfileCreated×2, CustomerOnboardingScreeningPassed, CustomerActivated, OriginatingClubLocked, ProfileActivated}` with **no** `ProfileApproved`/`ProfileRejected`/`MembershipApprovedByProducer`. **Docs:** extend `CONTEXT.md` with the resolved demand-side activation terms (approve/decline = audit-only; the OC one-shot lock; `ProfileActivated` on fee-paid/free-club; the `CustomerActivated` composite gate) + a Parties activation contract note (the three event payloads + the deferred seams: §13 capacity → Module A, `MembershipFeePaid` listener → Module E, Hold→suspended → `parties-membership-suspension`, segments, producer/console UI). Then run the **entire** Parties suite + the arch tests on **PostgreSQL 17**, and audit the other five guard files (`SpineCreationChainTest`, `ComplianceChainTest`, `CustomerTest`, `HoldChainTest`, `HoldRegistryTest`) stay green unamended.
  - Acceptance: the chain records exactly the expected name-set (no invented event); the OC lock fires once across two approvals; `ActivateCustomer`'s gate + `ActivateProfile`/`ApproveProfile` from-state guards hold; `CONTEXT.md` carries the new terms with verbatim spec anchors (§ 4.1/§ 4.2.1/§ 6.1/§ 13/§ 15) + no canonical-term drift; the **entire** Parties test set is green on SQLite **and** local PostgreSQL 17 (docker `postgres:17` per `knowledge/testing/rules.md`); record the PG run in `progress.md`; `openspec validate parties-membership-activation --strict` green.
  - Test hint: `tests/Feature/Modules/Parties/MembershipActivationChainTest.php` — assert `DomainEvent::pluck('name')` over the chain `toEqualCanonicalizing([...])` and `->where('name','ProfileApproved')->count()===0`; assert `isClear()`/state flips along the chain; run `php -d memory_limit=512M vendor/bin/pest` against PG17 and confirm green; docs verified by a terminology re-read against the spec.
