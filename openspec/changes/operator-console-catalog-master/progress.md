# Progress — operator-console-catalog-master

## Codebase Patterns
(consolidated reusable patterns — read first each iteration)

- **Running the FULL test suite:** `php artisan test` (and `composer test`) blow the box's default 128M PHP memory limit during `laravel/pao` result aggregation over the ~950-test suite — the `-d memory_limit` flag does NOT reach the pao worker. Run the full suite as **`php -d memory_limit=-1 vendor/bin/pest`** and PHPStan as **`php -d memory_limit=-1 vendor/bin/phpstan analyse`**. A *filtered* run (`php artisan test --filter=Foo`) stays under 128M and is fine for step 2.
- **Asserting Filament panel discovery (no resource needed):** `Filament::getPanel('admin')` exposes public getters `getResourceNamespaces()/getResourceDirectories()`, `getPageNamespaces()/getPageDirectories()`, `getWidgetNamespaces()/getWidgetDirectories()` — each returns the verbatim `for:` / `in:` args passed to `discoverResources/Pages/Widgets`. Use these to assert the discovery repoint directly (`->toContain('App\Modules\OperatorPanel\Filament\Resources')`) without scaffolding a real resource. Filament's `discoverComponents` silently no-ops on a missing directory (`HasComponents.php:496`), so an empty `.gitkeep` skeleton is a valid discovery target.
- **Operator-panel test auth:** authenticate with `actingAs(Operator::factory()->create(), 'operator')` (the `operator` session guard); `Operator` = `App\Modules\OperatorPanel\Models\Operator`. `get('/admin')->assertOk()` proves the panel boots + `canAccessPanel`.
- **Repo-local PHPStan custom rule (type-aware arch test):** the stack ships phpstan as a phar BUT `PHPStan\Testing\RuleTestCase` is autoloadable (phpstan's `bootstrap.php` is composer-`files`-loaded → `class_exists` true). Pattern: (1) rule class under `tests/PHPStan/Rules/` (namespace `Tests\…`, already dev-autoloaded — **no composer.json change**, so the "empty composer diff" rule holds); implement `Rule<TNode>` with `getNodeType()` + `processNode()`; resolve receiver types via `$scope->getType($node->var)` and `(new ObjectType(Model::class))->isSuperTypeOf($t)->yes()`; build errors with `RuleErrorBuilder::message()->identifier('x.y')->line($node->getStartLine())->build()`. (2) Register in `phpstan.neon` under top-level `services:` with `tags: [phpstan.rules.rule]` — this wires it into `type_check`. (3) **Scope a rule to a directory via a constructor `scopedPathNeedles` arg** (checked against `$scope->getFile()`), NOT a hardcoded path: production registration passes the real needle, the RuleTestCase passes the fixture's needle — same rule, both testable. (4) Test with a class extending `RuleTestCase` under `tests/Architecture/` (Pest binds the Laravel `TestCase` only to `Feature/`, so a plain PHPUnit class elsewhere runs clean); `gatherAnalyserErrors([$fixture])` is public and returns `Error[]` with `getLine()/getIdentifier()`. (5) **`excludePaths` the fixture dir** in phpstan.neon so its deliberate violations don't red the main run. (6) Verify API surface in `vendor/` by reflection before writing — `$class_exists`/`ReflectionClass` dump (lessons.md). Two-call shapes cover the writes: `MethodCall` (`$model->save()`) on a Model-typed `->var`; `StaticCall` (`Model::create()`) on a Name resolved via `$scope->resolveName()`.
- **Marker-driven fixture line assertions:** when a test asserts which fixture lines a rule flags, derive the expected lines from a `// flag`-style trailing marker scanned out of the fixture source (`file()` + `str_contains`), not hardcoded numbers — Pint's `fully_qualified_strict_types` can silently add a `use` import (e.g. from a `{@see FQCN}` docblock) and shift every line. Keep the literal marker token OUT of the fixture's own docblock/prose or the scan self-matches.
- **Module-boundary carve-out (`tests/Architecture/ModuleBoundariesTest.php`):** the privacy law forbids `App\Modules\{M}` importing any other module except the namespaces fed to `->ignoring()`, now built by the top-level helper `moduleBoundaryAllowedImports(Module $source)` (single source of truth for the law + its guard test). `->ignoring()` matches by namespace PREFIX (`str_starts_with`), so listing `…\Models` permits `…\Models\Foo`. To permit a new cross-module import for ONE source module, add the prefix inside that helper under a `$source === Module::X` guard — NEVER unconditionally (that widens it to all nine). Pin the scoping with a logic-level guard test over the helper's output (positive: the source's list contains the prefix; negative: a lateral module's list does NOT and never the whole-module namespace) — the permanent analog of "plant a violation". Live red-proof is transient: drop a temp class importing the target under `app/Modules/{M}/…` (NOT under a discovered Filament dir), run the boundary test, observe red→green, delete. **Assert membership via `in_array($x, $list, true)` + `->toBeTrue()/->toBeFalse()`, NOT a chained `expect($list)->toContain()->toContain()->not->toContain()` — multi-method Pest expectation chains degrade to `mixed`/nullable under the larastan Pest extension and red `type_check`; `toContain` is only phpstan-clean as a single modifier (e.g. `->and($s)->toContain('x')`).**
- **Console cross-module surface = exactly {Models, Actions}:** the operator console reaches each operated module through only two sub-namespaces — `Models\*` (read-bind Filament resources, read-only) and `Actions\*` (`app(<Action>)->handle()` write-through). To keep it that tight for the lifecycle tasks (3.1–5.2): **catch domain rejections via base types** (`\Throwable` / `\DomainException` / `Illuminate\Validation\ValidationException`, then `$e->getMessage()`), do NOT `use App\Modules\Catalog\Exceptions\…`; and **render enum casts via their instance** (`$record->lifecycle_state->…`), do NOT `use App\Modules\Catalog\Enums\…`. Following this, no later console task needs to re-amend the boundary carve-out. If a task genuinely must reference a third sub-namespace, extend the helper (guarded) + the guard test together.
- **Filament 5 write-through CREATE page (verified API — the console's WRITE half, task 3.1; reuse for any console create):** a Create page extends `Filament\Resources\Pages\CreateRecord` with `protected static string $resource`; override `protected function handleRecordCreation(array $data): Model` to `return app(<DomainAction>::class)->handle(...)` — NEVER the default `new Model($data); $record->save()`. The form lives on the **Resource** as `public static function form(Schema $schema): Schema` (`->components([TextInput/Select/Textarea::make(...)])`, `Filament\Forms\Components\*`); the Create page inherits it. **Register the page** in `getPages()` (`'create' => Pages\Create<X>::route('/create')`) — this flips `canCreate()` true. **Coercing `$data` (PHPStan-strict):** Filament types it `array<string, mixed>`; `(int)`/`strval()`/`intval()` on a `mixed` are ALL flagged here (`cast.int`, and the strict stubs reject `mixed` for `strval`/`intval`) — instead **narrow with an `is_string()`/`is_numeric()` guard that throws** (`InvalidArgumentException`) on the impossible mismatch, then PHPStan sees the concrete type and `(int) $numeric` is clean. **Map a domain rejection to a form field error:** catch the rejection's base type (the Catalog dedup is `DuplicateProductMasterIdentity extends RuntimeException`, so `catch (\RuntimeException $e)` — honours the no-`Catalog\Exceptions`-import convention) and re-throw `ValidationException::withMessages(['data.<field>' => $e->getMessage()])` — the message is already localized by the action (catalog.php), and `assertHasFormErrors(['<field>'])` prepends the form state-path `data`, so the key MUST be `data.<field>`. **Transactions:** `CreateRecord::create()` wraps in `beginDatabaseTransaction()`; the action's own `DB::transaction` nests; a non-`Halt` `Throwable` from `handleRecordCreation` rolls back + re-throws (a dedup collision persists nothing — 0 rows/events). `DomainEventRecorder` requires `transactionLevel ≥ 1` and writes the event row regardless, so the event is assertable — use **`DatabaseMigrations`** (not RefreshDatabase) for write-action console tests to get real commits + `DB::afterCommit` delivery. **Reach-the-page affordance MUST be a plain link, never `CreateAction::make()`:** a resource-page `CreateAction` is configured as an inline-modal form (`ListRecords.php:94`) whose `action()` does `new Model; fill(); save()` — bypassing the domain action (and invisible to the task-1.2 rule, since it's vendor code). Use `Filament\Actions\Action::make('create')->url(<Resource>::getUrl('create'))` in `getHeaderActions()`; assert it with `assertActionHasUrl('create', …)` (a CreateAction has no URL). **Test** with `Livewire::test(Create<X>::class)->fillForm([...])->call('create')->assertHasNoFormErrors()` (or `->assertHasFormErrors(['name'])`), then assert domain effects (state + `DomainEvent::…->sole()` + `actor_role`/`actor_id`); auth via `actingAs(Operator::factory()->create(), 'operator')`.
- **Filament 5 read-only resource (verified API — the console's READ half, task 2.1):** a `Resource` sets `protected static ?string $model`; `public static function table(Table $table): Table` with `TextColumn::make()->label()->getStateUsing(fn (Model $record) => …)`; `public static function infolist(Schema $schema): Schema` with `->components([TextEntry::make()…])` (`Filament\Schemas\Schema` has both `->components()` and the alias `->schema()`); `getPages(): array` → `['index' => Pages\ListX::route('/'), 'view' => Pages\ViewX::route('/{record}')]`, page classes under `…\<Resource>\Pages\` extending `Filament\Resources\Pages\{ListRecords,ViewRecord}`. Both table columns AND infolist entries get `getStateUsing`/`formatStateUsing` from `Filament\Support\Concerns\HasCellState`. **Read-only = add nothing:** `getHeaderActions()` defaults to `getActions()` = `[]` (no Create/Edit button unless added); a table with no `recordActions/toolbarActions/bulkActions` has no row/bulk action; omitting create/edit pages from `getPages()` makes `canCreate()` false. Labels via static `getModelLabel()/getPluralModelLabel()` returning `(string) __('operator_console.…')` (NOT the `$modelLabel` static-property form — it can't call `__()` at class-load). Filament authz with no policy + non-strict mode = allowed (`get_authorization_response`), so an authed operator isn't 403'd. **Test** with `Livewire::test(ListX::class)` + macros `assertCanSeeTableRecords([$m])` / `assertTableActionDoesNotExist('delete')` / `assertTableBulkActionDoesNotExist('delete')` (registered by each Filament package's ServiceProvider), and `Livewire::test(ViewX::class, ['record' => $m->getKey()])->assertSee(…)`; auth via `actingAs(Operator::factory()->create(), 'operator')`.

---

## [2026-06-20 08:05] — 1.1 Repoint Filament discovery into the OperatorPanel module
- **What:** repointed `AdminPanelProvider::panel()` `discoverResources/discoverPages/discoverWidgets` from the shell's default `app_path('Filament/...')` (`App\Filament\*`) to `app_path('Modules/OperatorPanel/Filament/...')` with the matching `App\Modules\OperatorPanel\Filament\{Resources,Pages,Widgets}` namespaces (ADR 2026-06-19). Created the discovery skeleton `Filament/Resources/Catalog/`, `Filament/Pages/`, `Filament/Widgets/` (each a `.gitkeep`). No resource yet (1.x+).
- **Files changed:** `app/Modules/OperatorPanel/Providers/AdminPanelProvider.php` (modified); `app/Modules/OperatorPanel/Filament/{Resources/Catalog,Pages,Widgets}/.gitkeep` (new); `tests/Feature/Modules/OperatorPanel/PanelDiscoveryTest.php` (new, 4 tests / 10 assertions).
- **Quality loop:** green — format ✓; filtered ✓ (4/4); full suite ✓ **953/953** (4698 assertions, SQLite); phpstan ✓ (0 errors); pint --test ✓. `openspec validate --strict` ✓. composer.json/lock diff vs main empty; no migrations added; no protected files touched.
- **Acceptance walked:** panel boots at `/admin` (dashboard `assertOk` for an authed operator) ✓; discovery namespaces+directories repointed to the module (asserted via the panel getters, incl. a negative assertion the old `App\Filament\*` is gone) ✓; the `operator-auth-foundation` auth tests (login, password-reset routes, no-registration, opt-in TOTP, default guard) stay green ✓.
- **PG17:** not run this task — 1.1 touches no DB-writing action (panel-config + discovery only; the new test's lone DB use is `Operator::factory()` on the shared `operators` table). Cross-engine PG17 runs land on the DB-action tasks (2.1+) and the 6.2 close, per tasks.md.
- **Learnings for future iterations:**
  - The two patterns above (full-suite memory flag; panel-discovery getters) are now in Codebase Patterns — read them before running tests or asserting panel config.
  - Discovery dirs may be empty: Filament no-ops on a missing dir, so resources can land in `Filament/Resources/Catalog/` incrementally without touching `AdminPanelProvider` again.
  - `AdminPanelProvider` is app code (design L3) — editing it is in-bounds; `pages([Dashboard::class])` + `widgets([AccountWidget, FilamentInfoWidget])` are explicit framework registrations and were left intact.
---

## [2026-06-20 08:26] — 1.2 Architecture test — no Eloquent write in App\Modules\OperatorPanel
- **What:** the operator-console write-discipline guard (ADR 2026-06-19; design L4), implemented as the **primary** option — a **PHPStan custom rule** (type-aware, AST-level), not the Pest token-scan fallback. The rule flags a `MethodCall`/`StaticCall` whose method ∈ {save, saveQuietly, update, updateQuietly, delete, forceDelete, create, insert, fill, setAttribute} on an **Eloquent-`Model` receiver**, in files matching a constructor-injected path needle. Registered in `phpstan.neon` → runs on every `type_check`.
- **Scope decision (kept the clean tree green):** scoped to the **console surface** `app/Modules/OperatorPanel/Filament/`, NOT the whole module. `Operator` (`extends Authenticatable`, which IS a `Model`) legitimately self-writes via `$this->save()` in `Models/Operator.php:100,124` (operator-auth-foundation) — a blanket module-wide ban would red the clean tree. The design's own fallback already scopes to `Filament/**`, and per L3 all console code lives under `Filament/`, so the auth principal's own writes are correctly out of scope.
- **Files changed:** `tests/PHPStan/Rules/NoEloquentWriteInOperatorPanelRule.php` (new, the rule); `tests/Architecture/NoEloquentWriteInOperatorPanelRuleTest.php` (new, `RuleTestCase`, 1 test / 12 assertions); `tests/Architecture/Fixtures/OperatorPanelEloquentWriteFixture.php` (new, planted writes + reads, `// flag` markers); `phpstan.neon` (modified — `services:` registration scoped to `Modules/OperatorPanel/Filament/`; `excludePaths` the fixture dir).
- **Quality loop:** green — format ✓; filtered ✓ (1/1, 12 assertions); full suite ✓ **954/954** (4710 assertions, SQLite); phpstan ✓ (0 errors, rule loaded); pint --test ✓. `openspec validate --strict` ✓. composer diff vs main empty; no migrations; no protected files touched.
- **Acceptance walked:** **red→green proven two ways.** (1) Automated/permanent: the `RuleTestCase` fixture asserts exactly the 10 planted writes are flagged (identifier `operatorPanel.noEloquentWrite`) and the 3 reads are not. (2) Production wiring: planted `$master->update([...])` **and** `ProductMaster::create(...)` in a real `app/Modules/OperatorPanel/Filament/Resources/Catalog/__RedProof.php` → `type_check` **failed** (both flagged); deleting it → `type_check` **0 errors**. Rule wired into `type_check` ✓; reads (`$master->name`, `Model::query()`, `refresh()`) not flagged ✓.
- **PG17:** N/A — 1.2's test is pure static analysis (phpstan over a fixture), no DB touched. Cross-engine PG17 runs land on the DB-action tasks (2.1+) and the 6.2 close, per tasks.md.
- **Learnings for future iterations:**
  - The PHPStan-custom-rule pattern + the marker-driven fixture-line pattern are now in Codebase Patterns — read them before writing the next arch rule or fixture-line assertion.
  - **Next (1.3):** amend the import-boundary test (`tests/Architecture/ModuleBoundariesTest.php`) so `App\Modules\OperatorPanel` MAY import `App\Modules\*\Models` (read-binding carve-out), scoped to OperatorPanel only; a planted lateral import (e.g. Catalog → `Parties\Models`) must still fail. That test iterates `Module::cases()` with `->not->toUse(...)->ignoring(...)` — the carve-out is an extra `->ignoring()` entry on the OperatorPanel source only. Read what it asserts before editing.
  - The rule bans writes on **any** Eloquent `Model` receiver in scope; it does NOT special-case foreign vs own models (the `Filament/` path scope already excludes the `Operator` auth model under `Models/`). If a future console legitimately needs to write its *own* OperatorPanel model from a Filament class, revisit the scope (own-namespace carve-out) rather than widening the path.
---

## [2026-06-20 08:45] — 1.3 Amend the import-boundary test for the OperatorPanel read exception
- **What:** amended `tests/Architecture/ModuleBoundariesTest.php` to encode the operator-console carve-out (ADR 2026-06-19; design L1/L2). Extracted the per-source `->ignoring()` allow-list into a top-level helper `moduleBoundaryAllowedImports(Module $source)` (single source of truth). Baseline unchanged (every other module's `Contracts\*` + `Events\*`); **for `Module::OperatorPanel` only** it also permits each operated module's `Models\*` (read-bind, L1) **and** `Actions\*` (write-through `app(<Action>)->handle()`, L2). Added a guard test pinning the carve-out OperatorPanel-only.
- **Scope decision — Models AND Actions, not Models-only:** the task headline + design L29 name `Models\*`, but design L2's own code (`app(CreateProductMaster::class)->handle(...)`, the 7 Catalog lifecycle actions) makes the console reference `Catalog\Actions\*`, which trips this very test. **Empirically confirmed** (red-proof): a temp OperatorPanel class importing `Catalog\Models\ProductMaster` + `Catalog\Actions\CreateProductMaster` FAILED the un-amended test ("OperatorPanel not to use Catalog") — so pest-arch detects the `app(...::class)` action reference, and a Models-only carve-out would break task 3.1 here. Carving both (and nothing else — see the {Models, Actions} pattern) unblocks the whole change with no later re-amendment. The carve-out stays narrow: the guard test asserts OperatorPanel's allow-list never contains a whole-module namespace, so console code still can't reach `Catalog\Lifecycle`/internals; the read-only half is the task-1.2 PHPStan rule's job, not this import test's.
- **Files changed:** `tests/Architecture/ModuleBoundariesTest.php` (modified — helper + carve-out + guard test + updated header/docblock comments). Net: 2 boundary tests → 3.
- **Quality loop:** green — format ✓; filtered ✓ (3/3, 180 assertions); full suite ✓ **955/955** (4738 assertions, SQLite); phpstan ✓ (0 errors); pint --test ✓. `openspec validate --strict` ✓. composer.json/lock diff vs main empty; no migrations; only the non-protected `tests/` file touched.
- **Red→green→lateral proven (3 live runs, all temp files deleted):** (1) RED — temp `App\Modules\OperatorPanel\__BoundaryRedProof` importing `Catalog\Models` + `Catalog\Actions` failed the un-amended test. (2) GREEN — same file passed after the carve-out. (3) LATERAL FAIL — temp `App\Modules\Catalog\__LateralRedProof` importing `Parties\Models\Customer` still failed ("Catalog not to use Parties"), proving the carve-out is OperatorPanel-only. Clean tree after deletion: boundary test 3/3.
- **Acceptance walked:** OperatorPanel→`Catalog\Models\ProductMaster` (and `Catalog\Actions\*`) passes the boundary test ✓; a planted lateral Catalog→`Parties\Models\Customer` still fails ✓; quality commands green ✓.
- **PG17:** N/A — pure Pest arch test (static namespace analysis, no DB). Cross-engine PG17 runs land on the DB-action tasks (2.1+) and the 6.2 close.
- **gotcha (→ lessons.md):** PHPStan red on a multi-method Pest chain `expect($list)->toContain()->toContain()->not->toContain()` — the chain degrades to `mixed`/nullable under the larastan Pest extension. Fixed with `in_array(..., true)` + `->toBeTrue()/->toBeFalse()`. `toContain` is phpstan-clean only as a single modifier.
- **Learnings for future iterations:**
  - The boundary carve-out mechanics + the {Models, Actions} surface convention are now in Codebase Patterns — **read them before 3.1–5.2**: catch domain exceptions via base types and render enums via instances so no later task re-amends the carve-out.
  - **Next (2.1):** build `ProductMasterResource` (read-projection) + List + View under `app/Modules/OperatorPanel/Filament/Resources/Catalog/`; `$model = \App\Modules\Catalog\Models\ProductMaster::class`; producer column via `\App\Modules\Catalog\Models\ProducerState`; no Edit/Delete default action. **First PG17 task** — seed via `CreateProductMaster` + a `ProducerState` row, run the test on docker `postgres:17` and record it. The 1.2 arch rule + this 1.3 carve-out now both guard it.
---

## [2026-06-20 09:12] — 2.1 ProductMasterResource (read-projection) + List + View
- **What:** the operator console's FIRST Filament resource — a READ-ONLY surface over Catalog `ProductMaster` (design L1/L10; ADR 2026-06-19). `ProductMasterResource` (`$model = \App\Modules\Catalog\Models\ProductMaster`) with a List page (table: name [searchable/sortable], product_type, lifecycle_state [badge], producer, version) and a View page (infolist: the neutral core + the WINE attribute set appellation/region/winery_story). The producer column/entry resolves status via Catalog's OWN `ProducerState` projection (read-only, never Module K). NO create/edit/delete default action; `getPages()` = {index, view} only. Seeded the `operator_console` i18n group (EN + IT).
- **Files changed:** `app/Modules/OperatorPanel/Filament/Resources/Catalog/ProductMasterResource.php` (new); `.../ProductMasterResource/Pages/ListProductMasters.php` + `ViewProductMaster.php` (new); `lang/en/operator_console.php` + `lang/it/operator_console.php` (new); `tests/Feature/Modules/OperatorPanel/Catalog/ProductMasterResourceTest.php` (new, 5 tests / 18 assertions). `AdminPanelProvider` untouched (discovery already repointed in 1.1; the resource lands under the discovered `Resources/Catalog/`).
- **Quality loop:** green — format ✓; filtered ✓ (5/5, 18 assertions); full suite ✓ **960/960** (4756 assertions, SQLite); phpstan ✓ (0 errors); pint --test ✓. `openspec validate --strict` ✓. composer.json/lock diff vs main empty; no migrations; no protected files touched (only app code + lang + tests).
- **PG17 ✓ (first PG17 task):** local box has no native psql and the Docker daemon was down — started Docker Desktop (`open -a Docker`, up ~4s), ran docker `postgres:17`, executed `tests/Feature/Modules/OperatorPanel` on PG17 = **40/40, 110 assertions, exit 0** (`DB_CONNECTION=pgsql … php -d memory_limit=512M vendor/bin/pest`); container torn down. The producer-column enum-cast read + the `ProducerState` status CHECK/cast are all clean on PG.
- **Acceptance walked:** authed operator sees the paginated Master list with lifecycle_state ✓; producer column resolves via the projection (active row → "#id · active"; no row → localized "Not projected") ✓; view renders the wine attrs ✓; no Edit/Delete action (`getPages`={index,view}; `assertTableActionDoesNotExist` edit/delete; `assertTableBulkActionDoesNotExist` delete) ✓; arch (1.2) + boundary (1.3) tests green in the full suite ✓; quality commands green ✓; PG17 ✓.
- **Learnings for future iterations:**
  - **`EloquentBuilder::value('enum_col')` applies the cast** (hydrates → returns the enum, not a raw string) — caught by the test (producer showed "Not projected" for an active row). Render module enums via the cast instance (`->first()?->status->value`), consistent with the no-`Catalog\Enums`-import convention (→ lessons.md).
  - The verified Filament-5 **read-only resource skeleton** is now a Codebase Pattern (top of this file) — read it before 3.x–5.x; it pins the table/infolist/getPages/header-action/authz/test shape so those tasks don't re-derive the API.
  - **Next (3.1):** the Create page → `app(CreateProductMaster::class)->handle(name, producerId, appellation, region, wineryStory)` via `handleRecordCreation(array $data): Model`; producer **select** sourced from `ProducerState`; map the BR-Identity-1 dedup domain rejection to a **form field error** (catch via base exception type / `getMessage()`, NOT `use Catalog\Exceptions`). First console WRITE — drive through the action (never `$model->save()`); assert the `draft` Master + exactly 1 `ProductMasterCreated` carrying `actor_role: newco_ops` + `actor_id` (ActorContext via the operator guard). Re-confirm Filament 5 `handleRecordCreation`/`Filament\Actions\Action` signatures in `vendor/` first.
---

## [2026-06-20 09:38] — 3.1 Create page → CreateProductMaster, with dedup + producer picker
- **What:** the operator console's FIRST WRITE surface — a Filament write-through Create page for Product Master (design L2/L6/L8; ADR 2026-06-19). `Pages\CreateProductMaster::handleRecordCreation(array $data): Model` routes the form into `app(CreateProductMaster::class)->handle(name, producerId, appellation, region, wineryStory)` and returns the new `ProductMaster` — Filament's default `new Model; $record->save()` fully overridden (no `$model->save()`; the task-1.2 PHPStan rule stays green). The form (added to the Resource as `form(Schema)`) collects name + a producer **select sourced from `ProducerState`** (Catalog's own projection, never Module K) + appellation/region + an optional translatable winery story (EN baseline → `TranslatableText::of(['en' => …])`). The BR-Identity-1 dedup rejection (`DuplicateProductMasterIdentity extends RuntimeException`) is caught via `\RuntimeException` and re-raised as a `ValidationException` on `data.name`, so a duplicate is a **form field error**, not a 500. The actor envelope (`actor_role: newco_ops` + operator id) is resolved by the action from the `operator` guard — the console builds none. List header gains a plain `Action::make('create')->url(getUrl('create'))` LINK (never `CreateAction`, whose inline-modal `$record->save()` bypasses the action).
- **Files changed:** `app/Modules/OperatorPanel/Filament/Resources/Catalog/ProductMasterResource/Pages/CreateProductMaster.php` (new — the write-through page); `ProductMasterResource.php` (added `form()` + `producerOptions()` helper + `'create'` page in `getPages()`); `.../Pages/ListProductMasters.php` (added the safe header create link); `lang/{en,it}/operator_console.php` (added `fields.name/producer/winery_story_help` + `actions.create`); `tests/Feature/Modules/OperatorPanel/Catalog/ProductMasterCreateConsoleTest.php` (new, 3 tests / 24 assertions); `tests/Feature/Modules/OperatorPanel/Catalog/ProductMasterResourceTest.php` (updated the 2.1 `getPages()` assertion to `{index, create, view}`).
- **Quality loop:** green — format ✓; filtered ✓ (3/3, 24 assertions); full suite ✓ **963/963** (4780 assertions, SQLite, +3 vs 2.1); phpstan ✓ (0 errors); pint --test ✓. `openspec validate --strict` ✓. composer.json/lock diff vs main empty; no migrations; no protected files (only app code + lang + tests).
- **PG17 ✓ (first console WRITE):** docker `postgres:17`, `tests/Feature/Modules/OperatorPanel` = **43/43, 134 assertions, exit 0** (`DB_CONNECTION=pgsql … php -d memory_limit=512M vendor/bin/pest`); container torn down. The write-through path (domain action → `DomainEventRecorder` append inside Filament's create() transaction → dedup rejection rollback) is clean on PG.
- **Acceptance walked:** valid submit → `draft` Master + its 1:1 WINE attrs + exactly **1** `ProductMasterCreated` (`actor_role: NewcoOps`, `actor_id == operator->id`, `entity_id`) ✓; colliding (producer+name+appellation) → `assertHasFormErrors(['name'])`, **0** new Masters, **0** new events ✓; the page invokes the action, no `$model->save()` (arch rule green) ✓; create reachable via a header link (URL asserted), not an inline CreateAction ✓; quality green ✓; PG17 ✓.
- **Learnings for future iterations:**
  - The verified **Filament-5 write-through CREATE page** pattern is now a Codebase Pattern (top) — read it before 4.x/5.x (the lifecycle Actions): the action-routing, the `is_string`/`is_numeric` guard for `array<string,mixed>` form data (strict PHPStan rejects `(int)`/`strval`/`intval` on `mixed`), the `data.<field>` ValidationException key, `DatabaseMigrations` for write-action tests, and the `CreateAction`-bypass trap.
  - **Next (4.1):** Submit-for-review + Reject Filament **Actions** on the resource/view (not a page) → `app(SubmitProductMasterForReview)->handle($record)` (audit-only, `draft→reviewed`) and `app(RejectProductMasterReview)->handle($record, $notes)` (a `->form()` collecting `notes`, stays `reviewed`). These are `Filament\Actions\Action` with `->action(fn ($record) => …)` + domain-exception → danger **notification** (re-confirm `Filament\Notifications\Notification` + the Action `->action()`/`->form()`/`->requiresConfirmation()` signatures in `vendor/`). Assert `reviewed` + **0** domain events + an `AuditRecord` carrying `actor_role: newco_ops`. **PG17 task.**
---
