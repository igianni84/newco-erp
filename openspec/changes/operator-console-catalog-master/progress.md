# Progress — operator-console-catalog-master

## Codebase Patterns
(consolidated reusable patterns — read first each iteration)

- **Running the FULL test suite:** `php artisan test` (and `composer test`) blow the box's default 128M PHP memory limit during `laravel/pao` result aggregation over the ~950-test suite — the `-d memory_limit` flag does NOT reach the pao worker. Run the full suite as **`php -d memory_limit=-1 vendor/bin/pest`** and PHPStan as **`php -d memory_limit=-1 vendor/bin/phpstan analyse`**. A *filtered* run (`php artisan test --filter=Foo`) stays under 128M and is fine for step 2.
- **Asserting Filament panel discovery (no resource needed):** `Filament::getPanel('admin')` exposes public getters `getResourceNamespaces()/getResourceDirectories()`, `getPageNamespaces()/getPageDirectories()`, `getWidgetNamespaces()/getWidgetDirectories()` — each returns the verbatim `for:` / `in:` args passed to `discoverResources/Pages/Widgets`. Use these to assert the discovery repoint directly (`->toContain('App\Modules\OperatorPanel\Filament\Resources')`) without scaffolding a real resource. Filament's `discoverComponents` silently no-ops on a missing directory (`HasComponents.php:496`), so an empty `.gitkeep` skeleton is a valid discovery target.
- **Operator-panel test auth:** authenticate with `actingAs(Operator::factory()->create(), 'operator')` (the `operator` session guard); `Operator` = `App\Modules\OperatorPanel\Models\Operator`. `get('/admin')->assertOk()` proves the panel boots + `canAccessPanel`.
- **Repo-local PHPStan custom rule (type-aware arch test):** the stack ships phpstan as a phar BUT `PHPStan\Testing\RuleTestCase` is autoloadable (phpstan's `bootstrap.php` is composer-`files`-loaded → `class_exists` true). Pattern: (1) rule class under `tests/PHPStan/Rules/` (namespace `Tests\…`, already dev-autoloaded — **no composer.json change**, so the "empty composer diff" rule holds); implement `Rule<TNode>` with `getNodeType()` + `processNode()`; resolve receiver types via `$scope->getType($node->var)` and `(new ObjectType(Model::class))->isSuperTypeOf($t)->yes()`; build errors with `RuleErrorBuilder::message()->identifier('x.y')->line($node->getStartLine())->build()`. (2) Register in `phpstan.neon` under top-level `services:` with `tags: [phpstan.rules.rule]` — this wires it into `type_check`. (3) **Scope a rule to a directory via a constructor `scopedPathNeedles` arg** (checked against `$scope->getFile()`), NOT a hardcoded path: production registration passes the real needle, the RuleTestCase passes the fixture's needle — same rule, both testable. (4) Test with a class extending `RuleTestCase` under `tests/Architecture/` (Pest binds the Laravel `TestCase` only to `Feature/`, so a plain PHPUnit class elsewhere runs clean); `gatherAnalyserErrors([$fixture])` is public and returns `Error[]` with `getLine()/getIdentifier()`. (5) **`excludePaths` the fixture dir** in phpstan.neon so its deliberate violations don't red the main run. (6) Verify API surface in `vendor/` by reflection before writing — `$class_exists`/`ReflectionClass` dump (lessons.md). Two-call shapes cover the writes: `MethodCall` (`$model->save()`) on a Model-typed `->var`; `StaticCall` (`Model::create()`) on a Name resolved via `$scope->resolveName()`.
- **Marker-driven fixture line assertions:** when a test asserts which fixture lines a rule flags, derive the expected lines from a `// flag`-style trailing marker scanned out of the fixture source (`file()` + `str_contains`), not hardcoded numbers — Pint's `fully_qualified_strict_types` can silently add a `use` import (e.g. from a `{@see FQCN}` docblock) and shift every line. Keep the literal marker token OUT of the fixture's own docblock/prose or the scan self-matches.

---

## [2026-06-20 08:05] — 1.1 Repoint Filament discovery into the OperatorPanel module
- **What:** repointed `AdminPanelProvider::panel()` `discoverResources/discoverPages/discoverWidgets` from the shell's default `app_path('Filament/...')` (`App\Filament\*`) to `app_path('Modules/OperatorPanel/Filament/...')` with the matching `App\Modules\OperatorPanel\Filament\{Resources,Pages,Widgets}` namespaces (ADR 2026-06-19). Created the discovery skeleton `Filament/Resources/Catalog/`, `Filament/Pages/`, `Filament/Widgets/` (each a `.gitkeep`). No resource yet (1.x+).
- **Files changed:** `app/Modules/OperatorPanel/Providers/AdminPanelProvider.php` (modified); `app/Modules/OperatorPanel/Filament/{Resources/Catalog,Pages,Widgets}/.gitkeep` (new); `tests/Feature/Modules/OperatorPanel/PanelDiscoveryTest.php` (new, 4 tests / 10 assertions).
- **Quality loop:** green — format ✓; filtered ✓ (4/4); full suite ✓ **953/953** (4698 assertions, SQLite); phpstan ✓ (0 errors); pint --test ✓. `openspec validate --strict` ✓. composer.json/lock diff vs main empty; no migrations added; no protected files touched.
- **Acceptance walked:** panel boots at `/admin` (dashboard `assertOk` for an authed operator) ✓; discovery namespaces+directories repointed to the module (asserted via the panel getters, incl. a negative assertion the old `App\Filament\*` is gone) ✓; the `operator-auth-foundation` auth tests (login, password-reset routes, no-registration, opt-in TOTP, default guard) stay green ✓.
- **PG17:** not run this task — 1.1 touches no DB-writing action (panel-config + discovery only; the new test's lone DB use is `Operator::factory()` on the shared `operators` table). Cross-engine PG17 runs land on the DB-action tasks (2.1+) and the 6.2 close, per tasks.md.
- **Learnings for future iterations:**
  - The two patterns above (full-suite memory flag; panel-discovery getters) are now in Codebase Patterns — read them before running tests or asserting panel config.
  - Discovery dirs may be empty: Filament no-ops on a missing dir, so resources can land in `Filament/Resources/Catalog/` incrementally without touching `AdminPanelProvider` again.
  - `AdminPanelProvider` is app code (design L3) — editing it is in-bounds; `pages([Dashboard::class])` + `widgets([AccountWidget, FilamentInfoWidget])` are explicit framework registrations and were left intact.
---

## [2026-06-20 08:26] — 1.2 Architecture test — no Eloquent write in App\Modules\OperatorPanel
- **What:** the operator-console write-discipline guard (ADR 2026-06-19; design L4), implemented as the **primary** option — a **PHPStan custom rule** (type-aware, AST-level), not the Pest token-scan fallback. The rule flags a `MethodCall`/`StaticCall` whose method ∈ {save, saveQuietly, update, updateQuietly, delete, forceDelete, create, insert, fill, setAttribute} on an **Eloquent-`Model` receiver**, in files matching a constructor-injected path needle. Registered in `phpstan.neon` → runs on every `type_check`.
- **Scope decision (kept the clean tree green):** scoped to the **console surface** `app/Modules/OperatorPanel/Filament/`, NOT the whole module. `Operator` (`extends Authenticatable`, which IS a `Model`) legitimately self-writes via `$this->save()` in `Models/Operator.php:100,124` (operator-auth-foundation) — a blanket module-wide ban would red the clean tree. The design's own fallback already scopes to `Filament/**`, and per L3 all console code lives under `Filament/`, so the auth principal's own writes are correctly out of scope.
- **Files changed:** `tests/PHPStan/Rules/NoEloquentWriteInOperatorPanelRule.php` (new, the rule); `tests/Architecture/NoEloquentWriteInOperatorPanelRuleTest.php` (new, `RuleTestCase`, 1 test / 12 assertions); `tests/Architecture/Fixtures/OperatorPanelEloquentWriteFixture.php` (new, planted writes + reads, `// flag` markers); `phpstan.neon` (modified — `services:` registration scoped to `Modules/OperatorPanel/Filament/`; `excludePaths` the fixture dir).
- **Quality loop:** green — format ✓; filtered ✓ (1/1, 12 assertions); full suite ✓ **954/954** (4710 assertions, SQLite); phpstan ✓ (0 errors, rule loaded); pint --test ✓. `openspec validate --strict` ✓. composer diff vs main empty; no migrations; no protected files touched.
- **Acceptance walked:** **red→green proven two ways.** (1) Automated/permanent: the `RuleTestCase` fixture asserts exactly the 10 planted writes are flagged (identifier `operatorPanel.noEloquentWrite`) and the 3 reads are not. (2) Production wiring: planted `$master->update([...])` **and** `ProductMaster::create(...)` in a real `app/Modules/OperatorPanel/Filament/Resources/Catalog/__RedProof.php` → `type_check` **failed** (both flagged); deleting it → `type_check` **0 errors**. Rule wired into `type_check` ✓; reads (`$master->name`, `Model::query()`, `refresh()`) not flagged ✓.
- **PG17:** N/A — 1.2's test is pure static analysis (phpstan over a fixture), no DB touched. Cross-engine PG17 runs land on the DB-action tasks (2.1+) and the 6.2 close, per tasks.md.
- **Learnings for future iterations:**
  - The PHPStan-custom-rule pattern + the marker-driven fixture-line pattern are now in Codebase Patterns — read them before writing the next arch rule or fixture-line assertion.
  - **Next (1.3):** amend the import-boundary test (`tests/Architecture/ModuleBoundariesTest.php`) so `App\Modules\OperatorPanel` MAY import `App\Modules\*\Models` (read-binding carve-out), scoped to OperatorPanel only; a planted lateral import (e.g. Catalog → `Parties\Models`) must still fail. That test iterates `Module::cases()` with `->not->toUse(...)->ignoring(...)` — the carve-out is an extra `->ignoring()` entry on the OperatorPanel source only. Read what it asserts before editing.
  - The rule bans writes on **any** Eloquent `Model` receiver in scope; it does NOT special-case foreign vs own models (the `Filament/` path scope already excludes the `Operator` auth model under `Models/`). If a future console legitimately needs to write its *own* OperatorPanel model from a Filament class, revisit the scope (own-namespace carve-out) rather than widening the path.
---
