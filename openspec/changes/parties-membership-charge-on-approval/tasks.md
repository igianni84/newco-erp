## 1. Domain — the atomic approve = charge = activation collapse

- [x] 1.1 **`ActivateProfile` — re-home the `MembershipFeePaid` seam E → S (docblock / seam-name only, behaviour-neutral)** (design — Decisions #3; spec — *Profile Activation*) — rewrite the `ActivateProfile` class docblock so the production trigger reads **Module S emits `MembershipFeePaid`; Module E records; Module K consumes (DEC-173)**, firing **INV1 — no INV0 (DEC-157)** — correcting the frozen "Module E / INV0" framing. State the two invocation modes: invoked **synchronously by `ApproveProfile`** in the approval transaction (the K-internal atomic activate-on-approval) now, and by the **Module-S `MembershipFeePaid` listener** later. Name `IllegalProfileTransition` in prose only (Pint import-boundary lesson 2026-06-20). Change **no** executable code: `handle()` stays `Approved → Active` + records `ProfileActivated`, from-state guarded. Do NOT fabricate a `MembershipFeePaid` class/listener (landmine #2).
  - Acceptance: the docblock cites DEC-173 + DEC-157 (Module-S emit, INV1, no INV0) and the two invocation modes; no behaviour changes; `ProfileActivationTest` passes **unchanged** (the isolated `Approved → Active` contract incl. `cannotActivate`); `grep` finds no `class MembershipFeePaid` under `app/Modules/Parties/Events/`.
  - Test hint: run `--filter=ProfileActivationTest` → green with zero edits to the test; assert `glob(app_path('Modules/Parties/Events/*.php'))` still contains no `MembershipFeePaid` (the `ProfileLapseGraceTest` events-glob idiom).

- [ ] 1.2 **`ApproveProfile` — drive `Applied → Approved → Active` atomically, and invert every approve-outcome observer in the same iteration** (design — Decisions #1/#2, Risks #6/#7; spec — *Profile Membership Approval*, *Demand-Side Activation Events*) — inject `ActivateProfile` into `ApproveProfile` (container-autowired — no call site passes explicit ctor args); after the `Applied → Approved` write + the conditional Originating-Club lock, invoke `app(ActivateProfile)` (or the injected instance) **inside the same `DB::transaction`** to reach `Active`. `Approved` is transient. The internal activation writes **only** `Profile.state` + `ProfileActivated` — **never** `originating_club_id` (landmine #4, `ComplianceIndependenceTest`). Rewrite the `ApproveProfile` docblock: atomic approve = charge = activation; the charge is a no-op Module-S seam today (delegates to the Module-S `MembershipFeePaid` listener later); the charge-fail contract (fail → stays `Applied`, no seat, no OC lock) is the Module-S target. **This is a red-green behaviour flip: in the SAME iteration, invert every test that observed the durable two-step** (each keeps the suite green):
    - `ProfileMembershipApprovalTest` — test 1: approve → `state == Active` (was `Approved`), event count `1 → 2` (`OriginatingClubLocked` + `ProfileActivated`); test 2: profileB → `Active`, total events `1 → 3`; tests 3–5 (decline, illegal-from-state) stand.
    - `MembershipActivationChainTest` — delete the explicit `app(ActivateProfile)->handle($profileC->id)` (now throws `cannotActivate`); profileC AND profileD both reach `Active`; the event multiset gains a second `ProfileActivated` (7 → 8); keep OC-lock-once + Account-`Active` assertions; rewrite the "stops at approved" prose.
    - Precondition helpers — **delete the now-illegal `ActivateProfile` line** (approve alone reaches `Active`; do NOT try/catch): `ProfileCancellationTest:64-65`, `ProfileSuspensionTest:53-54`, `ProfileLapseGraceTest:65-66`, `MembershipSuspensionChainTest:150-153`.
    - Console approve-observers — `ProfileApprovalConsoleTest` (approve → `Active`, not `Approved`; OC-lock-once stands) and `ProfileMembershipChainTest` (drop the separate `->callAction('activate')` leg — the verb is now hidden once the profile is `Active`; the approve leg lands `Active`; fix the ordered event sequence to co-emit `OriginatingClubLocked` + `ProfileActivated`).
  - Acceptance: `ApproveProfile` leaves the Profile `Active` in one transaction, recording exactly `ProfileActivated` (+ `OriginatingClubLocked` on first-ever approval) and **no** `ProfileApproved`; a from-wrong-state approve still throws `IllegalProfileTransition` leaving everything unchanged; `DeclineProfile` untouched; **`SupplyLifecycleChainTest` + `ComplianceIndependenceTest` pass unamended** (no Action/Event class added/renamed; OC write still exactly-once on `ApproveProfile`).
  - Test hint: `--filter` each inverted file green; assert `Profile::find($id)->state === ProfileState::Active` after approve; assert `DomainEvent::pluck('name')` contains `ProfileActivated` once per approval and `OriginatingClubLocked` once for the Customer; run `SupplyLifecycleChainTest` + `ComplianceIndependenceTest` unedited → green (the allow-list + OC-write guards); a first-ever approve records both events as **roots** (`causation_id` null).

## 2. Operator console — collapse the two verbs

- [ ] 2.1 **Remove the dead `activate` verb + realign the i18n contract** (design — Decisions #4, Risks #5/#11; spec — *Profile Membership Approval*) — delete the `activate` lifecycle verb (and its `ActivateProfile` import) from `ViewProfile::getHeaderActions()` — it is visibility-gated `state == approved`, structurally unreachable now that `Approved` is never durable. Keep the **Approve** verb (label unchanged — the L-PP producer write); reword its success copy to name the atomic outcome (Q2 Option A: EN "Membership approved and activated." / IT "Iscrizione approvata e attivata."). Atomically: drop `operator_console.profile.actions.activate` + `operator_console.profile.notifications.activated` from **both** `lang/en/operator_console.php` and `lang/it/operator_console.php`, and remove both keys from `profileConsoleKitKeys()` in `ProfileConsoleI18nTest`. Remove `ProfileActivationConsoleTest` (its verb no longer exists). Adjust the `approved`-state visibility rows in `ProfileLifecycleConsoleTest` (no lifecycle verb is visible from the transient `approved` state) and any `ProfileMembershipChainTest`/`ProfileResourceTest` residue. Do NOT touch the six `customer.*` Account keys in `ProfileConsoleI18nTest` (landmine #5).
  - Acceptance: `ViewProfile` exposes 8 verbs (no `activate`); clicking **Approve** drives the Profile to `Active`; `ProfileConsoleI18nTest` passes (EN-completeness + IT-distinct + sink-scan) with the two keys gone from both the lang files and the contract; no raw-key label renders; `ProfileActivationConsoleTest` is deleted; the full OperatorPanel Parties suite is green.
  - Test hint: `--filter=ProfileConsoleI18nTest` green; assert `Lang::has('operator_console.profile.actions.activate','en',false)` is now **false** and the key is absent from `profileConsoleKitKeys()`; a Livewire page test on an `applied` Profile asserts the `approve` action is present and the `activate` action is absent; drive `approve` → `assertSee` the reworded notification, Profile `Active`.

## 3. Verification — cross-engine + guard tests

- [ ] 3.1 **Full Parties suite green on SQLite AND PostgreSQL 17; guards green unamended; static analysis clean** (design — Risks #3/#8; `knowledge/testing/rules.md`) — run the whole suite on both engines (the `php -d memory_limit=2G vendor/bin/pest` invocation; the local `postgres:17` docker on port 55432 with `--tmpfs` for cross-engine). Confirm the exact-set Action allow-list (`SupplyLifecycleChainTest`), the OC-write guard (`ComplianceIndependenceTest`), the creation-chain guard (`SpineCreationChainTest`) and `ProfileActivationTest` all pass **without edits**. `vendor/bin/phpstan analyse` at max → 0 errors; `vendor/bin/pint --test` clean.
  - Acceptance: full suite green on SQLite `:memory:` and PG17 (event names/counts asserted by name, payloads by key — PG jsonb trap 3); PHPStan max 0; Pint clean; `SupplyLifecycleChainTest` / `ComplianceIndependenceTest` / `SpineCreationChainTest` / `ProfileActivationTest` diff-free.
  - Test hint: `git diff --stat` shows those four guard files unchanged; the PG run uses the hot.md cross-engine recipe (`docker rm -f pg` after); capture the pass counts.

## 4. Memory

- [ ] 4.1 **Consolidate `progress.md`, overwrite `hot.md`, append `log.md`** (`.claude/CLAUDE.md` — Hot Cache & Log discipline) — write the iteration narrative + `## Codebase Patterns` into `openspec/changes/parties-membership-charge-on-approval/progress.md`; overwrite `hot.md` from current state (RM-03 shape-collapse shipped; charge deferred to Module S/E; seat gate → RM-05); append one line via `scripts/memlog.sh "spec-to-change→impl" "parties-membership-charge-on-approval" "<outcome>"`. If a durable pattern emerged (e.g. "a behaviour flip inverts every observer in one iteration; the isolated writer's contract stands"), record it in `knowledge/testing/` or `lessons.md`.
  - Acceptance: `progress.md` has the narrative + patterns; `hot.md` ≤ 550 words reflecting the merged state; `log.md` has the one-line entry (real clock, ≤ 280 chars); `memory-health.sh` Stop hook clean.
  - Test hint: n/a (docs) — verify `hot.md` word count and the `log.md` line format.
