# Progress — parties-membership-charge-on-approval

## Codebase Patterns
(consolidated reusable patterns — read first each iteration)

- **Prove a docblock/seam-name change is behaviour-neutral** with `git diff <file> | grep -E '^[+-]' | grep -vE '^[+-]{3}' | grep -vE '^\s*[+-]\s*\*'` → empty output means every changed line is a `*` comment. Pair it with the Action's isolated contract test passing **unedited** + PHPStan resolving the docblock `{@see}` refs.
- **Docblock `{@see}` imports:** same-namespace Actions (`ApproveProfile`, `DeclineProfile`, `ActivateProducer`, `CloseClub`, `IssueClubCredit`) and already-imported classes need **no** new `use` — only a *cross-namespace* `{@see}` would force one (Pint import-boundary lesson 2026-06-20). Reference `MembershipFeePaid` as backticked prose, never `{@see}` (no class exists — zero-invention).
- **Docs-first within a change:** a docblock may describe the change's **target** design (here: `ApproveProfile` invoking `ActivateProfile` synchronously) one task before the wiring lands, as long as no test asserts the wiring from prose — green stays green between tasks.
- **A red-green FSM-shape flip inverts EVERY observer in the SAME iteration** (one commit stays green): grep `ProfileState::Approved` + the Action name across `tests/` to enumerate the observers exhaustively BEFORE editing — the design's enumeration is a checklist, the grep is the proof it's complete. Split them into (a) *outcome observers* (assert state/events after the real Action → flip these), (b) *precondition helpers* (double-drove approve→activate → **delete** the now-illegal second call, no try/catch), (c) *factory-forced-state* datasets (never call the flipped Action → leave untouched), (d) *source-scan guards* (`SupplyLifecycleChainTest` allow-list, `ComplianceIndependenceTest` OC-write count → stay diff-free iff no Action/Event class is added and the literal `'…' =>` write count is unchanged). The isolated writer's own contract (`ProfileActivationTest`) stands unedited.
- **Bare-path `--filter` on `OperatorPanel/Parties/` reds 5 `*ConsoleI18nTest`** with "expected the shared operator-console sink scanner to be loaded" — `scanOperatorConsoleHardcodedSinks()` is declared in `ProductMasterConsoleI18nTest` (Catalog dir) and only loaded by the FULL suite (lesson 2026-06-20). Not a regression; verify these via `php -d memory_limit=2G vendor/bin/pest` (whole suite), never a bare Parties path.
- **Nested-Action atomicity:** an Action may invoke a sibling Action's `handle()` inside its own `DB::transaction` — the sibling opens a savepoint (nested tx), so `DomainEventRecorder`'s `transactionLevel() === 0` guard stays satisfied, and any throw rolls the whole outer tx back. The recorder threads only *explicitly-passed* causation, so a sibling recording with no causation stays a ROOT even when called inside another Action's tx.

---

## [2026-07-03 10:41] — 1.1 `ActivateProfile` — re-home the `MembershipFeePaid` seam E → S (docblock-only, behaviour-neutral)
- **What:** Rewrote the `ActivateProfile` class docblock to (a) **re-home the `MembershipFeePaid` seam E → S** — *Module S emits; Module E records; Module K consumes* (DEC-173), firing **INV1, no INV0** (DEC-157) — correcting the frozen "Module E / INV0" framing; (b) state the **TWO INVOCATION MODES** — synchronously by `ApproveProfile` inside the approval transaction (the K-internal atomic activate-on-approval) now, and by the Module-S `MembershipFeePaid` listener later; (c) reframe the first paragraph so the approve/decline **writes** are audit-only while `ApproveProfile` drives straight through activation (canon MVP-DEC-016, `Approved` transient). Executable code (`handle()`, ctor, imports) untouched; no `MembershipFeePaid` class fabricated.
- **Files changed:** `app/Modules/Parties/Actions/ActivateProfile.php` (docblock only, +25 / −13 comment lines); `openspec/changes/parties-membership-charge-on-approval/tasks.md` (1.1 checked).
- **Quality loop: green** — Pint format + `--test` clean; `--filter=ProfileActivationTest` 5/5 (test file unedited); full suite `php -d memory_limit=2G vendor/bin/pest` **1947/1947** (10459 assertions, SQLite); PHPStan max **0 errors**; `openspec validate parties-membership-charge-on-approval --strict` valid.
- **Acceptance:** all 4 bullets met — docblock cites DEC-173 + DEC-157 + the two modes; no behaviour change (every diff line is a `*` comment; suite green); `ProfileActivationTest` unchanged (incl. the `cannotActivate` from-state cases); no `MembershipFeePaid` event file under `app/Modules/Parties/Events/`.
- **Learnings for future iterations:**
  - This was the behaviour-neutral prep for **1.2**. The docblock now *describes* `ApproveProfile` invoking `ActivateProfile` synchronously ("mode (a)"), but the real wiring lands in **1.2** — the docblock states the change's target design (docs-first), and green holds because no test asserts wiring from prose.
  - `{@see ApproveProfile}` anticipates 1.2 yet resolves today (same namespace, class exists). Kept the `ActivateProducer` KYC-seam analogy; corrected the Club-Credit note's "Module E's listener" → "Module S's listener" (DEC-173 consistency).
  - Cross-engine PG **not** run here (docblock-only, no runtime surface) — that is task **3.1**'s explicit job.

---

## [2026-07-03 11:05] — 1.2 `ApproveProfile` atomic flip + invert every approve-outcome observer
- **What:** Wired the atomic **approve = charge = activation** collapse (canon MVP-DEC-016). `ApproveProfile` now injects `ActivateProfile` (container-autowired ctor param) and, inside its existing `DB::transaction`, after the `Applied → Approved` write + the conditional Originating-Club lock, invokes `$this->activateProfile->handle($profile->id)` → the Profile lands `Active` in ONE transaction (`Approved` is a transient pass-through). The atomic op records `OriginatingClubLocked` (first-ever approval only) + `ProfileActivated` (from the internal activation), NEVER `ProfileApproved`. The internal activate writes only `Profile.state` + `ProfileActivated` — no `originating_club_id` (ComplianceIndependenceTest guard holds). Rewrote the `ApproveProfile` docblock: atomic shape, charge = no-op Module-S seam today (delegates to the Module-S `MembershipFeePaid` listener later — DEC-173/DEC-157), charge-fail contract (fail → stays `Applied`, no seat, no OC lock) = the Module-S target. No `MembershipFeePaid` class fabricated. Then inverted all 8 approve-outcome observers in the SAME iteration.
- **Files changed (9):** `app/Modules/Parties/Actions/ApproveProfile.php` (inject + drive-through + docblock). Tests inverted: `ProfileMembershipApprovalTest` (tests 1&2 → `Active`; event counts 1→2 / 1→3; added the explicit `ProfileActivated`-root assertion; +`ProfileActivated` import), `MembershipActivationChainTest` (dropped explicit `ActivateProfile` + import; both Profiles `Active`; multiset 7→8), the 4 precondition helpers (`ProfileSuspensionTest` / `ProfileLapseGraceTest` / `ProfileCancellationTest` / `MembershipSuspensionChainTest` — **deleted** the now-illegal `ActivateProfile` line + import, no try/catch), console `ProfileApprovalConsoleTest` (→ `Active`) + `ProfileMembershipChainTest` (removed the dead `activate` leg; approve leg lands `Active`; the 8-event ordered sequence unchanged — OCL→ProfileActivated already the array order).
- **Quality loop: green** — Pint clean; full suite `php -d memory_limit=2G vendor/bin/pest` **1947/1947** (10460 assertions, SQLite); PHPStan max **0**; `openspec validate --strict` valid.
- **Acceptance:** all 4 bullets + all 5 test-hints met — approve → `Active` in one tx; exactly `ProfileActivated` (+ conditional `OriginatingClubLocked`), no `ProfileApproved`; wrong-from-state still throws; `SupplyLifecycleChainTest` + `ComplianceIndependenceTest` diff-free & green (git-confirmed); both events are roots (`causation_id` null).
- **Learnings for future iterations:**
  - The 8-event ordered sequence in `ProfileMembershipChainTest` was UNCHANGED by the flip: `ApproveProfile` records `OriginatingClubLocked` (lock) THEN `ProfileActivated` (internal activate) — exactly the array's `[..., 'OriginatingClubLocked', 'ProfileActivated', ...]` order. Removing the separate `activate` console leg dropped nothing from the sequence.
  - Running the `OperatorPanel/Parties/` dir as a bare path reds 5 `*ConsoleI18nTest` (sink-scanner not loaded — see Codebase Patterns); the full-suite run is the source of truth and is green.
  - **Next = 2.1** (console cleanup): remove the now-dead `activate` verb from `ViewProfile::getHeaderActions()` + its `ActivateProfile` import; drop `profile.actions.activate` / `profile.notifications.activated` from en+it `operator_console.php` AND `profileConsoleKitKeys()` in `ProfileConsoleI18nTest` (atomic — landmine #5); reword `notifications.approved` (Q2 Opt A: EN "Membership approved and activated." / IT "Iscrizione approvata e attivata."); **delete** `ProfileActivationConsoleTest`; adjust the `approved`-state visibility rows in `ProfileLifecycleConsoleTest` + `ProfileResourceTest` residue. Do NOT touch the six `customer.*` keys.

---
