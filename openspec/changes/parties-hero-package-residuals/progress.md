# Progress — parties-hero-package-residuals

## Codebase Patterns
(consolidated reusable patterns — read first each iteration)

- **Two mutants, not one — and the second is the one that matters.** A mutant that reds the new pin *and* the pre-existing tests proves the pin **fires**, not that it is **needed** (delete the pin; the suite still catches that mutant). Necessity is proved by the weaker, realistic drift that the existing suite **passes** and only the new pin fails. Empirically here: moving `ApproveProfile`'s from-state guard below the whole gate block reds 13 tests (7 of them pre-existing) — loud and uninformative; moving it just past `lockAndCountOccupiedSeats()` but **before** the gate's `if` leaves all 13 pre-existing tests green and reds exactly the 6 new dataset rows, each on `expect($statements)->toBeEmpty()`. Design R5 asks for the first; ask for the second too, or "the positive assertion discriminates nothing" stays an argument rather than a measurement. (`knowledge/testing/hypotheses.md` — corollary added.)
- **A chained `expect(...)->and(...)` SHORT-CIRCUITS, so ONE MUTANT PER CONJUNCT — and the assertion COUNT proves which conjuncts ran.** Pest evaluates the chain left to right and throws on the first failure, so a mutant breaking conjunct 1 never evaluates conjunct 2, leaving it unfalsified — decoration a later iteration will delete. Count the independent ways the *claim* can break, not the assertions. Measured in 2.1 on the root-ness pair (`causation_id` null · `correlation_id === event_id`): green baseline **12384** assertions; mutant A (`causationId:`) → 1 failure at the `toBeNull()` line, **12383** assertions (the chain aborted); mutant B (a shared `correlationId:`) → 1 failure at the `toBe()` line, **12384** (conjunct 1 ran and passed). Two numbers, and the short-circuit stops being a guess. Design each mutant so the other conjuncts stay green. **Not** the two-mutants corollary above: that is loud-vs-realistic drift against *one* assertion; this is one mutant per *independent* assertion. Task 3.2's `title` + `status` + `body` notification pin is three conjuncts. (`knowledge/testing/hypotheses.md` — new hypothesis, 1/3.)
- **`domain_events` is immutable BY TRIGGER, and `causation_id` is an FK — so an event's causality can only be set at `record()` time, from a donor that already exists.** `2026_06_12_000004_add_immutability_triggers.php` rejects `UPDATE` at the engine (`domain_events is immutable: UPDATE is rejected`, both lanes) — note `2026_06_12_000001`'s docblock still says the triggers "arrive in a later migration"; they arrived. And `causation_id` is `foreignId()->nullable()->constrained('domain_events')`, so a fabricated id raises `FOREIGN KEY constraint failed` at insert. Consequence for mutation work: in a transaction that records exactly ONE event, `causation_id` cannot be driven non-null by any *quiet* drift, and the one drift that can (recording a donor event first) is caught by whatever `DomainEvent::query()->count()` assertion the test already carries. **Count the transaction's events before promising a `causationId` mutant.**
- **A conjunct can be DOMINATED by an earlier assertion in the same test — then enumerate the property's CAUSES instead of hunting for a mutant.** When every candidate mutant reds an integrity error or an earlier line rather than the line you aimed at, that assertion is unfalsifiable *today*. This is not grounds to delete it — the spec may require it, as `WaitingListJoined`'s root-ness does at *both* entry points — it is grounds to write the enumeration into the test comment: name the assertion that dominates it, and the condition under which it becomes the last guard standing. Skip that and a later iteration deletes it, correctly, as decoration. Measured in 2.2: the donor mutant reds `count()->toBe(1)` 27 lines above the pin (12373 assn, 2 failures, neither at the pin). (`knowledge/testing/hypotheses.md` — corollary to the short-circuit hypothesis, now 2/3.)
- **Pest's `toThrow($class, $message)` compares the message with `toBe`, i.e. EXACTLY** (`vendor/pestphp/pest/src/Mixins/Expectation.php:960`; the `assertStringContainsString` calls on :972/:978 serve the *other* arities). So *"raises reason X, never reason Y"* needs no second assertion — passing the localized `cannot_approve` string already excludes `club_at_capacity`. Do not add a defensive `->not->toContain(...)`; it is dead weight.
- **`Factory::count(0)->create()` is safe** — `Factory::make()` short-circuits on `$this->count < 1` and returns an empty collection (`vendor/laravel/framework/.../Factories/Factory.php:443`). It does **not** reach `range(1, 0)`, which in PHP counts *downwards* and would seed two rows. A capacity-`0` at-parity fixture therefore needs no `if ($occupied > 0)` guard.
- **An at-parity fixture must account for the SUBJECT's own seat, and capacity `0` is the only one that need not.** `wouldOversell()` is true at every occupancy when capacity is `0`, so one fixture serves every from-state. A *free-seat* fixture cannot be reused that way: `active` occupies a seat (`OCCUPYING_STATES = [Active, Suspended]`) and `lapsed` does not, so `capacity: 2, occupied: 1` is at parity for an `active` subject and below parity for a `lapsed` one. Cap well above the seeded occupancy (`5, 1`) when the row must read "a free seat" for both.
- **The only honest uncapped fixture is an explicit `null` in `by_club_id` beneath a CAPPED `default`.** The adapter resolves the override on `array_key_exists`, so presence wins and the null pins that one Club uncapped. A huge cap (`PHP_INT_MAX`) exercises the **capped** branch (`$capacity !== null`) of a Club that merely is not full — a different code path, and not the claim.
- **Inline `config()->set(...)` beats a new global helper.** Pest `include`s every selected test file into one process while building the suite, so a second `function fooBar()` is a **fatal redeclare that kills the whole run** before any test executes. This file already declares `seatClubTo()` / `freeOneSeat()`; the new pin needed a `?int` capacity and a capped default, so it sets config inline — the shape the file's own from-state test and `ProfileRenewalCapacityGateTest`'s dataset both already use. A helper would have bought nothing and risked everything.
- **An envelope pinned on event E recorded by Action X says NOTHING about event E recorded by Action Y — `grep`ping the event NAME will tell you it is covered, and it is not.** Design R3 states this for *causality* (two `record()` call sites ⇒ two pins); it holds identically for the **actor envelope**, and the repo proves it. `ProfileApprovalConsoleTest.php:330` asserts `WaitingListJoined.actor_role === newco_ops` — at `ApproveProfile`'s **divert**. `CreateProfile`'s **birth** call site was blind to both `actor_role` and `actor_id`, and `actor_id` was asserted at *neither*. Measured in 3.1: hardcoding `ActorRole::System` on the birth call reds exactly 1 test of 2388. Before promising a pin is redundant, locate the `record()` **call site**, not the event class.
- **When a mutant reds a Livewire/Filament console test, trust the test NAME and the failure MESSAGE — the reported LINE can point outside the failing test entirely.** Measured in 3.1: mutant C (a `capacity` field added to `ProfileResource::form()`) reported `line: 247`, a **comment** inside a different test 114 lines past the failing test's closing brace; the failing test was the form-shape pin at :133 (*"Failed asserting that a field with the name [capacity] does not exist"*). Mechanism undetermined (Pest resolves the frame through Livewire's `Testable` `__call` chain, and every Pest closure shares the file). The near-miss is expensive: a line number that lands in an unrelated body reads as *"my pin never fired, the mutant is dominated"* — the exact conclusion that deletes a live pin. Cost one re-run with the full failure record; budget for it. Task 3.2's notification mutants run on this same surface.

---

## [2026-07-09 21:37] — 1.1 Pin guard-before-lock negatively (R1, R2)

- **What was implemented.** The negative ordering pin for `ApproveProfile`'s from-state guard, as a new `it('locks no Club row for an out-of-state approval, whatever the gate that never ran would have said')` in `ProfileApprovalCapacityGateTest.php`. `DB::listen` captures every statement naming `parties_clubs`; a doomed approval must emit **none** (`toBeEmpty()`). Driven over a 6-row dataset — `{active, lapsed}` × `{at parity (capacity 0), a free seat (5/1), explicitly uncapped (a null override beneath a capped default of 1)}` — so the guard's independence from the capacity gate is *shown*, not coincidental. Each row also asserts the exact localized `cannot_approve` message (never `club_at_capacity`), that the Profile is **not** moved to `waiting_list`, and that the event log is a clean zero. The file's five-claim docblock had its claim 4 extended to say *why* the pin is negative.
- **Files changed.** `tests/Feature/Modules/Parties/ProfileApprovalCapacityGateTest.php` (+59/−1); `knowledge/testing/hypotheses.md` (confirmation 2/3 + the two-mutant corollary). **No `app/` file** — `git diff --name-only | grep -c '^app/'` ⇒ `0`, per design R1 / Non-Goals.
- **Quality loop: green.** Pint clean (no reformat, and no `use` import manufactured). File: 19/19. Full suite **SQLite 2387/2387** (12 382 assn) and **PG17 2387/2387** (12 389 assn) — the baseline was 2381, so the 6 new rows are the entire delta and nothing else moved. PHPStan max **0**. `openspec validate parties-hero-package-residuals --strict` green.
- **Mutation (design R5), run twice — and the second run is the finding.**
  - *Mutant A* (the one tasks.md prescribes — guard moved below the entire gate block): 13 red. The `at parity` rows throw nothing at all, because the `active`/`lapsed` Profile is **diverted onto the waitlist** — exactly the defect the superseded prose described; the `free seat` / `uncapped` rows still throw the right reason and red solely on `toBeEmpty()`. Both predicted effects observed.
  - *Mutant B* (guard between `lockAndCountOccupiedSeats()` and the gate's `if` — the realistic drift): **all 13 pre-existing tests stayed green**, including all 7 rows of the file's own positive from-state test, and **only** the 6 new rows reddened, every one on `expect($statements)->toBeEmpty()`. This turns design R2's thesis from an argument into a measurement: a from-state guard sliding below the Club-row lock was invisible to the entire repository until this pin existed.
  - Restored with `git checkout --`; `diff` against the pre-mutant copy confirms byte-identical; file re-run green.
- **Learnings for future iterations:**
  - The `_Source:_` line of this change's own delta spec quotes the **superseded** prose verbatim, on purpose. Task 4.1's sweep grep (`"only if a seat is free"`, `"assert the from-state"`) will match it. **Read the grep, do not count it** — tasks.md says so, and it is not a formality.
  - Tasks 2.2 and 3.x add assertions to this same file. It declares `seatClubTo()` and `freeOneSeat()`; the console files must not reuse either name (fatal redeclare, process-wide).
  - The two `WaitingListJoined` root-ness pins (2.1 / 2.2) sit at two distinct `record()` call sites — `CreateProfile`'s birth and `ApproveProfile.php:163`'s divert. This task touched neither; the divert's recorded envelope is reachable from this file's existing at-capacity divert test (the first `it(...)` in the file, which already `sole()`s the event).

---

## [2026-07-09 22:02] — 2.1 Pin WaitingListJoined root-ness at the birth entry point (R3)

- **What was implemented.** The root-ness pin for the `WaitingListJoined` recorded by `CreateProfile`, appended to the existing `it('births a Profile in waiting_list when the target Club is at capacity, recording both events')` — the one test in the file that already `sole()`s the event, so the pin reuses its fixture and adds no helper. Two assertions, mirroring `ProfileActivationTest.php:74-75` verbatim: `causation_id` is null, and `correlation_id === event_id`. The file's docblock claim 1 now states the root-ness and points at `ProfileApprovalCapacityGateTest` for the divert entry point (task 2.2), so the split is legible from either file.
- **Why the birth entry point is the dangerous one.** `ProfileCreated` is recorded **first, in the same transaction**, so it reads like the cause of the waitlist join. It is not — a birth is not an edge (the Action writes no transition out of its birth state), so nothing caused the join and the two are **sibling roots**. The comment says this, because the next reader's instinct will be to chain them.
- **Files changed.** `tests/Feature/Modules/Parties/ProfileBirthStateRoutingTest.php` (+11/−4); `knowledge/testing/hypotheses.md` (new hypothesis, 1/3). **No `app/` file** — `git diff --name-only | grep -c '^app/'` ⇒ `0`, per design R1 / Non-Goals.
- **Quality loop: green.** Pint clean (no reformat). File: 12/12. Full suite **SQLite 2387/2387** (12 384 assn) and **PG17 2387/2387** (12 391 assn) — both baselines rose by exactly 2, the two new assertions, so nothing else moved. PHPStan max **0**. `openspec validate parties-hero-package-residuals --strict` green. `toBe` on `correlation_id`/`event_id` is safe on PG (both UUID text columns — **not** the `causation_id` bigint that `ProducerConsoleChainTest` has to compare with the loose `toEqual`).
- **Mutation (design R5), two mutants — one per conjunct, because the chain short-circuits.** The task prescribed one (`causationId`). That mutant can only ever falsify the *first* assertion; run it alone and `correlation_id === event_id` ships unfalsified.
  - *Mutant A* — `causationId: $created->id` (capture the discarded return of the `ProfileCreated` `record()`; the realistic "the birth caused the join" drift). **Full suite: 2386 passed, exactly 1 failed** — the new pin, at the `toBeNull()` line, `12383` assertions.
  - *Mutant B* — `correlationId: $created->correlation_id`, leaving `causation_id` null (the tempting "one correlation per transaction"). **Full suite: 2386 passed, exactly 1 failed** — the new pin, at the `toBe($event_id)` line, `12384` assertions (conjunct 1 ran and passed).
  - Both mutants were run against the **full suite**, not the file: the necessity claim is repo-wide, and it holds — **the entire repository was blind to both drifts** before this pin. `grep -rn causation_id tests/` confirmed no pre-existing assertion anywhere touches `WaitingListJoined`'s envelope causality.
  - Restored with `git checkout --`; `diff` against the pre-mutant copy byte-identical (`md5 25e6bb8a…`); file re-run green.
- **Learnings for future iterations:**
  - **The assertion count is a free instrument.** Pest prints `assertions:` per run; a short-circuited chain reports one fewer than the green baseline. Two mutants that report the *same* count means one of them never reached the assertion you believed it was falsifying. Promoted to § Codebase Patterns and to `knowledge/testing/hypotheses.md`.
  - Task **2.2** is the divert entry point (`ApproveProfile.php:163`). Its `WaitingListJoined` is recorded in a transaction where **no prior event exists** — the divert records exactly one event — so the "sibling roots" hazard of 2.1 does **not** apply there, and mutant B (a shared `correlationId`) has no natural donor. Expect to fabricate the correlation (e.g. `correlationId: (string) Str::uuid7()`) to falsify that conjunct. The at-capacity divert test is the file's first `it(...)`, which already `sole()`s the event.
  - Task **3.2**'s notification pin asserts `title` + `status` + `body` — **three** conjuncts, so three mutants by the rule above, unless the assertions are split so a failure of one does not mask the others.
---

## [2026-07-09 22:37] — 2.2 Pin WaitingListJoined root-ness at the divert entry point (R3)

- **What was implemented.** The root-ness pin for the `WaitingListJoined` recorded by `ApproveProfile`'s capacity divert (`ApproveProfile.php:163`), appended to the file's first `it(...)` — the at-capacity divert test, which already `sole()`s the event. Two assertions mirroring `ProfileActivationTest.php:74-75`: `causation_id` is null, `correlation_id === event_id`. Docblock claim 1 now names the event a ROOT event and points at `ProfileBirthStateRoutingTest` for the birth entry point, so the two-`record()`-call-sites split (design R3) is legible from either file.
- **Files changed.** `tests/Feature/Modules/Parties/ProfileApprovalCapacityGateTest.php` (+24/−1); `knowledge/testing/hypotheses.md` (confirmation 2/3 + the domination corollary). **No `app/` file** — `git diff --name-only | grep -c '^app/'` ⇒ `0`, per design R1 / Non-Goals.
- **Quality loop: green.** Pint clean. File: 19/19. Full suite **SQLite 2387/2387** (12 386 assn) and **PG17 2387/2387** (12 393 assn) — both baselines up by exactly 2, the two new assertions, so nothing else moved. PHPStan max **0**. `openspec validate parties-hero-package-residuals --strict` green.
- **Mutation (design R5) — and the prescribed mutant DOES NOT EXIST at this entry point.** tasks.md says *"pass a `causationId` into `ApproveProfile`'s `record()` call"*. It cannot be done quietly, and proving that took three runs:
  - *Mutant A* — self-parent after the insert (`$joined->update(['causation_id' => $joined->id])`, the only FK-valid non-null causation a single-event transaction can name). **Rejected by the engine**: `domain_events is immutable: UPDATE is rejected`. 2 failures + 3 errors, all integrity violations, none at the pin. The immutability triggers that `2026_06_12_000001`'s docblock says "arrive in a later migration" **have arrived** (`2026_06_12_000004_add_immutability_triggers.php`).
  - *Probe* — `causationId: 999`, a dangling id. `FOREIGN KEY constraint failed` at the insert; the approve throws, the test errors at `handle()`. `causation_id` really is `foreignId()->constrained('domain_events')`.
  - *Mutant C* — the realistic donor drift: record a `'ProfileApproved'` event first, parent the join under it. **Full suite: 2385 passed, 2 failed, 12 373 assertions — and the divert test reds at line 111, `expect(DomainEvent::query()->count())->toBe(1)`, twenty-seven lines ABOVE the pin.** The pin never executes. `ProfileUngatedTransitionsTest` reds too. The pre-existing suite already owns this drift.
  - *Mutant B* — `correlationId: (string) Str::uuid7()`, the recorder's own documented trap (*"`correlation_id` defaulting to the event's OWN `event_id` for a root event (NOT an independent fresh UUID)"*). **Full suite: 2386 passed, exactly 1 failed** — the pin, at the `toBe($event_id)` line, **12 386** assertions (= baseline: conjunct 1 ran and passed, so conjunct 2 was genuinely reached). The other 2386 tests were blind to it.
  - `ApproveProfile.php` restored after every mutant; final `diff` against the pre-mutation copy byte-identical (md5 `608fd7ec…`).
- **The finding.** The same two-conjunct pin on the same event has **opposite** falsifiability at its two entry points. At **birth** both conjuncts have quiet mutants — `ProfileCreated` is a real donor sitting in the same transaction. At **divert** only `correlation_id` does: the approve write is audit-only (§ 15.2 names no `ProfileApproved`), so the transaction records exactly one event and there is no donor; the FK forbids inventing one; the trigger forbids back-filling it. `causation_id`-is-null is therefore held up by *schema*, not by this test — and its only reachable cause is dominated by the `count()->toBe(1)` above it. I kept the assertion (the truth spec requires root-ness *"at either entry point"*) and wrote the whole enumeration into the test comment, naming the dominating assertion and the condition that would make this line the last guard standing. An unfalsifiable assertion with no explanation is decoration; with the explanation it is a spec citation.
- **Learnings for future iterations:**
  - **Before promising a `causationId` mutant, count the events in the transaction.** One event ⇒ no donor ⇒ no quiet mutant. This generalizes to every root-event envelope pin in the repo.
  - **`domain_events` rows cannot be UPDATEd** (trigger, both engines). Any test or fixture that wants to perturb a recorded envelope must do it at `record()` time.
  - Task **3.2**'s notification pin (`title` + `status` + `body`) is three conjuncts; by the corollary above, check for domination before assuming three mutants exist — `assertNotified()`-style title checks elsewhere in that file may already dominate the `title` conjunct.
---

## [2026-07-09 23:14] — 3.1 Console create-at-capacity (R4)

- **What was implemented.** The console leg of the Hero-Package capacity BIRTH, as a new `it('births a waiting_list Profile through the console when the Club is at capacity, both events carrying the operator envelope')` at the foot of `ProfileCreateConsoleTest.php`, under its own section banner. An `active` Club at exact parity (capacity `1`, one `Active` member holding the seat, config set **inline**), an authenticated operator, the real Filament create surface (`fillForm` → `call('create')` → `assertHasNoFormErrors()` — a full Club *admits*, so there is no form error, unlike this file's other rejection tests). Then: the Profile is born `waiting_list`, and **both** `ProfileCreated` and `WaitingListJoined` carry `actor_role: newco_ops` + `actor_id` = the operator's id. Separately, the file's form-shape test gained `assertFormFieldDoesNotExist('capacity')` (and its name/comment), pinning that the birth state is decided by the domain and never composed by the operator.
- **What was deliberately NOT asserted** (design R4 — *the console pins assert what only the console can break*): the routed state's payload, the event multiset, the absent Club-row lock — all already pinned in `ProfileBirthStateRoutingTest`. Nor the grand-total event count: design.md **Non-Goals** names the birth-path grand-total as a remaining low-severity SUGGESTION, so `sole()` per event name is the ceiling here.
- **Files changed.** `tests/Feature/Modules/OperatorPanel/Parties/ProfileCreateConsoleTest.php` (+79/−4); `knowledge/testing/hypotheses.md` (a confirmation MERGE + a count correction — see below). **No `app/` file** — `git diff --name-only | grep -c '^app/'` ⇒ `0`, per design R1 / Non-Goals.
- **Quality loop: green.** Pint clean (no reformat). File: 8/8. Full suite **SQLite 2388/2388** (12 396 assn) and **PG17 2388/2388** (12 403 assn) — both baselines up by exactly 1 test and 10 assertions, so nothing else moved. PHPStan max **0**. `openspec validate parties-hero-package-residuals --strict` green. Re-run green after every mutant was restored.
- **Mutation (design R5) — four mutants, because the pin is four independent claims.** `CreateProfile.php` and `ProfileResource.php` were each `cp`'d aside first and `diff`'d byte-identical after (`md5 25e6bb8a…`).
  - *Mutant A* (the one tasks.md prescribes — birth state hardcoded to `Applied`): **10 red**, of which **9 are pre-existing** (`ProfileBirthStateRoutingTest` ×7, `ProfileWaitlistDeclineTest`, and the structural `countOccupiedSeats(` source-grep). Loud and uninformative, exactly as § Codebase Patterns predicts: it proves the pin *fires*, not that it is *needed*. My pin reds at the `state` line (240), so mutant A never reaches the envelope conjuncts at all — it falsifies **none** of what this task exists to add.
  - *Mutant B1* (the necessity mutant, conjunct 1 — `actorRole: ActorRole::System` on `CreateProfile.php:181`'s `WaitingListJoined` record call): **exactly 1 failure repo-wide**, at the `actor_role` line, **12 395** assertions (baseline −1 ⇒ the chain aborted and conjunct 2 never ran). The other 2387 tests were green: the repository was blind.
  - *Mutant B2* (conjunct 2 — `actorId: null`, same call): **exactly 1 failure repo-wide**, at the `actor_id` line, **12 396** assertions (= baseline ⇒ conjunct 1 ran and passed, so conjunct 2 was genuinely reached). Two numbers, and the short-circuit is measured rather than assumed.
  - *Mutant C* (a `capacity` `Select` added to `ProfileResource::form()`, labelled with an existing i18n key so the hardcoded-copy scanner cannot red it for the wrong reason): **exactly 1 failure of 2388** — the form-shape pin. The whole repo was blind to an operator-composable capacity field.
- **The finding — and the near-miss.** Mutant C reported `line: 247`, which is a **comment inside a different test**, 114 lines past the failing test's closing brace. Read as a line number it says *"the form-shape pin didn't fire; something in the new test broke instead"* — i.e. *the pin is dominated, delete it*. The test **name** and the message (*"Failed asserting that a field with the name [capacity] does not exist on the form"*) say the opposite, unambiguously. Promoted to § Codebase Patterns: on this surface, locate a mutant's red by name + message, never by the reported line.
- **A knowledge-base correction, not a promotion.** The short-circuit hypothesis stood at **2/3**, but *both* of its confirmations were tasks 2.1 and 2.2 of **this same change** — while its own criterion reads *"need 1 more distinct change"*, and the one-change-one-date rule (`.claude/CLAUDE.md`; spelled out verbatim in the NON-gate hypothesis: *"one change, one confirmation … but two independent demonstrations"*) makes a change worth exactly one. Left alone, my 3.1 measurement would have read as 3/3 and promoted a hypothesis to a **rule** on the strength of a single change — the precise failure the 3× threshold exists to prevent. I merged 2.1 / 2.2 / 3.1 into ONE dated confirmation carrying three demonstrations and set the count to **1/3**. The evidence grew; the count shrank. That is the rule working.
- **Learnings for future iterations:**
  - **`grep`ping an event NAME does not tell you whether its envelope is pinned.** `WaitingListJoined.actor_role` was already asserted `newco_ops` — at `ApproveProfile`'s divert. `CreateProfile`'s birth call site was blind to it, and `actor_id` was blind at both. Locate the `record()` **call site**. (§ Codebase Patterns.)
  - **A dominated conjunct is worth keeping when the requirement names it, and only if the comment says why.** `ProfileCreated`'s envelope here is dominated by this file's applied-path test (same unconditional `record()` call) — no realistic mutant reaches it. Kept, with the enumeration written in place, per the 2.2 corollary.
  - Task **3.2** touches this same console surface: expect the misleading-line artefact on its `title`/`status`/`body` mutants, and read `session('filament.notifications')` via a file-distinct helper name (`approvalConsoleToasts()` is already taken, process-wide, by `ProfileApprovalConsoleTest`).
---
