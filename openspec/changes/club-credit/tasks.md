# Tasks — club-credit

> Dependency order: schema/enum → entity/model → writer Actions (issue → apply → forfeit/restore) → §11.4 guard + i18n + docs → full-suite gate. Tests are **never optional** (`.claude/skills/references/acceptance-criteria.md`). Every DB-touching task is verified on **PostgreSQL 17** in addition to SQLite. The four writers are **audit-only** (no domain event — design L3); never call `DomainEventRecorder` for a Club Credit state write, and never create a `MembershipFeePaid` / `ClubCredit*` event class.

## 1. Schema & Entity

- [x] 1.1 **`ClubCreditState` enum** (`app/Modules/Parties/Enums/ClubCreditState.php`): cases `Active = 'active'`, `Redeemed = 'redeemed'`, `Forfeited = 'forfeited'`. Add helper(s) mirroring the sibling enums (e.g. `isActive()` / `isTerminal()` where `Redeemed`/`Forfeited` are terminal-for-forfeiture but `Redeemed` is restore-reachable — document the nuance).
  - Acceptance: enum is a string-backed `enum`; values are lowercase verbatim; helpers covered by a small unit test. Typecheck passes; tests pass.
- [x] 1.2 **Migration** `database/migrations/<ts>_create_parties_club_credits_table.php`: table `parties_club_credits` with `id`; `profile_id` (FK → `parties_profiles`, `restrictOnDelete()` — shared parent, design L1); MoneyCast columns `amount_minor` (integer) + `amount_currency` (string 3), `remaining_minor` + `remaining_currency`; `valid_from` + `valid_to` (`timestamp`/datetime); `state` (string, default none — set by the Action); `timestamps`. Add the **partial unique index** `parties_club_credits_one_active_per_profile ON parties_club_credits (profile_id) WHERE state = 'active'` via raw `DB::statement('CREATE UNIQUE INDEX … WHERE state = \'active\'')` on **both** engines (the `2026_06_15_000007_create_parties_profiles_table.php` precedent — not driver-guarded). Reversible `down()`.
  - Acceptance: migrates clean on SQLite **and PostgreSQL 17**; the partial index exists and a second `active` row for one `profile_id` is rejected while a `redeemed`/`forfeited` row frees the slot (assert in 1.3's model test or a dedicated migration test). `down()` drops table + index. Typecheck passes; tests pass.
- [x] 1.3 **`ClubCredit` model** (`app/Modules/Parties/Models/ClubCredit.php`) + **factory** (`database/factories/Parties/ClubCreditFactory.php`): `protected $table = 'parties_club_credits'`; `$guarded = []` (persistence-only — Actions are sole writers); casts `amount`/`remaining` → `MoneyCast::class`, `state` → `ClubCreditState::class`, `valid_from`/`valid_to` → `immutable_datetime`; within-module `profile()` `belongsTo`; `@property` docblock. Factory default: an `active` credit on a credit-generating Club's fee currency, `remaining` = `amount`.
  - Acceptance: `ClubCredit::factory()->create()` round-trips a `Money` with no precision loss; `state` casts to the enum; `profile()` resolves. Feature test asserts the one-active partial index (two `active` → rejected; terminal → slot freed). Typecheck passes; tests pass.
- [x] 1.4 **`Profile::activeClubCredit()`** within-module relation (`app/Modules/Parties/Models/Profile.php`): a `hasOne(ClubCredit::class)` scoped to `state = 'active'` (the at-most-one active credit). Within-module relation only (both Module K — no boundary breach).
  - Acceptance: returns the active credit when present, `null` otherwise; a `redeemed`/`forfeited`-only Profile returns `null`. Covered by a Profile test. Typecheck passes; tests pass.

## 2. Issuance

- [x] 2.1 **`IssueClubCredit` Action** (`app/Modules/Parties/Actions/IssueClubCredit.php`) + exceptions (`Exceptions/IllegalClubCreditTransition.php` and/or a `ClubCreditIssuancePrecondition` exception): one `DB::transaction`; re-read the Profile + its Club `lockForUpdate()`; guard `Club.generates_credit === true` (else reject) and `Club.fee !== null` (else reject — design L2 fee-null guard); create the credit `active` with `amount = Club.fee` verbatim, `remaining = amount`, `valid_from = CarbonImmutable::now()`, `valid_to = CarbonImmutable::now()->endOfYear()`. **Audit-only**: no `record(...)`. Docblock pins the deferred Module-E `MembershipFeePaid` seam + the K.18/K.19 retained seams (mirror `ActivateProfile`/`LapseProfile` docblock style).
  - Acceptance: issues an `active` credit with `amount`/`currency` = the Club's fee, `remaining` = `amount`, `valid_to` = 31 Dec of the issuance year; the partial index makes a re-issue while `active` exists fail (one-active). Typecheck passes; tests pass.
- [x] 2.2 **`IssueClubCredit` tests** (`tests/Feature/Modules/Parties/ClubCreditIssuanceTest.php`): happy path (amount = fee, remaining = amount, valid_to = year-end, state active); **reject** when `generates_credit = false` (no row); **reject** when `fee` is null (no row); issuance **not** blocked when an active Hold covers the Profile (issuance asymmetry — no Hold check); **no `domain_events` row** is recorded by the issuance (assert count of any event by this Action is 0). Verify on PostgreSQL 17.
  - Acceptance: all cases asserted incl. the no-event assertion; PG17 lane green. Typecheck passes; tests pass.

## 3. Redemption & Carry-Forward

- [x] 3.1 **`ApplyClubCredit` Action** (`app/Modules/Parties/Actions/ApplyClubCredit.php`): inputs `clubCreditId` + redeemed `Money`. One `DB::transaction`; `lockForUpdate()` re-read the credit + its Profile. Guards before any write: credit `state === Active`; redeemed `currency` equals credit currency (explicit equality → localized exception, so `Money::minus` never throws); redeemed `minorUnits <= remaining.minorUnits`; Profile `state !== Suspended` (freeze — AC-K-FSM-2a). Then `remaining = remaining->minus($redeemed)`; if `remaining.minorUnits === 0` → `state = Redeemed`, else stay `Active` (K.17 carry-forward). **Audit-only**: no event. Docblock pins the Module-S checkout/mutual-exclusion/Hold-gate seam.
  - Acceptance: partial leaves reduced `remaining` + `active`; full → `redeemed`; the four guards each reject pre-write with state/remaining unchanged. Typecheck passes; tests pass.
- [x] 3.2 **`ApplyClubCredit` tests** (`tests/Feature/Modules/Parties/ClubCreditRedemptionTest.php`): partial redemption (25000 → redeem 9000 → remaining 16000, still active); full redemption (→ redeemed, remaining 0); over-application (redeem > remaining → rejected, unchanged); currency mismatch → rejected; **freeze**: redeem while Profile `Suspended` → rejected, then succeeds after restore to `Active`; no `domain_events` row recorded. Verify on PostgreSQL 17.
  - Acceptance: all cases asserted incl. no-event + freeze-then-restore; PG17 green. Typecheck passes; tests pass.

## 4. Forfeiture & Restoration

- [x] 4.1 **`ForfeitClubCredit` Action** (`app/Modules/Parties/Actions/ForfeitClubCredit.php`): one `DB::transaction`; `lockForUpdate()` re-read; guard `state === Active` (else reject); set `state = Forfeited` (terminal). **Audit-only**: no event. Docblock pins the four §11.3 triggers as deferred seams (year-end scheduler, renewal forfeit-before-issue, Profile-cancellation cascade, Club-closure → Module-S DEC-043 conversion — AC-K-XM-23) and notes the forfeit-before-issue ordering is provable via the one-active invariant.
  - Acceptance: `active → forfeited`; forfeiture on a non-active credit rejected; `forfeited` is terminal (a second forfeit/apply/restore rejected). Typecheck passes; tests pass.
- [x] 4.2 **`RestoreClubCredit` Action** (`app/Modules/Parties/Actions/RestoreClubCredit.php`): one `DB::transaction`; `lockForUpdate()` re-read the credit + its Profile; guard `state === Redeemed` (else reject) **and** the Profile has no other `active` Club Credit (else reject — one-active respected, design L7); set `state = Active`. **Audit-only**: no event. Docblock pins the Module-S order-cancellation-window trigger as the seam.
  - Acceptance: `redeemed → active` when no other active credit; rejected when the credit is not `redeemed`; rejected when another `active` credit exists for the Profile. Typecheck passes; tests pass.
- [ ] 4.3 **Forfeiture/Restoration tests** (`tests/Feature/Modules/Parties/ClubCreditForfeitureTest.php`): forfeit active → forfeited (terminal — second transition rejected); **forfeit-before-issue ordering**: with an active credit, `IssueClubCredit` rejects (one-active), then `ForfeitClubCredit` + `IssueClubCredit` succeeds; restore redeemed → active; restore rejected when not redeemed; restore rejected when another active credit exists; no `domain_events` row recorded by any of these. Verify on PostgreSQL 17.
  - Acceptance: all cases asserted incl. the ordering pair + no-event; PG17 green. Typecheck passes; tests pass.

## 5. §11.4 boundary, i18n, docs & gate

- [ ] 5.1 **§11.4 audit-only guard test** (`tests/Feature/Modules/Parties/ClubCreditEventOwnershipTest.php`): assert **no** event class named `MembershipFeePaid` / `ClubCreditIssued` / `ClubCreditApplied` / `ClubCreditRestored` / `ClubCreditForfeited` exists under `app/Modules/Parties/Events` (mirror the `SupplyLifecycleChainTest` event-non-existence loop), and assert that running each of the four writers records **zero** `domain_events` rows. (No shipped forbidden-list pre-names these — design L9 — so this is a NEW guard, not a realignment.)
  - Acceptance: the class-absence loop + the zero-event assertions pass. Typecheck passes; tests pass.
- [ ] 5.2 **i18n** (`lang/en/parties.php`): add a `club_credit` group with the localized exception messages (issuance refused — no credit policy / no fee; illegal transition; currency mismatch; over-application; frozen-while-suspended; restore one-active-conflict). No hardcoded user-facing strings (invariant 12).
  - Acceptance: every thrown localized message resolves via `__('parties.club_credit.…')`; English baseline only (DEC-127). Typecheck passes; tests pass.
- [ ] 5.3 **Docs**: refine the **CONTEXT.md** Club Credit glossary entry (the entity + the four within-module writers + the audit-only §11.4 boundary) and the deferred-seam notes in the `ActivateProfile` / `RenewProfile` / `SuspendProfile` / `SuspendCustomer` docblocks' neighbouring CONTEXT.md sections (the `club-credit` freeze seam is now closed; record the residual Module-E/S/scheduler/cancellation-cascade seams). Add `knowledge/data-model` or `knowledge/module-k` notes as warranted (the audit-only-writer-for-Module-E-owned-events pattern; the one-active partial-index + restore interaction).
  - Acceptance: CONTEXT.md reflects the landed entity + residual seams; no stale "club-credit unbuilt" claim remains for the freeze. (Docs task — no test, but it must not break the build.)
- [ ] 5.4 **Full-suite gate**: `php -d memory_limit=-1 artisan test` green on SQLite; the new DB-touching tests also green on the **PostgreSQL 17** lane; `vendor/bin/phpstan analyse` (max) 0 errors; `vendor/bin/pint --test` clean. Then `openspec validate club-credit --strict` passes.
  - Acceptance: full suite green both engines; PHPStan max 0; Pint clean; `openspec validate --strict` green.
