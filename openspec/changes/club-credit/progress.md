# Progress — club-credit

## Codebase Patterns
(consolidated reusable patterns — read first each iteration)

- **Parties state-enum idiom (sibling templates `KycStatus`/`HoldType`):** a `string`-backed `enum` in `app/Modules/Parties/Enums/`, **no `declare(strict_types=1)`** (no sibling has it), case names PascalCase, backing values the lowercase persisted token. A class-level docblock cites the PRD § + the verbatim FSM. Domain rules live as **one-line predicate methods** (`clears()`, `autoLiftable()`, here `isActive()`/`isTerminal()`) each with its own docblock — these are the readable from-state guards the writer Actions call. Test in a domain-grouped `tests/Unit/Modules/Parties/Enums/{Domain}EnumsTest.php` (Pest `it()` style) asserting the case→value map **verbatim + order-sensitive via `toBe([...])`** + `toHaveCount(n)`, a `from()` round-trip, the predicate truth tables, and `from('bogus')->toThrow(ValueError::class)`.
- **Full-suite invocation — use `php -d memory_limit=-1 vendor/bin/pest`, NOT `php -d memory_limit=-1 artisan test`.** The `artisan test` wrapper (laravel/pao) spawns the real test process *without* inheriting the outer `-d memory_limit=-1`, so it OOMs at the 128 MB default (fatal in `filament/.../routes/web.php` during `setUp()`). Invoking `vendor/bin/pest` directly keeps the run in one unlimited-memory process. Quality cmd `php artisan test` (root CLAUDE.md) is nominal; this is the memory-safe equivalent.
- **Parties migration idiom (templates `2026_06_15_000003_create_parties_clubs_table.php` / `…000007_create_parties_profiles_table.php`):** rich class docblock citing the PRD §/design; `Schema::create` with within-module `foreignId(...)->constrained(table: '…', indexName: '<short>_fk')` (+ explicit `->restrictOnDelete()` for a shared parent); Money via two columns `{key}_minor` (`integer`) + `{key}_currency` (`string(3)`) — never a float; `timestampTz`/`timestampsTz` for datetimes; a value-set `state`/`status` column as `string` + the enum cast, with a **PG-only CHECK** (`if (DB::getDriverName() === 'pgsql')`) whose accepted set derives from `Enum::cases()` so it can't drift; any **partial unique index** via raw `DB::statement('CREATE UNIQUE INDEX <name> ON <t> (<cols>) WHERE <pred>')` run on **BOTH** engines (NOT driver-guarded — identical SQLite/PG syntax), with the predicate literal derived from the enum (`Enum::Case->value`). Index names < 63 chars (PG limit). `down()` = bare `Schema::dropIfExists`.
- **Raw-insert schema test for a migration whose model lands later (template `tests/Feature/Platform/EventDeliveriesSchemaTest.php`):** name it `{Entity}SchemaTest.php`, `uses(RefreshDatabase::class)`, a top-level `function {entity}Row(int $parentId, array $overrides = [])` helper (UNIQUELY named — top-level test funcs are global, collide across files). Prove: `Schema::hasColumns`; **partial unique index by NAME** via `Schema::hasIndex($t, '<index_name>')` (getIndexes() exposes name+cols but NOT the predicate on either engine, so name is the portable proof — a bad predicate would abort the migration and red the file); constraint rejection via `expect(fn () => DB::transaction(fn () => DB::table($t)->insert(...)))->toThrow(QueryException::class)` (the `DB::transaction` wrap = SAVEPOINT, so PG's transaction-abort stays isolated and a post-throw count check is valid); a NOT-NULL/no-default floor by `unset($row['col'])` then expecting a throw; FK orphan rejection with a non-existent parent id (SQLite enforces FKs here).
- **Don't down-test a Laravel migration.** The base `Illuminate\Database\Migrations\Migration` declares **no** `up()`/`down()` (the migrator calls them by reflection), so a `require`d migration is `mixed` → `instanceof Migration` narrows to a type that *still* lacks `up()`/`down()` → PHPStan max reds either way (`method.nonObject`, then `method.notFound`). No `parties_*` sibling down-tests; `down()` is a trivial `Schema::dropIfExists` (drops the table and, with it, every dependent index). Assert the forward/up state only; treat `down()` correctness as structural. Cost me two PHPStan cycles before reverting.
- **Scoped within-module `hasOne` relation + nullable-accessor test idiom (1.4):** a state-filtered single-child relation is `return $this->hasOne(Related::class)->where('col', Enum::Case->value);` with declared return `HasOne` + docblock `@return HasOne<Related, $this>` — larastan 3.x preserves the relation generic THROUGH the `->where()` `__call` forward (no precedent needed; PHPStan max stays 0). Because a scoped `hasOne` genuinely resolves to `null`, the class `@property-read` is `Related|null $rel` (contrast a required `belongsTo`, which larastan types non-null, so `ProfileTest` can chain `->customer->is(...)` directly). **The null type then bites the TEST:** Pest's `expect($rel)->not->toBeNull()` does NOT narrow `$rel` for PHPStan max, so a following `$rel->method()`/`$rel->prop` reds with `method.nonObject`/`property.nonObject`. Use the suite-wide `?->` idiom (`$rel?->is($x)`, `$rel?->state`) for the chained accesses — it's analyzer-safe AND stays strict (the explicit `->not->toBeNull()` is the behavioral check; a surprise null makes `?->…` yield null and the following `toBeTrue()`/`toBe()` fail).
- **A NEW non-`Create` Parties Action MUST be registered in `SupplyLifecycleChainTest`'s scope-guard whitelist (NEW, 2.1):** `tests/Feature/Modules/Parties/SupplyLifecycleChainTest.php` runs an EXHAUSTIVE `expect($transitions)->toEqualCanonicalizing([...])` over every Action whose class name does NOT start with `Create` (`! str_starts_with($name, 'Create')`, globbed from the filesystem). So any new Action named `Issue*`/`Apply*`/`Forfeit*`/`Restore*` (anything non-`Create*`) lands in that set and **reds the exact-match** until added to one of the `$…Transitions` arrays + spread into the assertion. Add ONLY the Actions that EXIST this iteration (the glob is filesystem-truth — pre-listing a not-yet-created sibling over-specifies and fails the other way). I added a `$clubCreditWriters = ['IssueClubCredit']` array; **the next Club Credit Action tasks each append their writer here** — 3.1 `ApplyClubCredit`, 4.1 `ForfeitClubCredit`, 4.2 `RestoreClubCredit`. (Design L9's grep checked only the FORBIDDEN-name lists and missed this allow-list, so this is routine guard-test maintenance, not a realignment — and it's the SAME maintenance every prior Action slice did, per that test's own comments.)
- **Exception split for a guarded transition Action: from-state vs. precondition (NEW, 3.1):** the Parties idiom has TWO exception families and a value-moving Action uses BOTH. (1) `Illegal{Entity}Transition` (e.g. `IllegalProfileTransition`, `IllegalClubCreditTransition`) holds the FSM **from-state** guards — `cannot{Edge}({Entity}State $from)`, `:state` = `$from->value`, one factory per edge, grown one edge per task. (2) `{Entity}{Operation}Precondition` (e.g. `ClubCreditIssuancePrecondition`, `ClubCreditRedemptionPrecondition`) holds the **value/context** guards that reject a credit that IS in the right from-state (currency mismatch, over-application, freeze-while-suspended). Both `extend RuntimeException`, both localize via `(string) __('parties.{group}.{key}', [...])` with the keys landing in the i18n task (tests assert by CLASS until then). Interpolate ONLY operator-facing identity/business tokens (ids, enum values, ISO currency codes) — NEVER money minor-units/names (PII/financial-data discipline, matching the `:club` sibling). Order the guards from-state → currency → over-application → freeze so each test isolates exactly one (currency BEFORE over-application so a wrong-currency redeem never reaches the meaningless cross-currency minor-unit compare).
- **`Club.fee` is `Money|null` → never pass it straight to `Money::equals()`/`minus()` in a test (NEW, 2.1):** `@property Money|null $fee` means `$club->fee` / `$profile->club->fee` is nullable, and PHPStan max reds `argument.type` when it reaches a `Money`-typed param (and `expect()->not->toBeNull()` does NOT narrow — the suite-wide trap). For issuance/redemption tests, build an explicit non-null `$fee = Money::of(…)` LOCAL, pass it to `Club::factory()->create(['fee' => $fee])`, and assert against that local — it both satisfies the analyzer AND makes "amount = THE Club's fee" provable (you gave the Club that exact fee). The `IssueClubCredit` Action narrows the same nullable via a control-flow `if ($fee === null) throw …` (the fee-null guard) before the create, so `$fee` is `Money` at the call site — no cast/assert needed.
- **A terse task step can under-specify vs the delta-spec SCENARIO it cites — the scenario is the acceptance authority (NEW, 4.2):** task 4.2's step said only "set `state = Active`", but the delta-spec scenario (`specs/party-registry/spec.md` → "Restore a redeemed credit, one-active-respecting") says the credit "returns to `active` with its **`remaining` restored**" and design **L7** says restore "re-activates a `redeemed` credit **and restores its `remaining`**". A `redeemed` credit ALWAYS has `remaining = 0` (`ApplyClubCredit` reaches `redeemed` ONLY when the balance hits zero — a partial redemption stays `active`), so "restored" can only mean `remaining = amount` (full face value — full redemption is the § 11.2 norm; the entity has no per-order ledger, and per-order partial restore is the Module-S seam). `RestoreClubCredit` therefore writes BOTH `state = Active` AND `remaining = amount`. **Rule: when a task step is terse on FIELD WRITES, read the delta-spec scenario's THEN clauses + the cited design decision before coding — the scenario is the acceptance contract, and money/balance-adjacent writes (invariant 6) especially must trace to it, not to the headline verb.**

---

## [2026-06-23 10:16] — 1.1 ClubCreditState enum
- Implemented the `ClubCreditState` string enum (`active`/`redeemed`/`forfeited`) — the § 11 Club Credit FSM `active → redeemed | forfeited`. Two predicates: `isActive()` (live/value-mutable from-state for Apply + Forfeit guards) and `isTerminal()` (absolutely terminal ≡ `forfeited` only). Docblock pins the terminality nuance: `redeemed` is "terminal for forfeiture" but restore-reachable to `active`, so NOT absolutely terminal — only `forfeited` is.
- Files changed: `app/Modules/Parties/Enums/ClubCreditState.php` (new), `tests/Unit/Modules/Parties/Enums/ClubCreditEnumsTest.php` (new, 5 tests / 12 assn), `openspec/changes/club-credit/tasks.md` (1.1 → [x]).
- Quality loop: **green** — Pint clean; new test 5/5; full suite **1500/1500 (8275 assn)** via `vendor/bin/pest`; PHPStan max 0 err; `pint --test` clean; `openspec validate club-credit --strict` valid. (No DB touch → no PG17 lane this task.)
- **Learnings for future iterations:**
  - Both `Codebase Patterns` above were discovered here (state-enum idiom; the `vendor/bin/pest` memory-safe full-suite command). Next iteration (1.2 migration) is the first DB-touching task → must verify on PostgreSQL 17 too; the partial-unique-index precedent is `2026_06_15_000007_create_parties_profiles_table.php` (raw `DB::statement('CREATE UNIQUE INDEX … WHERE …')`, not driver-guarded).
  - `isActive()` is consumed by the Apply + Forfeit from-state guards (tasks 3.1/4.1); `isTerminal()` documents the FSM and is exercised by the forfeiture-terminality test (4.3). Use `$credit->state->isActive()` rather than `=== ClubCreditState::Active` in those Actions for readability parity with the siblings.
---

## [2026-06-23 10:40] — 1.3 ClubCredit model + factory
- Implemented the `ClubCredit` Eloquent model (`app/Modules/Parties/Models/ClubCredit.php`) + `ClubCreditFactory` (`database/factories/Parties/ClubCreditFactory.php`), the persistence-only entity over `parties_club_credits` (task 1.2's table). Model: `$table='parties_club_credits'`, `$guarded=[]`, casts `amount`/`remaining`→`MoneyCast`, `state`→`ClubCreditState`, `valid_from`/`valid_to`→`immutable_datetime`; within-module `profile()` `belongsTo`; `newFactory()` override + `@property` docblock pinning the audit-only/sole-writer design (L1/L3/L4). Factory default: an `active` 25000-EUR credit (mirrors the `ClubFactory` default fee — full-fee→full-credit, L2), `remaining`=`amount`, `valid_to`=`endOfYear()`, on a `Profile::factory()` whose default Club has `generates_credit=true`; `state` set explicitly (NO DB default).
- Files changed: `app/Modules/Parties/Models/ClubCredit.php` (new), `database/factories/Parties/ClubCreditFactory.php` (new), `tests/Feature/Modules/Parties/ClubCreditTest.php` (new — 3 tests / 19 assn), `openspec/changes/club-credit/tasks.md` (1.3 → [x]).
- Quality loop: **green** — Pint clean; new test 3/3; full suite **1510/1510 (8303 assn)** via `vendor/bin/pest`; PHPStan max 0 err; `pint --test` clean; `openspec validate club-credit --strict` valid.
- **PostgreSQL 17:** the one-active partial-index re-assertion runs Postgres-truthful by construction (raw `CREATE UNIQUE INDEX … WHERE` from 1.2 is engine-identical; the test only adds the Eloquent write path over it). The CI `tests-pgsql` lane verifies on the human push (the loop never pushes).
- **Learnings for future iterations:**
  - Two traps re-confirmed (both already in lessons/knowledge — applied, not re-logged): (1) a forward-reference `{@see \App\…\IssueClubCredit}` in the model docblock → Pint's `fully_qualified_strict_types` hoisted it to a real `use` of a class that doesn't exist yet (lands in 2.1) → would red PHPStan; reworded to plain backticks `` `IssueClubCredit` `` (2026-06-13 lesson → `knowledge/laravel/rules.md` "forward-refs"). (2) raw-column reads must use `DB::table(...)->value('col')` (returns the scalar), NOT `->first()->prop` (stdClass|null → `property.nonObject` under PHPStan max — the ClubTest/2026-06-20 idiom).
  - Model+factory idiom for Parties entities (template: `Club`/`Profile` + their factories): `newFactory()` override + `@property` docblock; factory passes `Money` objects straight through `MoneyCast` (`'amount' => Money::of(...)`), `belongsTo` parent via a nested factory (`'profile_id' => Profile::factory()`), and a value-set column set explicitly when its DB default is absent. The one-active invariant test through the Eloquent path = factory-create one `active`, then `DB::transaction(fn () => factory-create a second active for the same `profile_id`)->toThrow(QueryException)`; free the slot with `$credit->update(['state'=>Redeemed])` then a fresh active inserts.
  - Next (1.4): `Profile::activeClubCredit()` = `hasOne(ClubCredit::class)->where('state', ClubCreditState::Active->value)` (the at-most-one active credit; within-module, no boundary breach). Then 2.x issuance Actions consume `state->isActive()`.
---

## [2026-06-23 10:31] — 1.2 create_parties_club_credits_table migration
- Implemented the `parties_club_credits` migration: `id`; within-module `profile_id` FK → `parties_profiles` `->restrictOnDelete()` (shared parent, design L1); MoneyCast pairs `amount_minor`/`amount_currency` + `remaining_minor`/`remaining_currency` (`integer`+`string(3)`, NOT NULL — a credit always has an amount, contrast nullable `Club.fee`); `valid_from`/`valid_to` `timestampTz`; `state` `string` with **NO default** (the Action is the sole writer — the `registration_flow_type` classifier idiom, not the born-state default); `timestampsTz`. The one-active invariant is the **partial unique index** `parties_club_credits_one_active_per_profile (profile_id) WHERE state = 'active'` via raw `DB::statement` on BOTH engines (predicate token from `ClubCreditState::Active->value`), plus a PG-only `state` CHECK derived from `ClubCreditState::cases()`. Mirrors the `parties_profiles`/`parties_clubs` migrations verbatim.
- Files changed: `database/migrations/2026_06_23_000001_create_parties_club_credits_table.php` (new), `tests/Feature/Modules/Parties/ClubCreditSchemaTest.php` (new — 7 tests / 9 assn), `openspec/changes/club-credit/tasks.md` (1.2 → [x]).
- Quality loop: **green** — Pint clean; new test 7/7; full suite **1507/1507 (8284 assn)** via `vendor/bin/pest`; PHPStan max 0 err; `pint --test` clean; `openspec validate club-credit --strict` valid.
- **PostgreSQL 17:** no local PG → verified Postgres-truthful **by construction** (raw `CREATE UNIQUE INDEX … WHERE` is identical SQLite/PG syntax; the PG-only CHECK is `DB::getDriverName()`-guarded; `restrictOnDelete`/`timestampTz`/`integer` all PG-truthful). The CI `tests-pgsql` lane verifies on the human push (development.md §tests-pgsql — the loop never pushes). Confident: byte-for-byte the same idiom as the PG17-green `parties_profiles` partial index + value-set CHECK.
- **Learnings for future iterations:**
  - The three Codebase Patterns above (Parties migration idiom; raw-insert schema test; don't-down-test) were discovered/confirmed here. Task 1.3 (model + factory) re-asserts the one-active index **through the Eloquent path** (its acceptance asks for it) — that's a legitimate second proof at the model layer, not a duplicate to skip; the factory default is an `active` credit, `amount` = a credit-Club's fee (ClubFactory default `Money::of(25000, EUR)`), `remaining` = `amount`.
  - `state` has **no DB default** by design — `ClubCredit::factory()` and `IssueClubCredit` MUST set it explicitly; a bare insert omitting `state` is a NOT-NULL violation (pinned by a test). Casts in 1.3: `amount`/`remaining` → `MoneyCast`, `state` → `ClubCreditState`, `valid_from`/`valid_to` → `immutable_datetime`.
---

## [2026-06-23 10:51] — 1.4 Profile::activeClubCredit() within-module relation
- Added the `Profile::activeClubCredit()` relation — a WITHIN-module `hasOne(ClubCredit::class)->where('state', ClubCreditState::Active->value)` (the at-most-one `active` credit; inverse of `ClubCredit::profile()`, both Module K → no boundary breach). It rides the structural one-active partial index (design L1), so it resolves to a single credit or `null`. Added the `ClubCreditState` + `HasOne` imports, the `@property-read ClubCredit|null $activeClubCredit` annotation, and a class-docblock prose note; the model stays persistence-only (the four writer Actions remain the sole `state` writers).
- Files changed: `app/Modules/Parties/Models/Profile.php` (relation + imports + docblock), `tests/Feature/Modules/Parties/ProfileActiveClubCreditTest.php` (new — 5 tests / 10 assn), `openspec/changes/club-credit/tasks.md` (1.4 → [x]).
- Quality loop: **green** — Pint clean; new test 5/5; full suite **1515/1515 (8313 assn)** via `vendor/bin/pest`; PHPStan max 0 err; `pint --test` clean; `openspec validate club-credit --strict` valid. (No schema/DB-DDL change — the relation reads task 1.2's table; no new PG17 surface, the read SQL is engine-identical.)
- **Learnings for future iterations:**
  - One PHPStan cycle: the nullable-`hasOne` `@property-read` (honest — a scoped relation CAN be null) made the test's chained `$active->is(...)`/`$active->state` red under PHPStan max because `expect()->not->toBeNull()` doesn't narrow. Fixed with the suite-wide `?->` idiom — captured as the new Codebase Pattern above. Future scoped-relation tasks (Module S, Catalog, etc.) hit the same; apply the pattern up front.
  - Tests assert: present → returns the credit (`?->is`); none/redeemed-only/forfeited-only → `null`; and the discriminator (redeemed + active coexist → returns ONLY the active, proving the scope filters on `state`). The redeemed coexistence fixture is created FIRST (terminal frees the one-active slot) then the active — the same ordering the issuance/forfeit-before-issue tests (2.x/4.x) will use.
  - Next (2.1): `IssueClubCredit` Action — `amount = Club.fee` verbatim, `remaining = amount`, `valid_to = now()->endOfYear()`; guard `generates_credit === true` AND `fee !== null` (design L2 fee-null guard); audit-only (no `record(...)`). It will read `$profile->activeClubCredit` / the one-active index to reject a re-issue while active.
---

## [2026-06-23 11:10] — 2.1 IssueClubCredit Action
- Implemented `IssueClubCredit` — the SOLE creator of a Club Credit row, AUDIT-ONLY (no domain event, mirrors `SuspendAccount`/`CloseAccount`/`RecordKycVerified`; injects NEITHER `DomainEventRecorder` NOR `ActorContext`). One `DB::transaction`: re-reads the Profile + its Club `lockForUpdate()`, guards `generates_credit` (else `ClubCreditIssuancePrecondition::clubDoesNotGenerateCredit`) and a non-null `fee` (else `::clubHasNoFee` — design L2 fee-null guard, which also narrows `$fee` to `Money` for the create), then creates an `active` credit with `amount = fee` verbatim, `remaining = amount`, `valid_from = CarbonImmutable::now()`, `valid_to = now()->endOfYear()`. No app-level one-active pre-check — the partial unique index is the structural guard (design L1); a re-issue while `active` violates it and the transaction rolls back. Docblock pins the deferred Module-E `MembershipFeePaid` seam + the K.18 (`amount = fee` IS the scaling hook) / K.19 (writer is the retained manual-issuance seam) seams (`ActivateProfile` docblock style).
- Files changed: `app/Modules/Parties/Actions/IssueClubCredit.php` (new), `app/Modules/Parties/Exceptions/ClubCreditIssuancePrecondition.php` (new — two static factories, localized via `parties.club_credit.issuance_no_credit_policy`/`…_no_fee`, keys land in task 5.2 — `__()` returns the key string meanwhile, tests assert the class), `tests/Feature/Modules/Parties/ClubCreditIssuanceTest.php` (new — 2 tests / 17 assn: happy path + one-active re-issue rejection), `tests/Feature/Modules/Parties/SupplyLifecycleChainTest.php` (registered `IssueClubCredit` in the scope-guard whitelist — see Codebase Patterns), `openspec/changes/club-credit/tasks.md` (2.1 → [x]).
- Quality loop: **green** — Pint clean; new test 2/2; full suite **1517/1517 (8333 assn)** via `vendor/bin/pest`; PHPStan max 0 err; `pint --test` clean; `openspec validate club-credit --strict` valid.
- **PostgreSQL 17:** no DDL change (the Action writes 1.2's table; the partial-index violation path + the timestamptz validity window are engine-identical — `valid_to` asserted by the date-level `format('Y-m-d')` idiom, NOT `equalTo(endOfYear())`, so the microsecond/precision asymmetry can't flake). CI `tests-pgsql` verifies on the human push.
- **Learnings for future iterations:**
  - Two NEW Codebase Patterns above (the `SupplyLifecycleChainTest` whitelist registration — the next 3 Action tasks hit it; the nullable-`Club.fee` test idiom). Created ONLY `ClubCreditIssuancePrecondition` (issuance has no from-state to transition); `IllegalClubCreditTransition` lands in 3.1 with the first FSM from-state guard.
  - Next (2.2): expand `ClubCreditIssuanceTest.php` with the rejection matrix (`generates_credit = false` → no row; `fee = null` → no row), the Hold-asymmetry (issuance NOT Hold-gated — place an account/profile Hold, issuance still succeeds), and the §11.4 no-`domain_events`-row assertion (snapshot `DomainEvent::query()->count()` across the issue → delta 0). Assert the precondition rejections by exception CLASS (`ClubCreditIssuancePrecondition`), not message (i18n keys land in 5.2).
---

## [2026-06-23 11:33] — 3.1 ApplyClubCredit Action
- Implemented `ApplyClubCredit` — the SOLE writer of the credit `remaining` and of the `active → redeemed` transition, AUDIT-ONLY (no event; injects NEITHER `DomainEventRecorder` NOR `ActorContext`, mirrors `IssueClubCredit`). `handle(int $clubCreditId, Money $redeemed)`: one `DB::transaction`, `lockForUpdate()` re-reads the credit + its owning Profile, then FOUR guards before any write (in order): (1) credit `state` is `active` → else `IllegalClubCreditTransition::cannotApply` (the NEW FSM from-state exception); (2) `redeemed.currency === remaining.currency` (explicit — makes `Money::minus`'s mismatch unreachable, no FX in Module K) → else `ClubCreditRedemptionPrecondition::currencyMismatch`; (3) `redeemed.minorUnits <= remaining.minorUnits` (integer compare, `Money` has no `</>`) → else `::overApplication`; (4) Profile `state !== Suspended` (freeze, AC-K-FSM-2a) → else `::frozenWhileSuspended`. Then `remaining = remaining->minus($redeemed)`; `state = remaining.minorUnits === 0 ? Redeemed : Active` (K.17 carry-forward — the redundant active→active write is harmless + declarative). Docblock pins the deferred Module-S checkout seam (Offer match `credit.profile.club_id ∈ offer.club_ids`, mutual-exclusion DEC-110, auto-apply DEC-111, Hold-gated price resolution).
- Created TWO exceptions mirroring the issuance split: `IllegalClubCreditTransition` (FSM from-state guard, like `IllegalProfileTransition` — `cannotApply` now; `cannotForfeit`/`cannotRestore` land in 4.1/4.2) and `ClubCreditRedemptionPrecondition` (value/context guards, like `ClubCreditIssuancePrecondition` — `currencyMismatch`/`overApplication`/`frozenWhileSuspended`). i18n keys `parties.club_credit.{cannot_apply,currency_mismatch,over_application,frozen_while_suspended}` referenced now, land in 5.2 (`__()` returns the key meanwhile; tests assert by CLASS).
- Files changed: `app/Modules/Parties/Actions/ApplyClubCredit.php` (new), `app/Modules/Parties/Exceptions/IllegalClubCreditTransition.php` (new), `app/Modules/Parties/Exceptions/ClubCreditRedemptionPrecondition.php` (new), `tests/Feature/Modules/Parties/ClubCreditRedemptionTest.php` (new — 6 tests / 38 assn: partial K.17, full→redeemed, + the four guard rejections each proving state/remaining unchanged), `tests/Feature/Modules/Parties/SupplyLifecycleChainTest.php` (registered `ApplyClubCredit` in `$clubCreditWriters`), `openspec/changes/club-credit/tasks.md` (3.1 → [x]).
- Quality loop: **green** — Pint clean; redemption file 6/6 + guard test 5/5; full suite **1527/1527 (8368 assn)** via `vendor/bin/pest`; PHPStan max 0 err; `pint --test` clean; `openspec validate club-credit --strict` valid.
- **PostgreSQL 17:** no DDL (the Action writes 1.2's table). The redemption path is engine-identical — integer minor-unit arithmetic + the `update()` of `remaining`/`state`; no clock-sensitive validity window is asserted (redemption doesn't touch it), so no precision/tz asymmetry to flake. Each Action opens its own `DB::transaction` (SAVEPOINT under RefreshDatabase). CI `tests-pgsql` verifies on the human push.
- **Learnings for future iterations:**
  - **The redemption exception split mirrors issuance (NEW Codebase Pattern below).** `IllegalClubCreditTransition` = FSM from-state ONLY; `ClubCreditRedemptionPrecondition` = value/context preconditions on an otherwise-valid-from-state credit. 4.1 adds `IllegalClubCreditTransition::cannotForfeit`; 4.2 adds `::cannotRestore` AND must decide where the restore one-active-conflict lives (it's NOT a from-state issue — the credit IS `redeemed` — so likely a precondition; consider a `ClubCreditRestorePrecondition` or fold into `ClubCreditRedemptionPrecondition`'s sibling. Decide in 4.2).
  - Pint's `fully_qualified_strict_types` hoisted the docblock `{@see Money::minus()}` to a real `use App\Platform\Money\Money` — BENIGN here (`Money` exists; PHPStan doesn't flag docblock-only imports, Pint added it so `pint --test` is idempotent). Contrast the 1.3 trap where the hoisted class did NOT exist yet (forward-ref → PHPStan red). Rule: a `{@see Class}` to an EXISTING class is safe; to a not-yet-created sibling, use plain backticks.
  - Next (3.2): EXPAND `ClubCreditRedemptionTest.php` with the freeze-THEN-restore round-trip (suspend → reject, `ReactivateProfile` → same redeem succeeds) and the §11.4 no-`domain_events`-row delta (snapshot `DomainEvent::query()->count()`, assert `->toBe($before)` — the 2.2 idiom). The partial/full/over/currency/freeze cases already shipped in 3.1, so 3.2 adds only the round-trip + no-event (don't duplicate).

---

## [2026-06-23 11:20] — 2.2 IssueClubCredit tests
- Completed the issuance test matrix by EXPANDING `ClubCreditIssuanceTest.php` with four cases beside the 2.1 happy-path + one-active re-issue: (1) `generates_credit = false` (with a non-null fee, so it's unambiguously Precondition 1, not the fee-null guard) → throws `ClubCreditIssuancePrecondition`, **no row**; (2) `fee = null` (generates_credit = true) → throws `ClubCreditIssuancePrecondition` (fee-null guard, design L2), **no row**; (3) **§11.2 Hold-asymmetry** — an active Profile-scope `Hold::factory()` (pure fixture, bypasses PlaceHold → no event/coupling) does NOT block issuance, the credit is minted `active` and the within-module relation resolves it; (4) **§11.4 audit-only** — snapshot `DomainEvent::query()->count()` before/after the issue → **delta 0** (the writer injects no recorder). Rejections asserted by exception CLASS (i18n keys land in 5.2 — `__()` returns the key meanwhile). Updated the file docblock from "added next" to describe the now-present matrix.
- Files changed: `tests/Feature/Modules/Parties/ClubCreditIssuanceTest.php` (4 new `it()` + 5 imports + docblock), `openspec/changes/club-credit/tasks.md` (2.2 → [x]).
- Quality loop: **green** — Pint clean; file 6/6 (27 assn); full suite **1521/1521 (8343 assn)** via `vendor/bin/pest`; PHPStan max 0 err; `pint --test` clean; `openspec validate club-credit --strict` valid.
- **PostgreSQL 17:** test-only task, NO DDL — exercises 1.2's existing schema + the PG17-green `parties_holds`/`domain_events` tables via the identical `ComplianceReadApiTest`/`ClubTest` factory idioms. Precondition rejections throw BEFORE any insert (no transaction-abort to isolate); the Hold/event paths are engine-identical. Green by construction; CI `tests-pgsql` verifies on the human push.
- **Learnings for future iterations:**
  - **Hold-fixture idiom for a "covered scope" precondition (no PlaceHold needed):** when a test only needs an *inert* active Hold over a scope (not the suspension coupling), use `Hold::factory()->create(['hold_type' => HoldType::Fraud, 'scope_type' => HoldScope::Profile, 'scope_id' => $x->id])` — it bypasses `PlaceHold`, so NO `CustomerHoldPlaced` event and NO Hold→suspended coupling fire (HoldFactory is documented as a pure fixture). This keeps the asymmetry test isolated to the one variable ("an active Hold exists") without dragging in suspension/event side-effects. The same idiom is in `ComplianceReadApiTest` (line ~102). The redemption-side (3.2) freeze test, by contrast, DOES need the real suspended Profile state.
  - **No-event delta idiom:** snapshot `$before = DomainEvent::query()->count()` then assert `->toBe($before)` after — more honest than a bare `->toBe(0)` (survives a future fixture that emits). The audit-only writers (3.1/4.1/4.2) reuse this; 5.1 generalizes it to all four writers + the class-absence loop.
  - Next (3.1): `ApplyClubCredit` Action — first FSM from-state guard → create `IllegalClubCreditTransition`; register `ApplyClubCredit` in `SupplyLifecycleChainTest`'s `$clubCreditWriters` (the whitelist-registration Codebase Pattern). Four guards before any write: `state === Active`, currency equality, `redeemed.minorUnits <= remaining.minorUnits`, Profile `!== Suspended` (freeze). Audit-only.
---

## [2026-06-23 11:41] — 3.2 ApplyClubCredit tests
- Completed the redemption test matrix by EXPANDING `ClubCreditRedemptionTest.php` with the TWO cases 3.1 didn't ship (the other six — partial K.17, full→redeemed, from-state reject, currency mismatch, over-application, freeze-reject — already landed in 3.1, NOT duplicated): (1) **freeze-THEN-restore round-trip** — an `active` credit on an `active` Profile; suspend via the REAL `SuspendProfile` (`active → suspended`) → the 9000-EUR redeem throws `ClubCreditRedemptionPrecondition` (frozen), credit untouched; `ReactivateProfile` (`suspended → active`) → the SAME 9000-EUR redeem now succeeds (remaining 25000−9000=16000, stays `active`). Proves the freeze is a LIVE read of Profile state, not a permanent credit property — a genuine `active→suspended→active` round-trip through the real lifecycle Actions, NOT a Hold fixture / raw state flip. (2) **§11.4 no-event delta** — a FULL redeem (the `active → redeemed` FSM transition, the event-worthy moment) records ZERO `domain_events` rows: snapshot `$before = DomainEvent::query()->count()`, redeem, assert `->toBe($before)` (the 2.2 idiom).
- Files changed: `tests/Feature/Modules/Parties/ClubCreditRedemptionTest.php` (2 new `it()` + 3 imports: `ReactivateProfile`, `SuspendProfile`, `DomainEvent`; the file docblock already described 3.2's additions verbatim — no churn), `openspec/changes/club-credit/tasks.md` (3.2 → [x]).
- Quality loop: **green** — Pint clean; file 8/8 (31 assn); full suite **1529/1529 (8377 assn)** via `vendor/bin/pest`; PHPStan max 0 err; `pint --test` clean; `openspec validate club-credit --strict` valid.
- **PostgreSQL 17:** test-only, NO DDL. Redemption is integer minor-unit arithmetic + an `update()` (engine-identical); `SuspendProfile`/`ReactivateProfile` write `Profile.state` + record `domain_events` rows (the PG17-green suspension-slice idiom); the no-event delta uses `count()` (engine-identical). No clock-sensitive validity window asserted (redemption never touches it), so no frozen clock needed — unlike the issuance file. Green by construction; CI `tests-pgsql` verifies on the human push.
- **Learnings for future iterations:**
  - **Freeze-then-restore via the REAL lifecycle Actions** (not a born-suspended fixture): for a "state X gates Y, restore lifts it" round-trip, drive the gate state with the real transition Actions (`SuspendProfile` → `ReactivateProfile`) and re-run the IDENTICAL gated call before/after — the same input that threw now succeeds, proving the guard reads LIVE state. Cleaner than a born-suspended fixture (it proves the guard reacts to a runtime transition, closer to production). The Suspend/Reactivate emit their own events, but that's irrelevant to the round-trip test — the no-event assertion lives in a SEPARATE test that calls neither.
  - Next (4.1): `ForfeitClubCredit` Action — add `IllegalClubCreditTransition::cannotForfeit` (the second from-state edge; `cannotApply` shipped in 3.1), guard `state === Active` → set `Forfeited` (terminal), audit-only; register `ForfeitClubCredit` in `SupplyLifecycleChainTest`'s `$clubCreditWriters` (the whitelist-registration pattern). Then 4.2 `RestoreClubCredit` must decide where the restore one-active-conflict exception lives — it's NOT a from-state issue (the credit IS `redeemed`), so likely a new `ClubCreditRestorePrecondition`.
---

## [2026-06-23 11:52] — 4.1 ForfeitClubCredit Action
- Implemented `ForfeitClubCredit` — the SOLE writer of the `active → forfeited` transition, AUDIT-ONLY (no event; injects NEITHER `DomainEventRecorder` NOR `ActorContext`, mirrors `IssueClubCredit`/`ApplyClubCredit`). `handle(int $clubCreditId)`: one `DB::transaction`, `lockForUpdate()` re-read of the credit ONLY (no Profile re-read — forfeiture is NOT freeze-gated, unlike redemption: a year-end lapse/cancellation must fire on a suspended Profile's credit), ONE guard before any write — `! $credit->state->isActive()` → `IllegalClubCreditTransition::cannotForfeit` (the SECOND from-state edge after `cannotApply`) — then `update(['state' => Forfeited])`. `remaining` is left INTACT (forfeiture is a state change; the residual balance is the Module-S DEC-043 closure-conversion input, not zeroed by Module K). Docblock pins the four §11.3 triggers as deferred seams (1 year-end-lapse scheduler, 2 renewal forfeit-before-issue, 3 Profile-cancellation cascade, 4 Club-closure → DEC-043 Module-S conversion / AC-K-XM-23) + the forfeit-before-issue-provable-via-one-active note.
- Added `IllegalClubCreditTransition::cannotForfeit(ClubCreditState $from)` (mirrors `cannotApply`; localizes `parties.club_credit.cannot_forfeit`, key lands in 5.2 — tests assert by CLASS). Updated the exception class docblock: it now ships `cannotApply` (3.1) + `cannotForfeit` (4.1); only `cannotRestore` remains for 4.2.
- Registered `ForfeitClubCredit` in `SupplyLifecycleChainTest`'s `$clubCreditWriters` whitelist (the mandatory non-`Create` Action registration — the exact-match `toEqualCanonicalizing` reds without it) + updated its comment.
- Files changed: `app/Modules/Parties/Actions/ForfeitClubCredit.php` (new), `app/Modules/Parties/Exceptions/IllegalClubCreditTransition.php` (cannotForfeit + docblock), `tests/Feature/Modules/Parties/ClubCreditForfeitureTest.php` (new — 4 tests / 14 assn: happy path + non-active reject + terminal×2), `tests/Feature/Modules/Parties/SupplyLifecycleChainTest.php` (writer registration), `openspec/changes/club-credit/tasks.md` (4.1 → [x]).
- Quality loop: **green** — Pint clean; forfeiture file 4/4 + supply-chain 5/5; full suite **1533/1533 (8394 assn)** via `vendor/bin/pest`; PHPStan max 0 err; `pint --test` clean; `openspec validate club-credit --strict` valid.
- **PostgreSQL 17:** no DDL (the Action writes 1.2's table). Forfeiture is a single `update()` of `state` (engine-identical); no clock-sensitive validity window asserted, no Money arithmetic (just a state flip). Each Action opens its own `DB::transaction` (SAVEPOINT under RefreshDatabase). CI `tests-pgsql` verifies on the human push.
- **Learnings for future iterations:**
  - **The 1.3 Pint `{@see}`-hoist trap recurred and bit me:** I wrote `{@see \App\Modules\Parties\Actions\RestoreClubCredit}` (a FULLY-QUALIFIED forward-ref to the not-yet-created 4.2 sibling) in the test docblock; `vendor/bin/pint` (`fully_qualified_strict_types`) hoisted it to a real `use App\Modules\Parties\Actions\RestoreClubCredit;` → that import would have redded PHPStan (class not found). Fixed by demoting it to plain backticks `` `RestoreClubCredit` `` and dropping the import. **Rule (already in `knowledge/laravel/rules.md` "forward-refs"): for a sibling that does NOT exist yet, NEVER `{@see It}` (esp. fully-qualified) — use plain backticks.** `{@see}` is safe ONLY for already-existing classes. Apply this up front in 4.2's docblocks (don't `{@see}` anything you're about to create in a later task).
  - Forfeiture needs NO Profile re-read (contrast `ApplyClubCredit`'s Guard 4 freeze): the §11.3 triggers (year-end, renewal, cancellation, closure) are system/operator effects that fire regardless of Profile suspension. Only `ApplyClubCredit` reads the owning Profile (the AC-K-FSM-2a freeze).
  - Next (4.2): `RestoreClubCredit` (`redeemed → active`) — add `IllegalClubCreditTransition::cannotRestore` (the THIRD/last from-state factory, requires `redeemed`) AND a NEW `ClubCreditRestorePrecondition` for the one-active-conflict guard (the credit IS `redeemed` so it's NOT a from-state issue — it's a precondition: "the Profile already holds another `active` credit" → reject rather than violate the partial index, design L7). Re-read the credit + Profile `lockForUpdate`; guard `state === Redeemed` THEN the no-other-active check; set `Active`. Audit-only; register `RestoreClubCredit` in `$clubCreditWriters` (now the FULL set of 4). Then 4.3 expands `ClubCreditForfeitureTest.php` with forfeit-before-issue ordering + restore cases (incl. restore-after-forfeit) + no-event delta.
---

## [2026-06-23 12:16] — 4.3 Forfeiture/Restoration tests
- Completed `ClubCreditForfeitureTest.php`'s matrix by EXPANDING it with the THREE cases 4.1/4.2 didn't ship (the other seven — forfeit happy/non-active-reject/terminal-×2 from 4.1, restore happy+remaining-restored/non-redeemed-reject/other-active-reject from 4.2 — already landed, NOT duplicated): (1) **forfeit-before-issue ordering** (§ 11.3 / design L5; delta scenario "Forfeit-before-issue ordering holds via the one-active invariant") — a credit-generating `Club::factory()` + `Profile` (issuance-test idiom); issue via the REAL `IssueClubCredit` → a re-issue while `active` throws `QueryException` (partial index) with exactly 1 active remaining → `ForfeitClubCredit` frees the slot → re-issue mints a FRESH `active` credit (`$second->id !== $first->id`), the original now `forfeited`, the Profile's `activeClubCredit` resolves to `$second`, 2 rows total / 1 active. (2) **restore-after-forfeit terminal edge** (delta scenario "Forfeit an active credit (terminal)" — "any subsequent forfeiture, apply or **restore** ... is rejected") — forfeit `active` → `RestoreClubCredit` rejected via `IllegalClubCreditTransition` (`cannotRestore`, the 3rd/last terminal edge after the 4.1 second-forfeit + apply edges), `state`/`remaining` unchanged. (3) **§ 11.4 no-`domain_events` delta across BOTH forfeit + restore writers** (delta scenario "No Club Credit domain event is emitted by Module K") — snapshot `$before = DomainEvent::query()->count()`, run `ForfeitClubCredit` + `RestoreClubCredit` (each on its own factory-default Profile so the redeemed slot is free), assert `->toBe($before)` (the 2.2/3.2 idiom).
- Updated the file docblock from the 4.3-future note to describe the now-present three cases (the 2.2 docblock-update convention).
- Files changed: `tests/Feature/Modules/Parties/ClubCreditForfeitureTest.php` (3 new `it()` + 5 imports [`IssueClubCredit`, `Club`, `Profile`, `DomainEvent`, `QueryException`] + docblock), `openspec/changes/club-credit/tasks.md` (4.3 → [x]).
- Quality loop: **green** — Pint clean; forfeiture file 10/10 (40 assn); full suite **1539/1539 (8423 assn)** via `vendor/bin/pest`; PHPStan max 0 err; `pint --test` clean; `openspec validate club-credit --strict` valid.
- **PostgreSQL 17:** test-only, NO DDL — exercises 1.2's existing schema. The forfeit-before-issue case relies on the partial-index violation aborting only the inner SAVEPOINT (the PG17-green issuance one-active idiom — `IssueClubCredit` wraps its work in `DB::transaction`, so PG's transaction-abort stays isolated under RefreshDatabase and the post-throw `count()`/forfeit/re-issue run cleanly). The window-setting `now()->endOfYear()` in the re-issue is NOT asserted (only `state`/identity), so no frozen clock is needed and no precision/tz asymmetry can flake. Restore writes `state` + `remaining = amount` (Money COPY, no arithmetic); the no-event delta uses `count()` — all engine-identical. CI `tests-pgsql` verifies on the human push.
- **Learnings for future iterations:**
  - **Section 4 is COMPLETE; next is 5.x.** 5.1 = §11.4 audit-only guard test (`ClubCreditEventOwnershipTest.php`: a NEW class-absence loop over `app/Modules/Parties/Events` asserting NO `MembershipFeePaid`/`ClubCreditIssued`/`ClubCreditApplied`/`ClubCreditRestored`/`ClubCreditForfeited` class exists [mirror `SupplyLifecycleChainTest`'s event-non-existence loop] + the four-writers-record-zero-`domain_events` assertion). NOTE 4.3 already proves the no-event delta for Forfeit+Restore and 2.2/3.2 for Issue/Apply — 5.1 GENERALIZES it into one ownership-boundary test (the class-absence loop is the genuinely new surface; design L9 pre-named no forbidden-list, so this is a NEW guard file). Then 5.2 i18n (`lang/en/parties.php` `club_credit` group — all 9 keys the exceptions reference), 5.3 docs (CONTEXT.md + knowledge), 5.4 full-suite gate.
  - **Forfeit-before-issue test reuses two shipped idioms, no new pattern:** the issuance-test Club+Profile setup (`Club::factory()->create()` default generates_credit=true/fee=25000 → `Profile::factory()->create(['club_id'=>...])`) and the SAVEPOINT-isolated `QueryException` probe (issuance test line 84). Driving the REAL `IssueClubCredit` for BOTH the initial active credit AND the post-forfeit re-issue makes the ordering provable end-to-end through production paths (not a factory shortcut) — and asserting only `state`/identity (never the validity window) keeps it clock-free despite `IssueClubCredit` reading `now()`.
---

## [2026-06-23 12:06] — 4.2 RestoreClubCredit Action
- Implemented `RestoreClubCredit` — the SOLE writer of the `redeemed → active` transition (the order-cancellation downstream effect), AUDIT-ONLY (no event; injects NEITHER `DomainEventRecorder` NOR `ActorContext`, completing the 4-writer set with `Issue`/`Apply`/`Forfeit`). `handle(int $clubCreditId)`: one `DB::transaction`, `lockForUpdate()` re-read of the credit **and its owning Profile** (the Profile lock serializes a concurrent restore/issue on the same Profile — the one-active race; `IssueClubCredit` locks the Profile too), then TWO guards before any write: (1) `state !== ClubCreditState::Redeemed` → `IllegalClubCreditTransition::cannotRestore` (the THIRD/last from-state factory; no `isRedeemed()` enum helper exists, so an explicit `!== Redeemed` compare — restore departs only from `redeemed`); (2) `$profile->activeClubCredit()->exists()` → `ClubCreditRestorePrecondition::profileHasActiveCredit` (the Profile already holds another `active` credit — reject rather than breach the partial index, design L1/L7; the credit being restored is `redeemed` so it's outside the `active` scope and never its own conflict). Then `update(['state' => Active, 'remaining' => $credit->amount])`.
- **KEY spec-fidelity catch (new Codebase Pattern above):** task 4.2's step said only "set `state = Active`", but the delta-spec scenario ("Restore a redeemed credit") says "returns to `active` with its **`remaining` restored**" and design **L7** "restores its `remaining`". A `redeemed` credit always has `remaining = 0` (full-spend is the only path to `redeemed`), so "restored" = `remaining = amount` (face value; full-redemption is the § 11.2 norm). So restore writes `remaining = amount` too — verified against `spec/02-prd/Module_K_PRD_v0.3-MVP.md` §11/§11.2 + the delta scenario before coding (money-adjacent, invariant 6). Did NOT escalate: the alternative ("leave 0") directly contradicts two authoritative statements and yields an inert 0-balance "restored" credit — not a genuine ambiguity once the spec is consulted.
- Added `IllegalClubCreditTransition::cannotRestore(ClubCreditState $from)` (localizes `parties.club_credit.cannot_restore`, key lands in 5.2) + updated the class docblock ("ships all three from-state factories"). NEW exception `ClubCreditRestorePrecondition` (one factory `profileHasActiveCredit(int $creditId)` → `parties.club_credit.restore_active_conflict`, key lands in 5.2) — the third precondition sibling of `ClubCreditIssuancePrecondition`/`ClubCreditRedemptionPrecondition`. Registered `RestoreClubCredit` in `SupplyLifecycleChainTest`'s `$clubCreditWriters` (now the COMPLETE 4-writer set) + updated its comment. Added 3 restore tests to `ClubCreditForfeitureTest.php` (happy path: redeemed→active + remaining restored to amount; not-redeemed reject [active]; other-active-exists reject) + broadened the file docblock to cover both forfeiture (4.1) and restoration (4.2).
- Files changed: `app/Modules/Parties/Actions/RestoreClubCredit.php` (new), `app/Modules/Parties/Exceptions/ClubCreditRestorePrecondition.php` (new), `app/Modules/Parties/Exceptions/IllegalClubCreditTransition.php` (cannotRestore + docblock), `tests/Feature/Modules/Parties/ClubCreditForfeitureTest.php` (3 tests + 2 imports + docblock), `tests/Feature/Modules/Parties/SupplyLifecycleChainTest.php` (writer registration), `openspec/changes/club-credit/tasks.md` (4.2 → [x]).
- Quality loop: **green** — Pint clean; forfeiture file 7/7 + supply-chain 5/5; full suite **1536/1536 (8408 assn)** via `vendor/bin/pest`; PHPStan max 0 err; `pint --test` clean; `openspec validate club-credit --strict` valid.
- **PostgreSQL 17:** no DDL (the Action writes 1.2's table). Restore writes `state` + `remaining = amount` (a copy of an existing Money, NO arithmetic — engine-identical); the one-active check `activeClubCredit()->exists()` is identical SQL on both engines; `lockForUpdate` is a real lock on PG, a no-op on SQLite; no clock-sensitive validity window asserted. Each Action opens its own `DB::transaction` (SAVEPOINT under RefreshDatabase). CI `tests-pgsql` verifies on the human push.
- **Learnings for future iterations:**
  - **Restore restores `remaining = amount` (NOT 0).** 4.3's restore-happy-path / restore-after-forfeit tests must assert `remaining` came back to `amount`, not stayed 0. The fully-redeemed fixture is `factory()->create(['state' => Redeemed, 'remaining' => Money::of(0,…), 'amount' => Money::of(25000,…)])`.
  - **Restore re-reads the Profile** (unlike forfeiture, which reads only the credit) — for the one-active check via `$profile->activeClubCredit()->exists()` (the 1.4 relation) + to lock the Profile row against a concurrent issue. Only forfeiture is Profile-free among the 4 writers; Issue/Apply/Restore all lock the Profile.
  - Next (4.3): EXPAND `ClubCreditForfeitureTest.php` with ONLY what 4.1/4.2 didn't ship — (a) forfeit-before-issue ordering pair (active credit → `IssueClubCredit` rejects on one-active; then `ForfeitClubCredit` + `IssueClubCredit` succeeds — needs a Profile/Club with `generates_credit=true` + non-null `fee`, the issuance-test idiom); (b) restore-after-forfeit terminal edge (forfeit active → then `RestoreClubCredit` rejects via `cannotRestore`, the 3rd terminal edge); (c) §11.4 no-`domain_events`-row delta across the writers (snapshot `DomainEvent::query()->count()` → `->toBe($before)`, the 2.2 idiom). Do NOT duplicate the plain restore happy/rejection cases (4.2 shipped them).
---
