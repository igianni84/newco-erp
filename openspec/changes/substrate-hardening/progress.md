# Progress — substrate-hardening

## Codebase Patterns
(consolidated reusable patterns — read first each iteration)

- **White-box concurrency-guard test (substrate).** A race a single-connection SQLite test DB cannot interleave through the public surface is tested deterministically by constructing the exact post-race row state and invoking the private guard method via reflection (`inlineInvokePrivate()` in `InlineDeliveryTest.php`, matching the Money/FxRate `ReflectionMethod` pattern — PHPStan-max clean, since phpstan analyses `tests/`). Make the discriminating assertion load-bearing and non-vacuous: re-invocation is caught via `RecordingConsumer::$handled` (empty vs `[event_id]`), resurrection via terminal `status` (the attempts value can be vacuous when stale+1 == DB value, so don't rely on it alone). Engine-agnostic → identical on both lanes; the PG lane is the true-contention proof (`lockForUpdate()` = real `FOR UPDATE` on PG, documented no-op on single-writer SQLite via the base grammar's `compileLock`).
- **Query-builder UPDATE vs model UPDATE (casts).** A query-builder `->update([...])` runs NO model casts: pass an enum as its `->value` and an `immutable_datetime` column as a raw `CarbonImmutable`. The Carbon binding is formatted by `Connection::prepareBindings()` via `$grammar->getDateFormat()` — the SAME `Y-m-d H:i:s` the model cast uses (`Model::getDateFormat()` also reads the grammar) — so the column round-trips identically on both engines. Reach for the builder form when you need a conditional write (`where('status', …)->update(…)`) a model `->update()` cannot express. A builder `->update()` also returns the **affected-row count** — use it to branch (e.g. `$recorded === 0` ⇒ a sibling already won the row, so there is nothing to log/surface).
- **Pinning a connector-applied DB session setting (timezone / isolation / search_path).** A value Laravel's connector pushes to the session on connect (`config/database.php` `pgsql.timezone` → `PostgresConnector::configureTimezone` issues `SET TIME ZONE 'UTC'`) is proven on two levels: (1) a **config-pin** — `expect(config('database.connections.pgsql.timezone'))->toBe('UTC')` — runs on BOTH lanes (config arrays load regardless of the active engine) and fails loudly if the key is dropped; (2) an **engine-guarded behavioural read-back** — `if (DB::getDriverName() === 'pgsql') expect(DB::scalar('show time zone'))->toBe('UTC')` — proves the connector *actually applied* it, runs only on the PG lane, and leaves the off-lane (SQLite) assertion positive (the config pin), never a vacuous skip. `DB::scalar('show time zone')` returns the live PG session zone (`UTC`). Home for connection-level config pins: `tests/Feature/EnvironmentTest.php` (mirrors its existing `database.connections.sqlite.database` pin + inline engine guard).
- **Assert a log was emitted at level X with context Y (PHPStan-max-clean facade spy).** `Log::fake()` does NOT exist on this framework — use the Mockery facade spy: `$log = Log::spy()` (typed `\Mockery\MockInterface`), trigger, then `$log->shouldHaveReceived('warning')` / `->shouldNotHaveReceived('error')`. To match the structured **context without pinning the message wording**, pass a closure as the SECOND arg: `$log->shouldHaveReceived('info', fn (string $message, array $context): bool => …)` — Mockery treats it as a `withArgs` matcher and the 2-arg `shouldHaveReceived` returns `self` (no `->once()` chaining, which the `MockInterface` type would reject; the closure form asserts "≥1 matching call"). Three PHPStan-max gotchas: (1) the closure param needs a `/** @param array<string, mixed> $context */` docblock — PHPStan reads it on an inline closure/arrow-fn; (2) reading `$context['k']` yields `mixed`, so `=== 2` is fine but `str_contains((string) $x, …)` trips `cast.string` — narrow with `is_string($x)` (a full closure + local var), never a `(string)` cast; (3) `Log::spy()` does NOT throw on un-asserted calls, so the non-spying failure tests (FailingConsumer) keep logging to the real test log (`storage/logs`, exists/writable) harmlessly.
- **Engine-guarded DB-CHECK-constraint test (assert BOTH halves of the SQLite/PG asymmetry — never a vacuous skip).** A constraint that exists on PG ONLY — a CHECK added via `DB::statement('ALTER TABLE … ADD CONSTRAINT …')` in the migration's `pgsql` branch, because Blueprint has no portable CHECK API and SQLite can't `ALTER TABLE ADD CHECK` — is tested by branching on `DB::getDriverName()` and asserting POSITIVELY on each lane: `pgsql` → a raw query-builder insert (which bypasses the model/enum cast, the app-layer floor) that violates the CHECK throws a `QueryException` whose message contains the **declared constraint NAME** (`…_actor_role_check`); assert the NAME, never an engine SQLSTATE, so it's a stable contract. `sqlite` → the same insert is ACCEPTED (no DB CHECK) → assert it landed (`$message === ''` + row `exists()`), documenting that the value-set floor on that lane is the cast a builder write never runs. Wrap the bad insert in `DB::transaction()` (a `captureConstraintViolation` helper mirroring `captureImmutabilityError`) so a PG constraint-abort stays isolated and any row-state check after the throw is valid (testing-rule #5). Build a COMPLETE, well-typed row (real `Str::uuid()` for strict PG `uuid` cols, trap #2) so the CHECK is the SOLE failure cause — verify on PG the rejection is SQLSTATE **23514** (check_violation), not 23502 (not-null) or 22P02 (uuid). Non-vacuity proof: a direct PG probe confirms a VALID value is ACCEPTED through the same builder path (the CHECK admits valid values, isn't rejecting everything). The branch is engine-guard, not a skip: SQLite asserts the accept-half, PG asserts the reject-half — same assertion COUNT on both lanes (here 3 per test → 6 total, 895===895). Home: a dedicated `…ConstraintTest.php` with locally-named row builders (`constraintDomainEventRow`/`constraintAuditRow`, distinct from `ImmutabilityTest`'s globals — Pest test-file functions share one global namespace, so names must not collide).
- **Editing a workflow/YAML config that's tested only by string `toContain` — parse it for real too.** `CiWorkflowTest` matches `ciWorkflow()` (raw file text) with `toContain`/`substr_count`/`ciGatePosition`; string assertions cannot catch a malformed-YAML or wrong-indentation regression. After editing `.github/workflows/*.yml`, cross-check well-formedness with a real parser — `ruby -ryaml -e 'YAML.load_file("…")'` (psych ships with macOS Ruby; **python `yaml` and php `yaml_parse_file` are NOT installed in this env** — don't reach for them). For a **workflow-level** directive (`concurrency:`, `permissions:`, `defaults:`) that must govern ALL jobs, pin *structure*, not just presence: `substr_count(…, 'concurrency:')->toBe(1)` (exactly one block, not duplicated per-job) AND `ciGatePosition('concurrency:') < ciGatePosition('jobs:')` (top-level, declared before `jobs:` — a job-scoped block would sort after it). NB a generic YAML parser reads GitHub's `on:` key as the boolean `true` — expected, not a defect.

---

## [2026-06-13 16:16] — 1.1 C1 close the inline-vs-sweep delivery race
- **Implemented:** the C1 race fix in `InlineDeliveryExecutor` so a delivery completed `done` by one runner is never re-invoked nor resurrected by a contending runner (inline post-commit hook vs scheduled sweep). `attempt()` now re-reads the row inside its delivery transaction under `->lockForUpdate()->first()` and returns early (no handler call) if the row is gone or no longer `pending`, operating on the locked row for the handler + `done` flip. `recordFailure()` became a conditional builder update guarded on `where('status', DeliveryStatus::Pending->value)`, so a row a sibling completed `done` between the failed attempt and the failure write matches zero rows and is never flipped to `pending`/`failed`. `attempt()` kept its `void` return (the C3 tally/return-type is task 1.2). Docblock updated to describe the concurrent guard.
- **Tests (TDD, RED→GREEN):** two engine-agnostic guard tests added to `tests/Feature/Platform/InlineDeliveryTest.php` via a new reflection helper `inlineInvokePrivate()`. (a) re-invocation guard: stale `pending` model + DB row already `done` → `attempt()` leaves `RecordingConsumer::$handled` empty (load-bearing) and the row `done`/attempts 1; (b) resurrection guard: model refreshed to `done`/attempts 1 → `recordFailure()` leaves it `done`/attempts 1. Confirmed RED first (`$handled` was `[1]`; status was `Pending`), then GREEN.
- **Files changed:** `app/Platform/Events/InlineDeliveryExecutor.php` (attempt + recordFailure + class docblock), `tests/Feature/Platform/InlineDeliveryTest.php` (+helper, +2 tests), `openspec/changes/substrate-hardening/tasks.md` (1.1 checked), this `progress.md`.
- **Quality loop: green.** pint clean · C1 filter 2/2 · full suite 245/245 on **SQLite AND PostgreSQL 17** (local docker per `knowledge/testing/rules.md:9-13`) · phpstan 0 @ max · pint --test clean · `openspec validate substrate-hardening --strict` valid.
- **Learnings for future iterations:**
  - Both delta-spec scenarios of *Concurrent Delivery Safety* now map to tests (task 6.2 traceability): "not re-invoked by a concurrent attempt" → the re-invocation guard; "late failure never resurrects" → the resurrection guard.
  - The two reusable patterns above are consolidated at top. The PG lane added the one assertion difference (864 vs 865) from a pre-existing engine-guarded test, not from these tests (which are engine-agnostic).
  - Next task 1.2 (C3) will change `deliverDue()`/`attempt()` to return a tally — it can fold the `attempt()` outcome (delivered/skipped/failed) into a return without disturbing this guard; the early-`return` skip branch is a distinct "neither delivered nor failed" outcome to account for in the tally.
---

## [2026-06-13 16:35] — 1.2 C3 dead-letter logging + sweep run summary
- **Implemented:** the C3 observability floor for dead-letter-in-place. (1) New `App\Platform\Events\AttemptOutcome` enum (`Delivered`/`Failed`/`Skipped`) — the tri-state result of one `attempt()`, where `Skipped` is the C1 concurrency outcome (a sibling already won the row) counted as neither delivered nor failed. (2) `InlineDeliveryExecutor::attempt()` now `return`s an `AttemptOutcome` (`return DB::transaction(fn (): AttemptOutcome …)` — the framework types `transaction()` generically as the closure's return; `Skipped` on the locked-status bail, `Delivered` on the `done` flip, else the catch returns `recordFailure()`'s outcome). (3) `recordFailure()` now returns `AttemptOutcome` and **logs**: it branches on the builder `->update()` affected-row count — `0` ⇒ a sibling won the row ⇒ `Skipped`, no log; otherwise `Log::warning` for a still-retryable failure or `Log::error` on the `$attempts >= $maxAttempts` dead-letter transition, each with context `{delivery_id, consumer, attempts, error}`. (4) `deliverDue()` signature `void` → `array{delivered:int, failed:int}`, tallied per-`attempt()` (a `Skipped` increments neither); `deliver()` stays `void` (inline path drops the outcome — no summary surface). (5) `SweepCommand::handle()` logs `Log::info` `events:sweep complete: swept=%d failed=%d` (swept = delivered + failed) with a `{swept, failed}` context, still returns `self::SUCCESS`.
- **Tests (TDD, RED→GREEN):** 3 *Delivery Failure Observability* scenarios in `tests/Feature/Platform/SweepTest.php` via `Log::spy()` — (a) retryable failure → `warning` (context identifies delivery+error) + NOT `error`, row stays `pending`; (b) `max_attempts=1` → `error` + NOT `warning`, row `failed`; (c) mixed run (1 delivers, 1 fails) → one `info` summary with `swept=2 failed=1`. Confirmed RED first ("warning/error/info … called 0 times"), then GREEN.
- **Files changed:** `app/Platform/Events/AttemptOutcome.php` (new), `app/Platform/Events/InlineDeliveryExecutor.php` (Log import; attempt/deliverDue/recordFailure + docblocks; deliver() note), `app/Platform/Events/SweepCommand.php` (Log import; summary), `tests/Feature/Platform/SweepTest.php` (+Log import, +3 tests), `tasks.md` (1.2 ✓), this `progress.md`.
- **Quality loop: green.** pint clean · observability filter 3/3 · full suite **248/248** (245+3) on **SQLite AND PostgreSQL 17** (local docker, `knowledge/testing/rules.md:9-13`) · phpstan **0 @ max** · pint --test clean · `openspec validate substrate-hardening --strict` valid.
- **Learnings for future iterations:**
  - All 3 *Delivery Failure Observability* delta scenarios now map to tests (task 6.2 traceability): "retryable → warning" / "dead-letter → error" / "sweep → summary".
  - The PHPStan-max-clean log-assertion recipe is consolidated above in Codebase Patterns (facade spy, closure-as-2nd-arg context matcher, `is_string` not `(string)`). First facade-spy usage in the repo.
  - The tri-state `AttemptOutcome` makes the C1 skip a first-class, countable outcome — any future caller tallying attempts must remember `Skipped` ≠ delivered ≠ failed. `recordFailure()`'s affected-row-count branch (`$recorded === 0` ⇒ `Skipped`, no log) is the failure-path twin of `attempt()`'s locked-status bail.
  - Next task 1.3 (C2 mutex TTL) is a `routes/console.php` one-liner (`withoutOverlapping(2)`) + doc-comment + the `SweepTest` schedule pin (`->and($sweep->expiresAt)->toBe(2)`); pure config, engine-agnostic.
---

## [2026-06-13 16:51] — 2.1 C4 raise the PHP floor to 8.5
- **Implemented:** aligned the declared PHP floor with reality (runtime 8.5.2, both CI lanes already on 8.5) and with root `CLAUDE.md` ("PHP ≥ 8.5"). `composer.json:9` `"php": "^8.3"` → `"^8.5"`; `composer update --lock` re-synced the lock. Verified the `composer.lock` diff is **exactly** lock-hash-only: `content-hash` `332c5ad…` → `97d23cb…` plus the `platform.php` mirror `^8.3` → `^8.5` — **zero package version changes** (no upgrades), as the task mandates. `composer validate` clean (plain and `--strict`).
- **Test pin:** `tests/Feature/PlatformRequirementsTest.php` — assertion `80400` → `80500`, test name `>= 8.4` → `>= 8.5`, and the file's header comment block (`:4`, which *cites* CLAUDE.md's floor) `PHP >= 8.4` → `>= 8.5`. This is a floor *tightening*, not a red→green behavior test: both `80400` and `80500` pass on the 8.5.2 runtime; the guard just fails loudly if a future env drifts below 8.5.
- **Falsified-comment corrections (collateral of the floor bump, no test pins them):** `.github/workflows/ci.yml:2` `repo floor is >= 8.4` → `>= 8.5`. Two stale floor comments that this exact change made factually wrong — fixed for internal consistency, not scope creep. (The `php-version: '8.5'` pins at `ci.yml:34,107` and `CiWorkflowTest:36` were already 8.5 → VERIFY-ONLY, untouched, confirmed via grep.) Doc floor refs (`docs/development.md`, `README.md`) are deliberately left for tasks 5.2/5.3.
- **Files changed:** `composer.json` (floor), `composer.lock` (hash + platform mirror), `tests/Feature/PlatformRequirementsTest.php` (assertion + name + comment), `.github/workflows/ci.yml` (header comment), `tasks.md` (2.1 ✓), this `progress.md`.
- **Quality loop: green.** pint clean · PlatformRequirements filter 2/2 (4 asserts) · full suite **248/248** on SQLite (878 asserts, no regression) · phpstan **0 @ max** · pint `--test` clean · `composer validate` clean · `openspec validate substrate-hardening --strict` valid. **No PG run:** this task has zero DB surface (config + a `PHP_VERSION_ID` guard + comments) → SQLite-green is sufficient; the cross-engine sweep is task 6.1 and the standing `tests-pgsql` CI lane covers PG.
- **Learnings for future iterations:**
  - `composer update --lock` on a `php`-constraint change touches only `content-hash` + the lock's `platform.php` mirror — no `packages[]` entries. Always `git diff composer.lock` to confirm zero version bumps (the task's "no package upgrades" guard). Composer prints a confusing "Nothing to modify in lock file" + "Writing lock file" pair — the hash still rewrites; trust the diff, not the message.
  - When a config/dep bump *falsifies* an existing comment (here two `>= 8.4` floor comments), correcting that comment is part of the change, not scope creep — but only the ones this change makes wrong; unrelated doc floor refs stay with their own tasks (5.2/5.3).
  - Section 2 progresses 1/3. Next: **2.2 — C5 document the sweep tunables in `.env.example`** (design D6): a commented block after `QUEUE_CONNECTION=database` (~:38) mirroring `config/events.php:25,28,31` defaults; no test, verify `cp .env.example .env && php artisan key:generate` boots.
---

## [2026-06-13 16:43] — 1.3 C2 bound the sweep overlap-mutex TTL
- **Implemented:** bounded the `events:sweep` overlap mutex to a 2-minute lease. `routes/console.php:16` `->withoutOverlapping()` → `->withoutOverlapping(2)`. Verified in vendor: `ManagesAttributes::withoutOverlapping($expiresAt = 1440)` sets the public `Event::$expiresAt` (default 1440 min = 24h); `CacheEventMutex` takes the lock with TTL `$event->expiresAt * 60` seconds (`CacheEventMutex.php:45,50,64,81`). So `2` = a 120-second lease: a sweep that crashes mid-run without releasing the lock self-heals in ~2 min instead of stalling every later 30s tick for the framework's 24h default. Updated the inline comment in `routes/console.php` and the `SweepCommand` class docblock (both described the guard) to state the bounded lease + TTL formula + C2/design D4.
- **Test (pin, RED→GREEN):** added `->and($sweep->expiresAt)->toBe(2)` to the existing schedule test in `tests/Feature/Platform/SweepTest.php` (`it('schedules events:sweep every thirty seconds without overlapping')`, now ~:290-292). Confirmed RED first ("Failed asserting that 1440 is identical to 2" — the framework default), then GREEN after the `withoutOverlapping(2)` change.
- **Files changed:** `routes/console.php` (config + comment), `app/Platform/Events/SweepCommand.php` (docblock), `tests/Feature/Platform/SweepTest.php` (+1 assertion), `tasks.md` (1.3 ✓), this `progress.md`.
- **Quality loop: green.** pint clean · schedule-test filter 1/1 (3 assertions) · full suite **248/248** on **SQLite AND PostgreSQL 17** (local docker, `knowledge/testing/rules.md:9-13`) · phpstan **0 @ max** · pint --test clean · `openspec validate substrate-hardening --strict` valid.
- **Learnings for future iterations:**
  - Pure-config + engine-agnostic, as predicted — no new Codebase Pattern. The schedule test reads in-memory `Event` properties (`repeatSeconds`/`withoutOverlapping`/`expiresAt`) off the `Schedule` singleton after `app(Kernel::class)->bootstrap()`; no DB, identical on both engines. The PG-vs-SQLite assertion delta stays 877 vs 878 (the pre-existing engine-guarded test, unrelated to this change).
  - Section 1 (substrate correctness, C1/C2/C3) is now complete (3/3). Next is **task 2.1 — C4 raise the PHP floor to 8.5**: `composer.json:9` `"^8.3"` → `"^8.5"` + `composer update --lock` (no package upgrades), and `tests/Feature/PlatformRequirementsTest.php:7-9` `80400`→`80500` / `>= 8.4`→`>= 8.5`. CI (`.github/workflows/ci.yml:34,107`) and `CiWorkflowTest:36` already pin 8.5 — VERIFY-ONLY there.
---

## [2026-06-13 16:59] — 2.2 C5 document the sweep tunables in .env.example
- **Implemented:** documented the three `events:sweep` retry tunables in `.env.example` as a commented block immediately after `QUEUE_CONNECTION=database` (now ~:40-47), with a 3-line header comment noting the keys mirror `config/events.php` defaults and stay commented so the env runs on the baked fallbacks. The three keys are verbatim the config defaults: `# EVENTS_SWEEP_MAX_ATTEMPTS=5` (`config/events.php:25`), `# EVENTS_SWEEP_BACKOFF_BASE_SECONDS=30` (`:28`), `# EVENTS_SWEEP_BACKOFF_CAP_SECONDS=3600` (`:31`).
- **No test** (per task — example/template file, not read by the test runner, which gets its env from `phpunit.xml`). Verified via the task's boot check instead: backed up the real (gitignored) `.env` to `/tmp` (outside the repo so `git add` can't sweep it), `cp .env.example .env` → `php artisan key:generate` (kernel boots, APP_KEY written) → `php artisan config:clear` (config compiles from the copied env) → config read-back `config('events.sweep.*')` returned `max=5 base=30 cap=3600` (the baked defaults — proves the keys are commented + don't override, and the env parses cleanly) → restored the real `.env`. `git status` confirmed only `.env.example` modified, no stray files.
- **Files changed:** `.env.example` (+commented `EVENTS_SWEEP_*` block), `tasks.md` (2.2 ✓), this `progress.md`.
- **Quality loop: green.** pint --test passed · phpstan **0 @ max** · full suite **248/248** on SQLite (878 asserts, no regression — `.env.example` has zero test-runtime effect) · `openspec validate substrate-hardening --strict` valid. No PG run: zero DB/code surface (template file only) → SQLite-green is sufficient; cross-engine is task 6.1 + the standing `tests-pgsql` CI lane.
- **Learnings for future iterations:**
  - **Verifying an `.env.example` edit without nuking the dev `.env`:** back up the real `.env` to `/tmp` (outside the repo), `cp .env.example .env && php artisan key:generate && php artisan config:clear`, then a `config()` read-back to confirm commented keys fall back to defaults, then restore + a final `config:clear`. The read-back is the non-vacuous proof — a typo'd or accidentally-uncommented key surfaces as a wrong value rather than silently passing.
  - Section 2 now 2/3. Next: **2.3 — C6 pin the pgsql session timezone to UTC** (`config/database.php` pgsql block ~:87-100: add `'timezone' => 'UTC',`). This one IS DB-touching → run the PostgreSQL 17 lane locally (docker, `knowledge/testing/rules.md:9-13`), not just SQLite; optionally assert `config('database.connections.pgsql.timezone') === 'UTC'`.
---

## [2026-06-13 17:08] — 2.3 C6 pin the PostgreSQL session timezone to UTC
- **Implemented:** added `'timezone' => 'UTC',` to the `pgsql` connection in `config/database.php` (after `search_path`, before `sslmode`, with a 3-line rationale comment). Verified in vendor that `Illuminate\Database\Connectors\PostgresConnector::configureTimezone()` (`:136-142`) reads `$config['timezone']` and issues `set time zone 'UTC'` on connect — so every pgsql session now renders `timestamptz` in UTC regardless of the server's local zone, matching the substrate's app-set-UTC discipline (`occurred_at`/`available_at`). SQLite has no such key → unaffected.
- **Test (TDD, RED→GREEN):** added `it('pins the PostgreSQL session timezone to UTC')` to `tests/Feature/EnvironmentTest.php` (the existing sibling that pins `database.connections.sqlite.database` + guards engine-specifics inline). Two levels: (1) config-pin `expect(config('database.connections.pgsql.timezone'))->toBe('UTC')` — runs on BOTH lanes, fails loudly if the key is dropped; (2) engine-guarded behavioural read-back `if (DB::getDriverName() === 'pgsql') expect(DB::scalar('show time zone'))->toBe('UTC')` — the production-faithful proof the connector applied it. Confirmed RED first ("null is identical to 'UTC'"), then GREEN.
- **Files changed:** `config/database.php` (+`timezone` key +comment), `tests/Feature/EnvironmentTest.php` (+1 test), `tasks.md` (2.3 ✓), this `progress.md`.
- **Quality loop: green on BOTH engines.** pint clean · filter green · full suite **249/249** (was 248; +1 test) on **SQLite (879 asserts)** AND **PostgreSQL 17** (local docker, `knowledge/testing/rules.md:9-13`) · phpstan **0 @ max** · pint --test clean · `openspec validate substrate-hardening --strict` valid.
- **Non-vacuous PG proof (not inferred from counts):** the filter on the PG lane runs **2 assertions** (config pin + behavioural) vs **1** on SQLite (behavioural branch gated out), and a direct `tinker` read-back printed `driver=pgsql show-time-zone=UTC` — so the connector demonstrably issued `SET TIME ZONE 'UTC'` and the live session reports UTC.
- **Learnings for future iterations:**
  - The reusable two-level recipe (config-pin on both lanes + engine-guarded `DB::scalar('show time zone')` read-back on PG) is consolidated at the top in Codebase Patterns; it generalises to any other connector-applied session setting (isolation level, `application_name`, `search_path`).
  - PG-readiness gate that avoids a flaky "connection refused" without `sleep`: loop a PHP `new PDO('pgsql:host=…;port=55432;…')` connect (PHP boot ~100ms self-paces the loop; bounded to ~80 attempts) — it tests the exact TCP path the suite uses and sidesteps the postgres-init double-restart false-positive that socket-only `pg_isready` can hit.
  - Section 2 (config/deps) is now complete (3/3); **5/17 → 6/17**. Next is **3.1 — C7 pin `event_id` as UUIDv7** (design D7): in `DomainEventRecorderTest.php` envelope read-back (~:114-115) add `->and($read->event_id[14])->toBe('7')` (the UUIDv7 version nibble; recorder emits `Str::uuid7()` at `DomainEventRecorder.php:83`). Engine-agnostic, keep the existing `Str::isUuid(...)` assertion.
---

## [2026-06-13 17:16] — 3.1 C7 pin `event_id` as UUIDv7
- **Implemented:** pinned the public `event_id` as specifically a **UUIDv7** (not v4 or any other version). The recorder already emits `(string) Str::uuid7()` (`DomainEventRecorder.php:83`); this task tightens the test contract so a future regression to `Str::uuid()` (v4) fails loudly. Test-only change — zero production-code edit.
- **Test (pin on shipping behaviour, GREEN-on-add):** one assertion added to the envelope read-back test (`tests/Feature/Platform/DomainEventRecorderTest.php`, `it('records a domain event and reads it back complete with its full envelope')`, now `:116`): `->and($read->event_id[14])->toBe('7')` — the UUIDv7 version nibble at string index 14 (`xxxxxxxx-xxxx-7xxx-…`) — placed right after the existing `Str::isUuid(...)` (kept). No natural RED (behaviour already ships), so non-vacuity was proven directly instead: a one-off boot showed `Str::uuid7()`→index14 `'7'` vs `Str::uuid()`(v4)→`'4'`, so `->toBe('7')` discriminates v7 from the framework's default v4 (the exact regression it guards).
- **Files changed:** `tests/Feature/Platform/DomainEventRecorderTest.php` (+1 assertion), `tasks.md` (3.1 ✓), this `progress.md`.
- **Quality loop: green on BOTH engines.** pint clean · affected file 11/11 (48 asserts) on SQLite AND PG · full suite **249/249** (was 248-tests/879-asserts → **880 asserts**, +1 from this assertion, runs on both lanes) on **SQLite (880)** AND **PostgreSQL 17 (880)** (local docker, `knowledge/testing/rules.md:9-13`) · phpstan **0 @ max** · pint --test clean · `openspec validate substrate-hardening --strict` valid.
- **Non-vacuous PG proof (the assertion actually ran on PG, not engine-skipped):** the affected file reports **48 assertions on PG === 48 on SQLite** (the assertion is unconditional, so it executed on both); and a direct PG round-trip — `Str::uuid7()` value INSERT'd into a `uuid` column and read back — returned byte-identical with `index14=7`, proving the PG `uuid` type (migration `2026_06_12_000001:32`, strict on PG per testing-trap #2) preserves the version nibble at string index 14.
- **Learnings for future iterations:**
  - **Pinning a generated identifier's format/version** (uuid7 vs uuid4, ulid, etc.): assert the discriminating character by string index, and prove the pin non-vacuous by exhibiting the *alternative* value at that index (here v4→`'4'`) rather than chasing an artificial RED — a pin on already-shipping behaviour is GREEN-on-add by construction. When the value persists through a typed column, also confirm the column type preserves the representation on PG (a `uuid` column canonicalises but keeps the version nibble; index 14 survives).
  - Section 3 (test-coverage gaps) opens 1/4; **6/17 → 7/17**. Next is **3.2 — C8 assert the exponential-backoff CAP** (design D7): in `SweepTest.php`, with `Config::set('events.sweep.backoff_base_seconds', 2000)` (cap 3600, max 5) under a frozen clock, drive a failing consumer so the 1st failure window is `t+2000` (uncapped) and the 2nd is `t+3600` (capped, `2000*2=4000>3600`); assert `available_at?->equalTo($t->addSeconds(3600))` at the capped attempt. Engine-agnostic (config + `CarbonImmutable::setTestNow()`; existing `afterEach` resets the clock).
---

## [2026-06-13 17:25] — 3.2 C8 assert the exponential-backoff CAP
- **Implemented:** the missing coverage that pins the backoff *ceiling* by VALUE. The cap already ships — `backoffSeconds()` returns `min($base * 2**($attempts-1), $cap)` (`InlineDeliveryExecutor.php:258`) — but no test asserted it: the growth test (`SweepTest.php:90`) stays below the cap and the dead-letter test (`:120`) advances by hours (always-due) without ever pinning the window. Test-only change — zero production-code edit.
- **Test (pin on shipping behaviour, GREEN-on-add):** one new test `it('caps the exponential backoff window at the configured ceiling')` placed between the growth test and the dead-letter test (thematic order grow → cap → dead-letter). With `Config::set('events.sweep.backoff_base_seconds', 2000)` (cap stays the 3600 default, max the 5 default) under a frozen clock: 1st sweep → attempt 1, `available_at = t0 + min(2000·2^0, 3600) = t0 + 2000` (**uncapped** — proves the base·2^(N-1) path); advance the clock to exactly `t1 = t0+2000` (row due again, `available_at <= now`); 2nd sweep → attempt 2, `available_at = t1 + min(2000·2^1, 3600) = t1 + 3600` (**capped** — raw 4000s clamped). Asserts `equalTo($t1->addSeconds(3600))` (the ceiling) AND `lessThan($t1->addSeconds(4000))` (strictly below the raw window — mirrors the growth test's `greaterThan` comparator idiom). Stays `Pending` (2 < max 5).
- **Non-vacuity proven by a transient RED (then reverted):** temporarily replaced the `min(…, $cap)` with the raw `$base * 2**($attempts-1)` in `backoffSeconds()`, re-ran the test → **RED at exactly the cap assertion** (`SweepTest.php:150`: uncapped attempt-2 window = 4000s ≠ 3600s; attempt-1's 2000s assertion still passed, so 8 of 9 asserts ran — the failure pinpoints the ceiling). Reverted the one-line edit; `git diff app/Platform/Events/InlineDeliveryExecutor.php` empty → byte-identical to HEAD. So the assertion guards precisely the regression C8 targets (a dropped/broken cap).
- **Files changed:** `tests/Feature/Platform/SweepTest.php` (+1 test), `tasks.md` (3.2 ✓), this `progress.md`. (No production file in the commit — the transient cap edit was reverted.)
- **Quality loop: green on BOTH engines.** pint clean · cap-test filter 1/1 (9 asserts) · full suite **250/250** (was 249; +1 test) / **889 asserts** (was 880; +9) on **SQLite AND PostgreSQL 17** (local docker, `knowledge/testing/rules.md:9-13`) · phpstan **0 @ max** · pint --test clean · `openspec validate substrate-hardening --strict` valid.
- **Non-vacuous PG proof (the assertion ran on PG, not skipped):** the test carries no engine guard, and the full-suite assertion count is **identical on both lanes — 889 on PG === 889 on SQLite** → the 9 cap-test asserts executed on PostgreSQL too; SweepTest alone reports 10 tests / 65 asserts on PG. The `available_at` `timestamptz` round-trip + Carbon `equalTo` comparison is the same machinery the growth test already exercises on the PG lane.
- **Learnings for future iterations:**
  - **Driving N successive backoff failures under a frozen clock:** advance test-now to (at least) the previous `available_at` so the row re-qualifies as due (`available_at <= now`); `setTestNow(t0->addSeconds(window))` exactly hits the boundary (the `<=` in `dueDeliveries()` makes the boundary due). The Nth window is computed against the clock value *at that attempt*, not t0 — so capture `$t1` and assert `$t1->addSeconds(cap)`, never `$t0+…`.
  - **Non-vacuity for a pin on already-shipping behaviour (cap/clamp/min-max):** pick a base that makes one data point fall BELOW the bound and the next AT it (here 2000 → 2000<3600, then 4000>3600⇒3600). The below/at contrast is itself the proof the bound engages where expected. A transient prod edit (remove the clamp) → run only the new test → confirm RED → revert (verify `git diff` empty) is the gold-standard confirmation and is SAFE in the loop: a forgotten revert makes the new test fail at quality-step 2, blocking the commit.
  - Section 3 now 2/4; **7/17 → 8/17**. Next is **3.3 — C9 `actor_role` CHECK rejection on PostgreSQL** (design D8): a NEW `tests/Feature/Platform/ActorRoleConstraintTest.php` (DatabaseMigrations) inserting an out-of-enum `actor_role` via `DB::table()->insert()` (bypassing the cast), engine-guarded — `pgsql` expects a `QueryException` (CHECK `domain_events_actor_role_check`/`audit_records_actor_role_check`, wrap in a nested `DB::transaction()` savepoint if asserting row-absence, trap #5); `sqlite` accepts the raw insert (no DB CHECK) → assert the row exists, documenting the asymmetry. THIS ONE IS GENUINELY PG-SPECIFIC → the PG lane is mandatory, not optional.
---

## [2026-06-13 17:40] — 3.3 C9 actor_role CHECK rejection on PostgreSQL
- **Implemented:** the missing coverage that the `actor_role` value-set is enforced at the DB layer on PostgreSQL by the `domain_events_actor_role_check` / `audit_records_actor_role_check` CHECKs (migrations `2026_06_12_000001:70-79` / `000002:80-89`, derived from `ActorRole::cases()`), and that SQLite's floor is the application cast (no portable CHECK there). NEW file `tests/Feature/Platform/ActorRoleConstraintTest.php` (`DatabaseMigrations`, the Platform substrate-test convention). **Test-only — zero production-code change** (the CHECKs already ship from `foundations-domain-events-audit`).
- **Two engine-guarded tests** (one per table): a raw `DB::table()->insert()` of a complete, DB-valid row whose `actor_role` is the out-of-enum literal `'intruder'` (bypassing the Eloquent `ActorRole` cast). Branch on `DB::getDriverName()`: `pgsql` → `expect($message)->toContain('…_actor_role_check')` + row `exists()` is `false`; `sqlite` → `expect($message)->toBe('')` + row `exists()` is `true` (the documented asymmetry — a positive accept-half assertion, never a vacuous skip). Each test opens with a **premise guard** (`expect(ActorRole values)->not->toContain('intruder')`) so a future enum addition of that token can't make the test pass for the wrong reason. The bad insert is wrapped in `DB::transaction()` via a local `captureConstraintViolation()` helper (mirrors `ImmutabilityTest`'s `captureImmutabilityError`) so a PG constraint-abort stays isolated (testing-rule #5); locally-named row builders `constraintDomainEventRow`/`constraintAuditRow` (distinct from `ImmutabilityTest`'s globals — Pest shares one function namespace).
- **Files changed:** `tests/Feature/Platform/ActorRoleConstraintTest.php` (new, 2 tests + 3 helpers), `tasks.md` (3.3 ✓), this `progress.md` (+Codebase Pattern, +entry).
- **Quality loop: green on BOTH engines.** pint clean · filter 2/2 (6 asserts) · full suite **252/252** (was 250; +2 tests) / **895 asserts** (was 889; +6) on **SQLite AND PostgreSQL 17** (local docker, `knowledge/testing/rules.md:9-13`) · phpstan **0 @ max** · pint --test clean · `openspec validate substrate-hardening --strict` valid · PG container torn down, tree clean.
- **Non-vacuous PG proof (the reject-half genuinely fired, and discriminates):** the PG filter run is GREEN, so `captureConstraintViolation` returned a message *containing the constraint name* (else `toContain` fails on `''`). A direct PG probe via the SAME builder path then showed `valid(system) => ACCEPTED` while `invalid(intruder) => REJECTED [names-the-CHECK] sqlstate=23514` (PG `check_violation`, not `23502` not-null / `22P02` uuid) — so the complete-row discipline held and the CHECK is the sole failure cause. Full-suite assert count is identical on both lanes (895 === 895): the new test's 6 asserts ran on PG too (3 per test, reject-half on PG vs accept-half on SQLite — same count).
- **Learnings for future iterations:**
  - The reusable **engine-guarded DB-CHECK-constraint test** pattern is consolidated at top (assert BOTH halves, name not SQLSTATE, savepoint-wrap, complete row → 23514, valid-value accept-probe for non-vacuity). It's the template for any future PG-only CHECK (e.g. a module enum column).
  - Task 6.2 traceability: C9's "engine-guarded CHECK-constraint test" Codebase Pattern is now recorded (as 6.2 asks). The two CHECK constraints (`domain_events_actor_role_check`, `audit_records_actor_role_check`) now both map to a test.
  - Section 3 now 3/4; **8/17 → 9/17**. Next is **3.4 — C10 combined structural + before/after UPDATE rejected** (design D8): in `ImmutabilityTest.php`, seed a valid audit row, then `captureImmutabilityError(fn () => DB::table('audit_records')->where('id',$id)->update(['action' => 'tampered.action', 'before' => $redacted, 'after' => $redacted]))`; assert `toContain('immutable')` AND `action` unchanged (`voucher.cancel`). Proves the trigger fires on ANY structural change regardless of before/after — the redaction seam is strictly before/after-only. Behaviour-only via the savepoint helper → both engines, no PG-specific surface (but a PG run is still cheap insurance).
---

## [2026-06-13 17:49] — 3.4 C10 combined structural + before/after UPDATE rejected
- **Implemented:** the missing coverage that the `audit_records` immutability trigger rejects a structural edit **even when `before`/`after` change in the same statement** — i.e. a structural change can't be smuggled in under cover of a (otherwise-permitted) GDPR redaction. The existing tests cover the two pure cases (structural-only → rejected `:109`; before/after-only → allowed `:120`); this pins the COMBINATION. The trigger's guard is an OR over `$auditStructuralColumns` ONLY (migration `000004:51-54`, `before`/`after` deliberately excluded), evaluated independently of `before`/`after` — so `action` differing trips it regardless. **Test-only — zero production change** (the trigger already ships from `foundations-domain-events-audit`).
- **Test (pin on shipping behaviour, GREEN-on-add):** one new test `it('rejects a combined structural + before/after UPDATE — a structural edit cannot ride inside a redaction')`, placed between the redaction-allowed test and the audit DELETE test (narrative: structural rejected → redaction allowed → combination rejected → delete rejected). Exactly the task's update payload `['action' => 'tampered.action', 'before' => $redacted, 'after' => $redacted]` via `captureImmutabilityError` (the savepoint-wrapped helper, trap #5). Asserts `toContain('immutable')` + `action` still `voucher.cancel` (query-builder) + **`before`/`after` still the ORIGINAL `['email' => 'user@example.com']`, NOT redacted** (read through `AuditRecord`'s array cast — trap #3 jsonb-normalisation). The last two assertions are the load-bearing extra over the existing structural-only test: they prove the whole statement aborted ATOMICALLY, so the bundled redaction never landed either.
- **Non-vacuity proven by a transient RED (then reverted):** temporarily removed `'action'` from `$auditStructuralColumns` (migration `000004`) → re-ran the file → **exactly 2 RED** (the existing structural-only test `:116` AND this new combined test `:167`, both `'' contains "immutable"` — the combined update became allowed, so the helper returned `''`); the other 5 stayed green. Reverted the one-token edit; `git diff database/migrations/2026_06_12_000004…` empty → byte-identical to HEAD. So all four assertions guard precisely the regression C10 targets (a structural column losing trigger protection, or the guard wrongly consulting before/after).
- **Files changed:** `tests/Feature/Platform/ImmutabilityTest.php` (+1 test), `tasks.md` (3.4 ✓), this `progress.md`. (No production file — the transient migration edit was reverted.)
- **Quality loop: green on BOTH engines.** pint clean · combined-test filter 1/1 (4 asserts) · full suite **253/253** (was 252; +1 test) / **899 asserts** (was 895; +4) on **SQLite AND PostgreSQL 17** (local docker, `knowledge/testing/rules.md:9-13`) · phpstan **0 @ max** · pint --test clean · `openspec validate substrate-hardening --strict` valid · PG container torn down, tree clean.
- **Non-vacuous PG proof (the assertion ran on PG, not engine-skipped):** the test carries no engine guard, and the full-suite assert count is identical on both lanes — **899 on PG === 899 on SQLite** → the 4 new asserts executed on PostgreSQL too; ImmutabilityTest alone is 7 tests / 18 asserts on PG. This matters because the PG immutability trigger is a SEPARATE code path — a plpgsql `IF (… IS DISTINCT FROM …) THEN RAISE EXCEPTION` (`000004:141-157`) vs SQLite's `CREATE TRIGGER … WHEN (… IS NOT …) … RAISE(ABORT)` (`000004:106-114`) — so green-on-PG proves the PG trigger function's IF-guard rejects the combined update exactly as SQLite's WHEN clause does (true cross-dialect parity, not just a re-run).
- **Learnings for future iterations:**
  - No new Codebase Pattern — this reuses the existing immutability-test idiom (savepoint-wrapped `captureImmutabilityError` + behaviour-only `toContain('immutable')`) and the two relevant traps (#5 savepoint, #3 read jsonb through the model cast). The one transferable note: when pinning a STRONGER variant of an existing guard test (here combination vs single), assert the extra columns are UNCHANGED too (atomic-abort proof), and prove non-vacuity by a transient mutation that flips BOTH the old and new test RED — the new test going RED in lockstep with the proven-existing one is the cleanest confirmation the guard is load-bearing.
  - Task 6.2 traceability: the `audit_records_immutable` structural-update guard now has THREE mapped cases — structural-only rejected, before/after-only allowed, combined rejected — fully pinning the "redaction seam is strictly before/after-only" contract from design D8.
  - Section 3 (test-coverage gaps) is now COMPLETE (4/4); **9/17 → 10/17**. Next is **4.1 — C11 workflow concurrency group** (design D9): add a top-level `concurrency:` block to `.github/workflows/ci.yml` after the `permissions:` block (~:21) — `group: ci-${{ github.ref }}` + `cancel-in-progress: true` — and a `CiWorkflowTest` test asserting `toContain('concurrency:')`/`toContain('cancel-in-progress: true')`/the `ci-${{ github.ref }}` group. CiWorkflowTest reads the file from disk; engine-agnostic (no DB surface).
---

## [2026-06-13 17:55] — 4.1 C11 workflow concurrency group
- **Implemented:** added a workflow-level `concurrency:` block to `.github/workflows/ci.yml`, inserted between the `permissions:` block and `jobs:` (so it is top-level, governing BOTH jobs) — `group: ci-${{ github.ref }}` + `cancel-in-progress: true`, with a 4-line rationale comment (C11/design D9). Effect: when a newer commit lands on the same ref, GitHub cancels the superseded in-flight run instead of letting it finish — most valuable for the `tests-pgsql` service-container lane (spins up a postgres:17 container). `github.ref` keys the group per-branch / per-PR, so unrelated branches never cancel each other. One block covers both lanes (no per-job duplication).
- **Test (TDD, RED→GREEN):** one new test in `tests/Feature/CiWorkflowTest.php` (`it('cancels superseded in-flight CI runs via one workflow-level concurrency group')`), appended to the structural-guard cluster at the end (beside the gates-once / scopes-pgsql positional tests it mirrors). Asserts the three required substrings — `concurrency:`, `group: ci-${{ github.ref }}`, `cancel-in-progress: true` — PLUS two structure guards that make it load-bearing: `substr_count(…, 'concurrency:')->toBe(1)` (exactly one block, not duplicated per-job) and `ciGatePosition('concurrency:') < ciGatePosition('jobs:')` (top-level, declared before `jobs:` — a job-scoped block would sort after). Confirmed RED first (file had no `concurrency:` — failed on the first `toContain`, 1 assertion ran), then GREEN (5 assertions).
- **YAML well-formedness cross-checked** (a `toContain` string test can't catch malformed YAML): `ruby -ryaml` (psych) parsed the file — top-level keys include `concurrency`, which deserialises to exactly `{group: "ci-${{ github.ref }}", cancel-in-progress: true}`. (python `yaml` and php `yaml_parse_file` are not installed in this env; ruby/psych is the available parser — noted in Codebase Patterns.)
- **Files changed:** `.github/workflows/ci.yml` (+`concurrency:` block +comment), `tests/Feature/CiWorkflowTest.php` (+1 test), `tasks.md` (4.1 ✓), this `progress.md` (+Codebase Pattern, +entry).
- **Quality loop: green.** pint clean · new-test filter 1/1 (5 asserts) · full suite **254/254** (was 253; +1 test) / **904 asserts** (was 899; +5) on **SQLite** · phpstan **0 @ max** · pint --test clean · `openspec validate substrate-hardening --strict` valid. **No PG run:** zero DB surface — `CiWorkflowTest` reads the YAML from disk via `file_get_contents`, so SQLite-green is sufficient; the standing `tests-pgsql` CI lane (which this very block now protects from stale-run waste) covers PG, and task 6.1 is the cross-engine sweep.
- **Learnings for future iterations:**
  - New Codebase Pattern (top): editing a workflow/YAML tested only by string `toContain` → also parse it for real (ruby/psych here), and pin *structure* (count==1 + position-before-`jobs:`) for a workflow-level directive, not just presence.
  - Section 4 (CI) is now COMPLETE (1/1); **10/17 → 11/17**. Next is **5.1 — C12 GUIDE §2.7 local-PostgreSQL verify step** (design D10): insert a local-PG-17 verify step BEFORE the merge step in `GUIDE.md` §2.7 (~:114-138), using the exact command from `knowledge/testing/rules.md:9-13` (`docker run … postgres:17` → `DB_CONNECTION=pgsql … php artisan test` → `docker rm -f pg`), in Italian to match the guide. Do NOT touch the F1 status line (`FoundationsDocsTest` pins `foundations-money-i18n-flags` / `F1 completata 3/3`). Docs-only; suite stays green, no PG run.
---

## [2026-06-13 18:03] — 5.1 C12 GUIDE §2.7 local PostgreSQL verify step
- **Implemented:** inserted a local-PostgreSQL-17 verify step into the closing ritual `GUIDE.md` §2.7, placed BEFORE the merge step (design D10/C12). New `# 2. Verifica locale su PostgreSQL 17` carries the docker recipe **verbatim** from `knowledge/testing/rules.md:10-12` (`docker run … postgres:17` → `DB_CONNECTION=pgsql … php artisan test` → `docker rm -f pg`), with a 2-line Italian rationale ("SQLite-verde è necessario, non sufficiente … conferma il motore di produzione PRIMA del merge"). Renumbered the rest of the ritual: Merge & push 2→3, Verifica semantica 3→4, Archive 4→5. Operationalises design D10's note that the §2.7 step "makes that [PG] run routine before merge": the loop only runs SQLite and the `tests-pgsql` CI lane fires only on a human push, so a human merging a `CHANGE_COMPLETE` branch now has the production-engine check inline in the ritual.
- **Internal-consistency edit (same file, same logical change):** the §8 cheatsheet "Chiusura" block mirrors §2.7 but would have omitted the now-required PG step; added a one-line pointer `#   → verifica locale su PostgreSQL 17 PRIMA del merge (recipe docker completa in §2.7)` after the `git log` line — matching the cheatsheet's existing `#   → verifica semantica … (§2.7)` idiom (points to §2.7 rather than duplicating the multi-line recipe → copy-paste-safe).
- **No new test (docs-only — matches the 2.2 precedent + design D10):** acceptance is "suite stays green"; design D10 explicitly notes `FoundationsDocsTest` "only pins the F1 status line → safe" (a guard-rail, not a request for a pin). Grep confirmed `FoundationsDocsTest` is the ONLY test reading `GUIDE.md`, and only its §4 F1 line (`foundations-money-i18n-flags` / `F1 completata 3/3`, line ~174) — untouched by these §2.7/§8 edits. Adding a §2.7 pin to the F1-scoped `FoundationsDocsTest` would be a scope-mismatch; a new test file for one doc assertion exceeds what the design (and tasks 5.3/5.4) call for. Verified by keeping the suite green.
- **Files changed:** `GUIDE.md` (§2.7 +PG verify step +renumber 2→3/3→4/4→5; §8 cheatsheet +pointer line), `tasks.md` (5.1 ✓), this `progress.md`.
- **Quality loop: green.** `FoundationsDocsTest` 8/8 (26 asserts — GUIDE reader + F1 pin intact) · full suite **254/254 (904 asserts)** on SQLite (unchanged — zero code/DB surface) · phpstan **0 @ max** · pint --test clean · `openspec validate substrate-hardening --strict` valid. **No PG run:** docs-only, nothing code/DB touched → SQLite-green is sufficient; the very step this task documents IS the PG-run procedure, and task 6.1 is the cross-engine sweep.
- **Learnings for future iterations:**
  - No new Codebase Pattern — pure doc edit. One transferable note: when a doc edit adds a REQUIRED step to a detailed procedure that a condensed cheatsheet in the SAME file mirrors, update the cheatsheet too (a pointer line, not a duplicated recipe) so the document stays internally consistent — leaving the quick-reference contradicting the procedure is a half-done change, not scope discipline.
  - Docs-task test policy in this change (now confirmed across 2.2 and 5.1): non-code doc/template files are verified by "suite stays green" + a targeted run of whatever existing doc-reader test covers the file (here `FoundationsDocsTest`), NOT by inventing a new pin. Tasks 5.2 (keeps `DevelopmentDocsTest` green) and 5.3 ("No test pins these strings") follow the same per-task author intent.
  - Section 5 (docs) opens 1/4; **11/17 → 12/17**. Next is **5.2 — C13 `docs/development.md` corrections** (design D10): (a) add a `RALPH_MODEL` bullet + correct the stale "no model option of its own" sentence (`ralph.sh:24-25,204` pins `--model "${RALPH_MODEL:-claude-opus-4-8[1m]}" --effort "${RALPH_EFFORT:-max}"`); keep the `RALPH_EFFORT` token (`DevelopmentDocsTest` pins it). (b) `:113` ".claude/" correction (no `.mcp.json`, but hooks/skills/team-memory live there). (c) PHP floor 8.4→8.5 at `:11`, `:76`, `:121`. Keep `DevelopmentDocsTest` green (locked versions + RALPH_EFFORT token).
---

## [2026-06-13 18:11] — 5.2 C13 docs/development.md corrections
- **Implemented:** brought `docs/development.md` back in line with reality on three fronts (design D10/C13). (a) **Env-vars list (~:93-95):** added a `RALPH_MODEL` bullet (`default: claude-opus-4-8[1m] — Opus 4.8, 1M context`; override example) and rewrote the stale `CLAUDE_FLAGS` bullet — its old "The script has no model option of its own, so the loop inherits the account-default model" was falsified when `ralph.sh:204` started pinning `--model "${RALPH_MODEL:-claude-opus-4-8[1m]}" --effort "${RALPH_EFFORT:-max}"` (header docs at `ralph.sh:24-25`). New wording: `CLAUDE_FLAGS` is appended *after* the `--model`/`--effort` pins and the later flag wins, so it still overrides the model (the `RALPH_MODEL=… ./ralph.sh` and `CLAUDE_FLAGS="--model opus" ./ralph.sh` paths both documented). The `RALPH_EFFORT` token is preserved verbatim (`DevelopmentDocsTest` pins it). (b) **`.claude/` correction (:114):** the parenthetical "no MCP server or skills are installed (no `.mcp.json`, nothing under `.claude/`)" was factually wrong — corrected to "it installs no MCP server (`.mcp.json` is absent), but `.claude/` carries the loop's hooks, skills, and team memory" (verified on disk: `.mcp.json` absent; `.claude/` has `hooks/`, `skills/`, `memory/`, plus `settings.json`/`commands`/`CLAUDE.md`). (c) **PHP floor `8.4 → 8.5`** at the three stale spots task 2.1 (C4) deferred here: prerequisites (:11), the CI-PHP-pin paragraph (:76, both the "project floor of PHP ≥ 8.5" and "never drop below 8.5" mentions), and the version-table constraint column (:122 `≥ 8.5 (project floor)`). The `8.5.2` snapshot version is untouched at both :11 and :122.
- **No new test (docs-only — 2.2/5.1 precedent + design D10):** acceptance is "suite stays green" + the existing doc-reader test. `DevelopmentDocsTest` is the doc-reader for this file; design D10's Risks table maps it as "RALPH_EFFORT token + locked versions" — both preserved (version table rows untouched except the PHP constraint column, and PHP is *not* one of the test's checked packages; the `RALPH_EFFORT` token survives). The PHP-floor *string* is NOT pinned by any test (`PlatformRequirementsTest` pins the runtime `80500`, not the doc copy), so `≥ 8.4 → ≥ 8.5` is safe.
- **Files changed:** `docs/development.md` (5 edits across 3 corrections), `tasks.md` (5.2 ✓), this `progress.md`.
- **Quality loop: green.** `DevelopmentDocsTest` filter **6/6 (31 asserts)** · full suite **254/254 (904 asserts)** on SQLite (UNCHANGED — zero code/DB surface) · phpstan **0 @ max** · pint --test clean · `openspec validate substrate-hardening --strict` valid. **No PG run:** docs-only, nothing code/DB touched → SQLite-green is sufficient; task 6.1 is the cross-engine sweep, the standing `tests-pgsql` CI lane covers PG.
- **Learnings for future iterations:**
  - No new Codebase Pattern — reuses the docs-task test policy (verify by "suite green" + the file's existing doc-reader test, here `DevelopmentDocsTest`, NOT a new pin). One transferable note: when a config/script change (here `ralph.sh` gaining `--model`) *falsifies* a doc claim, the falsified-comment correction belongs with whatever task next touches that doc — C4 (2.1) deliberately deferred the `docs/development.md` floor refs to here (5.2), and 5.2 also swept the `RALPH_MODEL`/`CLAUDE_FLAGS` falsification from the same root cause. Cross-check the test's pinned tokens (grep them) BEFORE running the suite so a dropped pin surfaces immediately, not as a red.
  - Doc-pin map holds: `DevelopmentDocsTest` pins `RALPH_EFFORT` + the locked package versions + the five Quality Commands + the Filament llms.txt / boost:install strings — NOT the PHP-floor copy. Changing the floor string is unpinned; dropping `RALPH_EFFORT` or a locked version would red it.
  - Section 5 (docs) now 2/4; **12/17 → 13/17**. Next is **5.3 — C14 `README.md` exit codes + semantic-verify** (design D10): (a) the ralph exit-codes line (verify current line — was `:68`, not the stale "~19-21") add `2` (preflight) + `5` (integrity violation) to match `docs/development.md:104`; (b) the ASCII-flow line (~:28) replace the non-existent `/opsx:verify` with the semantic-verify reference (GUIDE §2.7), aligning with `:53` (already correct); soften the skills-table "verify" mention (~:94) if needed. **No test pins these strings** — verify by suite-green + a read of the touched lines.
---

## [2026-06-13 18:18] — 5.3 C14 README.md exit codes + semantic-verify
- **Implemented:** brought `README.md` in line with reality on four fronts (design D10/C14 + one floor-sweep completion). Verified each against ground truth BEFORE editing. (a) **Exit-codes line (`:68`):** added the two missing codes `2` (preflight error) and `5` (integrity violation), inserted in ordinal position, so README now lists all six codes `ralph.sh` actually returns — verified against ground truth (`ralph.sh:54` `fail(){ … exit 2; }` = preflight; `:216` `exit 5` = integrity violation; plus the existing `0/1/3/4`), matching `docs/development.md:105`'s coverage. Kept README's terser altitude (a short `(a protected layer was modified)` clarifier on `5`, mirroring the existing code-`4` parenthetical; not the dev guide's full protected-path list). (b) **ASCII-flow line (`:28`):** replaced the non-existent `/opsx:verify` (the live opsx skill set is propose/apply/explore/sync/archive — no `verify`; grep confirmed `/opsx:verify` appeared ONLY here) with `semantic-verify (GUIDE §2.7)`, aligning with `:53`'s already-correct "semantic verification (prompt in GUIDE.md §2.7)". **Skills table (`:94`):** softened the same phantom `verify` in the `/opsx:*` examples → `sync` (a real subcommand), so every listed example is a real command.
- **Floor-sweep completion (`:39`, transparent scope note):** `# 0. Prerequisites: … php >= 8.4 …` → `php >= 8.5`. This was the LAST stale `8.4` in the entire repo (grep across README/development.md/GUIDE.md — development.md was cleaned by 5.2/C13). C4 (task 2.1, THIS change) raised the floor to `^8.5`, and 2.1's progress note explicitly deferred "README floor refs … for tasks 5.2/5.3". Design D10/C14 enumerates `:68`/`:28`/`:94` but is silent on `:39`; I fixed it here rather than ship a Quickstart prerequisite (`php >= 8.4`) that `composer.json` `^8.5` would itself reject — a known-false statement in the most-read file, contradicting CLAUDE.md "PHP ≥ 8.5". Zero-risk (no test pins it), same file, completes C4's own sweep. Flag at human review if a stricter design-scope reading is preferred.
- **No new test (docs-only — 2.2/5.1/5.2 precedent + the task's own "No test pins these strings"):** grep confirmed NO test reads `README.md` (nor the strings `opsx:verify` / `integrity violation`). Acceptance = suite stays green + a read of the touched lines (done: `:28`, `:39`, `:68`, `:94` all render correctly; `│`/`▼` box alignment preserved since only mid-line text changed).
- **Files changed:** `README.md` (4 edits: exit codes, ASCII flow, skills table, PHP floor), `tasks.md` (5.3 ✓), this `progress.md`.
- **Quality loop: green.** full suite **254/254 (904 asserts)** on SQLite — UNCHANGED (zero code/DB surface, proving no regression) · phpstan **0 @ max** · pint --test clean · `git diff README.md` is exactly 4 insertions / 4 deletions · `openspec validate substrate-hardening --strict` valid. **No PG run:** docs-only, nothing code/DB touched → SQLite-green is sufficient; task 6.1 is the cross-engine sweep, the standing `tests-pgsql` CI lane covers PG.
- **Learnings for future iterations:**
  - No new Codebase Pattern — reuses the docs-task test policy (verify by "suite green" + the file's doc-reader test, or — when none exists, as for README — by suite-green + a direct read of the touched lines). Transferable note: when correcting doc references to commands/codes, verify against the AUTHORITATIVE source, not the sibling doc — exit codes from `ralph.sh` itself (`grep exit`), the opsx command set from the live skill registry — so a sibling doc that is ALSO wrong can't propagate the error.
  - **Scope judgment recorded:** a doc-correctness task on file X should also fix a known-false adjacent claim in X that THIS change's own earlier task created (here C4's floor bump orphaned README:39), even when the design's line enumeration omits it — completing the sweep beats shipping a self-contradicting file. Documented transparently above for the reviewer rather than done silently.
  - Section 5 (docs) now 3/4; **13/17 → 14/17**. Next is **5.4 — C15 track the untracked gates in `decisions/INDEX.md`** (design D10/C15, `design.md:65`): extend the "Open decisions" section (~`:14-16`) with explicit one-line trigger/gate entries for the four currently-untracked gates — **secrets management**, **observability**, **PCI boundary**, **architectural security review**. Keep them in `decisions/INDEX.md` (the existing editable open-gates registry), NOT a new gates doc (design's explicit decision: CLAUDE.md's stack-gate table is protected + stack-scoped, so INDEX.md is the single home for these cross-cutting operational/security gates). Verify current `:14-16` line numbers first; no test pins this → suite-green + a read of the added entries.
---

## [2026-06-13 18:26] — 5.4 C15 track the untracked gates in decisions/INDEX.md
- **Implemented:** registered the four currently-untracked cross-cutting gates in the `## Open decisions` section of `decisions/INDEX.md` (design D10/C15). Restructured the section into two clearly-labeled groups rather than one flat `·` list, so the two kinds of gate stay semantically distinct: **(1) Stack gates** — the existing paragraph (Identity/auth · queue driver · object storage · hosting · frontend), preserved VERBATIM, now prefixed with a label noting it mirrors root CLAUDE.md's protected "Open stack decisions" table (that table is the authoritative copy); **(2) Operational / security gates** — a new bulleted group for the four C15 gates, each a one-line `gate = …` trigger: **Secrets management** (gate = staging env / first stored production credential — DB, object storage, payment, Logilize; covers storage/rotation/CI-CD injection) · **Observability** (gate = staging env / first production alerting need; notes the substrate-hardening C3 dead-letter+sweep log channel is a deliberate placeholder pending this gate) · **PCI boundary** (gate = first real payment-processor charge/capture — Module S order completion / Module E settlement; fixes cardholder-data boundary + SAQ scope) · **Architectural security review** (gate = before first customer-facing release / production cutover; threat model + review of substrate, compliance gates, module boundaries). The group intro encodes the design's rationale inline (tracked only here because the CLAUDE.md table is protected + stack-scoped → INDEX.md is their single home).
- **Why the two-group split (elegance pause):** a flat 9-item list would leave a reader unable to tell why 5 gates are authoritative in CLAUDE.md and 4 are not. The labeled split makes the structure self-documenting and bakes the C15 design decision into the doc; it preserves the existing paragraph's content unchanged (diff is 9 insertions / 0 deletions). Bulleted form for the new group (vs inline `·`) because each carries a gate + context — "one-line trigger/gate" maps cleanly to one bullet per gate. The observability bullet deliberately back-references the C3 work, closing the loop with design.md's own Open Question ("revisit the channel … when the observability gate (now tracked by C15) is decided").
- **No new test (docs-only — 2.2/5.1/5.2/5.3 precedent + the task's own "no test pins this"):** grepped `tests/` for `decisions/INDEX`, `Open decisions`, and all four gate tokens → ZERO matches (no test reads this file). Acceptance = suite stays green + a read of the added entries (done: section renders correctly, all four gates present with gates, stack-gate paragraph intact).
- **Files changed:** `decisions/INDEX.md` (+9 lines: two group labels + 4 gate bullets, existing paragraph untouched), `tasks.md` (5.4 ✓), this `progress.md`.
- **Quality loop: green.** pint --test clean · phpstan **0 @ max** · full suite **254/254 (904 asserts)** on SQLite — UNCHANGED from iter 14 (zero code/DB surface → proves no regression) · `git diff decisions/INDEX.md` exactly **9+/0−** · `openspec validate substrate-hardening --strict` valid. **No PG run:** docs-only, nothing code/DB touched → SQLite-green is sufficient; task 6.1 is the cross-engine sweep, the standing `tests-pgsql` CI lane covers PG.
- **Learnings for future iterations:**
  - No new Codebase Pattern — reuses the docs-task test policy (verify by "suite green" + the file's doc-reader test, or — when none exists, as for INDEX.md and README — by suite-green + a direct read of the touched lines; grep the candidate tokens in `tests/` FIRST to confirm nothing pins them).
  - **Scope/elegance note:** when "extend section X with N entries" would otherwise dump the new entries into a flat list that obscures their relationship to the existing ones, a labeled regrouping that PRESERVES existing content verbatim (insertion-only diff) is the elegant move — it satisfies "extend" while making the structure self-documenting and encoding the change's rationale into the doc. Confirm it's insertion-only via `git diff --stat` (here 9+/0−) so the regroup provably didn't alter existing wording.
  - Section 5 (docs) is now COMPLETE (4/4); **14/17 → 15/17**. Only Section 6 remains: **6.1 cross-engine green** (SQLite quality cmds + the local PostgreSQL 17 docker lane per GUIDE §2.7 / `knowledge/testing/rules.md:9-13` — this is the change's first MANDATORY full PG run since 3.4, since 4.1/5.x were all SQLite-sufficient docs/CI edits) then **6.2 spec validation + traceability** (map each delta scenario → its test: *Concurrent Delivery Safety* ×2 → the 1.1 reflection tests; *Delivery Failure Observability* ×3 → the 1.2 `Log::spy` tests; record the durable patterns — already consolidated at top).
---

## [2026-06-13 18:32] — 6.1 Cross-engine green
- **Verified (no code change — global-acceptance gate):** the whole `substrate-hardening` change is green on BOTH engines. **SQLite lane:** `vendor/bin/pint --test` clean · `vendor/bin/phpstan analyse` **0 errors @ max** · `php artisan test` **254/254 (904 assertions)**. **PostgreSQL 17 lane** (local docker per `knowledge/testing/rules.md:9-13`): `docker run … postgres:17` (port 55432) → `DB_CONNECTION=pgsql … php artisan test` **254/254 (904 assertions)** → `docker rm -f pg`. `openspec validate substrate-hardening --strict` valid. Tree clean before and after (no stray files).
- **Assertion-count parity is exact (904 === 904 on both lanes) and is the CORRECT state, not a missed engine-guard.** The engine-guarded asymmetries net to zero: a pre-existing SQLite-only assertion (tracked since 1.1 as the 864-vs-865 delta) is balanced by the PG-only `DB::scalar('show time zone')` read-back that task 2.3 added to `EnvironmentTest`. Lineage: 1.3 had SQLite 878 / PG 877 (SQLite +1); 2.3's timezone test added +1 both lanes (config-pin) +1 PG-only (read-back) ⇒ equalised at 879/879; every task since (3.1 +1, 3.2 +9, 3.3 +6, 3.4 +4, 4.1 +5 → 904) added engine-agnostic asserts on both lanes, so the equality has held. Verified non-vacuously: the suite is genuinely 254 tests on PG (not a reduced/skipped set), and the engine-specific tests (ActorRole CHECK reject-half, immutability plpgsql trigger, pgsql `show time zone`) all ran on the PG lane this run.
- **PG-readiness gate used (no `sleep`, no flaky pg_isready):** in-PHP PDO connect loop to `pgsql:host=127.0.0.1;port=55432` (bounded 80 attempts, PHP boot self-paces) — ready on attempt 1 returning `PostgreSQL 17.10`. TCP-listening only begins post-init (the entrypoint's temp init-server is socket-only), so a successful TCP connect is the real server, never the double-restart false positive.
- **Files changed:** `tasks.md` (6.1 ✓), this `progress.md`. (Zero production/test files — this is a verification gate; the "test" is the full suite itself, run on both engines.)
- **Quality loop: green on BOTH engines** (the task IS the quality loop, run cross-engine). 16/17 done; only **6.2 spec validation + traceability** remains (likely `CHANGE_COMPLETE`).
- **Learnings for future iterations:**
  - A global-acceptance "cross-engine green" task has no new code/test of its own — its deliverable is the dual-lane proof. Don't invent a pin; run the existing suite on SQLite then the PostgreSQL 17 docker lane, confirm `openspec validate --strict`, record the counts. The non-vacuity check that matters here is *that the PG lane ran the full 254-test set* (same test count, same assertion count or a known engine-guard delta), not just that it reported green.
  - Assertion-count PARITY across lanes (904===904) is expected for this change, NOT a red flag: net-zero engine guards (one SQLite-only assertion balanced by one PG-only one) are still both genuinely executing — equal totals ≠ skipped assertions. When the totals are equal, the proof the PG-specific paths fired is the per-file/per-test breakdown, not the grand total.
  - 6.2 is now teed up: `openspec validate --strict` already re-confirmed green this iter; the remaining work is purely the scenario→test traceability mapping (both *Concurrent Delivery Safety* scenarios → 1.1 reflection tests; all three *Delivery Failure Observability* scenarios → 1.2 `Log::spy` tests) plus confirming the two durable patterns are recorded at the top of this file (they are: white-box concurrency-guard via reflection; engine-guarded DB-CHECK test).
---
