Conventions for every task (see `design.md` for the full rationale, `proposal.md` for the why):

> **TDD** for behavior changes (1.1, 1.2): write the failing test(s) FIRST, then implement to green.
> **Both engines:** every DB-touching test must pass on SQLite (`:memory:`) AND PostgreSQL 17 — SQLite-green is necessary, never sufficient (`knowledge/testing/rules.md`). Engine-specific behavior is engine-guarded (`DB::getDriverName()`); follow the five portability traps (NUL-byte FQCNs, strict uuid/jsonb/timestamptz, savepoint-after-RAISE).
> **Pins:** where a test pins a string that changes (CiWorkflowTest, SweepTest), update the pin in the SAME task.
> **Verify file:line before editing** — line numbers below are from 2026-06-13 and may have shifted.
> **Never** hand-edit `openspec/specs/**` or any protected file; PHP target is 8.5.x (latest stable).

## 1. Substrate correctness (spec-delta-backed)

- [x] 1.1 **C1 — close the inline-vs-sweep delivery race** (TDD, satisfies spec → *Concurrent Delivery Safety*; design D1/D2). RED first, two engine-agnostic tests via reflection/bound-closure on the private methods (single-connection SQLite can't interleave runners): (a) `recordFailure()` does NOT flip a `done` row — seed a delivery, set its DB row `done`/`attempts 1`, invoke `recordFailure($model, new RuntimeException(...))`, assert it stays `done`/`attempts 1`; (b) `attempt()` on a stale-`pending` model whose DB row is already `done` does NOT invoke the handler (`RecordingConsumer::$handled` empty) and leaves the row `done`/`attempts 1`. Then GREEN in `app/Platform/Events/InlineDeliveryExecutor.php`: in `attempt()` (~:117) re-fetch inside the txn with `->lockForUpdate()->first()` and `return` if null or `status !== DeliveryStatus::Pending`, using the locked row for handler + `done` flip; make `recordFailure()` (~:155) a conditional builder update `EventDelivery::query()->whereKey(...)->where('status', DeliveryStatus::Pending->value)->update([...])`. Suite green on both engines.
- [x] 1.2 **C3 — dead-letter logging + sweep run summary** (TDD, satisfies spec → *Delivery Failure Observability*; design D3). RED first with `Log::fake()`/spy (assert channel + level, not wording). GREEN: in `recordFailure()` log `Log::warning` for a still-retryable failure and `Log::error` on the transition to `failed` (the `$attempts >= $maxAttempts` branch), each carrying delivery id + consumer + error message; change `InlineDeliveryExecutor::deliverDue()` (~:80) to return `array{delivered:int, failed:int}` tallied per `attempt()` (keep `deliver()` returning `void`); `SweepCommand::handle()` (`app/Platform/Events/SweepCommand.php:29`) logs an `Log::info` summary (`swept=… failed=…`) from that result and still returns `self::SUCCESS`. Tests: retryable failure → warning + stays `pending`; max-attempt failure → error + becomes `failed`; a sweep run → summary logged. PHPStan max types the new return. Green on both engines.
- [x] 1.3 **C2 — bound the sweep overlap-mutex TTL** (design D4). `routes/console.php:16`: `->withoutOverlapping()` → `->withoutOverlapping(2)` (a 2-minute mutex lease vs the framework's 24h default; `Event::withoutOverlapping($expiresAt = 1440)` sets the public `$expiresAt`, TTL = `expiresAt * 60` s). Update the inline comment in `routes/console.php` and the `SweepCommand` docblock (`:18-21`) that describe the guard. Update the pinned schedule assertion in `tests/Feature/Platform/SweepTest.php` (~:206-218): add `->and($sweep->expiresAt)->toBe(2)` beside the existing `repeatSeconds`/`withoutOverlapping` asserts. Green on both engines.

## 2. Config / dependencies (PHP 8.5)

- [x] 2.1 **C4 — raise the PHP floor to 8.5** (design D5). `composer.json:9` `"php": "^8.3"` → `"^8.5"`, then `composer update --lock` to re-sync the lock content-hash (no package upgrades). `tests/Feature/PlatformRequirementsTest.php:7-9`: `80400` → `80500` and the description `>= 8.4` → `>= 8.5`. VERIFY-ONLY (no edit): `.github/workflows/ci.yml:34,107` already pin `php-version: '8.5'` and `CiWorkflowTest:36` already asserts it. Run that requirement test green; `composer validate` clean. (DevelopmentDocsTest pins neither the floor string nor the PHP row → safe.)
- [x] 2.2 **C5 — document the sweep tunables in `.env.example`** (design D6). Add a commented block after `QUEUE_CONNECTION=database` (~:38): `# EVENTS_SWEEP_MAX_ATTEMPTS=5`, `# EVENTS_SWEEP_BACKOFF_BASE_SECONDS=30`, `# EVENTS_SWEEP_BACKOFF_CAP_SECONDS=3600` — verbatim the `config/events.php` defaults (`:25,28,31`), kept commented so the env stays on defaults. No test (example file); verify `cp .env.example .env && php artisan key:generate` still boots.
- [x] 2.3 **C6 — pin the PostgreSQL session timezone to UTC** (design D6). `config/database.php` `pgsql` connection (`:87-100`): add `'timezone' => 'UTC',`. The connector issues `SET TIME ZONE 'UTC'`; `timestamptz` read-back is unaffected (recorder/immutability tests already assert UTC). SQLite unaffected. Verify the full suite green on PostgreSQL 17; optionally assert `config('database.connections.pgsql.timezone') === 'UTC'`.

## 3. Test-coverage gaps (already-specified behavior — no spec change)

- [x] 3.1 **C7 — pin `event_id` as UUIDv7** (design D7). `tests/Feature/Platform/DomainEventRecorderTest.php` envelope read-back (~:114-115): add `->and($read->event_id[14])->toBe('7')` (the UUIDv7 version nibble at string index 14; the recorder emits `Str::uuid7()` at `DomainEventRecorder.php:83`). Keep the existing `Str::isUuid(...)` assertion. Green on both engines.
- [x] 3.2 **C8 — assert the exponential-backoff CAP** (design D7). `tests/Feature/Platform/SweepTest.php`: add a test that, with `Config::set('events.sweep.backoff_base_seconds', 2000)` (cap stays 3600, max stays 5) under a frozen clock, drives a failing consumer so the first failure's window is `t+2000` (uncapped — proves the base path) and the second is `t+3600` (capped, since `2000*2 = 4000 > 3600`); assert `available_at?->equalTo($t->addSeconds(3600))` at the capped attempt. Engine-agnostic (config + `CarbonImmutable::setTestNow()`; the existing `afterEach` already resets the clock).
- [x] 3.3 **C9 — `actor_role` CHECK rejection on PostgreSQL** (engine-guarded; new file; design D8). New `tests/Feature/Platform/ActorRoleConstraintTest.php` (DatabaseMigrations). Insert a complete, DB-valid `domain_events` row (and an `audit_records` row) whose `actor_role` is an out-of-enum literal, via `DB::table()->insert()` (bypassing the `ActorRole` cast). Guard on `DB::getDriverName()`: `pgsql` → expect a `QueryException` (CHECK `domain_events_actor_role_check` / `audit_records_actor_role_check`, migrations `2026_06_12_000001:70-79` / `000002:80-89`), wrapping the insert in a nested `DB::transaction()` savepoint if asserting row-absence after the throw (trap #5); `sqlite` → the raw insert is accepted (no DB CHECK — the floor there is the enum cast) → assert the row exists, documenting the asymmetry (positive, non-vacuous). Green on both lanes.
- [x] 3.4 **C10 — combined structural + `before`/`after` UPDATE is rejected** (design D8). `tests/Feature/Platform/ImmutabilityTest.php`: add a scenario — seed a valid audit row, then `captureImmutabilityError(fn () => DB::table('audit_records')->where('id', $id)->update(['action' => 'tampered.action', 'before' => $redacted, 'after' => $redacted]))`; assert `toContain('immutable')` and that `action` is unchanged (`voucher.cancel`). Proves the trigger fires on ANY structural change regardless of `before`/`after` — the redaction seam is strictly `before`/`after`-only. Behaviour-only assertion via the savepoint-wrapped helper → both engines.

## 4. CI

- [x] 4.1 **C11 — workflow concurrency group** (design D9). `.github/workflows/ci.yml`: add a top-level `concurrency:` block after the `permissions:` block (~:21) — `group: ci-${{ github.ref }}` and `cancel-in-progress: true` (one workflow-level block governs both lanes). `tests/Feature/CiWorkflowTest.php`: add a test asserting `ciWorkflow()->toContain('concurrency:')->toContain('cancel-in-progress: true')` and the `ci-${{ github.ref }}` group. Run the suite green (CiWorkflowTest reads the file from disk).

## 5. Docs (non-protected)

- [x] 5.1 **C12 — GUIDE §2.7 local PostgreSQL verify step** (design D10). `GUIDE.md` §2.7 (~:114-138): insert a local-PostgreSQL-17 verify step BEFORE the merge step, with the exact command from `knowledge/testing/rules.md:9-13` (`docker run … postgres:17` → `DB_CONNECTION=pgsql … php artisan test` → `docker rm -f pg`), in Italian to match the guide. Do not touch the F1 status line (`FoundationsDocsTest` pins `foundations-money-i18n-flags` / `F1 completata 3/3`). Suite stays green.
- [x] 5.2 **C13 — `docs/development.md` corrections** (design D10). (a) Add a `RALPH_MODEL` bullet to the env-vars list (~:91-94) and correct the stale "The script has no model option of its own" sentence — `ralph.sh` pins `--model "${RALPH_MODEL:-claude-opus-4-8[1m]}" --effort "${RALPH_EFFORT:-max}"` (`ralph.sh:24-25,204`); keep the `RALPH_EFFORT` token (DevelopmentDocsTest pins it). (b) Correct `:113` "nothing under `.claude/`" → no MCP server (`.mcp.json` absent) but `.claude/` carries hooks/skills/team-memory. (c) PHP floor `8.4 → 8.5` at `:11`, `:76`, and the `:121` constraint column. `DevelopmentDocsTest` stays green (locked versions + `RALPH_EFFORT` token preserved).
- [x] 5.3 **C14 — `README.md` exit codes + semantic-verify** (design D10). (a) The ralph exit-codes line (verify current line — was `:68`, not the stale "~19-21"): add `2` (preflight error) and `5` (integrity violation) to match `docs/development.md:104`. (b) The ASCII-flow line (~:28): replace the non-existent `/opsx:verify` with the semantic-verify reference (GUIDE §2.7), aligning with `:53` which is already correct; soften the skills-table "verify" mention (~:94) if needed. No test pins these strings.
- [x] 5.4 **C15 — track the untracked gates in `decisions/INDEX.md`** (design D10). Extend the "Open decisions" section (~:14-16) with explicit entries for the four currently-untracked gates — **secrets management**, **observability**, **PCI boundary**, **architectural security review** — each with a one-line trigger/gate. Keep them in `decisions/INDEX.md` (the existing editable open-gates registry), not a new doc.

## 6. Final verification & traceability

- [ ] 6.1 **Cross-engine green** (global acceptance). On SQLite: `vendor/bin/pint --test` clean, `vendor/bin/phpstan analyse` 0 errors @ max, `php artisan test` all green. Then the PostgreSQL 17 lane locally (GUIDE §2.7 / `knowledge/testing/rules.md:9-13`): `docker run … postgres:17` + `DB_CONNECTION=pgsql … php artisan test` all green; `docker rm -f pg`.
- [ ] 6.2 **Spec validation + traceability** (global acceptance). `openspec validate substrate-hardening --strict` green. Confirm each delta scenario maps to a test: *Concurrent Delivery Safety* ×2 → the 1.1 reflection tests; *Delivery Failure Observability* ×3 → the 1.2 `Log::fake` tests. Record the durable patterns in the change's `progress.md` Codebase Patterns (white-box concurrency-guard test via reflection; engine-guarded CHECK-constraint test).
