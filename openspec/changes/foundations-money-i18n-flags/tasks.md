# Tasks — foundations-money-i18n-flags

> Every task: the five Quality Commands green at the end (format · test_filter · test · type_check · lint), `openspec validate foundations-money-i18n-flags --strict` green, no protected files touched. Read `design.md` D1–D7 before starting any task. Standing rules: **verify every vendor API in `vendor/` before writing it** (lessons.md) — this change leans on Pennant, Laravel custom casts and Laravel localization, all easy to mis-remember; DB-touching test files opt in per-file with `uses(RefreshDatabase::class)`. **Dependency churn is allowed but bounded:** exactly one dependency is added — `laravel/pennant` (latest stable, `^1.23`), in task 3.1 only; every other task keeps `git diff main -- composer.json composer.lock` empty. **Do NOT bump the PHP constraint** (`composer.json` `php ^8.3`→`^8.5` belongs to `substrate-hardening`). The boundary arch test needs **no amendment** — `App\Platform` covers the new sub-namespaces by name-prefix (design D1).

## 1. Money Value Objects (`App\Platform\Money`)

- [x] 1.1 `Currency` (design D2): ISO 4217 code + minor-unit exponent, launch set EUR (base, exp 2) · USD (2) · GBP (2) · CHF (2) · JPY (0); construction from an unsupported code is rejected (fail-closed); a `base()` accessor returns EUR; adding a currency is a one-entry change to the supported map (documented in the class).
  - Acceptance: the four delta scenarios pass (JPY exp 0; EUR/USD/GBP/CHF exp 2; EUR is base; unsupported code throws); type-clean at PHPStan max; happy + failure cases tested; quality commands green.
  - Test hint: `tests/Unit/Platform/Money/CurrencyTest.php` — assert exponents with `toBe`; `Currency::of('XAU')` (or the chosen factory) throws (`InvalidArgumentException`/`ValueError` — pick one, document it); assert the supported set is exactly the five. If realised as a backing enum, mirror the `EnumsTest` verbatim-case style from F1 2/3.
- [ ] 1.2 `Money` (design D2): immutable VO of `int $minorUnits` + `Currency`; **no float-accepting construction path**; `plus`/`minus`/`negate` on minor units with a same-currency guard; value equality; negative amounts valid; `toPayload()` (or equivalent) → `['minor_units' => int, 'currency' => 'EUR']`.
  - Acceptance: the five delta scenarios pass (no float path — assert via the public API surface/reflection that no float parameter exists; minor-units round-trip; cross-currency add throws; payload shape is integer + code; negative representable); happy + failure tested; quality commands green.
  - Test hint: `tests/Unit/Platform/Money/MoneyTest.php` — `Money::of(1234, $eur)` then `->plus(Money::of(66,$eur))` equals `Money::of(1300,$eur)`; `->plus(Money::of(1,$usd))` throws; `expect($m->toPayload())->toBe(['minor_units'=>1234,'currency'=>'EUR'])`; cross-check the no-float contract by asserting the constructor/factory signature takes `int` (a `ReflectionMethod` param-type assertion makes the invariant non-vacuous).
- [ ] 1.3 `FxRate` (design D3): wraps a validated exact decimal string; **no float path**; round-trips the string verbatim; rejects malformed strings.
  - Acceptance: the three delta scenarios pass (`"1.0842"` preserved exactly as a string; no float construction path; malformed strings rejected); happy + failure tested; quality commands green.
  - Test hint: `tests/Unit/Platform/Money/FxRateTest.php` — `expect((string) FxRate::of('1.0842'))->toBe('1.0842')` and `->toBeString()`; `FxRate::of('1.08.42')`, `FxRate::of('abc')`, `FxRate::of('')` each throw; reflection assertion that the factory param is `string`, not `float`.
- [ ] 1.4 `DualCurrencyAmount` (design D3): customer `Money` + EUR-equivalent `Money` + locked `FxRate` + rate timestamp; EUR leg asserted EUR at construction; serialises to the DEC-169 shape `amount` + `currency` + `eur_equivalent_amount` + `fx_rate` + `fx_rate_date`; preserves the locked rate verbatim; **carries no FX policy** (pure representation).
  - Acceptance: the three delta scenarios pass (serialises to the exact DEC-169 key set; non-EUR EUR-leg throws; locked rate preserved for refunds); a guard test asserts the class exposes no rate-deriving/snapshot method (pure representation — `get_class_methods` allow-list); happy + failure tested; quality commands green.
  - Test hint: `tests/Unit/Platform/Money/DualCurrencyAmountTest.php` — build with customer `Money(USD)`, eur `Money(EUR)`, `FxRate('1.0842')`, a fixed date (pass the date in — `Date::setTestNow`/explicit `CarbonImmutable` literal, never `now()` in the assertion); `expect($d->toPayload())->toHaveKeys([...])` and exact values; constructing the EUR leg as USD throws.
- [ ] 1.5 `MoneyCast` (design D2): an Eloquent `CastsAttributes` mapping a `Money` to/from two columns (a minor-units integer + a currency code) — the reusable cast F2 columns (Club Credit, prices) will use. No model uses it yet; prove it against a throwaway test model/table.
  - Acceptance: a `Money` set on a model persists as integer minor units + currency code and rehydrates equal, with no precision loss; reading a row round-trips; quality commands green. (Verify the Laravel 13 `CastsAttributes` two-column `set()` return convention in vendor first — design D2.)
  - Test hint: `tests/Feature/Platform/Money/MoneyCastTest.php` with `uses(RefreshDatabase::class)`; define an anonymous/throwaway migration + model in the test (or a `tests/Support` fixture) with `amount_minor` + `amount_currency` columns cast via `MoneyCast`; write `Money::of(1999,$eur)`, reload, assert equality and the raw column values (`1999`, `'EUR'`).

## 2. Internationalisation (`App\Platform\I18n` + `lang/` + `config/i18n.php`)

- [ ] 2.1 Supported-locale registry (design D4): `config/i18n.php` + a `SupportedLocale` enum (the typed anchor) listing exactly `en, it, fr, de, ja, zh_Hans` with `en` as fallback; a validation helper (`isSupported`/`assertSupported`). `config/app.php` `locale`/`fallback_locale` already = `en` — leave as-is.
  - Acceptance: the three delta scenarios pass (registry is exactly the six; fallback is `en`; an unsupported locale is rejected); quality commands green.
  - Test hint: `tests/Unit/Platform/I18n/SupportedLocaleTest.php` — assert the case/value set verbatim with `toBe` (order-sensitive, mirroring F1 2/3 `EnumsTest`); `SupportedLocale::isSupported('es')` is false, `'zh_Hant'` is false, all six are true; fallback accessor returns `en`.
- [ ] 2.2 `lang/` scaffolding (design D4): translation resources for all six locales — English authored with the keys this change introduces (incl. the welcome-page keys from 2.4); the other five present and falling back to English per key (partial coverage allowed). Verify the Laravel 13 file convention in vendor (`lang/{locale}/*.php` vs `lang/{locale}.json`) before choosing; document the choice in `docs/`.
  - Acceptance: each of the six locales has a resolvable translation resource; a known key resolves in `en` and, when absent in another locale, falls back to `en`; quality commands green.
  - Test hint: `tests/Feature/Platform/I18n/LocalizationTest.php` — `App::setLocale('it')` then `__('welcome.headline')` (or the chosen key) returns the `it` value when present and the `en` value when not; assert a `lang/` resource exists per supported locale (filesystem assertion driven by the `SupportedLocale` registry so it can't silently drift).
- [ ] 2.3 `TranslatableText` VO + `TranslatableTextCast` (design D4): holds `{locale: text}`; `resolve(?string $locale)` returns the locale's text or the English value **for that attribute only** when absent; locale keys validated against the registry at the application layer; round-trips to/from JSON without loss. Do NOT attach it to any module column (no module entities yet).
  - Acceptance: the four delta scenarios pass (resolve returns requested locale; missing locale → English per-attribute; unsupported locale key rejected; JSON round-trip exact); the cast persists/rehydrates the i18n-keyed JSON on a throwaway model; happy + failure tested; quality commands green.
  - Test hint: `tests/Unit/Platform/I18n/TranslatableTextTest.php` for the VO (build `{en,it}`, resolve `it`→it, resolve `fr`→en fallback, build with `es`→throws, `json_encode`/rehydrate equal) + `tests/Feature/Platform/I18n/TranslatableTextCastTest.php` with `RefreshDatabase` for the cast against a throwaway JSON column. Verify the `CastsAttributes` JSON `get`/`set` contract in vendor first.
- [ ] 2.4 `welcome.blade.php` remediation (design D4, debt S3): replace the 72KB default Laravel page with a minimal localized holding page whose visible strings all use `__()` keys (added to `lang/` in 2.2); no routing changes, no SPA, no frontend-stack choice.
  - Acceptance: the delta scenario passes (the welcome view renders only keyed strings — no hardcoded user-facing copy); the page renders 200 at `/`; switching `App::setLocale()` changes the rendered copy (or falls back to `en`); quality commands green.
  - Test hint: `tests/Feature/Platform/I18n/WelcomePageTest.php` — `$this->get('/')->assertOk()` and assert the rendered body contains the resolved translation value for a known key (not a hardcoded literal); a second assertion sets a non-`en` locale that has the key translated and asserts the translated text appears. Keep the placeholder small — the TanStack frontend replaces it later.

## 3. Feature Flags (Laravel Pennant)

- [ ] 3.1 Install Pennant (design D5): `composer require laravel/pennant` (resolves to `^1.23`, latest stable); publish `config/pennant.php`; run/verify the published `features` table migration is SQLite-clean (`:memory:`) and Postgres-truthful (adjust per the DB ADR documented-fallback path if a published construct breaks SQLite); record the exact installed version in `docs/development.md` (+ `progress.md`). This is the ONLY task that changes `composer.json`/`composer.lock`.
  - Acceptance: the first feature-flags delta scenario passes (the `features` table exists after migration; the Pennant service resolves from the container); the full suite stays green on SQLite; `docs/development.md` records the pinned Pennant version; `composer.lock` committed; quality commands green. (If the resolver refuses to install a Laravel-13-compatible Pennant, escalate `HUMAN_NEEDED` — do not force.)
  - Test hint: `tests/Feature/Platform/Features/PennantInstalledTest.php` with `RefreshDatabase` — `expect(Schema::hasTable('features'))->toBeTrue()`; resolve Pennant's manager from the container and assert it is bound. Verify Pennant's published migration/table name in vendor before pinning the assertion.
- [ ] 3.2 EXT-1 NFT/on-chain flag + accessor (design D5): define the `nft-on-chain` feature (global) returning `false` by default; a reusable `App\Platform\Features\Features` accessor (typed name constant/enum + a thin `active()` helper) so call-sites never pass a magic string; document the NS-path-as-universal-fallback convention in `docs/`.
  - Acceptance: the two remaining feature-flags delta scenarios pass (EXT-1 resolves OFF by default; it is resolvable via the accessor and is the single named on-chain gate); the NS-fallback convention is documented; happy + failure (a different/undefined flag name) tested; quality commands green. (Verify Pennant's global-feature definition + `Feature::active`/scope API in vendor first.)
  - Test hint: `tests/Feature/Platform/Features/NftFlagTest.php` with `RefreshDatabase` — assert the accessor reports the flag OFF with no stored value; assert the canonical name constant resolves; a guard asserting the serialization workflow is not gated is a doc/naming assertion at this stage (no serialization code exists yet) — pin the documented convention via the docs test (5.1) rather than fabricating a workflow.

## 4. Actor Context Resolver (`App\Platform\Events`)

- [ ] 4.1 `ActorContext` seam (design D6): resolves `(ActorRole, ?int $actorId)` for the current context — defaults to `(System, null)`; a `runAs(ActorRole, ?int, callable)` scoped override that restores the prior context afterward; **reads no authentication state** and imports no auth/Filament/module code. Refactor the existing platform demo (and any seeder) to obtain its role via `ActorContext` instead of the literal `ActorRole::System` (behaviour-preserving). The `ActorRole` enum and the envelope column are untouched.
  - Acceptance: the three delta scenarios pass (default → System/null; `runAs(NewcoOps,42)` yields that within the callable and restores after; an authenticated session still yields System — auth ignored); the demo/seeder refactor changes no observed behaviour (the F1 2/3 demo + pipeline tests stay green unchanged); happy + failure tested; quality commands green.
  - Test hint: `tests/Unit/Platform/Events/ActorContextTest.php` (or Feature if a container binding is involved) — assert default; wrap a closure in `runAs(ActorRole::NewcoOps, 42, ...)` and assert inside vs. after; the gate-safe case: `$this->actingAs(User::factory()->make())` (or a faked auth) then assert `current()` still returns `System` — proving it reads no auth. A targeted arch-style assertion that `ActorContext` does not import `Illuminate\\Support\\Facades\\Auth`/`App\\Modules` makes the gate-safety non-vacuous.

## 5. Documentation, Traceability & Final Sweep

- [ ] 5.1 Forward-reference cleanup + glossary (design D7): update `docs/event-substrate.md` and the `DomainEventRecorder` docblock from "the F1 3/3 value objects **will** make [a float] unrepresentable" to present tense, pointing at `App\Platform\Money` as the payload-building path; refresh the `GUIDE.md` F1 status line to mark 3/3 authored/done; extend `CONTEXT.md` with Money, Currency, FX Rate, Dual-Currency Amount, Supported locale, Translatable text, Feature flag / EXT-1, and Actor context in the existing house format; document the NS-fallback convention (3.2) and the `lang/` file convention (2.2). Do NOT rewrite the two archived change proposals (historical record).
  - Acceptance: the named docs no longer contain the future-tense forward-reference; `CONTEXT.md` carries the new glossary terms with `_Avoid_` lines where the house format uses them; doc-pin tests cover the behaviour-bearing docs (happy + one failure-ish case per the sibling idiom); `git status` shows no edit to `spec/**`, `openspec/specs/**`, or any archived proposal; quality commands green.
  - Test hint: extend/author `tests/Feature/EventSubstrateDocsTest.php`-style pins (`toContain` on the present-tense sentence + `App\Platform\Money`); if `CONTEXT.md` has a pin test, add the new terms there; otherwise a light `toContain` pin on the new glossary anchors. Mirror the non-vacuous reader-helper pattern (fails loudly on a missing file).
- [ ] 5.2 Final traceability + quality sweep: run the five Quality Commands in CLAUDE.md table order, all green; `openspec validate foundations-money-i18n-flags --strict` green; walk every `#### Scenario:` across all four delta specs (`money`, `i18n`, `feature-flags`, `event-substrate`) and record the scenario → covering-test mapping table in `progress.md` (any uncovered scenario gets its test on the spot); confirm `git diff main -- composer.json composer.lock` shows ONLY the Pennant addition; confirm no protected file is modified.
  - Acceptance: five commands green in table order; validate --strict green; the scenario→test mapping table is in `progress.md` with no uncovered scenario; composer diff is Pennant-only; no protected files touched; the PHP constraint is unchanged.
  - Test hint: this task adds no new tests unless the mapping finds a gap; the deliverable is the recorded mapping + the green sweep. Cross-check the four capability deltas resolve (`openspec show foundations-money-i18n-flags --json --deltas-only` or the installed equivalent — verify the flag in vendor/CLI help).
