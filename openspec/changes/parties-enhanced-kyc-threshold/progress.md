# Progress — parties-enhanced-kyc-threshold

## Codebase Patterns
(consolidated reusable patterns — read first each iteration)

- **Run the FULL suite as `php -d memory_limit=2G vendor/bin/pest`.** `php artisan test` re-spawns Pest as a child process that does NOT inherit an outer `-d memory_limit` flag, and the ~1889-test suite exhausts the 128M CLI default during result collection (a `TestResult.php` fatal, not a test failure). Filtered runs (`php artisan test --filter=Name`) fit in 128M fine.
- **Read DB scalars with `->value('col')`, not `->first()->prop`.** PHPStan (max) flags `DB::table(...)->first()->reason` as property access on `stdClass|null`. `->value('col')` returns the scalar directly (the `ClubCreditSchemaTest` idiom) — no nullable-object access, no suppression needed. Assert money/bigint columns with `->toEqual` (PG returns bigint as a numeric string), never `->toBe`.
- **Value-set CHECK shape follows nullability.** A NOT-NULL enum-backed column → plain PG-only `CHECK (col IN (...))`; an additive-nullable column → `CHECK (col IS NULL OR col IN (...))` (the `2026_06_17_000001_add_compliance_to_parties` variant). Both derive the accepted set from `Enum::cases()` (guarded by `DB::getDriverName() === 'pgsql'`) so the constraint can never drift; on SQLite the enum cast is the floor.
- **Console i18n = console-chrome vs domain-value split, and `operator_console.php` is `array_replace_recursive([main-blocks], [sections-overlay])`.** Console CHROME (column headers, action/notification labels, and per-feature groups like `holds`, `compliance_reviews`) lives in the MAIN `customer` block of `lang/{en,it}/operator_console.php`; its `sections.*` HEADING keys live in a SECOND `customer` block that the file's `array_replace_recursive` merges in — so a feature group goes in the main block while its section heading goes in the overlay block (both land under `customer`). Guard console-chrome keys by front-loading their suffixes into `customerConsoleKitKeys()` in `CustomerConsoleI18nTest` (gives EN-complete + IT-distinct + IT⊆EN for free, ahead of the resolving surface — the holds/kyc-sanctions precedent); `sections.*` headings are conventionally left out of that list (verified by the view render). Enum VALUE labels rendered on a READ surface (a review's `reason`/`threshold_kind`) are DOMAIN vocabulary → they live in the module domain-copy file (`lang/en/parties.php`, `parties.compliance_review.*`), EN-only (there is no `lang/it/parties.php`; per-key EN fallback, DEC-127), guarded by a dedicated enum-iterating test. Enums carry NO `label()` method (SanctionsStatus/HoldType precedent); a Filament Select renders operands value→value (no per-value i18n — "domain data, not UI chrome"), but a read-only DISPLAY of an enum value maps `->value` through the domain-label keys.
- **Narrow `trans()`'s `string|array` return per-group, not per-element.** `trans('a.b.group', [], 'en')` is `string|array`; `assert(is_array($group))` narrows the whole variable so `array_keys($group)` is clean (PHPStan max). Accessing `$group['sub']` afterwards is still `mixed` → resolve each sub-group with its OWN `trans('a.b.group.sub', [], 'en')` + assert (the `CustomerConsoleI18nTest` idiom), never `array_keys($group['sub'])`.

---

## [2026-07-02 19:49] — 1.1 Migration: parties_compliance_reviews
- **Implemented** `database/migrations/2026_07_02_000003_create_parties_compliance_reviews_table.php`: `id`; within-module FK `customer_id`→`parties_customers` (RESTRICT, explicit short index `parties_comp_reviews_customer_fk`); `reason` + `threshold_kind` (string + enum cast + PG-only value-set CHECK derived from `::cases()`); `tripped_amount_minor` (`bigInteger`) + `tripped_currency` (3); nullable `resolved_at` (`timestampTz`); `timestampsTz`. Both value-set columns are NOT NULL → plain `IN (...)` CHECK. Dev-only `down()`. Postgres-truthful + SQLite-compatible, additive, no PG extension.
- **Prerequisite created** — the two enums `ComplianceReviewReason` (`EnhancedKycThreshold`, sole case) + `ThresholdKind` (`SingleTransaction`/`CumulativeAnnual`). Task 1.1's CHECK derives from `::cases()`, so the migration cannot compile without them; hard-coding the tokens would violate the mandated idiom and force a later migration edit. **⇒ Task 2.1's enums ALREADY EXIST** — 2.1's remaining work is ONLY its dedicated enum unit test (case→value mapping + `count(ComplianceReviewReason::cases())===1`).
- **Test** `tests/Feature/Modules/Parties/ComplianceReviewSchemaTest.php` (6 tests, 12 assertions): `hasColumns` (9 cols); open-row insert + amount round-trip via `->value()`/`->toEqual`; `customer_id` column-type == `parties_customers.id`; FK orphan rejection; PG-CHECK-rejects-vs-SQLite-accepts asymmetry for `reason` AND `threshold_kind` (savepoint-wrapped).
- **Files changed:** `ComplianceReviewReason.php`, `ThresholdKind.php`, the migration, the schema test, `tasks.md` (1.1 flipped).
- **Quality loop: green** — Pint clean; filtered 6/6; full suite **1889/1889** (SQLite `:memory:`, via `php -d memory_limit=2G vendor/bin/pest`); PHPStan **0**; `openspec validate --strict` valid. PG17 CHECK-rejection branch runs in the CI tests-pgsql lane / task 7.1 cross-engine close (no local PG server).
- **Learnings for future iterations:**
  - See the three Codebase Patterns above (suite memory-limit command; `->value()` over `->first()->prop`; NOT-NULL vs nullable CHECK shape) — all discovered this iteration.
  - Migration index sequence is continuous: this is `2026_07_02_000003` (after `...000002_create_parties_addresses_table`).
  - The FK index name is `parties_comp_reviews_customer_fk` (abbreviated — the long `parties_compliance_reviews` table name keeps auto-names near PG's 63-char limit). Auto-named CHECKs `parties_compliance_reviews_{reason,threshold_kind}_check` are 39/47 chars — safe.
---

## [2026-07-02 20:35] — 2.3 Event CustomerEnhancedKycReviewRequired (PII-free escalation event)
- **Implemented** `app/Modules/Parties/Events/CustomerEnhancedKycReviewRequired.php` — `final` class mirroring the shipped Parties event idiom (`CustomerRescreeningPassed` for the NAME/ENTITY_TYPE/static-payload facets; `CustomerAnonymised` for the net-new-over-frozen-§15.6 framing per design D5). `const NAME = 'CustomerEnhancedKycReviewRequired'`, `const ENTITY_TYPE = 'Customer'`, `static payload(Customer $customer, ComplianceReview $review): array` = **ONLY** `customer_id`, `enhanced_kyc_at` (`?->toIso8601String()` — the persisted Customer column, single source of truth, the `CustomerAnonymised` idiom), `threshold_kind` (`$review->threshold_kind->value`), `amount` (`Money::of($review->tripped_amount_minor, Currency::of($review->tripped_currency))->toPayload()` → `{minor_units, currency}`, the `ClubCreated::fee` precedent). STRICT PII-free (no name/email/phone/dob).
- **Test** `tests/Unit/Modules/Parties/Events/EnhancedKycReviewEventTest.php` (5 tests, 21 assertions; `TestCase`, NO `RefreshDatabase` — the `ScreeningEventsTest` no-DB idiom): NAME, ENTITY_TYPE, `final`; single-tx €10k payload = `array_keys(...)->toEqualCanonicalizing([the four keys])` + value asserts (`customer_id`=7, `enhanced_kyc_at`==`$c->enhanced_kyc_at?->toIso8601String()`, `threshold_kind`='single_transaction', `amount`==`{1_000_000,'EUR'}`) + PII-free by key AND by value + a `json_encode(...,JSON_THROW_ON_ERROR)` sweep (catches nested/DOB leakage); cumulative €50k variant → 'cumulative_annual' + `{5_000_000,'EUR'}`.
- **Files changed:** `CustomerEnhancedKycReviewRequired.php` (new), `EnhancedKycReviewEventTest.php` (new), `tasks.md` (2.3 flipped).
- **Quality loop: green** — Pint clean; filtered 5/5; full suite **1916/1916** (+5, via `php -d memory_limit=2G vendor/bin/pest`); PHPStan max **0**; `pint --test` clean; `openspec validate --strict` valid.
- **Learnings for future iterations:**
  - No new Codebase Patterns — pure application of the shipped event idiom (`final` + NAME/ENTITY_TYPE/static payload; PII-free; `Money::toPayload()` for the `amount` key) + the `ScreeningEventsTest` no-DB `make()` fixture pattern.
  - **Fixture avoided a DB hit:** `ComplianceReview::factory()->make(['customer_id' => 7, ...])` — the explicit `customer_id` override replaces the factory's `'customer_id' => Customer::factory()` FK, so `make()` resolves no parent and touches no DB (lets the event test stay `TestCase`/no-`RefreshDatabase` like its `ScreeningEventsTest` sibling). In a `make()`-only test the money scalar stays a PHP int → `->toBe(1_000_000)` is safe (the PG-bigint-as-string trap only bites values read back from the DB).
  - **Next → task 3.1** contract `CustomerTransactionTotalsReader::forCustomer(int): CustomerTransactionTotals` in `app/Modules/Parties/Contracts/` + DTO with two readonly EUR `Money` (`largestSingleTransaction`, `trailingTwelveMonthCumulative`); docblock the rolling-trailing-12-month window (design D3). Then 3.2 the `NullCustomerTransactionTotalsReader` + `PartiesServiceProvider::register()` binding (the `PartyComplianceStatusReader` precedent).
---

## [2026-07-02 20:26] — 2.2 Model ComplianceReview (persistence-only + factory)
- **Implemented** `app/Modules/Parties/Models/ComplianceReview.php` (persistence-only, mirroring `Address`/`ClubCredit`): `$guarded = []`, `$table = 'parties_compliance_reviews'`, `casts()` = `reason`→`ComplianceReviewReason`, `threshold_kind`→`ThresholdKind`, `tripped_amount_minor`→`integer`, `resolved_at`→`immutable_datetime`; within-module `belongsTo(Customer::class, 'customer_id')`. NO mutation surface (the `CreateComplianceReview` Action, task 4.1, owns writes); NO domain event of its own (detection Action records `CustomerEnhancedKycReviewRequired`). NO inverse `hasMany` on Customer — Customer.php untouched (read surfaces query by `customer_id`+`resolved_at IS NULL`, task 6.1).
- **Factory** `database/factories/Parties/ComplianceReviewFactory.php`: OPEN single-transaction €10k breach default (`ComplianceReviewReason::EnhancedKycThreshold`, `ThresholdKind::SingleTransaction`, `1_000_000` minor + `Currency::EUR->value`, `resolved_at` NULL); `resolved()` state stamps `resolved_at` (for tasks 6.1/7.1). Enum instances set directly (HoldFactory/ClubCreditFactory convention). Bypasses the Action (pure fixture).
- **Test** `tests/Feature/Modules/Parties/ComplianceReviewModelTest.php` (2 tests, 9 assertions, `RefreshDatabase`): (1) create via factory (override to CumulativeAnnual + €50k), re-fetch, assert both enum casts, `tripped_amount_minor` via BOTH the model `integer` cast (`->toBe(5_000_000)` int-proof) AND the raw column (`->toEqual`, PG bigint-as-string), `tripped_currency`='EUR', `resolved_at` null, `$read->customer->is($customer)` (within-module belongsTo); (2) `resolved()` state → `resolved_at` hydrates to `CarbonImmutable`.
- **Files changed:** `ComplianceReview.php` (new), `ComplianceReviewFactory.php` (new), `ComplianceReviewModelTest.php` (new), `tasks.md` (2.2 flipped).
- **Quality loop: green** — Pint clean; filtered 2/2; full suite **1911/1911** (+2, via `php -d memory_limit=2G vendor/bin/pest`); PHPStan max **0**; `pint --test` clean; `openspec validate --strict` valid.
- **Learnings for future iterations:**
  - No new Codebase Patterns — pure application of the existing sibling model/factory idiom (`Address` = the closest match: within-module `belongsTo(Customer)`, `$guarded=[]`, per-module factory + `newFactory()` override).
  - **Deliberately NO MoneyCast** on the tripping amount: the currency column is `tripped_currency` (not the MoneyCast `{key}_currency` convention), and task 2.3's event re-assembles `Money::of($review->tripped_amount_minor, Currency::of($review->tripped_currency))`. So `tripped_amount_minor`→`integer` + `tripped_currency` stays a raw string — two scalars, documented on the model so a future reader doesn't "fix" it to MoneyCast.
  - **Next → task 2.3** event `CustomerEnhancedKycReviewRequired` (`final`, mirror `CustomerRescreeningPassed`): `const NAME`/`ENTITY_TYPE='Customer'`, `static payload(Customer,ComplianceReview): array` = ONLY `customer_id`, `enhanced_kyc_at` (ISO), `threshold_kind` (`->value`), `amount` (`Money::of(...)->toPayload()`). PII-free. Test: `array_keys(...)->toEqualCanonicalizing([...])` + no PII values present.
---

## [2026-07-02 20:18] — 2.1 Enums ComplianceReviewReason + ThresholdKind (dedicated unit pin)
- **Implemented** ONLY the dedicated enum unit test — the two enums themselves **already exist** (created in task 1.1 as the migration's `::cases()`-derived CHECK prereq; `app/Modules/Parties/Enums/{ComplianceReviewReason,ThresholdKind}.php`). Task 2.1's remaining work was the case→value pin, per the 1.1 progress note.
- **Test** `tests/Unit/Modules/Parties/Enums/ComplianceReviewEnumsTest.php` (5 tests, 9 assertions), mirroring `ComplianceEnumsTest`/`HoldEnumsTest` verbatim-and-order-sensitive convention: (1) `ComplianceReviewReason` case→value map `['EnhancedKycThreshold'=>'enhanced_kyc_threshold']` + `toHaveCount(1)`; (2) `ThresholdKind` map `['SingleTransaction'=>'single_transaction','CumulativeAnnual'=>'cumulative_annual']` + `toHaveCount(2)`; (3) `from()` round-trip of all three spec tokens; (4)+(5) out-of-domain `from()` → `ValueError` for each. Docblock ties the pin to the 1.1 value-set CHECK (derives from `::cases()`, so drift fails here first).
- **Naming:** new file follows the RM-02 `ComplianceReview*` artifact prefix (table `parties_compliance_reviews`, model `ComplianceReview`, `ComplianceReviewSchemaTest`, `ComplianceReviewCopyTest`) and the `*EnumsTest` sibling convention — kept distinct from the `parties-compliance` change's `ComplianceEnumsTest` (KycStatus/SanctionsStatus/ScreeningTriggerSource); no cross-change file edited.
- **Non-redundant:** `ComplianceReviewCopyTest` enumerates the cases only to prove EN labels *resolve*; it never pins the case→value tokens or the count. This is the missing token/count pin (its own docblock names "task 2.1" as the home).
- **Files changed:** `tests/Unit/Modules/Parties/Enums/ComplianceReviewEnumsTest.php` (new), `tasks.md` (2.1 flipped).
- **Quality loop: green** — Pint clean; filtered 5/5; full suite **1909/1909** (+5, via `php -d memory_limit=2G vendor/bin/pest`); PHPStan max **0**; `pint --test` clean; `openspec validate --strict` valid.
- **Learnings for future iterations:**
  - No new Codebase Patterns — pure application of the existing `*EnumsTest` verbatim-map+count+from()-round-trip+out-of-domain-`ValueError` idiom.
  - **Next → task 2.2** model `ComplianceReview` (persistence-only, `$guarded=[]`, `$table='parties_compliance_reviews'`, casts for both enums + `tripped_amount_minor`→integer + `resolved_at`→immutable_datetime, within-module `belongsTo(Customer)`, factory under `Database\Factories\Parties\`). Assert `tripped_amount_minor` with `->toEqual` (PG bigint-as-string trap — Codebase Pattern).
---

## [2026-07-02 20:11] — 1.2 Localized copy + CONTEXT.md glossary
- **Implemented** the front-loaded i18n copy + glossary for RM-02 (all consumed by later tasks — 6.1 console surface, the review-reason display):
  - `lang/en/parties.php`: new `compliance_review` DOMAIN-label group — `reason.enhanced_kyc_threshold` = "Enhanced-KYC threshold"; `threshold_kind.{single_transaction,cumulative_annual}` = "Single transaction" / "Rolling 12-month cumulative" (label says "rolling" — the `cumulative_annual` token is trailing-12mo, not calendar-YTD, design D3). These render the enum backing VALUES on a read surface (the enums carry no `label()`).
  - `lang/{en,it}/operator_console.php`: new `customer.compliance_reviews` CHROME group (`enhanced_kyc_flag`, `enhanced_kyc_at`, `columns.{reason,threshold_kind,amount,opened_at}`) in the MAIN block + `customer.sections.compliance_reviews` heading in the sections OVERLAY block. IT genuinely translated (KYC/compliance kept as domain terms; "enhanced"→"rafforzata"), each distinct from EN.
  - `CONTEXT.md`: added 3 glossary terms under Compliance & Finance — **Enhanced-KYC Threshold**, **Compliance Review Queue** (`ComplianceReview`, naming the PII-free `CustomerEnhancedKycReviewRequired` payload), **CustomerTransactionTotalsReader**; updated the *Screening trigger source* term + the two "deferred" seam bullets (§ compliance-screening line 217 + the trigger-source line) so `aml_threshold` reads **now landed** while the 12-month **cadence** stays deferred (no self-contradiction — the file's "**now landed**" convention).
- **Tests:** new `tests/Feature/Modules/Parties/ComplianceReviewCopyTest.php` (3 tests — enumerates every `ComplianceReviewReason`/`ThresholdKind` case → asserts an authored EN label that resolves ≠ raw key, + a reverse orphan-copy guard `array_keys == enum tokens`); extended `customerConsoleKitKeys()` in `CustomerConsoleI18nTest` with the 6 `compliance_reviews.*` chrome suffixes (front-loaded, +12 dataset cases across the EN-complete + IT-distinct legs) + a docblock bullet.
- **Files changed:** `lang/en/parties.php`, `lang/en/operator_console.php`, `lang/it/operator_console.php`, `CONTEXT.md`, `tests/Feature/Modules/Parties/ComplianceReviewCopyTest.php` (new), `tests/Feature/Modules/OperatorPanel/Parties/CustomerConsoleI18nTest.php`, `tasks.md` (1.2 flipped).
- **Quality loop: green** — Pint clean; full suite **1904/1904** (+15: 3 new + 12 dataset); PHPStan max **0**; `vendor/bin/pint --test` clean; `openspec validate --strict` valid.
- **Learnings for future iterations:**
  - See the new Codebase Patterns above (console-chrome-vs-domain-value i18n split + the `array_replace_recursive([main],[sections-overlay])` file shape; per-group `trans()` narrowing for PHPStan max).
  - **Task 6.1 consumes these keys** — the console panel resolves `operator_console.customer.compliance_reviews.*` (chrome, already in the i18n contract) + `customer.sections.compliance_reviews` (heading) and maps the review's `reason`/`threshold_kind` `->value` through `parties.compliance_review.*` (domain labels). No new copy needed there beyond wiring.
  - **`CustomerConsoleI18nTest` cannot run by bare path** — its sink-scanner (`scanOperatorConsoleHardcodedSinks`) is declared in `ProductMasterConsoleI18nTest` and only loaded by `--filter`/full-suite; a bare-path run fails that one test (expected, not a regression).
  - Running two test files by explicit path also skips cross-file function declarations → run the **full suite** for the authoritative green.
---

## [2026-07-02 20:43] — 3.1 Contract CustomerTransactionTotalsReader + DTO CustomerTransactionTotals
- **Implemented** the Module-S transaction-totals read-port seam (design D3/D4; party-registry — *Enhanced-KYC Threshold Detection*):
  - `app/Modules/Parties/Contracts/CustomerTransactionTotals.php` — a plain readonly boundary DTO (the `ComplianceStatus` precedent) carrying two readonly `App\Platform\Money\Money`: `largestSingleTransaction` + `trailingTwelveMonthCumulative`, both EUR. Docblock pins the **rolling trailing-12-month** window (design D3, NOT calendar-YTD), the two-independent-OR-signals framing, and the money/PII-free boundary posture.
  - `app/Modules/Parties/Contracts/CustomerTransactionTotalsReader.php` — the interface `forCustomer(int $customerId): CustomerTransactionTotals` (naming mirrors `PartyComplianceStatusReader::forCustomer`). Docblock documents the rolling window as the contract's semantic obligation, and that the real impl is Module S's (deferred, Phase 4) with the launch binding a zero-returning null adapter (task 3.2) → detection a correct no-op until Module S lands.
- **Test** `tests/Unit/Modules/Parties/Contracts/CustomerTransactionTotalsReaderTest.php` (4 tests, 11 assertions) — PURE value test, NO `uses(TestCase::class)` (no DB, no app boot: DTO + Money are pure values, composer-autoloaded). An in-test anonymous-class fake reader returns caller-set totals; asserts the DTO round-trips both EUR figures (identity `->toBe` + `Money::equals` + `currency` = `Currency::EUR`), and a 3-row dataset proves the two figures are held independently (field-swap guard; realistic cumulative ≥ single, incl. the `[0,0]` null-adapter shape).
- **Files changed:** `CustomerTransactionTotals.php` (new), `CustomerTransactionTotalsReader.php` (new), `CustomerTransactionTotalsReaderTest.php` (new), `tasks.md` (3.1 flipped).
- **Quality loop: green** — Pint clean (auto-imported `Money` in the interface + shortened its `{@see}`); filtered 4/4; full suite **1920/1920** (+4, via `php -d memory_limit=2G vendor/bin/pest`); PHPStan max **0**; `pint --test` clean; `openspec validate --strict` valid.
- **Learnings for future iterations:**
  - No new Codebase Patterns — application of the `ComplianceStatus`/`PartyComplianceStatusReader` boundary read-port idiom (plain readonly DTO in `Contracts/` beside its interface; PII-free; `forCustomer(int)` naming).
  - **Pure-value unit tests need NO `uses(TestCase::class)`** — `tests/Pest.php` binds `TestCase` only to `Feature`; a no-DB/no-cast value test (Money + DTO + an anonymous fake) runs on the plain PHPUnit base, faster than booting the app. Contrast the sibling event/model no-DB tests, which DO call `uses(TestCase::class)`: they need the boot for the models' enum/datetime CASTS; a pure DTO has none.
  - **DTO carries no currency guard** (deliberate): mirrors the `ComplianceStatus` plain-carrier precedent; EUR is a documented contract, and currency-safety is enforced downstream where it bites — task 4.2's threshold comparison via `Money` arithmetic throws on a currency mismatch (fail-closed), so a DTO guard would be redundant, not root-cause.
  - **Next → task 3.2** `NullCustomerTransactionTotalsReader` in `app/Modules/Parties/Reads/` (both fields `Money::of(0, Currency::of('EUR'))`) bound in `PartiesServiceProvider::register()` (the `PartyComplianceStatusReader`→`DatabaseComplianceStatusReader` bind precedent, line 26); + an arch assertion it references no `App\Modules\Commerce\*`.
---
