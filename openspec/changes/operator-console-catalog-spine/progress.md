# Progress — operator-console-catalog-spine

## Codebase Patterns
(consolidated reusable patterns — read first each iteration)

- **The operator-console kit — extracted in tasks 1.1 + 1.2; tasks 2.1–5.2 REUSE it verbatim.** It lives under `app/Modules/OperatorPanel/Filament/Console/` — deliberately NOT under `Filament/{Resources,Pages,Widgets}`, because Filament's `discoverResources/discoverPages/discoverWidgets` scan only those three dirs (1.1 of the predecessor repointed them there), so an **abstract** base Resource/Page placed in `Console/` is never auto-discovered (and never instantiated as a real page/resource). Five pieces:
  - `Console/Concerns/SurfacesDomainActions` (**trait**): the operator-action wrapper. `surfaceLifecycleOutcome(Closure $run, string $successTitle)` (now an INSTANCE method — it reads `$this->i18nKey()` for the `notifications.action_failed` danger title; renders success/danger `Notification`; catches `RuntimeException` by **base type** so nothing is imported from a module's `Exceptions` namespace — the {Models, Actions} surface holds). `lifecycleAction(string $verb, string $successKey, Closure $invoke, ?array $form, ?string $confirmationKey): Action` — the factory that builds one uniform lifecycle action: label = `operator_console.<i18nKey>.actions.`.`Str::snake($verb)` (so the camelCase action id `retireCascade` resolves the existing `retire_cascade` key), success notification = `…notifications.<successKey>`, optional reject-notes form, optional `…<confirmationKey>` confirmation affordance. `recordOf(class-string<TModel>, Model): TModel` — narrows the page record to the concrete model. `abstract i18nKey(): string` (INSTANCE — pages).
  - `Console/OperatorConsoleViewRecord` (**abstract base View**, extends `Filament\Resources\Pages\ViewRecord`, `use SurfacesDomainActions`): builds the FIVE uniform header actions (submit · reject[notes] · activate[`affordance.second_actor`] · retire · reopen) from `abstract lifecycleInvocations(): array<string, Closure(Model, string): mixed>`.
  - `Console/OperatorConsoleResource` (**abstract base Resource**, extends `Filament\Resources\Resource`) — the read-only conventions (task 1.2): `getModelLabel/getPluralModelLabel` resolve `operator_console.<entity>.{label,plural_label}` off `abstract protected static function i18nKey(): string` (STATIC here — labels/columns are static); `lifecycleStateColumn(): TextColumn` (the badge, rendering `$record->getAttribute('lifecycle_state')` via `instanceof BackedEnum ? (string) $state->value : ''` — type-clean at phpstan max, NO `Catalog\Enums` import) + `versionColumn(): TextColumn`. Adds NO mutating action. A subclass sets `$model`/`$recordTitleAttribute`, supplies `i18nKey()` + its own `form/table/infolist/getPages`, and spreads `static::lifecycleStateColumn(), static::versionColumn()` into its columns (interleave if a column sits between them, as Master's producer does). Master-only producer picker / cascade stay on the subclass (design L6).
  - `Console/OperatorConsoleCreateRecord` (**abstract base Create**, extends `Filament\Resources\Pages\CreateRecord`) — the write-through create template (task 1.2): `$canCreateAnother=false`; `handleRecordCreation()` FULLY overrides Filament's default `new Model($data); $record->save()` (which the no-Eloquent-write rule would fail) and delegates to `abstract createViaAction(array $data): Model` wrapped in the rejection catch — a localized domain `RuntimeException` → `data.<createRejectionField()>` form error (Master dedup, Composite `<2`). `abstract createRejectionField(): string`. **PR is the one special case:** its `createViaAction` catches the framework `Illuminate\Database\UniqueConstraintViolationException` itself and throws a `ValidationException` (NOT a RuntimeException → sails through the base catch) with a console-owned key, so the raw SQL never renders (design L5).
  - A **per-entity View page** extends the base, sets `protected static string $resource`, returns its `operator_console.<entity>` root from `i18nKey()` and its 5 invocations from `lifecycleInvocations()`. Entity-specific extras override `getHeaderActions()` and **spread `parent::getHeaderActions()`** (e.g. Master appends only `retireCascade`). A **per-entity Create page** supplies just `createViaAction` (narrow form state → `app(<CreateAction>)->handle(...)`; a malformed payload throws `InvalidArgumentException` = LogicException, which propagates past the base's RuntimeException catch — a 500, not a form error) + `createRejectionField`. The producer picker / cascade are Master-only extensions, never in the base (design L6).
- **The typing pattern that keeps the kit PHPStan-`max` clean with NO shared action interface (verified):** the Catalog actions share no interface and each `handle()` takes its CONCRETE model (param e.g. `$master`), so a dynamic `app($classString)->handle($record)` cannot type-check (the action resolves to `object`/`mixed`). Instead the per-entity subclass supplies **typed invoke-closures**: `fn (Model $record, string $notes) => app(<ConcreteAction>::class)->handle($this->recordOf(<Model>::class, $record)[, $notes])`. `recordOf` narrows `Model`→concrete via `instanceof $class` on a `class-string<TModel>` (phpstan narrows the return to `TModel`); `app(<Literal>::class)` returns the concrete action (literal class-string → template-bound), so `->handle(<concrete>)` is clean. Give every invoke-closure the uniform `(Model $record, string $notes)` signature (`$notes` is `''` for the form-less verbs) — a uniform `string $notes` avoids the `array<string,mixed>` annotation friction inline closures otherwise need; the factory extracts notes from Filament's `$data` once.
- **`Action::form()`'s component base is `Filament\Schemas\Components\Component`, NOT `Filament\Forms\Components\Component` (the latter does NOT exist in Filament 5).** A `@param array<int, Component>` for an action form MUST reference `Filament\Schemas\Components\Component` (`Textarea`→`Field`→`Schemas\Components\Component`). Pint's `fully_qualified_strict_types` turns a bare/`Forms\` `Component` docblock into an import of the **non-existent** `Filament\Forms\Components\Component` → phpstan `class.notFound`; import the Schemas one explicitly. (→ lessons.md)
- **Filament action `data` injection is ALWAYS an array** — `->action(fn (Model $record, array $data) => …)` is safe for form-LESS actions too: `Action::resolveDefaultClosureDependencyForEvaluationByName('data')` returns `getData(): array` = `[]` when no form is mounted (verified in `vendor/filament/actions/src/Action.php`). So one uniform factory closure can always request `$data` and narrow `notes` from it (`is_string($data['notes'] ?? null) ? … : ''`).
- **In the read-only `<Entity>Resource`, name the create-action in PROSE backticks, NEVER `{@see \App\Modules\Catalog\Actions\Create<Entity>}`.** Pint's `fully_qualified_strict_types` rewrites a FQ `@see` into a short `@see` + a real `use` import — pulling a Catalog *Action* into a Resource that uses it only in a docblock (an unused import; allowed by the {Models, Actions} carve-out so it's harmless to tests, but untidy and not auto-removed by Pint's preset). The Resource's only Catalog import should be the *Model*. The Create *page* legitimately imports + `@see`s the action (it calls it). Verified on 2.1 Format (the action import had to be hand-removed); applies to 2.2–4.1.
- **Reference-integrity retire-block test scaffold (2.2 Case Config; reused by PR 3.2).** The blocking active referencer is stood up **directly via the model factory** (`SellableSku::factory()->create(['case_configuration_id' => $cc->id, 'lifecycle_state' => Active])`), NOT through the `CreateSellableSku`+activate chain — the `RetirementReferenceIntegrityGate` reads only `lifecycle_state`, so a factory-active SKU is a legitimate fixture, and the factory bypasses the Create action so **no `SellableSKUCreated` muddies the event count** (proven shape: `tests/Feature/Modules/Catalog/RetirementCascadeTest.php`). The factory auto-builds the SKU's `product_reference_id` (irrelevant to a Case-Config retire gate, which only reads SKUs by `case_configuration_id`). `RetirementReferenceIntegrityViolation extends RuntimeException`, so the kit's `surfaceLifecycleOutcome` catches it by base type → the `…notifications.action_failed` danger title (body = the domain's `catalog.retirement.blocked_by_active_references`, already shipped — **no console-owned key needed**). Block test asserts: `assertNotified(action_failed)` + entity stays `active` + 0 `*Retired` + the SKU unaffected; unblock half flips the SKU to `Retired` (a closed reference does not block) → retire succeeds + 1 `*Retired`. For PR 3.2, the same shape with the SKU referencing the **PR** (`product_reference_id`) plus the Composite-SKU constituent-junction referencer (`CompositeSku::factory()->hasAttached($pr, ['position' => 1], 'constituents')`).
- **Hierarchical-entity recipe confirmed on 3.1 (Product Variant) — PURE reuse + a parent picker, zero base/kit change.** A hierarchical spine entity adds, over the standalone recipe, exactly TWO things on the *create* surface and ONE test, and NOTHING on the view page: (1) a **parent picker** `Select::make('<parent_fk>')->options(self::<parent>Options(...))->required()` sourced from the parent's own Catalog model via a `private static <parent>Options(): array` helper (mirror `ProductMasterResource::producerOptions()` — render the parent's `lifecycle_state->value` in the label, NO `Catalog\Enums` import; the picker lists EVERY parent — the cascade gate, not the picker, enforces parent-active); (2) the `createViaAction` narrows the parent FK with `is_numeric → (int)`. The **cascade gate is surfaced FOR FREE** — `Activate<Child>` throws `ActivationCascadeViolation` (extends `RuntimeException`) when a parent is not `active`, the kit's `surfaceLifecycleOutcome` catches it by base type → `…notifications.action_failed` danger title (body = `catalog.gate.parent_not_active`, already shipped) — so the View page is the EXACT Case-Config shape (5 invocations, NO `getHeaderActions()` override, NO cascade-retire). The new test is the **cascade-gate-blocked activate**: a parent stood up NON-active via `<Parent>::factory()->create(['lifecycle_state' => Reviewed])`, child submitted with **3 distinct operators** (so it's the cascade — not SoD — that blocks), `callAction('activate')` → `assertNotified(action_failed)` + child stays `reviewed` + 0 `*Activated` + 0 activated audit. The success-activate path uses a **factory-active parent** (`->create(['lifecycle_state' => Active])` — bypasses the Create action, no event). A hierarchy-parent retire test proves single-entity retire **preserves active children** (seed `<Child>::factory()->create(['<fk>' => $parent->id, 'lifecycle_state' => Active])`; retire the parent → parent `retired`, child stays `active`, 1 `*Retired`) — `Retire<Variant/Reference/SKU>` carries no within-catalog block (only PR/CaseConfig retire do). **Gotcha (3.1):** displaying a wine attr in the read surface uses the within-Catalog `wineAttributes` relation, which is `|null` (model docblock) — in a test, update it through the loaded **model** (`$x->wineAttributes?->update([...])`, property + nullsafe) NOT the relation **query** (`$x->wineAttributes()->update([...])`) — the query form bypasses the model casts (a `TranslatableText` → raw-SQL bind error) and the model-property form needs `?->` for phpstan. In a resource helper that reads the relation twice, bind it to a local + explicit `=== null` guard (avoids the `nullsafe.neverNull` on a `?->x ?? ''`). **3.2 (PR) CONFIRMED:** this recipe with TWO parent pickers (Variant + Format, each via a `<parent>Options()` helper) PLUS the PR-special create-error catch PLUS the retire reference-integrity block (the 2.2 scaffold) — see the dedicated bullets below. **3.3 (Sellable SKU)** = the same two-parent recipe (PR + Case Configuration pickers + commercial fields) but NO create guard (a PR+CaseConfig pair may back multiple SKUs — do not add uniqueness); the cascade gate is PR **and** Case Config active; it is a leaf (retire has no within-catalog block). **4.1 (Composite)** adds the ordered N≥2 constituent picker + the `<2` create-error (a localized RuntimeException — the base catch handles it, no PR-style framework catch needed).
- **The PR create-error special case (3.2, design L5) — CONFIRMED, and the trap that makes it special.** `CreateProductReference` has NO app-layer dedup; a dup `(variant,format)` surfaces as `Illuminate\Database\UniqueConstraintViolationException`. **That exception IS a `RuntimeException`** (via `QueryException → PDOException → RuntimeException`), so if it propagates it hits the base `OperatorConsoleCreateRecord::handleRecordCreation`'s `catch (RuntimeException)` and renders `$e->getMessage()` = raw SQL on the `createRejectionField()`. So the Create page MUST catch `UniqueConstraintViolationException` **inside** `createViaAction` and re-throw `ValidationException::withMessages(['data.product_variant_id' => (string) __('operator_console.product_reference.duplicate_reference')])`. `ValidationException` is NOT a `RuntimeException` (extends `Exception`), so it sails through the base catch and Filament renders it as a form error. The console-owned key (`operator_console.product_reference.duplicate_reference`) is the ONE console-owned i18n key the whole change adds — and it **must be colon-free**: Livewire's `assertErrorMatchesRuleOrMessage` truncates an expected message at the first `:` before the `assertContains`, so a `:` in the copy would silently weaken the message assertion. `UniqueConstraintViolationException` is a FRAMEWORK class — importing it does NOT touch the {Models, Actions} carve-out. Under `DatabaseMigrations` the dup insert's violation is rolled back to the action's own `DB::transaction` savepoint (the `CreateProductReference` action is designed for this), so the connection survives the assertions on PG — verified 145/145 on PG17.
- **Asserting a console form-error MESSAGE (not just the field):** `->assertHasFormErrors(['<field>' => (string) __('<console key>')])` — Filament prefixes `<field>`→`data.<field>` (the form statePath) and Livewire's matcher asserts the error's messages array CONTAINS EXACTLY that string. This is what proves the rendered copy is the console key, not the raw SQL (an int-keyed `['<field>']` only checks existence — pass the string-keyed form to pin the message). Verified on the PR duplicate.
- **PR retire reference-integrity covers BOTH terminal-sellable edges (3.2).** `RetireProductReference`'s gate reads, within Module 0, active Sellable SKUs by `product_reference_id` AND active Composite SKUs bundling the PR via the `constituents` junction. Two block tests: (a) `SellableSku::factory()->create(['product_reference_id' => $pr->id, 'lifecycle_state' => Active])` (factory auto-builds the SKU's Case Config; no `SellableSKUCreated` event); (b) `CompositeSku::factory()->hasAttached($pr, ['position' => 1], 'constituents')->create(['lifecycle_state' => Active])` — `hasAttached` runs before the factory's `afterCreating` (which auto-attaches two PRs only when the bundle is empty), so the PR is the bundle's sole constituent, enough for the gate's `whereHas`. Both surface `action_failed` (body = the shipped `catalog.retirement.blocked_by_active_references`) + PR stays `active` + 0 `*Retired`. Unlike 3.1 (Variant), a PR has NO "preserve active children" test — its only children are exactly those terminal SKUs, which BLOCK the retire rather than being preserved.
- **Spine-entity reuse confirmed PURE on 2.1 (Format).** Adds exactly: `FormatResource extends OperatorConsoleResource` (`i18nKey()='format'`, `$model`/`$recordTitleAttribute`, scalar create form, columns name/size_label/volume_ml + `static::lifecycleStateColumn()`/`versionColumn()`, infolist, `getPages()`); `ListFormats` (header create-link); `ViewFormat extends OperatorConsoleViewRecord` (`i18nKey()` + 5 typed invocations, NO `getHeaderActions()` override → exactly the kit's 5 actions, NO cascade); `CreateFormat extends OperatorConsoleCreateRecord` (`createRejectionField()='name'` safety-net + `createViaAction()` narrowing → `CreateFormatAction::handle`). Zero base/kit change, zero boundary amendment. The scope-guard "no cascade-retire for the six" is pinned by `assertActionDoesNotExist('retireCascade')` on the view page.

---

## [2026-06-20 14:20] — 1.1 Operator-action wrapper + base View page; retrofit Master's View page
- **What:** extracted the shared **operator-console kit** (resolving the predecessor's design-L9 deferral; ADR 2026-06-20) and **retrofitted Product Master's view page** onto it — no Master behaviour change. Created the trait `SurfacesDomainActions` (promotes the `surfaceLifecycleOutcome` try/notify helper — previously a `private static` on `ViewProductMaster` — to a shared instance method keyed off `i18nKey()`; adds the `lifecycleAction` factory + the `recordOf` narrowing primitive) and the abstract base `OperatorConsoleViewRecord` (builds the FIVE uniform lifecycle header actions from a per-entity `lifecycleInvocations()` map). `ViewProductMaster` now `extends OperatorConsoleViewRecord` and defines only `$resource`, `i18nKey()` (`product_master`), its 5 invocations, and a `getHeaderActions()` override appending ONLY the Master-only `retireCascade`.
- **Files changed:** `app/Modules/OperatorPanel/Filament/Console/Concerns/SurfacesDomainActions.php` (new); `app/Modules/OperatorPanel/Filament/Console/OperatorConsoleViewRecord.php` (new); `app/Modules/OperatorPanel/Filament/Resources/Catalog/ProductMasterResource/Pages/ViewProductMaster.php` (retrofitted — net −20 lines, the 5 actions + the helper now come from the kit). No lang/test/migration/composer change.
- **Quality loop:** green — format ✓ (Pint); filtered ✓ `ProductMasterLifecycleConsoleTest` 17/17 (157 assertions); OperatorPanel+Architecture ✓ 86/86 (673); full suite ✓ **996/996** (5115, SQLite — unchanged vs the Master-merge baseline: a behaviour-preserving refactor adds no tests); phpstan ✓ 0 errors; pint --test ✓. `openspec validate --strict` ✓. composer diff vs main empty; no migrations; no protected files (only app code under `Filament/`).
- **PG17 ✓:** docker `postgres:17` (port 55432), `tests/Feature/Modules/OperatorPanel` = **76/76, 469 assertions, exit 0** (`DB_CONNECTION=pgsql … php -d memory_limit=512M vendor/bin/pest`); container torn down. The retrofit's write-through actions, the cascade multi-entity retire, the SoD self-approval + Producer-gate rejections all surface clean on PG.
- **Acceptance walked:** `ViewProductMaster extends OperatorConsoleViewRecord` and defines only its Master-only extra (cascade-retire) ✓; the shipped Master lifecycle/console tests stay green — submit/reject/activate/retire/reopen + SoD self-approval + producer-gate surfacing + cascade all in the 17/17 lifecycle file + the 86/86 OperatorPanel/arch run (resource, create-with-dedup, chain, i18n) ✓; `NoEloquentWriteInOperatorPanelRule` (phpstan 0) + `ModuleBoundariesTest` (in the 86/86) stay green — the kit catches `RuntimeException` by base type, imports only Filament + `Eloquent\Model` + global exceptions, so the `{Models, Actions}` carve-out is untouched (no boundary amendment, as design L9 predicted) ✓; quality commands green ✓; PG17 ✓.
- **Key design calls:**
  - **No shared action interface ⇒ per-entity typed invoke-closures + `recordOf`.** With phpstan at `max` and the Catalog actions sharing no contract (each `handle()` takes its concrete model), a class-string-driven dynamic dispatch can't type-check. The kit owns the uniform Action *assembly* (label/form/confirmation/surfacing); the subclass owns only the one irreducible per-verb line `app(<Action>::class)->handle($this->recordOf(<Model>::class, $record)[, $notes])`. This is the real DRY win the ADR named (the surfacing wrapper) while staying fully typed.
  - **Kit home = `Filament/Console/`** (not under the three discovered Filament dirs) so the abstract bases are never auto-discovered. Task 1.2's `OperatorConsoleResource`/`OperatorConsoleCreateRecord` go here too (a base `Resource` placed under `Filament/Resources/` WOULD be discovered + fail).
  - **Action order changed** (cascade moved to the end via the `parent` spread) — harmless: every Master test uses `callAction`/`assertActionExists` (order-independent).
  - **No new test added (deliberate, per the task hint).** `surfaceLifecycleOutcome` + `lifecycleAction` are exhaustively exercised through Master by the shipped `ProductMasterLifecycleConsoleTest` (both the success-notification and the danger-`action_failed` paths, across all six verbs + SoD + gate + cascade) — that suite IS the retrofit's regression net. A standalone trait test would need a dummy Filament host + the notification session and would only duplicate that coverage.
- **Learnings for future iterations:**
  - The kit shape + the no-interface typing pattern + the two Filament gotchas (`Schemas\Components\Component`; `data` always an array) are now in **Codebase Patterns** (top) — read them before 2.1. Each of the six entities is now: a per-entity `<Entity>Resource` (task 1.2's base Resource, pending) + a `View<Entity>` page extending `OperatorConsoleViewRecord` (5 invocations) + a `Create<Entity>` page (task 1.2's base Create) + `operator_console.<entity>.*` copy.
  - **Next (1.2):** extract the base read-only `OperatorConsoleResource` + the base `OperatorConsoleCreateRecord` into `Filament/Console/`, and retrofit `ProductMasterResource` + `CreateProductMaster` onto them (Master keeps its producer picker + dedup mapping as extensions). Keep the whole Master suite + arch/boundary green; confirm composer diff stays empty. The base CreateRecord's `handleRecordCreation` MUST delegate to a per-entity `createViaAction` (never the default `new Model; save()` — the 1.2 PHPStan rule proves it). **PG17 task.**
---

## [2026-06-20 14:36] — 1.2 Base read-only Resource + base Create page; retrofit Master's Resource + Create page
- **What:** completed the operator-console kit (resolving design L9). Extracted the two remaining bases under `Filament/Console/` and retrofitted Product Master onto them — behaviour-preserving. `OperatorConsoleResource` (abstract, extends `Filament\Resources\Resource`): `getModelLabel/getPluralModelLabel` off a `static i18nKey()`, the shared `lifecycleStateColumn()` (BackedEnum-cast badge) + `versionColumn()` helpers, no mutating action. `OperatorConsoleCreateRecord` (abstract, extends `CreateRecord`): `$canCreateAnother=false` + a `handleRecordCreation` template that delegates to `abstract createViaAction(array): Model` wrapped in the localized-`RuntimeException`→`data.<createRejectionField()>` catch, FULLY overriding Filament's default `new Model; save()`. `ProductMasterResource` now `extends OperatorConsoleResource` (defines `i18nKey()='product_master'`, spreads `static::lifecycleStateColumn()`/`versionColumn()` into its columns at the original positions, keeps its Master-only producer picker + `producerLabel/producerOptions`); `CreateProductMaster` now `extends OperatorConsoleCreateRecord` (supplies `createRejectionField()='name'` + `createViaAction()` = the payload narrowing + `app(CreateProductMaster)->handle(...)`).
- **Files changed:** `app/Modules/OperatorPanel/Filament/Console/OperatorConsoleResource.php` (new); `app/Modules/OperatorPanel/Filament/Console/OperatorConsoleCreateRecord.php` (new); `ProductMasterResource.php` (retrofit — net −15 lines: the two label methods + the inline lifecycle/version columns now come from the kit); `ProductMasterResource/Pages/CreateProductMaster.php` (retrofit — `$canCreateAnother` + the try/catch now in the base; the page is `createViaAction` + `createRejectionField`). No lang/test/migration/composer change.
- **Quality loop:** green — format ✓ (Pint); OperatorPanel+Architecture ✓ 86/86 (673 assertions); full suite ✓ **996/996** (5115, SQLite — unchanged vs the 1.1 baseline; behaviour-preserving refactor adds no tests); phpstan ✓ 0 errors (the `NoEloquentWriteInOperatorPanelRule` analyses both new `Console/` bases — green proves the base contains no Eloquent write, i.e. it does NOT fall back to `new Model; save()`); pint --test ✓. `openspec validate --strict` ✓. composer diff vs main empty; no migrations; no protected files (only app code under `Filament/`).
- **PG17 ✓:** docker `postgres:17` (port 55432), `tests/Feature/Modules/OperatorPanel` = **76/76, 469 assertions, exit 0** (`DB_CONNECTION=pgsql … php -d memory_limit=-1 vendor/bin/pest`); container torn down. The retrofitted write-through create + the BR-Identity-1 dedup→form-error + the lifecycle actions surface clean on PG.
- **Acceptance walked:** `ProductMasterResource extends OperatorConsoleResource` + `CreateProductMaster extends OperatorConsoleCreateRecord` ✓; the ENTIRE shipped Master suite (resource/list/view in `ProductMasterResourceTest`, create-with-dedup in `ProductMasterCreateConsoleTest`, i18n in `ProductMasterConsoleI18nTest`, the closing chain in `ProductMasterConsoleChainTest`, lifecycle in `ProductMasterLifecycleConsoleTest`) + arch/boundary (`NoEloquentWriteInOperatorPanelRuleTest`, `ModuleBoundariesTest`) all in the 86/86 green ✓; no Master behaviour/i18n key change (no lang file touched; column order preserved; full suite unchanged) ✓; base does NOT fall back to `new Model; save()` — phpstan-with-the-arch-rule green ✓; composer diff empty ✓; PG17 ✓.
- **Key design calls:**
  - **`createRejectionField()` is abstract (uniform), the catch lives in the base.** Every Create subclass declares the field a localized create-rejection lands on; for a guardless entity (Format/CaseConfig/Variant/Sellable) it is a harmless safety net (their create ships no domain guard). This keeps the rejection→form-error path in ONE place (design L5) while letting the field vary per entity.
  - **PR's special case needs NO base change.** Its `createViaAction` will catch the framework `UniqueConstraintViolationException` and throw a `ValidationException` directly; `ValidationException` is not a `RuntimeException`, so it passes through the base catch untouched — the base template already supports it. Confirmed by reading the hierarchy, not yet exercised (PR is task 3.2).
  - **`i18nKey()` is static on the Resource, instance on the pages** — labels/columns resolve statically on a `Resource`, the action wrapper resolves per-instance on a page. Same name, two hierarchies, no collision; reads consistently as "the entity's i18n root".
  - **Behaviour-preserving column reuse.** The lifecycle_state badge moved from `fn (ProductMaster $r) => $r->lifecycle_state->value` to the base's generic `getAttribute(...)` + `instanceof BackedEnum` resolution — same rendered `reviewed`/etc., now type-clean for any entity; the column ORDER is preserved by interleaving the two helpers around Master's producer column (not spreading them as a pair).
- **Learnings for future iterations:**
  - The kit is now COMPLETE (Resource + Create + View + the wrapper trait) — Codebase Patterns (top) carries all five pieces + the create-template + PR-special-case shapes. **2.1 (Format) is now pure reuse:** `FormatResource extends OperatorConsoleResource` (`i18nKey()='format'`; columns name/sizeLabel/volume + the two helpers); `ViewFormat extends OperatorConsoleViewRecord` (5 invocations → the `Format` actions); `CreateFormat extends OperatorConsoleCreateRecord` (`createRejectionField()='name'` + `createViaAction()` → `CreateFormat::handle(name, sizeLabel, volumeMl)`); a `ListFormats` (header create-link, copy from `ListProductMasters`); + `operator_console.format.*` EN/IT copy. No base change expected for any of 2.1–3.3; PR (3.2) adds only its own `createViaAction` catch; Composite (4.1) verifies the ordered-multi-input API.
---

## [2026-06-20 14:50] — 2.1 Format console
- **What:** the first spine console, built as PURE reuse of the kit (no base/kit change). `FormatResource extends OperatorConsoleResource` read-binding `\App\Modules\Catalog\Models\Format` (scalar create form name/size_label/volume_ml; list+infolist columns name/size_label/volume_ml + the kit's `lifecycleStateColumn()`/`versionColumn()`); `ListFormats` (header create-LINK, not a CreateAction); `ViewFormat extends OperatorConsoleViewRecord` (5 typed invocations → the `*Format` actions; NO `getHeaderActions()` override → exactly the kit's five uniform lifecycle actions, NO cascade — Format is standalone); `CreateFormat extends OperatorConsoleCreateRecord` (`createRejectionField()='name'` safety-net + `createViaAction()` narrowing → `CreateFormatAction::handle(name, sizeLabel, volumeMl)`). Added `operator_console.format.*` EN (full) + IT (per-key, label/plural_label omitted → EN fallback) copy.
- **Files changed:** `app/Modules/OperatorPanel/Filament/Resources/Catalog/FormatResource.php` (new); `…/FormatResource/Pages/{ListFormats,ViewFormat,CreateFormat}.php` (new); `lang/{en,it}/operator_console.php` (+`format` block); `tests/Feature/Modules/OperatorPanel/Catalog/{FormatResourceTest,FormatLifecycleConsoleTest}.php` (new). No migration/composer/protected-file change.
- **Quality loop:** green — Pint ✓; filtered ✓ (the two new files 15/15, 127 assertions); full suite ✓ **1011/1011** (5242 assertions, SQLite — +15 vs the 1.2 baseline of 996); phpstan ✓ 0 (the `NoEloquentWriteInOperatorPanelRule` analysed the new Format Filament classes — green proves no Eloquent write); pint --test ✓. `openspec validate --strict` ✓. composer diff vs main empty; no migrations; no protected files (only app code under `Filament/` + lang + tests).
- **PG17 ✓:** docker `postgres:17` (port 55432), `tests/Feature/Modules/OperatorPanel` = **91/91, 596 assertions, exit 0** (76 baseline + 15 new; `DB_CONNECTION=pgsql … php -d memory_limit=-1 vendor/bin/pest`); container torn down. The console create write-through + the standalone activate (no parent gate) + the SoD self-approval surfacing all clean on PG.
- **Acceptance walked:** create → `draft` + 1 `FormatCreated` (newco_ops + operator id) ✓; submit → reviewed (audit-only, 0 Format events beyond create) ✓; activate (3 distinct actors, NO parent gate) → `active` + 1 `FormatActivated` (approver id) ✓ — the spec "standalone reference entity activates with no parent gate" scenario; self-approval → danger notification, unchanged (0 event/audit) ✓; retire → `retired` + 1 `FormatRetired` ✓; reopen → `reviewed` (audit-only, event count unchanged) ✓; out-of-state reject/retire → danger notification, unchanged ✓; pages == [index, create, view], no edit/delete table/bulk action ✓; **NO cascade-retire** (`assertActionDoesNotExist('retireCascade')`) ✓; arch/boundary green (full suite + phpstan) ✓; quality green ✓; PG17 ✓.
- **Learnings for future iterations:**
  - Reuse is genuinely PURE (Codebase Patterns updated). 2.2 (Case Configuration) is the same shape, also standalone (no parent gate): list name/units_per_case/packaging_type, create `CreateCaseConfiguration::handle(name, unitsPerCase, packagingType)` (**no breakability field**), + the reference-integrity retire-block test (Case Config under an active Sellable SKU → blocked, surfaced) which Format does not have.
  - **Pint gotcha (now in Codebase Patterns):** in the read-only Resource, reference the create action in PROSE backticks, not `{@see \App\…\Create<Entity>}` — Pint's `fully_qualified_strict_types` otherwise imports the Action (unused-in-code; harmless to the boundary but untidy and not auto-pruned). Resource imports the Model only; the Create page imports the Action (it calls it).
---

## [2026-06-20 15:10] — 2.2 Case Configuration console
- **What:** the second spine console, again PURE reuse of the kit (no base/kit change, no boundary amendment) — same recipe as Format plus the reference-integrity retire-block. `CaseConfigurationResource extends OperatorConsoleResource` read-binding `\App\Modules\Catalog\Models\CaseConfiguration` (scalar create form name/units_per_case/packaging_type — **NO breakability field**, BR-RefData-2; list+infolist columns name/units_per_case/packaging_type + the kit's `lifecycleStateColumn()`/`versionColumn()`); `ListCaseConfigurations` (header create-LINK); `ViewCaseConfiguration extends OperatorConsoleViewRecord` (5 typed invocations → the `*CaseConfiguration` actions; NO `getHeaderActions()` override → exactly the kit's five lifecycle actions, NO cascade — standalone); `CreateCaseConfiguration extends OperatorConsoleCreateRecord` (`createRejectionField()='name'` safety-net + `createViaAction()` narrowing → `CreateCaseConfiguration::handle(name, unitsPerCase, packagingType)`). Added `operator_console.case_configuration.*` EN (full) + IT (per-key; label/plural_label omitted → EN fallback).
- **Files changed:** `app/Modules/OperatorPanel/Filament/Resources/Catalog/CaseConfigurationResource.php` (new); `…/CaseConfigurationResource/Pages/{ListCaseConfigurations,ViewCaseConfiguration,CreateCaseConfiguration}.php` (new); `lang/{en,it}/operator_console.php` (+`case_configuration` block); `tests/Feature/Modules/OperatorPanel/Catalog/{CaseConfigurationResourceTest,CaseConfigurationLifecycleConsoleTest}.php` (new). No migration/composer/protected-file change.
- **Quality loop:** green — Pint ✓; filtered ✓ (the two new files 18/18, 151 assertions); full suite ✓ **1029/1029** (5393 assertions, SQLite — +18 vs the 1011 baseline of 2.1); phpstan ✓ 0 (the `NoEloquentWriteInOperatorPanelRule` analysed the new Case Config Filament classes — green proves no Eloquent write); pint --test ✓. `openspec validate --strict` ✓. composer diff vs main empty; no migrations; no protected files (only app code under `Filament/` + lang + tests).
- **PG17 ✓:** docker `postgres:17` (port 55432), `tests/Feature/Modules/OperatorPanel` = **109/109, 747 assertions, exit 0** (91 baseline + 18 new; `DB_CONNECTION=pgsql … php -d memory_limit=-1 vendor/bin/pest`); container torn down. The console create write-through + the standalone activate (no parent gate) + the SoD self-approval + the retire reference-integrity block all surface clean on PG.
- **Acceptance walked:** create → `draft` + 1 `CaseConfigurationCreated` (newco_ops + operator id, entity_type `CaseConfiguration`) ✓; create form exposes name/units_per_case/packaging_type, **NO breakability** (`assertFormFieldDoesNotExist('breakability'|'breakable')`) ✓; submit → reviewed (audit-only, 0 events beyond create) ✓; reject(notes) → reviewed (audit-only) ✓; activate (3 distinct actors, NO parent gate) → `active` + 1 `CaseConfigurationActivated` (approver id) ✓; self-approval → danger notification, unchanged ✓; retire (unreferenced) → `retired` + 1 `CaseConfigurationRetired` ✓; reopen → `reviewed` (audit-only) ✓; out-of-state reject/retire → danger notification, unchanged ✓; **retire blocked by an active Sellable SKU → `action_failed` danger notification, stays `active`, 0 `CaseConfigurationRetired`, SKU unaffected** ✓; retire once the SKU is `Retired` → succeeds + 1 event ✓; pages == [index, create, view], no edit/delete table/bulk action ✓; **NO cascade-retire** (`assertActionDoesNotExist('retireCascade')`) ✓; arch/boundary green (full suite + phpstan) ✓; quality green ✓; PG17 ✓.
- **Learnings for future iterations:**
  - Reuse stays PURE; the new reusable bit is the **reference-integrity retire-block scaffold** (Codebase Patterns, top) — factory-active blocking referencer, no Create-action chain, `RetirementReferenceIntegrityViolation` is a `RuntimeException` caught by the kit → `action_failed`, domain body already in `catalog.php`. **3.2 (PR) reuses this exact shape** (SKU referencing the PR via `product_reference_id` + a Composite constituent-junction referencer).
  - **3.1 (Product Variant) is next — the FIRST hierarchical entity (parent-active cascade gate).** New vs the two standalone consoles: a **Product Master picker** on the create form (`CreateProductVariant::handle(productMasterId, variantIdentifier, vintageYear?, nonVintage, tastingNotes?)`); activate surfaces the **cascade gate** (Variant ← Master active → `gate.parent_not_active`, already in `catalog.php`) as a danger notification; the view should also show the wine attrs off the `wineAttributes` relation (confirm the relation name in `ProductVariant`). Scaffold a parent Master to `active` via factory (`ProductMaster::factory()->create(['lifecycle_state' => Active])`) for the success-activate path; a non-active Master for the gate-blocked path.
---

## [2026-06-20 15:24] — 3.1 Product Variant console
- **What:** the FIRST hierarchical spine console, built as PURE reuse of the kit (zero base/kit change, zero boundary amendment) — the standalone recipe (Format/Case Config) PLUS a parent **Product Master picker** on create and the surfaced cascade gate. `ProductVariantResource extends OperatorConsoleResource` read-binding `Catalog\Models\ProductVariant` (create form: a required `Select` `product_master_id` sourced from `ProductMaster` via `productMasterOptions()` + `variant_identifier` + optional `vintage_year`/`non_vintage` Toggle/`tasting_notes`; list cols variant_identifier + parent Master name (off the within-Catalog `master()` relation) + a combined `vintage` column (year, or a "Non-vintage" marker) + the kit's `lifecycleStateColumn()`/`versionColumn()`; infolist adds the wine attrs off `wineAttributes`); `ListProductVariants` (header create-LINK); `ViewProductVariant extends OperatorConsoleViewRecord` (5 typed invocations → the `*ProductVariant` actions; NO `getHeaderActions()` override → the kit's five actions, NO cascade — the cascade GATE surfaces for free); `CreateProductVariant extends OperatorConsoleCreateRecord` (`createRejectionField()='variant_identifier'` safety-net + `createViaAction()` narrowing → `CreateProductVariantAction::handle(...)`, tasting_notes → `TranslatableText` or null). Added `operator_console.product_variant.*` EN (full) + IT (per-key; label/plural_label → EN fallback).
- **Files changed:** `app/Modules/OperatorPanel/Filament/Resources/Catalog/ProductVariantResource.php` (new); `…/ProductVariantResource/Pages/{ListProductVariants,ViewProductVariant,CreateProductVariant}.php` (new); `lang/{en,it}/operator_console.php` (+`product_variant` block); `tests/Feature/Modules/OperatorPanel/Catalog/{ProductVariantResourceTest,ProductVariantLifecycleConsoleTest}.php` (new). No migration/composer/protected-file change.
- **Quality loop:** green — Pint ✓; filtered ✓ (the two new files 17/17, 151 assertions); full suite ✓ **1046/1046** (5544 assertions, SQLite — +17 vs the 1029 baseline of 2.2); phpstan ✓ 0 (the `NoEloquentWriteInOperatorPanelRule` analysed the new Variant Filament classes — green proves no Eloquent write); pint --test ✓. `openspec validate --strict` ✓. composer diff vs main empty; no migrations; no protected files (only app code under `Filament/` + lang + tests).
- **PG17 ✓:** docker `postgres:17` (port 55432), `tests/Feature/Modules/OperatorPanel` = **126/126, 898 assertions, exit 0** (109 baseline + 17 new; `DB_CONNECTION=pgsql … php -d memory_limit=-1 vendor/bin/pest`); container torn down. The console create write-through + the cascade-gate-blocked activate + the SoD self-approval + the active-child-preserving retire all surface clean on PG.
- **Acceptance walked:** create under a Master → `draft` + 1 `ProductVariantCreated` (newco_ops + operator id, entity_type `ProductVariant`), parent FK + wine attrs persisted ✓; create form exposes the **Product Master picker** (`product_master_id`) + variant_identifier + vintage_year + non_vintage + tasting_notes, and **no producer picker** (`assertFormFieldDoesNotExist('producer_id')`) ✓; submit → reviewed (audit-only, 0 events beyond create) ✓; reject(notes) → reviewed (audit-only) ✓; activate (Master active, 3 distinct actors) → `active` + 1 `ProductVariantActivated` (approver id) ✓; **cascade-gate-blocked activate** (Master `reviewed`, 3 distinct actors → it's the cascade not SoD) → `action_failed` danger notification, stays `reviewed`, 0 `ProductVariantActivated`, 0 activated audit ✓; self-approval → danger notification, unchanged ✓; retire (active) → `retired` + 1 `ProductVariantRetired` ✓; **single-entity retire preserves an active child PR** (seed active PR under the Variant → Variant `retired`, child PR stays `active`) ✓; reopen → `reviewed` (audit-only, event total unchanged) ✓; out-of-state retire → danger notification, unchanged ✓; pages == [index, create, view], no edit/delete table/bulk action ✓; **NO cascade-retire** (`assertActionDoesNotExist('retireCascade')`) ✓; arch/boundary green (full suite + phpstan) ✓; quality green ✓; PG17 ✓.
- **Learnings for future iterations:**
  - The **hierarchical-entity recipe** is now in Codebase Patterns (top): parent picker + free cascade-gate surfacing + the gate-blocked & active-child-preserving tests + the `wineAttributes` `?->update`/local-bind gotchas. **3.2 (Product Reference)** is the same recipe with TWO parent pickers (Variant + Format), PLUS the PR-special create-error catch (dup `(variant,format)` → `UniqueConstraintViolationException` → a console-owned `operator_console.product_reference.duplicate_reference` form error — the ONE console-owned key the change adds; design L5), PLUS the retire reference-integrity block (the 2.2 scaffold with the SKU referencing the PR via `product_reference_id` + a Composite constituent-junction referencer). 3.2 is a **PG17 task**.
---

## [2026-06-20 16:05] — 3.2 Product Reference console
- **What:** the SECOND hierarchical spine console (the atomic product key: Variant + Format), built as PURE reuse of the kit (zero base/kit change, zero boundary amendment) — the 3.1 hierarchical recipe with TWO parent pickers plus the change's TWO standout specials. `ProductReferenceResource extends OperatorConsoleResource` read-binding `Catalog\Models\ProductReference` (create form: required `Select` `product_variant_id` via `productVariantOptions()` + required `Select` `format_id` via `formatOptions()`, both rendering the parent's `lifecycle_state->value`, NO `Catalog\Enums` import; list cols Variant identifier (off `variant()`) + Format name (off `format()`) + the kit's `lifecycleStateColumn()`/`versionColumn()`; infolist mirrors); `ListProductReferences` (header create-LINK); `ViewProductReference extends OperatorConsoleViewRecord` (5 typed invocations → the `*ProductReference` actions; NO `getHeaderActions()` override → the kit's five actions, NO cascade — the two-parent cascade gate AND the retire reference-integrity block both surface FOR FREE); `CreateProductReference extends OperatorConsoleCreateRecord` — the ONE create special case: `createViaAction` catches the framework `UniqueConstraintViolationException` and re-throws `ValidationException` with the console-owned `operator_console.product_reference.duplicate_reference` key (design L5). Added `operator_console.product_reference.*` EN (full, incl. `duplicate_reference`) + IT (per-key; label/plural_label → EN fallback).
- **Files changed:** `app/Modules/OperatorPanel/Filament/Resources/Catalog/ProductReferenceResource.php` (new); `…/ProductReferenceResource/Pages/{ListProductReferences,ViewProductReference,CreateProductReference}.php` (new); `lang/{en,it}/operator_console.php` (+`product_reference` block); `tests/Feature/Modules/OperatorPanel/Catalog/{ProductReferenceResourceTest,ProductReferenceLifecycleConsoleTest}.php` (new). No migration/composer/protected-file change.
- **Quality loop:** green — Pint ✓; filtered ✓ (the two new files 19/19, 158 assertions); full suite ✓ **1065/1065** (5702 assertions, SQLite — +19 vs the 1046 baseline of 3.1); phpstan ✓ 0 (the `NoEloquentWriteInOperatorPanelRule` analysed the new PR Filament classes — green proves no Eloquent write); pint --test ✓. `openspec validate --strict` ✓. composer diff vs main empty; no migrations; no protected files (only app code under `Filament/` + lang + tests).
- **PG17 ✓:** docker `postgres:17` (port 55432), `tests/Feature/Modules/OperatorPanel` = **145/145, 1056 assertions, exit 0** (126 baseline + 19 new; `DB_CONNECTION=pgsql … php -d memory_limit=-1 vendor/bin/pest`); container torn down. The console create write-through + the **duplicate-pair UniqueConstraintViolationException rolled back to the action's savepoint** + the two-parent cascade-gate-blocked activate + the SoD self-approval + **both** retire reference-integrity blocks (Sellable SKU by `product_reference_id` AND Composite SKU constituent) all surface clean on PG.
- **Acceptance walked:** create from Variant+Format → `draft` + 1 `ProductReferenceCreated` (newco_ops + operator id, entity_type `ProductReference`), both parent FKs persisted ✓; create form exposes the **two parent pickers** (`product_variant_id`, `format_id`) + **no producer picker** (`assertFormFieldDoesNotExist('producer_id')`) ✓; **duplicate `(variant,format)` → form error** carrying the localized console message (asserted EXACTLY via `assertHasFormErrors(['product_variant_id' => __(duplicate_reference)])` — proves the console key, NOT raw SQL), PR count stays 1, 0 new `ProductReferenceCreated` ✓; submit → reviewed (audit-only, 0 events beyond create) ✓; reject(notes) → reviewed (audit-only) ✓; activate (both parents active, 3 distinct actors) → `active` + 1 `ProductReferenceActivated` (approver id) ✓; **cascade-gate-blocked activate** (Format `reviewed`, 3 distinct actors → it's the cascade not SoD) → `action_failed`, stays `reviewed`, 0 `ProductReferenceActivated`, 0 activated audit ✓; self-approval → `action_failed`, unchanged ✓; retire (active, unreferenced) → `retired` + 1 `ProductReferenceRetired` ✓; **retire blocked by active Sellable SKU** → `action_failed`, stays `active`, 0 `*Retired`, SKU unaffected ✓; **retire blocked by active Composite SKU constituent** → `action_failed`, stays `active`, 0 `*Retired`, composite unaffected ✓; reopen → `reviewed` (audit-only, event total unchanged) ✓; out-of-state retire → `action_failed`, unchanged ✓; pages == [index, create, view], no edit/delete table/bulk action ✓; **NO cascade-retire** (`assertActionDoesNotExist('retireCascade')`) ✓; arch/boundary green (full suite + phpstan) ✓; quality green ✓; PG17 ✓.
- **Learnings for future iterations:**
  - The PR create-error special case + the form-error-MESSAGE assertion idiom + the BOTH-edges PR retire block are now in **Codebase Patterns** (top). Key trap captured: `UniqueConstraintViolationException` IS a `RuntimeException`, so it must be caught INSIDE `createViaAction` and converted to `ValidationException` (not a RuntimeException) — else the base catch leaks raw SQL; and the console-owned message must be COLON-FREE for the message assertion.
  - **3.3 (Sellable SKU) is next** — the same two-parent recipe (PR + Case Configuration pickers + commercial name / marketing copy fields, `CreateSellableSku::handle(productReferenceId, caseConfigurationId, commercialName, marketingCopy?)`); the cascade gate is PR **and** Case Config active; **NO create guard** (a PR+CaseConfig pair may back multiple SKUs — assert two SKUs over the same pair both succeed, do NOT add uniqueness); it is a LEAF (retire has no within-catalog block, no reference-integrity test). Note the event name is `SellableSKUCreated`/`SellableSKUActivated`/`SellableSKURetired` (SKU UPPER-case). **PG17 task.**
---
