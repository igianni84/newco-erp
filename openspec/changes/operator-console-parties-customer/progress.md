# Progress — operator-console-parties-customer

## Codebase Patterns
(consolidated reusable patterns — read first each iteration)

- **Read-surface group = ONE green unit (the `getPages()` boot coupling), and `CreateCustomer` ships REAL now.** `CustomerResource::getPages()` eagerly references all three page classes, so `CustomerResource` + `ListCustomers` + `CreateCustomer` + `ViewCustomer` must all exist together or the panel fails to boot. `OperatorConsoleCreateRecord` has TWO abstract methods (`createViaAction`/`createRejectionField`), so a clean scaffold IS the real page (a throw-stub is dead code — predecessor precedent). So the REAL `CreateCustomer.createViaAction` (narrow payload → `Currency::of()` / `SupportedLocale::from()` operands → `app(CreateCustomerAction::class)->handle(...)`, alias `CreateCustomer as CreateCustomerAction` for the page/action name clash) + `createRejectionField()='email'` are DONE in group 1. ⇒ **Groups 1+2 DONE** (2026-06-21): the `CustomerResource::form()` (email/name `TextInput`, preferred_currency/preferred_locale `Select` off `currencyOptions()`/`localeOptions()` — mirroring `ClubResource`, platform enums imported freely; phone `TextInput`, date_of_birth `DatePicker`; NO `status`) + `fields.{email,name}` i18n landed group 2 and the `CustomerCreateConsoleTest` now exercises the createViaAction end-to-end (valid → `pending` Customer + one `active` Account + one `CustomerCreated` + zero Profiles; duplicate email → `email` form error, full rollback). **Next = group 3 (ViewCustomer verbs).**
- **Three orthogonal lifecycle badges via ONE shared resolver.** `private static function enumBadgeState(string $attribute): Closure` returns the `getAttribute($attribute) instanceof BackedEnum ? ->value : ''` closure (mixed → the instanceof is meaningful, NOT always-true), applied to BOTH the `TextColumn` (table) and `TextEntry` (infolist) for `status`/`kyc_status`/`sanctions_status`. No `Parties\Enums` import (D2 — state enums via the cast). NEVER `lifecycleStateColumn()` (catalog `lifecycle_state`). Each badge resolves `operator_console.customer.columns.<attr>` as a full literal `__()` key.
- **Nullable `hasOne` read (Account `status`) — the predecessor's nullable-`belongsTo` lesson generalizes.** `$account = $record->account; return $account === null ? '' : $account->status->value;` — the `@property-read Account|null` makes the local null-checkable; a nullsafe `$record->account?->status` would red phpstan `nullsafe.neverNull` (typed off the non-null `HasOne` generic). `status` is non-null once the account exists → `->status->value` directly (NO `instanceof` — avoids `instanceof.alwaysTrue`). Resolver: `accountStatusState(): Closure` (Customer-typed).
- **Within-Parties relation LIST in an infolist** (the Customer's Profiles) = `$record->profiles->map(fn (Profile $profile): string => $profile->club->display_name)->implode(', ')`: imports the `Profile` Model (a SECOND Parties\Models import — the {Models} carve-out admits it, `ModuleBoundariesTest` green), typed map → `$profile->club` is non-null `@property-read Club $club` so `->display_name` is clean. The count COLUMN is `$record->profiles->count()` (no import). The `', '` separator inside `getStateUsing` is NOT a sink (predecessor-proven). [Profile cascade is out of console scope — the seed stays profile-less in the chain test, D9.]
- **Create operands PLATFORM-level → NO boundary change (D6).** `Currency::of($code)` (`App\Platform\Money`) + `SupportedLocale::from($value)` (`App\Platform\I18n`) — both always-importable; NO `Parties\Enums` operand-enum import, so the operand-enum carve-out (ADR 2026-06-21) is NOT exercised and `ModuleBoundariesTest` needs NO widening (unlike Club). The `date_of_birth` `DatePicker` will dehydrate `'Y-m-d'` → `CarbonImmutable::parse` (the ProducerAgreement date idiom); `email`/`name`/`preferred_currency`/`preferred_locale` narrow with `is_string`, phone/dob with `is_null || is_string`.
- **i18n is authored INCREMENTALLY across groups (predecessor precedent).** Group 1 = `label`/`plural_label` (EN only → IT EN-fallback, DEC-127) + `columns.{name,email,status,kyc_status,sanctions_status,account_status,profiles,version}` + the infolist `fields.{phone,date_of_birth,preferred_currency,preferred_locale}` + `actions.create`. Group 2 adds `fields.{email,name}`. Group 3 adds `actions.{activate,suspend,reactivate,close}` + the `notifications.{activated,suspended,reactivated,closed,action_failed}` block. **IT 'Email' collides with EN 'Email'** → IT `columns.email` = **'Indirizzo email'** (the group-4 IT-differs guard requires every non-label key IT != EN). The group-4 test count will be |kit| EN + |differs| IT + 2 fallback + 1 IT⊆EN + 1 sink (recompute the kit).

---

## [2026-06-21 22:00] — 1.1 / 1.2 / 1.3 Customer read-only Resource + list + scaffold + resource/infolist i18n
- **What:** the Customer read surface — the FIRST **demand-side** Parties (Module K) operator console, the non-catalog trait-reuse pattern. Done as ONE green unit (the `getPages()` boot coupling forces all four page classes to exist together — Codebase Patterns §"Read-surface group"). `CustomerResource extends OperatorConsoleResource` read-binding `Parties\Models\Customer`, `i18nKey()='customer'`: list = name/email + three orthogonal lifecycle badges (`status`/`kyc_status`/`sanctions_status`, all via the shared `enumBadgeState()` BackedEnum-cast resolver — NO `Parties\Enums` import, NOT `lifecycleStateColumn()`, D2) + `account_status` (the co-provisioned Account's state, the nullable-`hasOne` clean shape) + profiles count + `versionColumn()`; infolist = name/email/phone/date_of_birth/preferred_currency/preferred_locale + the three badges + account_status + a read-only Profiles list (club display_names). `ListCustomers` (header create-LINK, not a CreateAction). To keep the panel booting, shipped **the real `CreateCustomer`** (per Codebase Patterns: `OperatorConsoleCreateRecord`'s two abstract methods → a clean scaffold IS the real page): `createViaAction` narrows the payload + constructs the platform `Currency`/`SupportedLocale` operands (D6 — no boundary change) + calls `CreateCustomerAction`; `createRejectionField()='email'`. Bare `ViewCustomer extends ViewRecord` (trait + four status verbs deferred to 3.1). EN (full) + IT (per-key; label/plural_label → EN fallback) `operator_console.customer.*` group-1 keys.
- **Files changed (new):** `app/Modules/OperatorPanel/Filament/Resources/Parties/CustomerResource.php` + `…/CustomerResource/Pages/{ListCustomers,CreateCustomer,ViewCustomer}.php`; `tests/Feature/Modules/OperatorPanel/Parties/CustomerResourceTest.php`. **(modified):** `lang/{en,it}/operator_console.php` (+`customer` block). No migration/composer/protected-file/`ModuleBoundariesTest` change.
- **Quality loop: green** — pint clean; filtered `CustomerResourceTest` 5/5 (26 assn); full SQLite suite **1330/1330** (7386 assn, +5 vs the 1325 baseline); phpstan **0** (the `NoEloquentWriteInOperatorPanelRule` analysed the four new Filament classes — green proves no Eloquent write, the create routes through the action); pint --test clean; `ModuleBoundariesTest` 3/3 (189 assn) — admits `Parties\Models\{Customer,Profile}` + `Parties\Actions\CreateCustomer`, NO `Parties\Enums` (**D6 proven — operands platform-level**); `openspec validate … --strict` valid; composer diff vs `main` empty.
- **Acceptance walked (group 1):** a seeded Customer renders in the list with its `status` (`active`), `kyc_status` (`verified`), `sanctions_status` (`passed`) + a never-screened Customer's NULL kyc/sanctions render blank (no crash) ✓; `account_status` renders the co-provisioned Account's distinct `suspended` ✓; the view page renders the read-only attributes + the three badges ✓; `CustomerResource::getPages()` keys `toEqualCanonicalizing(['index','create','view'])` ✓; table exposes no edit/delete/bulk action, create is a header link (`assertActionHasUrl`) ✓; EN `customer.columns.name`=`Name`, IT=`Nome`, `label` IT→EN fallback `Customer` ✓; boundary green — no `Parties\Enums` import introduced (`enumBadgeState` reads via the cast) ✓.
- **Learnings for future iterations:**
  - **`CreateCustomer` shipped REAL now (the abstract-method precedent).** Group 2's remaining work is `CustomerResource::form()` + `fields.{email,name}` ONLY — the createViaAction/operands/createRejectionField are done (dormant until the form exists; phpstan validates the types now). See Codebase Patterns §"Read-surface group".
  - **The three-badge resolver is shared between table + infolist.** `enumBadgeState(string $attribute): Closure` (Model-typed, `getAttribute` mixed → meaningful `instanceof`) is applied to both `TextColumn` and `TextEntry`. `accountStatusState()` is the Customer-typed nullable-`hasOne` resolver (`->status->value` direct, no instanceof). Both in Codebase Patterns.
  - **Profiles infolist list imports the `Profile` Model** (a second `Parties\Models` import — the {Models} carve-out admits it; `ModuleBoundariesTest` green) for the typed `->map(fn (Profile $p) => $p->club->display_name)`. Mostly empty in tests (a console-created Customer has zero Profiles — D7).
  - **IT `columns.email` = 'Indirizzo email'** (not 'Email') to satisfy the group-4 IT-differs guard (every non-label key IT != EN). Recorded in Codebase Patterns.
  - **Next → group 2 (task 2.1/2.2):** add `CustomerResource::form()` (email required TextInput email-rule; name required TextInput; preferred_currency required Select ISO 4217; preferred_locale required Select `SupportedLocale` cases; phone optional TextInput; date_of_birth optional DatePicker; **NO `status`**) + `fields.{email,name}` i18n + `CustomerCreateConsoleTest` (valid → Customer `pending` + exactly one co-provisioned Account `active` + one `CustomerCreated`, zero Profiles — D7; duplicate email → `DuplicateCustomerEmail` on `email`, no Customer/Account/event; `assertFormFieldDoesNotExist('status')`). The Select options helpers: currency `collect(Currency::cases())->mapWithKeys(...)` and locale `collect(SupportedLocale::cases())->mapWithKeys(...)` — but these are PLATFORM enums (import freely, not the operand-enum carve-out). Then 3 lifecycle / 4 i18n / 5 PG17 chain.
---

## [2026-06-21 22:11] — 2.1 / 2.2 Customer create surface (form) + create-field i18n
- **What:** the Customer write-through create surface, completing group 2. The `CreateCustomer` page (real `createViaAction` + `createRejectionField`) already shipped group 1, so the remaining work was `CustomerResource::form()` ONLY + the `fields.{email,name}` i18n that completes the labels. Added `form()` (placed after `i18nKey()`, mirroring `ClubResource`/`ProducerResource`): `email` (`->email()->required()->maxLength(255)`), `name` (required, maxLength 255), `preferred_currency` required `Select` off `currencyOptions()`, `preferred_locale` required `Select` off `localeOptions()`, `phone` optional `TextInput`, `date_of_birth` optional `DatePicker` — **NO `status`** (born `pending`, D5). Two private option helpers (`currencyOptions()`/`localeOptions()`) `collect(Enum::cases())->mapWithKeys(value=>value)` over the PLATFORM `Currency`/`SupportedLocale` enums (freely importable — D6, NOT the operand-enum carve-out). With the form present, the createViaAction is now exercised end-to-end. EN `fields.{email,name}` = Email/Name; IT = **Indirizzo email**/Nome (the IT-differs guard needs IT != EN; email re-uses the `columns.email` collision-breaker).
- **Files changed (modified):** `app/Modules/OperatorPanel/Filament/Resources/Parties/CustomerResource.php` (+5 imports: `App\Platform\I18n\SupportedLocale`, `App\Platform\Money\Currency`, `Filament\Forms\Components\{DatePicker,Select,TextInput}`; + `form()` + 2 helpers); `lang/{en,it}/operator_console.php` (+`customer.fields.{email,name}`). **(new):** `tests/Feature/Modules/OperatorPanel/Parties/CustomerCreateConsoleTest.php` (3 it, `DatabaseMigrations`). No migration/composer/protected-file/`ModuleBoundariesTest` change.
- **Quality loop: green** — pint clean; filtered `CustomerCreateConsoleTest` 3/3 (38 assn); full SQLite **1333/1333** (7424 assn, +3 tests / +38 assn vs the 1330 baseline); phpstan **0** (the `NoEloquentWriteInOperatorPanelRule` over `CustomerResource`/`CreateCustomer` green — the form COLLECTS, the action WRITES); pint --test clean; `ModuleBoundariesTest` 3/3 (189 assn) — UNCHANGED (the form's only new imports are `App\Platform\*`, not `Parties\Enums` — **D6 proven again**); `openspec validate … --strict` valid; composer diff vs `main` empty.
- **Acceptance walked (group 2):** valid submit → Customer `pending` + name/preferred_currency/preferred_locale/phone persisted + date_of_birth non-null ✓; exactly one co-provisioned Account `active` ✓; exactly one `CustomerCreated` (`actor_role` NewcoOps, `actor_id` = operator, `entity_type` Customer, `entity_id` = `(string)$customer->id`) ✓; zero Profiles (D7) ✓; duplicate email → `email` form error, no new Customer / no Account-count change / no event ✓; form exposes email/name/preferred_currency/preferred_locale/phone/date_of_birth, NOT `status` ✓; no `$model->save()` (phpstan) ✓; boundary green ✓.
- **Learnings for future iterations:**
  - **The Select-options helper is the established sibling idiom** (`ClubResource::currencyOptions()`): `collect(Enum::cases())->mapWithKeys(static fn ($c) => [$c->value => $c->value])->all()`, wired via the first-class-callable `->options(self::currencyOptions(...))`. For Customer both helpers drive PLATFORM enums (`Currency`/`SupportedLocale`) — imported freely, the boundary is untouched (D6). Reuse this shape wherever a console Select offers a closed enum domain.
  - **`->email()` is a real Filament 5 `TextInput` method** (`vendor/filament/forms/src/Components/TextInput.php`) — applies the `email` validation rule; the duplicate-email rejection still comes from the ACTION (a valid-format but taken email passes form validation, then the action's uniqueness pre-check throws → mapped to the `email` field by the base catch via `createRejectionField`).
  - **Next → group 3 (task 3.1/3.2):** fill `ViewCustomer` (`extends ViewRecord` + `use SurfacesDomainActions`, NOT `OperatorConsoleViewRecord` — D1/D8) with the four form-less, no-`confirmationKey` status verbs (activate/suspend/reactivate/close via `lifecycleAction`, each closure `(Model $record, string $notes)`) + `actions.{activate,suspend,reactivate,close}` + `notifications.{activated,suspended,reactivated,closed,action_failed}` i18n. LANDMINE (D5): `activate`'s gate is cross-slice — a gate-unmet attempt rejecting (danger notification, no event) is CORRECT, do NOT set gate columns from the console.
---
