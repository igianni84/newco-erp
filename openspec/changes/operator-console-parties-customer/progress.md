# Progress — operator-console-parties-customer

## Codebase Patterns
(consolidated reusable patterns — read first each iteration)

- **Read-surface group = ONE green unit (the `getPages()` boot coupling), and `CreateCustomer` ships REAL now.** `CustomerResource::getPages()` eagerly references all three page classes, so `CustomerResource` + `ListCustomers` + `CreateCustomer` + `ViewCustomer` must all exist together or the panel fails to boot. `OperatorConsoleCreateRecord` has TWO abstract methods (`createViaAction`/`createRejectionField`), so a clean scaffold IS the real page (a throw-stub is dead code — predecessor precedent). So the REAL `CreateCustomer.createViaAction` (narrow payload → `Currency::of()` / `SupportedLocale::from()` operands → `app(CreateCustomerAction::class)->handle(...)`, alias `CreateCustomer as CreateCustomerAction` for the page/action name clash) + `createRejectionField()='email'` are DONE in group 1. ⇒ **Groups 1+2 DONE** (2026-06-21): the `CustomerResource::form()` (email/name `TextInput`, preferred_currency/preferred_locale `Select` off `currencyOptions()`/`localeOptions()` — mirroring `ClubResource`, platform enums imported freely; phone `TextInput`, date_of_birth `DatePicker`; NO `status`) + `fields.{email,name}` i18n landed group 2 and the `CustomerCreateConsoleTest` now exercises the createViaAction end-to-end (valid → `pending` Customer + one `active` Account + one `CustomerCreated` + zero Profiles; duplicate email → `email` form error, full rollback). **⇒ Group 3 DONE too** (2026-06-21): `ViewCustomer` filled with the four form-less status verbs (see the next bullet). **Next = group 4 (i18n kit-key completeness test).**
- **Non-catalog status-FSM view page = `ViewRecord` + `use SurfacesDomainActions` + bespoke `getHeaderActions()` (now proven twice — Producer + Customer).** Each verb is `$this->lifecycleAction('<verb>', '<successKey>', fn (Model $r, string $notes) => app(<Action>::class)->handle($this->recordOf(<Model>::class, $r)->id))` — FORM-LESS, NO `confirmationKey` (no SoD floor). The Parties lifecycle actions all take `int $id` (unlike catalog actions that take the model), so the closure passes `recordOf(...)->id`; `$notes` is uniformly unused. Label key snake-cases the verb (`reactivate`/`close` are single words → unchanged); success notification = `notifications.<successKey>`, rejection → `notifications.action_failed` danger title via the trait's `surfaceLifecycleOutcome` (catches `RuntimeException` by base type → NO `Parties\Exceptions` import, {Models,Actions} carve-out holds). `OperatorConsoleViewRecord` imported ONLY for the `{@see}` docblock cross-ref (mirrors `ViewProducer`; pint treats `{@see FQCN}` as usage → not unused). **D8 rule-of-three CLOSED: this trait-level shape, not a verb-list base, is the demand-side standard.**
- **Cross-slice activation gate surfaces gracefully (D5) — the console seeds NO gate columns.** `ActivateCustomer`'s composite gate (3 onboarding acceptances ∧ `sanctions_status=passed` ∧ KYC-cleared-if-required) is set by the consumer-onboarding flow + compliance console, NOT this slice. A gate-unmet `activate` rejects (`action_failed`, **records no event**) — correct surface-ahead-of-drivers. Lifecycle test seeds gate-MET via `Customer::factory()->create(['email_verified_at'=>now(),'tc_accepted_at'=>now(),'privacy_accepted_at'=>now(),'sanctions_status'=>SanctionsStatus::Passed])` (status defaults `pending`, `kyc_required` null → KYC rider clears, DEC-071). A bare `factory()->create()` is gate-UNMET (no acceptances, NULL sanctions). The factory is event-free + Account-free + Profile-free, so after any rejected verb `DomainEvent::count()===0`, and a profile-less Customer keeps suspend/reactivate **cascade-silent** (exactly one root event).
- **Lifecycle console test idiom (`DatabaseMigrations`).** `Livewire::test(ViewX::class, ['record' => $id])->callAction('<verb>')->assertNotified((string) __('operator_console.<key>.notifications.<x>'))`; then `DomainEvent::query()->where('name', '<Event>')->sole()` + envelope (`module`/`entity_type`/`entity_id` strict, `actor_role` `ActorRole::NewcoOps`, `actor_id` **loose `toEqual`** — PG bigint-as-string). Out-of-state verbs parametrize via `->with([...] => ['<verb>', CustomerStatus::<seed>])`. Scope-guards: `assertActionExists(v, fn (Action $a) => ! $a->isConfirmationRequired())` + `assertActionDoesNotExist('submit'/'reject'/'reopen'/'placeHold'/'liftHold'/'requireKyc')`.
- **Three orthogonal lifecycle badges via ONE shared resolver.** `private static function enumBadgeState(string $attribute): Closure` returns the `getAttribute($attribute) instanceof BackedEnum ? ->value : ''` closure (mixed → the instanceof is meaningful, NOT always-true), applied to BOTH the `TextColumn` (table) and `TextEntry` (infolist) for `status`/`kyc_status`/`sanctions_status`. No `Parties\Enums` import (D2 — state enums via the cast). NEVER `lifecycleStateColumn()` (catalog `lifecycle_state`). Each badge resolves `operator_console.customer.columns.<attr>` as a full literal `__()` key.
- **Nullable `hasOne` read (Account `status`) — the predecessor's nullable-`belongsTo` lesson generalizes.** `$account = $record->account; return $account === null ? '' : $account->status->value;` — the `@property-read Account|null` makes the local null-checkable; a nullsafe `$record->account?->status` would red phpstan `nullsafe.neverNull` (typed off the non-null `HasOne` generic). `status` is non-null once the account exists → `->status->value` directly (NO `instanceof` — avoids `instanceof.alwaysTrue`). Resolver: `accountStatusState(): Closure` (Customer-typed).
- **Within-Parties relation LIST in an infolist** (the Customer's Profiles) = `$record->profiles->map(fn (Profile $profile): string => $profile->club->display_name)->implode(', ')`: imports the `Profile` Model (a SECOND Parties\Models import — the {Models} carve-out admits it, `ModuleBoundariesTest` green), typed map → `$profile->club` is non-null `@property-read Club $club` so `->display_name` is clean. The count COLUMN is `$record->profiles->count()` (no import). The `', '` separator inside `getStateUsing` is NOT a sink (predecessor-proven). [Profile cascade is out of console scope — the seed stays profile-less in the chain test, D9.]
- **Create operands PLATFORM-level → NO boundary change (D6).** `Currency::of($code)` (`App\Platform\Money`) + `SupportedLocale::from($value)` (`App\Platform\I18n`) — both always-importable; NO `Parties\Enums` operand-enum import, so the operand-enum carve-out (ADR 2026-06-21) is NOT exercised and `ModuleBoundariesTest` needs NO widening (unlike Club). The `date_of_birth` `DatePicker` will dehydrate `'Y-m-d'` → `CarbonImmutable::parse` (the ProducerAgreement date idiom); `email`/`name`/`preferred_currency`/`preferred_locale` narrow with `is_string`, phone/dob with `is_null || is_string`.
- **Console i18n completeness test = enumerate the kit contract + 5 guards (proven 5×: ProductMaster, Spine, Producer, Club, Customer).** Mirror `ClubConsoleI18nTest` (the broader-key-set sibling — it enumerates `columns.*`+`fields.*`+`actions.create`+verbs, not just the kit-concatenated keys). Two `function`s (`{entity}ConsoleKitKeys()` = the full list; `{entity}ConsoleItDiffersKeys()` = `array_diff(kit, ['label','plural_label'])`) + 5 `it()`: (1) EN baseline `Lang::has(…, 'en', false)` over the kit (`->with(kitKeys())`); (2) IT-differs (`Lang::has(…,'it',false)` AND `__()->not->toBe(trans(…,[],'en'))`) over the diff set; (3) `label`/`plural_label` EN-fallback (absent in `it`, resolves to the EN value — DEC-127); (4) IT⊆EN `array_diff(itKeys, enKeys) === []` filtered `str_starts_with($dotKey, '{entity}.')` (**trailing dot load-bearing** — no sibling `columns.{entity}` collision); (5) `scanOperatorConsoleHardcodedSinks($source)` behind `function_exists` over `File::allFiles(...Resources/{Module})` filtered `str_contains(pathname, '{Entity}Resource')`. **WHY enumeration, not just the sink scan:** the kit resolves `label`/`plural_label`/`columns.version`/the verb `actions.*`+`notifications.*` BY STRING CONCATENATION in the base/trait → invisible to a source scan, and behaviour tests render the raw key as the label so they pass even with a dropped key. **GOTCHAS:** `scanOperatorConsoleHardcodedSinks` is declared ONCE in `ProductMasterConsoleI18nTest` (reuse via `function_exists`, NEVER redeclare) → **run via `--filter` or the full suite, never a bare file path** (else the helper isn't loaded → non-vacuity guard false-reds). The whole-OperatorPanel sink scan already covers every Filament file, so a per-console scoped scan is a guaranteed-passing subset (it exists to *travel with the change*).
- **i18n is authored INCREMENTALLY across groups (predecessor precedent).** Group 1 = `label`/`plural_label` (EN only → IT EN-fallback, DEC-127) + `columns.{name,email,status,kyc_status,sanctions_status,account_status,profiles,version}` + the infolist `fields.{phone,date_of_birth,preferred_currency,preferred_locale}` + `actions.create`. Group 2 adds `fields.{email,name}`. Group 3 adds `actions.{activate,suspend,reactivate,close}` + the `notifications.{activated,suspended,reactivated,closed,action_failed}` block. **IT 'Email' collides with EN 'Email'** → IT `columns.email` = **'Indirizzo email'** (the group-4 IT-differs guard requires every non-label key IT != EN). The group-4 test count will be |kit| EN + |differs| IT + 2 fallback + 1 IT⊆EN + 1 sink (recompute the kit).

---

## [2026-06-21 22:00] — 1.1 / 1.2 / 1.3 Customer read-only Resource + list + scaffold + resource/infolist i18n
- **What:** the Customer read surface — the FIRST **demand-side** Parties (Module K) operator console, the non-catalog trait-reuse pattern. Done as ONE green unit (the `getPages()` boot coupling forces all four page classes to exist together — Codebase Patterns §"Read-surface group"). `CustomerResource extends OperatorConsoleResource` read-binding `Parties\Models\Customer`, `i18nKey()='customer'`: list = name/email + three orthogonal lifecycle badges (`status`/`kyc_status`/`sanctions_status`, all via the shared `enumBadgeState()` BackedEnum-cast resolver — NO `Parties\Enums` import, NOT `lifecycleStateColumn()`, D2) + `account_status` (the co-provisioned Account's state, the nullable-`hasOne` clean shape) + profiles count + `versionColumn()`; infolist = name/email/phone/date_of_birth/preferred_currency/preferred_locale + the three badges + account_status + a read-only Profiles list (club display_names). `ListCustomers` (header create-LINK, not a CreateAction). To keep the panel booting, shipped **the real `CreateCustomer`** (per Codebase Patterns: `OperatorConsoleCreateRecord`'s two abstract methods → a clean scaffold IS the real page): `createViaAction` narrows the payload + constructs the platform `Currency`/`SupportedLocale` operands (D6 — no boundary change) + calls `CreateCustomerAction`; `createRejectionField()='email'`. Bare `ViewCustomer extends ViewRecord` (trait + four status verbs deferred to 3.1). EN (full) + IT (per-key; label/plural_label → EN fallback) `operator_console.customer.*` group-1 keys.
- **Files changed (new):** `app/Modules/OperatorPanel/Filament/Resources/Parties/CustomerResource.php` + `…/CustomerResource/Pages/{ListCustomers,CreateCustomer,ViewCustomer}.php`; `tests/Feature/Modules/OperatorPanel/Parties/CustomerResourceTest.php`. **(modified):** `lang/{en,it}/operator_console.php` (+`customer` block). No migration/composer/protected-file/`ModuleBoundariesTest` change.
- **Quality loop: green** — pint clean; filtered `CustomerResourceTest` 5/5 (26 assn); full SQLite suite **1330/1330** (7386 assn, +5 vs the 1325 baseline); phpstan **0** (the `NoEloquentWriteInOperatorPanelRule` analysed the four new Filament classes — green proves no Eloquent write, the create routes through the action); pint --test clean; `ModuleBoundariesTest` 3/3 (189 assn) — admits `Parties\Models\{Customer,Profile}` + `Parties\Actions\CreateCustomer`, NO `Parties\Enums` (**D6 proven — operands platform-level**); `openspec validate … --strict` valid; composer diff vs `main` empty.
- **Acceptance walked (group 1):** a seeded Customer renders in the list with its `status` (`active`), `kyc_status` (`verified`), `sanctions_status` (`passed`) + a never-screened Customer's NULL kyc/sanctions render blank (no crash) ✓; `account_status` renders the co-provisioned Account's distinct `suspended` ✓; the view page renders the read-only attributes + the three badges ✓; `CustomerResource::getPages()` keys `toEqualCanonicalizing(['index','create','view'])` ✓; table exposes no edit/delete/bulk action, create is a header link (`assertActionHasUrl`) ✓; EN `customer.columns.name`=`Name`, IT=`Nome`, `label` IT→EN fallback `Customer` ✓; boundary green — no `Parties\Enums` import introduced (`enumBadgeState` reads via the cast) ✓.
- **Learnings for future iterations:**
  - **`CreateCustomer` shipped REAL now (the abstract-method precedent).** Group 2's remaining work is `CustomerResource::form()` + `fields.{email,name}` ONLY — the createViaAction/operands/createRejectionField are done (dormant until the form exists; phpstan validates the types now). See Codebase Patterns §"Read-surface group".
  - **The three-badge resolver is shared between table + infolist.** `enumBadgeState(string $attribute): Closure` (Model-typed, `getAttribute` mixed → meaningful `instanceof`) is applied to both `TextColumn` and `TextEntry`. `accountStatusState()` is the Customer-typed nullable-`hasOne` resolver (`->status->value` direct, no instanceof). Both in Codebase Patterns.
  - **Profiles infolist list imports the `Profile` Model** (a second `Parties\Models` import — the {Models} carve-out admits it; `ModuleBoundariesTest` green) for the typed `->map(fn (Profile $p) => $p->club->display_name)`. Mostly empty in tests (a console-created Customer has zero Profiles — D7).
  - **IT `columns.email` = 'Indirizzo email'** (not 'Email') to satisfy the group-4 IT-differs guard (every non-label key IT != EN). Recorded in Codebase Patterns.
  - **Next → group 2 (task 2.1/2.2):** add `CustomerResource::form()` (email required TextInput email-rule; name required TextInput; preferred_currency required Select ISO 4217; preferred_locale required Select `SupportedLocale` cases; phone optional TextInput; date_of_birth optional DatePicker; **NO `status`**) + `fields.{email,name}` i18n + `CustomerCreateConsoleTest` (valid → Customer `pending` + exactly one co-provisioned Account `active` + one `CustomerCreated`, zero Profiles — D7; duplicate email → `DuplicateCustomerEmail` on `email`, no Customer/Account/event; `assertFormFieldDoesNotExist('status')`). The Select options helpers: currency `collect(Currency::cases())->mapWithKeys(...)` and locale `collect(SupportedLocale::cases())->mapWithKeys(...)` — but these are PLATFORM enums (import freely, not the operand-enum carve-out). Then 3 lifecycle / 4 i18n / 5 PG17 chain.
---

## [2026-06-21 22:11] — 2.1 / 2.2 Customer create surface (form) + create-field i18n
- **What:** the Customer write-through create surface, completing group 2. The `CreateCustomer` page (real `createViaAction` + `createRejectionField`) already shipped group 1, so the remaining work was `CustomerResource::form()` ONLY + the `fields.{email,name}` i18n that completes the labels. Added `form()` (placed after `i18nKey()`, mirroring `ClubResource`/`ProducerResource`): `email` (`->email()->required()->maxLength(255)`), `name` (required, maxLength 255), `preferred_currency` required `Select` off `currencyOptions()`, `preferred_locale` required `Select` off `localeOptions()`, `phone` optional `TextInput`, `date_of_birth` optional `DatePicker` — **NO `status`** (born `pending`, D5). Two private option helpers (`currencyOptions()`/`localeOptions()`) `collect(Enum::cases())->mapWithKeys(value=>value)` over the PLATFORM `Currency`/`SupportedLocale` enums (freely importable — D6, NOT the operand-enum carve-out). With the form present, the createViaAction is now exercised end-to-end. EN `fields.{email,name}` = Email/Name; IT = **Indirizzo email**/Nome (the IT-differs guard needs IT != EN; email re-uses the `columns.email` collision-breaker).
- **Files changed (modified):** `app/Modules/OperatorPanel/Filament/Resources/Parties/CustomerResource.php` (+5 imports: `App\Platform\I18n\SupportedLocale`, `App\Platform\Money\Currency`, `Filament\Forms\Components\{DatePicker,Select,TextInput}`; + `form()` + 2 helpers); `lang/{en,it}/operator_console.php` (+`customer.fields.{email,name}`). **(new):** `tests/Feature/Modules/OperatorPanel/Parties/CustomerCreateConsoleTest.php` (3 it, `DatabaseMigrations`). No migration/composer/protected-file/`ModuleBoundariesTest` change.
- **Quality loop: green** — pint clean; filtered `CustomerCreateConsoleTest` 3/3 (38 assn); full SQLite **1333/1333** (7424 assn, +3 tests / +38 assn vs the 1330 baseline); phpstan **0** (the `NoEloquentWriteInOperatorPanelRule` over `CustomerResource`/`CreateCustomer` green — the form COLLECTS, the action WRITES); pint --test clean; `ModuleBoundariesTest` 3/3 (189 assn) — UNCHANGED (the form's only new imports are `App\Platform\*`, not `Parties\Enums` — **D6 proven again**); `openspec validate … --strict` valid; composer diff vs `main` empty.
- **Acceptance walked (group 2):** valid submit → Customer `pending` + name/preferred_currency/preferred_locale/phone persisted + date_of_birth non-null ✓; exactly one co-provisioned Account `active` ✓; exactly one `CustomerCreated` (`actor_role` NewcoOps, `actor_id` = operator, `entity_type` Customer, `entity_id` = `(string)$customer->id`) ✓; zero Profiles (D7) ✓; duplicate email → `email` form error, no new Customer / no Account-count change / no event ✓; form exposes email/name/preferred_currency/preferred_locale/phone/date_of_birth, NOT `status` ✓; no `$model->save()` (phpstan) ✓; boundary green ✓.
- **Learnings for future iterations:**
  - **The Select-options helper is the established sibling idiom** (`ClubResource::currencyOptions()`): `collect(Enum::cases())->mapWithKeys(static fn ($c) => [$c->value => $c->value])->all()`, wired via the first-class-callable `->options(self::currencyOptions(...))`. For Customer both helpers drive PLATFORM enums (`Currency`/`SupportedLocale`) — imported freely, the boundary is untouched (D6). Reuse this shape wherever a console Select offers a closed enum domain.
  - **`->email()` is a real Filament 5 `TextInput` method** (`vendor/filament/forms/src/Components/TextInput.php`) — applies the `email` validation rule; the duplicate-email rejection still comes from the ACTION (a valid-format but taken email passes form validation, then the action's uniqueness pre-check throws → mapped to the `email` field by the base catch via `createRejectionField`).
  - **Next → group 3 (task 3.1/3.2):** fill `ViewCustomer` (`extends ViewRecord` + `use SurfacesDomainActions`, NOT `OperatorConsoleViewRecord` — D1/D8) with the four form-less, no-`confirmationKey` status verbs (activate/suspend/reactivate/close via `lifecycleAction`, each closure `(Model $record, string $notes)`) + `actions.{activate,suspend,reactivate,close}` + `notifications.{activated,suspended,reactivated,closed,action_failed}` i18n. LANDMINE (D5): `activate`'s gate is cross-slice — a gate-unmet attempt rejecting (danger notification, no event) is CORRECT, do NOT set gate columns from the console.
---

## [2026-06-21 22:31] — 4.1 Customer i18n kit-key completeness (EN/IT)
- **What:** the Customer console's **capability-close i18n guard** (group 4) — a pure locale + static-source-scan feature test (no DB, no `RefreshDatabase`), closely mirroring `ClubConsoleI18nTest` (the broader-key-set sibling — Club also enumerates `columns.*`+`fields.*`+`actions.create`+verbs). Enumerates the **26** kit-contract keys the console resolves: `label`/`plural_label` + `columns.{name,email,status,kyc_status,sanctions_status,account_status,profiles,version}` (8) + `fields.{email,name,phone,date_of_birth,preferred_currency,preferred_locale}` (6) + `actions.{create,activate,suspend,reactivate,close}` (5) + `notifications.{activated,suspended,reactivated,closed,action_failed}` (5). **Half are invisible to a source scan** (string-concatenated in the base/trait — confirmed by grep: `label`/`plural_label` off `getModelLabel()/getPluralModelLabel()`, `columns.version` off `versionColumn()`, the 4 verb `actions.*` + 4 success `notifications.*` off `lifecycleAction()`, `notifications.action_failed` off `surfaceLifecycleOutcome()`), so enumeration is the ONLY guard that catches a dropped key. Five guards: (1) EN baseline `Lang::has(…, 'en', false)` ×26; (2) IT-differs (`it` authored AND ≠ en) over the 24 kit-minus-`{label,plural_label}`; (3) `label`/`plural_label` EN-fallback (absent in `it`, resolves to the EN value — DEC-127); (4) IT⊆EN filtered `str_starts_with($dotKey, 'customer.')` (trailing dot load-bearing — scopes to this entity, no sibling `columns.customer` collision); (5) reused `scanOperatorConsoleHardcodedSinks()` behind `function_exists` scoped to `CustomerResource*`.
- **Files changed (new):** `tests/Feature/Modules/OperatorPanel/Parties/CustomerConsoleI18nTest.php` (2 helper fns + 5 `it()`, 54 cases). **No** app-code / lang / migration / composer / protected-file / `ModuleBoundariesTest` change (a test-only iteration).
- **Quality loop: green** — pint clean; filtered `CustomerConsoleI18nTest` **54/54** (109 assn: 26 EN-baseline + 24 IT-differs + 2 EN-fallback + 1 IT⊆EN + 1 sink); full SQLite **1396/1396** (7629 assn, +54 tests / +109 assn vs the 1342 baseline); phpstan **0**; pint --test clean; `ModuleBoundariesTest` 3/3 (189 assn) UNCHANGED; `openspec validate … --strict` valid; composer diff vs `main` empty.
- **Acceptance walked (group 4):** the enumeration is green ✓; **a deliberately removed key fails it** — proven by a reversible mutation: dropping EN `notifications.closed` turned it RED from two angles (EN-baseline missed the key + IT⊆EN caught the now-dangling IT key), restored via `git checkout` and re-confirmed 54/54 green ✓; the sink scan found zero hardcoded strings in `…/Resources/Parties/CustomerResource*` ✓; run via `--filter` (54/54) AND the full suite (1396/1396), never a bare path (the helper-load false-red — `scanOperatorConsoleHardcodedSinks` is declared in `ProductMasterConsoleI18nTest`) ✓.
- **Learnings for future iterations:**
  - **The whole-OperatorPanel sink scan (`ProductMasterConsoleI18nTest` line 115) ALREADY scans every Filament file including Customer's**, so a new per-console scoped sink scan is a guaranteed-passing subset once the full suite is green — low-risk to add. The per-console scope only makes the guard *travel with the change* as siblings accrete under `Resources/Parties/`.
  - **All Customer i18n was authored across groups 1–3; group 4 added ZERO keys** — it only enumerates. The IT-differs guard held with no new copy because group 1–3 already broke every EN/IT collision (`columns.email`/`fields.email` IT = **'Indirizzo email'** ≠ EN 'Email'). When the i18n is authored collision-free up front, the completeness test is pure verification.
  - **Next → group 5 (task 5.1):** `CustomerConsoleChainTest` (PG17, D9) — one `it()` driving Create→View pages: (a) console-create a `pending` Customer, then a gate-unmet `activate` → `action_failed`, stays `pending`, **records no event**; (b) factory-seed a **gate-met, profile-less** Customer (`email_verified_at`/`tc_accepted_at`/`privacy_accepted_at`/`sanctions_status=Passed`, kyc_required null → event/Account/Profile-free) and drive activate→suspend→reactivate→close, asserting the emergent `DomainEvent` name set `toEqualCanonicalizing(['CustomerCreated','CustomerActivated','CustomerSuspended','CustomerReactivated','CustomerClosed'])` (gate-unmet activate added nothing; profile-less seed kept the cascade silent), every event `module==='parties'` / `actor_role===NewcoOps` / `actor_id` non-null, a representative `actor_id` loose-`toEqual` the operator. **Run on SQLite AND PG17** (the command in the tasks.md preamble — append `ProductMasterConsoleI18nTest.php` for the folder-wide run so the shared sink helper loads). This is the LAST task → final-pass `<promise>CHANGE_COMPLETE</promise>`.
---

## [2026-06-21 22:22] — 3.1 / 3.2 Customer status lifecycle verbs (ViewCustomer) + actions/notifications i18n
- **What:** the Customer write-through STATUS surface, completing group 3 — the full demand-side status FSM (`pending → active → suspended → closed`) on the view page. Filled the bare `ViewCustomer` stub: `extends \Filament\Resources\Pages\ViewRecord` + `use SurfacesDomainActions` (the trait-level recipe, NOT the catalog `OperatorConsoleViewRecord` base — D1/D8), `i18nKey()='customer'`, and a `getHeaderActions()` assembling the four FORM-LESS, no-`confirmationKey` verbs via `lifecycleAction` (D3 — the Customer FSM has no SoD floor): `activate→activated→ActivateCustomer`, `suspend→suspended→SuspendCustomer`, `reactivate→reactivated→ReactivateCustomer`, `close→closed→CloseCustomer`, each closure `fn (Model $record, string $notes) => app(<Action>::class)->handle($this->recordOf(Customer::class, $record)->id)` (the Parties actions take `int $id` — D4; `$notes` unused). NO Hold/KYC/sanctions/account/profile verb (the compliance/account/profile-slice boundary). `activate` is cross-slice-gated by the Action (D5) — surfaced as-is, gate columns NEVER written from the console. Added the EN (full) + IT (per-key, all ≠ EN) `actions.{activate,suspend,reactivate,close}` + `notifications.{activated,suspended,reactivated,closed,action_failed}` blocks.
- **Files changed (modified):** `app/Modules/OperatorPanel/Filament/Resources/Parties/CustomerResource/Pages/ViewCustomer.php` (stub → full: +trait, +`i18nKey()`, +`getHeaderActions()`, +6 imports — 4 `Parties\Actions\*`, `Parties\Models\Customer`, `Filament\Actions\Action`, `Model`, and `OperatorConsoleViewRecord` for the `{@see}` docblock); `lang/{en,it}/operator_console.php` (+`customer.actions.{activate,suspend,reactivate,close}` + `customer.notifications.*`). **(new):** `tests/Feature/Modules/OperatorPanel/Parties/CustomerLifecycleConsoleTest.php` (7 it / 9 cases, `DatabaseMigrations`). No migration/composer/protected-file/`ModuleBoundariesTest` change.
- **Quality loop: green** — pint clean; filtered `CustomerLifecycleConsoleTest` 9/9 (96 assn); full SQLite **1342/1342** (7520 assn, +9 tests / +96 assn vs the 1333 baseline); phpstan **0** (the `NoEloquentWriteInOperatorPanelRule` over `ViewCustomer` green — the verbs route through actions, no Eloquent write); pint --test clean; `ModuleBoundariesTest` 3/3 (189 assn) — UNCHANGED (the new imports are `Parties\Actions\*` + `Parties\Models\Customer`, all within the {Models, Actions} carve-out — **no boundary widening**); `openspec validate … --strict` valid; composer diff vs `main` empty.
- **Acceptance walked (group 3):** gate-met `pending` → `active` + one `CustomerActivated` (newco_ops + operator id) ✓; gate-UNMET activate on a bare `pending` → `action_failed` danger, stays `pending`, **no event** (D5) ✓; suspend `active` → `suspended` + one `CustomerSuspended` (cascade-silent, profile-less) ✓; reactivate `suspended` → `active` + one `CustomerReactivated` ✓; close `active` → `closed` + one `CustomerClosed` ✓; out-of-state suspend-`pending` / reactivate-`active` / close-`pending` → `action_failed`, state + event-log unchanged ✓; scope-guards `assertActionExists` ×4 + `! isConfirmationRequired()` ×4 + `assertActionDoesNotExist` submit/reject/reopen/placeHold/liftHold/requireKyc ✓.
- **Learnings for future iterations:**
  - **The trait-level status-FSM view page is now the proven demand-side standard** (Producer + Customer). See Codebase Patterns §"Non-catalog status-FSM view page" — `lifecycleAction(verb, successKey, fn (Model, string) => app(Action)->handle(recordOf(Model, r)->id))`, form-less, no `confirmationKey`; `OperatorConsoleViewRecord` imported ONLY for the `{@see}` docblock (pint counts it used). D8 rule-of-three is CLOSED to this shape.
  - **The cross-slice gate (D5) is a TEST-SEED concern, not a code concern.** Gate-met seed = the 4 onboarding fields + `sanctions_status=Passed` (kyc_required null); gate-unmet = bare factory. The factory is event/Account/Profile-free, so rejected verbs leave `DomainEvent::count()===0` and a profile-less Customer keeps the suspend/reactivate cascade silent. Both captured in Codebase Patterns.
  - **Next → group 4 (task 4.1):** `CustomerConsoleI18nTest` enumerating the full kit-contract keys (resource `label`/`plural_label`/`columns.*`; the four verbs' `actions.*` + `actions.create`; `notifications.*`; create `fields.*`) with the FIVE `ProducerConsoleI18nTest`-mirrored guards (EN baseline, IT-differs over kit-minus-`{label,plural_label}`, label/plural_label EN-fallback, IT⊆EN filtered to `customer.*`, sink-scan scoped to `CustomerResource*`). Then group 5 = `CustomerConsoleChainTest` (PG17). Recompute the kit count now that all keys are authored.
---

## [2026-06-22 08:25] — 5.1 Customer full closing-chain integration test (PG17)
- **What:** the change's **CLOSING integration proof** (group 5, the LAST task) — one `it()` driving a Customer through the WHOLE console slice end-to-end through the **pages** (not raw Actions), per D9. Two parts in one demo: **(a)** create via the `CreateCustomer` page → a `pending` Customer + 1 `CustomerCreated` (Account leg event-silent, D7), then a gate-UNMET `activate` via `ViewCustomer` → `action_failed` danger notification, stays `pending`, **records no event** (the rejected txn rolled back — documents the cross-slice gate D5); an intermediate `expect(DomainEvent::pluck('name')->all())->toEqual(['CustomerCreated'])` **localises** the gate-reject claim at its point of cause. **(b)** factory-seed a **gate-MET, profile-less** Customer (`email_verified_at`/`tc_accepted_at`/`privacy_accepted_at`=now + `sanctions_status: Passed`, `kyc_required` null → KYC rider clears; factory bypasses `CreateCustomer` → event/Account/Profile-free) and drive `activate → suspend → reactivate → close` through `ViewCustomer`. Emergent-set proof over the whole run: `DomainEvent::pluck('name')->all()` `->toEqualCanonicalizing(['CustomerCreated','CustomerActivated','CustomerSuspended','CustomerReactivated','CustomerClosed'])` (5 events — gate-unmet activate added nothing; profile-less seed kept the suspend/reactivate Profile cascade silent, D9); `->toHaveCount(5)` + a `foreach` asserting every event `module==='parties'` / `actor_role===ActorRole::NewcoOps` / `actor_id` non-null; then the representative `CustomerCreated` (create page) + `CustomerClosed` (a view-page verb) `actor_id` **loose `toEqual`** the operator (spans BOTH surfaces; PG bigint-as-string). Modelled on `ClubConsoleChainTest`, adapted to D9's two-part shape.
- **Files changed (new):** `tests/Feature/Modules/OperatorPanel/Parties/CustomerConsoleChainTest.php` (1 `it`, `DatabaseMigrations`). **No** app-code / lang / migration / composer / protected-file / `ModuleBoundariesTest` change (a test-only iteration).
- **Quality loop: green on SQLite AND PG17** — pint clean; filtered `CustomerConsoleChainTest` 1/1 (54 assn) SQLite; full SQLite **1397/1397** (7683 assn, +1 test / +54 assn vs the 1396 baseline); **PG17 folder run 274/274** (1274 assn — Parties console folder incl. this chain test + the Catalog `ProductMasterConsoleI18nTest` sink-helper loader, `postgres:17` on 55432, container torn down after); phpstan **0**; pint --test clean; `ModuleBoundariesTest` 3/3 (189 assn) UNCHANGED; `openspec validate … --strict` valid; composer diff vs `main` empty.
- **Acceptance walked (group 5):** (a) page-create → `pending` ✓; gate-unmet activate → `action_failed`, stays `pending`, only `CustomerCreated` so far ✓; (b) gate-met seed activate→suspend→reactivate→close advances `pending→active→suspended→active→closed` ✓; emergent set `toEqualCanonicalizing` the five Customer events ✓; `toHaveCount(5)` + envelope `foreach` (`parties` / `NewcoOps` / non-null actor) ✓; representative `CustomerCreated`+`CustomerClosed` `actor_id` loose-`toEqual` operator ✓; green SQLite AND PG17, phpstan 0, pint clean ✓. **ALL 9 TASKS DONE.**
- **Learnings for future iterations:**
  - **The emergent-set assumption held empirically** (the `toEqualCanonicalizing` is self-verifying): nothing consumes a Customer lifecycle event into the recorder (grep — the 4 event names appear only as the Actions' emitters + Profile-event docblock cross-refs), and the profile-less seed makes suspend/reactivate cascade-silent. A console-created Customer's Account leg records NO event (D7). So the only domain_events are the 5 console writes.
  - **The two-part chain pattern for a cross-slice-gated FSM:** drive the gate-UNMET path through the real create page (proves the surface rejects gracefully, D5) THEN a factory-seeded gate-MET record through the full FSM (the create page can't reach `active`). An intermediate single-element `toEqual([...])` right after the reject localises the gate claim before the global emergent-set assertion. Reusable for any future console whose activate/transition is gated by columns the slice can't set.
  - **PG17 ritual (GUIDE §2.7):** `docker run -d --name pg … postgres:17 -p 55432:5432` → poll `pg_isready` → run the folder pest with the `DB_CONNECTION=pgsql …` env (append a Catalog i18n test so the shared `scanOperatorConsoleHardcodedSinks` helper loads) → `docker rm -f pg`. The loop never pushes/merges (RALPH.md) — PG verify only.
---
