# Tasks — operator-console-parties-customer

> Pure operator surface over shipped Parties domain Actions. **No new domain code, no migration, no composer dep, no module-boundary change** (create operands are platform-level — `Currency`, `SupportedLocale` — so the operand-enum carve-out is NOT exercised; do not touch `ModuleBoundariesTest`). Read the predecessor `## Codebase Patterns` (`openspec/changes/archive/2026-06-21-operator-console-parties-supply-side/progress.md`) and this change's `design.md` (D1–D9) before starting. The ADR (`decisions/2026-06-21-operator-console-rule-of-three-trait-vs-verb-list.md`) is authored with this change — implement to it (continue at the trait level; do NOT extend `OperatorConsoleViewRecord`), do not re-decide it. **Group 1 scaffolds the Create/View page stubs so `CustomerResource::getPages()` boots — sequence it first.** Every task ends with the quality loop green: `vendor/bin/pint`, `php -d memory_limit=-1 vendor/bin/pest` (filtered + full), `php -d memory_limit=-1 vendor/bin/phpstan analyse`, `vendor/bin/pint --test`, `openspec validate operator-console-parties-customer --strict`, composer diff vs `main` empty, no protected-file edits. PG17 closing-chain command (Group 5): `DB_CONNECTION=pgsql DB_HOST=127.0.0.1 DB_PORT=55432 DB_DATABASE=newco_test DB_USERNAME=newco DB_PASSWORD=newco php -d memory_limit=-1 vendor/bin/pest tests/Feature/Modules/OperatorPanel/Parties` (append `tests/Feature/Modules/OperatorPanel/Catalog/ProductMasterConsoleI18nTest.php` for the folder-wide run so the shared `scanOperatorConsoleHardcodedSinks` helper loads).

## 1. Customer read-only Resource + list + scaffold + resource/infolist i18n

- [x] 1.1 Create `app/Modules/OperatorPanel/Filament/Resources/Parties/CustomerResource.php` — `extends OperatorConsoleResource` (reuse the label helpers off `static i18nKey(): string => 'customer'`, and `versionColumn()`). Read-bind `\App\Modules\Parties\Models\Customer`. List columns: `name` · `email` · a **`status`** badge `TextColumn` rendering `CustomerStatus->value` via `getStateUsing` + `instanceof BackedEnum` (**no `Parties\Enums` import** — state enum via cast, D2) · a `kyc_status` badge and a `sanctions_status` badge (same cast idiom — the two orthogonal axes, rendered independently) · account status (`fn (Customer $r) => $r->account?->status->value` — a within-module read, no extra import) · profiles count (`fn (Customer $r) => $r->profiles->count()`) · `versionColumn()`. Infolist: name / email / phone / date_of_birth / preferred_currency / preferred_locale, the status + kyc_status + sanctions_status badges, account status, and a read-only Profiles list. **No** create/edit/delete mutating action. `getPages()` → index/create/view. Do **NOT** call `lifecycleStateColumn()` (that is the catalog `lifecycle_state`, D2).
- [x] 1.2 Create `…/CustomerResource/Pages/ListCustomers.php` — the header create-LINK page (copy the shape from `ListProducers`, not a `CreateAction`). Scaffold `…/Pages/CreateCustomer.php` (real `createViaAction` lands in 2.1) and `…/Pages/ViewCustomer.php` (bare `ViewRecord` stub; verbs land in 3.1) so the Resource's `getPages()` boots (the eager-reference coupling — design Risks).
- [x] 1.3 Add the `operator_console.customer.*` resource/column/infolist copy to `lang/en/operator_console.php` (full) and `lang/it/operator_console.php` (per-key; `label`/`plural_label` may fall back to EN — DEC-127): `label`, `plural_label`, `columns.{name,email,status,kyc_status,sanctions_status,account_status,profiles,version}`, and the infolist field labels.
- **Tests** (`tests/Feature/Modules/OperatorPanel/Parties/CustomerResourceTest.php`, `RefreshDatabase`): a seeded Customer renders in the list with its `status`, `kyc_status`, `sanctions_status`; `CustomerResource::getPages()` keys == `['index','create','view']`; the table exposes no edit/delete/bulk action. Arch/boundary: `NoEloquentWriteInOperatorPanelRule` + `ModuleBoundariesTest` stay green over the new class (no `Parties\Enums` import introduced).

## 2. Customer create surface (write-through, platform operands)

- [x] 2.1 Create `…/CustomerResource/Pages/CreateCustomer.php` — `extends OperatorConsoleCreateRecord`; alias `use App\Modules\Parties\Actions\CreateCustomer as CreateCustomerAction;` (page/action name collision). Form (on `CustomerResource::form()`): `email` required `TextInput` (email rule); `name` required `TextInput`; `preferred_currency` required `Select` (ISO 4217 options); `preferred_locale` required `Select` (the `SupportedLocale` cases); `phone` optional `TextInput`; `date_of_birth` optional `DatePicker`. **No `status` field.** `createViaAction(array $data): Model` narrows the payload, constructs the platform operands `Currency::of($code)` (`App\Platform\Money\Currency`) and `SupportedLocale::from($value)` (`App\Platform\I18n\SupportedLocale`) — **no `Parties\Enums` import, no boundary change** (D6) — and calls `app(CreateCustomerAction::class)->handle(email:, name:, preferredCurrency:, preferredLocale:, phone:, dateOfBirth:)`. `createRejectionField(): string => 'email'` (a `DuplicateCustomerEmail` lands there). No `$model->save()`.
- [x] 2.2 Add the create-field labels to `lang/{en,it}/operator_console.customer.fields.*` and `operator_console.customer.actions.create`.
- **Tests** (`CustomerCreateConsoleTest.php`, `DatabaseMigrations`): submitting a valid Customer invokes the action → a Customer in `pending` **plus exactly one co-provisioned Account in `active`** + exactly one `CustomerCreated` event (`actor_role: newco_ops`, `actor_id` = the acting operator, `entity_type` `Customer`); the created Customer has **zero Profiles** (D7); a duplicate email → `DuplicateCustomerEmail` surfaced on `email`, no Customer + no Account + no event; the form exposes email/name/preferred_currency/preferred_locale/phone/date_of_birth and **no** `status` field (`assertFormFieldDoesNotExist('status')`). Boundary: `ModuleBoundariesTest` green (operands platform-level — proves D6). No `$model->save()` (phpstan rule green).

## 3. Customer status lifecycle — activate / suspend / reactivate / close (ViewCustomer)

- [x] 3.1 Fill `…/CustomerResource/Pages/ViewCustomer.php` — `extends \Filament\Resources\Pages\ViewRecord` and `use SurfacesDomainActions` (NOT `OperatorConsoleViewRecord`, D1). `i18nKey(): string => 'customer'`. `getHeaderActions()` returns the four status actions via `lifecycleAction` — **form-less, no `confirmationKey`** (no SoD affordance, D3): `activate`→`activated`→`fn (Model $r) => app(ActivateCustomer::class)->handle($this->recordOf(Customer::class,$r)->id)`; `suspend`→`suspended`→`app(SuspendCustomer::class)->handle(...->id)`; `reactivate`→`reactivated`→`app(ReactivateCustomer::class)->handle(...->id)`; `close`→`closed`→`app(CloseCustomer::class)->handle(...->id)` (D4). Each closure has the uniform `(Model $record, string $notes)` signature (`$notes` unused). **No** place-hold / lift-hold / KYC / sanctions / account / profile verb (the compliance/account/profile-slice boundary).
- [x] 3.2 Add `operator_console.customer.actions.{activate,suspend,reactivate,close}` and `notifications.{activated,suspended,reactivated,closed,action_failed}` to `lang/{en,it}`.
- **Tests** (`CustomerLifecycleConsoleTest.php`, `DatabaseMigrations`): activate a **gate-met** `pending` Customer (seed `email_verified_at`/`tc_accepted_at`/`privacy_accepted_at` + `sanctions_status: Passed`, `kyc_required` null) → `active` + exactly one `CustomerActivated` (`actor_role: newco_ops`, `actor_id` = operator); **gate-unmet activate** on a fresh `pending` Customer → `action_failed` danger notification, stays `pending`, **no event** (the cross-slice gate, D5); suspend an `active` → `suspended` + one `CustomerSuspended`; reactivate a `suspended` → `active` + one `CustomerReactivated`; close an `active` → `closed` + one `CustomerClosed`; out-of-state (suspend not-`active` / reactivate not-`suspended` / close `pending`) → `action_failed`, state + event log unchanged; **scope-guards**: `assertActionExists('activate'/'suspend'/'reactivate'/'close')`, each `! $action->isConfirmationRequired()`, and `assertActionDoesNotExist('submit'/'reject'/'reopen'/'placeHold'/'liftHold'/'requireKyc')`.

## 4. Customer i18n kit-key completeness (EN/IT)

- [x] 4.1 Add `CustomerConsoleI18nTest.php` enumerating the **kit-contract** keys this console resolves by string concatenation: resource `label`/`plural_label`/`columns.{name,email,status,kyc_status,sanctions_status,account_status,profiles,version}`; per the four verbs `actions.{activate,suspend,reactivate,close}` + `actions.create`; `notifications.{activated,suspended,reactivated,closed,action_failed}`; plus the create `fields.*`. Five guards mirroring `ProducerConsoleI18nTest` — EN baseline (`Lang::has("operator_console.customer.{$suffix}", 'en', false)`), IT-differs (authored in `it` AND `!= en`) over the kit-minus-`{label,plural_label}` set, `label`/`plural_label` EN-fallback, IT⊆EN over the `customer.*` dotted block (filter by `str_starts_with($dotKey, 'customer.')` — guard against sweeping other entities' `columns.*` collisions), and the reused `scanOperatorConsoleHardcodedSinks()` behind a `function_exists` guard scoped to `CustomerResource*`.
- **Tests**: the enumeration is green (a deliberately removed key fails it); the sink scan finds zero hardcoded strings in `…/Resources/Parties/CustomerResource*`. Run via `--filter=CustomerConsoleI18nTest` or the full suite, NOT a bare file path (the helper-load false-red — design Risks).

## 5. Customer full closing-chain integration test (PG17)

- [ ] 5.1 Add `CustomerConsoleChainTest.php` — one `it()` driving a Customer through the console **pages** per D9. **(a)** `Livewire::test(CreateCustomer::class)->fillForm([valid])->call('create')->assertHasNoFormErrors()` → assert the new Customer is `pending`; then `Livewire::test(ViewCustomer::class, ['record' => $pendingId])->callAction('activate')->assertNotified(...action_failed...)` → assert it stays `pending` (the gate-not-met path, **records no event**). **(b)** Seed a **gate-met, profile-less** Customer event-free via `Customer::factory()->create(['email_verified_at' => now(), 'tc_accepted_at' => now(), 'privacy_accepted_at' => now(), 'sanctions_status' => SanctionsStatus::Passed])` (the factory bypasses `CreateCustomer` → no Account, no `CustomerCreated`; `kyc_required` null → KYC rider clears), then `Livewire::test(ViewCustomer::class, ['record' => $id])` `->callAction('activate')` → `('suspend')` → `('reactivate')` → `('close')`. Assert: the emergent event set `DomainEvent::query()->pluck('name')->all()` `->toEqualCanonicalizing(['CustomerCreated','CustomerActivated','CustomerSuspended','CustomerReactivated','CustomerClosed'])` (the gate-unmet activate added nothing; the profile-less seed kept the cascade silent — D9); a `foreach` asserting every event `module === 'parties'`, `actor_role === ActorRole::NewcoOps`, `actor_id` non-null; a representative `actor_id` equals the acting operator (loose `toEqual` — PG returns a numeric string).
- **Tests**: green on SQLite AND PG17 (the command in the preamble); phpstan 0; pint clean.
