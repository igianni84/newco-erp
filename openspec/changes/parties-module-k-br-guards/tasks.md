## 1. Canon mini-ADRs (do first — ground every canon guard; LIVE-sourced)

- [x] 1.1 Write `decisions/2026-07-07-adopt-mvp-dec-009-agreement-club-active-scope.md` (Agreement-4). Re-fetch LIVE canon read-only (`git -C ../documentation fetch cmless main`; read `handoff/04-decisions/MVP_Decisions_Register_v0.1.md` MVP-DEC-009 + `handoff/03-acceptance/Module_K_Acceptance…` AC-K-BR-Agreement-4 via `git show cmless/main:…` — never modify the clone). Record: new per-Club agreement requires an `active` Club; Producer-wide ungated; supersession inherits scope + exempt; canon absent from frozen `spec/`@MVP-DEC-007. _Acceptance:_ ADR present with the standard frontmatter (`status: active`) and Decision/Context/Alternatives/Reasoning/Trade-offs/References; `decisions/INDEX.md` row added.
- [x] 1.2 Write `decisions/2026-07-07-adopt-mvp-dec-010-settlement-cadence-closed-set.md` (RM-22). From LIVE canon MVP-DEC-010 + AC-K-BR-Agreement-2: closed set `{quarterly (default), monthly, semi-annual}`, server-enforced (API + DB); **`annual`/sub-monthly excluded** → record the `DemoSeeder` `annual → semi-annual` migration as a consequence. _Acceptance:_ ADR + INDEX row; the excluded values + the seeder change are explicit.
- [x] 1.3 Write `decisions/2026-07-07-adopt-mvp-dec-022-club-membership-governance.md` (Club-6 + Identity-6 + Profile-5 + Producer-5). From LIVE canon MVP-DEC-022 (CML-89) + AC-K-BR-Club-6 / Identity-6 / Producer-5 / Profile-5. **Record the interim postures explicitly:** Producer-5 = a content-immutability lock while `active` (safety core), full re-arm deferred (no Producer `reviewed`/edit path — RM-06/RM-14 precedent); Profile-5 = K-side inherit + operator-set, customer self-toggle deferred (Consumer Portal); Identity-6 null-DOB → block. _Acceptance:_ ADR + INDEX row; each of the four criteria + its build-vs-deferred split named.

## 2. Schema, enums, exceptions (foundation)

- [x] 2.1 Add `App\Modules\Parties\Enums\SettlementCadence` (`Quarterly`/`Monthly`/`SemiAnnual`, string-backed `quarterly`/`monthly`/`semi_annual`) + cast `ProducerAgreement.settlement_cadence` to it; migration regenerates the `parties_producer_agreements` `settlement_cadence` CHECK from `SettlementCadence::cases()` (Postgres-truthful; SQLite skips CHECK, the cast is the floor). _Acceptance:_ `SettlementCadenceEnumsTest` — `toHaveCount(3)`, the three values, default `quarterly`; PG migration emits the 3-token CHECK.
- [x] 2.2 Migration: add `parties_profiles.auto_renew` (bool, not-null) + `parties_clubs.auto_renew_default` (bool, not-null, default `true`); add `@property bool $auto_renew` / `$auto_renew_default` + casts. _Acceptance:_ migration up/down clean on SQLite + PG17; model casts assert bool.
  > ℹ 2.2 added both columns NOT-NULL with a `true` DB default (not "no default"): an additive NOT-NULL column needs a default on SQLite `ADD COLUMN`, and every existing writer (Club/Profile factory, DemoSeeder L266/L435, `CreateProfile`/`CreateClub`) omits it — the `true` floor keeps all inserts green with ZERO writer changes. **3.x/4.2 note:** task 4.2 makes `CreateProfile` inherit the Club's `auto_renew_default` explicitly (the default stays a harmless floor — no drop-default migration; spec mandates the inheritance, not the DB default's absence).
- [x] 2.3 Migration: **drop** `parties_clubs.invite_only`; remove `invite_only` from `Club` model, `CreateClub`, and the `ClubCreated` event payload. _Acceptance:_ column gone; `ClubCreated::payload()` carries no `invite_only`; `grep -rn invite_only app/ database/` is empty (lang/tests handled in their groups).
  > ℹ 2.3 was an ATOMIC sweep (design R1: "one migration drops the column; sweep all 16 files"), not the minimal core the split implies. Dropping the column forces every reader/writer in ONE commit or the suite reds (`Club::factory()` → SQL "no such column" across the whole suite; the console page→Action named-arg `inviteOnly:` → TypeError). **DROP = EDIT the `2026_06_15_000003` create-table in place** (removed the column line), NOT a new drop-migration: the acceptance `grep database/ empty` is only satisfiable by editing create-table, and it's pre-launch (no deployed DB; every run is `migrate:fresh`). The hot.md "new additive migration, never edit create-table" hint was the 2.2 ADDITIVE heuristic misapplied to a DROP — the acceptance criteria (change artifact) override a NEXT-task prediction. Swept 14 files (app model/action/event + 3 Filament + create-table/factory/seeder + 4 tests + `CONTEXT.md` payload doc). Verified column GONE on SQLite + PG17. **Left for 6.4 (stay green untouched):** lang EN/IT `invite_only` key + `ClubConsoleI18nTest` L48/L62 — the key is still authored + en/it parity holds → the i18n scanner passes; 2.3's grep scope is app/+database/ ONLY.
- [x] 2.4 Add the five localized guard exceptions (EN + IT copy in `lang/{en,it}/parties.php`), each a `RuntimeException` subclass with static named factories matching the module idiom (`SeparationOfDutiesViolation` shape): `ProducerAgreementScopeConflict`, `ProducerAgreementClubNotActive`, `ClubNotAcceptingMemberships`, `BelowMinimumRegistrationAge`, `ProducerReviewGovernedContentLocked`. _Acceptance:_ per-exception unit tests assert `toBeInstanceOf(RuntimeException::class)` (bare, no `use` — global-namespace test trap) + the localized message resolves in EN and IT.
  > ℹ 2.4 authored the five exceptions as PURE definitions (factories + copy + tests) — NO wiring (the guards land in 3.2 `ProducerAgreementClubNotActive::forClub` / 3.3 `ProducerAgreementScopeConflict::producerWideBlockedByClubScope|clubScopeBlockedByProducerWide` / 4.1 `ClubNotAcceptingMemberships::forClub` / 5.1 `BelowMinimumRegistrationAge::belowMinimum|missingDateOfBirth` / 5.2 `ProducerReviewGovernedContentLocked::whileActive`). Factory signatures (int ids, `string $state`) are the sibling `MissingClubProducer`/`DuplicateProfileForClub` convention. Copy lives in the EXISTING groups: `producer` (content-lock), `club` (not_accepting_memberships), `producer_agreement` (club_not_active + 2 scope_conflict), `customer` (2 age-gate). **IT is genuinely authored (mirrors the `approval` group, not the EN-only `compliance_review` fallback)** — the task acceptance says "resolves in EN and IT"; `PartiesApprovalCopyTest`'s IT⊆EN parity holds (every IT key has an EN counterpart). PII discipline: age-gate interpolates ONLY `:min_age` (never DOB/derived age); all others name the rule + operator-facing id/state.

## 3. ProducerAgreement guards (RM-22, Agreement-4, RM-20)

- [x] 3.1 **RM-22** — enforce the closed cadence set server-side in `CreateProducerAgreement` (reject an out-of-set/typo cadence before the write). Migrate `DemoSeeder`'s `annual` row → `semi-annual`. _Acceptance:_ `ProducerAgreementTest` — `quarterly`/`monthly`/`semi-annual` admitted, `annual`/`weekly` rejected (no row, no event); `SettlementCadence` cast round-trips; DemoSeeder seeds without a CHECK violation on PG17.
  > ℹ 2.1 ALREADY did the DemoSeeder `annual → semi_annual` migration (mandatory consequence of adding the enum cast — the `create()` would `ValueError` on `'annual'`) + the `SettlementCadence` cast round-trip (`ProducerAgreementTest:50`). 3.1 is now ONLY the server-side out-of-set/typo **rejection guard** in the `CreateProducerAgreement` action (the action still passes `?string` through → a bad free-text value currently throws a raw `ValueError`; 3.1 replaces that with a clean localized reject before the write). Note the enum token is `semi_annual` (underscore), not `semi-annual`.
- [x] 3.2 **Agreement-4** — Club-active scoping guard in the `CreateProducerAgreement` path: a per-Club scope whose Club is `sunset`/`closed` → `ProducerAgreementClubNotActive`; Producer-wide (`club_id` NULL) ungated. _Acceptance:_ `ProducerAgreementTest` — active-Club scope admitted, sunset/closed-Club scope rejected (no row/event), Producer-wide admitted; the supersession-inherits-scope exemption is covered in 3.3.
- [x] 3.3 **RM-20** — cross-shape mutual-exclusion guard in `ActivateProducerAgreement`: reject activation of a Producer-wide agreement while any per-Club agreement of that Producer is `active`, and vice versa (`ProducerAgreementScopeConflict`), pre-write in the txn (state + event log unchanged). Keep same-scope supersession intact; confirm a supersession whose Club has since become `sunset` still activates (Agreement-4 exemption). **Invert the shipped coexistence tests:** `ProducerAgreementLifecycleTest` L157 + L206 flip from "both active" to "rejected with `ProducerAgreementScopeConflict`". _Acceptance:_ new + inverted `ProducerAgreementLifecycleTest` — Producer-wide-then-per-Club and per-Club-then-Producer-wide both rejected; same-scope supersession (L95/L98) still green; console `ProducerAgreementLifecycleConsoleTest` surfaces the conflict as a notification. Grep `grep -rn 'ActivateProducerAgreement' app/ tests/` + `callAction('activate')` first.

## 4. Profile + Club guards (RM-21, Profile-5, Club-6, RM-19)

- [x] 4.1 **RM-21** — Club-active guard in `CreateProfile`: a `sunset`/`closed` target Club → `ClubNotAcceptingMemberships` (no Profile/event). Grep all `CreateProfile` callers (`app/ tests/ database/` + `callAction('create')`) — confirmed all use active Clubs. _Acceptance:_ `ProfileTest` — active-Club create admitted; sunset + closed create rejected (no row/event); the full suite stays green (no caller regressed).
  > ℹ 4.1 wired the 3rd 2.4 exception (`ClubNotAcceptingMemberships::forClub(int, string)`) via the 3.2 guard-placement mechanics — but the guard is UNCONDITIONAL (no `if ($clubId !== null)` wrapper): `CreateProfile.clubId` is a REQUIRED `int`, so every Profile targets exactly one Club. Placed FIRST in the txn, BEFORE the BR-K-Identity-2 uniqueness check (a `sunset` Club is a blanket gate on the target — it blocks ALL new memberships, so it precedes the per-pair check). Non-existent Club → `first()` null → skip to FK backstop (only a real non-active Club trips it). RM-08 blast radius = ZERO (full suite 2017/2017; every `CreateProfile` caller uses default-`active` ClubFactory / `CreateClub`-born-active). **`auto_renew` inheritance NOT built here** — that is 4.2 (Profile-5): the delta-spec § *Profile — Multi-Profile Membership* bundles both in one requirement paragraph (L261/L266), but the TASKS split them; 4.2 must add the Club-`auto_renew_default` inheritance (the `true` DB floor from 2.2 is the interim). No PG17 run (within-module PK read + PHP guard, engine-agnostic).
- [x] 4.2 **Profile-5** — `CreateProfile` sets `auto_renew` = the target Club's `auto_renew_default` at creation; add an operator Action `SetProfileAutoRenew` (sole writer, audit-only, no event); the customer self-toggle is NOT built (Consumer-Portal seam). _Acceptance:_ `ProfileTest`/`ProfileAutoRenewTest` — inherit true/false from the Club default; operator override flips + persists (no domain event recorded — assert `%auto_renew%` produces no event via the event-ownership pattern); a code-surface assertion that no Consumer-Portal write exists.
  > ℹ 4.2 wired the inheritance into `CreateProfile`'s `create([...])` reusing the `$club` the 4.1 guard fetched (no re-query). **Ternary, NOT `$club?->auto_renew_default ?? true`:** Larastan reads `Club::query()->whereKey()->first()` as non-null → `?->` reports `nullsafe.neverNull` (PHPStan-max fail); `$club === null ? true : $club->auto_renew_default` is PHPStan-clean (the always-false `=== null` isn't flagged at this level — same as the 4.1 guard's un-flagged `!== null`) AND keeps the non-existent-Club path a clean FK QueryException (the documented backstop) rather than a property-on-null Error. `SetProfileAutoRenew` = audit-only, NO domain event (the `CancelProfile`/`ApproveProfile` precedent — § 15.2 names none; Module K writes no `audit_records` rows today, so the state write IS the record), NO from-state guard (a preference, not an FSM edge — settable in any state), no constructor deps (mirrors `CancelProfile`). **Mandatory ripple:** `SupplyLifecycleChainTest`'s EXACT-SET non-`Create*` Action whitelist (L418 `toEqualCanonicalizing`) required a new `$profilePreferenceWriters = ['SetProfileAutoRenew']` group + a union entry (the other 3 Action-globbing tests — `ComplianceIndependenceTest`, `CustomerClosureAndAccountStatusTest`, `ProfileLapseGraceTest` — are subset checks, unaffected). Code-surface "no Consumer-Portal write" = a boot-free scan of `Actions/*.php` for the `'auto_renew'` write-key (the trailing quote excludes `'auto_renew_default'`; docblocks use backticks) → exactly `{CreateProfile, SetProfileAutoRenew}`. No 2.4 exception wired (4.2 is not a guard — still 3/5). No PG17 run (no schema/SQL — column+cast were 2.2-verified on PG17).
- [x] 4.3 **Club-6** — make `OpenRegistration` non-selectable (a create/update setting `open_registration` is rejected; the three launch values admitted), and assert **no `registration_flow_type` value auto-approves** a membership (every value still requires the approval write to reach `Active`). Complete the `invite_only` removal sweep (2.3) across `ClubResource`, `CreateClub` page, `ClubsRelationManager`, factory, seeder, EN/IT lang. _Acceptance:_ `ClubTest`/`ClubRegistrationFlowTest` — the three launch values admitted, `open_registration` rejected; a membership under each launch value still needs `ApproveProfile` to activate; `grep -rn invite_only app/ tests/ database/ lang/` empty; the 5 touched Club test files green.
  > ℹ 2.3 already swept the app/+database/ invite_only refs (`ClubResource`, `CreateClub` page, `ClubsRelationManager`, factory, seeder — all done). 4.3's invite_only leg is now ONLY: the EN/IT lang keys + `ClubConsoleI18nTest` L48/L62 (shared with 6.4). 4.3's SUBSTANTIVE work is the Club-6 registration-flow logic (OpenRegistration non-selectable + no-auto-approve), untouched by 2.3.
  > ℹ 4.3 DONE: enforced via a `Club::booted()` **`saving` guard** (NOT an action guard) — the factory + DemoSeeder both `Club::create()` directly (bypass `CreateClub`), so a model guard is the only floor covering EVERY write path + the spec's literal "create OR update"; new value-domain exception `ClubRegistrationFlowNotSelectable::forFlow` (the `InvalidSettlementCadence` shape, NOT one of the 2.4 five). Factory default + 3 DemoSeeder rows migrated `OpenRegistration → ApplicationWithApproval` (the spec default; else the model guard reds the whole factory-using suite — mandatory, like 2.1's `annual→semi_annual`). No-auto-approve = characterization only (`CreateProfile` is born `Applied` regardless of flow — zero code change). invite_only lang sweep = `operator_console.php` EN/IT key + `ClubConsoleI18nTest` L48/L62 → grep-empty (removing the key drops 2 i18n dataset cases, so full suite = 2029 +8 new −2 = **2035**). `EnumsTest:135` untouched (open_registration carried latent — enum keeps 4 cases). No PG17 run (pure-PHP guard, no schema/SQL).
- [x] 4.4 **RM-19** — `RetireProducer` performs the Profile-leg cascade: after each `ClubSunset`, query Profiles by the sunsetting Club ids (no `Club→Profile` relation) and drive `CancelProfile` with a Producer-initiated reason for every `Active`/`Lapsed` Profile, in-txn, parent-before-child; **no new event** (audit-only). Grep `RetireProducer` callers (`ViewProducer`, `ProducerLifecycleTest`, `SupplyLifecycleChainTest`, `CatalogLifecycleChainTest`) and migrate any that assert a Profile-free retirement. _Acceptance:_ `ProducerLifecycleTest`/`SupplyLifecycleChainTest` — retirement cancels each `Active`/`Lapsed` Profile under sunsetting Clubs with a reason (audit-only, **no `ProfileCancelled` event**, `%ProfileCancelled%===0`), after the `ClubSunset`; already-terminal Profiles unchanged; Profiles in non-`Active`/`Lapsed` states untouched.
  > ℹ 4.4 DONE. `RetireProducer` gained a `CancelProfile` constructor dep + a `public const OFFBOARDING_CANCELLATION_REASON = 'producer_offboarding'` (the `ProfileCancellationTest` producer-offboarding token — a plain domain token the console renders verbatim, NOT localized copy: `cancellation_reason` is uncast free text, same as `LapseProfile`'s `lapsed_grace_expired`). **BATCH walk after the sunset loop** (per proposal/design D1: `Profile::whereIn('club_id', $activeClubs->pluck('id'))->whereIn('state', [Active,Lapsed])`) — parent-before-child = all sunsets THEN all cancellations, one txn. The **`whereIn('state', …)` from-state filter is load-bearing**: an unfiltered walk would hand a terminal/Applied Profile to `CancelProfile`, whose `IllegalProfileTransition::cannotCancel()` guard would throw + roll back the WHOLE retirement — the query filter (not just tolerance) is why terminal/other-state Profiles survive. Scope = the just-sunset `$activeClubs` ONLY (a since-`closed` Club is not sunsetting now → its Profiles untouched). AUDIT-ONLY: `CancelProfile` records no event → cascade adds no event (§ 15.2 names no `ProfileCancelled`); Module-S signal + Club-Credit conversion stay the §15.7/DEC-043 seam. **No migration, no PG17 run** (design: "guards read existing columns"; a within-module `whereIn` read on `club_id`/`state` — baseline-exercised on PG17). **Migration audit (grep RetireProducer callers):** ONLY `ProducerLifecycleTest`'s file docblock ("Profile leg … is deferred") was stale → corrected; every caller (`ViewProducer` console page — container resolves the new dep; `SupplyLifecycleChainTest`/`ProducerLifecycleConsoleTest`/`CatalogLifecycleChainTest`/`ProducerLifecycleProjectorTest`/`ClubLifecycleTest`/`ComplianceIndependenceTest`) creates NO Active/Lapsed Profile under a retired Producer's Clubs → all green unamended. **`SupplyLifecycleChainTest` deliberately NOT extended** — its core invariant is "the demand side stays inert through the whole supply-side chain" (asserts zero Profile-entity_type events + "supply side never touches a Profile"); its no-Profile chain no-ops the leg and STAYS GREEN (the "Profile-free retirement" it asserts remains true — CancelProfile is audit-only AND no Profiles exist), so injecting Profiles would contradict its purpose. Substantive coverage lives in `ProducerLifecycleTest` (+2 `it()`: the Active+Lapsed→Cancelled happy path with audit-only event-count 3, and the scoping/negative matrix — Applied/Suspended/already-Cancelled + Active-under-closed-Club all untouched). No 2.4 exception wired (RM-19 is orchestration, not a guard — still 3/5).

## 5. Customer + Producer guards (Identity-6, Producer-5)

- [x] 5.1 **Identity-6** — age-gate in `CreateCustomer`: reject a `date_of_birth` implying age below a configurable platform min-age constant (default 18) — and a missing DOB — with `BelowMinimumRegistrationAge` before any Customer/Account/event. Add the min-age constant (mirror the RM-02 enhanced-KYC threshold config pattern). Grep `CreateCustomer` callers for under-18/null DOB and migrate. _Acceptance:_ `CustomerTest`/`RegistrationAgeGateTest` — under-min DOB rejected (no Customer/Account/`CustomerCreated`), at/over-min admitted, null DOB rejected; the min-age is a configurable platform constant (default 18); `CustomerFactory` (DOB `1990-01-01`) keeps the suite green.
  > ℹ 5.1 wired the 4th 2.4 exception (`BelowMinimumRegistrationAge::belowMinimum(int)`/`missingDateOfBirth(int)` — verbatim). Guard placed **BEFORE `DB::transaction()`** (fail-fast pure-input value-domain reject, no DB read — the 3.1 cadence-reject placement, contrast the in-txn `DuplicateCustomerEmail` which reads) → "nothing created" is structural (the txn never opens). Two legs: null DOB → `missingDateOfBirth`; then `CarbonImmutable::now()->subYears(MIN)` cutoff + `$dateOfBirth->greaterThan($cutoff)` → `belowMinimum` (the `RenewProfile.php:70` PHPStan-clean Carbon idiom — arithmetic on the immutable `now()`, DOB only compared never mutated; INCLUSIVE — DOB == cutoff ⇒ exactly-MIN ⇒ admitted). Min-age = `public const MINIMUM_REGISTRATION_AGE = 18` on `CreateCustomer` (RM-02's `private const` shape but PUBLIC — the 4.4 public-const precedent: the test derives boundary DOBs from it + asserts `->toBe(18)`, and 6.3's console surface will reference it). **Caller migration (RM-08):** every action caller that OMITTED `dateOfBirth` (→ null → now `missingDateOfBirth`) got an adult `CarbonImmutable::parse('1990-01-01')` — 7 `CustomerTest` calls, `Membership{Activation,Suspension}ChainTest` (+`use Carbon\CarbonImmutable`), and the `CustomerCreateConsoleTest` dup-email fillForm (else it tripped the age gate instead of the dup guard). `DemoSeeder` bypasses the action (direct `Customer::create` L361) — NO seeder change; `CustomerFactory` DOB is adult AND bypasses the gate. NO migration/schema/event (design: guards read existing columns; `date_of_birth` already present). PHPStan trap: the Pest extension chokes on `try/catch`+`$this->fail()`+chained `->not->toContain` — use `->toThrow(Class::class, $messageSubstring)` for a gate-level message assertion (the copy's full PII-free proof stays in `BrGuardExceptionsTest`). Suite 2037 → **2043** (+6 `RegistrationAgeGateTest`). No PG17 run (pure-PHP boundary guard). Still 4/5 2.4 exceptions wired — 5.2 wires the last (`ProducerReviewGovernedContentLocked`).
- [x] 5.2 **Producer-5** — `Producer::booted()` `updating` guard throws `ProducerReviewGovernedContentLocked` when `name`/`description`/`region`/`website` is dirty while the persisted `status = active`; a `draft` Producer edits freely; a status/KYC-only update passes. _Acceptance:_ `ProducerContentLockTest` (RM-24 shape) — editing a review-governed field on an `active` Producer throws + leaves content unchanged (drive via `setRawAttributes`/`forceFill` past casts); a `draft` content set succeeds; activation/retirement/KYC updates (dirtying only `status`/`kyc_status`) pass — the no-false-positive guard. Full suite green (no existing Producer update regresses — verified: all touch only `status`/`kyc_status`).
  > ℹ 5.2 DONE — wired the LAST 2.4 exception (`ProducerReviewGovernedContentLocked::whileActive(int)`, verbatim). `Producer::booted()` `static::updating` guard: `getOriginal('status') === ProducerStatus::Active && isDirty(self::REVIEW_GOVERNED_FIELDS)` → throw. The **SIBLING model-guard to 4.3's Club `saving` guard** but `updating` (fires on UPDATE only, so factory/CreateProducer INSERT of `draft` content is free) + a CONDITIONAL state-gate (Club-6 is unconditional). **PERSISTED status via `getOriginal('status')`** (the exception docblock + delta spec both say "persisted") — returns the CAST enum (Laravel `transformModelValue`), `mixed === EnumCase` is PHPStan-clean (left side mixed, not narrowed). Fields = new `public const REVIEW_GOVERNED_FIELDS = ['name','description','region','website']` (4.4 public-const-for-test-DRY precedent; `isDirty(array)` = ANY-dirty). **R5 no-false-positive is STRUCTURAL, not just tested:** the guard keys on the review-governed set, and the RM-08 sweep confirmed EVERY `parties_producers` writer touches only `status` (Activate/Retire) or `kyc_status` (4 KYC actions) — the Catalog `ProducerLifecycleProjector:105 $state->update` is the boundary-clean read-model, not the Parties model. NO migration/schema/event (design: guards read existing columns). **Pint trap (2.1 pattern recurrence):** `{@see ProductMaster::booted()}` in BOTH docblocks → `fully_qualified_strict_types` auto-imports `ProductMaster` (a CROSS-MODULE import — invariant 10) → switched to backticked prose in both. Test: `forceFill` past casts (incl. `TranslatableText` description read back via `->resolve('en')`); no-false-positive driven via REAL actions (RetireProducer/RequireProducerKyc on a factory-`active` Producer = the meaningful persisted-active case; CreateProducer+ActivateProducer inline SoD for activation = persisted-draft, structurally exempt). No new Action → the Action-whitelist tests (4.2's `SupplyLifecycleChainTest` L418) are UNTOUCHED. Suite 2043 → **2051** (+8). No PG17 run (pure-PHP guard, no SQL). **ALL FIVE 2.4 exceptions now wired.**

## 6. Console surfaces + i18n

- [ ] 6.1 `ProducerAgreementResource` create surface: settlement-cadence free-text `TextInput` → a `Select` over `SettlementCadence` (default `quarterly`, constructs the operand enum); the Club picker offers only the Producer's `active` Clubs; surface `ProducerAgreementClubNotActive`/out-of-set-cadence rejections. _Acceptance:_ `ProducerAgreementCreateConsoleTest` — cadence is a select (not free text); non-active Club not selectable / rejected + surfaced; valid create still records `ProducerAgreementCreated`.
- [ ] 6.2 `ProfileResource` create surface: surface `ClubNotAcceptingMemberships`; Club picker presents `active` Clubs. Add the `SetProfileAutoRenew` operator affordance to the Profile view (a toggle wired to the Action). _Acceptance:_ `ProfileCreateConsoleTest` — a sunset/closed Club is rejected + surfaced; `ProfileConsole…Test` — the auto-renew toggle drives `SetProfileAutoRenew`.
- [ ] 6.3 `CustomerResource` create surface: surface `BelowMinimumRegistrationAge` on the date-of-birth field (the field is effectively required). _Acceptance:_ `CustomerCreateConsoleTest` — an under-age submit surfaces the rejection, no Customer created; a valid adult submit records `CustomerCreated`.
- [ ] 6.4 `ClubResource` + `CreateClub` page + `ClubsRelationManager`: remove the invite-only field; the registration-flow select offers only the three launch values (not `open_registration`). Update EN/IT `operator_console.php` (drop invite-only keys). _Acceptance:_ `ClubCreateConsoleTest`/`ClubConsoleI18nTest` — no invite-only field; the select excludes `open_registration`; i18n sink-scan green (all strings localized).
  > ℹ 2.3 already removed the invite-only FIELD from `ClubResource`/`CreateClub` page/`ClubsRelationManager` (the Toggles + IconEntry). 6.4's invite_only leg is now ONLY: drop the EN/IT `operator_console.php` `fields.invite_only` key (lang/en L642, lang/it L430 + the L631 comment) AND remove `fields.invite_only` from `ClubConsoleI18nTest` L62 (+ the L48 comment). Optionally add `assertFormFieldDoesNotExist('invite_only')` for the "no invite-only field" positive guard. The registration-flow select work is untouched by 2.3.
  > ℹ **UPDATE (4.3 done):** 4.3 dropped the `operator_console.php` invite_only key (EN L642 + L631 comment, IT L430) AND the `ClubConsoleI18nTest` L48/L62 refs to satisfy its `grep invite_only … lang/` -empty acceptance → 6.4's invite_only leg is now FULLY pre-done. 6.4's ONLY remaining work = the registration-flow SELECT (exclude the latent `open_registration` from the console options; the server-side model guard is already the floor) + the optional `assertFormFieldDoesNotExist('invite_only')` positive guard.

## 7. Close & verify (§2.7 — human-gated; NOT part of the ralph loop)

- [ ] 7.1 Full quality gate: `php -d memory_limit=-1 vendor/bin/pest` (SQLite) **and** the PG17 recipe (`DB_CONNECTION=pgsql … :55432`) both green; `vendor/bin/phpstan analyse` max 0; `vendor/bin/pint --test` clean; `openspec validate parties-module-k-br-guards --strict` green. _Acceptance:_ both engines green with the new/inverted tests; counts recorded in `progress.md`.
- [ ] 7.2 Traceability + i18n sweep: every delta requirement maps to ≥1 test; no hardcoded user-facing string (all new copy via `lang/{en,it}`); the three mini-ADRs + `decisions/INDEX.md` consistent. _Acceptance:_ a per-requirement → test matrix in `progress.md`; i18n scanner green.
- [ ] 7.3 Update `docs/validation/Remediation_Tracker.md` (RM-19/20/21/22/23 → ✅ with evidence; note J-15a/Producer-5-full/Profile-5-self-toggle deferrals), `hot.md`, and append `log.md` via `scripts/memlog.sh`. _Acceptance:_ tracker §3 + §4 + §6 updated; `hot.md` ≤550 words; one `log.md` line.
