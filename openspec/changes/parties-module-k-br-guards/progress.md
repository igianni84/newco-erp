# Progress — parties-module-k-br-guards

## Codebase Patterns
(consolidated reusable patterns — read first each iteration)

- **LIVE-canon grounding recipe (mini-ADRs 1.1–1.3).** The three canon DECs adopted here (MVP-DEC-009/010/022) are ABSENT from our frozen `spec/` (pin `4f48277` stops at MVP-DEC-007). Verify against LIVE canon, read-only: `git -C ../documentation fetch cmless main` (clone at `../documentation`, remote `cmless` → `github.com/c-mless/documentation`), then `git show cmless/main:handoff/04-decisions/MVP_Decisions_Register_v0.1.md` and `…/handoff/03-acceptance/Module_K_Acceptance_v0.3-MVP.md`. cmless/main is pinned at `360df0b` this session. **Never modify the clone.** Register-line map: MVP-DEC-007 = `:132`, MVP-DEC-008 = `:133`, MVP-DEC-009 = `:134`, MVP-DEC-010 = `:135`, MVP-DEC-011 = `:136` … MVP-DEC-015 = `:140` (one row per line, sequential). Module_K_Acceptance: AC-K-BR-Agreement-1 = `:192`, …-2 = `:193`, …-3 = `:194`, …-4 = `:195`.
- **Number-collision trap (canon MVP-DEC-NNN ≠ greenfield DEC-NNN).** Every canon-adoption ADR here must banner the collision: greenfield `DEC-009` = *"Crurated as Discovery supplier"* (`spec/04-decisions/decisions.md:71`, superseded `:139`), unrelated to MVP-DEC-009; greenfield `DEC-010` = *"12.5% club-sales margin / 5% Discovery revenue-share"* (`:76`), unrelated to MVP-DEC-010. Always use the full `MVP-DEC-NNN` token. Same trap flagged by RM-03 (DEC-016) and RM-06 (DEC-019).
- **Canon-adoption *kind* taxonomy — name it in every ADR (frames the "our spec carries the gap, not a wrong rule" argument).** Three distinct shapes: (a) **erratum of omission** — canon *adds a missing BR* absent from our frozen spec (MVP-DEC-009 adds BR-K-Agreement-4; *"No new DEC — editorial completion of DEC-070"*); (b) **value-domain tightening** — canon *closes an open set* on a BR our spec already carries (MVP-DEC-010 closes DEC-042's open "e.g." cadence set on the existing BR-K-Agreement-2; *"No new DEC — bridges DEC-042"*); (c) **behaviour inversion** — canon *flips a shipped rule* (RM-20 replaces the "MAY both be active" coexistence claim). (a)+(b) are completeness fixes (low blast radius, add a negative test); (c) inverts shipped tests. 1.3/MVP-DEC-022 is a *mix* — Club-6 collapses a redundant field (tightening), Identity-6 adds a new gate (omission-like), Producer-5 is an interim safety lock. The bridged greenfield DEC (070/042/…) being *present* in our spec is the tell that it's (a)/(b) not (c).
- **Doc-only mini-ADR = enum/DB details cited from real code, not the tracker.** The 1.2 grounding cited the *current* free-text state from source (`…producer_agreements_table.php:59` = `->string()->nullable()`; model `@property string|null`; `DemoSeeder.php:296` = the `annual` row; `ProducerAgreementFactory.php:44` = `monthly`) — the "settlement_cadence is a dropdown" tracker premise was already corrected in `design.md`. Verify the schema/seed reality before writing the ADR's "What changes" + Trade-offs, even though no code is touched this task.
- **Multi-criterion canon-adoption ADR = one BUILD/DEFER pair per criterion, kind-tagged individually (1.3/MVP-DEC-022).** A batch canon DEC (structured by sub-question) maps N BRs to N build-vs-deferred boundaries — write one numbered "What changes" block per criterion, each stating BUILD (the enforceable safety core, at an existing chokepoint) and DEFER (the named seam), then tag each with its taxonomy kind separately (they differ: Club-6 = tightening+field-collapse, Identity-6/Profile-5 = errata of omission, Producer-5 = interim safety lock). The unifying argument stays the same as the single-criterion ADRs: *none inverts a shipped rule of ours* because every target BR is absent from the frozen spec (the gap-not-wrong-rule tell = the bridged greenfield DEC being present, here DEC-069 `:545` + DEC-073 `:617`). Register/AC line-map extends: MVP-DEC-022 = `Register:147` (132 + 15, one row/line); AC-K-BR **Club-6=`:175`, Identity-6=`:146`, Producer-5=`:185`, Profile-5=`:164`** (NOT sequential — grep them, don't extrapolate the Agreement-1..4 `:192-195` run).
- **Value-set enum cast = a read-type change with a grep-wide blast radius (RM-08 landmine, FIRST confirmed on code in 2.1).** Casting a previously-`string` column to a backed enum (`ProducerAgreement.settlement_cadence` → `SettlementCadence`) ripples to EVERY reader, not just the migration: **(a) model-read assertions** (`->settlement_cadence)->toBe('quarterly')` → `->toBe(SettlementCadence::Quarterly)`); **(b) the event payload** — mirror the sibling enum field's `->value` (`status => $x->status->value`) so `settlement_cadence => $x->settlement_cadence?->value` keeps the wire token a string → the DB-round-tripped payload assertions stay green with NO test change; **(c) any seeder/factory literal now out-of-set throws `ValueError` AT THE PHP CAST LAYER on every engine, before the DB** (DemoSeeder `'annual'` HAD to migrate here — pre-satisfies 3.1's seeder sub-item). Grep three shapes: `->{col}` reads (change), `{col} =>` writes (check in-set), `->where('{col}',` DB queries (stay raw-string-keyed — DON'T change). **Filament `TextColumn`/`TextEntry` render a non-`HasLabel` BackedEnum state gracefully (its `->value`)** — the `ViewProducerAgreement` + `OperatorConsoleUiPassTest` render tests stayed green with NO resource change; the console Select is task 6.1's, not a cast consequence. Empirically verify render tests, don't speculatively touch the console.
- **PG value-set CHECK on a NULLABLE column** = the `col IS NULL OR col IN (...)` idiom (`2026_06_17_000001_add_compliance_to_parties`), derived from `Enum::cases()`; a separate additive migration (the column already exists), PG-gated `up()` + `down()` DROP `IF EXISTS`. Test it with the **`HoldSchemaTest` both-lanes pattern** (savepoint-wrapped raw insert: PG rejects by the declared constraint name / SQLite ACCEPTS the raw write — non-vacuous, never a bare skip). The per-iteration SQLite loop asserts the accept-lane; the PG reject-lane runs in 7.1's PG17 gate (mirror a pattern the baseline already runs green on PG17). Auto-name `<table>_<col>_check` — check it's under PG's 63-char limit (`parties_producer_agreements_settlement_cadence_check` = 52).
- **Additive NOT-NULL column on an existing table → a DB `default` is MANDATORY, not optional (first hit in 2.2, `auto_renew`/`auto_renew_default`).** Two independent forces each require it: (a) SQLite `ALTER TABLE ADD COLUMN … NOT NULL` is REJECTED without a non-NULL default — a DDL rule, independent of row count (even an empty table); (b) every un-migrated writer that omits the new column (factory, `DemoSeeder`, the `Create*` Action until its later wiring task) would violate NOT-NULL at insert → the whole suite reds. So a not-null additive column gets a DB default **even when the design frames it as "no default / set explicitly at creation"** — the create-table classifier idiom (`registration_flow_type`/`state` carry no default) does NOT transfer to `ALTER TABLE`. Pick a default a later explicit-writer task will simply override (here `true`, matching the source `auto_renew_default` + sibling `generates_credit`), so the floor stays harmless and needs NO drop-default follow-up migration (the spec mandates the inheritance, not the DB default's absence). Verify up/down on BOTH engines via a temp-DB `migrate`→`rollback`→`migrate` (SQLite, `DB_DATABASE=/tmp/x.sqlite`) + `migrate:fresh`→`rollback`→`migrate` (PG17 recipe), checking exit 0 — `RefreshDatabase` only exercises `up()`, never `down()`. Explicit-field event payloads (`ClubCreated`/`ProfileCreated` list columns, not `toArray()`) mean a net-new column does NOT leak into the wire format → no payload-assertion ripple (contrast the 2.1 cast blast-radius, which DID ripple the payload).
- **Localized guard exception = the `SeparationOfDutiesViolation` shape (2.4).** A `RuntimeException` subclass whose static named factories resolve `(string) __("parties.{group}.{key}", [...])` — 2+ factories sharing a placeholder shape use a private `build(string $key, …)` helper (SoD / `BelowMinimumRegistrationAge`); a single factory inlines the `__()` (`MissingClubProducer`). int for operator-facing `:producer`/`:club` ids, `string $state` for a `ClubStatus` token — the `MissingClubProducer`/`DuplicateProfileForClub` convention. **PII discipline: name the RULE, interpolate only non-PII (ids/state/config constants); never a DOB/email/derived age** (the age-gate factory receives ONLY `:min_age` so PII is structurally un-leakable). New keys go in the EXISTING lang group (`producer`/`club`/`producer_agreement`/`customer`), not a new group. **IT is genuinely authored when the task says "resolves in EN + IT"** (mirrors the bilingual `approval` group) — `PartiesApprovalCopyTest` enforces IT⊆EN parity (every it key needs an en counterpart), and a bulk `trans('parties',[],'it')` returns ONLY the it-file array (no per-key fallback merge), so partial-IT groups are fine.
- **PHPStan-max landmine on data-driven Pest tests (2.4): a loose `array $replace` closure param + `(string) $mixedElement` BOTH red** (`argument.type` — `trans()` wants `array<string,scalar|null>` — plus `cast.string`). Pest does NOT propagate a dataset's element types into the closure param. Fix = the `ClubCreditExceptionsTest` idiom: group keys by placeholder SHAPE, pass only the key string through `->with([...])`, build the replacement array INLINE as a literal (`['club'=>5566,'state'=>'sunset']`), assert literal token strings (never `(string) $value`). And a **bare `RuntimeException::class` in a Pest test is CORRECT** (global-namespace file → resolves to `\RuntimeException`; a `use` is redundant); instantiate each class explicitly, not `new $class('')` from a dataset (PHPStan flags dynamic-string `new`).
- **Value-domain reject ≠ business-rule guard — two distinct exception shapes; pick by what failed (3.1, RM-22).** A **value-domain reject** = an operand outside a closed set/format (a bad cadence token, a malformed country code) → the `InvalidAddressCountryCode` shape: a `RuntimeException` subclass, `forX(string $value): self` → `(string) __('parties.{group}.invalid_x', ['x' => $value])`, thrown at the ACTION boundary as pure input validation (BEFORE the transaction — fail-fast, no DB read needed), the offending value INTERPOLATED (a token/code is not PII). A **business-rule guard** = a state/relationship violation (Club not active, cross-shape conflict, SoD) → the `SeparationOfDutiesViolation` / 2.4-five shape, thrown INSIDE the txn (often after a DB read of the offending state). The tell in the delta spec: a scenario that NAMES an exception (`ProducerAgreementClubNotActive`) is a business-rule guard; one that just says "rejected server-side" with no named exception is a value-domain reject (author a fresh `Invalid*` exception THIS task — it wasn't in the 2.4 guard batch precisely because it's a different shape). Mechanically: resolve a `?string` operand to its enum via `Enum::tryFrom($x) ?? throw Invalid*::forX($x)` (PHPStan-max clean on 8.5, narrows `?Enum`→`Enum`; parenthesize the `??` inside a ternary), then pass the resolved enum instance to `create()` — the enum-cast `is_object`/`is_null` branches store `->value`/null, so no `->value` unwrap is needed. Grep every Action caller for out-of-set operands first (RM-08): a new value-set guard reds any caller/seeder/console-fill passing a now-invalid literal.
- **Column DROP (pre-launch) = EDIT the create-table migration in place + it is an ATOMIC sweep (first hit 2.3, `invite_only`; the inverse of 2.2's additive rule).** TWO facets. (1) **Where the drop lives:** an acceptance that greps `database/` empty for the column is ONLY satisfiable by removing the column's line from the CREATE-TABLE file — a separate `dropColumn` migration leaves the token in the create-table text, and a drop-migration + edited-create-table can't coexist (a fresh `migrate` would drop a never-created column). It's safe because pre-launch (Migration Plan: "no production data touched"; every run is `migrate:fresh`/`:memory:`), so the "never edit a released migration" convention doesn't bind an un-deployed one. A hot.md "next task" hint saying "new additive migration, never edit create-table" is the 2.2 ADDITIVE heuristic mis-transferred to a DROP — the task's OWN acceptance (a change artifact) overrides a NEXT-task prediction. Verify column-gone on BOTH engines with a temp-DB `migrate:fresh` + `Schema::hasColumn()`/`getColumnListing()` (RefreshDatabase only exercises `:memory:`). (2) **Atomic ripple:** the instant the column/model-cast is gone EVERY reference is invalid at once — `{Model}::factory()` with the column in `definition()` → SQL "no such column" across EVERY test using the factory (huge blast radius); a console page passing a named arg (`inviteOnly:`) to an Action that dropped the param → TypeError; `$read->{col}`/`payload['{col}']` → null-fail. So a drop CANNOT be split "core now, resources+tests later" and stay green — grep `{col}` across app/+database/+tests/ and cut them together (design R1's "sweep all N files"). Only genuinely-decoupled surfaces defer: a still-authored lang key + an i18n test that checks authored-ness/parity (NOT usage) stay green untouched → clean deferral to a later i18n task.

---

## [2026-07-07 15:05] — 1.1 Mini-ADR MVP-DEC-009 (Agreement-4: per-Club scope requires active Club)
- **Recovered a crashed iteration.** The prior instance hit the session limit (`.last-output`: *"You've hit your session limit · resets 3pm"*) mid-1.1: it had written the 71-line ADR but left it **untracked** — never committed, no `decisions/INDEX.md` row, no memory update. This iteration verified + completed it.
- **What was implemented:** completed task 1.1 — the ADR `decisions/2026-07-07-adopt-mvp-dec-009-agreement-club-active-scope.md` (already on disk, verified faithful) + the `decisions/INDEX.md` row (the missing acceptance criterion).
- **Verification (re-grounded, not assumed):**
  - Re-fetched LIVE canon read-only (`git -C ../documentation fetch cmless main`); `cmless/main` = `360df0b` — exactly the ADR's pin.
  - `git show cmless/main:…MVP_Decisions_Register…:134` confirms MVP-DEC-009 verbatim (new per-Club scope requires `active` Club; `sunset`/`closed` not selectable; Producer-wide ungated; supersession inherits scope + exempt; *"erratum of omission — editorial completion of DEC-070, No new DEC"*; ✅ 2026-06-18). Line map exact: `:132`=MVP-DEC-007, `:134`=MVP-DEC-009.
  - `…Module_K_Acceptance…:195` confirms AC-K-BR-Agreement-4's four paths verbatim (reject sunset, reject closed, admit active, admit since-sunset supersession). `:192`=Agreement-1.
  - Frozen spec cross-check: `grep` confirms **BR-K-Agreement-1/2/3 only** (Agreement-4 absent) → the "erratum of omission" framing is correct; greenfield `DEC-009:71` = *Crurated as Discovery supplier* (real, unrelated collision); `spec.lock` `source_sha 4f48277` matches.
- **Files changed:** `decisions/2026-07-07-adopt-mvp-dec-009-agreement-club-active-scope.md` (from prior iteration, now committed) · `decisions/INDEX.md` (+1 row, top of table) · `tasks.md` (1.1 → `[x]`) · `progress.md`.
- **Quality loop:** green — doc-only task (no runtime surface → no code test; the acceptance is ADR-present + INDEX-row). `openspec validate parties-module-k-br-guards --strict` → **valid**. No PHP touched → format/test/typecheck/lint not triggered by this task.
- **Learnings for future iterations:**
  - The ADR body is a solid template for 1.2 (MVP-DEC-010) + 1.3 (MVP-DEC-022) — same collision-banner + LIVE-canon-References structure. Grounding recipe + line map now in Codebase Patterns above.
  - When recovering a crashed iteration: check `git status` for untracked artifacts BEFORE re-doing work; the file may already exist and only need verification + the missing sidecar updates.
  - Two orthogonal ProducerAgreement guards live in this change and must not be conflated: Agreement-4 (1.1/3.2) = **creation-time** Club-active; RM-20 (3.3) = **activation-time** cross-shape mutual-exclusion. Different chokepoints, different exceptions.
---

## [2026-07-07 15:14] — 1.2 Mini-ADR MVP-DEC-010 (settlement-cadence closed set)
- **What was implemented:** the ADR `decisions/2026-07-07-adopt-mvp-dec-010-settlement-cadence-closed-set.md` (RM-22) + the `decisions/INDEX.md` row (top-of-table, above the sibling 009 row). Doc-only — no code touched (the enum/CHECK/validation land in tasks 2.1 + 3.1).
- **Grounding (LIVE-sourced, line-verified against the recipe line-map):**
  - Re-fetched LIVE canon read-only (`git -C ../documentation fetch cmless main`); `cmless/main` = `360df0b` — the ADR's pin.
  - `Register:135` = **MVP-DEC-010** verbatim (closed to 3 — `quarterly`/`monthly`/`semi-annual`, BMD §3.10; `annual`/sub-monthly named nowhere; **API + DB, not UI-only** because it times Module-E settlement + Module-D PO issuance / D19 seam; extensible post-launch; representation dev's call DEC-073; **DEC-042 framework unchanged — closes the "e.g." set as an MVP-scoping delta**; **No new DEC — bridges DEC-042**; ✅ 2026-06-18). Line map exact (`:132`=MVP-DEC-007, `:135`=MVP-DEC-010).
  - `Acceptance:193` = **AC-K-BR-Agreement-2** verbatim (closed set + server-side + negative path `weekly`/typo `quaterly` → rejected *at the API/DB layer*). Line map exact (`:193`; the frozen open version is at `spec/…:186`).
  - **Code reality (cited from source, not the tracker):** `settlement_cadence` = free-text `->string()->nullable()` (`…producer_agreements_table.php:59`), model `@property string|null` no cast (`ProducerAgreement.php:36`); `DemoSeeder.php:296` = the out-of-set `'annual'` `sanguido` row (→ `semi-annual`, task 3.1); `ProducerAgreementFactory.php:44` = default `'monthly'` (in-set, unchanged).
  - Number collision: greenfield **DEC-010** = *12.5% club margin / 5% Discovery revenue-share* (`spec/…decisions.md:76`) — real, unrelated → bannered. Bridged framework **DEC-042** (`:291`) IS present in our frozen spec (the tell it's a tightening, not an inversion). `spec.lock` `source_sha 4f48277` confirmed (stops at MVP-DEC-007).
- **Files changed:** `decisions/2026-07-07-adopt-mvp-dec-010-settlement-cadence-closed-set.md` (new) · `decisions/INDEX.md` (+1 row) · `tasks.md` (1.2 → `[x]`) · `progress.md` (this block + 2 new Codebase Patterns).
- **Quality loop:** green — doc-only (no runtime surface → no code test; acceptance = ADR-present + INDEX-row + excluded-values/seeder-change explicit, all met). `openspec validate parties-module-k-br-guards --strict` → **valid**. No PHP touched → format/test/typecheck/lint not triggered.
- **Acceptance walk:** ADR has `status: active` frontmatter + Decision/Context/Alternatives/Reasoning/Trade-offs/References (grep-confirmed); INDEX row present; the excluded values (`annual`/sub-monthly/`weekly`/typo) AND the DemoSeeder `annual → semi-annual` migration are explicit in the ADR (What-changes #3 + Trade-offs) + the INDEX row.
- **Learnings for future iterations:**
  - The ADR body remains a solid template for 1.3 (MVP-DEC-022). But 1.3 is a *4-criterion batch* (Club-6 + Identity-6 + Profile-5 + Producer-5) and a *mix of kinds* (see the new "canon-adoption kind taxonomy" pattern) — record each criterion's build-vs-deferred split (task 1.3 acceptance names them: Producer-5 interim lock / re-arm deferred; Profile-5 K-side inherit / self-toggle deferred; Identity-6 null-DOB → block).
  - 010 vs 009: both "our spec carries the gap, not a wrong rule", but different *mechanism* — 009 adds a missing BR, 010 tightens an existing BR's value set. Say which, so a reviewer sees why the blast radius is a single negative test, not a shipped-test inversion.
---

## [2026-07-07 15:23] — 1.3 Mini-ADR MVP-DEC-022 (Club-6 + Identity-6 + Profile-5 + Producer-5)
- **What was implemented:** the last of the three canon mini-ADRs — `decisions/2026-07-07-adopt-mvp-dec-022-club-membership-governance.md` (RM-23 batch, CML-89) + the `decisions/INDEX.md` row (top-of-table, above the sibling 010/009 rows). Doc-only — no code touched (the guards land in tasks 2.x–5.x).
- **Grounding (LIVE-sourced, line-verified):**
  - Re-fetched LIVE canon read-only (`git -C ../documentation fetch cmless main`); `cmless/main` = `360df0b` — the ADR's pin.
  - `Register:147` = **MVP-DEC-022** verbatim (one canon DEC structured by 7 sub-questions; the 4 in scope: sub-2 Club-6, sub-4 Producer-5, sub-5b Identity-6, sub-7 Profile-5; *"No new DEC — bridges DEC-069/073 + MVP-DEC-013/014/019 + BMD §2.4/§2.8"*; ✅ 2026-06-24). Line-map exact (132+15).
  - AC-K-BR criteria verbatim (**grepped, not extrapolated** — non-sequential): **Club-6 `:175`** (`open` latent, `invite_only` subsumed, no auto-approve), **Identity-6 `:146`** (under-min DOB → no Customer; min-age configurable default 18), **Profile-5 `:164`** (inherit `auto_renew` + customer self-toggle + operator set), **Producer-5 `:185`** (edit re-arms Creator→Reviewer→Approver). AC-K-J-10 `:89` cross-ref.
  - **Frozen-spec gap confirmed:** grep proves the frozen `Module_K_Acceptance` carries Club-1..5 / Identity-1..5 / Producer-1..4 / Profile-1..4 — **all four target BRs absent**. Bridged greenfield DEC-069 `:545` + DEC-073 `:617` **present** (the completeness-not-inversion tell). `spec.lock source_sha 4f48277` / `source_date 2026-06-16` predates MVP-DEC-022's 2026-06-24.
  - **Code reality cited from source:** `ClubRegistrationFlowType` = 4 cases (`OpenRegistration='open_registration'` → latent + 3 launch); `invite_only` = separate bool column `->default(false)` (migration `:66`, model `:39/:93`, `ClubCreated` payload `:48`, factory `:46`, seeder `:273`, CreateClub `:72`); `Customer.date_of_birth` = `CarbonImmutable|null` `immutable_date` (`:71/:182`), nullable → no gate; Producer `name`/`region`/`description`/`website` present, `ProducerStatus` = Draft/Active/Retired (**no `reviewed` state** — the re-arm has no home, confirms D9); Profile `auto_renew` / Club `auto_renew_default` net-new.
  - Number collision bannered (greenfield **DEC-022** `:148` = *NFC/NFT recovery policies*, unrelated).
- **Files changed:** `decisions/2026-07-07-adopt-mvp-dec-022-club-membership-governance.md` (new, ~95 lines) · `decisions/INDEX.md` (+1 row) · `tasks.md` (1.3 → `[x]`) · `progress.md` (this block + 1 new Codebase Pattern).
- **Quality loop:** green — doc-only (no runtime surface → no code test; acceptance = ADR-present + INDEX-row + each of the 4 criteria + its build-vs-deferred split named). `openspec validate parties-module-k-br-guards --strict` → **valid**. No PHP touched → format/test/typecheck/lint not triggered.
- **Acceptance walk:** ADR has `status: active` frontmatter + Decision/Context/Alternatives/Reasoning/Trade-offs/References (grep-confirmed 6/6); each of Club-6/Identity-6/Profile-5/Producer-5 has an explicit **BUILD** + **DEFER** block (grep 8 split-markers) + an individual kind-tag; INDEX row present.
- **Learnings for future iterations:**
  - **The 3 canon mini-ADRs are DONE (1.1/1.2/1.3 = 3/23).** §2 onward is code. NEXT = task **2.1** (`SettlementCadence` enum + cast + CHECK regen) — the first PHP task; the doc-only "no test triggered" shortcut ends here, the full Quality Loop (format→test→phpstan→pint) applies from 2.1.
  - The 1.3 ADR records each criterion's deferred seam by name (Producer re-arm; Profile customer self-toggle; Identity-6 per-jurisdiction floors + document-verification) — tasks 4.2/5.1/5.2 must ship the BUILD half only and cite these deferrals, not silently drop or over-build them.
  - Producer-5 interim is **stricter than canon** (block-the-edit vs re-arm-review) — the delta spec `:74` says the lock is *replaced* when the edit path lands. A future reviewer/iteration must not read the lock as the permanent Producer-5 behaviour.
---

## [2026-07-07 15:47] — 2.1 SettlementCadence enum + cast + PG CHECK (first PHP task)
- **What was implemented:** new `App\Modules\Parties\Enums\SettlementCadence` (3 cases `quarterly`(default)/`monthly`/`semi_annual` + a `default(): self` → Quarterly, mirroring `HoldType`'s small-method idiom) + cast `ProducerAgreement.settlement_cadence` to it (`@property SettlementCadence|null` + casts() row + docblock) + new additive migration `2026_07_07_000001_add_settlement_cadence_check_to_parties_producer_agreements.php` adding the PG-only nullable value-set CHECK derived from `cases()` (`col IS NULL OR col IN (...)`, SQLite-skipped).
- **Ripple handled (the cast is a read-type change — see the new Codebase Pattern):** event `ProducerAgreementCreated` payload → `settlement_cadence?->value` (mirror `status->value`, keeps the wire token a string); DemoSeeder `'annual' → 'semi_annual'` (MANDATORY — `create()` `ValueError`s on `'annual'` at the cast layer; pre-satisfies 3.1's seeder sub-item, see the ℹ note under 3.1); factory `'monthly'` → `SettlementCadence::Monthly` (+ import, consistency with the sibling `status` enum + fixes the stale "free string" comment); 2 model-read assertions (`ProducerAgreementTest:50`, `ProducerAgreementCreateConsoleTest:66`) → the enum. **Console UNCHANGED** — Filament renders the enum state gracefully (empirically confirmed via the `ViewProducerAgreement`/UI-pass render tests); the Select is task 6.1's.
- **Tests:** new `SettlementCadenceEnumsTest` (count 3 + verbatim/ordered values + `default()`=Quarterly + `from()` round-trip + reject `annual`/`weekly`/`quaterly`-typo); new `ProducerAgreementSchemaTest` (`HoldSchemaTest` both-lanes CHECK — PG rejects `annual` by constraint name / SQLite accepts, + all 3 valid tokens + NULL insert on both engines).
- **Files changed (11):** enum (new) · model · event · migration (new) · seeder · factory · `ProducerAgreementTest` · `ProducerAgreementCreateConsoleTest` · `SettlementCadenceEnumsTest` (new) · `ProducerAgreementSchemaTest` (new) · tasks.md.
- **Quality loop: green.** focused 32/32 → full suite **1980/1980** (baseline 1975 + 5 new cases) on SQLite · PHPStan max **0** · Pint clean · `openspec validate --strict` valid. Runtime sanity: `count=3 values=quarterly,monthly,semi_annual default=quarterly`. (PG CHECK reject-lane runs in 7.1's PG17 gate — mirrored the HoldSchemaTest pattern the baseline runs green on PG17.)
- **Pint trap avoided (design R4, here same-module):** the enum docblock's `{@see \App\...\ProducerAgreement}` got auto-imported by `fully_qualified_strict_types` into a circular same-module `use` — switched to backticked prose (the `HoldType` enum-docblock convention). Harmless here (same module) but inelegant; the rule generalizes: **enum docblocks name related classes in backticks, never `{@see FQCN}`.**
- **Learnings for future iterations:**
  - Everything durable is in the two new Codebase Patterns (cast blast-radius + nullable-PG-CHECK). The doc-only "no test triggered" shortcut is over; every §2+ task runs the full loop.
  - NEXT = **2.2** — pure additive-column migration (`parties_profiles.auto_renew` bool not-null + `parties_clubs.auto_renew_default` bool not-null default `true`) + `@property`/casts. NO cast-ripple (net-new columns, no existing readers) — mirror `2026_06_19_000001_add_lifecycle_columns_to_parties_profiles` / the `anonymised_at` additive migration. Watch: `auto_renew` is not-null with NO default → any existing Profile-creating caller/factory/seeder must supply it or the migration needs a backfill; grep `parties_profiles` inserts (factory + DemoSeeder + `CreateProfile`) before adding the not-null column.
---

## [2026-07-07 16:06] — 2.2 auto_renew + auto_renew_default columns (additive schema)
- **What was implemented:** one additive migration `2026_07_07_000002_add_auto_renew_preference_to_parties_profiles_and_clubs.php` adding `parties_clubs.auto_renew_default` (bool NOT-NULL, default `true`) + `parties_profiles.auto_renew` (bool NOT-NULL, default `true`) + the `@property bool` + `boolean` casts on both models (Profile-5 K-side schema; design D8; ADR MVP-DEC-022). SCHEMA only — the CreateProfile inheritance BEHAVIOUR is task 4.2.
- **The `auto_renew` "no default" decision, resolved (the task-list's flagged trap):** the task/`hot.md` framed `auto_renew` as "not-null with NO default" (contrast `auto_renew_default`'s explicit default). But a NOT-NULL additive column MUST carry a DB default — see the new Codebase Pattern: (a) SQLite `ADD COLUMN … NOT NULL` is rejected without one; (b) every Profile writer omits it (ProfileFactory L34, `CreateProfile.php:78` insert, DemoSeeder `Profile::create` L435) → not-null-no-default reds the suite. Chose default `true` (matches the source `auto_renew_default` + sibling `generates_credit`); task 4.2 layers CreateProfile's explicit Club-inheritance on top, the default stays a harmless floor (no drop-default migration; the spec mandates inheritance, not the DB default's absence). Documented the reconciliation in the migration docblock + a tasks.md ℹ note + the Profile/Club class docblocks.
- **Blast radius = zero writer changes (RM-08 check clean):** the `true` default covers every column-omitting insert (Club/Profile factories, DemoSeeder L266/L435, `CreateClub`/`CreateProfile`). Event payloads are explicit-field (`ClubCreated`/`ProfileCreated` list columns, not `toArray()`) → new columns don't leak into the wire format → no payload-assertion ripple. No `$fillable` to update (`$guarded = []` on both). Console renders explicit fields → no auto-surfacing.
- **Files changed (5):** migration (new) · `Profile.php` (@property + cast + docblock) · `Club.php` (@property + cast + docblock) · `AutoRenewColumnsTest.php` (new, mirrors `ProfileLifecycleColumnsTest`) · `tasks.md`.
- **Quality loop: green — verified on BOTH engines.** SQLite: focused 4/4 → full suite **1984/1984** (1980 baseline + 4 new) · PHPStan max **0** · Pint clean · `openspec validate --strict` valid. Up/down: SQLite temp-DB `migrate`→`rollback`→`migrate` all exit 0 (`000002` DONE on both ups); PG17 recipe `migrate:fresh`→`rollback`→`migrate` all exit 0 + the focused test passes on PG17 (4/4) — proving up() + casts + columns + the NOT-NULL-default floor land on PostgreSQL 17.
- **Learnings for future iterations:**
  - The durable insight is the new "Additive NOT-NULL column → DB default MANDATORY" Codebase Pattern (SQLite DDL + un-wired-writer, twin forcing functions; verify up/down on both engines because RefreshDatabase only runs `up()`).
  - NEXT = **2.3** — DROP `parties_clubs.invite_only` + remove it from `Club` model, `CreateClub`, and the `ClubCreated` payload. This is a REMOVAL-with-ripple (the mirror of an additive column): `ClubCreated::payload()` currently carries `'invite_only' => $club->invite_only` (event L—) + the `ClubTest` payload assertion (`ClubTest.php:108` `$event->payload['invite_only']`) + factory L46 + DemoSeeder L273 + model @property/cast. Grep `invite_only` across `app/ database/ tests/` before cutting; task 2.3's acceptance scopes app/+database (lang/tests handled in 4.3/6.4). The `2026_06_15_000003` create-table migration is `spec/`-frozen? NO — it's our migration; but DROP via a NEW additive migration (never edit the create-table), mirroring this task's additive shape in reverse.
---

## [2026-07-07 16:30] — 2.3 Drop parties_clubs.invite_only (Club-6 collapse, atomic sweep)
- **What was implemented:** dropped the redundant `parties_clubs.invite_only` boolean (Club-6 / MVP-DEC-022: `invite_only=true` ⟺ `registration_flow_type=invitation_only`, so the flag is subsumed — ADR `2026-07-07-adopt-mvp-dec-022`, design D6). Removed every app/+database/ reference in ONE commit (atomic ripple — new Codebase Pattern).
- **DROP mechanism (the key call, contra hot.md):** EDITED the `2026_06_15_000003_create_parties_clubs_table.php` create-table in place (removed the column line + fixed the docblock/inline comment to singular), NOT a new drop-migration. The acceptance `grep database/ empty` is only satisfiable by removing the column from the create-table file, and it's pre-launch (no deployed DB; every run is `migrate:fresh`). The hot.md "new additive migration, never edit create-table" hint was the 2.2 additive heuristic mis-applied to a DROP — overridden by the task's own acceptance criteria. Design R1 confirms: "one migration drops the column; sweep all 16 files."
- **Files changed (14):** app — `Club` model (@property/cast/docblock), `CreateClub` action (param/use/insert), `ClubCreated` event (payload), `ClubResource` (Toggle + IconEntry + 2 docblocks), `CreateClub` page (var/is_bool/named-arg/docblock), `ClubsRelationManager` (Toggle/var/is_bool/named-arg); database — create-table migration, `ClubFactory` (L46), `DemoSeeder` (10 row tuples + destructuring + insert); tests — `ClubTest`, `ClubCreateConsoleTest`, `ClubConsoleChainTest`, `ClubResourceTest`; docs — `CONTEXT.md` (ClubCreated payload row + Club glossary prose).
- **Deferred to 6.4 (deliberately, stay green untouched):** lang EN/IT `fields.invite_only` key + `ClubConsoleI18nTest` L48/L62. The i18n scanner checks authored-ness + en/it parity + source-hardcoding (NOT key-usage), so a present-but-unused key stays green — 2.3's grep scope is app/+database/ ONLY (6.4 adds lang/). tasks.md ℹ notes added to 4.3 + 6.4 recording what 2.3 pre-did.
- **Quality loop: green on BOTH engines.** SQLite focused Club tests 60/61 (the 1 fail = the shared `scanOperatorConsoleHardcodedSinks` helper undefined when `ClubConsoleI18nTest` runs WITHOUT `ProductMasterConsoleI18nTest` — a subset-isolation artifact, green in the full run) → full suite **1984/1984** · PHPStan max **0** · Pint clean · `openspec validate --strict` valid. Column-gone verified on SQLite + PG17 (temp-DB `migrate:fresh` + `Schema::hasColumn`/`getColumnListing`: `invite_only` absent, all other cols incl. `auto_renew_default` intact).
- **Learnings for future iterations:**
  - Durable insight = the new "Column DROP (pre-launch)" Codebase Pattern (edit create-table in place + atomic ripple). It is the mirror of the 2.2 "Additive NOT-NULL column" pattern.
  - 4.3/6.4's invite_only-sweep sub-items are now PRE-SATISFIED except the two lang/i18n items. 4.3's real remaining work = the Club-6 registration-flow logic (OpenRegistration non-selectable + no-auto-approve); 6.4's = drop the lang keys + the registration-flow select. Neither is touched by 2.3.
  - Trap avoided: running a SUBSET of the OperatorPanel i18n tests fails on the shared `scanOperatorConsoleHardcodedSinks` helper (declared in `ProductMasterConsoleI18nTest`) — NOT a regression; confirm i18n-test failures against the FULL suite before diagnosing.
  - NEXT = **2.4** — five localized guard exceptions (EN+IT in `lang/{en,it}/parties.php`), each a `RuntimeException` subclass with static named factories (`SeparationOfDutiesViolation` shape): `ProducerAgreementScopeConflict`, `ProducerAgreementClubNotActive`, `ClubNotAcceptingMemberships`, `BelowMinimumRegistrationAge`, `ProducerReviewGovernedContentLocked`. Per-exception unit tests assert `toBeInstanceOf(RuntimeException::class)` (bare, no `use` — global-namespace trap) + the localized message resolves in EN + IT.
---

## [2026-07-07 17:32] — 2.4 Five localized BR-guard exceptions (definitions only, no wiring)
- **What was implemented:** the five `RuntimeException` subclasses the §3–5 guards will throw, as PURE definitions (factory + copy + tests; NO Action wiring). Each mirrors the `SeparationOfDutiesViolation` shape (localized `(string) __()` reason; 2-factory classes use a private `build()` helper, 1-factory classes inline like `MissingClubProducer`):
  - `ProducerAgreementScopeConflict` (RM-20, task 3.3) — 2 direction-aware factories `producerWideBlockedByClubScope(int)` / `clubScopeBlockedByProducerWide(int)` → `producer_agreement.scope_conflict_{producer_wide,club_scope}`.
  - `ProducerAgreementClubNotActive` (Agreement-4, task 3.2) — `forClub(int, string $state)` → `producer_agreement.club_not_active`.
  - `ClubNotAcceptingMemberships` (RM-21, task 4.1) — `forClub(int, string $state)` → `club.not_accepting_memberships`.
  - `BelowMinimumRegistrationAge` (Identity-6, task 5.1) — 2 factories `belowMinimum(int)` / `missingDateOfBirth(int)` → `customer.{below_minimum_registration_age,missing_date_of_birth}`.
  - `ProducerReviewGovernedContentLocked` (Producer-5, task 5.2) — `whileActive(int)` → `producer.review_governed_content_locked`.
- **Copy placement:** keys added to the EXISTING lang groups (not new groups) — `producer` / `club` / `producer_agreement` / `customer`. int ids for `:producer`/`:club` (the `MissingClubProducer`/`DuplicateProfileForClub` convention), `string $state` for the offending `ClubStatus` token.
- **IT is GENUINELY authored (mirrors the bilingual `approval` group, not the EN-only `compliance_review` fallback).** The task acceptance says "resolves in EN and IT"; `hot.md` said "EN+IT". So `lang/it/parties.php` grew from 1 group (`approval`) to 5 — added `producer`/`club`/`producer_agreement`/`customer` with ONLY the new keys (product-domain terms Producer/Club/ProducerAgreement/Profile stay English per Crurated convention). `PartiesApprovalCopyTest`'s IT⊆EN parity holds (every new IT key has an EN counterpart; a bulk `trans('parties',[],'it')` returns ONLY the it-file array, no per-key fallback merge).
- **PII discipline:** the age-gate copy interpolates ONLY `:min_age` (a public platform constant, NOT PII) — the DOB and derived age are structurally un-leakable (the factory receives only the constant), the `duplicate_email`/`gate_not_met` discipline. All others name the rule + an operator-facing id/state.
- **Files changed (9):** 5 new exceptions · `lang/en/parties.php` (+7 keys) · `lang/it/parties.php` (+4 groups/7 keys + header) · new `BrGuardExceptionsTest` · `tasks.md`.
- **Quality loop: green.** focused 20/20 (81 assertions) → full suite **2004/2004** (1984 baseline + 20 new) · PHPStan max **0** · Pint clean · `openspec validate --strict` valid. Copy-parity `PartiesApprovalCopyTest` + all Parties exception tests 117/117.
- **Learnings for future iterations:**
  - **PHPStan-max landmine on Pest datasets: a `array $replace` closure param + `(string) $value` on a `mixed` element BOTH fail (`argument.type` for `trans()`'s `array<string,scalar>` + `cast.string`).** Pest does NOT propagate a dataset's element types into the closure param — it stays loose `array`. Fix = the `ClubCreditExceptionsTest` idiom: group keys by placeholder SHAPE, pass only the key string through `->with()`, and build the replacement array INLINE as a literal (`['club' => 5566, 'state' => 'sunset']` → `array<string,int|string>`, exactly the `trans()` contract). Assert literal token strings, never `(string) $value`.
  - **The bare `RuntimeException::class` in a Pest test is CORRECT, not a lint miss** (a Pest file is global-namespace, so the token already resolves to `\RuntimeException`; a `use RuntimeException;` would be redundant). Instantiate each class explicitly (`new X('')`) — NOT `new $class('')` from a dataset (PHPStan flags dynamic-string instantiation).
  - The five factory NAMES + signatures are now fixed API — the wiring tasks (3.2/3.3/4.1/5.1/5.2) must call these verbatim (recorded in the tasks.md 2.4 ℹ note). NEXT = **3.1** (RM-22 server-side out-of-set cadence reject in `CreateProducerAgreement`; the DemoSeeder `annual→semi_annual` + cast round-trip were pre-done in 2.1 — 3.1 is ONLY the reject guard, throwing a clean localized reject before the `SettlementCadence::from()` `ValueError`).
---

## [2026-07-07 17:47] — 3.1 RM-22 server-side cadence closed-set reject in CreateProducerAgreement
- **What was implemented:** the server-side out-of-set/typo settlement-cadence rejection guard in the `CreateProducerAgreement` action (RM-22 / BR-K-Agreement-2 / canon MVP-DEC-010). Resolve the free-text `?string $settlementCadence` operand against the closed `SettlementCadence` set at the action boundary — a null operand stays null (cadence optional, column nullable), a non-null out-of-set token is rejected with a new localized `InvalidSettlementCadence` BEFORE the write (ahead of the raw `ValueError` the enum cast throws on `create()`). The 2.1 pre-work (DemoSeeder `annual→semi_annual` + the cast round-trip) was NOT re-touched — 3.1 is ONLY the reject guard.
- **The reject uses a VALUE-DOMAIN exception, not one of the 2.4 five** (which are business-rule guards for 3.2–5.2). A cadence out-of-set is a value-domain/format reject — the `InvalidAddressCountryCode` shape, NOT the SoD/guard shape. This is exactly why RM-22 was excluded from the 2.4 exception batch (and why the delta spec's cadence scenario, unlike the Club-active one, names NO exception). New `InvalidSettlementCadence extends RuntimeException` with `forCadence(string): self` → `(string) __('parties.producer_agreement.invalid_settlement_cadence', ['cadence' => …])`; the offending token IS interpolated (a cadence token is NOT PII — the `:country`/`:producer`/`:club` id discipline).
- **Guard placement = BEFORE the transaction** (pure input validation, fail-fast — the `CreateCustomerAddress` precedent), not inside it like the Producer-existence check (that's a DB read needing txn consistency). Resolved `?SettlementCadence` threaded into the closure `use` + passed to `create()` (the enum-cast `is_object`/`is_null` branches store `->value`/null — passing the enum instance is correct, no string needed).
- **Copy:** EN + IT `producer_agreement.invalid_settlement_cadence` (IT genuinely authored, IT⊆EN parity holds). The three accepted tokens listed are the enum BACKING values (`quarterly`/`monthly`/`semi_annual`) — actionable for the operator/API caller who supplied a bad token (label form is `semi-annual`); precise over the spec's prose hyphen-form to avoid a copy-back footgun.
- **Blast radius = zero existing callers broke (RM-08 grep clean):** every `CreateProducerAgreement` action caller passes an in-set (`monthly`/`quarterly`) or null cadence — `SpineCreationChainTest`, `SupplyLifecycleChainTest` (both `monthly`), the console `createViaAction()` + `ProducerAgreementsRelationManager` (free string, empty→null), and the console tests (`ProducerAgreementCreateConsoleTest` fills `monthly`/`quarterly`, `ProducerAgreementConsoleChainTest` `monthly`/`quarterly`). The remaining `'annual'` literals are reject-tests only (`SettlementCadenceEnumsTest:42`, `ProducerAgreementSchemaTest:32`) — correct, untouched.
- **Files changed (6):** `InvalidSettlementCadence` (new exception) · `CreateProducerAgreement` (import + 2-guard docblock + resolve-or-throw) · `lang/en/parties.php` (+1 key) · `lang/it/parties.php` (+1 key + header) · `ProducerAgreementTest` (+2 `it()` = +4 cases) · `tasks.md`.
- **Quality loop: green.** focused `ProducerAgreementTest` 12/12 (58 assertions) → full suite **2008/2008** (2004 baseline + 4 new cases) on SQLite · PHPStan max **0** · Pint clean · `openspec validate --strict` valid · DemoSeeder SQLite smoke exit 0 (`semi_annual` seeds clean). **No PG17 run needed** — 3.1 touches NO DB (no migration, no new SQL; the reject is pre-DB via `tryFrom()`); the CHECK + seeder PG17 verification is 2.1's (`ProducerAgreementSchemaTest` both-lanes).
- **Learnings for future iterations:**
  - Durable insight in the new Codebase Pattern below (value-domain reject vs business-rule guard). NEXT = **3.2** (Agreement-4 Club-active scoping guard in the SAME `CreateProducerAgreement` path → `ProducerAgreementClubNotActive::forClub(int, string $state)` from 2.4). 3.2 adds a THIRD guard to this action's docblock ("Two guards" → "Three") + needs a DB read (the target Club's status), so it likely lives INSIDE the transaction next to the Producer-existence check, not before it like the pure-input cadence guard. Producer-wide (`club_id` NULL) ungated; supersession-inherits-scope exemption is 3.3's.
  - `throw` as an expression inside `??` (`tryFrom($x) ?? throw …::forCadence($x)`) is PHPStan-max clean on PHP 8.5 and narrows `?Enum` → `Enum`. Wrap the `??` in parens inside a ternary for reader clarity (`$x === null ? null : (tryFrom … ?? throw …)`).
---
