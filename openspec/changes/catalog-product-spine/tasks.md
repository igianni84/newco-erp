# Tasks — catalog-product-spine

> Read `design.md` (D1–D9) and the ADR `decisions/2026-06-14-catalog-category-neutral-representation.md` before any task. **Standing rules per task:** the five Quality Commands green at the end (format · test_filter · test · type_check · lint), `openspec validate catalog-product-spine --strict` green, **no protected files touched**, no new Composer dependency (`git diff main -- composer.json composer.lock` stays empty). **Verify every framework/vendor API in `vendor/` before writing it** (lessons.md). **Every DB-touching task is verified on a real PostgreSQL 17 before it is marked done** — SQLite-green is necessary, never sufficient (`knowledge/testing/rules.md`, the five portability traps): driver-guard enum `CHECK` constraints (`DB::getDriverName()==='pgsql'`, mirror the `domain_events` migration); assert `TranslatableText`/json columns by key or through the cast (never byte-compare jsonb); named test doubles only for persisted values. **Boundary law:** `producer_id` is a plain column — never a DB foreign key, never an Eloquent relation, never a Parties import (the arch test `tests/Architecture/ModuleBoundariesTest.php` must stay green with no amendment). **Scope guard:** NO lifecycle transition, NO approval workflow, NO `*Activated`/`*Retired` event — entities are born `draft` and stay there (deferred to `catalog-lifecycle-approval`).

## 1. Catalog enums (prerequisite)

- [x] 1.1 `App\Modules\Catalog\Enums\ProductType` (backed string enum, sole case `Wine = 'wine'`) and `App\Modules\Catalog\Enums\LifecycleState` (backed string enum, cases `Draft='draft'`, `Reviewed='reviewed'`, `Active='active'`, `Retired='retired'`) — design D2/D3. No DB.
  - Acceptance: `ProductType` has exactly the one `Wine` case; `LifecycleState` has exactly the four cases with those backing values; both type-clean at PHPStan max; quality commands green.
  - Test hint: `tests/Unit/Modules/Catalog/Enums/EnumsTest.php` — assert the case/value sets verbatim with `toBe` (order-sensitive), mirroring the platform `EnumsTest` style; assert `ProductType::cases()` is length 1 (the WINE-only-at-launch guard, AC-0-XM-9) and `LifecycleState::cases()` length 4.

## 2. Reference-data entities (no parent)

- [x] 2.1 **Format** (design D5) — migration `catalog_formats` (bigint id; `name`; physical-measure columns e.g. `size_label`/`unit`; `lifecycle_state` string + driver-guarded `CHECK` + cast, default `draft`; audit/version), model `App\Modules\Catalog\Models\Format`, factory `Database\Factories\Catalog\FormatFactory`, the `App\Modules\Catalog\Events\FormatCreated` event class (establish the one-class-per-event `Events/` convention — design D7), and a `CreateFormat` action (design D8) that inserts + records `FormatCreated` in one `DB::transaction`.
  - Acceptance: a Format is created in `draft`; a `FormatCreated` domain event is recorded in the same transaction (module `catalog`, entityType `Format`, PII-free payload); spec scenario "Create a Format and reference it" (creation half) passes; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Catalog/FormatTest.php` with `uses(RefreshDatabase::class)` — `CreateFormat` then assert `lifecycle_state === LifecycleState::Draft`; query `domain_events` for one `FormatCreated` row tagged `catalog` (read the payload through the model's array cast, assert by key — trap 3). Wine-display note: Format keeps its name in the cascade (§18).

- [x] 2.2 **Case Configuration** (design D5) — migration `catalog_case_configurations` (bigint id; `name`; `units_per_case`; `packaging_type`; `lifecycle_state`; audit/version) with **NO breakability column**, model `CaseConfiguration`, factory, `CaseConfigurationCreated` event, `CreateCaseConfiguration` action (transactional + event).
  - Acceptance: a Case Configuration is created in `draft` and records `CaseConfigurationCreated`; the schema carries no breakability attribute (spec scenario "Case Configuration has no breakability flag"); quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Catalog/CaseConfigurationTest.php` — assert creation + event; assert the table has no `breakable`/`breakability`-named column (`Schema::hasColumn` is false) — the §7-stays-downstream guard (AC-0-BR-RefData-2).

## 3. Product hierarchy (core + per-type WINE attributes)

- [x] 3.1 **Product Master** (design D1/D4/D6) — migrations `catalog_product_masters` (neutral core: `name`, `product_type` string + driver-guarded `CHECK` + `ProductType` cast, `producer_id` plain `unsignedBigInteger` **no FK**, `lifecycle_state`, audit/version) + `catalog_product_master_wine_attributes` (1:1; `product_master_id` FK within module; `appellation`; `region`; `winery_story` json via `TranslatableTextCast`); models `ProductMaster` (within-module `hasOne` wineAttributes) + `ProductMasterWineAttributes`; factory; `ProductMasterCreated` event; `CreateProductMaster` action that, in one transaction, runs the **dedup check** (non-retired collision on `producer_id + name + appellation` → reject with a localized reason) then inserts core + wine attrs and records `ProductMasterCreated`.
  - Acceptance: creating a `WINE` master persists the neutral core + appellation/region/winery-story in the per-type table, born `draft`, producer captured as a bare id (no relation); a duplicate `producer_id + name + appellation` (non-retired) is rejected; two distinct identity tuples both succeed; `ProductMasterCreated` recorded PII-free (producer by id only); a non-`WINE` `product_type` is rejected (fail-closed); spec scenarios "Create a WINE Product Master", "Duplicate identity key is rejected", "WINE is the only launch Product Type", "The neutral core holds no wine-specific attribute" pass; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Catalog/ProductMasterTest.php` — assert `Schema::hasColumn('catalog_product_masters','appellation')` is **false** (it lives on the wine table — the neutral-core guard, AC-0-GEN-2); resolve the winery-story `TranslatableText` in `it` and assert English fallback (trap 3: assert through the cast); dedup negative path creates the same tuple twice and asserts the second throws a domain exception (use real values; the producer is a plain int). Verify the Laravel 13 two-source `CHECK`/enum-cast idiom in vendor first.

- [x] 3.2 **Product Variant** (design D1/D5) — migrations `catalog_product_variants` (neutral: `product_master_id` FK within module, `variant_identifier` type-neutral, `lifecycle_state`, audit/version) + `catalog_product_variant_wine_attributes` (1:1; `vintage_year` nullable int, `non_vintage` bool, `tasting_notes` json `TranslatableTextCast`); models `ProductVariant` (+ wine attrs hasOne; `belongsTo` master within module) + `ProductVariantWineAttributes`; factory; `ProductVariantCreated` event; `CreateProductVariant` action (transactional + event).
  - Acceptance: a Variant belongs to exactly one Master; the `WINE` vintage lives in the per-type table (not the neutral core); created `draft` + records `ProductVariantCreated`; spec scenarios "A Variant belongs to exactly one Master", "Wine vintage lives in the attribute set", "The variant axis is type-neutral" pass; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Catalog/ProductVariantTest.php` — assert `Schema::hasColumn('catalog_product_variants','vintage_year')` is **false** (AC-0-GEN-3); assert the FK `product_master_id` is single (one parent) and that the within-module `belongsTo`/`hasOne` works; event row asserted by key.

- [x] 3.3 **Product Reference** (design D5) — migration `catalog_product_references` (`product_variant_id` FK, `format_id` FK, **unique `(product_variant_id, format_id)`**, **no `case_configuration_id`**, `lifecycle_state`, audit/version); model `ProductReference`; factory; `ProductReferenceCreated` event; `CreateProductReference` action (transactional + event).
  - Acceptance: a PR references exactly one Variant + one Format and carries no case-configuration dimension; the `(variant, format)` pair is unique (a second identical pair is rejected by the DB unique index); created `draft` + records `ProductReferenceCreated`; spec scenarios "A PR is Variant + Format only" and "Packaging does not change the PR" (uniqueness half) pass; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Catalog/ProductReferenceTest.php` — assert `Schema::hasColumn('catalog_product_references','case_configuration_id')` is **false** (BR-Identity-3); insert a duplicate `(variant,format)` inside a nested `DB::transaction` (savepoint — trap 5) and assert the unique violation surfaces; assert the same `(variant,format)` resolves to one PR id.

## 4. Sellable SKUs

- [x] 4.1 **Sellable SKU (Intrinsic)** (design D5) — migration `catalog_sellable_skus` (`product_reference_id` FK, `case_configuration_id` FK, commercial attrs e.g. `commercial_name`, `lifecycle_state`, audit/version); model `SellableSku`; factory; `SellableSkuCreated` event; `CreateSellableSku` action (transactional + event).
  - Acceptance: an Intrinsic SKU references exactly one PR + one Case Configuration + commercial attrs, born `draft`, records `SellableSkuCreated`; three SKUs for the same Variant+Format with different Case Configurations all reference the **one** same PR (completes spec scenario "Packaging does not change the PR"); spec scenario "An Intrinsic SKU composes PR + Case Configuration" passes; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Catalog/SellableSkuTest.php` — build one Variant+Format → one PR; create three Case Configurations (loose/OWC6/CARTON12) and three SKUs; assert all three `product_reference_id` are equal; event asserted by key.

- [x] 4.2 **Composite SKU** (design D5/D9) — migrations `catalog_composite_skus` (`lifecycle_state`, audit/version) + `catalog_composite_sku_constituents` (`composite_sku_id` FK, `product_reference_id` FK, `position`, unique `(composite_sku_id, product_reference_id)`); model `CompositeSku` (constituents M:N, ordered); factory; `CompositeSkuCreated` event; `CreateCompositeSku` action that enforces **N ≥ 2** constituents and **does NOT validate producer composition** (transactional + event).
  - Acceptance: a Composite with ≥2 constituent PRs is created `draft` and records `CompositeSkuCreated`; a Composite with <2 constituents is rejected; a **multi-producer** constituent set is accepted (PIM producer-agnostic — BR-SKU-5); the same PR can be a constituent of two different Composites (M:N); spec scenarios "A Composite requires at least two constituents", "PIM is producer-agnostic about constituents", "A Product Reference can be in multiple Composites" pass; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Catalog/CompositeSkuTest.php` — single-constituent create asserts a domain rejection; build constituents whose underlying Masters carry **different** `producer_id` and assert the Composite is accepted (no producer check); add one PR to two Composites and assert both valid. Do NOT add a single-producer guard — that belongs to Module S (design D9).

## 5. Naming-cascade guard, docs & cross-engine integration

- [x] 5.1 **Naming-cascade guard** (design D7; §18 / AC-0-GEN-6) — a convention test asserting the canonical category-neutral identifiers are used and no `Wine*`/`BottleReference*` **structural or event** identifier exists in `app/Modules/Catalog`; add wine-display-alias docblocks ("Wine Master" / "Wine Variant" / "Bottle Reference (BR)") to the relevant model classes.
  - Acceptance: the canonical model classes (`ProductMaster`, `ProductVariant`, `ProductReference`, `Format`, `CaseConfiguration`, `SellableSku`, `CompositeSku`) and the seven `*Created` event classes exist with the §18 names; no Catalog class or event name contains `Wine`/`BottleReference` as a structural identifier; quality commands green.
  - Test hint: `tests/Architecture/CatalogNamingCascadeTest.php` — scan `App\Modules\Catalog` class names + the `Events/` directory; assert none match `/Wine|BottleReference/` as a type/event name; assert each canonical class exists (`class_exists`). Aliases live only in docblocks/`CONTEXT.md`.

- [x] 5.2 **Docs** — extend `CONTEXT.md` (root) with the resolved spine glossary terms (Product Master, Product Variant, Product Reference [+ "Bottle Reference" alias], Format, Case Configuration, Sellable SKU [Intrinsic/Composite], Product Type, the naming-cascade alias rule) and add a Catalog event-contract note documenting the seven `*Created` payload shapes (PII-free, ids only). No code.
  - Acceptance: `CONTEXT.md` carries the spine terms with the canonical/alias distinction; the event-contract note lists the seven `*Created` events + payload keys; `openspec validate catalog-product-spine --strict` green; quality commands green (docs-only — no test, but run lint/format).

- [ ] 5.3 **Full-chain integration + cross-engine close** (AC-0-J-4 creation half) — one feature test driving the whole spine: create Master → Variant → (Format) → Reference → Intrinsic SKU, and a Composite, asserting each `*Created` event is recorded, each entity is `draft`, payloads are PII-free, and **no** `*Activated`/`*Retired` event exists; then run the full Catalog suite on PostgreSQL 17.
  - Acceptance: the end-to-end chain records exactly the expected `*Created` events (seven event types exercised) and zero `*Activated`/`*Retired`; the dedup rejection and the producer-agnostic Composite both hold in the integrated flow; the **entire** Catalog test set is green on SQLite **and** on a local PostgreSQL 17 (`knowledge/testing/rules.md` command block); record the PG run in `progress.md`.
  - Test hint: `tests/Feature/Modules/Catalog/SpineCreationChainTest.php` — assert `domain_events` contains the `*Created` names and `where('name','like','%Activated%')->count()===0` and same for `Retired`; assert every entity `lifecycle_state === Draft`. This is the substrate-wiring proof (the Q1 decision) and the cross-engine gate.
