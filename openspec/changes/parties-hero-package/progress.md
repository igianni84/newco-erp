# Progress — parties-hero-package

## Codebase Patterns
(consolidated reusable patterns — read first each iteration)

- **The K read-port seam, now shipped three times.** A Module-K read of a deferred module is three files + one line: `Contracts/<X>Reader.php` (interface, one method), `Reads/<Adapter>.php` (launch stand-in), and a plain `$this->app->bind(Interface::class, Adapter::class)` in `PartiesServiceProvider::register()` — never `singleton`. Precedents: `CustomerTransactionTotalsReader`→`Null…` (RM-02, Module S), `PartyComplianceStatusReader`→`Database…`, and now `HeroPackageCapacityReader`→`ConfigHeroPackageCapacityReader` (Module A). The adapter's docblock carries *why it is a stand-in* and *what replaces it*.
- **PHPStan max analyses `tests/`, and it audits your fakes.** An in-test anonymous-class fake of a `?int`-returning contract that never returns `null` on any path draws `return.unusedType` ("never returns null so it can be removed from the return type"). Do **not** narrow the fake's signature to silence it — the flag means the fake is too weak to prove the consumer handles the null arm. Make it answer both arms (`$clubId === 1 ? null : $clubId * 10`). **Tasks 2.2 / 2.4 / 3.2 / 6.1 all bind fake capacity readers — they will hit this.**
- **`env()` yields a string; cast at the adapter boundary, once.** `config('parties.hero_package.capacity.default')` is `mixed`. The house normalisation idiom is `is_numeric($x) ? (int) $x : <safe default>` (`Catalog\Lifecycle\ApprovalGovernance::roleCount()` is the precedent). For capacity the safe default is `null` ⇒ uncapped, so a malformed value dark-launches rather than garbage-casting.
- **A per-id config override must be keyed on `array_key_exists`, not truthiness / `??`.** `by_club_id[$clubId] ?? $default` would silently discard an explicit `null` override (pin one Club uncapped beneath a capped default) and fall through to the default. Presence wins.
- **The uncapped test default is the reason the suite stays green.** No `PARTIES_HERO_PACKAGE_CAPACITY` in `phpunit.xml`, and no `.env.testing` exists in this repo at all ⇒ `default = null` ⇒ every one of the 2221 pre-existing tests runs against unchanged behaviour. Any task tempted to set that env var globally breaks the whole suite; bind a fake reader or `config()->set()` per-test instead.
- **The seat set has ONE definition: `ClubSeatOccupancy::OCCUPYING_STATES` (`[Active, Suspended]`).** Never re-spell it in an Action, a query or a test. `Approved` is excluded, and that is sound *only* because `Approved` is transient (`ApproveProfile` gates before writing it and drives through to `active` in the same transaction). If any task ever makes a Profile rest durably in `approved`, the seat set is wrong — not the Action.
- **Two counting entry points, and choosing between them IS the invariant.** `lockAndCountOccupiedSeats()` (locks the `parties_clubs` row, *then* counts) is the ONLY one a **seat-consuming** transition may call, and only inside an already-open `DB::transaction` — tasks 2.2 / 2.4 / 3.2. `countOccupiedSeats()` is lock-free and exists for exactly one caller: `CreateProfile`'s birth-state routing gate (D6), whose two outcomes (`Applied`/`WaitingList`) hold no seat and therefore structurally cannot oversell. `wouldOversell($clubId, $occupied)` touches no database at all — the caller supplies the count it already made, and reuses that same number in the 1.3 rejection message rather than counting twice.
- **`{@see \Fully\Qualified\Name}` in a docblock is not prose — Pint turns it into a real `use` import.** The `fully_qualified_strict_types` fixer shortens docblock FQCNs *and adds the import to the file*. Pest's `not->toUse(...)` reads real imports, so an explanatory `{@see \App\Modules\Allocation\…}` inside a Module K docblock would manufacture the exact boundary violation task 4.1 asserts against. Name non-dependencies as plain backticked text; `git diff` the `use` block after the first `pint` run. (`knowledge/laravel/hypotheses.md`)
- **A lock-before-count ordering is invisible to every behavioural test on SQLite** (its grammar compiles no lock clause at all), so pin it as **SQL statement order** via `DB::listen`, then driver-gate the `for update` needle — assert both halves, never skip (`ActorRoleConstraintTest:110`). That order-pin is the only cross-engine proof the D3 fix is still in place; task 3.2's two-connection race is its PG17-only complement. (`knowledge/testing/hypotheses.md`)

---

## [2026-07-09 12:14] — 1.1 Capacity read-port + config-backed launch adapter + binding

- **What was implemented.** The Module-K-owned Hero-Package capacity seam (design D1/D2). `Contracts\HeroPackageCapacityReader` — one method `forClub(int $clubId): ?int`, `null` ⇒ uncapped. `Reads\ConfigHeroPackageCapacityReader` — reads the `by_club_id` per-Club override, else the global `default`, normalising both to `?int`. `config/parties.php` — new for Parties (Module K used no `config()` call before this; the banner records that, names Module A as the owner of `qty`, and marks the file as the launch adapter's source that disappears when Module A lands). Bound with a plain `bind` in `PartiesServiceProvider::register()`.
- **Files changed.** `app/Modules/Parties/Contracts/HeroPackageCapacityReader.php` (new) · `app/Modules/Parties/Reads/ConfigHeroPackageCapacityReader.php` (new) · `config/parties.php` (new) · `app/Modules/Parties/Providers/PartiesServiceProvider.php` (+1 bind, +2 imports) · `tests/Feature/Modules/Parties/HeroPackageCapacityReaderBindingTest.php` (new, 13 tests).
- **Quality loop: green.** Pint clean · new test 13/13 · **full suite 2234/2234** (2221 baseline + 13 — zero pre-existing tests moved, which is the task's central claim) · PHPStan max **0** · `openspec validate parties-hero-package --strict` valid. One PHPStan cycle: `return.unusedType` on the swappability fake (fixed by strengthening the fake, not the signature).
- **Acceptance walked.** Interface + one method + `null` ⇒ uncapped ✅ · adapter override-then-default with `int` cast ✅ · `config/parties.php` with the Module-A banner ✅ · plain `bind` (not `singleton`) in `register()` ✅ · **no** env var in `phpunit.xml` / `.env.testing` ✅ (verified by `git diff --name-only` — untouched; `.env.testing` does not exist) · tests cover `int` default, `by_club_id` override winning, `null` when neither set, the **string** `env()` shape casting to `int`, and container resolution ✅ · **no new file under `Actions/`** ✅ (`git status app/Modules/Parties/Actions/` empty — `SupplyLifecycleChainTest` untouched).
- **Learnings for future iterations:**
  - Two edge cases the task bullets did not name, both now pinned and both load-bearing for the gate: **`0` is a real capacity** (a Club admitting nobody), never an absence — so `is_numeric` guards the read, not `empty()` / `??`. And an **explicit `null` in `by_club_id`** pins one Club uncapped beneath a capped default — `array_key_exists`, not `??`.
  - `expect(Class::class)->not->toUse('App\\Modules\\Allocation')` works inside a plain Feature test (the `NullCustomerTransactionTotalsReader` precedent) — no separate Architecture file needed for a class-scoped boundary assertion. Task 4.1 still needs the module-wide one.
  - No architecture test enumerates `Parties/Contracts/` or `Parties/Reads/`, so adding files there is free. Only `Parties/Actions/` is set-pinned (`SupplyLifecycleChainTest`). Confirms design D10's premise.
  - The adapter performs **no** Club lookup (the id is a lookup key, not a row), so its test needs no `RefreshDatabase` — booting the app is the whole fixture.

---

## [2026-07-09 12:36] — 1.2 K-internal seat-occupancy helper `Support/ClubSeatOccupancy`

- **What was implemented.** `App\Modules\Parties\Support\ClubSeatOccupancy` (design D3/D6/D10) — the single definition of what a *seat* is, plus the lock that makes counting one trustworthy. `OCCUPYING_STATES = [Active, Suspended]`; `lockAndCountOccupiedSeats()` acquires the `parties_clubs` row lock and *then* counts (the ordering is the oversell fix); `countOccupiedSeats()` is the lock-free read reserved for D6's birth-state gate; `wouldOversell($clubId, $occupied)` reads capacity through the injected `HeroPackageCapacityReader` port, compares with `>=`, and returns `false` on a `null` (uncapped) capacity. A plain class — not a `Contract`, not container-bound, not published, not under `Actions/`.
- **Files changed.** `app/Modules/Parties/Support/ClubSeatOccupancy.php` (new) · `tests/Feature/Modules/Parties/ClubSeatOccupancyTest.php` (new, 21 tests) · `knowledge/testing/hypotheses.md` (+1) · `knowledge/laravel/hypotheses.md` (+1).
- **Quality loop: green.** Pint clean · new test 21/21 · **full suite 2255/2255** (2234 baseline + 21 — zero pre-existing tests moved) · PHPStan max **0** · `openspec validate parties-hero-package --strict` valid. No fix cycles: the 1.1 fake-strength pattern was applied pre-emptively, so `return.unusedType` never fired.
- **Acceptance walked.** `Support/`, not `Actions/` ✅ (`git status app/Modules/Parties/Actions/` empty; `SupplyLifecycleChainTest` green). Lock strictly before count ✅ — and *pinned structurally*, see below. Predicate reads the injected port, `null` ⇒ never at capacity ✅. Not a Contract / not bound / not published ✅ (only two new files; `PartiesServiceProvider` untouched). Docblock states the `DB::transaction` requirement and the SQLite no-op ✅. Tests: all **9** states driven through one Club ⇒ occupancy `2` (the `AC-K-J-13` seat-set proof), plus a per-state dataset for diagnostics; per-Club isolation ✅; `null` capacity ⇒ never at capacity ✅.
- **Learnings for future iterations:**
  - **I added a third method the task bullets did not name: `countOccupiedSeats()` (lock-free).** D6 forbids `CreateProfile` from taking the Club-row lock, so without it task 2.1 must either take the lock (violating D6) or re-spell the seat-set query — duplicating the single most load-bearing definition in this change. The locking variant is now literally its composition, which is *how* "lock strictly before count" becomes structural rather than remembered.
  - **How 1.2 proves its own fix on SQLite.** A behavioural test cannot: SQLite's grammar compiles no lock clause, so the racy and fixed implementations behave identically there. The test captures the emitted SQL with `DB::listen` and asserts `parties_clubs` precedes the `count(*)` on `parties_profiles`, then driver-gates the `for update` needle (present on `pgsql`, absent on SQLite). `DB::transaction()` issues its `SAVEPOINT` through the raw PDO handle, so it never pollutes the captured statements. **Task 3.2 must still run** — statement order is not serialisation.
  - **Pint edited my `use` block from a docblock.** `{@see \App\Modules\Parties\Actions\ApproveProfile}` (explanatory prose) became a real `use` import of four Action classes in a `Support/` class that depends on none. Harmless within-module, lethal across one: task 4.1 asserts no Module K file imports `Modules\Allocation\*`, and Pest's `toUse` reads real imports. Rewritten as backticked prose. Check the `use` block after every `pint` run on a docblock-heavy file.
  - **`wouldOversell()` takes the occupancy as an argument on purpose.** It performs no DB access, so the caller's lock/no-lock decision stays visible at the call site (D6 vs D3), and 1.3's rejection message can reuse the already-counted number rather than counting a second time. Tasks 2.2 / 2.4 will inject **both** the reader and this helper, exactly as task 2.1's bullet prescribes.
  - The Club-row lock target is `parties_clubs`, and `lockAndCountOccupiedSeats()` throws `ModelNotFoundException` on an unknown Club — gating a seat-consuming transition against a phantom occupancy of `0` would silently admit everyone. The lock-free read has no row to look up and answers `0`.
---
