## 1. Prepare the surface (keep every iteration green)

- [x] 1.1 Relax the now-stale absence guards: in `tests/Feature/Modules/OperatorPanel/Parties/CustomerLifecycleConsoleTest.php` (and any other console test) drop the `assertActionDoesNotExist('placeHold')` and `('liftHold')` assertions; **KEEP** the `requireKyc` / record-screening / account / profile absence guards (those surfaces stay deferred). Removing a currently-passing absence assertion keeps the suite green now; the verbs land in groups 3–4. Acceptance: `php artisan test --filter=CustomerLifecycleConsoleTest` green; no `placeHold`/`liftHold` absence assertions remain.
- [x] 1.2 Pin the Filament 5 vehicle for a **non-relation** Holds table with per-row actions hosted on a `ViewRecord` page (table widget vs. an `InteractsWithTable` component), verified against the installed Filament version — do NOT write the table/row-action API from memory (the repo's arch-from-memory ban). Record the chosen vehicle in the `ViewCustomer` docblock. Acceptance: a minimal Holds table compiles and renders on `ViewCustomer` with a placeholder row action; `vendor/bin/phpstan analyse` clean for the new file.
- [x] 1.3 Front-load i18n. Extend the `customer` block in `lang/en/operator_console.php` AND `lang/it/operator_console.php` with: `actions.place_hold`, `actions.lift_hold`; `fields.hold_type`, `fields.hold_scope`, `fields.profile`, `fields.reason`, `fields.lift_reason`; `holds.columns.{hold_type,scope_type,status,reason,placed_by,placed_at,lifted_by,lifted_at}`; `notifications.hold_placed`, `notifications.hold_lifted` (reuse the existing `notifications.action_failed`). Every new key's IT value MUST differ from EN (the IT-differs guard). Enumerate the new keys in `tests/Feature/Modules/OperatorPanel/Parties/CustomerConsoleI18nTest.php` (`customerConsoleKitKeys()` and the IT-differs set). Acceptance: run the i18n test via `--filter=CustomerConsoleI18nTest` OR the full suite (so the shared `scanOperatorConsoleHardcodedSinks` helper loads) — all five guards green with the new keys.

## 2. Holds read table on the Customer view

- [x] 2.1 Render the read-only Holds table sourced by a direct `Hold::query()` over the Customer's scope-set — `(scope_type=customer AND scope_id=$record->id) OR (scope_type=account AND scope_id=$record->account?->id) OR (scope_type=profile AND scope_id IN $record->profiles->pluck('id'))`. Columns: `hold_type`, `scope_type`, `status`, `reason`, placed-by (actor role + id) + `created_at`, lifted-by + `lifted_at`. Render `hold_type` / `scope_type` / `status` via the model cast (`->value`); import NO state enum (`HoldStatus` stays cast-only). Acceptance + test: `CustomerHoldsConsoleTest` seeds Holds at all three scopes (customer / its Account / one of its Profiles) and asserts each renders with its type/scope/status/reason; assert rows have no inline edit/delete affordance. `vendor/bin/phpstan analyse` at max clean (no `nullsafe.neverNull` — `$record->account` is a nullable `hasOne`).

## 3. Place Hold (header action)

- [x] 3.1 Add the `placeHold` header action on `ViewCustomer` with a form: `hold_type` `Select` over `HoldType::cases()`, `scope_type` `Select` over `HoldScope::cases()`, a `profile_id` `Select` over `$record->profiles` visible only when `scope_type === 'profile'`, and an optional `reason` `Textarea`. Import `App\Modules\Parties\Enums\HoldType` + `HoldScope` (operand enums — the `CreateClub`→`ClubRegistrationFlowType` precedent). Acceptance + test: `CustomerHoldsConsoleTest` asserts the form exposes all six `HoldType` options and three `HoldScope` options, and the `profile_id` field is hidden unless `scope_type = profile`.
- [x] 3.2 Wire `placeHold` write-through: `->action(fn (array $data) => $this->surfaceLifecycleOutcome(fn () => app(PlaceHold::class)->handle(HoldType::from($type), HoldScope::from($scope), $scopeId, $reason), (string) __('operator_console.customer.notifications.hold_placed')))`, resolving `$scopeId` — `customer` → `$record->id`, `account` → `$record->account->id`, `profile` → the selected `profile_id` — and narrowing every `$data` value with `is_string` guards. NO `$model->save()` (route through the Action only). Acceptance + test: `CustomerHoldsConsoleTest` — (a) placing an `admin` Hold on an `active` Customer creates exactly one `active` `Hold` and records exactly one `CustomerHoldPlaced` (envelope `module=parties`, `entity_type=Hold`, `actor_role=ActorRole::NewcoOps`, `actor_id` loose `toEqual` operator id) AND one `CustomerSuspended`, with the Customer now `suspended`; (b) placing an `admin` Hold on a `pending` Customer records `CustomerHoldPlaced` and **no** `CustomerSuspended`, Customer stays `pending`; (c) placing with `scope_type=account` writes a Hold with `scope_type=account` and `scope_id=$account->id`.

## 4. Lift Hold (per-row action)

- [ ] 4.1 Add a per-row `lift` action to the Holds table, **visible iff** `$row->status === HoldStatus::Active && ! $row->hold_type->autoLiftable()` (i.e. `admin` / `fraud` / `compliance` / `credit`), with an optional `lift_reason` `Textarea`. Wire `->action(fn () => $this->surfaceLifecycleOutcome(fn () => app(LiftHold::class)->handle($row->getKey(), $reason), (string) __('operator_console.customer.notifications.hold_lifted')))`. Do NOT import `IllegalHoldLift` (caught by base `RuntimeException` in `surfaceLifecycleOutcome`). Acceptance + test: `CustomerHoldsConsoleTest` asserts the `lift` row action is present on `admin`/`fraud`/`compliance`/`credit` **active** rows and absent on `kyc`/`payment` rows and on already-`lifted` rows.
- [ ] 4.2 Test the lift + restore coupling and the rejection path: `CustomerHoldsConsoleTest` — a Customer covered by two active customer-scope Holds (`admin` + `fraud`), hence `suspended`: lifting `admin` records one `CustomerHoldLifted`, the Customer stays `suspended`, NO `CustomerReactivated`; lifting the last (`fraud`) records `CustomerHoldLifted` + `CustomerReactivated`, Customer → `active`. A `lift` invoked against a `kyc` Hold through the domain raises `IllegalHoldLift` (auto-managed) → surfaced as the `action_failed` danger notification, Hold unchanged, no event recorded (`assertNotified((string) __('operator_console.customer.notifications.action_failed'))`).

## 5. PG17 closing-chain integration test

- [ ] 5.1 Add `tests/Feature/Modules/OperatorPanel/Parties/CustomerHoldsChainTest.php` using `DatabaseMigrations` (NOT `RefreshDatabase` — each console action opens its own `DB::transaction`; the in-transaction event append must commit). Drive the chain through the page/table actions: place `admin` on an `active` Customer → `suspended` (`CustomerHoldPlaced` + `CustomerSuspended`); place `fraud` → still `suspended`; lift `admin` → no restore; lift `fraud` (last covering) → `CustomerReactivated`, `active`; place `admin` on a `pending` Customer → Hold recorded, no transition; assert the per-row `lift` is absent on a `kyc` row and an operator lift of it is rejected. Assert the emergent `DomainEvent` set with `pluck('name')->toEqualCanonicalizing([...])` + per-event envelope (`module` / `entity_type` / `entity_id` strict, `actor_role` `NewcoOps`, representative `actor_id` loose `toEqual`). Acceptance: green on SQLite (`php artisan test --filter=CustomerHoldsChainTest`).
- [ ] 5.2 Run the PG17 ritual (GUIDE §2.7): `docker run -d --name pg … postgres:17 -p 55432:5432` → poll `pg_isready` → `DB_CONNECTION=pgsql DB_HOST=127.0.0.1 DB_PORT=55432 DB_DATABASE=newco_test DB_USERNAME=newco DB_PASSWORD=newco php -d memory_limit=-1 vendor/bin/pest tests/Feature/Modules/OperatorPanel/Parties tests/Feature/Modules/OperatorPanel/Catalog/ProductMasterConsoleI18nTest.php` (the appended Catalog i18n test loads the shared `scanOperatorConsoleHardcodedSinks` helper) → `docker rm -f pg`. Acceptance: the Parties OperatorPanel folder is green under PostgreSQL 17 (loop = verify only; no push/merge).

## 6. Quality gates + memory

- [ ] 6.1 `vendor/bin/pint` clean; `vendor/bin/phpstan analyse` at max green — including the architecture tests `NoEloquentWriteInOperatorPanelRule` and `ModuleBoundariesTest` **UNCHANGED** (the `Parties\Enums` import is already permitted by the operand-enum carve-out — do NOT widen the allowlist). `php artisan test` (full) green. Acceptance: all four quality commands pass; the git diff vs `main` touches no `spec/**` and no `openspec/specs/**` (hand-edits forbidden) and adds no composer dependency or migration.
- [ ] 6.2 `openspec validate operator-console-parties-holds --strict` green. Append a `log.md` entry via `scripts/memlog.sh "<op>" "<target>" "<outcome>"`; overwrite `hot.md` from current state; consolidate the durable patterns into the change's `progress.md` `## Codebase Patterns` (the non-relation per-row-action table vehicle; the bespoke-action-reusing-`surfaceLifecycleOutcome` recipe for multi-operand / Hold-id writes; "operand-enum carve-out exercised, `ModuleBoundariesTest` needs no widening").
