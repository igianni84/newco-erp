# Progress — parties-compliance

## Codebase Patterns
(consolidated reusable patterns — read first each iteration)

- **Compliance enums (task 1.1, shipped).** `app/Modules/Parties/Enums/{KycStatus,SanctionsStatus,ScreeningTriggerSource}.php` — all string-backed, PascalCase case names, persisted-token values.
  - `KycStatus` cases `NotRequired|Pending|Verified|Rejected` + **`clears(): bool`** → true for `Verified`/`NotRequired` only. The producer gate (task 5.1) is `$kyc === null || $kyc->clears()` — NULL is the absence of a case, handled at the gate, not in the enum.
  - `SanctionsStatus` cases `Pending|Passed|Failed|UnderReview`. **Completions = `Passed`/`Failed`** (each fires a § 15.6 event); `UnderReview` fires none.
  - `ScreeningTriggerSource` cases `Onboarding|Cadence|AmlThreshold|ComplianceAdHoc`. **`Onboarding` → `CustomerOnboardingScreening{Passed,Failed}`; every other source → `CustomerRescreening{Passed,Failed}`** (event-family selector for task 4.2).
- **Enum house style.** No methods on the parties-core enums; `clears()` is the first predicate. File docblock cites design L-tag + spec §. Tests pin **verbatim, order-sensitive** case→value maps + `toHaveCount` + a `from()` ValueError rejection — mirror `tests/Unit/Modules/Parties/Enums/EnumsTest.php`. New compliance enum test is a **separate** file `ComplianceEnumsTest.php` (don't amend `EnumsTest.php`).
- **Quality loop runners.** Full suite = `php -d memory_limit=512M vendor/bin/pest` (plain `php artisan test` OOMs at 128M). Filtered = pass the test file path. `vendor/bin/phpstan analyse --no-progress` (level max, 0 errors required); `vendor/bin/pint` to format, `vendor/bin/pint --test` to check. `openspec validate parties-compliance --strict` is a standing per-task gate (delta specs present).
- **Additive nullable-value-set migration idiom (task 1.2, shipped).** `Schema::table()` + `string('col')->nullable()` (no default — DEC-071) + the backed-enum cast on the model; the PG-only CHECK is **widened** to `CHECK (col IS NULL OR col IN (<Enum::cases()>))` (a private `addNullableValueSetCheck($table,$column,$cases)` helper on the anonymous migration keeps the 4 constraints DRY). Constraint name `<table>_<column>_check` (auto, ≤63 chars). `down()` drops the PG CHECKs (`DROP CONSTRAINT IF EXISTS`, pgsql-guarded) **then** the columns. Mirrors the create-table idiom (`2026_06_15_000005`), just nullable. **Pint rewrites `\BackedEnum` → `BackedEnum`** in the no-namespace migration file (`fully_qualified_strict_types`) — resolves to global, PHPStan-clean.
- **timestampTz columns cast `immutable_datetime`** (matches `domain_events.occurred_at`/`event_deliveries.available_at`); assign `CarbonImmutable::now()` in tests, **never `now()`** (mutable `Illuminate\Support\Carbon` trips PHPStan against a `CarbonImmutable` `@property`). Nullable `boolean` cast → null stays null (primitive casts skip null). Enum casts on a NULL column → null.
- **PG-only CHECK test, per-file-safe.** Don't reuse the cross-file `captureConstraintViolation` global (a filtered single-file `pest <path>` run doesn't load sibling files → undefined-function fatal). Inline the savepoint locally: `try { DB::transaction(fn () => DB::table($t)->where('id',$id)->update([$col=>$bogus])); } catch (QueryException $e) { $msg=$e->getMessage(); }`, then branch on `DB::getDriverName()` — pgsql asserts `toContain('<constraint>')` + row untouched; sqlite asserts `$msg===''` + raw write accepted (the cast is the floor there; non-vacuous, never a skip).
- **Compliance transition-guard exceptions (task 1.3, shipped).** `app/Modules/Parties/Exceptions/{IllegalKycTransition,IllegalSanctionsTransition}.php` — both `extends RuntimeException`, mirroring the `IllegalProducerTransition` house style: a static named factory per illegal move building `new self((string) __('parties.<group>.<key>', ['state' => $from->value]))`. `(string)` coerces the translator `mixed` return (PHPStan-max). Factories the downstream Actions call: `IllegalKycTransition::cannotRequire|cannotVerify|cannotReject` (Customer 2.1 + Producer 3.1 KYC), `::cannotWaive` (Producer 3.1 only — illegal when already `not_required`); `IllegalSanctionsTransition::onboardingAlreadyScreened` (4.2 — `last_screening_at` already set, **no `:state`**, the timestamp is PII) + `::cannotResolve(SanctionsStatus $from)` (the `under_review → terminal` resolve path; re-screens stay permissive per design L4). Copy: new `kyc` + `sanctions` groups in `lang/en/parties.php` with a `:state` placeholder (a business enum value, never PII) — preserve the 5 pre-existing groups.
- **Lang-key interpolation test idiom (mirror `TransitionExceptionsTest`).** No DB → `uses(TestCase::class)` (NOT `RefreshDatabase`); booting the app gives the translator. Per factory, pick a from-state token **absent from that key's literal template** so its presence in the message proves `:state` was interpolated (not the copy spelling a similar word). A `->with([...])` dataset asserts every state-bearing key resolves with a neutral probe token (`'suspended'` — a Customer-status token absent from all KYC/sanctions templates), `->not->toBe($key)` catches a missing key; a final case re-asserts the pre-existing groups still resolve.
- **Migration `down()` reversibility — verify via the real command, not an in-suite test.** `require <migration>` returns `mixed` and the anonymous `extends Migration` class has no nameable type carrying `up()`/`down()` (the base `Migration` is abstract, declares neither) — so an in-suite `$m->down()` can't be PHPStan-max-clean without reflection. Prove it on the PG17 gate instead: `php artisan migrate:rollback --step=1` (rolls back exactly the latest migration) → assert columns+CHECKs gone → `php artisan migrate` → assert restored.
- **Eventless transition Action idiom (KYC, task 2.1, shipped).** KYC records NO domain event (design L3), so the KYC Actions have **NO constructor / no dependencies** — pure `DB::transaction` → `Model::query()->whereKey($id)->lockForUpdate()->firstOrFail()` → from-state assert → `$model->update([...])` → `return $model`. Contrast the *evented* Actions (`ActivateProducer`/`SunsetClub`) which inject `DomainEventRecorder` + `ActorContext` to `record(...)`. `version` is NOT bumped (identity-attribute revisions only). Invoke via `app(RequireKyc::class)->handle($id)` (zero-arg container build). **This is the template for 3.1 (Producer KYC — also eventless).** 4.2 (sanctions) DOES emit → it injects the recorder + actor like the spine Actions. The future `kyc` Hold coupling (`parties-holds`) will add a dependency then (within-module Action orchestration, not events — design L3).
- **Nullable from-state ⇒ nullable exception factory (task 2.1, shipped).** `kyc_status` is nullable (DEC-071 un-screened), so verify/reject are reachable from NULL → `IllegalKycTransition::cannotVerify`/`cannotReject` take **`?KycStatus`** and render NULL via **`$from->value ?? 'unset'`** (NOT `$from?->value ?? 'unset'` — PHPStan strict `nullsafe.neverNull` rejects the redundant `?->`; `??` already null-coalesces via isset-semantics). `cannotRequire`/`cannotWaive` stay **non-null** — NULL is a *legal* from-state for require (→pending) and waive (→not_required), so their guards never reach the throw with NULL. Widening a 1.3-shipped factory the moment the consuming Action reveals the NULL case is legitimate intra-change refinement (non-null callers are unaffected — contravariant). **Producer KYC (3.1) reuses the SAME exception; its verify/reject also face NULL.**
- **Scope-guard exact-set whitelist GROWS per compliance task.** `SupplyLifecycleChainTest`'s "only these non-Create transition Actions exist" `toEqualCanonicalizing([...$supplySideTransitions, ...$complianceTransitions])` is an exact set — **every task that adds a non-Create transition Action MUST add it to `$complianceTransitions` or the full suite goes RED** (2.1 added the 3 Customer-KYC actions; 3.1 adds the 4 Producer-KYC; 4.2 adds `RecordCustomerScreening`). The guard's intent (no demand-side **status** transition: no `ActivateCustomer`/`SuspendAccount`/`ApproveProfile`/`LockOriginatingClub`) is preserved; task 6.1's `ComplianceIndependenceTest` adds the independence-matrix angle.
- **PHPStan: never call an enum method on a nullable `@property` in tests.** `$model->kyc_status->clears()` trips `method.nonObject` (the property is `?KycStatus`). Assert the reached state with `->toBe(KycStatus::X)` and leave the enum-predicate truth (`clears()` truth table) to `ComplianceEnumsTest` (1.1) — don't re-test the enum from the lifecycle test.

---

## [2026-06-17 11:50] — 1.1 Compliance enums + the KYC cleared predicate
- **Implemented:** the three string-backed compliance enums with verbatim spec tokens — `KycStatus` (`not_required|pending|verified|rejected`) + the `clears()` cleared-state predicate (true for `verified`/`not_required` only — § 4.4 / design L1), `SanctionsStatus` (`pending|passed|failed|under_review` — § 9.2), `ScreeningTriggerSource` (`onboarding|cadence|aml_threshold|compliance_ad_hoc` — § 9.2 trigger paths, DEC-030/DEC-035).
- **Files changed:** `app/Modules/Parties/Enums/KycStatus.php`, `…/SanctionsStatus.php`, `…/ScreeningTriggerSource.php` (new); `tests/Unit/Modules/Parties/Enums/ComplianceEnumsTest.php` (new, 8 tests); `openspec/changes/parties-compliance/tasks.md` (1.1 checked).
- **Quality loop: green.** Pint ✅ · filtered test 8/8 ✅ · full suite **621/621** (613 baseline + 8) ✅ · PHPStan max 0 errors ✅ · Pint --test ✅ · `openspec validate --strict` ✅. No DB touched this task (PG17 gate N/A — first DB task is 1.2).
- **Learnings for future iterations:**
  - The cleared predicate lives on `KycStatus::clears()` so the producer gate (5.1) and the deferred customer purchase gate share one definition; NULL-as-cleared is a **gate** concern, not encoded in the enum (NULL = no case).
  - The event-family selector for sanctions (4.2) keys on `ScreeningTriggerSource`: `Onboarding` → onboarding events, anything else → rescreening events; `UnderReview` verdict records no event at all.
  - Verified the trigger tokens against PRD § 9.2 (lines 479–488): onboarding, 12-month `cadence`, `aml_threshold` (DEC-035), `compliance_ad_hoc`; country-change is explicitly NOT a launch trigger (used as the negative `from()` case).
---

## [2026-06-17 12:08] — 1.2 Additive migration — KYC/sanctions columns on parties_customers + parties_producers
- **Implemented:** one additive migration `database/migrations/2026_06_17_000001_add_compliance_to_parties.php` — 8 nullable cols on `parties_customers` (`kyc_status`, `kyc_required`, `enhanced_kyc_flag`, `enhanced_kyc_at`, `sanctions_status`, `last_screening_at`, `next_rescreen_at`, `screening_trigger_source`) + 1 on `parties_producers` (`kyc_status`). All nullable / no default / no backfill (DEC-071). PG-only `CHECK (col IS NULL OR col IN (<Enum::cases()>))` on the 3 Customer value-set cols + the Producer `kyc_status` (4 CHECKs, via a private `addNullableValueSetCheck` helper). Added the casts (3 enum + 2 boolean + 3 immutable_datetime) and `@property` annotations to `Customer`, the `kyc_status` enum cast + `@property` to `Producer`; models stay persistence-only (`$guarded = []` untouched).
- **Files changed:** the migration (new); `app/Modules/Parties/Models/Customer.php`, `app/Modules/Parties/Models/Producer.php` (casts + @property + docblock); `tests/Feature/Modules/Parties/ComplianceSchemaTest.php` (new, 4 it + 4-row dataset = 8 tests); `tasks.md` (1.2 checked).
- **Quality loop: green.** Pint ✅ · filtered 8/8 ✅ · full suite **629/629** SQLite ✅ · PHPStan max 0 ✅ · Pint --test ✅ · `openspec validate --strict` ✅ · `git diff main -- composer.json composer.lock` empty ✅.
- **PG17 verified.** Parties suite **132/132** on `postgres:17`; `migrate:fresh` DONE; direct psql probes — Producer NULL `kyc_status` + Customer NULL `kyc_status`/`sanctions_status`/`screening_trigger_source` insert cleanly (NULL admitted), `kyc_status='bogus'` rejected by the named CHECK on both tables, valid tokens + explicit NULL accepted; `down()` reversibility via `migrate:rollback --step=1` (cols + 4 CHECKs gone) → `migrate` (restored).
- **Learnings for future iterations:**
  - The 8 Customer casts the transition Actions (2.x/4.x) write through are now live: `kyc_status`→KycStatus, `sanctions_status`→SanctionsStatus, `screening_trigger_source`→ScreeningTriggerSource, `kyc_required`/`enhanced_kyc_flag`→bool, `enhanced_kyc_at`/`last_screening_at`/`next_rescreen_at`→immutable_datetime. Producer `kyc_status`→KycStatus (the only compliance col there; the gate task 5.1 reads it).
  - Asymmetric NULL is a *gate/meaning* concern (5.1 + downstream Module S), NOT schema: at the DB level a NULL value-set col is simply admitted by the widened CHECK on both tables — proven on PG17.
  - See Codebase Patterns (top) for the nullable-CHECK idiom, the timestampTz `immutable_datetime`/`CarbonImmutable::now()` rule, the per-file-safe PG-CHECK test, and the migrate:rollback reversibility recipe.
---

## [2026-06-17 12:16] — 1.3 Transition-guard exceptions + localized copy
- **Implemented:** the two compliance transition-guard exceptions + their localized copy. `IllegalKycTransition` (`extends RuntimeException`) with `::cannotRequire/cannotVerify/cannotReject/cannotWaive(KycStatus $from)` — one shared vocabulary for Customer (2.1) + Producer (3.1) KYC. `IllegalSanctionsTransition` with `::onboardingAlreadyScreened()` (no `:state` — the prior-screening timestamp is PII) + `::cannotResolve(SanctionsStatus $from)`. Both mirror the `IllegalProducerTransition` house style: `new self((string) __('parties.<group>.<key>', ['state' => $from->value]))`. Added new `kyc` + `sanctions` groups to `lang/en/parties.php` (dotted keys, `:state` placeholder, PII-free) — the 5 pre-existing groups (`producer`/`club`/`producer_agreement`/`customer`/`profile`) untouched.
- **Files changed:** `app/Modules/Parties/Exceptions/IllegalKycTransition.php`, `…/IllegalSanctionsTransition.php` (new); `lang/en/parties.php` (+2 groups, 6 keys); `tests/Unit/Modules/Parties/Exceptions/ComplianceTransitionExceptionsTest.php` (new, 13 tests / 40 assertions); `tasks.md` (1.3 checked).
- **Quality loop: green.** Pint ✅ · filtered 13/13 ✅ · full suite **642/642** (629 + 13) ✅ · PHPStan max 0 ✅ · Pint --test ✅ · `openspec validate --strict` ✅ · `git diff main -- composer.json composer.lock` empty ✅. **No DB touched** → PG17 gate N/A (pure PHP: exceptions + lang + test).
- **Learnings for future iterations:**
  - The exception vocabulary 2.1/3.1/4.2 consume is now live. KYC has **no domain event** (design L3) — these exceptions ARE the only observable signal of a rejected KYC transition besides the unchanged column. `cannotWaive` fires only when already `not_required` (nothing to deselect — ADR 2026-06-17).
  - `cannotResolve` guards the `under_review → passed|failed` resolve edge specifically; the broad re-screen path in 4.2 is permissive from any prior state (design L4), so 4.2's only hard guard is `onboardingAlreadyScreened` (`last_screening_at IS NULL` for `trigger_source = onboarding`).
  - See Codebase Patterns (top) for the exception+factory house style and the lang-key interpolation test idiom (probe-token `'suspended'`, token-absent-from-template trick, pre-existing-groups regression).
---

## [2026-06-17 12:36] — 2.1 RequireKyc + RecordKycVerified + RecordKycRejected
- **Implemented:** the three Customer KYC transition Actions — the first consumers of the 1.3 exception vocabulary and the first DB-touching Actions of the slice. Each is **eventless** (KYC records no domain event — design L3) → NO constructor/dependencies; pure `DB::transaction` → `lockForUpdate` re-read → from-state assert → `update()` → return. `RequireKyc`: `not_required`/NULL → `pending` + sets `kyc_required = true` (else `IllegalKycTransition::cannotRequire`). `RecordKycVerified`/`RecordKycRejected`: `pending → verified`/`rejected` (else `::cannotVerify`/`::cannotReject`). No Hold placed (scope guard — `parties-holds` owns it); `Customer.status` never moved (KYC FSM separate); `version` not bumped.
- **Refinement (necessary, not scope creep):** widened the 1.3-shipped `IllegalKycTransition::cannotVerify`/`cannotReject` to **`?KycStatus`** — a verify/reject on an un-screened Customer (NULL `kyc_status`) is a real, reachable illegal from-state; passing the nullable column to a non-null param was both a PHPStan error and a latent `TypeError`. NULL renders as the `unset` sentinel (`$from->value ?? 'unset'`). `cannotRequire`/`cannotWaive` stay non-null (NULL is legal for them). 1.3's `ComplianceTransitionExceptionsTest` stays green unamended (contravariant widening; non-null callers identical).
- **Files changed:** `app/Modules/Parties/Actions/{RequireKyc,RecordKycVerified,RecordKycRejected}.php` (new); `app/Modules/Parties/Exceptions/IllegalKycTransition.php` (verify/reject → `?KycStatus` + docblock); `tests/Feature/Modules/Parties/CustomerKycLifecycleTest.php` (new, 17 tests / 68 assertions); `tests/Feature/Modules/Parties/SupplyLifecycleChainTest.php` (scope-guard whitelist += the 3 KYC actions); `tasks.md` (2.1 checked).
- **Quality loop: green.** Pint ✅ · filtered 17/17 ✅ · full suite **659/659** SQLite ✅ · PHPStan max 0 ✅ · Pint --test ✅ · `openspec validate --strict` ✅ · `git diff main -- composer.json composer.lock` empty ✅.
- **PG17 verified.** Parties feature suite **110/110** on `postgres:17` (incl. the 17 KYC tests) — the illegal-transition rollback + verify-after-throw `findOrFail` SELECT survives on PG (trap 5: the from-state guard throws a PHP exception *before* any DML, inside the Action's own `DB::transaction` savepoint, so only the savepoint rolls back; the outer RefreshDatabase wrapper survives).
- **Learnings for future iterations:** see the new Codebase Patterns (top) — the eventless-Action template (3.1 reuses it), the nullable-from-state exception rule, the growing scope-guard whitelist (3.1/4.2 MUST extend it), and the no-enum-method-on-nullable-property PHPStan rule.
---
