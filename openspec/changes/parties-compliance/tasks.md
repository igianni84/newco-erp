# Tasks — parties-compliance

Read `proposal.md`, `design.md` (L1–L8) and the spec delta `specs/party-registry/spec.md` before any task. **Standing rules per task:** the five Quality Commands green at the end (format · test_filter · test · type_check · lint); `openspec validate parties-compliance --strict` green; **no protected files touched**; **exactly ONE additive migration** (nullable columns over `parties_customers` + `parties_producers`, PG-only `CHECK (col IS NULL OR col IN (...))`, **no backfill** — L1); **no new Composer dependency** (`git diff main -- composer.json composer.lock` stays empty). **Verify every framework/vendor API in `vendor/`/`app/` before writing it** (lessons.md) — in particular `DomainEventRecorder::record(...)` returns the `DomainEvent` and takes `?string $correlationId` / `?int $causationId`; reuse the `2026_06_15_000005_create_parties_customers_table.php` `CHECK`-from-`Enum::cases()` idiom. **Every DB-touching task is verified on a real PostgreSQL 17 before it is marked done** — SQLite-green is necessary, never sufficient (`knowledge/testing/rules.md`); the **asymmetric NULL semantics (Producer-KYC-NULL-cleared vs Customer-sanctions-NULL-blocked) MUST be asserted on PG17** (L1). **Boundary & persistence law:** every reference is within Module K (no cross-module import/join/event-consume); the Models stay persistence-only (`$guarded = []`) — the **transition Action is the sole writer** of the new columns. **Scope guard:** add NO Hold (no `kyc` Hold place/lift — `parties-holds` owns it), NO Customer/Account/Profile **status** transition, NO sanctions **enforcement** gate (Module S owns order-completion), NO screening-vendor adapter, NO enhanced-KYC detection job, NO 12-month cadence job (all documented seams — L6/L7).

## 1. Foundations (enums, migration, exceptions)

- [x] 1.1 **Compliance enums + the KYC cleared predicate** (design L1/L3/L4; spec — all four ADDED requirements) — `App\Modules\Parties\Enums\KycStatus` (`NotRequired`, `Pending`, `Verified`, `Rejected`; string-backed) with a `clears(): bool` returning true for `Verified`/`NotRequired`; `App\Modules\Parties\Enums\SanctionsStatus` (`Pending`, `Passed`, `Failed`, `UnderReview`); `App\Modules\Parties\Enums\ScreeningTriggerSource` (`Onboarding`, `Cadence`, `AmlThreshold`, `ComplianceAdHoc`). String values match the spec tokens verbatim (`not_required`, `under_review`, `aml_threshold`, …).
  - Acceptance: each enum is a string-backed enum whose `->value` equals the spec token; `KycStatus::Verified->clears()` and `KycStatus::NotRequired->clears()` are true, `Pending`/`Rejected` false; PHPStan max clean; quality commands green.
  - Test hint: `tests/Unit/Modules/Parties/Enums/ComplianceEnumsTest.php` — assert `KycStatus::from('not_required')`, `SanctionsStatus::from('under_review')`, `ScreeningTriggerSource::from('aml_threshold')` round-trip; assert the `clears()` truth table (4 cases).

- [x] 1.2 **Additive migration — KYC/sanctions columns on `parties_customers` + `parties_producers`** (design L1; spec — Customer KYC/Sanctions Lifecycle, Producer KYC Lifecycle) — one migration `database/migrations/2026_06_17_000001_add_compliance_to_parties.php`. `Schema::table('parties_customers')`: nullable `kyc_status` (string + `KycStatus` cast), nullable `kyc_required` (boolean), nullable `enhanced_kyc_flag` (boolean), nullable `enhanced_kyc_at` (timestampTz), nullable `sanctions_status` (string + `SanctionsStatus` cast), nullable `last_screening_at` (timestampTz), nullable `next_rescreen_at` (timestampTz), nullable `screening_trigger_source` (string + `ScreeningTriggerSource` cast). `Schema::table('parties_producers')`: nullable `kyc_status` (string + `KycStatus` cast). **All nullable, no default, no backfill** (DEC-071). PG-only `CHECK (col IS NULL OR col IN (<Enum::cases()>))` for each of the three value-set columns on each table (reuse the `pgsql` driver guard idiom). Add the casts to `Customer`/`Producer` models. `down()` drops the columns + constraints.
  - Acceptance: migrate fresh on SQLite **and PG17**; on PG17 a NULL value-set column inserts cleanly AND an invalid value (e.g. `kyc_status = 'bogus'`) is rejected by the `CHECK`; existing-row semantics preserved (a Producer row with NULL `kyc_status` exists and is valid); `Customer`/`Producer` model casts expose the typed enums; `down()` reverses cleanly; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/ComplianceSchemaTest.php` (`uses(RefreshDatabase::class)`) — factory a Customer + Producer, assert the new attributes are null at creation, set each to a valid enum and re-read the typed cast; on PG17 assert a raw `DB::table(...)->update(['kyc_status' => 'bogus'])` throws a `QueryException` (the `CHECK`) and that `['kyc_status' => null]` succeeds.

- [x] 1.3 **Transition-guard exceptions + localized copy** (design L2; spec — Customer/Producer KYC + Sanctions requirements) — `App\Modules\Parties\Exceptions\IllegalKycTransition` (named factories `::cannotRequire(KycStatus $from)`, `::cannotVerify(KycStatus $from)`, `::cannotReject(KycStatus $from)`, `::cannotWaive(KycStatus $from)` — covering Customer + Producer KYC) and `App\Modules\Parties\Exceptions\IllegalSanctionsTransition` (`::onboardingAlreadyScreened()`, `::cannotResolve(SanctionsStatus $from)`), each `extends RuntimeException` resolving localized copy from new `kyc` + `sanctions` groups in `lang/en/parties.php` (dotted keys, `:state` placeholder — not PII). Preserve the existing `producer`/`club`/`producer_agreement`/`customer`/`profile` lang groups.
  - Acceptance: each factory builds an exception naming the rule + the offending `:state`; copy lives in `lang/en/parties.php` (no hardcoded strings — invariant 12); `__('parties.kyc.cannot_verify', ['state' => 'verified'])` resolves; quality commands green.
  - Test hint: `tests/Unit/Modules/Parties/Exceptions/ComplianceTransitionExceptionsTest.php` (no DB) — assert each factory returns its class with a non-empty resolved message containing the state token; assert the new lang keys resolve and the pre-existing groups still resolve.

## 2. Customer KYC lifecycle

- [x] 2.1 **`RequireKyc` + `RecordKycVerified` + `RecordKycRejected`** (design L2/L3; spec — Requirement: Customer KYC Lifecycle) — three Actions under `app/Modules/Parties/Actions/`, each `DB::transaction` → `lockForUpdate` re-read → from-state assert → write `kyc_status` (and, for `RequireKyc`, set `kyc_required = true`). `RequireKyc`: from `NotRequired`/NULL → `Pending` (else `IllegalKycTransition::cannotRequire`). `RecordKycVerified`/`RecordKycRejected`: from `Pending` → `Verified`/`Rejected` (else `::cannotVerify`/`::cannotReject`). **No domain event** (L3 — the PRD names none); the change is audit-only. No Hold placed (scope guard).
  - Acceptance: spec scenarios "The kyc_required flag transitions not_required to pending", "Verified and rejected are reachable from pending", "This slice places no kyc Hold", and "Illegal KYC transitions are rejected" pass; `domain_events` gains **no** row for a KYC transition; no Hold row is created (assert against the Hold table absence — there is no Hold model in this change, so assert `domain_events` has no `*Hold*` and the Customer status is unchanged); quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/CustomerKycLifecycleTest.php` (`uses(RefreshDatabase::class)`) — factory a Customer (NULL kyc), `RequireKyc` → assert `kyc_status === Pending` and `kyc_required === true` and `domain_events()->count()` unchanged from baseline; `RecordKycVerified` → `Verified`; assert `RecordKycVerified` on a non-`Pending` Customer throws `IllegalKycTransition`; assert the Customer `status` column (`pending`) is untouched by any KYC transition.

## 3. Producer KYC lifecycle

- [x] 3.1 **`RequireProducerKyc` + `RecordProducerKycVerified` + `RecordProducerKycRejected` + `WaiveProducerKyc`** (design L2/L3; spec — Requirement: Producer KYC Lifecycle) — four Actions, same `DB::transaction`/`lockForUpdate`/guard idiom, sole writers of the Producer `kyc_status`. `RequireProducerKyc`: `NotRequired`/NULL → `Pending`. verified/rejected: `Pending → Verified`/`Rejected`. `WaiveProducerKyc`: any state → `NotRequired` (the operator-deselect — ADR `2026-06-17`). **No domain event** (L3). Each illegal transition throws `IllegalKycTransition`.
  - Acceptance: spec scenarios "Require, verify, reject the Producer KYC", "Operator waives Producer KYC to not_required", "Producer KYC is distinct from Customer KYC", and "Illegal Producer KYC transitions are rejected" pass; no `domain_events` row for a Producer-KYC transition; the Producer `status` (`draft`) is untouched; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/ProducerKycLifecycleTest.php` — factory a `draft` Producer (NULL kyc); `RequireProducerKyc` → `Pending`; `RecordProducerKycVerified` → `Verified`; `WaiveProducerKyc` on a `Rejected` Producer → `NotRequired`; assert `RecordProducerKycVerified` on a `NotRequired` Producer throws; assert no `domain_events` row was added by any of them and the Producer `status` stays `draft`.

## 4. Sanctions screening lifecycle + events

- [x] 4.1 **The four sanctions screening events** (design L3/L4; spec — Requirement: Sanctions Screening Events) — `App\Modules\Parties\Events\{CustomerOnboardingScreeningPassed, CustomerOnboardingScreeningFailed, CustomerRescreeningPassed, CustomerRescreeningFailed}`, each a `final` class with untyped `const NAME` (verbatim § 15.6 name), `const ENTITY_TYPE = 'Customer'`, and a static `payload(Customer): array` → `{customer_id, sanctions_status, trigger_source}` (PII-free — no name/email/phone/DOB).
  - Acceptance: each event's `NAME` is byte-for-byte the § 15.6 name; `payload()` returns only the three keys with no personal data; `ENTITY_TYPE === 'Customer'`; quality commands green.
  - Test hint: `tests/Unit/Modules/Parties/Events/ScreeningEventsTest.php` — assert each `NAME` constant equals the expected string; build a Customer, assert `payload()` keys are exactly `{customer_id, sanctions_status, trigger_source}` and contain no PII.

- [x] 4.2 **`RecordCustomerScreening` Action** (design L4/L6; spec — Requirements: Customer Sanctions Screening Lifecycle, Sanctions Screening Events) — `handle(int $customerId, SanctionsStatus $verdict, ScreeningTriggerSource $source): Customer` — `DB::transaction` → `lockForUpdate` re-read → guard (`source === Onboarding` requires `last_screening_at IS NULL`, else `IllegalSanctionsTransition::onboardingAlreadyScreened()`) → set `sanctions_status = $verdict`, stamp `last_screening_at = now()`, set `next_rescreen_at = now()->addMonths(12)`, set `screening_trigger_source = $source` → if `$verdict` is a **completion** (`Passed`/`Failed`) record the event family chosen by `$source` (`Onboarding` → `CustomerOnboardingScreening{Passed,Failed}`; else → `CustomerRescreening{Passed,Failed}`); a `UnderReview` verdict records **no** event (L4). Manual-first — the verdict is an input; **no vendor adapter** (L6). Sanctions transitions never touch `kyc_status` (independence).
  - Acceptance: spec scenarios "Onboarding screening records the first verdict and event", "under_review resolves to passed or failed", "Re-screening records the rescreening events with the trigger source", "Automated cadence and AML detection are deferred; the fields and ad-hoc path ship", and "An onboarding screening on an already-screened Customer is rejected" pass; `under_review` records no event; `next_rescreen_at` is set 12 months past `last_screening_at`; `kyc_status` is never modified; quality commands green; **verified on PG17** (assert the `timestamptz` round-trip and the nullable-column writes on PG).
  - Test hint: `tests/Feature/Modules/Parties/CustomerSanctionsLifecycleTest.php` — (a) onboarding `Passed` on a fresh Customer → `sanctions_status === Passed`, one `CustomerOnboardingScreeningPassed` row, `last_screening_at` set, `next_rescreen_at ≈ +12mo`, `trigger_source === Onboarding`; (b) onboarding `UnderReview` → state set, **zero** `domain_events` rows; then re-screen `ComplianceAdHoc` `Passed` → one `CustomerRescreeningPassed` row; (c) onboarding on a Customer with `last_screening_at` already set → `IllegalSanctionsTransition`; (d) assert a sanctions transition leaves `kyc_status` untouched.

## 5. Producer activation gate — retro-tighten (the one MODIFIED behaviour)

- [x] 5.1 **Tighten `ActivateProducer` with the KYC-cleared gate** (design L5; spec — MODIFIED Requirement: Producer Lifecycle) — after the existing `draft` from-state assert in `app/Modules/Parties/Actions/ActivateProducer.php`, add the gate: proceed iff `kyc_status IS NULL` **or** `KycStatus::clears()` (`Verified`/`NotRequired`); otherwise reject (`pending`/`rejected`) with a localized reason via a new `IllegalProducerTransition::kycNotCleared(KycStatus $from)` factory + a `lang/en/parties.php` `producer.kyc_not_cleared` key, leaving the Producer `draft` and recording no `ProducerActivated`. **Replace** the shipped `ProducerLifecycleTest` scenario "Activation enforces no KYC gate in this slice" with the AC-K-FSM-7 matrix and add a NULL-cleared regression. No other change to the Action (retire cascade etc. untouched).
  - Acceptance: spec scenarios "Activate a draft Producer", "Activation requires KYC cleared", and "Illegal Producer transitions are rejected" pass; activation succeeds for `Verified`/`NotRequired`/NULL and is rejected (Producer stays `draft`, no event) for `Pending`/`Rejected`; the **NULL-cleared regression** proves a pre-existing Producer (NULL `kyc_status`) still activates; the prior "no KYC gate" scenario is removed; the `RetireProducer` cascade tests stay green unamended; quality commands green; **verified on PG17**.
  - Test hint: update `tests/Feature/Modules/Parties/ProducerLifecycleTest.php` — parametrize activation over `kyc_status ∈ {null, NotRequired, Verified}` → asserts `Active` + `ProducerActivated`; over `{Pending, Rejected}` → asserts `IllegalProducerTransition` (`kycNotCleared`), `status` still `draft`, zero `ProducerActivated` rows; keep the existing retire-cascade + illegal-transition cases.

## 6. Independence, docs, and cross-engine close

- [x] 6.1 **Independence + scope-guard test** (design L1/L3; spec — Customer KYC/Sanctions Lifecycle, MODIFIED Birth States) — one feature test asserting: the KYC and sanctions FSMs are **separate from the Customer status FSM** and **independent of each other** (drive the (kyc × sanctions) 4-cell at the state level; assert each pair persists independently and the Customer `status` never moves); and the **scope guards** hold — no `kyc` Hold is placed by any KYC transition, no Customer/Account/Profile status transition exists, and no demand-side status event (`CustomerActivated`/`ProfileActivated`/`OriginatingClubLocked`/`CustomerSegmentChanged`) is recordable by this change.
  - Acceptance: the MODIFIED *Birth States* scenario "Supply-side and compliance transitions exist; demand-side status transitions do not" holds (reflect the Parties `Actions/` namespace — compliance + supply-side Actions exist; no Customer/Account/Profile **status** transition class exists; `originating_club_id` has no setter); the 4-cell independence matrix passes; `SpineCreationChainTest` stays green unamended; `ModuleBoundariesTest` + `ModulePersistenceConventionsTest` green (no amendment); quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/ComplianceIndependenceTest.php` — set all four (kyc ∈ {pending,verified} × sanctions ∈ {pending,passed}) combinations and assert each column holds its own value with `status` unchanged; reflect `App\Modules\Parties\Actions` and assert no class transitions Customer/Account/Profile status; assert `where('name','like','%Hold%')->count() === 0` and no `CustomerActivated`/`OriginatingClubLocked` in `domain_events`.

- [ ] 6.2 **Docs — CONTEXT.md terms + Parties compliance contract note** (proposal "What Changes"/Impact) — extend `CONTEXT.md` with the resolved compliance terms (KYC four-state + cleared-state `verified ∨ not_required`, the **NULL-cleared** producer-gate rule, sanctions four-state + independence, screening `trigger_source`, onboarding-vs-rescreen, `under_review` event-silence) and add a Parties compliance contract note documenting the four screening event payloads (PII-free) and the deferred seams (the `kyc` Hold coupling → `parties-holds`; the sanctions order-completion enforcement → Module S; the enhanced-KYC + cadence/AML detection automation; KYC document handling). No code.
  - Acceptance: `CONTEXT.md` carries the new terms with verbatim spec anchors; the contract note lists the four events + payload shapes + the four seams; no canonical-term drift from `CONTEXT.md`/CLAUDE.md; `openspec validate parties-compliance --strict` green; quality commands green.
  - Test hint: docs-only — no test; verify by a terminology re-read against the spec + `openspec validate`.

- [ ] 6.3 **Full compliance chain + cross-engine PG17 close** (spec — all ADDED + both MODIFIED requirements) — one feature test driving the whole slice: create a Customer + Producer → require/verify Customer KYC → run onboarding sanctions screening (passed) then an ad-hoc re-screen → require/waive Producer KYC → activate the Producer through the **new** KYC gate (and assert a `pending`-KYC Producer is blocked). Then run the entire Parties suite + the two arch tests on **PostgreSQL 17**.
  - Acceptance: the chain records exactly the sanctions events (no KYC event, no Hold), the producer gate admits cleared/NULL and blocks pending/rejected, and the demand-side stays inert; the **entire** Parties test set is green on SQLite **and** on a local PostgreSQL 17 (docker `postgres:17` per `knowledge/testing/rules.md`), including the asymmetric-NULL assertions (L1); record the PG run in `progress.md`.
  - Test hint: `tests/Feature/Modules/Parties/ComplianceChainTest.php` — assert `domain_events` contains only the expected `CustomerOnboardingScreening*` / `CustomerRescreening*` names (zero KYC/Hold names) with the expected counts; assert the producer activation matrix; run `php -d memory_limit=512M vendor/bin/pest` against PG17 and confirm green.
