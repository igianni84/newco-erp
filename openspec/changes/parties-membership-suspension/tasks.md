# Tasks — parties-membership-suspension

Read `proposal.md`, `design.md` (L1–L11), the spec delta `specs/party-registry/spec.md`, and the ADR `decisions/2026-06-19-hold-status-coupling.md` before any task. **Standing rules per task:** the five Quality Commands green at the end (format · test_filter · test · type_check · lint); `openspec validate parties-membership-suspension --strict` green; **no protected files touched** (the four guard tests under `tests/` are NOT protected — `spec/**`, `openspec/specs/**`, `CLAUDE.md`, `ralph.sh`, `.claude/**` are); **exactly ONE additive migration** (two nullable columns on `parties_profiles` — the partial-unique index is NOT migrated, it already excludes `{rejected,cancelled,inactive}`; no other table); **no new Composer dependency** (`git diff main -- composer.json composer.lock` stays empty). **Verify every framework/vendor API in `vendor/`/`app/` before writing it** (lessons.md) — in particular `DomainEventRecorder::record(string $name, string $module, ActorRole $actorRole, ?int $actorId, string $entityType, string $entityId, array $payload, ?string $correlationId = null, ?int $causationId = null): DomainEvent` (must run in an open transaction; omit the last two for a root event, pass them for a cascade child — design L11); `ActorContext::role()`/`actorId()`; **`Profile.state` is a `ProfileState` cast (NOT `status`)**, `Customer.status` a `CustomerStatus` cast, `Account.status` an `AccountStatus` cast; **every target state is already in the enums — NO state/enum migration** (`CustomerStatus`/`ProfileState`/`AccountStatus` already enumerate Suspended/Lapsed/Cancelled/Inactive/Closed); reuse `DatabaseComplianceStatusReader::activeHoldTypesForScopes()` (the `(scope) OR (its Customer)` coverage shape) for the Hold coupling; mirror the `ActivateCustomer`/`ActivateProducer` transition-Action idiom, the `RecordKycVerified` system-inline cross-entity-in-one-transaction template, and the `2026_06_17_000001` additive-migration idiom. **Every DB-touching task is verified on a real PostgreSQL 17 before it is marked done** — SQLite-green is necessary, never sufficient (`knowledge/testing/rules.md`; the coupling's nested-transaction savepoints make the PG run load-bearing). **Boundary & persistence law:** every reference is within Module K (Module A capacity, Module E `MembershipFeePaid`, Module S Club-Credit conversion / the AC-K-EVT-14 cancellation signal are deferred seams, NOT reads); the models stay persistence-only (`$guarded = []`) — the **Action is the sole writer**; `version` is **not** bumped. **i18n:** new copy goes in the existing `profile`/`customer` groups + a **new `account` group** of `lang/en/parties.php` only (English baseline + per-key fallback — DEC-127); `:state` tokens are business enum values, never PII. **Event naming traps (design L3):** `ProfileExpired` = `Active → Lapsed` (there is NO `ProfileLapsed`); `ProfileReactivated` = `Suspended → Active` ONLY; `ProfileRenewed` = `Lapsed → Active` grace; `CancelProfile` and ALL Account transitions are **audit-only** (record no event); coin **no** event outside the eight (`Customer{Suspended,Reactivated,Closed}` + `Profile{Suspended,Reactivated,Expired,Renewed,Inactive}`). **Guard tests (design L10):** new Actions/events trip `SupplyLifecycleChainTest` (exact-set transition whitelist + event-non-existence loop) and `ComplianceIndependenceTest` (forbidden-Action list); the coupling inverts `HoldRegistryTest`/`HoldChainTest` (the "Hold performs no status transition" scope-guards) — **only in the coupling tasks 4.x**. **Each task that introduces a tripping name narrows its guard slice in the SAME task** so the suite never goes red; before editing, `grep -rn` the introduced symbol across `tests/` to re-derive the blast radius (don't trust this list). **Scope guard (named seams — proposal boundary table):** NO Hero Package capacity gate (approval/activation stay uncapped — Module-A seam), NO `Applied → WaitingList`, NO Customer-segment derivation, NO `MembershipFeePaid` listener (Module-E seam — invoke `RenewProfile` directly), NO Club-Credit freeze, NO Producer-offboarding orchestration / AC-K-EVT-14 signal (Module-S seam), NO anonymisation, NO Filament/producer UI; **`ActivateAccount` stays forbidden** (Account born `active`); cancellation is **audit-only**.

## 1. Foundations (migration, exceptions, events)

- [x] 1.1 **Additive migration — `lapsed_at` + `cancellation_reason` on `parties_profiles`** (design L1; spec — Profile Lapse and Grace Renewal, Profile Cancellation and Deactivation) — one migration `database/migrations/2026_06_19_000001_add_lifecycle_columns_to_parties_profiles.php` adding a **nullable** `timestampTz` `lapsed_at` and a **nullable** `string` `cancellation_reason` (no default, no backfill; reversible `down()` drops both). Add an `immutable_datetime` cast for `lapsed_at` (+ `@property ?CarbonImmutable`) and a `@property ?string $cancellation_reason` to `app/Modules/Parties/Models/Profile.php`. **Do NOT touch the partial-unique index** (it already excludes the terminal set). No PG-only CHECK (plain nullable columns).
  - Acceptance: migrate fresh on SQLite **and PG17**; a Profile row round-trips `lapsed_at` (set + re-read as `CarbonImmutable`) and `cancellation_reason`; a default-factory Profile has both NULL; `down()` drops both and re-migrates clean; `ModulePersistenceConventionsTest` + `ModuleBoundariesTest` + the shipped Profile/Hold tests stay green; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/ProfileLifecycleColumnsTest.php` (`uses(RefreshDatabase::class)`) — `Profile::factory()->create(['lapsed_at' => now(), 'cancellation_reason' => 'producer_offboarding'])` then re-read asserts the typed cast + the string; a default Profile has both NULL; `Schema::hasColumn('parties_profiles', 'lapsed_at')` on PG17.

- [x] 1.2 **Transition exceptions + localized copy** (design L4; spec — all five Profile/Customer/Account status requirements) — extend `App\Modules\Parties\Exceptions\IllegalProfileTransition` with named factories `::cannotSuspend`, `::cannotReactivate`, `::cannotLapse`, `::cannotRenew`, `::cannotCancel`, `::cannotDeactivate` (each `(ProfileState $from): self`); extend `IllegalCustomerTransition` with `::cannotSuspend`, `::cannotReactivate`, `::cannotClose` (each `(CustomerStatus $from): self`); add a **new** `App\Modules\Parties\Exceptions\IllegalAccountTransition extends RuntimeException` with `::cannotSuspend`, `::cannotReactivate`, `::cannotClose` (each `(AccountStatus $from): self`). Each resolves localized copy from `lang/en/parties.php` — new keys in the existing `profile`/`customer` groups + a **new `account` group**, each with a `:state` business token. Mirror the existing `IllegalProfileTransition`/`IllegalCustomerTransition` factories. **Preserve all existing groups/keys.**
  - Acceptance: each factory returns its class with a non-empty resolved message containing the `:state` token; `__('parties.profile.cannot_suspend', ['state' => 'lapsed'])`, `__('parties.customer.cannot_close', ['state' => 'pending'])`, `__('parties.account.cannot_suspend', ['state' => 'closed'])` all resolve; a pre-existing key (e.g. `parties.profile.cannot_activate`, `parties.hold.cannot_lift_not_active`) still resolves; quality commands green.
  - Test hint: `tests/Unit/Modules/Parties/Exceptions/StatusTransitionExceptionsTest.php` (no DB) — assert each new factory returns its class with the token in the message and no PII; assert the new keys resolve and a pre-existing `parties.*` key still resolves; `IllegalAccountTransition` exists and extends `RuntimeException`.

- [x] 1.3 **The eight demand-side status event classes** (design L3/L11; spec — Demand-Side Status Events) — `App\Modules\Parties\Events\{CustomerSuspended,CustomerReactivated,CustomerClosed,ProfileSuspended,ProfileReactivated,ProfileExpired,ProfileRenewed,ProfileInactive}` — each a `final` class with an untyped `const NAME` (verbatim § 15 name), `const ENTITY_TYPE` (`'Customer'` for the three Customer events, `'Profile'` for the five Profile events), and a static `payload(...): array`: the Customer events `payload(Customer)` → `{customer_id, status}`; the Profile events `payload(Profile)` → `{profile_id, state}` (enum values via `->value`; PII-free). Mirror the shipped `CustomerActivated`/`ProfileActivated` shape. **Then `grep -rn` each name across `tests/`** to confirm none is in an event-existence guard's forbidden list (the `SupplyLifecycleChainTest` glob forbids only `AccountActivated`/`ProfileApproved`/`CustomerSegmentChanged` — none of the eight — so creating the classes trips no existence guard; the runtime "no demand-side event recorded" assertions fire only when the coupling *records* them, in tasks 3.x/4.x).
  - Acceptance: each `NAME` byte-equals its § 15 name (`'CustomerSuspended'`/…/`'ProfileInactive'`); `ENTITY_TYPE` correct; `payload()` returns exactly the listed keys with no PII; no existing guard test goes red (the eight names are absent from every forbidden list); quality commands green (no DB → no PG run).
  - Test hint: `tests/Unit/Modules/Parties/Events/StatusEventsTest.php` — build a `Customer` + `Profile`; assert each `NAME`/`ENTITY_TYPE`; assert `CustomerSuspended::payload($c)` keys are exactly `{customer_id, status}` and `ProfileExpired::payload($p)` keys exactly `{profile_id, state}`, both PII-free; confirm via `grep -rn` that no `*Test` names any of the eight in a `not->toContain` list.

## 2. Profile status transitions (self-edges)

- [x] 2.1 **`SuspendProfile` + `ReactivateProfile` (Active ↔ Suspended)** (design L4/L9/L10; spec — Profile Suspension and Restoration, Demand-Side Status Events) — `app/Modules/Parties/Actions/{SuspendProfile,ReactivateProfile}.php` (each `handle(int $profileId): Profile`), inject `DomainEventRecorder` + `ActorContext`; one `DB::transaction`: `lockForUpdate` re-read; from-state guard (`SuspendProfile`: `state === Active` else `IllegalProfileTransition::cannotSuspend`; `ReactivateProfile`: `state === Suspended` else `::cannotReactivate`); `update(['state' => Suspended|Active])`; `record(ProfileSuspended|ProfileReactivated::NAME, …, entityType: 'Profile', entityId: (string) $profile->id, payload: …::payload($profile))` (root). **State-preserving (L9):** the Action writes ONLY `state` — touches no voucher/order/reservation/credit (none exist). **Then narrow the guard** (L10): add `SuspendProfile`+`ReactivateProfile` to `SupplyLifecycleChainTest`'s exact-set transition whitelist (a new `demandSideStatusTransitions` sub-array is cleanest).
  - Acceptance: spec scenarios "Suspend an active Profile records ProfileSuspended and preserves state", "Restore a suspended Profile", "Illegal suspend or restore is rejected" pass; `Active → Suspended` → `state=Suspended`, exactly **1** `ProfileSuspended` (payload `{profile_id, state}`, root); `Suspended → Active` → **1** `ProfileReactivated`; illegal from-state → throws, unchanged, 0 events; the suspend writes only `Profile.state` (no other table row created/mutated); `SupplyLifecycleChainTest` green with the two added; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/ProfileSuspensionTest.php` (`uses(RefreshDatabase::class)`) — drive a Profile to `Active` (create → approve → activate, through the real Actions), `app(SuspendProfile::class)->handle($p->id)` asserts `state=Suspended` + 1 `ProfileSuspended`; assert `DomainEvent` count delta is exactly 1 (nothing else mutated); `ReactivateProfile` → `Active` + 1 `ProfileReactivated`; `expect(fn()=>app(SuspendProfile::class)->handle($appliedProfile->id))->toThrow(IllegalProfileTransition::class)` with 0 events.

- [ ] 2.2 **`LapseProfile` + `RenewProfile` (Active → Lapsed → Active grace)** (design L3/L4/L5/L10; spec — Profile Lapse and Grace Renewal, Demand-Side Status Events) — `app/Modules/Parties/Actions/{LapseProfile,RenewProfile}.php` (each `handle(int $profileId): Profile`). `LapseProfile`: guard `state === Active` else `::cannotLapse`; `update(['state' => Lapsed, 'lapsed_at' => CarbonImmutable::now()])`; `record(ProfileExpired::NAME, …, entityType: 'Profile', …)` (root). `RenewProfile`: guard `state === Lapsed` **and** `lapsed_at !== null && now ≤ lapsed_at->addDays(30)` else `::cannotRenew` (past-grace and wrong-from-state both reject); `update(['state' => Active, 'lapsed_at' => null])`; `record(ProfileRenewed::NAME, …)` (root). The validity-expiry trigger + the `MembershipFeePaid` renewal trigger are documented Module-E/scheduler seams in the docblocks (L5) — invoke directly. **Then narrow the guard** (L10): add both to the whitelist.
  - Acceptance: spec scenarios "Lapse an active Profile", "Renew a lapsed Profile within the 30-day grace", "Renewal past the grace window is rejected", "The lapse and renewal triggers are deferred seams" pass; `Active → Lapsed` → `state=Lapsed`, `lapsed_at` stamped, **1** `ProfileExpired` (NOT `ProfileLapsed`); in-grace `Lapsed → Active` → `state=Active`, `lapsed_at` cleared, **1** `ProfileRenewed`; `lapsed_at` 31 days ago → `RenewProfile` throws, stays `Lapsed`, 0 events; no `MembershipFeePaid`/Module-E class in the diff; whitelist green; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/ProfileLapseGraceTest.php` (`uses(RefreshDatabase::class)`) — an `Active` Profile → `LapseProfile` asserts `state=Lapsed` + `lapsed_at` set + 1 `ProfileExpired` + `DomainEvent::where('name','ProfileLapsed')->count()===0`; set `lapsed_at` to `now()->subDays(10)` then `RenewProfile` → `Active` + `lapsed_at` null + 1 `ProfileRenewed`; set `lapsed_at` to `now()->subDays(31)` → `toThrow(IllegalProfileTransition::class)` + still `Lapsed`. Use `CarbonImmutable` boundary arithmetic; assert the 30-day edge both sides.

- [ ] 2.3 **`CancelProfile` (audit-only) + `DeactivateProfile`** (design L2/L4/L10; spec — Profile Cancellation and Deactivation, Demand-Side Status Events) — `app/Modules/Parties/Actions/{CancelProfile,DeactivateProfile}.php`. `CancelProfile(int $profileId, ?string $reason = null): Profile`: guard `state ∈ {Active, Lapsed}` else `::cannotCancel`; `update(['state' => Cancelled, 'cancellation_reason' => $reason])`; **records NO event** (audit-only — L2; the AC-K-EVT-14 per-Profile signal is a deferred Module-S seam, noted in the docblock). `DeactivateProfile(int $profileId): Profile`: guard `state === Active` else `::cannotDeactivate`; `update(['state' => Inactive])`; `record(ProfileInactive::NAME, …, entityType: 'Profile', …)` (root). **Then narrow the guard** (L10): add both to the whitelist (note `CancelProfile` records no event — it is a transition Action, still whitelisted).
  - Acceptance: spec scenarios "Cancel an active Profile is terminal and event-silent", "Deactivate an active Profile records ProfileInactive", "Illegal cancel or deactivate is rejected" + the *Profile — Multi-Profile Membership* "A terminal Profile does not block a fresh application" scenario pass; `CancelProfile` → `state=Cancelled`, `cancellation_reason` set, **0** events, and a fresh `CreateProfile` for the same Customer–Club pair succeeds (`Applied`); `DeactivateProfile` → `state=Inactive`, **1** `ProfileInactive`; illegal from-state → throws, unchanged, 0 events; no `ProfileCancelled` event class is created; whitelist green; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/ProfileCancellationTest.php` (`uses(RefreshDatabase::class)`) — `CancelProfile` on an `Active` Profile asserts `state=Cancelled` + `cancellation_reason` + `DomainEvent` count unchanged; then `app(CreateProfile::class)` for the same pair succeeds (the partial index admits it); `DeactivateProfile` → `Inactive` + 1 `ProfileInactive`; a `suspended`/`lapsed` (non-terminal) Profile for the pair still triggers `DuplicateProfileForClub` on a fresh create. `grep -rn 'ProfileCancelled' app/` returns nothing.

## 3. Customer & Account status transitions (cascade + audit-only)

- [ ] 3.1 **`SuspendCustomer` (cascade) + `ReactivateCustomer` (cascade-restore, coverage-guarded)** (design L4/L7/L10/L11; spec — Customer Suspension and Closure, Demand-Side Status Events) — `app/Modules/Parties/Actions/{SuspendCustomer,ReactivateCustomer}.php` (each `handle(int $customerId): Customer`). `SuspendCustomer`: guard `status === Active` else `IllegalCustomerTransition::cannotSuspend`; `update(['status' => Suspended])`; `$root = record(CustomerSuspended::NAME, …, entityType: 'Customer', …)` (root); **then** re-read the Customer's Profiles `->lockForUpdate()->get()`, and for **each Profile in `Active`** `update(['state' => Suspended])` + `record(ProfileSuspended::NAME, …, entityType: 'Profile', entityId: (string) $profile->id, payload: ProfileSuspended::payload($profile), correlationId: $root->correlation_id, causationId: $root->id)` (**causation child** — L11). `ReactivateCustomer`: guard `status === Suspended` else `::cannotReactivate`; `update(['status' => Active])`; `$root = record(CustomerReactivated::NAME, …)`; **then** for each Profile in `Suspended`, restore to `Active` + `record(ProfileReactivated…, causationId: $root->id, correlationId: $root->correlation_id)` **iff** the Profile is no longer covered by any active Hold (reuse the `(Profile-scope) OR (its Customer-scope)` coverage query — `DatabaseComplianceStatusReader::activeHoldTypesForScopes()` shape, re-read under lock). All Module-K, all rows locked in the one transaction (the `RetireProducer` precedent). **Then narrow the guards** (L10): add `SuspendCustomer`+`ReactivateCustomer` to `SupplyLifecycleChainTest`'s whitelist; remove `SuspendCustomer` from `ComplianceIndependenceTest`'s forbidden-Action list (keep `ActivateAccount`/`Lock`/`Set`OriginatingClub).
  - Acceptance: spec scenarios "Suspend an active Customer cascades to its active Profiles", "Restore a Customer reactivates only the Profiles no longer covered by a Hold", "Illegal Customer status transition is rejected" pass; suspend a Customer with 2 `Active` + 1 `Lapsed` Profile → `status=suspended`, **1** `CustomerSuspended` (root) + **2** `ProfileSuspended` (each `causation_id`/`correlation_id` = the `CustomerSuspended`), the `Lapsed` Profile untouched; restore with one cascaded Profile carrying its own active Hold → that Profile stays `Suspended`, the other returns to `Active`; illegal from-state → throws, unchanged, 0 events; both guard tests green; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/CustomerSuspensionCascadeTest.php` (`uses(RefreshDatabase::class)`) — drive a Customer `active` with two `Active` Profiles + one `Lapsed`; `SuspendCustomer` asserts `status=suspended`, 2 Profiles `Suspended`, the `Lapsed` unchanged, and each `ProfileSuspended`'s `causation_id` equals the `CustomerSuspended` row's `id`; place a Profile-scope `admin` Hold on one suspended Profile (via `PlaceHold` — at this task it records the Hold without auto-suspending), then `ReactivateCustomer` → the held Profile stays `Suspended`, the other `Active`; `toThrow(IllegalCustomerTransition::class)` from a wrong from-state.

- [ ] 3.2 **`CloseCustomer` + the Account FSM (`SuspendAccount`/`ReactivateAccount`/`CloseAccount`, audit-only)** (design L4/L7/L8/L10; spec — Customer Suspension and Closure, Account Status Lifecycle) — `app/Modules/Parties/Actions/CloseCustomer.php` (`handle(int $customerId): Customer`): guard `status ∈ {Active, Suspended}` else `::cannotClose`; `update(['status' => Closed])`; `record(CustomerClosed::NAME, …, entityType: 'Customer', …)` (root); **no Profile cascade** (§ 15.1 names none — L7). `app/Modules/Parties/Actions/{SuspendAccount,ReactivateAccount,CloseAccount}.php` (each `handle(int $accountId): Account`): from-state guard (`active`/`suspended`/`{active,suspended}` respectively) → `IllegalAccountTransition`; `update(['status' => …])`; **record NO event** (audit-only — L8). **No `ActivateAccount`.** **Then narrow the guards** (L10): add `CloseCustomer`+`SuspendAccount`+`ReactivateAccount`+`CloseAccount` to the whitelist; remove `CloseCustomer`/`SuspendAccount`/`CloseAccount` from `ComplianceIndependenceTest`'s forbidden list — **keep `ActivateAccount` forbidden** (born `active`) and `Lock`/`Set`OriginatingClub.
  - Acceptance: spec scenarios "Close a Customer is terminal and does not cascade to Profiles", "Suspend and restore an Account, event-silently", "Close an Account is terminal", "Account has no activation Action and rejects illegal transitions" pass; `CloseCustomer` from `active`/`suspended` → `status=closed`, **1** `CustomerClosed`, no Profile transitioned; Account `active→suspended→active→closed` → status flips, **0** Account events; `ActivateAccount` class does not exist; wrong from-state → `IllegalAccountTransition`; both guard tests green; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/CustomerClosureAndAccountStatusTest.php` (`uses(RefreshDatabase::class)`) — `CloseCustomer` on a `suspended` Customer with an `Active` Profile asserts `status=closed` + 1 `CustomerClosed` + the Profile unchanged; `SuspendAccount`→`ReactivateAccount`→`CloseAccount` assert each status + `DomainEvent::query()->where('entity_type','Account')->count()===0`; `grep -rn 'class ActivateAccount' app/` returns nothing; wrong-from-state `toThrow(IllegalAccountTransition::class)`.

## 4. Hold → status coupling (the headline, ADR 2026-06-19)

- [ ] 4.1 **Coupling on PLACE — `PlaceHold` drives covered active scopes to `suspended`** (design L6/L10; spec — Hold-Driven Status Coupling; ADR 2026-06-19) — in `app/Modules/Parties/Actions/PlaceHold.php`, after the Hold is recorded and **within the same transaction**, dispatch on `scope_type`: `customer` ⇒ if the Customer is `active` invoke `SuspendCustomer` (cascade); `account` ⇒ if the Account is `active` invoke `SuspendAccount`; `profile` ⇒ if the Profile is `Active` invoke `SuspendProfile`. The from-state pre-check means a Hold on a non-suspendable scope (a `pending` Customer's onboarding `kyc` Hold, an `Applied` Profile) records the Hold and drives **no** transition. **Remove** the "performs NO status transition … deferred demand-side seam" docblock note (PlaceHold.php ≈ L38-41). **Then invert the guards** (L10): in `HoldRegistryTest`/`HoldChainTest`, amend the place-side scope-guards that now break — a Hold placed on an `active` Account / `active` Customer / `Active` Profile now suspends it (the birth-state cases — `pending` Customer, `Applied` Profile — stay unchanged); `grep -rn 'PlaceHold' tests/` to find every observer. The demand-side-event-absence loops stay valid for audit-only suspends (Account) and for Holds on birth-state scopes; amend only where a Customer/Profile suspend now records `CustomerSuspended`/`ProfileSuspended`.
  - Acceptance: spec scenarios "Placing a Hold on an active scope suspends it" + "A Hold on a non-suspendable scope drives no transition" pass; a customer-scope `admin` Hold on an `active` Customer → Customer `suspended` + its `Active` Profiles `Suspended` (cascade), in the placement transaction; an account-scope Hold on an `active` Account → Account `suspended` (no event); a `kyc` Hold on a `pending` Customer → Hold recorded, Customer still `pending`, no suspend event; `HoldRegistryTest`/`HoldChainTest` amended + green; quality commands green; **verified on PG17** (nested-transaction savepoint).
  - Test hint: `tests/Feature/Modules/Parties/HoldStatusCouplingPlaceTest.php` (`uses(RefreshDatabase::class)`) — `PlaceHold(admin, Customer, $activeCustomerId)` asserts the Customer `suspended` + cascade + 1 `CustomerSuspended`; `PlaceHold(admin, Account, $activeAccountId)` asserts `suspended` + 0 Account events; `PlaceHold(kyc, Customer, $pendingCustomerId)` asserts still `pending` + only `CustomerHoldPlaced`. Then run the amended `HoldRegistryTest`/`HoldChainTest` green.

- [ ] 4.2 **Coupling on LIFT — restore covered `suspended` scopes iff no residual coverage** (design L6/L10/L11; spec — Hold-Driven Status Coupling; ADR 2026-06-19) — in `app/Modules/Parties/Actions/LiftHold.php`, after the Hold is lifted and **within the same transaction**, dispatch on the lifted Hold's `scope_type`: re-query active-Hold coverage for the scope (the `(scope) OR (its Customer)` shape); if the scope is `suspended` **and no other active Hold covers it**, invoke the matching `Reactivate*` (customer ⇒ `ReactivateCustomer`, which cascade-restores its uncovered Profiles; account ⇒ `ReactivateAccount`; profile ⇒ `ReactivateProfile`). Apply the **same** restore logic in `app/Modules/Parties/Actions/RecordKycVerified.php` after it system-lifts the active `kyc` Hold(s) (a no-op at onboarding where the Customer was `pending`, never suspended; live on a post-activation re-screen). **Remove** the "performs NO status transition … deferred demand-side seam" docblock note (LiftHold.php ≈ L49-51). **Then invert the guards** (L10): amend the remaining `HoldRegistryTest`/`HoldChainTest` lift-side scope-guards (a place-then-lift sequence on an `active`-born scope now returns it to its original state — a final-state assertion may survive; a multi-Hold partial lift must assert "still suspended").
  - Acceptance: spec scenario "Lifting the last covering Hold restores; an earlier lift with coverage remaining does not" passes; place two Holds covering one Profile (its own + a cascading Customer Hold) → `Suspended`; lift one → still `Suspended`; lift the last → `Active` + 1 `ProfileReactivated`; lift an account-scope Hold → Account `active` again; `RecordKycVerified` after a post-activation kyc re-screen restores the Customer; both Hold tests green; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/HoldStatusCouplingLiftTest.php` (`uses(RefreshDatabase::class)`) — drive an `active` Profile, `PlaceHold(admin, Profile, …)` + `PlaceHold(fraud, Customer, …)` → Profile `Suspended`; `LiftHold` the profile-scope Hold → still `Suspended` (Customer Hold remains); `LiftHold` the customer-scope Hold → `Active` + `ProfileReactivated`; a `kyc` re-screen path (place `kyc` Hold on an `active` Customer via `RequireKyc` → suspended; `RecordKycVerified` → restored). Assert coverage via re-reading `parties_holds`.

## 5. Chain, docs, cross-engine close

- [ ] 5.1 **Full suspension chain + docs + cross-engine PG17 close** (design L1–L11; spec — all seven ADDED + both MODIFIED requirements) — one feature test driving the whole slice: a Customer `active` with Profiles, exercising suspend→restore, lapse→renew (grace) and lapse→cancel (past grace), cancel + deactivate, the Customer cascade (suspend cascades to Profiles, restore coverage-guarded), the Account FSM, and the **Hold-driven** path (place a Customer Hold → Customer + Profiles `suspended`; lift → restored; a residual Profile Hold keeps that Profile `suspended`). Assert the emergent `domain_events` name-set is exactly the eight status events + the Hold/spine events, with **no** `ProfileLapsed`/`ProfileCancelled`/`AccountSuspended`/`AccountClosed`/`WaitingListJoined`/`CustomerSegmentChanged`, and the cascade `ProfileSuspended` rows carry the `CustomerSuspended` `causation_id`. **Docs:** extend `CONTEXT.md` with the resolved suspension terms (the coverage-recompute coupling; the `ProfileExpired`=lapse / no-`ProfileLapsed` and audit-only-cancel naming traps; Account audit-only + no `ActivateAccount`; the deferred seams: Hero-cap/WaitingList/segments, the `MembershipFeePaid` listener → Module E, the AC-K-EVT-14 cancellation signal + Club-Credit conversion → Module S) + the eight event payloads. Then run the **entire** Parties suite + the arch tests on **PostgreSQL 17**, and audit that all guard files (`SupplyLifecycleChainTest`, `ComplianceIndependenceTest`, `HoldRegistryTest`, `HoldChainTest`, `SpineCreationChainTest`, `ComplianceChainTest`, `CustomerTest`) are green.
  - Acceptance: the chain records exactly the expected name-set (no invented event; cancellation + Account audit-only); the grace boundary (renew ≤30d, cancel >30d), the Customer cascade, the coverage-guarded restore, and every from-state guard hold; `CONTEXT.md` carries the new terms with verbatim spec anchors (§ 4.1/§ 4.2.1/§ 4.7/§ 10.1/§ 15) + no canonical-term drift; the **entire** Parties test set is green on SQLite **and** local PostgreSQL 17 (docker `postgres:17` per `knowledge/testing/rules.md`); record the PG run in `progress.md`; `openspec validate parties-membership-suspension --strict` green.
  - Test hint: `tests/Feature/Modules/Parties/MembershipSuspensionChainTest.php` — assert `DomainEvent::pluck('name')` over the chain `toEqualCanonicalizing([...])` and `->whereIn('name', ['ProfileLapsed','ProfileCancelled','AccountSuspended','AccountClosed'])->count()===0`; assert a cascaded `ProfileSuspended`'s `causation_id` matches the `CustomerSuspended` id; run `php -d memory_limit=512M vendor/bin/pest` against PG17 and confirm green; docs verified by a terminology re-read against the spec.
