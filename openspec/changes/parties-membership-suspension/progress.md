# Progress — parties-membership-suspension

## Codebase Patterns
(consolidated reusable patterns — read first each iteration)

- **Additive-column migration on a Parties table = the `2026_06_18_000002` template.** Plain `Schema::table(...)` with nullable columns, **no default, no backfill**, reversible `down()` that only `dropColumn([...])`s (leaves the table + indexes intact — proven by rolling back one step and asserting `to_regclass` still returns the table). A `timestamptz` carries no value-set CHECK (its domain is the type) → `timestampTz()->nullable()` + an `immutable_datetime` cast + `@property CarbonImmutable|null`. A free-text column with no enum (e.g. `cancellation_reason`) is a plain `string()->nullable()` with **no cast** and `@property string|null`. The partial-unique index on `parties_profiles` already excludes `{rejected,cancelled,inactive}` — never re-migrate it when making a terminal state reachable.
- **Schema-column test = the `CustomerOnboardingAcceptanceTest` template** (3 `it()`s, `uses(RefreshDatabase::class)`): (1) default factory row → new columns NULL by birth; (2) round-trip via a re-fetch (`Model::findOrFail($created->id)`) so hydration casts run — read a `timestamptz` back THROUGH the `immutable_datetime` cast + `->format('Y-m-d H:i:s')` (testing-rule #4: never byte-compare a raw timestamptz, `+00` on PG); (3) `Schema::hasColumn(...)` for each column (positively proves the column on whatever engine the run is on, incl. the PG17 cross-engine pass).
- **PG17 cross-engine close** (knowledge/testing/rules.md): `docker run -d --name pg … -p 55432:5432 postgres:17`; full suite via `DB_CONNECTION=pgsql DB_HOST=127.0.0.1 DB_PORT=55432 … php -d memory_limit=512M vendor/bin/pest` (artisan test OOMs at 128M + pao swallows the PG summary). `docker rm -f pg` after.
- **Transition-exception (`Illegal*Transition`) factory = the verbatim `cannotApprove` template:** a `public static fn cannotX(SomeEnum $from): self` returning `new self((string) __('parties.<group>.cannot_x', ['state' => $from->value]))` — the `(string)` cast tames Larastan's `mixed` `__()` return; the `:state` token is a business enum value (NOT PII), so it IS interpolated (PII like an email/timestamp would be named-not-interpolated, the `gateNotMet`/`DuplicateCustomerEmail` discipline). Copy lives in `lang/en/parties.php` only (English baseline + per-key fallback, DEC-127); a new entity-family gets a new top-level group (`account`). Adding factories/keys is purely additive — moves NO guard test (1.x).
- **Type-clean Pest test for a localized static factory (no-DB, `uses(TestCase::class)` to boot the translator):** call the factory in the test BODY (`$e = Foo::cannotX(Enum::Case); expect($e)->toBeInstanceOf(Foo::class);`) — NOT via a `->with([fn () => Foo::cannotX(...)])` dataset whose `callable` param makes `$factory()` return `mixed` (→ PHPStan `method.nonObject` on `$e->getMessage()`). A `@param class-string` PHPDoc on a *variable-assigned* closure is NOT honored by PHPStan — feed `toBeInstanceOf` a literal `::class` (already a `class-string`), never a `string` var. Prove `:state` was interpolated by choosing a from-state token ABSENT from the literal template (e.g. `Lapsed` against a "suspends only from active" message; for an all-tokens-named template like Account `cannot_close`, only `closed` is absent — and not a substring of "closes"). Prove a subclass via `(new ReflectionClass(X::class))->isSubclassOf(Y::class)`, never `is_subclass_of()`/`instanceof` (PHPStan constant-folds those to `alreadyNarrowedType`). PII guard mirrors `gateNotMet`: `preg_match('/\d/', $msg) === 0` + no `'@'` (so keep business digits like "30-day" OUT of copy — say "the grace window").
- **Self-edge status-transition Action = the verbatim `ActivateProfile` template.** A `Suspend*`/`Reactivate*`/`Lapse*`/`Deactivate*` Action (`handle(int $id): Model`) injects `DomainEventRecorder` + `ActorContext`; one `DB::transaction` → `Model::query()->whereKey($id)->lockForUpdate()->firstOrFail()` → from-state `if ($m->state !== ProfileState::X) throw Illegal*Transition::cannotY($m->state)` (FIRST, before any write) → `update(['state' => …])` (state-preserving — write ONLY the status field, L9) → `record(Event::NAME, Module::Parties->value, $this->actor->role(), $this->actor->actorId(), Event::ENTITY_TYPE, (string) $m->id, Event::payload($m))`. Directly-invoked = ROOT event (omit causationId/correlationId — the recorder self-correlates). Reference forward-task classes (`SuspendCustomer`, `RenewProfile`) in plain backticks in docblocks, `{@see}` only for already-shipped ones, so no broken `@see` accumulates as the slice lands tasks out of order.
- **Guard-narrowing for a new transition Action (`SupplyLifecycleChainTest`, the ONLY guard a Profile self-edge touches).** Add the Action name to the exact-set whitelist or it goes red on `toEqualCanonicalizing`: cleanest is a NEW sub-array (`$demandSideStatusTransitions = ['SuspendProfile','ReactivateProfile']`) spread into the assertion (design L10). `ComplianceIndependenceTest` stays GREEN unamended for Profile self-edges — its forbidden list is Customer/Account/`*OriginatingClub` only, and its per-Action `'originating_club_id' =>` write-count check puts a new Action in the 0-writes `else` branch. Always `grep -rn '<Action>' tests/` first to re-derive the real blast radius (only `SupplyLifecycleChainTest` + `ComplianceIndependenceTest` glob `Actions/*.php`).
- **State-preservation proof = the event-delta + table-count snapshot.** To prove a transition "writes only `state`" (L9) when the downstream tables (voucher/order/credit) don't exist yet: snapshot `DomainEvent::count()` + each sibling table count BEFORE the call, then assert the DomainEvent delta is exactly 1 (the single status event) and every other count is unchanged, plus the row's non-status columns (`customer_id`/`club_id`) are untouched. Drive the happy-path subject through the REAL `CreateProfile → ApproveProfile → ActivateProfile` chain (a top-level Pest helper, uniquely named per file — `createActiveProfile()`); the guard/restore cases use `factory()->create(['state' => …])` to pin a from-state in isolation. **PG id-type safety (trap #6):** compare a jsonb-stored id (written by the Action's re-read) against a value from the SAME hydration source (the helper's `findOrFail`, or a factory model — the `ProfileActivationTest` pattern proven on PG) so `->toBe()` stays type-matched on both engines; the PK `id` round-trips int on PG, FKs read back as numeric strings.
- **A time-windowed transition Action (lapse/grace) = the self-edge template PLUS a frozen-clock test.** `LapseProfile` stamps `update(['state' => …, 'lapsed_at' => CarbonImmutable::now()])`; `RenewProfile` guards a 30-day INCLUSIVE window — `$deadline = $profile->lapsed_at?->addDays(30); if ($state !== Lapsed || $deadline === null || CarbonImmutable::now()->greaterThan($deadline)) throw …` (strict `greaterThan` ⇒ exactly-on-the-edge still renews; both wrong-from-state AND past-grace throw the same `cannotRenew`), then clears `lapsed_at` on success. **Test the boundary with a frozen clock** (`afterEach(fn () => CarbonImmutable::setTestNow())` + per-test `setTestNow($now)`): factory a `Lapsed` Profile with `lapsed_at = $now->subDays(30)` (renews) vs `$now->subDays(30)->subSecond()` / `subDays(31)` (rejects) — proving both sides of the inclusive edge deterministically on PG. Assert a persisted `timestamptz` as an INSTANT — `$row->lapsed_at?->equalTo($now)` (null-safe `?->`, tz-robust across the SQLite/PG asymmetry, trap #4), never a byte-compare.
- **Two traps that fail ONLY at PHPStan / isolated-run (not in the green full suite):** (1) **`CarbonImmutable::create(...)` is typed `CarbonImmutable|null`** in this project's stubs → any `->subDays()`/`equalTo($it)` on the result is a `method.nonObject`/`argument.type` error. Use **`CarbonImmutable::parse('2026-06-19 12:00:00', 'UTC')`** for a non-null instant (the `SupplyLifecycleChainTest` idiom). `setTestNow(create(...))` is fine only because `setTestNow` accepts null. (2) **A Pest helper from a sibling file (`createActiveProfile()` in `ProfileSuspensionTest`) makes the new file pass in the FULL suite but FAIL run-in-isolation** (`Call to undefined function`). The global-helper rule's "reuse OR uniquely-name" → prefer a **uniquely-named file-local helper** (`lapseGraceActiveProfile()`) so the file is self-contained; never redeclare the sibling's name (fatal redeclare on full-suite load). Always run the new test file ALONE once, not just in the suite.

---

## [2026-06-19 12:20] — 1.1 Additive migration — `lapsed_at` + `cancellation_reason` on `parties_profiles`
- **What:** the one additive migration of the change — two nullable columns on `parties_profiles`: `lapsed_at` (`timestampTz`, the 30-day-grace anchor DEC-034) and `cancellation_reason` (`string`, the optional Producer-initiated reason `CancelProfile` records, cancellation being audit-only — design L2). No default, no backfill, reversible `down()`. `immutable_datetime` cast + `@property CarbonImmutable|null $lapsed_at` and `@property string|null $cancellation_reason` (uncast) on `Profile`. Partial-unique index **not touched** (already excludes `{rejected,cancelled,inactive}`). No PG-only CHECK (no enum to pin).
- **Files changed:** `database/migrations/2026_06_19_000001_add_lifecycle_columns_to_parties_profiles.php` (new); `app/Modules/Parties/Models/Profile.php` (import `CarbonImmutable`, 2 `@property`, docblock note, `lapsed_at` cast); `tests/Feature/Modules/Parties/ProfileLifecycleColumnsTest.php` (new, 3 tests / 7 assertions).
- **Quality loop: green.** Pint pass · `ProfileLifecycleColumnsTest` 3/3 · full suite **839/839 SQLite** (836 baseline + 3) · PHPStan 0 errors · Pint --test pass. `openspec validate parties-membership-suspension --strict` valid. `git diff main -- composer.json composer.lock` empty.
- **Verified on PG17** (docker `postgres:17` :55432): full suite **839/839 on PostgreSQL 17** (93s, `php -d memory_limit=512M vendor/bin/pest`). `migrate:fresh` lands both columns; explicit reversibility proof — `migrate:rollback --step=1` drops both, `to_regclass('public.parties_profiles')` still returns the table, re-`migrate` restores both. PG types: `lapsed_at` = `timestamp with time zone` NULL no-default; `cancellation_reason` = `character varying` NULL no-default. Partial-unique index `indexdef` unchanged (predicate still `state NOT IN ('rejected','cancelled','inactive')`).
- **Learnings for future iterations:**
  - Columns + cast are now in place for tasks 2.2 (`LapseProfile` stamps `lapsed_at` / `RenewProfile` clears it + reads it for the 30-day grace) and 2.3 (`CancelProfile` writes `cancellation_reason`). `lapsed_at` is a `CarbonImmutable` — use `CarbonImmutable::now()` to stamp and `lapsed_at?->addDays(30)` for the grace boundary (DEC-034).
  - `cancellation_reason` is **uncast** (a plain nullable string) — assert it with `->toBe('...')`, not through a cast. Only `lapsed_at` is a cast (`immutable_datetime`).
  - This task touched no Action and no event, so **no guard test moved** (`SupplyLifecycleChainTest`/`ComplianceIndependenceTest`/`HoldRegistryTest`/`HoldChainTest` all stayed green unamended). The guard-test realignment begins at task 2.1 (first new Action).
  - Working tree also carried the authored-but-uncommitted foundational artifacts (the ADR `decisions/2026-06-19-hold-status-coupling.md` + its `decisions/INDEX.md` row); committed alongside this task so the `ralph/parties-membership-suspension` branch is self-contained (the authoring session anticipated ralph carrying them in).
---

## [2026-06-19 12:38] — 1.2 Transition exceptions + localized copy
- **What:** the from-state guard vocabulary for the whole slice. Extended `IllegalProfileTransition` with six factories (`cannotSuspend`/`cannotReactivate`/`cannotLapse`/`cannotRenew`/`cannotCancel`/`cannotDeactivate`, each `(ProfileState $from): self`) and `IllegalCustomerTransition` with three (`cannotSuspend`/`cannotReactivate`/`cannotClose`, each `(CustomerStatus $from): self`); created the **new** `IllegalAccountTransition extends RuntimeException` with three (`(AccountStatus $from): self`) — no `cannotActivate` (Account born `active`, design L8). Each resolves a localized `:state` reason. Added 6 keys to the `profile` group, 3 to `customer`, and a **new `account` group** in `lang/en/parties.php` (English baseline only — DEC-127). All `:state` tokens are business enum values (PII-free). `cannot_renew` copy says "within the grace window" (no "30-day") to keep digits out of the message.
- **Files changed:** `app/Modules/Parties/Exceptions/IllegalProfileTransition.php` (+6 factories, docblock), `IllegalCustomerTransition.php` (+3, docblock), `IllegalAccountTransition.php` (**new**, +3); `lang/en/parties.php` (+12 keys / +1 group); `tests/Unit/Modules/Parties/Exceptions/StatusTransitionExceptionsTest.php` (**new**, 18 tests / 86 assertions).
- **Quality loop: green.** Pint pass · `StatusTransitionExceptionsTest` 18/18 · full suite **857/857 SQLite** (839 baseline + 18) · PHPStan 0 errors · Pint `--test` pass · `openspec validate parties-membership-suspension --strict` valid · `git diff main -- composer.json composer.lock` empty. **No DB → no PG17 run** (task is exceptions + lang only). No guard test touched (additive — realignment still begins at 2.1).
- **Learnings for future iterations:**
  - Tasks 2.x–4.x throw these: `SuspendProfile`→`cannotSuspend`, `ReactivateProfile`→`cannotReactivate`, `LapseProfile`→`cannotLapse`, `RenewProfile`→`cannotRenew` (also for past-grace, from-state IS `Lapsed`), `CancelProfile`→`cannotCancel`, `DeactivateProfile`→`cannotDeactivate`; `SuspendCustomer`→`cannotSuspend`, `ReactivateCustomer`→`cannotReactivate`, `CloseCustomer`→`cannotClose`; `SuspendAccount`→`IllegalAccountTransition::cannotSuspend`, `ReactivateAccount`→`cannotReactivate`, `CloseAccount`→`cannotClose`. Mirror `ActivateCustomer`: from-state guard throws the localized exception FIRST, before any other gate.
  - Two new PHPStan-typing gotchas landed in the Codebase Patterns (callable-dataset `mixed`; `@param class-string` not honored on a var-closure; `is_subclass_of` constant-folded). Reuse the in-body concrete-call + literal `::class` + `ReflectionClass::isSubclassOf` recipe for any future static-factory test.
---

## [2026-06-19 12:46] — 1.3 The eight demand-side status event classes
- **What:** the eight `final` event classes under `app/Modules/Parties/Events/`, each the single source of truth for its three contract facets (untyped `const NAME` = verbatim § 15 name, `const ENTITY_TYPE`, static `payload()`), mirroring the shipped `CustomerActivated`/`ProfileActivated` shape. Customer family (`CustomerSuspended`/`CustomerReactivated`/`CustomerClosed`): `ENTITY_TYPE='Customer'`, `payload(Customer)` → `{customer_id, status}` (`status->value`). Profile family (`ProfileSuspended`/`ProfileReactivated`/`ProfileExpired`/`ProfileRenewed`/`ProfileInactive`): `ENTITY_TYPE='Profile'`, `payload(Profile)` → `{profile_id, state}` (`state->value`). All PII-free. Docblocks encode the L3 naming traps (`ProfileExpired`=lapse not `ProfileLapsed`; `ProfileReactivated`=suspend-restore ONLY; `ProfileRenewed`=grace) and the L11 root-vs-causation-child note (the cascade `ProfileSuspended`/`ProfileReactivated` are causation children inside the Customer cascade — but the class is identical either way; causation is threaded by the Action, not the payload).
- **Files changed:** 8 new event classes (`CustomerSuspended`/`CustomerReactivated`/`CustomerClosed`/`ProfileSuspended`/`ProfileReactivated`/`ProfileExpired`/`ProfileRenewed`/`ProfileInactive`.php); `tests/Unit/Modules/Parties/Events/StatusEventsTest.php` (**new**, 8 tests / 53 assertions — mirrors `ActivationEventsTest.php`).
- **Quality loop: green.** Pint pass · `StatusEventsTest` 8/8 · full suite **865/865 SQLite** (857 baseline + 8) · PHPStan 0 errors · Pint `--test` pass · `openspec validate parties-membership-suspension --strict` valid · `git diff main -- composer.json composer.lock` empty. **No DB → no PG17 run** (pure event classes + a no-DB unit test, `uses(TestCase::class)`). **No guard test touched** — confirmed by reading the assertions, not assuming: the Events-namespace check in `SupplyLifecycleChainTest` (≈ L349-354) is a *forbidden-names-absent* loop (`not->toContain` for only `AccountActivated`/`ProfileApproved`/`CustomerSegmentChanged` — none of the eight); the Actions exact-set (≈ L338-339) is untouched (1.3 adds no Action); the runtime "zero demand-side events" loop (≈ L258-273) records none of the eight (nothing invokes them yet). `grep -rn` confirms no `*Test` names any of the eight in a `not->toContain` list.
- **Learnings for future iterations:**
  - The event classes are now in place for the recording Actions: 2.1 (`SuspendProfile`/`ReactivateProfile` → `ProfileSuspended`/`ProfileReactivated`), 2.2 (`LapseProfile`→`ProfileExpired`, `RenewProfile`→`ProfileRenewed`), 2.3 (`DeactivateProfile`→`ProfileInactive`; `CancelProfile` records NONE), 3.1 (`SuspendCustomer`→`CustomerSuspended` + cascade `ProfileSuspended` as causation children, `ReactivateCustomer`→`CustomerReactivated` + coverage-guarded cascade), 3.2 (`CloseCustomer`→`CustomerClosed`; Account FSM records none). Call `::NAME` + `::payload($model)` (post-`update()`, so the payload snapshots the new state) — never magic strings.
  - **The guard-test realignment begins at task 2.1, not before.** 1.3 added zero Actions so the `SupplyLifecycleChainTest` Actions exact-set (`toEqualCanonicalizing`, ≈ L338-339) stayed green untouched; the FIRST new Action (2.1) trips it and must add `SuspendProfile`+`ReactivateProfile` to the whitelist in the same task (design L10 suggests a new `demandSideStatusTransitions` sub-array). Re-grep `grep -rn '<ActionName>' tests/` before editing — don't trust the design's line numbers.
  - Test for these as a no-DB unit (`uses(TestCase::class)`, NOT `RefreshDatabase`): `factory()->make()` with EVERY FK overridden to an explicit id runs no query — the un-migrated schema is itself the guard a stray query would fail. Explicit `expect()->and()` chains (not callable-datasets) keep PHPStan clean (the dataset-`mixed` gotcha).
---

## [2026-06-19 12:59] — 2.1 `SuspendProfile` + `ReactivateProfile` (Active ↔ Suspended)
- **What:** the first two transition Actions of the slice and the first guard realignment. `SuspendProfile` (`Active → Suspended` → root `ProfileSuspended`) and `ReactivateProfile` (`Suspended → Active` → root `ProfileReactivated`), each the verbatim `ActivateProfile` template — inject `DomainEventRecorder` + `ActorContext`; one `DB::transaction` → `lockForUpdate` re-read → from-state guard FIRST (`IllegalProfileTransition::cannotSuspend`/`cannotReactivate`) → state-preserving `update(['state' => …])` (writes ONLY `state` — L9) → root `record(::NAME, …, ::payload($p))`. `ReactivateProfile` docblock pins the L3 naming trap (it is the `suspended → active` restore ONLY; the grace edge is `RenewProfile`'s `ProfileRenewed`).
- **Files changed:** `app/Modules/Parties/Actions/SuspendProfile.php` (**new**), `ReactivateProfile.php` (**new**); `tests/Feature/Modules/Parties/ProfileSuspensionTest.php` (**new**, 10 tests / 70 assertions); `tests/Feature/Modules/Parties/SupplyLifecycleChainTest.php` (**amended** — new `$demandSideStatusTransitions = ['SuspendProfile','ReactivateProfile']` sub-array spread into the exact-set whitelist, design L10).
- **Quality loop: green.** Pint pass · `ProfileSuspensionTest`+`SupplyLifecycleChainTest`+`ComplianceIndependenceTest` 21/21 · full suite **875/875 SQLite** (865 baseline + 10) via `php -d memory_limit=512M vendor/bin/pest` · PHPStan 0 errors · Pint `--test` pass · `openspec validate parties-membership-suspension --strict` valid · `git diff main -- composer.json composer.lock` empty.
- **Verified on PG17** (docker `postgres:17` :55432): `tests/Feature/Modules/Parties` + `tests/Unit/Modules/Parties` **378/378 on PostgreSQL 17** (8.2s) — includes the new suspension test, the amended chain guard, `ComplianceIndependenceTest` and the proven-on-PG `ProfileActivationTest`. The id-type reasoning held: same-source-hydrated id comparisons stayed type-matched (trap #6).
- **Learnings for future iterations:**
  - The four Codebase Patterns at the top now cover the whole 2.x/3.x template: the self-edge Action shape, the `SupplyLifecycleChainTest` exact-set narrowing (the ONLY guard a Profile self-edge trips — `ComplianceIndependenceTest` stays green for Profile edges, but its forbidden list MUST drop `SuspendCustomer`/`CloseCustomer`/Account at 3.x), the event-delta state-preservation proof, and the PG id-type-safety rule.
  - 2.2 (`LapseProfile`/`RenewProfile`) reuses this exact shape but ALSO writes `lapsed_at` (stamp on lapse via `CarbonImmutable::now()`, clear on renew) and adds the 30-day grace boundary in `RenewProfile`'s guard (`lapsed_at?->addDays(30)` ≥ now). `ProfileExpired` = `Active → Lapsed` (NOT `ProfileLapsed`); `ProfileRenewed` = the grace edge. Add both to the same `$demandSideStatusTransitions` sub-array.
  - `createActiveProfile()` is now a top-level Pest helper in `ProfileSuspensionTest.php` — it is GLOBAL (testing-rule: Pest helpers share one namespace). If 2.2/2.3 need an active Profile, REUSE it (do not redeclare) or name a new helper uniquely per file.
---

## [2026-06-19 13:14] — 2.2 `LapseProfile` + `RenewProfile` (Active → Lapsed → Active grace)
- **What:** the Profile lapse/renew pair, the self-edge Action template (2.1) extended with the `lapsed_at` grace anchor. `LapseProfile` (`Active → Lapsed`): from-state guard `cannotLapse` → `update(['state' => Lapsed, 'lapsed_at' => CarbonImmutable::now()])` → root `ProfileExpired` (the § 15.2 lapse event — NOT `ProfileLapsed`, naming trap L3). `RenewProfile` (`Lapsed → Active`): the DEC-034 30-day INCLUSIVE grace guard (`state === Lapsed` AND `now ≤ lapsed_at + 30 days`, else `cannotRenew` — covers both wrong-from-state and past-grace) → `update(['state' => Active, 'lapsed_at' => null])` → root `ProfileRenewed` (NOT `ProfileReactivated`, L3). Validity-expiry + `MembershipFeePaid` triggers are documented Module-E/scheduler seams in the docblocks (L5) — no Module-E class fabricated.
- **Files changed:** `app/Modules/Parties/Actions/LapseProfile.php` (**new**), `RenewProfile.php` (**new**); `tests/Feature/Modules/Parties/ProfileLapseGraceTest.php` (**new**, 15 tests / 96 assertions — self-contained via a uniquely-named `lapseGraceActiveProfile()` helper); `tests/Feature/Modules/Parties/SupplyLifecycleChainTest.php` (**amended** — `LapseProfile`+`RenewProfile` added to the `$demandSideStatusTransitions` whitelist + comment, design L10).
- **Quality loop: green.** Pint pass · `ProfileLapseGraceTest` 15/15 ISOLATED (self-contained) + 36/36 with the guards · full suite **890/890 SQLite** (875 baseline + 15) via `php -d memory_limit=512M vendor/bin/pest` · PHPStan 0 errors · Pint `--test` pass · `openspec validate parties-membership-suspension --strict` valid · `git diff main -- composer.json composer.lock` empty.
- **Verified on PG17** (docker `postgres:17` :55432): `tests/Feature/Modules/Parties` + `tests/Unit/Modules/Parties` **393/393 on PostgreSQL 17** (8.6s) — the `lapsed_at` timestamptz round-trips through the `immutable_datetime` cast, the frozen-clock grace-boundary arithmetic is deterministic, and the `equalTo` instant compare holds across the engine asymmetry. Container torn down.
- **Learnings for future iterations:**
  - Two PHPStan/isolation traps landed in the Codebase Patterns (`CarbonImmutable::create()` is `|null` → use `::parse()`; a sibling's Pest helper fails an isolated run → uniquely-name a file-local helper). The grace-window Action + frozen-clock-test idiom is also consolidated there — reuse it for any future time-windowed transition.
  - 2.3 (`CancelProfile`/`DeactivateProfile`) reuses the same self-edge shape: `CancelProfile` is AUDIT-ONLY (writes `state = Cancelled` + `cancellation_reason`, records NO event — § 15.2 names no `ProfileCancelled`, L2), so its test asserts the `DomainEvent` count is UNCHANGED + a fresh `CreateProfile` for the same pair succeeds (the partial index admits the terminal state). `DeactivateProfile` records `ProfileInactive`. Add BOTH to the same `$demandSideStatusTransitions` whitelist (CancelProfile is whitelisted even though event-silent — it is still a transition Action). `grep -rn 'ProfileCancelled' app/` must stay empty.
---
