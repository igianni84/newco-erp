# Tasks — parties-core

> Read `proposal.md`, `design.md` (D1–D10) and the spec delta `specs/party-registry/spec.md` before any task. **Standing rules per task:** the five Quality Commands green at the end (format · test_filter · test · type_check · lint), `openspec validate parties-core --strict` green, **no protected files touched**, no new Composer dependency (`git diff main -- composer.json composer.lock` stays empty). **Verify every framework/vendor API in `vendor/` before writing it** (lessons.md). **Every DB-touching task is verified on a real PostgreSQL 17 before it is marked done** — SQLite-green is necessary, never sufficient (`knowledge/testing/rules.md`): driver-guard enum `CHECK` constraints (`DB::getDriverName()==='pgsql'`, values from `Enum::cases()`, mirror the catalog migrations); assert `Money`/`TranslatableText`/json columns by key or through the cast (never byte-compare jsonb); name FK/index constraints explicitly and short (PostgreSQL's 63-char identifier limit — `parties_producer_agreements`); named test doubles only for persisted values. **Boundary & persistence law:** all references in this slice are **within Module K** (within-module FKs + relations allowed); every model declares `$table = 'parties_*'` (the auth-principal exemption does NOT apply) — `tests/Architecture/ModuleBoundariesTest.php` and `tests/Architecture/ModulePersistenceConventionsTest.php` must stay green with no amendment. **Scope guard:** NO lifecycle transition, NO approval/activation, NO membership approve/decline, NO `originating_club_id` setter, NO `*Activated`/`OriginatingClubLocked`/lifecycle event; **Supplier and Account record NO event**. Entities are born in their birth state and stay there.

## 1. Foundations — marker ADR + enums (prerequisite, no DB)

- [x] 1.1 **ADR: party-type marker on subtype** (design D1) — write `decisions/2026-06-15-party-type-marker-on-subtype.md` (status active; the standard ADR front-matter + sections) recording the decision to model the party-type marker as an immutable enum on each subtype (Customer/Supplier) and to **defer** the unified `parties_parties` registry, the `third_party_owner` subtype entity, and marker overlap; reference DEC-067 (Party Registry), DEC-073 (representation delegated), §4.4/§4.5, BR-K-Identity-5. Add the `decisions/INDEX.md` row.
  - Acceptance: the ADR exists with the front-matter (`type: decision`, `status: active`, `date: 2026-06-15`) and the Decision/Context/Alternatives/Reasoning/Trade-offs/References sections; `decisions/INDEX.md` lists it; no code/test (docs-only — run lint/format).

- [x] 1.2 **Identity & account enums** (design D1/D2) — `App\Modules\Parties\Enums\{PartyType, CustomerStatus, AccountStatus, AccountType}` (backed string enums, house style): `PartyType` cases `Customer='customer'`, `Supplier='supplier'`, `ThirdPartyOwner='third_party_owner'`; `CustomerStatus` `Pending/Active/Suspended/Closed`; `AccountStatus` `Active/Suspended/Closed`; `AccountType` sole case `Personal='personal'`. No DB.
  - Acceptance: each enum has exactly its spec cases/backing values; `AccountType::cases()` length 1 (the personal-only-at-launch guard); `PartyType` carries all three BR-K-Identity-5 markers though only two are produced this slice; type-clean at PHPStan max; quality commands green.
  - Test hint: `tests/Unit/Modules/Parties/Enums/EnumsTest.php` (no `RefreshDatabase`) — per enum, map `cases()` to `name => value` and assert verbatim + order-sensitive with `->toBe([...])`; add `->toHaveCount(n)` where "exactly N cases" is a spec rule; mirror `tests/Unit/Modules/Catalog/Enums/EnumsTest.php`.

- [x] 1.3 **Registry & membership enums** (design D2) — `App\Modules\Parties\Enums\{ProducerStatus, ClubStatus, ClubRegistrationFlowType, ProducerAgreementStatus, ProfileState}`: `ProducerStatus` `Draft/Active/Retired`; `ClubStatus` `Active/Sunset/Closed`; `ClubRegistrationFlowType` `OpenRegistration/ApplicationWithApproval/InvitationOnly/LinkOnboarding`; `ProducerAgreementStatus` `Draft/Active/Superseded/Terminated`; `ProfileState` `Applied/WaitingList/Approved/Rejected/Active/Suspended/Lapsed/Cancelled/Inactive`. No DB.
  - Acceptance: each enum has exactly its spec cases/backing values (verbatim §4.x state domains); `ProfileState::cases()` length 9; type-clean; quality commands green.
  - Test hint: extend `tests/Unit/Modules/Parties/Enums/EnumsTest.php` with the same verbatim case/value assertions; assert the `ProfileState` terminal set `{rejected, cancelled, inactive}` exists (referenced by the D8 partial index).

## 2. Supply-side registries

- [x] 2.1 **Producer** (design D2/D3/D4/D7/D10; spec — Requirement: Producer Registry) — migration `parties_producers` (`name`, `region`, `appellation` nullable, `country`, `description` json via `TranslatableTextCast` nullable, `website` nullable, `status` string + driver-guarded `CHECK` + `ProducerStatus` cast default `draft`, `version`, `timestampsTz`); model `App\Modules\Parties\Models\Producer`; factory `Database\Factories\Parties\ProducerFactory`; `App\Modules\Parties\Events\ProducerCreated` (`NAME='ProducerCreated'`, `ENTITY_TYPE='Producer'`, static `payload()`); `App\Modules\Parties\Actions\CreateProducer` that inserts + records `ProducerCreated` in one `DB::transaction`.
  - Acceptance: a Producer is created in `draft`; the description round-trips as `TranslatableText` with English fallback; a `ProducerCreated` event is recorded in the same transaction (module `parties`, entityType `Producer`, PII-free payload — no party data beyond ids); spec scenarios "Create a Producer" and "Creating a Producer does not auto-create a Supplier" pass; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/ProducerTest.php` (`uses(RefreshDatabase::class)`) — `CreateProducer` then assert `status === ProducerStatus::Draft`; resolve `description` through the cast and assert the `en` value; query `domain_events` for one `ProducerCreated` row tagged `parties` (payload by key — trap 3); assert no `parties_suppliers` row exists after creating a Producer.

- [ ] 2.2 **Supplier** (design D1/D4/D7/D10; spec — Requirement: Supplier — Minimal Party Subtype) — migration `parties_suppliers` (`legal_name`, `party_type` string + driver-guarded `CHECK` + `PartyType` cast, `timestampsTz` — **no status column, no version**); model `Supplier`; factory; `App\Modules\Parties\Actions\CreateSupplier` that inserts the Supplier (`party_type = supplier`) in one `DB::transaction` and records **NO** event.
  - Acceptance: a Supplier is created carrying the immutable `party_type = supplier` and standard timestamps, with no commercial-terms / link columns; **no** domain event is recorded; creating a Supplier creates no Producer; spec scenarios "Create a minimal Supplier", "Supplier creation records no event" pass; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/SupplierTest.php` — assert `party_type === PartyType::Supplier`; assert `DomainEvent::query()->count() === 0` after creation (the deliberate event silence, D7); assert `Schema::hasColumn('parties_suppliers','status')` is **false** (minimal entity); assert no `parties_producers` row created.

## 3. Producer-dependent structures

- [ ] 3.1 **Club** (design D2/D3/D4/D7/D9; spec — Requirement: Club) — migration `parties_clubs` (`display_name`, `producer_id` `unsignedBigInteger` **FK within module** `->constrained('parties_producers')` with a short explicit index name, `status` string + `CHECK` + `ClubStatus` cast default `active`, `fee` via `MoneyCast` → `fee_minor` int nullable + `fee_currency` string nullable, `registration_flow_type` string + `CHECK` + `ClubRegistrationFlowType` cast, `generates_credit` bool default true, `invite_only` bool default false, `version`, `timestampsTz`); model `Club` (`belongsTo` Producer within module; `fee` cast `MoneyCast`); factory; `App\Modules\Parties\Events\ClubCreated`; `App\Modules\Parties\Actions\CreateClub` that rejects a missing/non-existent `producer_id` (localized `MissingClubProducer` exception) then inserts + records `ClubCreated` in one transaction.
  - Acceptance: a Club is created in `active` for an existing Producer with the fee stored as `fee_minor` + `fee_currency` (a `Money` round-trips, never a float); Club creation with no Producer is rejected; `ClubCreated` recorded (payload carries `producer_id` by id + fee as `{minor_units, currency}`); spec scenarios "Create a Club for a Producer", "Club creation rejects a missing Producer", "The operating-Producer link is immutable" pass; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/ClubTest.php` — build a Producer via factory; create a Club with `Money::of(25000, Currency::of('EUR'))` and assert `club->fee->equals(...)` after re-fetch; missing-producer path asserts the domain exception; assert the `ClubCreated` payload `fee` key is `{minor_units:25000, currency:'EUR'}` by key; assert no setter reassigns `producer_id` in the action surface.

- [ ] 3.2 **ProducerAgreement** (design D2/D3/D4/D7; spec — Requirement: ProducerAgreement) — migration `parties_producer_agreements` (`producer_id` FK within module, `club_id` FK within module **nullable**, `status` string + `CHECK` + `ProducerAgreementStatus` cast default `draft`, `term_start` date nullable, `term_end` date nullable, `settlement_cadence` string nullable, `version`, `timestampsTz` — **explicit short FK/index names**, 63-char limit); model `ProducerAgreement` (`belongsTo` Producer + optional `belongsTo` Club, within module); factory; `App\Modules\Parties\Events\ProducerAgreementCreated`; `App\Modules\Parties\Actions\CreateProducerAgreement` that rejects a missing `producer_id` then inserts + records `ProducerAgreementCreated` (one transaction).
  - Acceptance: a draft agreement is created with a Producer (Club optional) and records `ProducerAgreementCreated`; an agreement with no Producer is rejected; the single-active-per-scope rule is **not** enforced here (drafts create freely — it is an activation-time rule, noted in the spec); spec scenarios "Create a draft ProducerAgreement", "A ProducerAgreement requires a Producer" pass; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/ProducerAgreementTest.php` — create one Producer-wide draft and one Club-narrowed draft and assert both succeed (no single-active rejection in the spine); event row by key (`producer_id`, `club_id` nullable); missing-producer asserts the domain exception.

## 4. Customer + Account

- [ ] 4.1 **Customer + Account** (design D2/D3/D4/D5/D6/D7; spec — Requirements: Customer Identity, Account — Billing Container) — migrations `parties_customers` (`email` **unique**, `name`, `phone` nullable, `date_of_birth` date nullable, `party_type` string + `CHECK` + `PartyType` cast, `preferred_currency` string, `preferred_locale` string, `status` string + `CHECK` + `CustomerStatus` cast default `pending`, `originating_club_id` `unsignedBigInteger` **nullable** FK→`parties_clubs`, `version`, `timestampsTz`) + `parties_accounts` (`customer_id` FK within module, `account_type` string + `CHECK` + `AccountType` cast default `personal`, `name` default `'Personal'`, `status` string + `CHECK` + `AccountStatus` cast default `active`, `default_currency` string, `version`, `timestampsTz`); models `Customer` (`hasOne` Account; `belongsTo` originating Club within module) + `Account` (`belongsTo` Customer); factories; `App\Modules\Parties\Events\CustomerCreated`; `App\Modules\Parties\Exceptions\DuplicateCustomerEmail`; `App\Modules\Parties\Actions\CreateCustomer` that, in one transaction, runs the email pre-check (localized reject), inserts the Customer (`pending`, `party_type=customer`, `originating_club_id` NULL), provisions the 1:1 Account (`active`, `personal`) via the within-module relation, and records **only** `CustomerCreated` (no `AccountCreated`).
  - Acceptance: a Customer is created `pending` with `party_type=customer` and `originating_club_id` NULL; exactly one `active`/`personal` Account is co-provisioned; a duplicate email is rejected, two distinct emails both succeed; **no** mutation path sets `originating_club_id`; `CustomerCreated` recorded with a **PII-free** payload (asserted to contain no `email`/`name`/`phone`/`date_of_birth`); **no** `AccountCreated` event; spec scenarios "Create a Customer", "Duplicate email is rejected", "The Originating Club is born unset and has no mutation surface", "Customer creation provisions one Account", "The Account holds no monetary balance and emits no event" pass; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/CustomerTest.php` + `AccountTest.php` — after `CreateCustomer`, assert one Account row `account_type===Personal`, `status===Active`; assert `customer->originating_club_id` is null; duplicate-email path asserts `DuplicateCustomerEmail`; assert the `CustomerCreated` payload array `->not->toHaveKey('email')`/`'name'`/`'phone'`/`'date_of_birth'` (the PII-free guard, D7); assert exactly one `CustomerCreated` and zero `AccountCreated` in `domain_events`; assert `Schema::hasColumn('parties_accounts', ...)` carries no balance/credit column.

## 5. Profile (multi-profile membership)

- [ ] 5.1 **Profile** (design D2/D3/D4/D8; spec — Requirement: Profile — Multi-Profile Membership) — migration `parties_profiles` (`customer_id` FK within module, `club_id` FK within module, `state` string + `CHECK` + `ProfileState` cast default `applied`, `tier` string nullable, `role` string nullable, `invited_by_customer_id` `unsignedBigInteger` nullable, `version`, `timestampsTz`) **plus a partial unique index** `(customer_id, club_id) WHERE state NOT IN ('rejected','cancelled','inactive')` via raw `DB::statement` (portable PG17 + SQLite — verify both); model `Profile` (`belongsTo` Customer + Club within module); factory; `App\Modules\Parties\Events\ProfileCreated`; `App\Modules\Parties\Exceptions\DuplicateProfileForClub`; `App\Modules\Parties\Actions\CreateProfile` that requires customer_id + club_id, runs the non-terminal-duplicate pre-check (localized reject), inserts (`applied`), and records `ProfileCreated` (one transaction).
  - Acceptance: a Profile is created `applied` referencing exactly one Customer + one Club and records `ProfileCreated`; a Customer gets Profiles in three different Clubs (all succeed — multi-profile); a second Profile for an existing live (Customer, Club) pair is rejected (app pre-check AND the partial unique index); spec scenarios "Create a Profile", "One non-terminal Profile per Customer–Club pair", "A Customer may hold Profiles across many Clubs" pass; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/ProfileTest.php` — one Customer + three Clubs → three Profiles all `applied`; duplicate (customer,club) asserts `DuplicateProfileForClub`; additionally assert the DB partial index bites by inserting a raw duplicate inside a nested `DB::transaction` (savepoint — trap 5) and asserting the unique violation on **both** engines; event row by key.

## 6. Docs, guards & cross-engine close

- [ ] 6.1 **Docs** (design D1/D5/D6/D7/D9) — extend `CONTEXT.md` (root) with the resolved Parties spine glossary terms: Profile (the membership; no Membership entity), Supplier (minimal Party subtype), Club, Account (billing container — not a money ledger), ProducerAgreement, the party-type marker (marker-on-subtype), and the Originating-Club field (born NULL, locked at first approval — deferred). Add a Parties event-contract note documenting the **five** `*Created` payload shapes (PII-free; `CustomerCreated` omits name/email/phone/DOB; `ClubCreated` fee as `{minor_units, currency}`) and the **deliberate Supplier/Account event silence**. No code.
  - Acceptance: `CONTEXT.md` carries the spine terms with the canonical distinctions and the marker-on-subtype note; the event-contract note lists the five `*Created` events + payload keys and states Supplier/Account emit none; `openspec validate parties-core --strict` green; quality commands green (docs-only — run lint/format).

- [ ] 6.2 **Full-chain integration + cross-engine close** (spec — Requirements: Birth States…, Spine Creation Events) — one feature test driving the whole spine: Producer → Club → ProducerAgreement, Customer (+ Account), Profile, Supplier; asserting each evented entity records its `*Created`, Supplier + Account record none, each entity is in its birth state, payloads are PII-free, and **no** lifecycle/`*Activated`/`OriginatingClubLocked` event exists; then run the full Parties suite + the two arch tests on PostgreSQL 17.
  - Acceptance: the chain records exactly the five `*Created` events (Customer/Profile/Producer/Club/ProducerAgreement) and **zero** Supplier/Account events and **zero** `*Activated`/lifecycle events; birth states hold (`pending`/`active`/`draft`/`active`/`draft`/`applied`); the `CustomerCreated` payload is PII-free; `ModuleBoundariesTest` + `ModulePersistenceConventionsTest` green (no amendment); the **entire** Parties test set is green on SQLite **and** on a local PostgreSQL 17 (`knowledge/testing/rules.md` command block); record the PG run in `progress.md`.
  - Test hint: `tests/Feature/Modules/Parties/SpineCreationChainTest.php` — assert `domain_events` contains the five `*Created` names, `where('name','like','%Activated%')->count()===0`, no `OriginatingClubLocked`, and `whereIn('entity_type',['Supplier','Account'])->count()===0`; assert every entity's state equals its birth state; assert the `CustomerCreated` payload has no PII keys. This is the substrate-wiring proof and the cross-engine gate.
