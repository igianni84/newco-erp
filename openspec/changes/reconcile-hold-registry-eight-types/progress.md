# Progress — reconcile-hold-registry-eight-types

## Codebase Patterns
(consolidated reusable patterns — read first each iteration)

- **This change is verify-only (spec reconciliation, design L1).** Only task 1.1 runs the test suite; 1.2/2.1/3.1 are doc-writing (traceability table, fidelity-diff notes, Protected-file flag list) and 2.2 is `openspec validate`. **NO task writes production code, a new test, or a migration** — if one seems to, the premise is wrong (STOP).
- **Run the Hold suite via `php -d memory_limit=2G vendor/bin/pest --filter='…'`, NOT `php artisan test --filter`.** The artisan runner re-spawns a child that ignores `-d`, and this six-class Hold filter exhausts 128M during bootstrap (fatal at `HoldRegistryTest->setUp()`). The pest-direct form with the 2G flag runs the same 71 tests green in ~2s on SQLite. (Refines hot.md's "filtered runs fit 128M" — a *multi-class* filter does **not**.)
- **Scenario→test traceability in a MODIFIED-requirement delta (the 1.2 method):** a MODIFIED requirement reproduces the FULL current text verbatim, so its `#### Scenario:` blocks split into (a) genuinely-changed scenarios (here: the eight-value ones — map to the change's verification classes) and (b) carried-verbatim / count-independent scenarios (audit shape, concurrency, status coupling — map to their PRE-EXISTING green tests, which may live outside the verification filter). The table must cover BOTH; only (a) tests the reconciliation's substance. **Verify the predicate, not just the test:** a scenario can look under-covered by a test's literal dataset yet be fully pinned once you read the code (e.g. console Lift-visibility `!autoLiftable()` at `CustomerHoldsTable.php:311` makes the two new types inherit visibility — a test-only read would false-flag a gap). "Uncovered" (→ add a minimal assertion) means no test AND no verified-predicate entailment.
- **The eight-value contract's assertion points** (the source for task 1.2's scenario→test table): `HoldEnumsTest` (ordered 8-name `->value` map L27-36 + `toHaveCount(8)` L38 + `autoLiftable` partition L85-92) · `HoldRegistryTest` (all-8 manual-place dataset L103-112) · `HoldLifecycleTest` (operator-lift dataset incl. both finance-driven L189-195; auto-managed reject = `kyc`/`payment` only L215-218) · `ComplianceReadApiTest` (all-8 read-API dataset L171-180) · `CustomerAnonymisationHoldPrecedenceTest` (per-type matrix — only `compliance` blocks L85-94; enum-completeness tripwire L96-105) · `CustomerHoldsConsoleTest` (placeHold form options `=== HoldType::cases()` L134-135; "eight Hold types" L121).

---

## [2026-07-03 19:22] — 1.1 Regression-verify the shipped eight-value Hold contract
- **What was implemented:** verify-only. Ran the six-class Hold suite and confirmed every assertion task 1.1 lists is already present at the exact points the deltas assert — none absent, so **nothing added** (acceptance + design D4/L1). ZERO production code, ZERO new test, ZERO migration.
- **Files changed:** `tasks.md` (1.1 → `[x]`), `progress.md`, `hot.md`, `log.md`. No PHP touched; Protected `CLAUDE.md`/`CONTEXT.md` (already modified pre-iteration — Giovanni's hand-off) left untouched and un-staged.
- **Quality loop: green** — `php -d memory_limit=2G vendor/bin/pest --filter='HoldEnums|HoldRegistry|HoldLifecycle|ComplianceReadApi|CustomerAnonymisationHoldPrecedence|CustomerHoldsConsole'` → **71 passed, 399 assertions, ~2.0s, SQLite**. format/type_check/lint N/A (zero PHP touched). PG17 cross-engine deferred to the close ritual (proposal Impact; `knowledge/testing/rules.md`).
- **Assertion inventory (all present verbatim — do not re-add):**
  - `HoldEnumsTest`: eight-name ordered `->value` map (`admin`…`storage_payment_failed`) L27-36; `HoldType::cases()->toHaveCount(8)` L38; `autoLiftable` partition (`kyc`/`payment` true; `admin`/`fraud`/`compliance`/`credit`/`chargeback_review`/`storage_payment_failed` false) L85-92.
  - `HoldRegistryTest`: manual `PlaceHold` dataset over all eight types incl. `chargeback_review` L110 + `storage_payment_failed` L111 (L88-112).
  - `HoldLifecycleTest`: "lifts every operator-liftable Hold type" dataset incl. `chargeback_review` L193 + `storage_payment_failed` L194 (L173-195); "rejects an operator-lift of an auto-managed Hold type" dataset = `kyc`/`payment` only (L197-218).
  - `ComplianceReadApiTest`: "reports each of the eight Hold types … as a not-clear scope" dataset over all eight (L156-180).
  - `CustomerAnonymisationHoldPrecedenceTest`: per-type precedence matrix — `compliance` BLOCKS, both finance-driven types (+ five others) PROCEED (L85-94); enum-completeness tripwire binds the matrix to `HoldType::cases()` (L96-105).
  - `CustomerHoldsConsoleTest`: "exposes the placeHold form with the eight Hold types" — `hold_type` options `array_keys === HoldType::cases()` value map (L121-146).
- **Learnings for future iterations:**
  - Memory: use the 2G pest form (Codebase Patterns above). `php artisan test --filter` on this selection is a 128M fatal, not a suite failure.
  - Task 1.2 (traceability table) can build directly on the assertion inventory above — every `#### Scenario:` in the two delta files maps to one of these datasets; none is expected uncovered (design D4).
---

## [2026-07-03 19:35] — 1.2 Build the scenario→test traceability table
- **What was implemented:** the scenario→test traceability table (below). All **19** `#### Scenario:` blocks across `specs/party-registry/spec.md` (11) + `specs/operator-console/spec.md` (8) map to ≥1 green assertion. **Zero unmapped → nothing added** (design D4/L1: "a new assertion is added only if a modified scenario is found uncovered — none is expected"). Verified the console lift-visibility predicate is `active && ! autoLiftable()` at `CustomerHoldsTable.php:311` (NOT a hardcoded four-type allow-list), so the delta's six-operator-liftable-rows claim (design L3) is faithful to shipped code. ZERO production code, ZERO new test, ZERO migration, ZERO Protected-file edit.
- **Files changed:** `progress.md`, `hot.md`, `log.md`, `tasks.md` (1.2 → `[x]`). No PHP, no spec, no `CLAUDE.md`/`CONTEXT.md`.
- **Quality loop: green** — re-ran the **nine cited test files** (2G pest form): **86 passed, 571 assertions, ~2.9s, SQLite** (`--filter='HoldEnums|HoldRegistry|HoldLifecycle|ComplianceReadApi|CustomerAnonymisationHoldPrecedence|CustomerHoldsConsole|HoldStatusCoupling|CustomerHoldsChain'`). = 1.1's six-class 71 + `HoldStatusCouplingPlaceTest` 6 + `HoldStatusCouplingLiftTest` 8 + `CustomerHoldsChainTest` 1. So every test cited below is green THIS iteration (not merely "green at last full run"). format/type_check/lint N/A (no PHP touched). PG17 cross-engine deferred to the close ritual.

### Traceability — `specs/party-registry/spec.md` (11 scenarios)

| # | Requirement · Scenario | Covering test — file · `it()` · lines | Class |
|---|---|---|---|
| S1 | Hold Registry · The Hold entity carries its type, scope, status and audit metadata | `HoldLifecycleTest` "places an active Hold recording the type, scope, reason and placement actor…" L46–100 (persists `hold_type`/`scope_type`+`scope_id`/`status=active`/placement actor role+id/`created_at`; lift cols null) | 1.1 |
| S2 | Hold Registry · **The eight Hold types and the three scopes are the domain** ⭐ | `HoldEnumsTest` "backs HoldType with the eight spec Hold types" L17–39 (ordered 8-name `->value` map + `toHaveCount(8)`) + "backs HoldScope…" L41–55 + "marks only kyc and payment Hold types auto-liftable" L79–93 (kyc/payment true; the other six incl. chargeback_review/storage_payment_failed false) | 1.1 |
| S3 | Hold Registry · Multiple concurrent Holds may exist on one scope | `HoldRegistryTest` "records multiple concurrent active Holds on one scope and lifting one leaves the others active" L66–86 | 1.1 |
| S4 | Hold Registry · **The registry is trigger-agnostic with a manual-placement path for every type** ⭐ | `HoldRegistryTest` "places a Hold of each of the eight types via the manual operator path" L88–112 (dataset over all 8, incl. `chargeback_review` L110 + `storage_payment_failed` L111; recorded identically) | 1.1 |
| S5 | Hold Lifecycle · Place a Hold records the placement actor, reason and moment | `HoldLifecycleTest` "places an active Hold … records one PII-free CustomerHoldPlaced" L46–100 | 1.1 |
| S6 | Hold Lifecycle · **An operator lifts an operator-liftable Hold** ⭐ | `HoldLifecycleTest` "lifts an active admin Hold…" L120–171 (admin detail) + "lifts every operator-liftable Hold type…" L173–195 (dataset `fraud`/`compliance`/`credit`/`chargeback_review`/`storage_payment_failed`) — the six operator-liftable incl. the two finance-driven | 1.1 |
| S7 | Hold Lifecycle · **Operator-lift of an auto-managed Hold is rejected** ⭐ | `HoldLifecycleTest` "rejects an operator-lift of an auto-managed Hold type…" L197–218 (dataset `kyc`/`payment` → `IllegalHoldLift`, stays active, no lift event) | 1.1 |
| S8 | Hold Lifecycle · Lifting an already-lifted Hold is rejected | `HoldLifecycleTest` "rejects lifting an already-lifted Hold, leaving state and the event log unchanged" L220–239 | 1.1 |
| S9 | Hold-Driven Status Coupling · Placing a Hold on an active scope suspends it | `HoldStatusCouplingPlaceTest` "…active Customer and suspends it, cascading ProfileSuspended…" L64–98 · "…Account Hold on an active Account…" L100–114 · "…Profile Hold on an Active Profile…" L116–132 | coupling |
| S10 | Hold-Driven Status Coupling · A Hold on a non-suspendable scope drives no transition | `HoldStatusCouplingPlaceTest` "records the kyc Hold on a pending Customer but drives no transition" L134–147 · "records a Hold on an Applied Profile but drives no transition" L149–160 · reinforced by `HoldRegistryTest` L137–172 | coupling |
| S11 | Hold-Driven Status Coupling · Lifting the last covering Hold restores; an earlier lift with coverage remaining does not | `HoldStatusCouplingLiftTest` "keeps a Profile suspended while its Customer Hold remains, restoring it when the last covering Hold is lifted" L149–176 (exact match: Profile under own `admin` + cascading Customer Hold) · also "keeps a Customer suspended on an earlier lift…" L105–127 | coupling |

### Traceability — `specs/operator-console/spec.md` (8 scenarios) · Requirement: Operator places and lifts Customer Holds through the console

| # | Scenario | Covering test — file · `it()` · lines | Class |
|---|---|---|---|
| S12 | Placing a Hold records CustomerHoldPlaced with the audit metadata | `CustomerHoldsConsoleTest` "places an admin Hold on an active Customer through the console…" L148–190 (PlaceHold invoked; one `CustomerHoldPlaced` `actor_role: newco_ops` + `actor_id`=operator + entity `Hold` + PII-free) | 1.1 |
| S13 | Placing an operator Hold on an active Customer suspends it | `CustomerHoldsConsoleTest` L148–190 (active → `suspended` + `CustomerHoldPlaced` + `CustomerSuspended`) · `CustomerHoldsChainTest` part (a) L79–101 | 1.1 + chain |
| S14 | A Hold on a non-suspendable scope drives no status transition | `CustomerHoldsConsoleTest` "places an admin Hold on a pending Customer…" L192–217 (stays `pending`; `CustomerHoldPlaced`, no `CustomerSuspended`) · `CustomerHoldsChainTest` part (c) L129–140 | 1.1 + chain |
| S15 | An operator lifts an operator-liftable Hold; the last covering lift restores | `CustomerHoldsConsoleTest` "lifts one of two covering Holds without restoring, then restores the Customer on lifting the last…" L273–321 · `CustomerHoldsChainTest` part (b) L107–127 | 1.1 + chain |
| S16 | **Operator-lift of an auto-managed Hold is not offered and is rejected by the domain** ⭐ | `CustomerHoldsConsoleTest` "hides lift on an auto-managed kyc Hold and the domain independently rejects…" L323–359 (kyc: lift hidden + `LiftHold` throws `IllegalHoldLift`, unchanged, no event) + "shows the per-row lift only on active operator-liftable Holds (admin/fraud/compliance/credit)…" L240–271. Two finance-driven rows show Lift = **entailed** (see Note 1) | 1.1 (+ enum/domain) |
| S17 | Lifting an already-lifted Hold is rejected | `HoldLifecycleTest` "rejects lifting an already-lifted Hold…" L220–239 (domain `IllegalHoldLift` not-active, no event) + `CustomerHoldsConsoleTest` L240–271 (`$lifted` row → lift **HIDDEN**); console surfaces the guard by hiding lift (structural-unreachability, docblock L337–344 / chain L24–31) | 1.1 |
| S18 | A Hold can be placed on each of the three scopes | `CustomerHoldsConsoleTest` "resolves account scope to the Customer Account id…" L219–238 (account → account id) + L148–190 (customer → customer id) + "exposes the placeHold form … three scopes and a profile field gated…" L121–146 (profile scope resolves `profileOptions()`); domain three-scope place `HoldRegistryTest` L114–135. See Note 2 | 1.1 |
| S19 | The Holds table renders the Customer's Holds read-only across scopes | `CustomerHoldsConsoleTest` "renders Holds across the customer, account and profile scopes read-only (no inline edit or delete)" L75–119 | 1.1 |

### Coverage notes (transparency — no gap triggers a new assertion; design D4/L1)
- **Note 1 (S16 — the two finance-driven rows show Lift at the console): entailed, not a dedicated console dataset row.** The widget predicate is `active && ! $hold->hold_type->autoLiftable()` (verified `CustomerHoldsTable.php:311` — *not* a hardcoded `[admin,fraud,compliance,credit]` allow-list); `HoldEnumsTest` L91–92 pins `chargeback_review`/`storage_payment_failed` as `autoLiftable()===false`; `HoldLifecycleTest` L189–195 pins the domain operator-lift of both. So the shipped console DOES show Lift on those two rows (faithful — design L3). `CustomerHoldsConsoleTest` L240–271 pins the four core visible + kyc/payment hidden but does not enumerate the two new types. This is NOT a spec-reconciliation gap (the eight-value contract is pinned at enum + domain, and the console predicate keys on `!autoLiftable()`). **OPTIONAL future hardening** (a non-frozen-spec iteration): extend L240–271's visible-set dataset with `chargeback_review`+`storage_payment_failed` — a one-line change. NOT done here (design L1 verify-only; D4 no redundant test — the scenario is covered).
- **Note 2 (S18 — console profile-scope placement):** customer + account scope placements are **directly asserted** through the console (L148–190, L219–238, resolving `scope_id` from the Customer); the profile leg is covered by the form's `profileOptions()` resolution (L121–146) + the domain three-scope place (`HoldRegistryTest` L114–135), not a dedicated profile `callAction`. ≥1 direct console scope-resolution assertion exists (account, L219–238), so the scenario is mapped.

### Result
**19/19 scenarios mapped; 0 unmapped → 0 new assertions** (design D4 "none is expected" — confirmed). 14 map to the six task-1.1 classes; the 3 party-registry *Hold-Driven Status Coupling* scenarios (S9–S11) map to the dedicated coupling suite (`HoldStatusCouplingPlaceTest`/`LiftTest` — count-independent per proposal D2, so absent from the eight-value 1.1 filter but green in the full suite and re-run green this iteration); `CustomerHoldsChainTest` reinforces S13–S15. The two ⭐ eight-value read/anonymisation anchors in 1.1 (`ComplianceReadApiTest`, `CustomerAnonymisationHoldPrecedenceTest`) pin the eight-value domain at surfaces whose requirements are NOT modified-with-scenarios by this change, so they map to no scenario here — expected (they belong to the un-modified *Hold and Sanctions Read-API* / anonymisation requirements).

- **Learnings for future iterations:**
  - **Two scenario classes in a MODIFIED-requirement delta:** (a) genuinely eight-value scenarios (⭐ — S2/S4/S6/S7/S16) map to the 1.1 eight-value classes; (b) carried-verbatim / count-independent scenarios (audit shape, concurrency, status coupling) map to their pre-existing green tests (coupling suite, chain). Both must appear in the table; only (a) tests the reconciliation's substance.
  - **Verify the predicate, not just the test:** the console Lift-visibility scenario looked under-covered until reading `CustomerHoldsTable.php:311` — the predicate is `!autoLiftable()`, so the two new types inherit visibility. A test-only read (L240–271 lists four types) would have wrongly flagged a gap. Design L3 anticipated this.
  - Task 2.1 (fidelity diff) next: diff each MODIFIED block against live `openspec/specs/**`; only the eight-value tokens (six→eight, the two type names, operator-liftable list 4→6, the DEC-008/`AC-K-FSM-10/11`/`AC-K-EVT-18/19` citations, the "eight types" source notes) may differ.
---

## [2026-07-03 19:44] — 2.1 Fidelity check: diff each MODIFIED requirement vs the live truth-spec
- **What was implemented:** verify-only. Extracted each MODIFIED requirement block by exact name from both the change delta and the live `openspec/specs/**`, then ran a word-level `git diff --no-index --word-diff-regex='[^[:space:]]+'` on all four pairs. **Confirmed the only semantic change in every block is the eight-value reconciliation** (design L2 fidelity gate); no accidental spec edit → nothing reverted. ZERO production code, ZERO new test, ZERO migration, ZERO Protected-file edit. Change surface confirmed = **exactly 4 MODIFIED requirements** (party-registry: 3; operator-console: 1) — no stray ADDED/REMOVED requirement (grep of both delta files: `## MODIFIED Requirements` + only the four named `### Requirement:` headers).
- **Files changed:** `progress.md`, `hot.md`, `log.md`, `tasks.md` (2.1 → `[x]`). No PHP, no spec, no `CLAUDE.md`/`CONTEXT.md`. (Diff scratch under `/tmp/fidelity/`, not committed.)
- **Quality loop: green (N/A for PHP tools)** — the task's "test" is the diff itself. format/test/type_check/lint N/A (zero PHP/code touched; the delta files are the change artifact, unchanged this task). Suite already re-run green at 1.2 (86/86, this session); no code moved since.

### Fidelity-diff result — the complete token-change inventory (design L2)
Each MODIFIED block reproduces the live requirement **verbatim** except the eight-value tokens below. Requirement names match live exactly (party-registry L479/L508/L847; operator-console L550) → MODIFIED will apply cleanly at archive.

1. **Hold Registry** (party-registry) — `six-value`→`**eight-value**` domain; the two type names `chargeback_review`/`storage_payment_failed` added to the domain string, the "eight Hold types" scenario THEN, and the `autoLiftable` false-list; provenance clause naming the two finance-driven types (§4.8.1/§15.8, DEC-168/DEC-160, completed to eight by MVP-DEC-008 + adoption ADR); trigger-agnostic para names the two Module-E signals; "any of the **eight** types"; source note drops the "six types `admin/…/credit`" enumeration and adds §15.8 / AC-K-EVT-18/19 / MVP-DEC-008 / adoption-ADR / AC-K-MVP-2 citations + `AC-K-FSM-10` six→**eight**. **`autoLiftable()` unchanged** (`kyc`/`payment` only). Body (table def, additive-migration idiom, concurrency, registry-of-record) carried verbatim.
2. **Hold Lifecycle and Lift Discipline** (party-registry) — operator-liftable set `admin/fraud/compliance/credit` → +`chargeback_review`/`storage_payment_failed` (both **operator-lift-only at launch**: `chargeback_review` no auto-lift signal; `storage_payment_failed` manual-first D4, its `StoragePaymentSucceeded` per-cycle auto-lift a deferred Module-E seam — design D3/L4); operator-lift scenario enumeration; source note four→**six** operator-lift + `AC-K-FSM-11`/AC-K-EVT-18/19/MVP-DEC-008/DEC-168/adoption-ADR. The `kyc`/`payment` auto-managed rejection body carried verbatim (incl. the live spec's own "wired in this change via *Customer KYC Lifecycle*" phrasing — a live relic, NOT introduced here).
3. **Hold-Driven Status Coupling** (party-registry) — **SOURCE-NOTE-ONLY**, single token `six types;` → `**eight** types — canon MVP-DEC-008;`. Normative body + all **3 scenarios byte-identical** ⇒ acceptance "no normative change beyond its source note" ✓ (design D2 — coupling is count-independent, keys on coverage).
4. **Operator places and lifts Customer Holds through the console** (operator-console) — place-form `six-value`→`eight-value` + two type names + `— canon MVP-DEC-008`; the `HoldType::cases()`-derivation sentence (proposal-scoped mechanism: options derive value-keyed in enum order, so all eight offered); "every one of the **eight** types"; Lift para operator-liftable enumeration +two types + `(the six operator-lift-only types; …operator-lift-only at launch — canon MVP-DEC-008)`; the "no `Lift` on `kyc`" scenario names **six** Lift-exposing rows (design L3 — `admin/fraud/compliance/credit/chargeback_review/storage_payment_failed`); source note six→**eight** + §15.8 + `AC-K-FSM-11` +two types + AC-K-EVT-18/19 + MVP-DEC-008 + adoption ADR. Read-projection / status-coupling-domain-owned body carried verbatim.

**Artifact note (non-issue):** the word-diff hunks for Status Coupling (`@@ -20,5 +20,4 @@`) and Console (`@@ -58,5 +58,4 @@`) show −1 line each — a **trailing-blank-line artifact** of the awk extraction (the live blocks include the blank line that precedes the next `### Requirement:` header; the delta blocks are file-final with no trailing blank). Tail inspection confirmed the `_Source:` line is present and identical in both, bar the eight-value token — **no content dropped**.

- **Learnings for future iterations:**
  - **Fidelity-diff method (the L2 gate):** extract each MODIFIED block by *exact requirement name* (`awk '/^### Requirement: /{p=0} /^### Requirement: <NAME>$/{p=1} p'`) from BOTH the delta and the live `openspec/specs/**`, then `git diff --no-index --word-diff-regex='[^[:space:]]+'`. Word-diff (not line-diff) is what makes "only these tokens moved" auditable. A trailing-blank −1 hunk at the `_Source:` tail is an extraction artifact (live block runs into the next requirement's blank line) — confirm via tail, don't chase it.
  - **In-scope vs accidental edit:** an addition is in-scope if it's (a) a raw six→eight token, (b) one of the two type names, (c) a DEC-008/AC-family citation, (d) the operator-lift-only-at-launch + deferred-seam framing (D3/L4), or (e) the `HoldType::cases()`-derivation sentence — all enumerated in proposal "What Changes". Anything touching an unrelated normative clause (transaction discipline, scopes, coupling semantics) would be accidental → revert. None found.
  - Task 2.2 next: `openspec validate reconcile-hold-registry-eight-types --strict` (already passed at 1.2; re-confirm). Then 3.1 (Protected-file flag list — already drafted in hot.md Blockers + proposal Impact; transcribe into progress.md, edit nothing).
---

## [2026-07-03 19:49] — 2.2 `openspec validate --strict` passes
- **What was implemented:** verify-only. Ran `openspec validate reconcile-hold-registry-eight-types --strict` → **`Change 'reconcile-hold-registry-eight-types' is valid`**, exit 0. The change carries delta specs (party-registry + operator-console), so `--strict` is the mandatory RALPH §d gate — green. ZERO production code, ZERO new test, ZERO migration, ZERO Protected-file edit. Re-confirms the pass first observed at 1.2 (no delta-spec structure has moved since; 2.1 was a read-only diff).
- **Files changed:** `progress.md`, `hot.md`, `log.md`, `tasks.md` (2.2 → `[x]`). No PHP, no spec, no `CLAUDE.md`/`CONTEXT.md`.
- **Quality loop: green (N/A for PHP tools)** — the task's "test" IS the validator. format/test/type_check/lint N/A (zero PHP/code touched; the delta files are unchanged this task). Suite last green at 1.2 (86/86 across the 9 cited Hold files, this session); no code moved since.
- **Acceptance walk (1 bullet):** "green (fix structure until it passes)" → validator returned valid on the first run; no structure fix needed.
- **Learnings for future iterations:**
  - `openspec validate <change> --strict` is the RALPH §d hard gate for any change with delta specs; here it stays green because the deltas are well-formed MODIFIED requirements whose names match live (verified byte-for-byte at 2.1). Nothing to fix.
  - Only **3.1** remains (Protected-file terminology flag list → `progress.md`; edit NO Protected file). After it, all tasks `[x]` → final re-verify → `<promise>CHANGE_COMPLETE</promise>`.
---
