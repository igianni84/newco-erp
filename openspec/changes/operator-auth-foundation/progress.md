# Progress — operator-auth-foundation

## Codebase Patterns
(consolidated reusable patterns — read first each iteration)

- **spatie / `laravel-package-tools` publish tags** — packages built on `spatie/laravel-package-tools` derive their publish tags from `Package::shortName()`, which strips the `laravel-` name prefix. So `spatie/laravel-permission` (named `laravel-permission`) publishes via `--tag=permission-config` and `--tag=permission-migrations` (NOT `laravel-permission-*`). Confirm any spatie tag by reading `->name(...)` in the package's ServiceProvider + `shortName()` in `vendor/.../Package.php` before publishing.
- **Vendor-published migration vs PHPStan-max** — a published third-party migration that resolves its names through Laravel's `config()` helper (typed `mixed`) trips `level: max` with ~dozens of `offsetAccess.nonOffsetAccessible` / `argument.type` errors (spatie's `create_permission_tables` did: 66 of them). Fix: add the **single file** to `excludePaths` in `phpstan.neon` (glob `database/migrations/*_create_permission_tables.php` survives a re-publish timestamp change) — we publish vendor code verbatim and never hand-edit it, and the errors are framework `config()`-is-mixed noise, not a real bug. Scope to the one file so our own migrations stay analysed. Do NOT add `@phpstan-ignore`/casts/`@var` to the vendor file.
- **Adding a new dependency → `docs/development.md` is self-checking** — `tests/Feature/DevelopmentDocsTest.php` cross-checks a fixed package list against `composer.lock`. The established pattern (pennant 2026-06-13, spatie 2026-06-15) when adding a dep: (1) add a row to the "Installed versions" table, (2) add a dated prose paragraph explaining the dep + its ADR authorisation, (3) add the package name to that test's `$packages` array so its pinned version can't drift unnoticed.
- **Cross-engine close recipe** (`knowledge/testing/rules.md`) — `docker run -d --name pg -e POSTGRES_DB=newco_test -e POSTGRES_USER=newco -e POSTGRES_PASSWORD=newco -p 55432:5432 postgres:17`, wait for `pg_isready` (busy-poll loop — foreground `sleep` is sandbox-blocked), then `DB_CONNECTION=pgsql DB_HOST=127.0.0.1 DB_PORT=55432 DB_DATABASE=newco_test DB_USERNAME=newco DB_PASSWORD=newco php artisan test`, then `docker rm -f pg`. phpunit.xml's non-`force` `<env>` lets the CLI env win.
- **Auth-principal models are EXEMPT from the module-table-prefix arch test** (`ModulePersistenceConventionsTest`; design D7; `decisions/2026-06-15-auth-principal-table-naming.md`) — a model implementing `Illuminate\Contracts\Auth\Authenticatable` keeps the framework's flat auth-table name (`Operator` → `operators`, never `operator_panel_operators`). The test skips them via `implementsInterface(Authenticatable::class)` right after the concrete-`Model` guard (the proven-empty `$scannedFiles[]` proof is unaffected — the skip is *after* the scan append). Invariant 10's substance (no cross-module DB access) stays enforced by `ModuleBoundariesTest` + guard-by-name access. Forward-binds the deferred `customer-identity`/`producer-identity` principals; **every non-principal module model still needs its module prefix.**
- **Framework/vendor getters are typed to a CONTRACT, not the concrete class — narrow with a real `instanceof`, never `assert()`/`@var`** (phpstan-max). Manifestations seen: `Auth::guard('operator')` is `Guard|StatefulGuard` and `Auth::createUserProvider('operators')` is `?UserProvider` (so `SessionGuard::getProvider()` / `EloquentUserProvider::getModel()` are off-contract); `Panel::getMultiFactorAuthenticationProviders()` is `array<string, MultiFactorAuthenticationProvider>` (so `AppAuthentication::isRecoverable()` is off-contract). Calling a concrete-only method on the contract trips `method.notFound` **and** breaks Pest's `->and()` template resolution (the off-contract method's return type is unresolved). `expect($x)->toBeInstanceOf(Concrete::class)` does **not** narrow `$x` for a following `->and(...)`. Fixes: (a) assert the runtime type with `toBeInstanceOf(Concrete::class)`; (b) for an extra method/property, pull it to a var and gate with a real `instanceof` — `expect($x instanceof Concrete && $x->method())->toBeTrue()`; (c) where possible prove bindings declaratively (`config('auth.providers.<name>.model')`) and functionally (`actingAs($model,'<guard>')` then `Auth::guard('<guard>')->user() instanceof <Model>` — a stronger proof than introspection). Forward-applies to the deferred customer/producer guard tests + task 4.1's `ActorContext` guard read. **4.1 resolution:** `Auth::guard('operator')->check()` (→`bool`) and `->id()` are BOTH on the `Guard` contract → **no narrowing needed** in production code; but `Guard::id()` is contract-typed `int|string|null`, so coerce a bigint key to `?int` with a real cast — `$id = $guard->id(); return $id === null ? null : (int) $id;` — never `@var`/`assert()`. This is the boundary-clean way to read an operator id; the deferred customer/producer guards copy it verbatim.
- **End-to-end actor-provenance proof** (4.2; the integration shape for any auth principal → recorder pipeline) — to prove a principal's emission carries the right envelope, feed the recorder the values **RESOLVED from `ActorContext`** (`$ctx->role()` / `$ctx->actorId()`), **never literals**, under `actingAs($principal, '<guard>')`; resolve the singleton context *after* `actingAs` so its lazy per-call resolution is exercised. Use **`DatabaseMigrations`, NOT `RefreshDatabase`** (same reason the recorder tests do): the recorder's `DB::transactionLevel() === 0` guard makes the explicit `DB::transaction()` wrapper load-bearing and the emission a REAL commit — under RefreshDatabase the wrapper transaction silently satisfies the guard, hiding a missing wrapper. Use a **synthetic** event name (no registered consumer → the afterCommit hook is a clean no-op; verbatim spec event names are reserved for module changes). Assert **both** the model enum (`->actor_role`) AND the raw stored token (`DB::table('domain_events')->where('id',$id)->value('actor_role') === ActorRole::X->value`) so the persisted value is pinned, not just the hydrated cast; `actor_id` is uncast bigint → `->toEqual()` (loose) spans SQLite/PG. The deferred customer/producer guard slices copy this verbatim (their resolution extends `ActorContext` step 2).

---

## [2026-06-15 14:09] — 4.2 End-to-end recorder proof
- **What:** Proved the operator-action pipeline composes end to end (design D5): an `Operator` authenticated on the `operator` session guard → the `ActorContext` seam (wired in 4.1) → `DomainEventRecorder::record()` → a persisted `domain_events` row carrying (`actor_role = 'newco_ops'`, `actor_id =` the operator id). **Test-only task** — no production code changed; the wiring already existed (4.1 wired the seam; the recorder has always accepted role/id). The unique value over the two halves proven in isolation (`ActorContextTest` resolves NewcoOps+id; `DomainEventRecorderTest` persists whatever it is handed) is the **composition under a live operator session**, feeding the recorder the values RESOLVED from the seam (`$ctx->role()`/`$ctx->actorId()`), never literals — so a mis-wired seam fails this test.
- **Files changed:** `tests/Feature/Modules/OperatorPanel/OperatorActorContextWiringTest.php` (NEW — 2 tests/6 assert), `tasks.md` (4.2 flipped), `progress.md` (this + new Codebase Pattern). No app/config/migration change.
- **Test shape:** `DatabaseMigrations` (NOT RefreshDatabase — see pattern); a shared `recordWithResolvedActor()` helper resolves the singleton `ActorContext` at call time (after `actingAs`, exercising lazy per-call resolution) and records a **synthetic** `'OperatorActorContextDemoRecorded'` event (module `'platform'`, no registered consumer → no fan-out) inside `DB::transaction()`. Test 1: `actingAs(Operator::factory()->create(), 'operator')` → re-read row asserts `actor_role === ActorRole::NewcoOps` (enum cast), the **raw stored token** `=== 'newco_ops'` (literal acceptance, bypassing the cast), and `actor_id ->toEqual($operator->getKey())` (uncast bigint; loose compare spans engines). Test 2 (edge/contrast): no `actingAs` (guest precondition asserted) → `actor_role === System`, `actor_id` null — proves the envelope tracks the *actual* auth state, not a hardcoded role.
- **Quality loop:** green — pint format ✓, filter (2 tests/6 assert) ✓, full suite **349/349 SQLite** (347 + 2 new) ✓, phpstan **0 @ max** ✓, pint --test ✓. **Cross-engine:** confirmed live `DRIVER=pgsql / SERVER=17.10` then full suite **349/349 on PostgreSQL 17** (container removed). `openspec validate operator-auth-foundation --strict` valid.
- **Learnings for future iterations:**
  - **The afterCommit hook is a no-op for a consumer-less synthetic event under `DatabaseMigrations`:** the real commit fires `InlineDeliveryExecutor::deliver([$id])`, but with no consumer registered for the synthetic name `consumersFor()` is empty → zero delivery rows → the hook finds nothing. So an end-to-end actor-provenance proof needs **no** consumer registration (registering one would be scope creep) and stays clean.
  - **Cutover note still active:** the bootstrap `OperatorSeeder` is `User`-based until **5.2**, so any auth-context test in this window builds the principal with `Operator::factory()`, never the seeder (same rule as 3.1/4.1).
  - **Next task 5.1** (`RoleSeeder`): seed `Creator`/`Reviewer`/`Approver` on guard `operator` as **bare** roles (`firstOrCreate`, NO permissions), idempotent. New test `tests/Feature/Modules/OperatorPanel/RoleSeederTest.php` — assert the three role names on `guard_name='operator'`, every role's `permissions` empty, and `Role::count()===3` after a re-run. **Confirm the spatie `Role` FQCN (`Spatie\Permission\Models\Role`) + the `guard_name` usage in `vendor/` first.** Verify on PG17.
---

## [2026-06-15 13:59] — 4.1 Wire `ActorContext` to the operator guard
- **What:** Refactored the `App\Platform\Events\ActorContext` seam (design D5) from a gate-safe in-memory holder into a **lazy, per-call** resolver in precedence: (1) an active `runAs()` override; else (2) an operator authenticated on the `operator` session guard → (`ActorRole::NewcoOps`, the operator id); else (3) (`ActorRole::System`, null). The guard is read **by name** (`Auth::guard('operator')`); the seam imports nothing from `App\Modules\OperatorPanel` (only the `Auth` facade + same-namespace `ActorRole`), so `ModuleBoundariesTest` stays green **unamended**. The override is now a **nullable** field (`?ActorRole $overrideRole`) distinct from the default, so with no override in force steps 2/3 evaluate at `role()`/`actorId()` call time and are never memoised; `runAs()` save/restore (incl. nesting + throw) preserved by saving/restoring the prior override pair. Public API (`role()`/`actorId()`/`runAs()`) unchanged → all 8 Catalog action callers + the singleton binding (`AppServiceProvider:26`) untouched.
- **Files changed:** `app/Platform/Events/ActorContext.php` (refactor + rewritten class docblock — dropped the obsolete "GATE-SAFE / reads NO auth" paragraph), `tests/Feature/Platform/Events/ActorContextTest.php` (**rewritten** — 8 tests/28 assert), `tasks.md` (4.1 flipped), `progress.md` (this + extended contract-typing pattern).
- **Test rewrite:** added `uses(RefreshDatabase::class)` + `Operator::factory()->create()` (a real bigint key is needed so `->id()` is non-null) and `actingAs($operator, 'operator')`. Scenarios: no-auth→`System`/null (step 3); operator-guard→`NewcoOps`+`$operator->id` (step 2); **override beats a live operator session then reverts to the guard** (step 1 + per-call re-resolution proof); singleton; run-as apply/restore; restore-on-throw; nesting; and a **rewritten structural guard** — `not->toUse(['App\Modules','Filament'])` (the OLD test forbade `Auth\*`; that flips because reading auth is now the seam's job — the module/Filament non-import is what stays forbidden).
- **Quality loop:** green — pint format ✓, filter (8 tests/28 assert) ✓, full suite **347/347 SQLite** (was 346 − 7 old + 8 new) ✓, phpstan **0 @ max** ✓, pint --test ✓. **Cross-engine:** confirmed live `DRIVER=pgsql / SERVER=17.10` then full suite **347/347 on PostgreSQL 17** (container removed). `openspec validate operator-auth-foundation --strict` valid.
- **Learnings for future iterations:**
  - **The contract-typing trap is mild for `ActorContext`'s read:** both `Guard::check()` and `Guard::id()` are on the `Illuminate\Contracts\Auth\Guard` contract, so calling them on `Auth::guard('operator')` (`Guard|StatefulGuard`) needs **no** `instanceof` narrowing (unlike `getProvider()`/`getModel()` in 2.3). The only phpstan-max care: `Guard::id()` is `int|string|null` → coerce to `?int` with a real `(int)` cast in the non-null branch (see the consolidated pattern). No `@var`/`assert()`.
  - **Nullable-presence field beats a defaulted field for an override seam:** representing "no override" as `?ActorRole overrideRole === null` (not `= ActorRole::System`) is what lets `runAs(System, null, …)` be distinguished from "unset" — and is exactly what forces lazy guard resolution. The actorId override is plain `?int`; `overrideRole`'s null-ness is the sole presence signal, so `runAs`/restore only need to snapshot the pair.
  - **Cutover note:** `ActorContext` is now wired to the guard, but the bootstrap `OperatorSeeder` is still `User`-based until **5.2** — any new auth-context test in this window must build the principal with `Operator::factory()`, never the seeder (same rule as 3.1).
  - **Next task 4.2** (end-to-end recorder proof): `actingAs(Operator…, 'operator')`, then inside a `DB::transaction` call `DomainEventRecorder::record()` with the `ActorContext`-resolved role/id and a **synthetic** demo event name (NOT a verbatim spec event name — those are reserved for module changes); assert the `domain_events` row's `actor_role = 'newco_ops'` / `actor_id =` the operator id. New test `tests/Feature/Modules/OperatorPanel/OperatorActorContextWiringTest.php`. Verify `DomainEventRecorder::record()`'s exact signature in `app/Platform/Events/` before writing. **Verify on PG17.**
---

## [2026-06-15 13:49] — 3.1 `AdminPanelProvider` → operator guard + reset + opt-in 2FA
- **What:** Cut the `/admin` Filament panel over to the **operator** session guard with login + password reset + opt-in TOTP 2FA (design D2/D3). `AdminPanelProvider`: added `->authGuard('operator')` (after `->path('admin')`), kept `->login()`, added `->passwordReset()`, added `->multiFactorAuthentication([AppAuthentication::make()->recoverable()], isRequired: false)`. Did **not** add `->registration()` or `->emailVerification()` (scope guard). All Filament APIs verified in `vendor/` first (see Learnings).
- **Files changed:** `app/Modules/OperatorPanel/Providers/AdminPanelProvider.php` (import `Filament\Auth\MultiFactor\App\AppAuthentication` + the 3 panel-method calls), `tests/Feature/OperatorPanelTest.php` (**rewritten** for the operator-guard cutover — 7 tests/25 assert), `tasks.md` (3.1 flipped), `progress.md` (this + broadened Codebase Pattern).
- **Test rewrite (cutover-aware):** the old file `seed(OperatorSeeder::class)` for its login tests, but `OperatorSeeder` is still `User`-based until **5.2** — seeding a `User` against the now-`operator` guard would fail. So the login tests build the principal with `Operator::factory()->create(['email'=>…])` (factory password is `'password'`) and authenticate through the **real Filament `Login` page** → `assertAuthenticated('operator')` → `get('/admin')->assertOk()`. Tests: guest→`/admin/login`; operator login happy-path; `actingAs(Operator,'operator')`→dashboard; invalid-creds→`assertGuest('operator')`; `getAuthGuard()==='operator'`; login+password-reset routes present & **register route absent** (`Route::has('filament.admin.auth.register')` false + `hasRegistration()` false); opt-in MFA present (`hasMultiFactorAuthentication()` true, `isMultiFactorAuthenticationRequired()` false, `app` provider is `AppAuthentication` with `isRecoverable()` true).
- **Quality loop:** green — pint format ✓, filter (7 tests/25 assert) ✓, full suite **346/346 SQLite** (343 − 4 old + 7 new) ✓, phpstan **0 @ max** (one fix cycle — see Learnings) ✓, pint --test ✓. **Cross-engine:** full suite **346/346 on PostgreSQL 17** (DRIVER=pgsql printed; container removed). `openspec validate operator-auth-foundation --strict` valid.
- **Learnings for future iterations:**
  - **The phpstan-max contract-typing trap recurs for Filament getters, not just `Auth::*`.** `Panel::getMultiFactorAuthenticationProviders(): array<string, MultiFactorAuthenticationProvider>` is typed to the **contract**, which has no `isRecoverable()` (concrete-only on `AppAuthentication`). `expect($p)->toBeInstanceOf(AppAuthentication::class)` does **not** narrow `$p` for the next `->and(...)`, so `->and($p->isRecoverable())` trips `method.notFound` **and** breaks Pest's `->and()` template resolution. Fix (documented pattern, broadened below): pull the element to a var and narrow with a **real `instanceof`** — `expect($p instanceof AppAuthentication && $p->isRecoverable())->toBeTrue()` — never `assert()`/`@var`.
  - **MFA wiring is two builders, both verified in `vendor/`:** `AppAuthentication::make()` (factory, `app(static::class)`) + `->recoverable(bool=true)` enables recovery codes; the panel method is `multiFactorAuthentication(array|provider|Closure $providers, $setUpRequiredAction=…, bool $isRequired=false)`. `isRequired:false` is the default but I pass it explicitly per task text to pin the opt-in stance. A non-enrolled operator (null secret) hits **no** MFA challenge at login (`AppAuthentication::isEnabled()` = `filled(secret)`), so the happy-path login redirects straight to `/admin`.
  - **Filament auth route names** (from `vendor/filament/filament/routes/web.php`): `filament.{panelId}.auth.login`, `…auth.password-reset.request`, `…auth.password-reset.reset`, `…auth.register` (the register route exists **only if** `hasRegistration()`). Assert absence via `Route::has('filament.admin.auth.register')` false.
  - **Cutover ordering matters for tests:** the panel guard flips at 3.1 but the seeder flips at 5.2 — any test in this window that authenticates must use a **factory `Operator`**, not `OperatorSeeder` (still `User`). `OperatorSeederTest` keeps asserting a `User` until 5.2 (untouched, still green — it never hits the panel).
  - **Next task 4.1** (`ActorContext` → operator guard): refactor to resolve lazily per call — (1) run-as override wins, else (2) `Auth::guard('operator')->check()`→(`NewcoOps`, `->id()`), else (3) (`System`, null); read the guard **by name**, import nothing from `App\Modules\OperatorPanel`; `ModuleBoundariesTest` must stay green **unamended**. REWRITE the gate-safe scenario in `tests/Feature/Platform/Events/ActorContextTest.php` (currently `actingAs(User…)`→`system`). The contract-typing pattern applies to the guard read again.
---

- **What:** Added the `operator` **session guard** + its provider + its password-reset broker to `config/auth.php`, ALONGSIDE the bootstrap `web`/`users`/`User` shell (untouched until cleanup 6.1; cutover discipline D1). `guards.operator = {driver: session, provider: operators}`; `providers.operators = {driver: eloquent, model: env('AUTH_MODEL', Operator::class)}`; `passwords.operators = {provider: operators, table: password_reset_tokens, expire: 60, throttle: 60}`. Imported `App\Modules\OperatorPanel\Models\Operator` (config is platform glue, outside the arch-test scope — D1). App default guard stays `web`, default broker stays `users` (repointed only at 6.1).
- **Files changed:** `config/auth.php` (import + operator guard/provider/broker, all additive), `tests/Feature/Modules/OperatorPanel/OperatorGuardTest.php` (NEW — 7 tests/20 assert), `tasks.md` (2.3 flipped), `progress.md` (this + new Codebase Pattern).
- **Quality loop:** green — pint format ✓, filter (7 tests/20 assert) ✓, full suite **343/343 SQLite** (was 336 + 7 new) ✓, phpstan **0 @ max** ✓, pint --test ✓. **Cross-engine:** full suite **343/343 on PostgreSQL 17** (DRIVER=pgsql printed; container removed). `openspec validate operator-auth-foundation --strict` valid.
- **Learnings for future iterations:**
  - phpstan-max guard/provider **contract-typing** cost one fix cycle — the introspection chain `Auth::guard('operator')->getProvider()->getModel()` does not typecheck (see the new Codebase Pattern). Resolved by `toBeInstanceOf` + config + functional `actingAs` proof.
  - The `operators` provider uses `env('AUTH_MODEL', Operator::class)` **per the task text** — the SAME env var as the `users` provider's `env('AUTH_MODEL', User::class)`. Safe only because `AUTH_MODEL` is unset (verified `.env` / `.env.example` / `phpunit.xml`); if it were ever set it would force BOTH providers to one model. At 6.1 the `users` provider is removed, leaving a single `env('AUTH_MODEL', Operator::class)` — no collision after cleanup.
  - `password_reset_tokens` is shared verbatim by both brokers (D1 retains the generic table at launch). The operator broker uses the **literal** table name (task text), not the `users` broker's `env('AUTH_PASSWORD_RESET_TOKEN_TABLE', …)` indirection.
  - Config-only change but the test does a DB round-trip (`Operator::factory()->create()` + `actingAs`), so PG17 was run per the standing rule — green, no engine divergence (no new schema/SQL beyond the 2.2-proven Operator insert/select).
  - **Next task 3.1** (`AdminPanelProvider` → `->authGuard('operator')` + `->passwordReset()` + opt-in `->multiFactorAuthentication([AppAuthentication::make()…], isRequired: false)`): verify the exact MFA/recovery builder + `isRequired` flag in `vendor/filament` first; MODIFY `tests/Feature/OperatorPanelTest.php` to `actingAs(Operator::factory()->create(), 'operator')` (currently `User::factory()`) and assert no registration route.
---

## [2026-06-15 13:23] — 2.2 RESOLVED (founder chose Option A) — committed
- **The HUMAN_NEEDED blocker is resolved (interactive session, loop was stopped).** Founder authorised **Option A** — exempt auth-principal models from the module-table-prefix convention. The three ready files (`Operator`, `OperatorFactory`, `OperatorTest`) are committed alongside the resolution artifacts; task 2.2 flipped `[x]`.
- **What the resolution touched** (the agent's escalation proposed "test + ADR"; the *faithful* path also refines the truth spec so test & spec can't silently drift — invariant 11):
  - `tests/Architecture/ModulePersistenceConventionsTest.php` — auth-principal skip (`$reflection->implementsInterface(Authenticatable::class) → continue`) placed right after the concrete-`Model` guard; header comment + "domain model" wording updated; `use Illuminate\Contracts\Auth\Authenticatable` added.
  - `openspec/changes/operator-auth-foundation/specs/module-architecture/spec.md` — **NEW MODIFIED delta** refining "Module Persistence Conventions" (domain models prefixed; `Authenticatable` principals exempt) + new scenario "Authentication principal is exempt from the prefix". Truth spec updates on archive.
  - `design.md` **D7** — the carve-out + the rejected B/C alternatives. `decisions/2026-06-15-auth-principal-table-naming.md` — **NEW ADR** (+ INDEX row); forward-binds the customer/producer principals.
- **Why A, not B/C:** B (`operator_panel_operators`) breaks the delta spec + committed 2.1 migration/test + 6.1 (non-idiomatic stutter); C (relocate `Operator` to platform) breaks "owned by OperatorPanel" (D1 + ADR + delta). A honours every pinned authority; the carve-out is narrow (only login principals implement the contract).
- **Quality loop:** GREEN — pint format ✓, former blocker now passes (1/1), full suite **336/336 SQLite** ✓, **336/336 PG17** (DRIVER=pgsql printed, container removed) ✓, phpstan **0 @ max** ✓, pint --test ✓, `openspec validate --strict` valid ✓.
- **Next task: 2.3** (`config/auth.php` — `operator` guard alongside `web`). Suite green + committed → safe to relaunch `./ralph.sh`.
---

## [2026-06-15 11:31] — 2.2 `Operator` model + factory — ⚠ BLOCKED (HUMAN_NEEDED)
- **Status: blocked on a spec-vs-architecture-invariant contradiction the change never anticipated.** The implementation is written and green in isolation (8/8 OperatorTest, phpstan 0 @ max, pint clean), but it trips a *different* architecture law and I will NOT unilaterally weaken an invariant guard. Task **not flipped**, **not committed** (suite is red by one test).
- **What I implemented (correct, in the working tree, uncommitted):**
  - `app/Modules/OperatorPanel/Models/Operator.php` — `extends Authenticatable implements FilamentUser, HasAppAuthentication, HasAppAuthenticationRecovery`; `use HasFactory, HasRoles, Notifiable`; casts `password=>hashed`, `email_verified_at=>datetime`, `app_authentication_secret=>encrypted`, `app_authentication_recovery_codes=>encrypted:array`; the 5 MFA contract methods hand-rolled to the **exact** vendor signatures (`#[SensitiveParameter]`, `save()` inside the setters, `getAppAuthenticationHolderName()=>email`); `canAccessPanel()=>true`; `newFactory()` override (Catalog pattern); full `@property` block (the `encrypted:array` column needs `@property array<string>|null` or larastan infers the schema `string` and the `?array` return mismatches at max).
  - `database/factories/OperatorFactory.php` — flat `Database\Factories\OperatorFactory` (per the task text, mirroring the `UserFactory` it replaces), `protected $model = Operator::class`, memoised hashed password, un-enrolled (2FA cols null).
  - `tests/Feature/Modules/OperatorPanel/OperatorTest.php` — 8 tests: factory persists; `Authenticatable`+`FilamentUser`+`canAccessPanel`; both MFA contracts + `HasRoles` via `class_uses_recursive`; `hashed` cast; un-enrolled nulls; holder name; **encrypted round-trip** for secret + recovery codes incl. ciphertext-at-rest (read raw via `DB::table('operators')->where('id',…)`).
- **THE BLOCKER — irreconcilable, no exemption mechanism exists:**
  - **Spec truth** (delta `operator-identity` *"the operator principal (`Operator` / `operators`)"*; delta `platform`; proposal "What Changes"; the **committed** 2.1 migration `Schema::create('operators')` + its test; task 6.1 acceptance `hasTable('operators')`): the table **is** `operators` and the model **is** `App\Modules\OperatorPanel\Models\Operator`.
  - **Existing invariant** `tests/Architecture/ModulePersistenceConventionsTest.php` (the SQL-visible edge of **Key Invariant 10**, authored in `foundations-modules-skeleton`): EVERY concrete Eloquent model under `app/Modules/**` must declare `$table` starting with `{Module->value}_` → for OperatorPanel that is `operator_panel_`.
  - `Operator` (a `Model` subclass via `Foundation\Auth\User`, in `OperatorPanel/`) therefore **must** be prefixed `operator_panel_…`, but the spec **forbids** any name but `operators`. There is no allow-list / marker / skip in the test. Failing assertion: `App\Modules\OperatorPanel\Models\Operator must declare $table starting with "operator_panel_" (got none)`.
  - The change artifacts enumerated arch-test impact and authorised ONLY `ModuleBoundariesTest` ("stays green with **no amendment**", proposal Impact line); they **never mention** `ModulePersistenceConventionsTest`. Unanticipated.
- **Recommended resolution (for the human): Option A** — amend `ModulePersistenceConventionsTest` to **exempt auth-principal models** (those `isSubclassOf(Illuminate\Foundation\Auth\User)` / implementing `Illuminate\Contracts\Auth\Authenticatable`) from the module-prefix rule, because an auth login-shell deliberately mirrors the framework's flat table naming (`users`→`operators`) and is platform-auth infra accessed only via its guard — NOT a domain aggregate read cross-module, so Invariant 10's *substance* is untouched. Record it in a new ADR (it binds the deferred `customer-identity` / `producer-identity` principals too). **Rejected alternatives:** (B) rename `operator_panel_operators` — violates the delta spec + committed 2.1 migration/test + task 6.1; (C) move `Operator` to `app/Platform/**` — violates "owned by the OperatorPanel module" (delta spec + design D1).
- **Why escalate, not self-resolve:** amending the enforcement of a **Key Invariant**, forward-binding to all future auth principals, unauthorised by the change → a human-in-the-loop architectural decision (+ADR), not a model-task side effect.
- **To resume after a decision:** if Option A is blessed → amend the test (auth-principal skip + comment), write the ADR (`decisions/INDEX.md`), re-run quality + cross-engine (PG17 still owed for 2.2 — the model persists/round-trips need a PG pass), flip 2.2, commit. The three files above are ready.
- **Quality loop:** blocked — format ✓ · OperatorTest 8/8 ✓ · **full suite 335/336 (the one fail = `ModulePersistenceConventionsTest`)** · phpstan 0 @ max ✓ · pint ✓. PG17 not yet run (gated behind the decision).
---

## [2026-06-15 11:10] — 2.1 `operators` table migration
- **What:** New migration `2026_06_15_100000_create_operators_table.php` — the operator login principal, built **alongside** the bootstrap `users` table (cutover discipline D1; `users` is removed only at cleanup 6.1). Columns: `id`, `name`, `email` (unique), `email_verified_at` (nullable, framework-compat/unused — verification deferred), `password`, `app_authentication_secret` (text, nullable), `app_authentication_recovery_codes` (text, nullable), `rememberToken()`, `timestamps()`. Verified the two 2FA column names against Filament's MFA concern traits in `vendor/` before naming (see Learnings). Left the default 0001 migration (`users` + `password_reset_tokens` + `sessions`) untouched.
- **Files changed:** `database/migrations/2026_06_15_100000_create_operators_table.php` (new), `tests/Feature/Modules/OperatorPanel/OperatorsTableMigrationTest.php` (new — 4 tests/7 assert: table exists, principal columns, the two 2FA columns, and the transient `users`/`sessions`/`password_reset_tokens` intact). No composer change (the one authorised dep landed in 1.1).
- **Quality loop:** green — pint format ✓, filter (4 tests/7 assert) ✓, full suite **328/328** (SQLite, was 324 + 4 new) ✓, phpstan **0 @ max** ✓, pint --test ✓. **Cross-engine:** `migrate:fresh` clean on PostgreSQL 17 (DRIVER=pgsql printed; `operators` column listing confirmed = the 10 expected columns) + full suite **328/328 on PG17**, container removed. `openspec validate --strict` valid.
- **Learnings for future iterations:**
  - **The 2FA column names are fixed by Filament's concern traits, not free choice** — `vendor/filament/filament/src/Auth/MultiFactor/App/Concerns/InteractsWithAppAuthentication.php` reads/writes `$this->app_authentication_secret` (merge-cast `encrypted`, merge-hidden) and `InteractsWithAppAuthenticationRecovery.php` uses `$this->app_authentication_recovery_codes` (merge-cast `encrypted:array`, merge-hidden). Task 2.2's model can either implement the contract accessors **by hand** (`getAppAuthenticationSecret(): ?string` etc.) or just `use` those two concern traits (they satisfy the contracts AND auto-merge the encrypted casts + hidden). Either way the column names above are mandatory.
  - **Pint pulls `{@see \Fully\Qualified}` docblock refs into real `use` imports** (`fully_qualified_strict_types` fixer) and keeps them (docblock counts as usage; `no_unused_imports` won't strip them). phpstan-max does NOT flag docblock-only imports. So a `{@see}` cross-reference to a vendor class is fine and survives `pint --test`.
  - `operators` is a plain auth table → no backed-enum columns → **no PG-only CHECK constraints** (unlike the catalog domain migrations). It mirrors the `users` shell: plain `timestamps()`, NOT the domain tables' `timestampsTz()`.
  - Next task **2.2** (`Operator` model + factory): `extends Authenticatable implements FilamentUser, HasAppAuthentication, HasAppAuthenticationRecovery`; `use HasRoles, Notifiable`; casts `password=>hashed`, `email_verified_at=>datetime`, `app_authentication_secret=>encrypted`, `app_authentication_recovery_codes=>encrypted:array`; `canAccessPanel()` → `true`. Contract signatures confirmed in `vendor/` this iter (see the two contract files under `MultiFactor/App/Contracts/`).
---

## [2026-06-15 11:03] — 1.1 Install + configure spatie/laravel-permission
- **What:** Installed `spatie/laravel-permission:^8.0` (resolved **8.0.0**, stable; requires `illuminate ^12|^13` + `php ^8.3` — both satisfied). Published its config (`config/permission.php`, `teams => false` by default — left as-is; operator-scoping will key on `guard_name`) and its migration (`2026_06_15_085700_create_permission_tables.php`). Left the `models.role`/`models.permission` defaults untouched per the task. Added a migration-smoke test asserting the five permission tables (`roles`, `permissions`, `model_has_roles`, `model_has_permissions`, `role_has_permissions`) + their guard-aware columns + teams-off. Recorded the pinned version in `docs/development.md` (table row + prose note + the guarding test's package list).
- **Files changed:** `composer.json` / `composer.lock` (new dep — authorised by the identity/auth ADR), `config/permission.php` (published), `database/migrations/2026_06_15_085700_create_permission_tables.php` (published), `tests/Feature/Modules/OperatorPanel/PermissionTablesMigrationTest.php` (new), `phpstan.neon` (excludePaths for the one published migration), `docs/development.md` (version row + note), `tests/Feature/DevelopmentDocsTest.php` (spatie added to the cross-checked package list).
- **Quality loop:** green — pint format ✓, filter (4 tests/14 assert) ✓, full suite 324/324 (SQLite) ✓, phpstan 0 @ max ✓, pint --test ✓. **Cross-engine:** `migrate:fresh` clean on PostgreSQL 17 + full suite 324/324 on PG17 (DRIVER=pgsql, container removed). `openspec validate --strict` valid.
- **Learnings for future iterations:**
  - The published spatie migration is **excluded from phpstan** (see Codebase Patterns) — the exclusion glob covers a re-published file too; don't be surprised it's not analysed.
  - `config('permission.teams')` is already `false` and `permission.testing` is **absent** (falsy) → with teams off the `roles` table is clean (no `team_id`). The migration's `if ($teams || config('permission.testing'))` SQLite-team-column workaround stays dormant.
  - Next task is **2.1** (the `operators` table migration) — build it **alongside** the existing `users` table (cutover discipline D1); do not touch the `users`/`User` shell until cleanup task 6.1.
---
