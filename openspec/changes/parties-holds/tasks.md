# Tasks — parties-holds

Read `proposal.md`, `design.md` (L1–L8) and the spec delta `specs/party-registry/spec.md` before any task. **Standing rules per task:** the five Quality Commands green at the end (format · test_filter · test · type_check · lint); `openspec validate parties-holds --strict` green; **no protected files touched**; **exactly ONE additive migration** (the new `parties_holds` table — no change to existing tables; PG-only `CHECK (col IN (...))` from `Enum::cases()`); **no new Composer dependency** (`git diff main -- composer.json composer.lock` stays empty). **Verify every framework/vendor API in `vendor/`/`app/` before writing it** (lessons.md) — in particular `DomainEventRecorder::record(...)` returns the `DomainEvent` and takes `?string $correlationId` / `?int $causationId`; `ActorContext::role()`/`actorId()`; the `ActorRole` cast; reuse the `2026_06_15_000005_create_parties_customers_table.php` `CHECK`-from-`Enum::cases()` idiom. **Every DB-touching task is verified on a real PostgreSQL 17 before it is marked done** — SQLite-green is necessary, never sufficient (`knowledge/testing/rules.md`); the polymorphic-scope index + the value-set `CHECK`s MUST be asserted on PG17. **Boundary & persistence law:** every reference is within Module K (no cross-module import/join/event-consume) **except** the read contract, which exposes an interface + a PII-free DTO (never the `Hold` model) for downstream modules; the `Hold` Model stays persistence-only (`$guarded = []`) — the **Action is the sole writer**. **i18n:** new copy goes in `lang/en/parties.php` only (English baseline + per-key fallback — DEC-127); no hardcoded user-facing strings (invariant 12). **Lift discipline:** follow ADR `decisions/2026-06-18-hold-lift-discipline-per-type.md` — `kyc`/`payment` auto-managed (operator lift rejected), `admin`/`fraud`/`compliance`/`credit` operator-lift. **Scope guard:** NO downstream enforcement surface (Module S/C/E consume the read-API later), NO Customer/Account/Profile **status** transition (placing a Hold records the Hold only — no `suspended`), NO automatic `payment`/`fraud`/`compliance`/`credit` trigger, NO Hold-expiry job, NO GDPR-erasure×Hold logic, NO Filament console (all named seams — proposal boundary table).

## 1. Foundations (enums, table, exception)

- [x] 1.1 **Hold enums + the `autoLiftable` predicate** (design L1/L2; spec — Hold Registry, Hold Lifecycle and Lift Discipline) — `App\Modules\Parties\Enums\HoldType` (`Admin`, `Kyc`, `Payment`, `Fraud`, `Compliance`, `Credit`; string-backed, values `admin`/`kyc`/`payment`/`fraud`/`compliance`/`credit`) with `autoLiftable(): bool` returning true for `Kyc`/`Payment` only (the first predicate — mirror `KycStatus::clears()`); `App\Modules\Parties\Enums\HoldScope` (`Customer`, `Account`, `Profile`; values `customer`/`account`/`profile`); `App\Modules\Parties\Enums\HoldStatus` (`Active`, `Lifted`; values `active`/`lifted`). File docblocks cite design L-tag + spec § + the ADR.
  - Acceptance: each enum is string-backed and `->value` equals the spec token; `HoldType::Kyc->autoLiftable()` and `HoldType::Payment->autoLiftable()` are true, the other four false; `from()` round-trips each token and rejects a bogus value; PHPStan max clean; quality commands green.
  - Test hint: `tests/Unit/Modules/Parties/Enums/HoldEnumsTest.php` — assert verbatim, order-sensitive case→value maps + `toHaveCount` per enum; assert the `autoLiftable()` truth table (6 cases); assert `HoldType::from('bogus')` throws `ValueError` (mirror `ComplianceEnumsTest`).

- [x] 1.2 **Additive migration — the `parties_holds` table + `Hold` model + factory** (design L1; spec — Hold Registry) — one migration `database/migrations/2026_06_18_000001_create_parties_holds_table.php`: `id`; `hold_type` (string + `HoldType` cast); `scope_type` (string + `HoldScope` cast); `scope_id` (`unsignedBigInteger`); `status` (string + `HoldStatus` cast, default `active`); `reason` (nullable string); `placed_actor_role` (string + `ActorRole` cast); `placed_actor_id` (nullable `unsignedBigInteger`); `lift_reason` (nullable string); `lifted_actor_role` (nullable string + `ActorRole` cast); `lifted_actor_id` (nullable `unsignedBigInteger`); `lifted_at` (nullable `timestampTz`); `timestampsTz()`. Composite index `(scope_type, scope_id, status)`. PG-only `CHECK` for `hold_type`/`scope_type`/`status` from `Enum::cases()` (reuse the `pgsql` driver-guard idiom; non-nullable `CHECK (col IN (...))`). New `App\Modules\Parties\Models\Hold` (`$table='parties_holds'`, `$guarded=[]`, casts incl. the three enums + `ActorRole` + `lifted_at` `immutable_datetime`, `@property` docblock) + `Database\Factories\Parties\HoldFactory` (default `active`, type `admin`, scope `customer`, a `scope_id`). No DB FK on `scope_id` (polymorphic — L1).
  - Acceptance: migrate fresh on SQLite **and PG17**; a Hold row persists + re-reads with typed enum casts; on PG17 a raw `DB::table('parties_holds')->insert(['hold_type'=>'bogus', …])` is rejected by the `CHECK` and a valid row inserts; the `(scope_type, scope_id, status)` index exists on PG17; `down()` drops the table; `ModulePersistenceConventionsTest` + `ModuleBoundariesTest` stay green (the model is `parties_`-prefixed, within-module); quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/HoldSchemaTest.php` (`uses(RefreshDatabase::class)`) — `Hold::factory()->create()` then re-read asserts the `hold_type`/`scope_type`/`status`/`placed_actor_role` typed casts; on PG17 assert the bogus-`hold_type` `DB::table` insert throws `QueryException` and a valid insert succeeds (inline the savepoint per the per-file-safe idiom in `knowledge/testing/rules.md`).

- [x] 1.3 **`IllegalHoldLift` exception + localized copy** (design L2; spec — Hold Lifecycle and Lift Discipline) — `App\Modules\Parties\Exceptions\IllegalHoldLift extends RuntimeException` with named factories `::autoManaged(HoldType $type)` (operator-lift of a `kyc`/`payment` Hold) and `::notActive(HoldStatus $from)` (lifting an already-`lifted` Hold), each resolving localized copy from a new `hold` group in `lang/en/parties.php` (keys `cannot_lift_auto_managed` with `:type`, `cannot_lift_not_active` with `:state` — business enum values, not PII). Preserve the existing `producer`/`club`/`producer_agreement`/`customer`/`profile`/`kyc`/`sanctions` lang groups.
  - Acceptance: each factory returns the exception naming the rule + the offending `:type`/`:state`; copy lives in `lang/en/parties.php` (no hardcoded strings — invariant 12); `__('parties.hold.cannot_lift_auto_managed', ['type' => 'kyc'])` resolves; the pre-existing groups still resolve; quality commands green.
  - Test hint: `tests/Unit/Modules/Parties/Exceptions/HoldExceptionTest.php` (no DB) — assert each factory returns its class with a non-empty resolved message containing the token; assert the new `hold` keys resolve and a pre-existing key (e.g. `parties.kyc.cannot_verify`) still resolves.

## 2. Hold events

- [x] 2.1 **The two Hold events** (design L4; spec — Hold Events) — `App\Modules\Parties\Events\CustomerHoldPlaced` and `App\Modules\Parties\Events\CustomerHoldLifted`, each a `final` class with untyped `const NAME` (verbatim § 15.1 name), `const ENTITY_TYPE = 'Hold'`, and a static `payload(Hold): array` → `CustomerHoldPlaced` returns `{hold_id, hold_type, scope_type, scope_id, reason}`; `CustomerHoldLifted` returns `{hold_id, hold_type, scope_type, scope_id, lift_reason}` (enum values via `?->value`; PII-free — no name/email/phone/DOB). Mirror the `CustomerOnboardingScreeningPassed` shape (single source of truth for name/entityType/payload).
  - Acceptance: `CustomerHoldPlaced::NAME === 'CustomerHoldPlaced'` and `CustomerHoldLifted::NAME === 'CustomerHoldLifted'` byte-for-byte; `ENTITY_TYPE === 'Hold'`; `payload()` returns exactly the listed keys with no personal data; quality commands green.
  - Test hint: `tests/Unit/Modules/Parties/Events/HoldEventsTest.php` — assert each `NAME`/`ENTITY_TYPE`; build a `Hold`, assert `CustomerHoldPlaced::payload($hold)` keys are exactly `{hold_id, hold_type, scope_type, scope_id, reason}` and contain no PII; same for the lifted event with `lift_reason`.

## 3. Hold lifecycle Actions

- [x] 3.1 **`PlaceHold` Action** (design L3; spec — Hold Lifecycle and Lift Discipline, Hold Events, Hold Registry) — `app/Modules/Parties/Actions/PlaceHold.php` — `handle(HoldType $type, HoldScope $scope, int $scopeId, ?string $reason = null): Hold` — inject `DomainEventRecorder` + `ActorContext`; one `DB::transaction`: create the Hold (`status = active`, `placed_actor_role`/`placed_actor_id` from `ActorContext`, `reason`), then `record(CustomerHoldPlaced::NAME, Module::Parties->value, …, ENTITY_TYPE, (string) $hold->id, payload)` in the same transaction. This is the manual operator-placement path AND the path `RequireKyc` reuses for the auto `kyc` Hold (task 4.1).
  - Acceptance: spec scenario "Place a Hold records the placement actor, reason and moment" passes; an `active` Hold persists with the type/scope/reason + placement actor + `created_at`; exactly one `CustomerHoldPlaced` row in `domain_events` (module `parties`, `entity_type` `Hold`, PII-free payload); quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/HoldLifecycleTest.php` (`uses(RefreshDatabase::class)`) — `app(PlaceHold::class)->handle(HoldType::Admin, HoldScope::Customer, $customer->id, 'manual review')` → assert the persisted Hold fields + one `CustomerHoldPlaced` (`where('name','CustomerHoldPlaced')->count() === 1`) with `entity_type === 'Hold'` and payload keys; assert `ActorRole::System` (no auth) is recorded by default.

- [ ] 3.2 **`LiftHold` Action + the per-type lift discipline** (design L2/L3; spec — Hold Lifecycle and Lift Discipline, Hold Events) — `app/Modules/Parties/Actions/LiftHold.php` — `handle(int $holdId, ?string $reason = null): Hold` — one `DB::transaction`: `lockForUpdate` re-read; if `status !== Active` throw `IllegalHoldLift::notActive($from)`; if `$hold->hold_type->autoLiftable()` throw `IllegalHoldLift::autoManaged($type)` (operator path forbids `kyc`/`payment`); else set `status = lifted`, `lifted_actor_role`/`lifted_actor_id` from `ActorContext`, `lifted_at = CarbonImmutable::now()`, `lift_reason`, and `record(CustomerHoldLifted::NAME, …)` in the same transaction.
  - Acceptance: spec scenarios "An operator lifts an operator-liftable Hold", "Operator-lift of an auto-managed Hold is rejected", and "Lifting an already-lifted Hold is rejected" pass; lifting an `admin`/`fraud`/`compliance`/`credit` Hold sets `lifted` + records one `CustomerHoldLifted`; lifting a `kyc`/`payment` Hold throws `IllegalHoldLift` (no state change, no event); lifting an already-`lifted` Hold throws; quality commands green; **verified on PG17**.
  - Test hint: extend `tests/Feature/Modules/Parties/HoldLifecycleTest.php` — place+lift an `admin` Hold → `status === Lifted`, `lifted_at`/`lifted_actor_role` set, one `CustomerHoldLifted`; parametrize `{Kyc, Payment}` → `LiftHold` throws `IllegalHoldLift` with the `:type` token, Hold stays `active`, zero `CustomerHoldLifted`; double-lift an `admin` Hold → second call throws `::notActive`.

## 4. KYC↔Hold coupling (the one MODIFIED behaviour)

- [ ] 4.1 **Wire the `kyc` Hold onto `RequireKyc` + `RecordKycVerified`; flip the shipped test** (design L7; spec — MODIFIED Requirement: Customer KYC Lifecycle) — modify `app/Modules/Parties/Actions/RequireKyc.php`: inject `PlaceHold` (or recorder+actor), and after the `→ pending` write, place a Customer-scope `kyc` Hold (`reason = null` — L5) in the SAME transaction. Modify `app/Modules/Parties/Actions/RecordKycVerified.php`: inject recorder+actor, and after the `→ verified` write, lift the Customer's active `kyc` Hold(s) (the system path — set `lifted`, record `CustomerHoldLifted`) in the SAME transaction. `RecordKycRejected` is **unchanged** (the Hold remains — § 9.1). Update the docblocks (the shipped "NO Hold is placed … parties-holds owns it" notes flip to describe the coupling). **Replace** the `CustomerKycLifecycleTest` "no Hold" assertions with the coupling assertions.
  - Acceptance: MODIFIED spec scenarios "The kyc_required flag transitions not_required to pending and auto-places the kyc Hold", "Verified auto-lifts the kyc Hold; rejected leaves it in place", and "Illegal KYC transitions are rejected" pass; `RequireKyc` → one `CustomerHoldPlaced` + an `active` `kyc` Hold on the Customer; `RecordKycVerified` → the Hold becomes `lifted` + one `CustomerHoldLifted`; `RecordKycRejected` → the `kyc` Hold stays `active`, no Hold event; the `kyc_status` writes are unchanged; the Customer `status` is still untouched by KYC; quality commands green; **verified on PG17**.
  - Test hint: update `tests/Feature/Modules/Parties/CustomerKycLifecycleTest.php` — the "drives require → verify without placing any kyc Hold" test flips to: after `RequireKyc`, assert one `active` `kyc` Hold + one `CustomerHoldPlaced`; after `RecordKycVerified`, assert the Hold is `lifted` + one `CustomerHoldLifted`; add a case: `RequireKyc` then `RecordKycRejected` → the `kyc` Hold is still `active`, zero `CustomerHoldLifted`. Add `tests/Feature/Modules/Parties/KycHoldCouplingTest.php` if the coupling assertions outgrow the lifecycle file.

## 5. Read-API

- [ ] 5.1 **The `(sanctions_status, active-Hold-list)` read contract + cascade** (design L6; spec — Hold and Sanctions Read-API) — `app/Modules/Parties/Contracts/ComplianceStatus.php` (a readonly DTO: `?SanctionsStatus $sanctionsStatus`, `list<HoldType> $activeHoldTypes`, `isClear(): bool` = `sanctionsStatus === Passed && activeHoldTypes === []`); `app/Modules/Parties/Contracts/PartyComplianceStatusReader.php` (interface: `forCustomer(int $customerId): ComplianceStatus`, `forProfile(int $profileId): ComplianceStatus`); a concrete `app/Modules/Parties/Reads/DatabaseComplianceStatusReader.php` implementing cascade (`forProfile` ∪ parent-Customer active Holds + the parent Customer's `sanctions_status` — BR-K-Hold-3; `forCustomer` returns Customer-scope active Holds) bound in `PartiesServiceProvider`. The DTO carries `HoldType`s, NEVER the `Hold` model (boundary law). **No** downstream enforcement surface (scope guard).
  - Acceptance: spec scenarios "The read-API returns the sanctions/Hold tuple for a Customer", "A Customer-scope Hold cascades to the Customer's Profiles", "A Profile-scope Hold isolates to that Profile", and "The contract returns a PII-free DTO, not a model" pass; `app(PartyComplianceStatusReader::class)` resolves the bound implementation; the DTO exposes no `Hold` model; `ModuleBoundariesTest` green; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/ComplianceReadApiTest.php` (`uses(RefreshDatabase::class)`) — a sanctions-`passed` Customer with no Hold → `forCustomer()->isClear()` true; place a Customer-scope `fraud` Hold → `forCustomer()` lists `Fraud` and is not clear, AND `forProfile()` for each of that Customer's Profiles lists `Fraud` (cascade); place a Profile-scope `payment` Hold on Profile X → `forProfile(X)` lists `Payment` but `forProfile(Y)` (sibling) does not; assert the returned type is the DTO, not `Hold`.

## 6. Registry behaviour, docs, and cross-engine close

- [ ] 6.1 **Hold registry behaviour + scope-guard test** (design L1/L3; spec — Hold Registry, MODIFIED Birth States) — one feature test pinning the registry invariants: multiple concurrent active Holds on one scope (BR-K-Hold-1 — place two, both `active`, lift one → the other remains); the six types × three scopes are all placeable (trigger-agnostic manual path — AC-K-MVP-2); and the **scope guards** hold — placing/lifting a Hold performs **no** Customer/Account/Profile **status** transition (the Hold→`suspended` coupling is deferred), and no demand-side status event is recorded.
  - Acceptance: the MODIFIED *Birth States* scenario "Supply-side, compliance and Hold transitions exist; demand-side status transitions do not" holds (the Hold registry exposes place/lift; placing a Hold leaves the Customer `status` = `pending`); BR-K-Hold-1 multiple-holds passes; placing each of the six types succeeds; no `CustomerActivated`/`ProfileActivated`/`OriginatingClubLocked` in `domain_events`; `SpineCreationChainTest` stays green unamended; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/HoldRegistryTest.php` — place a `kyc` (via `RequireKyc`) AND an `admin` Hold on one Customer → both `active`; lift the `admin` → the `kyc` remains `active`; loop the six `HoldType` cases through `PlaceHold` (asserting a row each); assert the Customer `status` column is unchanged by any place/lift and no demand-side status event exists.

- [ ] 6.2 **Docs — CONTEXT.md Hold terms + Parties Hold contract note** (proposal "What Changes"/Impact) — extend `CONTEXT.md` with the resolved Hold terms (the six `HoldType`s; the three `HoldScope`s + the Customer→Profile cascade / Profile isolation; the per-type lift discipline with the ADR pointer; the `(sanctions_status, active-Hold-list)` read-API tuple; `reason` is a controlled business value, system holds null-reason) and add a Parties Hold contract note documenting the two event payloads (`CustomerHoldPlaced`/`CustomerHoldLifted`, PII-free), the `PartyComplianceStatusReader` contract, and the deferred seams (downstream enforcement → S/C/E; `payment`/`fraud`/`compliance`/`credit` auto-triggers + the finance subtypes → Module E; Hold→suspension → `parties-membership-lifecycle`; Hold expiry; GDPR×Hold; Filament console → `parties-operator-console`). Cross-reference ADR `2026-06-18-hold-lift-discipline-per-type.md`. No code.
  - Acceptance: `CONTEXT.md` carries the new terms with verbatim spec anchors (§ 4.8 / § 4.8.1 / § 15.1 / DEC-160 / DEC-181); the contract note lists the two events + payloads + the read contract + the seams; no canonical-term drift from `CONTEXT.md`/CLAUDE.md; `openspec validate parties-holds --strict` green; quality commands green.
  - Test hint: docs-only — no test; verify by a terminology re-read against the spec + `openspec validate`.

- [ ] 6.3 **Full Hold chain + cross-engine PG17 close** (spec — all ADDED + both MODIFIED requirements) — one feature test driving the whole slice: create a Customer (+ Profiles) → `RequireKyc` (assert the `kyc` Hold auto-placed + `CustomerHoldPlaced`) → `PlaceHold` an `admin` Hold → query the read-API (not clear; cascade to Profiles) → `LiftHold` the `admin` Hold (assert `CustomerHoldLifted`; assert lifting the `kyc` Hold via the operator path is rejected) → `RecordKycVerified` (assert the `kyc` Hold auto-lifts) → read-API clear once sanctions `passed`. Then run the entire Parties suite + the two arch tests on **PostgreSQL 17**.
  - Acceptance: the chain records exactly the expected `CustomerHoldPlaced`/`CustomerHoldLifted` names (with the expected counts; no invented Hold event), the lift discipline holds (operator-lift of the `kyc` Hold rejected; `admin` lift succeeds; `kyc` auto-lifts on verify), the read-API tuple + cascade are correct, and no demand-side status transition occurs; the **entire** Parties test set is green on SQLite **and** on a local PostgreSQL 17 (docker `postgres:17` per `knowledge/testing/rules.md`), including the polymorphic-scope index + value-set `CHECK`s; record the PG run in `progress.md`.
  - Test hint: `tests/Feature/Modules/Parties/HoldChainTest.php` — assert `domain_events` contains only the expected `CustomerHoldPlaced`/`CustomerHoldLifted` (zero other Hold names, zero KYC names) with the expected counts; assert the read-API `isClear()` flips across the chain; run `php -d memory_limit=512M vendor/bin/pest` against PG17 and confirm green.
