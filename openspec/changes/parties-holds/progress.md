# Progress — parties-holds

## Codebase Patterns
(consolidated reusable patterns — read first each iteration; seeded at authoring from the archived `2026-06-17-parties-compliance/progress.md` — read that file's full pattern library too, the Parties idioms carry over verbatim)

- **Carryover from parties-compliance (apply as-is).** Enum house style (string-backed, PascalCase cases, persisted-token values, the predicate is the first method, docblock cites design L + spec §; test pins verbatim order-sensitive case→value maps + `toHaveCount` + a `from()` `ValueError`). Quality runners: full suite `php -d memory_limit=512M vendor/bin/pest` (plain `php artisan test` OOMs at 128M); `vendor/bin/phpstan analyse --no-progress` (level max, 0 errors); `vendor/bin/pint` / `--test`; `openspec validate parties-holds --strict` is a standing per-task gate. `timestampTz` casts `immutable_datetime`; assign `CarbonImmutable::now()` in tests, never `now()`. PG-only-CHECK test is per-file-safe (inline the savepoint; branch on `DB::getDriverName()` — pgsql asserts `toContain('<constraint>')`, sqlite asserts the cast is the floor; never a vacuous skip). A compliance change's PG17 close runs `tests/Feature/Modules/Parties` + `tests/Architecture` together.
- **The migration idiom (this change's is NON-nullable CHECK — SHIPPED in 1.2).** `2026_06_18_000001_create_parties_holds_table.php` is a `Schema::create` (a NEW table — contrast parties-compliance's `Schema::table` additive columns), so the PG-only CHECK is the plain `CHECK (col IN (<Enum::cases()>))` of `2026_06_15_000005_create_parties_customers_table.php` (NOT the nullable `col IS NULL OR …` form — `hold_type`/`scope_type`/`status` are NOT NULL). The three CHECKs are emitted in ONE `foreach` over a `/** @var array<string, list<BackedEnum>> */ $valueSets = ['hold_type'=>HoldType::cases(), …]` map, with the constraint name derived from the column (`parties_holds_{$column}_check`) — DRY and PHPStan-max clean (the `@var` carries the heterogeneous-enum value type). `BackedEnum` is unqualified (global) in the no-namespace migration file; Pint keeps it. `scope_id` is a plain `unsignedBigInteger` with NO DB FK (polymorphic 3-way scope — design L1); the integrity is the Action's (`PlaceHold` resolves the scoped entity in-transaction). Composite index named explicitly `parties_holds_scope_status_index` on `(scope_type, scope_id, status)`. **PG17-proven** (catalog inspection: 3 named CHECKs with verbatim value-sets + the composite btree index; `migrate:fresh` + `migrate:rollback` both clean).
- **Pint `{@see \FQCN}` forward-ref trap (re-confirmed 1.2; the `knowledge/laravel/rules.md` rule).** A model/file docblock that references a class created in a LATER task (here the `Hold` model citing `PlaceHold`/`LiftHold`/`CustomerHoldPlaced`/`CustomerHoldLifted`, built in 3.1/3.2/2.1) must use **plain backticks**, NOT `{@see \FQCN}` — Pint's `fully_qualified_strict_types` auto-imports the FQCN into a `use`, and a `use` of a not-yet-existing class reds PHPStan. Concrete EXISTING classes (`HoldType`, `ActorRole`) keep `{@see}` fine. Eyeball the post-Pint `use` block on any rich-docblock file.
- **Evented-Action idiom (PlaceHold SHIPPED 3.1 / LiftHold SHIPPED 3.2 — design L3).** Both EMIT, so both inject `DomainEventRecorder` + `ActorContext` (the `RecordCustomerScreening` shape — contrast the eventless KYC Actions which inject nothing *today*; this change adds the recorder to `RequireKyc`/`RecordKycVerified` for the coupling). `record(NAME, Module::Parties->value, $actorRole, $actorId, ENTITY_TYPE, (string) $hold->id, payload)` runs INSIDE the Action's `DB::transaction` (the recorder's open-transaction guard makes write+emit atomic). **Resolve the actor ONCE per Action** (`$actorRole = $this->actor->role(); $actorId = $this->actor->actorId();`) into locals — PlaceHold stamps them onto BOTH the `placed_actor_*` columns AND the event envelope, so a single resolution keeps the row and its event from ever disagreeing (the seam resolves lazily per call, so two reads *could* differ under a `runAs` boundary). PlaceHold sets `status = HoldStatus::Active` EXPLICITLY on create (so the returned model reads `active` in-memory, not relying on the column default) and passes no causation/correlation → ROOT event. `LiftHold` re-reads `lockForUpdate` then guards `status` + `autoLiftable()`. Capture ONE `CarbonImmutable::now()` for `lifted_at`.
- **Lift discipline lives on the type (design L2; ADR 2026-06-18).** `HoldType::autoLiftable()` true for `Kyc`/`Payment` only (the first predicate — mirror `KycStatus::clears()`). `LiftHold` (operator path) rejects `autoLiftable()` types via `IllegalHoldLift::autoManaged($type)`; the `kyc` auto-lift is a SEPARATE within-module path in `RecordKycVerified` (it lifts the system-managed Hold the operator path forbids). Operator-lift of `kyc`/`payment` is rejected; `admin`/`fraud`/`compliance`/`credit` lift freely; already-`lifted` → `IllegalHoldLift::notActive`.
- **Event class idiom (design L4 — SHIPPED in 2.1).** `final class` (no base/interface), untyped `const NAME` (verbatim § 15.1 — `CustomerHoldPlaced`/`CustomerHoldLifted`), `const ENTITY_TYPE = 'Hold'` (the Hold is the subject; the scope is in the payload), static `payload(Hold): array` PII-free. **`->value` vs `?->value` is decided by the model's `@property` nullability, NOT a blanket rule:** `Hold::hold_type`/`scope_type`/`status` are NON-nullable (`@property HoldType $hold_type`) → plain `->value` (a `?->value` here trips PHPStan-max `nullsafe.neverNull`); a NULLABLE enum prop (`Customer::sanctions_status`, `@property ...|null`) → bare `?->value` (and never combine `?->` with a `?? fallback`). `reason`/`lift_reason` are nullable STRINGS, not enums → emit the value directly, no `->value`. Assert `NAME` byte-for-byte via the concrete class ref, never a `$class::NAME` on a string var. The two names cover ALL scopes (the § 15.1 catalog names no Profile/Account variant — design L4). No central event-name registry exists and no arch test globs the Events dir — a new event class is purely additive; the recorder wiring is pinned at the EMIT site (3.x/4.1).
- **`reason` PII discipline (design L5).** `reason`/`lift_reason` are controlled business strings; **system-placed Holds carry `reason = null`** (the auto `kyc` Hold — the type is the reason; keeps i18n clean, no hardcoded reason string). The event payload carries `reason` (null for system holds). If review prefers, `reason` can drop from the payload (stays on the entity) — flagged at the gate.
- **KYC coupling is the one MODIFIED behaviour (design L7 — SHIPPED 4.1).** ASYMMETRIC by design: `RequireKyc` injects `PlaceHold` and (after `→ pending`) calls `$this->placeHold->handle(Kyc, Customer, customerId)` — one Action reusing another, the `RetireProducer → SunsetClub` precedent (PlaceHold's nested `DB::transaction` is a savepoint → atomic). `RecordKycVerified` CANNOT reuse `LiftHold` (the operator path rejects `kyc` via `autoManaged`), so it injects `DomainEventRecorder` + `ActorContext` and INLINES a system-lift: resolve actor+`CarbonImmutable::now()` once, `Hold::query()->where('scope_type', HoldScope::Customer->value)->where('scope_id', …)->where('hold_type', HoldType::Kyc->value)->where('status', HoldStatus::Active->value)->lockForUpdate()->get()`, then per Hold `update([lifted, lifted_actor_*, lifted_at, lift_reason=>null])` + `record(CustomerHoldLifted::NAME, …)` IN-TX. (Loops over active `kyc` Hold(s) — BR-K-Hold-1 permits multiples.) `RecordKycRejected` UNCHANGED (Hold remains — § 9.1). **Enum-where uses `Enum::Case->value`** (the `RetireProducer` precedent, line 88 — NOT the bare enum). **THREE shipped test files flipped, not two** (the 3.x handoff named only `CustomerKycLifecycleTest` + `ComplianceIndependenceTest`): `ComplianceChainTest`'s `runComplianceChain()` ALSO drives `RequireKyc`+`RecordKycVerified` and its exact-count test (`count()===3`, `pluck unique`, `%Hold%===0`) breaks → its expected set gains both Hold events (now 5 events, `%Hold%===2`). The `%Kyc%===0` assertions all STAY (the Hold events are named `CustomerHold*`, not `*Kyc*` — KYC itself is still event-silent). No `lang/` copy added (system holds carry null reason).
- **Read contract = interface + DTO + bound impl (design L6 — SHIPPED 5.1).** `app/Modules/Parties/Contracts/{PartyComplianceStatusReader (interface), ComplianceStatus (DTO)}` + `app/Modules/Parties/Reads/DatabaseComplianceStatusReader` bound in `PartiesServiceProvider::register` via **`$this->app->bind(Interface::class, Concrete::class)`** (the reader is stateless — no ctor deps — so a plain `bind`, fresh-per-resolve, not `singleton`; AppServiceProvider's `singleton`s are for the shared-state seams). `Contracts\*` IS the module public surface (`ModuleBoundariesTest`) — no arch rule forces it to be interfaces-only, so a DTO class lives there cleanly; `Reads\` is internal (a NEW subdir under Parties is invisible to `ModuleConformanceTest`, which only checks the modules ROOT). DTO is the codebase value-object idiom (`Money`/`DualCurrencyAmount`): plain `class` with promoted `public readonly` props (NOT `final`/`readonly class`); carries `?SanctionsStatus` + `list<HoldType>` (NEVER the `Hold` model — boundary law) + `isClear()` = `sanctions === Passed && activeHoldTypes === []` (NULL/non-passed sanctions ⇒ not clear). **Cascade resolves at READ, one query** (`forProfile` = profile-scope ∪ parent-Customer-scope active holds + parent Customer's `sanctions_status`; `forCustomer` = customer-scope only; Account-scope NOT cascaded — design L6 risk note): an OR-group of `(scope_type, scope_id)` pairs under `status = active`, typed `@param non-empty-list<array{0: HoldScope, 1: int}>`, nested where closures typed bare **`Builder $query`** (the `InlineDeliveryExecutor` precedent — no generic needed at PHPStan max). Enum WHERE uses `Enum::Case->value`. **Distinct-types dedup**: `->get()->map(fn(Hold): HoldType)->unique(fn(HoldType): string => $type->value)` then **`array_values($coll->all())`** — `array_values` is what gives PHPStan a `list<HoldType>` (a bare `->all()` is `array<int,…>`, which trips a `list<>` return type). This is the one surface other modules import — but they don't yet (Module S/C/E are stubs); the test drives it directly via `app(PartyComplianceStatusReader::class)`.
- **Scope-guard whitelist GROWS (PlaceHold added 3.1, LiftHold added 3.2 — both SHIPPED).** `SupplyLifecycleChainTest`'s exact-set "only these non-Create transition Actions exist" `toEqualCanonicalizing([...])` is the **ONLY** exact-set whitelist (grep-confirmed) and the canonical home — a new non-Create Action there means RED unless declared. SHIPPED: `$holdTransitions = ['PlaceHold', 'LiftHold']` spread into the expected set (both Hold Actions now declared; the slice adds no further Action). `ComplianceIndependenceTest` ALSO globs `Actions/*.php` but uses a forbidden-NAME negative check (`not->toContain('ActivateCustomer'/…)`) — `PlaceHold`/`LiftHold` are neither asserted-present nor forbidden, so it needs **no** change. The guard's intent (no demand-side **status** transition Action: no `ActivateCustomer`/`SuspendAccount`/…) is preserved — placing a Hold writes no status. **4.1 TIME-BOMB:** `ComplianceIndependenceTest`'s `where('name','like','%Hold%')->count() === 0` (and `CustomerKycLifecycleTest`'s) stays green for 3.x because 3.x only DEFINES the Actions; the coupling (4.1) wiring `RequireKyc`/`RecordKycVerified` to place/lift the `kyc` Hold is what flips BOTH those assertions — 4.1 must update both files.
- **CONTEXT.md docs convention (every slice's `6.x` docs task — SHIPPED 6.2).** Three rules, all precedent-backed. (1) **Glossary TERMS group by domain theme, not by module:** the Hold cluster lives under `## Compliance & Finance` (beside KYC/Sanctions — where `parties-compliance` filed the compliance terms), NOT under `## Parties`. (2) **The EVENT-PAYLOAD CONTRACT is a `### <Module> <thing> events — payload contract` subsection** under the owning module's `##` heading (the Hold one is the **5th** such under `## Parties (Party Registry)`, after spine-creation / supply-side / compliance-screening) — a markdown table `| Event (name) | entity_type | Payload keys |` whose keys are copied VERBATIM from the event class's `payload()` (read the source — `CustomerHoldPlaced`/`CustomerHoldLifted` carry `{hold_id, hold_type, scope_type, scope_id, reason|lift_reason}`, `entity_type = 'Hold'`). (3) **When a slice CLOSES a previously-deferred seam, mark the old seam bullet `(**now landed**)`** rather than deleting it (the catalog `KYC-on-activation → parties-compliance (**now landed**)` precedent) — keeps the seam-count header honest and the history legible. And **REFINE, never silently contradict, a Protected-file statement**: CLAUDE.md Invariant #7 ("Holds are never auto-lifted") is over-broad, so the lift-discipline term says so and points to ADR `2026-06-18-hold-lift-discipline-per-type` as the authority. A docs task touches NO DB → no PG17 run; quality = Pint/PHPStan/full-suite unchanged (a `.md` edit cannot move PHP gates) + `openspec validate`.

## Iteration Log
(append one entry per ralph iteration: task id → what shipped → quality/PG17 result → any new pattern)

## [2026-06-18 11:33] — 6.2 Docs — CONTEXT.md Hold terms + Parties Hold contract note
- DOCS-ONLY task (no code, no test, no DB). Extended `CONTEXT.md` with the resolved Hold vocabulary + the Parties Hold contract note. Five surgical edits:
  - (1) **Parties intro** — added the `parties-holds` Hold registry to the "implemented by" list alongside `parties-producer-lifecycle` / `parties-compliance`.
  - (2) **Replaced the stale `**Hold**` seed** (under `## Compliance & Finance` — "6 types: chargeback, storage non-payment, KYC, sanctions, …" + "Never auto-lifted", both wrong) with a six-term cluster: **Hold** (registry-of-record, Hold-blind §9.3, no `status` transition), **Hold type** (verbatim six `admin/kyc/payment/fraud/compliance/credit`; finance subtypes `STORAGE_PAYMENT_FAILED`/`CHARGEBACK_REVIEW` MAP to `payment`/`fraud`, not new values), **Hold scope** (`customer/account/profile`; Customer→Profile cascade, Profile isolation, Account-scope placeable-but-not-cascaded), **Hold-lift discipline (per type)** (`autoLiftable()` kyc/payment; refines CLAUDE.md Invariant #7 → ADR), **Hold `reason`** (controlled business string, system holds null-reason), **Compliance read-API** (the `(sanctions_status, active-Hold-list)` tuple).
  - (3) **Augmented the `**KYC lifecycle**` entry** with the now-landed `kyc`-Hold coupling — precise: KYC still records no *KYC* event; the coupled events are the Hold events.
  - (4) **Marked the `kyc` Hold coupling deferred-seam** (under *Parties compliance screening events*) "(**now landed**)" — the catalog `KYC-on-activation (**now landed**)` precedent.
  - (5) **Added the `### Parties Hold events — payload contract` subsection** (the 5th Parties event-contract subsection): the two events + the PII-free payload table (`entity_type = Hold`), the read-contract note (`PartyComplianceStatusReader`/`ComplianceStatus`/`DatabaseComplianceStatusReader`, DEC-181, cascade), and six deferred Hold seams (downstream enforcement → S/C/E; auto-triggers + finance subtypes → E; Hold→`suspended` → membership-lifecycle; Hold expiry; GDPR×Hold; Filament console → operator-console).
- Files changed: `CONTEXT.md` (+45/-5), `openspec/changes/parties-holds/tasks.md` (6.2 flipped). No PHP, no migration, no `lang/`, no composer drift, no protected files.
- Quality loop: **green** — Pint --test passed · PHPStan max 0 · full suite **781/781** (3664 assertions, unchanged — docs-only cannot move tests) · `openspec validate parties-holds --strict` valid. **No PG17 run** (no DB touched — the one task in the slice that touches no database).
- All canonical names verified against SOURCE (the events/enums/contracts read directly, not trusted from progress notes) before documenting — no term drift.
- **Learnings for future iterations:**
  - **CONTEXT.md docs convention** (consolidated in Codebase Patterns above) — glossary terms group by domain theme; event-payload contract is a `### <Module> … events — payload contract` subsection under the owning `##`; closed seams get `(**now landed**)`, not deletion.
  - **Refine, don't silently contradict, a Protected-file statement.** CLAUDE.md Invariant #7 ("Holds are never auto-lifted") is over-broad; the Hold-lift-discipline entry says so explicitly and names the ADR as the standing authority — CONTEXT.md and a Protected CLAUDE.md line must never sit in unacknowledged conflict.
  - **Next (6.3 — the LAST task):** full Hold-chain feature test (`HoldChainTest`) driving create → `RequireKyc` (kyc Hold auto-placed) → `PlaceHold` admin → read-API not-clear + cascade → `LiftHold` admin (+ operator-lift-of-kyc rejected) → `RecordKycVerified` (kyc auto-lifts) → read-API clear once sanctions `passed`; assert `domain_events` carries ONLY the expected `CustomerHoldPlaced`/`CustomerHoldLifted` counts. Then the ENTIRE Parties suite + the two arch tests on PG17 → `<promise>CHANGE_COMPLETE</promise>`.
---

## [2026-06-18 11:25] — 6.1 Hold registry behaviour + scope-guard test
- TEST-ONLY task pinning the emergent registry contract (design L1/L3; party-registry — Hold Registry + the MODIFIED Birth States scope guard). New `tests/Feature/Modules/Parties/HoldRegistryTest.php` (4 `it` blocks → 9 cases): (1) BR-K-Hold-1 multiple concurrent active Holds on one Customer scope — `kyc` (via `RequireKyc`) + `admin` (via `PlaceHold`) both `active`, lift the `admin` → the `kyc` remains `active` (any one blocks independently); (2) the six `HoldType`s each placeable via the manual operator path (parametrized, AC-K-MVP-2 trigger-agnostic); (3) all three `HoldScope`s placeable with realistic scoped entities (Customer/Account/Profile); (4) the SCOPE GUARD — drive `RequireKyc` + `PlaceHold`×3 (customer/account/profile) + `LiftHold`×1, then assert every scope entity stays in its birth state (`pending`/`active`/`applied`, re-read), the four demand-side status names (`CustomerActivated`/`ProfileActivated`/`OriginatingClubLocked`/`CustomerSegmentChanged`) are absent, and `whereNotIn([CustomerHoldPlaced, CustomerHoldLifted])->count() === 0` (only Hold events recorded).
- Files changed: `tests/Feature/Modules/Parties/HoldRegistryTest.php` (new), `openspec/changes/parties-holds/tasks.md` (6.1 flipped). No production code, no migration, no `lang/`, no composer drift, no protected files; `SpineCreationChainTest` unamended.
- Quality loop: **green** — Pint clean · HoldRegistryTest 9/9 (41 assertions) · full suite **781/781** (3664 assertions, +9 from 772) · PHPStan max 0 · Pint --test clean · `openspec validate parties-holds --strict` valid.
- **PG17 verified** (docker `postgres:17`, port 55432): `tests/Feature/Modules/Parties` + `tests/Architecture` **211/211** (1394 assertions, +9 from 202). The 9 HoldRegistryTest cases ARE the PG proof — the multi-Hold/active-only WHERE queries, the polymorphic `scope_id` round-trip across all three scope_types, the enum-value WHERE clauses, the `whereNotIn` event-name guard, and the Customer/Account/Profile status re-reads all round-trip on PG. Container torn down.
- **Learnings for future iterations:**
  - **Demand-side status events are asserted absent BY STRING** (`CustomerActivated`/`ProfileActivated`/`OriginatingClubLocked`/`CustomerSegmentChanged`) — those classes do NOT exist (deferred to `parties-membership-lifecycle`), so there is no `::NAME` to reference; string literals in `whereIn(...)` are correct and match `SpineCreationChainTest`'s idiom. The supply-side `ProducerActivated`/`ProducerAgreementActivated` DO exist and are permitted — never use a blanket `%Activated%` to guard the demand side (it would also catch the legitimate supply-side names); assert the exact demand-side names.
  - **The non-vacuous "only Hold events" guard** = `whereNotIn([CustomerHoldPlaced::NAME, CustomerHoldLifted::NAME])->count() === 0`, paired with positive `CustomerHoldPlaced`=4 / `CustomerHoldLifted`=1 counts so the guard proves Holds were really placed/lifted (not that nothing happened). Robust because the Parties factories are pure fixtures (grep-confirmed: zero `record(`/`DomainEvent`/`dispatch` in `database/factories/Parties/`) — only the Actions emit, so the recorded-event set is exactly what the Actions wrote.
  - **`Collection::map(fn (Hold): string)->all()` is `array<int, string>`, not `list<string>`** at PHPStan max — declare the helper return type `array<int, string>` (no `array_values()` needed) when the consumer is `toEqualCanonicalizing` (order-insensitive, list-ness irrelevant). Reserve the `array_values($coll->all())` `list<>` trick (5.1) for when a `list<>` return type is actually required.

_seed — authored 2026-06-18 via `/spec-to-change`; awaiting human APPROVED + `./ralph.sh --change parties-holds`._

## [2026-06-18 09:47] — 1.1 Hold enums + the `autoLiftable` predicate
- Implemented the three string-backed Hold enums under `app/Modules/Parties/Enums/`: `HoldType` (`admin/kyc/payment/fraud/compliance/credit`) carrying the `autoLiftable(): bool` first predicate (true for `Kyc`/`Payment` only — mirrors `KycStatus::clears()`); `HoldScope` (`customer/account/profile`); `HoldStatus` (`active/lifted`). Docblocks cite design L1/L2 + spec § 4.8 + DEC-160/AC-K-FSM-11 + ADR 2026-06-18.
- Files changed: `app/Modules/Parties/Enums/HoldType.php` (new), `HoldScope.php` (new), `HoldStatus.php` (new), `tests/Unit/Modules/Parties/Enums/HoldEnumsTest.php` (new — 8 tests, mirrors `ComplianceEnumsTest`), `openspec/changes/parties-holds/tasks.md` (1.1 flipped).
- Quality loop: **green** — Pint clean · HoldEnumsTest 8/8 (19 assertions) · full suite 726/726 (3380 assertions, up from 718) · PHPStan max 0 errors · Pint --test clean · `openspec validate parties-holds --strict` valid.
- No DB touched (pure enums) → no PG17 run needed this task; the migration (1.2) is the first PG17-gated task.
- **Learnings for future iterations:**
  - The carryover enum house style held verbatim: `enum X: string`, PascalCase cases in spec order, predicate as the FIRST method, docblock = design L-tag + spec § + ADR. The test pins order-sensitive `case→value` maps via a `foreach`-built assoc array + `toBe([...])` + `toHaveCount` + a `from()` round-trip + per-enum `ValueError`. Bogus tokens chosen to be *semantically adjacent* (`expired` for type/status, `producer` for scope) so the test documents the domain edges, not just "rejects garbage".
  - `autoLiftable()` is intentionally `$this === self::Kyc || $this === self::Payment` (positive list), not `!in_array(...)` — same shape as `KycStatus::clears()`; keeps the auto-managed pair the single source of truth for the `LiftHold` guard (3.2) and the `RecordKycVerified` system-lift path (4.1).
---

## [2026-06-18 09:59] — 1.2 Additive migration — the `parties_holds` table + `Hold` model + factory
- Shipped the unified Hold registry table + persistence-only model + fixture factory. Migration `2026_06_18_000001_create_parties_holds_table.php`: `id`, `hold_type`/`scope_type`/`status` (string + enum cast + PG-only `CHECK (col IN (...))` from `Enum::cases()`), polymorphic `scope_id` (`unsignedBigInteger`, NO DB FK — design L1), `reason` (nullable), placement audit (`placed_actor_role` ActorRole + nullable `placed_actor_id`), lift audit (`lift_reason`/`lifted_actor_role`/`lifted_actor_id`/`lifted_at` all nullable), `timestampsTz()`, composite index `parties_holds_scope_status_index (scope_type, scope_id, status)`. `Hold` model (`$table='parties_holds'`, `$guarded=[]`, the three enum casts + ActorRole on both role columns + integer casts on scope/actor ids to dodge the PG numeric-string trap + `lifted_at` immutable_datetime, `@property` block, `newFactory()`). `HoldFactory` (active/admin/customer default, `system` actor, lift cols null).
- Files changed: `database/migrations/2026_06_18_000001_create_parties_holds_table.php` (new), `app/Modules/Parties/Models/Hold.php` (new), `database/factories/Parties/HoldFactory.php` (new), `tests/Feature/Modules/Parties/HoldSchemaTest.php` (new — 6 tests: typed-cast round-trip, lift-column round-trip, composite-index-exists, value-set CHECK ×3 columns), `openspec/changes/parties-holds/tasks.md` (1.2 flipped).
- Quality loop: **green** — Pint clean · HoldSchemaTest 6/6 (25 assertions) · full suite **732/732** (3405 assertions, +6 from 726) · PHPStan max 0 · Pint --test clean · `openspec validate parties-holds --strict` valid.
- **PG17 verified** (docker `postgres:17`, port 55432): HoldSchemaTest 6/6 on PG; `tests/Feature/Modules/Parties` + `tests/Architecture` **174/174** on PG (the new `Hold` model passes `ModulePersistenceConventionsTest`'s `parties_`-prefix check; `ModuleBoundariesTest` clean). Direct catalog proof: the 3 named CHECKs carry verbatim value-sets (`hold_type` admin/kyc/payment/fraud/compliance/credit · `scope_type` customer/account/profile · `status` active/lifted) and `parties_holds_scope_status_index` is a btree on `(scope_type, scope_id, status)`; `migrate:fresh` applies and `migrate:rollback` drops the table cleanly on PG. Container torn down.
- **Learnings for future iterations:**
  - The NON-nullable CHECK migration idiom + the single-`foreach`-over-`$valueSets` form + the Pint `{@see}` forward-ref trap are consolidated in the Codebase Patterns above. The forward-ref trap bit this task (the `Hold` docblock cites Actions/events built in 3.1/3.2/2.1) — fixed by plain backticks; tasks 2.1/3.x will create those classes and can then cite them with `{@see}`.
  - `Schema::getIndexes('parties_holds')` works on BOTH engines (no driver branch needed); match the composite index by its `['scope_type','scope_id','status']` column tuple rather than by name (robust + asserts leftmost-prefix order). The value-set CHECK test is per-file-safe: savepoint-wrapped `DB::transaction` insert, `pgsql` asserts `toContain($constraint)` + 0 rows, `sqlite` asserts the raw write is accepted (non-vacuous), then a valid insert succeeds on both.
---

## [2026-06-18 10:21] — 2.1 The two Hold events
- Added the two § 15.1 Hold events under `app/Modules/Parties/Events/`: `CustomerHoldPlaced` and `CustomerHoldLifted`, each a `final class` mirroring `CustomerOnboardingScreeningPassed` — untyped `const NAME` (verbatim, byte-for-byte), `const ENTITY_TYPE = 'Hold'` (the Hold is the subject; the scope rides in the payload), static `payload(Hold): array`. Placed payload `{hold_id, hold_type, scope_type, scope_id, reason}`; lifted payload `{hold_id, hold_type, scope_type, scope_id, lift_reason}`. PII-free (the Hold model carries no personal field).
- Files changed: `app/Modules/Parties/Events/CustomerHoldPlaced.php` (new), `CustomerHoldLifted.php` (new), `tests/Unit/Modules/Parties/Events/HoldEventsTest.php` (new — 6 tests: 2 NAMEs, 2 ENTITY_TYPEs, both `final`, both payload snapshots with exact-keys + PII-key absence, + a system null-reason placement case), `openspec/changes/parties-holds/tasks.md` (2.1 flipped).
- Quality loop: **green** — Pint clean · HoldEventsTest 6/6 (21 assertions) · full suite **744/744** (3446 assertions, +6 from 738) · PHPStan max 0 · Pint --test clean · `openspec validate parties-holds --strict` valid. No DB (pure value classes; the test uses `factory()->make()`, never queries) → no migration, no composer drift, no protected files; **no PG17 run needed** this task.
- **Learnings for future iterations:**
  - The `@property` nullability decides `->value` vs `?->value`: `Hold::hold_type`/`scope_type` are NON-nullable (`@property HoldType $hold_type`), so the payload uses plain `->value` — using `?->value` here would trip PHPStan-max `nullsafe.neverNull`. The Codebase-Patterns note "read nullable enum via `?->value`" is conditioned on the property being *nullable* (true for `Customer::sanctions_status`, false for the Hold enums); always check the model's `@property` line before choosing the operator.
  - The Pint `{@see}` forward-ref trap held again: `CustomerHoldLifted` cites `{@see CustomerHoldPlaced::payload()}` (SAME namespace `App\Modules\Parties\Events` → Pint adds no `use`, the class exists this task) safely, while the not-yet-built `PlaceHold`/`LiftHold`/`RequireKyc`/`RecordKycVerified` (Actions namespace, tasks 3.x/4.1) stay in PLAIN backticks. Post-Pint `use`-block confirmed clean (only `Hold` imported).
  - No central event-name registry and no arch test globs/counts the Events dir — defining a new event class is purely additive (a class is referenced only where it is emitted). The 3.x/4.1 emit sites are where the recorder wiring is pinned.
---

## [2026-06-18 10:28] — 3.1 `PlaceHold` Action
- Added the first Hold lifecycle Action `App\Modules\Parties\Actions\PlaceHold` — `handle(HoldType $type, HoldScope $scope, int $scopeId, ?string $reason = null): Hold`. Mirrors the `RecordCustomerScreening` evented-Action shape: injects `DomainEventRecorder` + `ActorContext`; ONE `DB::transaction` → resolve the actor ONCE (`role()`/`actorId()`, stamping both the row and the envelope) → `Hold::create([... status = HoldStatus::Active ...])` → `recorder->record(CustomerHoldPlaced::NAME, Module::Parties->value, role, id, ENTITY_TYPE, (string)$hold->id, payload)` in the same transaction (recorder's open-tx guard → write+emit atomic). Root event (no causation/correlation). The manual operator-placement path AND the path `RequireKyc` will reuse for the auto `kyc` Hold (4.1).
- Files changed: `app/Modules/Parties/Actions/PlaceHold.php` (new), `tests/Feature/Modules/Parties/HoldLifecycleTest.php` (new — 2 tests: operator placement w/ reason [active Hold fields + placement actor System + created_at + one PII-free CustomerHoldPlaced by-key]; system null-reason placement → null reason through to payload), `tests/Feature/Modules/Parties/SupplyLifecycleChainTest.php` (added `$holdTransitions = ['PlaceHold']` to the exact-set non-Create whitelist), `openspec/changes/parties-holds/tasks.md` (3.1 flipped).
- Quality loop: **green** — Pint clean · HoldLifecycleTest 2/2 + SupplyLifecycleChainTest 5/5 (112 assertions) · full suite **746/746** (3485 assertions, +2 from 744) · PHPStan max 0 · Pint --test clean · `openspec validate parties-holds --strict` valid. No migration, no composer drift (`git diff main -- composer.json composer.lock` empty), no protected files.
- **PG17 verified** (docker `postgres:17`, port 55432): `tests/Feature/Modules/Parties` + `tests/Architecture` **176/176** (+2 from 174). The HoldLifecycleTest assertions ARE the PG proof — the Hold row persists + re-reads with typed enum casts, the `CustomerHoldPlaced` records with the PII-free by-key payload, and the System actor resolves, all on PG. `ModuleBoundariesTest`/`ModulePersistenceConventionsTest` green (PlaceHold is within-module; no new model). Container torn down.
- **Learnings for future iterations:**
  - **`SupplyLifecycleChainTest` is the ONLY exact-set non-Create whitelist** (confirmed by grep: `toEqualCanonicalizing([...transitions])` lives only there). `ComplianceIndependenceTest` ALSO globs `Actions/*.php` but uses a forbidden-NAME negative check (`not->toContain` on demand-side STATUS transitions like `ActivateCustomer`) — `PlaceHold` is neither asserted-present nor forbidden there, so it needs NO change. Adding `LiftHold` in 3.2 → just extend `$holdTransitions` in SupplyLifecycleChainTest.
  - **`ComplianceIndependenceTest` line 99 (`where('name','like','%Hold%')->count() === 0`) is a TIME-BOMB for 4.1, not 3.1.** 3.1 only DEFINES `PlaceHold`; it doesn't wire it into `RequireKyc`/`RecordKycVerified`, so that test's KYC flow still records no Hold event → stays green now. Task 4.1 (the coupling) is what flips it — it must update BOTH `CustomerKycLifecycleTest` AND this `ComplianceIndependenceTest` `%Hold%` assertion.
  - **design L1 scope-resolution vs tasks.md 3.1 scope (resolved → defer).** design L1 + its risk note say "PlaceHold resolves the scoped entity before placing; a test asserts placing against a missing scope is rejected." BUT task 3.1's explicit description/acceptance/test-hint scope it to create+record only, and task 1.3 created NO scope-not-found exception (only `IllegalHoldLift`). The spec scenarios don't require scope-existence validation either. Took the task decomposition as operative (it's the most specific authority + the exception inventory confirms it): PlaceHold is create+record, no scope resolution. SAFE because the one live caller (`RequireKyc`, 4.1) places on a Customer it has just transaction-locked. If a reviewer wants strict 3-way scope resolution it's a small follow-up (would need a new exception). Flagged here, not escalated — the task is cleanly implementable as written.
  - **No PG numeric-string trap in the placement payload.** `scope_id` is model-cast `'integer'` → int on both engines; `hold_id`/`scope_id` in the jsonb payload trace to the same in-memory `$hold->*` values and JSON round-trips preserve type, so `toBe(...)` is consistent across engines (no loose `toEqual` needed). `entity_id` is `(string)$hold->id` → string column → string both engines. Confirmed green on PG17.
---

## [2026-06-18 10:07] — 1.3 `IllegalHoldLift` exception + localized copy
- Added the lift-discipline exception `App\Modules\Parties\Exceptions\IllegalHoldLift extends RuntimeException` with two named factories mirroring `IllegalKycTransition`: `::autoManaged(HoldType $type)` (operator-lift of a `kyc`/`payment` Hold → resolves `parties.hold.cannot_lift_auto_managed` with `:type`) and `::notActive(HoldStatus $from)` (lifting an already-`lifted` Hold → resolves `parties.hold.cannot_lift_not_active` with `:state`). Both `(string) __()`-coerce the translator return; the token is `$enum->value` (business enum, PII-free). Added a new `hold` group to `lang/en/parties.php` (the 8th group; the 7 pre-existing groups untouched).
- Files changed: `app/Modules/Parties/Exceptions/IllegalHoldLift.php` (new), `lang/en/parties.php` (+`hold` group, 2 keys), `tests/Unit/Modules/Parties/Exceptions/HoldExceptionTest.php` (new — 6 tests: kyc+payment auto-managed factories, not-active factory, parametrized new-key resolution ×2, pre-existing-group preservation), `openspec/changes/parties-holds/tasks.md` (1.3 flipped).
- Quality loop: **green** — Pint clean · HoldExceptionTest 6/6 (20 assertions) · full suite **738/738** (3425 assertions, +6 from 732) · PHPStan max 0 · Pint --test clean · `openspec validate parties-holds --strict` valid. No DB → no PG17 run (pure exception + lang; composer untouched, no protected files).
- **Learnings for future iterations:**
  - The Pint `{@see}` forward-ref trap held: the docblock cites `LiftHold` (task 3.2, not yet built) + `RecordKycVerified` in **plain backticks**, and the sibling `{@see IllegalKycTransition}` works **unqualified** (same namespace `App\Modules\Parties\Exceptions` → Pint adds no `use`, PHPStan stays green). Reuse `{@see <Sibling>}` only for same-namespace classes; cross-namespace forward-refs stay backticked.
  - i18n key copy must keep the offending token's VALUE out of the literal template so a `toContain('<token>')` assertion proves interpolation, not a coincidental word match. `cannot_lift_not_active` says "lifts only from active" (no literal `lifted`); `cannot_lift_auto_managed` says "auto-managed Hold" (no literal `kyc`/`payment`). The parametrized resolution test passes BOTH `type` + `state` placeholders (Laravel ignores the unreferenced one) so one dataset covers both keys despite their different placeholder names.
---

## [2026-06-18 10:38] — 3.2 `LiftHold` Action + the per-type lift discipline
- Added the lifting counterpart `App\Modules\Parties\Actions\LiftHold` — `handle(int $holdId, ?string $reason = null): Hold`. Same evented-Action shape as `PlaceHold` (inject `DomainEventRecorder` + `ActorContext`). ONE `DB::transaction`: `lockForUpdate` re-read (`Hold::query()->whereKey($holdId)->lockForUpdate()->firstOrFail()`, mirroring `ActivateProducer`) → **status guard BEFORE type guard**: `status !== Active` → `IllegalHoldLift::notActive($hold->status)`; `hold_type->autoLiftable()` → `IllegalHoldLift::autoManaged($hold->hold_type)` → resolve actor ONCE (after guards, lift path only) + capture one `CarbonImmutable::now()` → `update(status=Lifted, lifted_actor_role/_id, lifted_at, lift_reason)` → `record(CustomerHoldLifted::NAME, …)` in-tx. ROOT event (no causation/correlation).
- Files changed: `app/Modules/Parties/Actions/LiftHold.php` (new), `tests/Feature/Modules/Parties/HoldLifecycleTest.php` (+4 `it` blocks → +7 cases: detailed `admin` lift [full lift cols + envelope + by-key PII-free payload + placement cols preserved]; parametrized `{fraud,compliance,credit}` lift → `lifted` + one `CustomerHoldLifted`; parametrized `{kyc,payment}` reject → throws `IllegalHoldLift` with `:type`, stays `active`, 0 lift event, only the placement event; double-lift `admin` → `::notActive` with `:state`, state+log unchanged), `tests/Feature/Modules/Parties/SupplyLifecycleChainTest.php` (`$holdTransitions` gains `'LiftHold'`), `openspec/changes/parties-holds/tasks.md` (3.2 flipped). Also updated the file-docblock to pin both PLACE and LIFT paths.
- Quality loop: **green** — Pint clean · HoldLifecycleTest 9/9 + SupplyLifecycleChainTest 5/5 · full suite **753/753** (3548 assertions, +7 from 746) · PHPStan max 0 · Pint --test clean · `openspec validate parties-holds --strict` valid. No migration, no composer drift (`git diff main -- composer.json composer.lock` empty), no protected files.
- **PG17 verified** (docker `postgres:17`, port 55432): `tests/Feature/Modules/Parties` + `tests/Architecture` **183/183** (+7 from 176). The HoldLifecycleTest lift assertions ARE the PG proof — the Hold row round-trips `active → lifted` with typed enum casts, `lifted_at` (timestamptz → immutable_datetime), the `lifted_actor_role` ActorRole cast, and `CustomerHoldLifted` records with the PII-free by-key payload, all on PG. Container torn down.
- **Learnings for future iterations:**
  - **Guard ordering is intentional: status BEFORE type.** An already-`lifted` Hold throws `::notActive` regardless of type (an out-of-state lift is the more fundamental rejection); the double-lift test rides an `admin` Hold so the `notActive` path is reached cleanly (a type-first order would still pass the auto-managed test but would mis-report an already-lifted `kyc` Hold as `autoManaged`). The two rejection tests never collide because the auto-managed dataset uses `active` `kyc`/`payment` and the double-lift uses an already-`lifted` `admin`.
  - **`toThrow(IllegalHoldLift::class, $token)` proves i18n interpolation, not a literal match.** The `:type`/`:state` tokens (`kyc`/`payment`/`lifted`) are deliberately ABSENT from the lang templates (task 1.3), so a message-substring assertion on the token value can only pass if interpolation fired — and `$type->value`/`HoldStatus::Lifted->value` make the parametrized dataset self-document the offending value. (`lifts`/`lift` in the copy do NOT substring-match `lifted` — verified.)
  - **`reason` is preserved across lift; the payload reads the POST-update model.** `LiftHold` only writes the `lifted_*`/`lift_reason`/`status` columns, so the placement `reason` survives (asserted). `CustomerHoldLifted::payload($hold)` is built AFTER `$hold->update([...])`, so the in-memory `lift_reason` is the just-written value — no re-read needed (Eloquent `update` mutates the model in place).
  - **The 4.1 time-bomb is now the ONLY thing left coupling-wise (3.x done).** Both Hold Actions exist + are whitelisted; `ComplianceIndependenceTest` + `CustomerKycLifecycleTest` `%Hold%`-count-0 assertions are STILL green (3.x only defines the Actions). 4.1 wiring `RequireKyc`→`PlaceHold(Kyc,…)` and `RecordKycVerified`→lift flips BOTH — and `RecordKycVerified` currently injects NOTHING (eventless), so 4.1 adds the recorder+actor (or `PlaceHold`/`LiftHold`) to it. Note: `RecordKycVerified` can't reuse `LiftHold` for the `kyc` auto-lift (the operator path REJECTS `kyc` via `autoManaged`) — it needs its own system-lift path (set `lifted` + record `CustomerHoldLifted` directly, the design-L2 "separate within-module path").
---

## [2026-06-18 10:57] — 4.1 Wire the `kyc` Hold onto `RequireKyc` + `RecordKycVerified`; flip the shipped tests
- Wired the one MODIFIED behaviour (design L7). `RequireKyc` now injects `PlaceHold` and, after the `→ pending` write, calls `$this->placeHold->handle(HoldType::Kyc, HoldScope::Customer, $customer->id)` in the SAME transaction (reason null — system-placed; PlaceHold's nested transaction is a savepoint, so the KYC write + Hold place + `CustomerHoldPlaced` are atomic — the `RetireProducer → SunsetClub` precedent). `RecordKycVerified` now injects `DomainEventRecorder` + `ActorContext` and, after the `→ verified` write, runs an INLINE system-lift of the Customer's active `kyc` Hold(s) (it CANNOT reuse `LiftHold` — the operator path rejects `kyc` via `autoManaged`): resolve actor + one `CarbonImmutable::now()`, `->where(...HoldScope::Customer->value/...HoldType::Kyc->value/...HoldStatus::Active->value)->lockForUpdate()->get()`, per Hold `update([lifted, lifted_actor_*, lifted_at, lift_reason=>null])` + `record(CustomerHoldLifted::NAME, …)` in-tx. `RecordKycRejected` UNCHANGED (Hold stays — § 9.1). Both docblocks flipped (the "NO Hold is placed/lifted … parties-holds owns it" notes now describe the coupling). No new Action (system-lift is inline) → `SupplyLifecycleChainTest` whitelist unchanged.
- Files changed: `app/Modules/Parties/Actions/RequireKyc.php` (inject PlaceHold + place), `app/Modules/Parties/Actions/RecordKycVerified.php` (inject recorder+actor + system-lift), `tests/Feature/Modules/Parties/CustomerKycLifecycleTest.php` (flipped test 1 + the require→verify coupling test + NEW require→reject-keeps-Hold case; clarified the isolated-verify test), `tests/Feature/Modules/Parties/ComplianceIndependenceTest.php` (flipped the independence test: KYC now records 2 Hold events, `%Hold%`=2, `%Kyc%`=0), `tests/Feature/Modules/Parties/ComplianceChainTest.php` (flipped: 5 events incl. both Hold events, `%Hold%`=2), `openspec/changes/parties-holds/tasks.md` (4.1 flipped).
- Quality loop: **green** — Pint clean · the 5 affected files 63/63 then CustomerKycLifecycleTest 18/18 · full suite **754/754** (3566 assertions, +1 net from 753) · PHPStan max 0 · Pint --test clean · `openspec validate parties-holds --strict` valid. No migration, no composer drift (`git diff main -- composer.json composer.lock` empty), no protected files, no `lang/` change.
- **PG17 verified** (docker `postgres:17`, port 55432): `tests/Feature/Modules/Parties` + `tests/Architecture` **184/184** (1296 assertions, +1 from 183). The flipped lifecycle/independence/chain tests ARE the PG proof — the `kyc` Hold round-trips `active → lifted` driven by the KYC Actions, both Hold events record by-key PII-free, all on PG. Container torn down.
- **Learnings for future iterations:**
  - **PHPStan-max: `Collection::first()` is `Model|null`, `Builder::sole()` is non-null `Model`.** Test 1 first used `$holds->first()->status` after `toHaveCount(1)` → `property.nonObject` (PHPStan can't narrow a collection by a runtime count assertion). Fix: query with `->sole()` (asserts exactly-one AND returns non-null) and assert on that — the idiom already used by the LiftHold tests and the coupling tests in this same file. Never chain a property off `->first()` in a test without a null guard.
  - **A handoff's "affected files" list is a HINT, not the truth — grep for ALL callers.** The 3.x notes flagged only `CustomerKycLifecycleTest` + `ComplianceIndependenceTest` for the `%Hold%` flip; `ComplianceChainTest` was a silent THIRD (its `runComplianceChain()` drives `RequireKyc`+`RecordKycVerified` and asserts an exact event-count set). `grep -rn 'RequireKyc\b\|RecordKycVerified\b' tests` surfaced all three before any test ran. Always re-derive the blast radius from the code, never trust a hand-listed set.
  - **`%Kyc%` stays 0 after the coupling.** The coupled events are `CustomerHoldPlaced`/`CustomerHoldLifted` — KYC ITSELF remains event-silent (design L3). Every `where('name','like','%Kyc%')->count()===0` assertion across the Parties suite still holds; only the `%Hold%` and total-count assertions move. Useful when reasoning about which existing assertions a coupling actually disturbs.
---

## [2026-06-18 11:14] — 5.1 The `(sanctions_status, active-Hold-list)` read contract + cascade
- Shipped the uniform read-API (design L6): `app/Modules/Parties/Contracts/ComplianceStatus.php` (PII-free DTO — promoted `public readonly ?SanctionsStatus $sanctionsStatus` + `list<HoldType> $activeHoldTypes`, `isClear()` = sanctions `Passed` ∧ no active Hold; the `Money`/`DualCurrencyAmount` value-object idiom, NEVER the `Hold` model), `app/Modules/Parties/Contracts/PartyComplianceStatusReader.php` (interface: `forCustomer(int)`, `forProfile(int)`), `app/Modules/Parties/Reads/DatabaseComplianceStatusReader.php` (cascade-at-read in ONE query), bound in `PartiesServiceProvider::register` via `bind(Interface, Concrete)`. `forProfile` = profile-scope ∪ parent-Customer-scope active Holds + the parent Customer's `sanctions_status` (BR-K-Hold-3 cascade); `forCustomer` = customer-scope active Holds; Account-scope not cascaded (design L6 risk note). No downstream enforcement surface (scope guard — Module S/C/E consume later).
- Files changed: `app/Modules/Parties/Contracts/ComplianceStatus.php` (new), `app/Modules/Parties/Contracts/PartyComplianceStatusReader.php` (new), `app/Modules/Parties/Reads/DatabaseComplianceStatusReader.php` (new), `app/Modules/Parties/Providers/PartiesServiceProvider.php` (added the bind in `register`), `tests/Feature/Modules/Parties/ComplianceReadApiTest.php` (new — 10 `it` blocks → 18 cases: container resolution; clear/not-clear tuple; Customer→Profile cascade; Profile isolation + sibling-clear + Customer-doesn't-see-Profile-Hold; active-only [lift drops out, drives PlaceHold/LiftHold]; null/non-passed sanctions ×4 → not clear; six types ×6 → not clear; multi-Hold-one-type → DISTINCT entry; PII-free DTO-of-enums-not-model), `openspec/changes/parties-holds/tasks.md` (5.1 flipped).
- Quality loop: **green** — Pint clean · ComplianceReadApiTest 18/18 (57 assertions) · full suite **772/772** (3623 assertions, +18 from 754) · PHPStan max 0 · Pint --test clean · `openspec validate parties-holds --strict` valid. No migration, no composer drift (`git diff main -- composer.json composer.lock` empty), no protected files, no `lang/` change.
- **PG17 verified** (docker `postgres:17`, port 55432): `tests/Feature/Modules/Parties` + `tests/Architecture` **202/202** (1353 assertions, +18 from 184). The ComplianceReadApiTest cases ARE the PG proof — the cascade OR-group query (`status = active AND ((scope_type, scope_id) OR …)`), the enum-value WHERE clauses, the Customer→Profile cascade and Profile isolation, and the `findOrFail` + `sanctions_status` cast all round-trip on PG. `ModuleBoundariesTest` green (the read contract is within-module; no cross-module import). Container torn down.
- **Learnings for future iterations:**
  - The read-contract idiom (interface + DTO + `bind`-ed `Reads\` impl; cascade-at-read OR-group; the `array_values(...->all())` `list<>` trick; bare-`Builder` nested-where closures; DTO = plain `class` + promoted `public readonly`, not `final readonly class`) is consolidated in the Codebase Patterns above.
  - **PHPStan max rejects two chained `->not->…`** — `expect($x)->not->toContain($a)->not->toContain($b)` reds (`property.notFound` on `$not`, then `method.nonObject`). Re-anchor with `->and($x)->not->…` (the `HoldLifecycleTest` PII-absence idiom). Recorded in `lessons.md`.
  - **Account-scope is a deliberate cascade gap.** `forCustomer`/`forProfile` never read Account-scope Holds (the PRD specifies no Account cascade — design L6 risk note); `HoldScope::Account` is placeable (registry is trigger-agnostic) but has no read-API consumer this slice. Flag for the 6.2 CONTEXT.md note + any Module-S gate that later needs account-level blocking.
---

## [2026-06-18 11:26] — 6.1 Hold registry behaviour + scope-guard test  (backfill — committed 8eda7a2; per-task progress entry omitted by the loop)
- Added `tests/Feature/Modules/Parties/HoldRegistryTest.php` pinning the emergent registry contract: multiple concurrent `active` Holds on one scope (BR-K-Hold-1 — lift one, the other remains), the six types × three scopes all placeable via the manual operator path (AC-K-MVP-2), and the demand-side SCOPE GUARD (place/lift + RequireKyc move NO Customer/Account/Profile status and record no demand-side status event). `SpineCreationChainTest` stays green unamended.
- Quality (from commit + hot.md): full suite **781/781** (3664 assertions, +9 from 772); PHPStan max 0; Pint clean; `openspec validate` valid. PG17: `tests/Feature/Modules/Parties` + `tests/Architecture` **211/211**. No migration, no protected files.
---

## [2026-06-18 11:39] — 6.2 Docs — CONTEXT.md Hold terms + Parties Hold contract note  (backfill — committed 6f1c977; per-task progress entry omitted by the loop)
- DOCS-ONLY. Extended `CONTEXT.md` (5 edits): the resolved Hold vocabulary (six `HoldType`s; three `HoldScope`s + Customer→Profile cascade / Profile isolation; the per-type lift discipline + ADR pointer; the `(sanctions_status, active-Hold-list)` read-API tuple; system-null `reason`) grouped under `## Compliance & Finance`; the `**KYC lifecycle**` term gained the landed kyc-Hold coupling; a new `### Parties Hold events — payload contract` subsection (2 events + PII-free payload table + `PartyComplianceStatusReader` read-contract note DEC-181 + six deferred seams). Names verified against source.
- Quality: no DB → no PG17 run; suite unchanged at **781/781**; `openspec validate parties-holds --strict` valid; no PHP/migration/lang/composer/protected change.
---

## [2026-06-18 12:03] — 6.3 Full Hold chain + cross-engine PG17 close (LAST task → CHANGE_COMPLETE)
- Added the closing integration proof `tests/Feature/Modules/Parties/HoldChainTest.php` (2 `it` blocks, 46 assertions) driving the WHOLE slice through the real Actions in one chain: a sanctions-`passed` Customer (+2 Profiles, factory-born — no creation events) → `RequireKyc` (kyc NULL→pending + auto-places the Customer-scope `kyc` Hold + one `CustomerHoldPlaced`) → `PlaceHold(Admin, Customer)` → read-API NOT clear + the two Customer-scope Holds CASCADE to both Profiles → operator-lift of the `kyc` Hold REJECTED (`IllegalHoldLift`, `:type`='kyc', no state change) → `LiftHold(admin)` (one `CustomerHoldLifted`; the `kyc` Hold still active → still not clear) → `RecordKycVerified` (kyc pending→verified + auto-lifts the `kyc` Hold) → read-API CLEAR (sanctions `passed` ∧ no active Hold) + the cleared state cascades to both Profiles. The event-SET assertion pins EXACTLY `{CustomerHoldPlaced: 2, CustomerHoldLifted: 2}` and nothing else (4 rows, all module `parties` / entity_type `Hold` / System actor; `%Kyc%`=0 — KYC event-silent; sanctions factory-set so no screening event; no demand-side status event by exact name). Scope guard: Customer stays `pending`, Profiles stay `applied`.
- Files changed: `tests/Feature/Modules/Parties/HoldChainTest.php` (new), `openspec/changes/parties-holds/tasks.md` (6.3 flipped), `progress.md`, `log.md`, `hot.md`. No app/migration/lang/composer/protected-file change (`git diff main -- composer.json composer.lock` empty).
- Quality loop: **green** — Pint clean · HoldChainTest 2/2 (46 assertions) · full suite **783/783** (3710 assertions, +2 from 781) · PHPStan max 0 · Pint --test clean · `openspec validate parties-holds --strict` valid.
- **Cross-engine close on PG17** (docker `postgres:17`, port 55432): the **ENTIRE suite 783/783** (3710 assertions) green on PostgreSQL 17 — the entire Parties suite + the two arch tests are a subset, all green; this doubles as the §2.7 pre-merge production-engine gate. Direct PG-constraint evidence captured: `parties_holds` carries the three value-set CHECKs (`hold_type` ∈ admin/kyc/payment/fraud/compliance/credit; `scope_type` ∈ customer/account/profile; `status` ∈ active/lifted) and the polymorphic-scope index `parties_holds_scope_status_index (scope_type, scope_id, status)`; `HoldSchemaTest` 6/6 on PG is the direct CHECK-rejection + index proof. Container torn down.
- **CHANGE COMPLETE — 11/11 tasks.** Ready for the GUIDE §2.7 human ritual (review → merge --no-ff → semantic verify → archive).
- **Learnings for future iterations:**
  - **The closing chain test READS active Holds through the read-API, not a local helper** — `HoldRegistryTest` already declares the global `activeHoldTypesOnCustomer()`; Pest loads ALL test files before any run, so a second declaration of that name would be a fatal redeclare. Used `app(PartyComplianceStatusReader::class)->forCustomer(...)->activeHoldTypes` (also the very contract the chain exists to prove) + an inline `Hold::query()->…->sole()` for the kyc Hold id. No new global helper.
  - **`->toContain(enum)->toContain(enum)->toHaveCount(n)` is the order-insensitive set assertion for a `list<Enum>`** — avoids `toEqualCanonicalizing` sorting enum INSTANCES (unspecified) and avoids depending on insertion order; single-element / empty stay `->toBe([Enum::X])` / `->toBe([])` (the ComplianceReadApiTest idiom).
  - **The rejected operator-lift of the `kyc` Hold is an app-level guard thrown BEFORE any DML**, so the verify-after-throw query (`Hold::findOrFail(...)->status`) survives on PG with no savepoint wrapper — trap 5 (a PG constraint RAISE aborting the txn) does NOT apply to a pre-DML `IllegalHoldLift`.
---
