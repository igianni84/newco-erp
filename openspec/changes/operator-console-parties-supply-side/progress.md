# Progress — operator-console-parties-supply-side

## Codebase Patterns
(consolidated reusable patterns — read first each iteration)

- **Operand-enum carve-out is LIVE (ADR 2026-06-21, task 1.1 landed).** OperatorPanel may now import each operated module's `Enums\*`. So the Club create (task 3.1) can `use App\Modules\Parties\Enums\ClubRegistrationFlowType;` and call `ClubRegistrationFlowType::from($selected)` in `createViaAction` — `ModuleBoundariesTest` admits it. Discipline (NOT mechanically guarded): import **operand** enums only (an Action `handle()` parameter you must construct — `ClubRegistrationFlowType`); **state** enums (`ClubStatus`, `ProducerAgreementStatus`) stay rendered via the model cast (`->value` + `instanceof BackedEnum`), never imported (D2). A state-enum import = dead code, caught in review.
- **`ModuleBoundariesTest` carve-out source of truth = `moduleBoundaryAllowedImports(Module)`** (the helper) + the guard test `it('scopes the operator-console Models/Actions/Enums carve-out to OperatorPanel only')`. The allow-list is now `{Models, Actions, Enums}` for `Module::OperatorPanel` only; lateral modules (Catalog) still get `{Contracts, Events}`. Widening it again needs a new ADR + updated guard assertions.
- **Read-surface group = ONE green unit (the `getPages()` boot coupling).** A Filament Resource's `getPages()` eagerly references all three page classes at panel registration, so `<Entity>Resource` + `List<Entity>` + `Create<Entity>` + `View<Entity>` must all exist together or the panel fails to boot (every Filament test reds). Ship them in one iteration: the Resource (read columns/infolist + `getPages` index/create/view, own `status` badge via the cast — NEVER `lifecycleStateColumn()`, NEVER a `Parties\Enums` import on the read path), `List` (header create-LINK via `Action::make('create')->url(getUrl('create'))`, never a `CreateAction`), the **real** `Create` page (design.md: "scaffold Create… *with its real createViaAction*" — `OperatorConsoleCreateRecord` has two abstract methods, so a clean scaffold IS the real page; a throw-stub is hacky/dead), and a **bare** `View extends ViewRecord` (the `SurfacesDomainActions` verbs land in the lifecycle task). Resource i18n (label/plural_label EN-only→IT fallback, columns.*, the infolist field labels, `actions.create`) ships in the same unit. Mirror `ProducerResource`/`ClubResource`.
- **Verify model attributes, not task prose:** the operating-Producer read column is `$record->producer->name` (`Producer` has **`name`**; `display_name` is the *Club*'s attribute). Boolean infolist fields → `IconEntry::make(...)->boolean()` (i18n-safe). A `Money` field renders via `getStateUsing` with a local `$fee = $record->fee;` for phpstan null-narrowing.
- **Create form lives on `Resource::form()` (D6), uses `fields.*` for EVERY input** (mirroring Producer — even inputs that re-label a column get a `fields.X` key, e.g. `fields.display_name`). Pickers are an `->options(self::xOptions(...))` helper: a **model-driven** picker reads `Model::query()->orderBy('id')->get()->mapWithKeys(...)->all()` keyed by FK id (within `{Models}`, a pure read — the no-write rule is unaffected); an **enum-driven** Select reads `collect(OperandEnum::cases())->mapWithKeys(fn ($c) => [$c->value => $c->value])->all()` (token as both key and label — domain data, no per-value i18n key, only the Select's own `->label()` localized; importing the operand enum is the {Enums} carve-out). **A `->numeric()` field dehydrates as a FLOAT** → narrow it in `createViaAction` with `is_numeric()` (+ `=== ''`/`is_null` for optional-blank), never `is_string`/`is_int` (lessons.md 2026-06-21; the catalog `CreateProductVariant` is the reference). Money assembled `(int) $amount` only when both amount+currency present, else `null` (D11). No `{@see Money}` docblock ref unless Money is imported-and-used in that file (Pint would add a dangling import — use prose backticks).
- **Console i18n kit-key completeness test (twice-proven: Producer + Club; group 10 repeats it for ProducerAgreement).** A `…ConsoleI18nTest.php` with FIVE guards mirroring `ProducerConsoleI18nTest`, all driven off two top-level dataset fns `<entity>ConsoleKitKeys()` (the full list) + `<entity>ConsoleItDiffersKeys()` (= kit `array_diff` `['label','plural_label']`): (1) EN baseline `Lang::has("operator_console.<entity>.{$suffix}",'en',false)` over the kit; (2) IT-differs (`App::setLocale('it')`; `Lang::has(…,'it',false)` true AND `__($key) !== trans($key,[],'en')`) over the differs-set; (3) `label`/`plural_label` EN-fallback (absent in `it`, resolves to EN, `!= $key`); (4) IT⊆EN via `array_diff($itKeys,$enKeys)===[]` over `Arr::dot(trans('operator_console',[],L))` **filtered `str_starts_with($dotKey,'<entity>.')`** — the trailing dot is load-bearing (excludes `producer.fields.clubs`-style siblings + a future `<entity>s.*` group); (5) sink scan reusing `scanOperatorConsoleHardcodedSinks()` behind `function_exists`, files scoped `str_contains($pathname,'<Entity>Resource')`. **Enumerate EVERY key the console resolves**, not just the string-concat kit ones: the literal `__()` `columns.*`/`fields.*`/`actions.create` keys are visible to source but no behaviour test asserts they RESOLVE (a dropped key renders raw), so they belong in the contract too — include the infolist-only `fields.fee` (only this guard catches it). Test count = |kit| EN + |differs| IT + 2 fallback + 1 IT⊆EN + 1 sink (Club: 21+19+2+1+1 = 44). **Run via `--filter=<name>` or full suite, NEVER a bare path** — the sink helper lives in `Catalog/ProductMasterConsoleI18nTest.php`; a bare-path run won't load it and guard (5)'s `function_exists` correctly false-reds. Non-vacuity is provable: drop one EN key → guards (1) + (4) both fire.

---

## [2026-06-21 09:27] — 1.1 widen the OperatorPanel carve-out to admit operand enums
- **What:** Implemented ADR `2026-06-21-operator-console-operand-enum-carveout.md` (task group 1, prerequisite of the Club create in group 3). In `tests/Architecture/ModuleBoundariesTest.php`, added `$other->namespace().'\\Enums'` to the OperatorPanel branch of `moduleBoundaryAllowedImports()` (now `{Models, Actions, Enums}`). Updated the carve-out guard test (`it('scopes the operator-console Models/Actions/Enums carve-out to OperatorPanel only')`): the per-module loop now asserts `Enums` is admitted for OperatorPanel, plus a new lateral-module assertion that `Parties\Enums` is **false** for Catalog; the whole-module `toBeFalse()` is retained. Rewrote the header docblock + the three inline carve-out comments to record the operand-enum admission (citing the ADR) and split operand (imported/constructed) from state (rendered via cast) — the old "render enum casts via their instances → stays exactly {Models, Actions}" sentence was falsified by an operand enum in a create Action. Fixed the now-wrong "(Lifecycle, Services, **Enums**, Exceptions, …)" internals example (Enums is no longer an internal for OperatorPanel).
- **Files changed:** `tests/Architecture/ModuleBoundariesTest.php` (+46/−25). Bundled the change-authoring artifacts this ADR produced: the ADR file itself (untracked), `decisions/INDEX.md` (ADR index entry), `CONTEXT.md` (Operand enum / State enum glossary terms — cited by the ADR's References).
- **Quality loop: green.** pint clean; `ModuleBoundariesTest` 3/3 (189 assertions); full SQLite suite **1206/1206** (6859 assertions, +9 from the new guard assertions); phpstan 0; pint --test clean; `openspec validate … --strict` valid; composer diff vs `main` empty; no protected-file edits.
- **Learnings for future iterations:**
  - The first privacy-law `it()` did NOT need touching for green: adding `Enums` to `->ignoring()` only broadens what's stripped, and no OperatorPanel code imports a module `Enums` yet (the real import lands in 3.1) — so it stayed green automatically. The operand-enum import is *admitted* now; it is *exercised* by task 3.1.
  - The helper + guard test are the only references to `moduleBoundaryAllowedImports` / the test-name string (grep-confirmed file-local), so the rename was safe.
  - Arch test is self-contained (declares its own helper) → safe to run by bare path, unlike the i18n tests that reuse `scanOperatorConsoleHardcodedSinks`.
---

## [2026-06-21 09:46] — 2.1 / 2.2 / 2.3 Club read-only Resource + list + resource i18n
- **What:** the Club read surface — the SECOND Parties (Module K) operator console, the non-catalog trait-reuse pattern (ADR 2026-06-20). Done as ONE green unit (the `getPages()` boot coupling forces all three pages to exist together — predecessor §1 precedent). `ClubResource extends OperatorConsoleResource` read-binding `Parties\Models\Club` (own `status` badge + a `registration_flow_type` classifier column, both rendered via the BackedEnum cast — NOT `lifecycleStateColumn()`, no `Parties\Enums` import on the read path; list = display_name/producer/registration_flow_type/status/version; infolist = display_name/producer/registration_flow_type/fee(Money)/generates_credit/invite_only); `ListClubs` (header create-LINK). To keep the panel booting, also created **the real `CreateClub`** (per design.md Risks "scaffold Create… with its real createViaAction" — NOT a throw-stub; it constructs the `ClubRegistrationFlowType` operand enum + assembles the `Money` fee + calls `CreateClubAction`) and a bare read-only `ViewClub` (trait + sunset/close verbs deferred to 4.1). EN (full) + IT (per-key; label/plural_label → EN fallback) `operator_console.club.*`.
- **Files changed (new):** `app/Modules/OperatorPanel/Filament/Resources/Parties/ClubResource.php` + `…/ClubResource/Pages/{ListClubs,CreateClub,ViewClub}.php`; `tests/Feature/Modules/OperatorPanel/Parties/ClubResourceTest.php`. **(modified):** `lang/{en,it}/operator_console.php` (+`club` block). No migration/composer/protected-file change.
- **Quality loop: green** — pint clean; filtered ✓ (`ClubResourceTest` 5/5, 20 assn); full SQLite suite ✓ **1211/1211** (6879 assn, +5 vs the 1206 baseline); phpstan ✓ 0 (the `NoEloquentWriteInOperatorPanelRule` analysed the four new Filament classes — green proves no Eloquent write, the create routes through the action); pint --test ✓; `ModuleBoundariesTest` ✓ 3/3 (189 assn); `openspec validate … --strict` valid; composer diff vs `main` empty.
- **Acceptance walked (group 2):** a seeded Club renders in the list with its `status` (`active`), operating Producer name + `registration_flow_type` (`invitation_only`) ✓; `ClubResource::getPages()` keys `toEqualCanonicalizing(['index','create','view'])` ✓; table exposes no edit/delete/bulk action, create is a header link (`assertActionHasUrl`) ✓; view page renders display_name/producer/registration_flow_type/fee ✓; EN `club.columns.display_name`=`Name`, IT=`Nome`, `label` IT→EN fallback `Club` ✓; arch/boundary green — `NoEloquentWriteInOperatorPanelRule` (phpstan) + `ModuleBoundariesTest` confirm the read path imports only `Parties\Models\Club` and `CreateClub` imports `Parties\Actions\CreateClub` + `Parties\Enums\ClubRegistrationFlowType` (the {Models, Actions, Enums} carve-out — first production code to exercise the group-1 Enums admission) ✓.
- **Learnings for future iterations:**
  - **Task-text error caught (lessons.md):** task 2.1 said the producer column reads `$r->producer->display_name`, but `Producer` has **`name`**, not `display_name` (verified the `@property` block — `display_name` is the *Club*'s attribute). Used `$record->producer->name`; `->display_name` would have failed phpstan (`Access to an undefined property`). Verify every attribute against the model, never trust the task prose.
  - **CreateClub shipped REAL now (design.md > tasks.md wording).** tasks.md 2.2 says "scaffold CreateClub (real createViaAction lands in 3.1)", but design.md Risks says "scaffold Create… (with its real createViaAction)" and `OperatorConsoleCreateRecord` has two ABSTRACT methods (`createViaAction`/`createRejectionField`) — a clean scaffold must implement them, and a throw-stub is hacky + dead. So `CreateClub` is the real write-through page now (predecessor §1 pattern exactly). **Task 3.1's remaining work = `ClubResource::form()` only** (the createViaAction + operand-enum/Money/createRejectionField are DONE); **3.2 = `fields.{amount,currency}`** (`actions.create` already added with the resource i18n, predecessor §1 precedent); the group-3 `ClubCreateConsoleTest` exercises this createViaAction end-to-end. The `createViaAction` is dormant until 3.1's form() (no form on the Resource yet → empty create form → method not exercised by group-2 tests; phpstan validates its types now).
  - **Boolean infolist fields → `Filament\Infolists\Components\IconEntry` + `->boolean()`** (no codebase precedent — Producer had no booleans; i18n-safe: localized `->label()`, the value is an icon, nothing for the sink scanner to flag). The Money fee renders via `getStateUsing` (`$fee->minorUnits.' '.$fee->currency->value`) — assign `$fee = $record->fee` to a local so phpstan narrows `Money|null → Money` after the null check; the `' '` separator is inside `getStateUsing`, not a sink, so it is not flagged (cf. the predecessor's `implode(', ')`).
  - **Next → group 3 (task 3.1/3.2):** add `ClubResource::form()` (display_name required TextInput; `producer_id` required Select off `Producer::query()`; `registration_flow_type` required Select; optional fee `amount` TextInput + `currency` Select; `generates_credit`/`invite_only` Toggles, defaults true/false; NO `status` field) + `fields.{amount,currency}` i18n + `ClubCreateConsoleTest` (valid submit → `active` + 1 `ClubCreated`; fee present→Money / blank→null; bad Producer → `MissingClubProducer` on `producer_id`; `assertFormFieldDoesNotExist('status')`). Then group 4 = `ViewClub` sunset/close verbs.
---

## [2026-06-21 10:04] — 3.1 / 3.2 Club create surface (form + create-field i18n)
- **What:** the Club write-through Create surface — the group-3 green unit (form + its labels are inseparable; invariant 12). Added `ClubResource::form()` (display_name required `TextInput`; `producer_id` required model-driven `Select` off `Producer::query()`; `registration_flow_type` required enum-driven `Select`; optional fee `amount` `TextInput`->numeric + `currency` `Select`; `generates_credit`/`invite_only` `Toggle` defaults true/false; **no `status`**) + three private option helpers (`producerOptions` model-driven; `registrationFlowTypeOptions`/`currencyOptions` enum-driven). The `CreateClub` page's `createViaAction` was already shipped (group 2) — but its `amount` guard (`is_string||is_int`) rejected the float a `->numeric()` field dehydrates: fixed to `is_numeric()` (the only code change to CreateClub). Added EN+IT `club.fields.{display_name,producer,registration_flow_type,amount,currency}` (all IT≠EN for the group-5 IT-differs guard); `actions.create` already present.
- **Files changed:** `app/Modules/OperatorPanel/Filament/Resources/Parties/ClubResource.php` (+form + 3 helpers + 6 imports); `…/ClubResource/Pages/CreateClub.php` (amount guard `is_numeric`); `lang/{en,it}/operator_console.php` (+5 `club.fields.*` each); NEW `tests/Feature/Modules/OperatorPanel/Parties/ClubCreateConsoleTest.php` (4 it()). No migration/composer/protected-file change.
- **Quality loop: green** — pint clean; filtered `ClubCreateConsoleTest|ClubResourceTest` 9/9 (63 assn); full SQLite suite **1215/1215** (6922 assn, +4 tests vs 1211); phpstan 0 (the `collect(...)->mapWithKeys` enum-options + the `NoEloquentWriteInOperatorPanelRule` over the new read-query helper both clean); pint --test clean; `ModuleBoundariesTest` 3/3 (189 assn) — admits `ClubRegistrationFlowType` imported in BOTH `CreateClub` (construction) and `ClubResource` (Select options), **proving Group 1**; `openspec validate … --strict` valid; composer diff vs `main` empty.
- **Acceptance walked (group 3):** valid submit → Club `active` + exactly one `ClubCreated` (`actor_role` NewcoOps, `actor_id`=operator, `entity_type` `Club`) ✓; `amount`+`currency` → `fee` = `Money(50000, EUR)` ✓, blank → `fee` null ✓; non-existent `producer_id` (999999) → `MissingClubProducer` surfaced on `producer_id`, no Club + no event ✓; form exposes display_name/producer_id/registration_flow_type/amount/currency/generates_credit/invite_only and **no** `status` ✓; no `$model->save()` (phpstan rule green) ✓.
- **Learnings for future iterations:**
  - **`->numeric()` dehydrates as float** — the latent CreateClub guard bug surfaced only when the form first exercised the page (group 2 shipped the page un-exercised). Narrow numeric inputs with `is_numeric()`, never `is_string`/`is_int`. Recorded in lessons.md + Codebase Patterns. **ProducerAgreement create (8.1) has NO numeric/Money field** (ids/dates/string only — D7), so it won't hit this; but watch any future fee/quantity input.
  - **Form i18n convention = `fields.*` for every input** (even those re-labelling a column). The 5 new `club.fields.*` are authored EN+IT with distinct values; group-5's `ClubConsoleI18nTest` (5.1) will enumerate `columns.*` + `actions.{sunset,close,create}` + `notifications.*` + the create `fields.*` — all now present except the lifecycle `actions.{sunset,close}` + `notifications.{sunset,closed,action_failed}` which land with group 4 (ViewClub).
  - **Next → group 4 (task 4.1/4.2):** fill `ViewClub` — `extends ViewRecord` + `use SurfacesDomainActions`, `i18nKey()='club'`, two form-less verbs via `lifecycleAction` (no `confirmationKey`, D3): `sunset`→`sunset`→`app(SunsetClub::class)->handle($this->recordOf(Club::class,$r)->id)`; `close`→`closed`→`app(CloseClub::class)->handle(...->id)`; **no `activate`** (D9), close-from-`active` rejected (`IllegalClubTransition`). + `actions.{sunset,close}` & `notifications.{sunset,closed,action_failed}` i18n + `ClubLifecycleConsoleTest`.
---

## [2026-06-21 10:15] — 4.1 / 4.2 Club status lifecycle (sunset + close on ViewClub)
- **What:** the Club write-through STATUS surface — filled the bare `ViewClub` stub with the two form-less lifecycle verbs (the group-4 green unit; the page + its i18n + the test are one slice). `ViewClub` now `extends ViewRecord` + `use SurfacesDomainActions` (NOT the catalog `OperatorConsoleViewRecord`, D1), `i18nKey()='club'`, and `getHeaderActions()` returns exactly two `lifecycleAction`s — `sunset`→`sunset`→`app(SunsetClub::class)->handle($this->recordOf(Club::class,$r)->id)` and `close`→`closed`→`app(CloseClub::class)->handle(…->id)` (D4) — both **form-less, no `confirmationKey`** (single-operator, D3). **No `activate`** verb (a Club is born `active` — D9). Added EN+IT `club.actions.{sunset,close}` + the new `club.notifications.{sunset,closed,action_failed}` block (IT distinct from EN for the group-5 IT-differs guard: `Dismetti`/`Chiudi`, `Club dismesso.`/`Club chiuso.`, `Impossibile completare l'azione.`).
- **Files changed:** `app/Modules/OperatorPanel/Filament/Resources/Parties/ClubResource/Pages/ViewClub.php` (stub → real, +2 Action imports + Model/Club/SunsetClub/CloseClub + `OperatorConsoleViewRecord` for the D1 `{@see}`); `lang/{en,it}/operator_console.php` (+`club.actions.{sunset,close}` + `club.notifications` block); NEW `tests/Feature/Modules/OperatorPanel/Parties/ClubLifecycleConsoleTest.php` (5 it()). No migration/composer/protected-file change.
- **Quality loop: green** — pint clean; filtered `ClubLifecycleConsoleTest` 5/5 (52 assn); full SQLite suite **1220/1220** (6974 assn, +5 tests vs 1215); phpstan 0 (`NoEloquentWriteInOperatorPanelRule` over the new `ViewClub` green — all writes route through the actions); pint --test clean; `openspec validate … --strict` valid; composer diff vs `main` empty.
- **Acceptance walked (group 4):** sunset an `active` Club → `sunset` + exactly one `ClubSunset` (`actor_role` NewcoOps, `actor_id`=operator, `entity_type` `Club`) ✓; close a `sunset` Club → `closed` + one `ClubClosed` ✓; **close-from-`active` rejected** (`IllegalClubTransition`) → `action_failed` danger, Club stays `active`, 0 events (the D9 OR-branch) ✓; out-of-state sunset (a `closed` Club) → `action_failed`, unchanged, 0 events ✓; scope-guards: `assertActionExists('sunset'/'close')` each `! isConfirmationRequired()`, `assertActionDoesNotExist('activate'/'submit'/'reject'/'reopen'/'supersede')` ✓.
- **Learnings for future iterations:**
  - **The trait-reuse lifecycle template is now twice-proven (Producer + Club).** `ViewClub` is `ViewProducer` minus the four KYC verbs, with `activate`→`sunset`/`close`. Verb→successKey is NOT always identity: `sunset`→`sunset` but `close`→`closed` (the success notification key is the past-participle state, not the verb). The label key is `Str::snake(verb)` (`close`→`actions.close`), the success key is passed explicitly.
  - **`{@see OperatorConsoleViewRecord}` in the docblock is intra-module (OperatorPanel) — imported + Pint-clean, no boundary violation** (D5 note). Domain exceptions (`IllegalClubTransition`) stay PROSE-only; `CreateClub` (the Action, not imported here) is also prose. Only `SunsetClub`/`CloseClub`/`Club` (used in code) are imported + `{@see}`-able.
  - **Next → group 5 (task 5.1):** `ClubConsoleI18nTest` — 5 guards mirroring `ProducerConsoleI18nTest`, enumerating the kit-contract keys (label/plural_label/columns.*/actions.{sunset,close,create}/notifications.{sunset,closed,action_failed}/fields.*), IT⊆EN filtered by `str_starts_with($dotKey,'club.')`, sink scan scoped to `ClubResource*` via the reused `scanOperatorConsoleHardcodedSinks` (run via `--filter`, NOT a bare path). All enumerated keys now exist (resource i18n from group 2 + create fields from group 3 + lifecycle from group 4). Then group 6 = PG17 closing chain.
---

## [2026-06-21 10:24] — 5.1 Club i18n kit-key completeness (EN/IT)
- **What:** added `ClubConsoleI18nTest.php` — the Club console's capability-close i18n guard, the FIVE-guard kit-key-completeness pattern mirroring `ProducerConsoleI18nTest`. Two top-level dataset fns `clubConsoleKitKeys()` (21 keys) + `clubConsoleItDiffersKeys()` (= kit minus `label`/`plural_label` = 19). Guards: (1) EN baseline over the 21-key kit; (2) IT-differs (authored in `it` AND `!= en`) over the 19; (3) `label`/`plural_label` EN-fallback (DEC-127); (4) IT⊆EN over the `club.*` dotted block (filter `str_starts_with($dotKey,'club.')`); (5) reused `scanOperatorConsoleHardcodedSinks()` behind `function_exists`, files scoped `str_contains($pathname,'ClubResource')`. Enumerated EVERY key the console resolves — the string-concat kit keys (label/plural_label/columns.version/actions.{sunset,close}/notifications.{sunset,closed,action_failed}) AND the resource's literal `__()` keys (columns.{display_name,producer,registration_flow_type,status}, all 8 `fields.*` incl. the infolist-only `fee`, actions.create). No source/lang change — every enumerated key was already authored across groups 2–4.
- **Files changed (new):** `tests/Feature/Modules/OperatorPanel/Parties/ClubConsoleI18nTest.php`. No source/i18n/migration/composer/protected-file change.
- **Quality loop: green** — pint clean; filtered `ClubConsoleI18nTest` 44/44 (89 assn, = 21 EN + 19 IT + 2 fallback + 1 IT⊆EN + 1 sink); full SQLite suite **1264/1264** (7063 assn, +44 vs 1220); phpstan 0; pint --test clean; `openspec validate … --strict` valid; composer diff vs `main` empty; git status = only the new test file.
- **Acceptance walked (group 5):** the enumeration is green ✓; **non-vacuity proven** — temporarily dropping `club.fields.fee` from `lang/en` red-flagged BOTH guard (1) (`expected operator_console.club.fields.fee to be authored in en`) and guard (4) (IT's `club.fields.fee` dangles), then restored ✓; the sink scan finds zero hardcoded strings in `…/Resources/Parties/ClubResource*` ✓; ran via `--filter`/full suite, not a bare path ✓.
- **Learnings for future iterations:**
  - **Consolidated the i18n-test pattern in Codebase Patterns** (group 10 = `ProducerAgreementConsoleI18nTest` repeats it verbatim, filter `producer_agreement.`, scope `ProducerAgreementResource`, and watch the `producer_wide` placeholder + the nullable `club` column key). The pattern's load-bearing subtleties: the trailing dot in the `str_starts_with` filter; enumerate literal `__()` keys too (not just string-concat); include infolist-only keys (`fields.fee`); run via `--filter` (the sink helper is in another file).
  - **Test count is mechanical:** |kit| EN + |differs| IT + 2 EN-fallback + 1 IT⊆EN + 1 sink. Club = 21+19+2+1+1 = 44. ProducerAgreement's kit will differ (its columns are producer/club/status/term_start/term_end/version + producer_wide; verbs activate/terminate; create fields producer/club/term_start/term_end/settlement_cadence) → recompute.
  - **Next → group 6 (task 6.1):** `ClubConsoleChainTest.php` — one `it()` driving a Club through the console PAGES (`CreateClub` → `ViewClub` sunset → close), asserting the emergent event set `toEqualCanonicalizing(['ClubCreated','ClubSunset','ClubClosed'])`, every event `module==='parties'` + `actor_role===ActorRole::NewcoOps` + non-null `actor_id`. Must be green on SQLite AND PG17 (preamble command — append the Catalog i18n file for the folder-wide run; loose `toEqual` for PG numeric-string `actor_id`).
---

## [2026-06-21 10:33] — 6.1 Club full closing-chain integration test (PG17)
- **What:** added `ClubConsoleChainTest.php` — the Club console's CLOSING integration proof, one `it()` driving a Club through the WHOLE slice end-to-end via the **pages** (the closing-integration rule, `knowledge/testing/rules.md`), mirroring the predecessor `ProducerConsoleChainTest` minus the KYC steps + the Producer→Club cascade. Flow: seed an operating Producer event-free (`Producer::factory()`) → `Livewire::test(CreateClub::class)->fillForm([...])->call('create')` (Club born `active`, 1 `ClubCreated`) → re-instantiate `Livewire::test(ViewClub::class,['record'=>$id])->callAction('sunset')` (active→sunset, 1 `ClubSunset`) → re-instantiate `->callAction('close')` (sunset→closed, 1 `ClubClosed`). Asserts (a) the emergent set `DomainEvent::pluck('name')->all()` `->toEqualCanonicalizing(['ClubCreated','ClubSunset','ClubClosed'])`; (b) `toHaveCount(3)` + `foreach` every event `module==='parties'` / `actor_role===ActorRole::NewcoOps` / non-null `actor_id`; (c) representative `actor_id` `toEqual($operator->id)` on `ClubCreated` (create surface) + `ClubClosed` (view surface), loose for PG numeric-string. `DatabaseMigrations`.
- **Files changed (new):** `tests/Feature/Modules/OperatorPanel/Parties/ClubConsoleChainTest.php`. No source/i18n/migration/composer/protected-file change (test-only — every key/page already shipped groups 2–5).
- **Quality loop: green** — pint clean; filtered `ClubConsoleChainTest` 1/1 (29 assn); full SQLite suite **1265/1265** (7092 assn, +1 vs 1264); phpstan 0; pint --test clean; **PG17 folder-wide run green 142/142** (683 assn — `tests/Feature/Modules/OperatorPanel/Parties` + the appended `Catalog/ProductMasterConsoleI18nTest.php` so the shared `scanOperatorConsoleHardcodedSinks` helper loads); `openspec validate … --strict` valid; composer diff vs `main` empty; git status = only the new test file.
- **Acceptance walked (group 6):** drives `CreateClub`→`ViewClub` sunset→close through the pages ✓; emergent set `toEqualCanonicalizing(['ClubCreated','ClubSunset','ClubClosed'])` ✓; every event module/role/actor_id envelope ✓; representative `actor_id` `toEqual` operator on both surfaces ✓; green SQLite AND PG17, phpstan 0, pint clean ✓.
- **Learnings for future iterations:**
  - **Verified-before-asserting the emergent-set count:** grep'd `app/` for `ClubCreated|ClubSunset|ClubClosed` and found ONLY emitters/definitions (events, actions, enum, model, exception, the CreateClub page) — zero listeners/projectors. So create+sunset+close emits EXACTLY 3 domain events; nothing consumes a Club lifecycle event into the recorder (a projector would write a read-model row, never a domain event — the predecessor pinned this via its `toHaveCount(5)` with 2 `ClubSunset`). Always confirm "no projection event leaks" before a `toEqualCanonicalizing`/`toHaveCount` over the whole `domain_events` log.
  - **Re-instantiate `Livewire::test(ViewClub::class, ['record'=>$id])` per `callAction`** (not chained) — the proven predecessor idiom, faithful to "operator re-opens the page each time" and green on both engines. Both `sunset`/`close` actions always exist (no state-gated visibility — the lifecycle test asserts them unconditionally), and the domain action re-reads the from-state from the DB by id, so the order is enforced by the FSM, not the page.
  - **Next → group 7 (task 7.1/7.2/7.3):** the THIRD Parties console — `ProducerAgreementResource` read surface (Resource + List + scaffold Create/View pages for the `getPages()` boot + resource i18n), same one-green-unit shape as Club group 2. Watch the **nullable `club` column** (`$r->club?->display_name ?? __('…producer_wide')` — the `producer_wide` placeholder) and the `settlement_cadence` infolist-only field. Then 8 create / 9 lifecycle (activate/terminate, **no supersede verb** — D8 side-effect) / 10 i18n / 11 PG17 chain (the supersession OR-branch with `causation_id`).
---
