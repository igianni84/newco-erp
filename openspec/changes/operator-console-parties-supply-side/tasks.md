# Tasks — operator-console-parties-supply-side

> Pure operator surface over shipped Parties domain Actions. No new domain code, no migration, no composer dep. Read the predecessor `## Codebase Patterns` (`openspec/changes/archive/2026-06-20-operator-console-parties-producer/progress.md`) and this change's `design.md` (D1–D12) before starting. The ADR (`decisions/2026-06-21-operator-console-operand-enum-carveout.md`) is authored with this change — implement to it, do not re-decide it. **Group 1 (carve-out) is a prerequisite of Group 3 (Club create) — sequence it first.** Every task ends with the quality loop green: `vendor/bin/pint`, `php -d memory_limit=-1 vendor/bin/pest` (filtered + full), `php -d memory_limit=-1 vendor/bin/phpstan analyse`, `vendor/bin/pint --test`, `openspec validate operator-console-parties-supply-side --strict`, composer diff vs `main` empty, no protected-file edits. PG17 closing-chain command (Groups 6 + 11): `DB_CONNECTION=pgsql DB_HOST=127.0.0.1 DB_PORT=55432 DB_DATABASE=newco_test DB_USERNAME=newco DB_PASSWORD=newco php -d memory_limit=-1 vendor/bin/pest tests/Feature/Modules/OperatorPanel/Parties` (append `tests/Feature/Modules/OperatorPanel/Catalog/ProductMasterConsoleI18nTest.php` for the folder-wide run so the shared `scanOperatorConsoleHardcodedSinks` helper loads).

## 1. Widen the OperatorPanel carve-out to admit operand enums (ADR 2026-06-21)

- [x] 1.1 In `tests/Architecture/ModuleBoundariesTest.php`, add `$other->namespace().'\\Enums'` to the OperatorPanel branch of `moduleBoundaryAllowedImports()` (alongside `Models` + `Actions`, inside the `if ($source === Module::OperatorPanel)` block). Update the carve-out guard test (`it('scopes the operator-console Models/Actions carve-out to OperatorPanel only')`) to assert `Enums` is admitted for every operated module **for OperatorPanel only** and is **not** admitted for a lateral module (Catalog). Update the file's header carve-out comment to record that operand enums are admitted (cite the ADR); the "render enum casts via their instances" line is no longer the whole story. Do **not** widen any lateral module. (D7)
- **Tests** (`ModuleBoundariesTest.php` itself): both existing `it()` blocks stay green; the carve-out guard now asserts `in_array($operated->namespace().'\\Enums', $operatorPanelAllows, true)` is **true**, `in_array('App\\Modules\\Parties\\Enums', $catalogAllows, true)` is **false**, and the whole-module `in_array($operated->namespace(), $operatorPanelAllows, true)` stays **false** (the exception still cannot widen to a whole module). phpstan + pint clean.

## 2. Club read-only Resource + list + resource i18n

- [x] 2.1 Create `app/Modules/OperatorPanel/Filament/Resources/Parties/ClubResource.php` — `extends OperatorConsoleResource` (reuse label helpers off `static i18nKey(): string => 'club'`, and `versionColumn()`). Read-bind `\App\Modules\Parties\Models\Club`. Define a **`status`** badge `TextColumn` rendering `ClubStatus->value` via `getStateUsing` + `instanceof BackedEnum` (**no `Parties\Enums` import** — state enum via cast, D2). List columns: display_name · operating Producer (`fn (Club $r) => $r->producer->display_name` — read, no `Producer` import needed beyond Models) · registration_flow_type (via cast `->value`) · status · `versionColumn()`. Infolist: display_name / operating Producer / registration_flow_type / fee / generates_credit / invite_only. No create/edit/delete mutating action. `getPages()` → index/create/view. Do **NOT** call `lifecycleStateColumn()` (D2).
- [x] 2.2 Create `…/ClubResource/Pages/ListClubs.php` — the header create-LINK page (copy the shape from `ListProducers`, not a `CreateAction`). Scaffold `…/Pages/CreateClub.php` (real `createViaAction` lands in 3.1) and `…/Pages/ViewClub.php` (bare `ViewRecord` stub; verbs land in 4.1) so the Resource's `getPages()` boots (the eager-reference coupling — design Risks). (D6)
- [x] 2.3 Add the `operator_console.club.*` resource/column copy to `lang/en/operator_console.php` (full) and `lang/it/operator_console.php` (per-key; `label`/`plural_label` may fall back to EN): `label`, `plural_label`, `columns.{display_name,producer,registration_flow_type,status,version}`, and the infolist field labels.
- **Tests** (`tests/Feature/Modules/OperatorPanel/Parties/ClubResourceTest.php`, `RefreshDatabase`): a seeded Club renders in the list with its `status`; `ClubResource::getPages()` keys == `['index','create','view']`; the table exposes no edit/delete/bulk action. Arch/boundary: `NoEloquentWriteInOperatorPanelRule` + `ModuleBoundariesTest` stay green over the new class.

## 3. Club create surface (operand enum + Money + flags)

- [x] 3.1 Create `…/ClubResource/Pages/CreateClub.php` — `extends OperatorConsoleCreateRecord`; alias `use App\Modules\Parties\Actions\CreateClub as CreateClubAction;` (D6). Form (on `ClubResource::form()`): `display_name` required `TextInput`; an operating **Producer** required `Select` (options from `Producer::query()` — within `{Models}`); `registration_flow_type` required `Select`; optional **fee** as `amount` (`TextInput`, integer minor units) + `currency` (`Select`, ISO 4217); `generates_credit` `Toggle` default true; `invite_only` `Toggle` default false. **No `status` field.** `createViaAction(array $data): Model` narrows the payload, **constructs the `ClubRegistrationFlowType` operand enum** from the selected value (the carve-out from Group 1 — D7), assembles `Money::of($amount, Currency::of($code))` only when both present else `null` (D11), and calls `app(CreateClubAction::class)->handle(displayName, producerId, registrationFlowType, fee, generatesCredit, inviteOnly)`. `createRejectionField(): string => 'producer_id'` (a `MissingClubProducer` from a bad Producer lands there). No `$model->save()`.
- [x] 3.2 Add the create-field labels to `lang/{en,it}/operator_console.club.fields.*` and `operator_console.club.actions.create`.
- **Tests** (`ClubCreateConsoleTest.php`, `DatabaseMigrations`): submitting a valid Club invokes the action → a Club in `active` + exactly one `ClubCreated` event (`actor_role: newco_ops`, `actor_id` = the acting operator, `entity_type` `Club`); fee supplied → `fee` is the `Money` of that amount+currency, fee blank → `fee` null; a Producer id that does not exist → `MissingClubProducer` surfaced on `producer_id`, no Club + no event; the form exposes display_name/producer/registration_flow_type/fee/generates_credit/invite_only and **no** `status` field (`assertFormFieldDoesNotExist('status')`). Boundary: `ModuleBoundariesTest` green with the `ClubRegistrationFlowType` construction (proves Group 1). No `$model->save()` (phpstan rule green).

## 4. Club status lifecycle — sunset + close (ViewClub)

- [x] 4.1 Fill `…/ClubResource/Pages/ViewClub.php` — `extends \Filament\Resources\Pages\ViewRecord` and `use SurfacesDomainActions` (NOT `OperatorConsoleViewRecord`, D1). `i18nKey(): string => 'club'`. `getHeaderActions()` returns the two status actions via `lifecycleAction` — **form-less, no `confirmationKey`** (no SoD affordance, D3): `sunset`→`sunset`→`fn (Model $r) => app(SunsetClub::class)->handle($this->recordOf(Club::class,$r)->id)`; `close`→`closed`→`app(CloseClub::class)->handle(...->id)` (D4). **No `activate` verb** (D9). Each closure has the uniform `(Model $record, string $notes)` signature (`$notes` unused).
- [x] 4.2 Add `operator_console.club.actions.{sunset,close}`, `notifications.{sunset,closed,action_failed}` to `lang/{en,it}`.
- **Tests** (`ClubLifecycleConsoleTest.php`, `DatabaseMigrations`): sunset an `active` Club → `sunset` + exactly one `ClubSunset` (`actor_role: newco_ops`, `actor_id` = operator); close a `sunset` Club → `closed` + one `ClubClosed`; **close-from-`active` is rejected** (`IllegalClubTransition`) → `action_failed` danger notification, Club stays `active`, no event (the OR-branch, D9); out-of-state sunset (not `active`) / close (not `sunset`) → `action_failed`, state + event log unchanged; **scope-guards**: `assertActionExists('sunset'/'close')`, each with `! $action->isConfirmationRequired()`, and `assertActionDoesNotExist('activate'/'submit'/'reject'/'reopen'/'supersede')`.

## 5. Club i18n kit-key completeness (EN/IT)

- [x] 5.1 Add `ClubConsoleI18nTest.php` enumerating the **kit-contract** keys this console resolves by string concatenation: resource `label`/`plural_label`/`columns.{display_name,producer,registration_flow_type,status,version}`; per the two verbs `actions.{sunset,close}` + `actions.create`; `notifications.{sunset,closed,action_failed}`; plus the create `fields.*`. Five guards mirroring `ProducerConsoleI18nTest` — EN baseline (`Lang::has("operator_console.club.{$suffix}", 'en', false)`), IT-differs (authored in `it` AND `!= en`) over the kit-minus-`{label,plural_label}` set, `label`/`plural_label` EN-fallback, IT⊆EN over the `club.*` dotted block (filter by `str_starts_with($dotKey, 'club.')` — guard against sweeping `product_master.columns.producer`-style collisions), and the reused `scanOperatorConsoleHardcodedSinks()` behind a `function_exists` guard scoped to `ClubResource*`.
- **Tests**: the enumeration is green (a deliberately removed key fails it); the sink scan finds zero hardcoded strings in `…/Resources/Parties/ClubResource*`. Run via `--filter=ClubConsoleI18nTest` or the full suite, NOT a bare file path (the helper-load false-red — design Risks).

## 6. Club full closing-chain integration test (PG17)

- [x] 6.1 Add `ClubConsoleChainTest.php` — one `it()` driving a Club through the console **pages**: `Livewire::test(CreateClub::class)->fillForm([...])->call('create')` (an existing operating Producer seeded event-free via `Producer::factory()`), then `Livewire::test(ViewClub::class, ['record' => $id])->callAction('sunset')` → `callAction('close')`. Assert: the emergent event set `DomainEvent::query()->pluck('name')->all()` `->toEqualCanonicalizing(['ClubCreated','ClubSunset','ClubClosed'])`; a `foreach` asserting every event `module === 'parties'`, `actor_role === ActorRole::NewcoOps`, `actor_id` non-null; a representative `actor_id` equals the acting operator (loose `toEqual` — PG returns a numeric string).
- **Tests**: green on SQLite AND PG17 (the command in the preamble); phpstan 0; pint clean.

## 7. ProducerAgreement read-only Resource + list + resource i18n

- [x] 7.1 Create `app/Modules/OperatorPanel/Filament/Resources/Parties/ProducerAgreementResource.php` — `extends OperatorConsoleResource` (`static i18nKey(): string => 'producer_agreement'`, `versionColumn()`). Read-bind `\App\Modules\Parties\Models\ProducerAgreement`. Define a **`status`** badge `TextColumn` rendering `ProducerAgreementStatus->value` via `getStateUsing` + `instanceof BackedEnum` (no `Parties\Enums` import, D2). List columns: producer (`fn (ProducerAgreement $r) => $r->producer->display_name`) · club (`fn (ProducerAgreement $r) => $r->club?->display_name ?? __('…producer_wide')` — nullable → "Producer-wide") · status · term_start · term_end · `versionColumn()`. Infolist adds settlement_cadence. No mutating action. `getPages()` → index/create/view. No `lifecycleStateColumn()`.
- [x] 7.2 Create `…/ProducerAgreementResource/Pages/ListProducerAgreements.php` (header create-LINK). Scaffold `…/Pages/CreateProducerAgreement.php` (real `createViaAction` in 8.1) + `…/Pages/ViewProducerAgreement.php` (bare `ViewRecord` stub; verbs in 9.1) for the `getPages()` boot. (D6)
- [x] 7.3 Add the `operator_console.producer_agreement.*` resource/column copy to `lang/{en,it}/operator_console.php`: `label`, `plural_label`, `columns.{producer,club,status,term_start,term_end,version}`, the `producer_wide` placeholder, and the infolist labels.
- **Tests** (`ProducerAgreementResourceTest.php`, `RefreshDatabase`): a seeded agreement renders in the list with its `status`; a Producer-wide agreement (null `club_id`) renders the "Producer-wide" placeholder; `getPages()` keys == `['index','create','view']`; no edit/delete/bulk action. Boundary green.

## 8. ProducerAgreement create surface

- [x] 8.1 Create `…/ProducerAgreementResource/Pages/CreateProducerAgreement.php` — `extends OperatorConsoleCreateRecord`; alias `use App\Modules\Parties\Actions\CreateProducerAgreement as CreateProducerAgreementAction;` (D6). Form (on the Resource): a required **Producer** `Select` (options from `Producer::query()`); an optional **Club** `Select` (options from `Club::query()`; blank = Producer-wide); optional `term_start`/`term_end` `DatePicker`; optional `settlement_cadence` `TextInput` (free string). **No `status` field. No operand enum** (ids/dates/string only — stays within `{Models, Actions}`, D7). `createViaAction(array $data): Model` narrows and calls `app(CreateProducerAgreementAction::class)->handle(producerId, clubId, termStart, termEnd, settlementCadence)`, coercing blank Club/dates/cadence to `null`. `createRejectionField(): string => 'producer_id'` (a `MissingAgreementProducer` lands there). No `$model->save()`.
- [x] 8.2 Add the create-field labels to `lang/{en,it}/operator_console.producer_agreement.fields.*` and `…actions.create`.
- **Tests** (`ProducerAgreementCreateConsoleTest.php`, `DatabaseMigrations`): a valid submit → a ProducerAgreement in `draft` + exactly one `ProducerAgreementCreated` (`actor_role: newco_ops`, `actor_id` = operator, `entity_type` `ProducerAgreement`); leaving the Club blank → `club_id` null (Producer-wide); a Producer id that does not exist → `MissingAgreementProducer` surfaced on `producer_id`, no agreement + no event; **two drafts in the same Producer scope both succeed** (2 rows, 2 `ProducerAgreementCreated` — single-active not enforced at create); the form exposes producer/club/term_start/term_end/settlement_cadence and **no** `status` field. No `$model->save()`.

## 9. ProducerAgreement status lifecycle — activate + terminate (ViewProducerAgreement)

- [x] 9.1 Fill `…/ProducerAgreementResource/Pages/ViewProducerAgreement.php` — `extends \Filament\Resources\Pages\ViewRecord` and `use SurfacesDomainActions` (D1). `i18nKey(): string => 'producer_agreement'`. `getHeaderActions()` returns the two status actions via `lifecycleAction` — form-less, no `confirmationKey` (D3): `activate`→`activated`→`fn (Model $r) => app(ActivateProducerAgreement::class)->handle($this->recordOf(ProducerAgreement::class,$r)->id)`; `terminate`→`terminated`→`app(TerminateProducerAgreement::class)->handle(...->id)` (D4). **No `supersede` verb** — supersession is `ActivateProducerAgreement`'s side-effect (D8).
- [x] 9.2 Add `operator_console.producer_agreement.actions.{activate,terminate}`, `notifications.{activated,terminated,action_failed}` to `lang/{en,it}`.
- **Tests** (`ProducerAgreementLifecycleConsoleTest.php`, `DatabaseMigrations`): activate a `draft` with no prior active in scope → `active` + exactly one `ProducerAgreementActivated` and **no** `ProducerAgreementSuperseded` (`actor_role: newco_ops`, `actor_id` = operator); **supersession** — a prior `active` agreement + a new `draft` in the same `(producer_id, club_id)` scope, the draft activated through the console → draft becomes `active`, prior becomes `superseded`, one `ProducerAgreementSuperseded` carrying the `ProducerAgreementActivated` event's id as `causation_id` (the OR-branch, D8); **terminate** an `active` agreement → `terminated` + one `ProducerAgreementTerminated`, and the agreement's Producer `status` is **unchanged** (no cascade, D-spec); out-of-state activate (not `draft`) / terminate (not `active`) → `action_failed`, state + event log unchanged; **scope-guards**: `assertActionExists('activate'/'terminate')` each `! isConfirmationRequired()`, `assertActionDoesNotExist('supersede'/'submit'/'reject'/'reopen'/'activate'…)` — specifically assert **no `supersede`** action.

## 10. ProducerAgreement i18n kit-key completeness (EN/IT)

- [x] 10.1 Add `ProducerAgreementConsoleI18nTest.php` enumerating the kit-contract keys: `label`/`plural_label`/`columns.{producer,club,status,term_start,term_end,version}`/`producer_wide`; `actions.{activate,terminate,create}`; `notifications.{activated,terminated,action_failed}`; the create `fields.*`. Same five guards as 5.1, IT⊆EN filtered by `str_starts_with($dotKey, 'producer_agreement.')`, sink scan scoped to `ProducerAgreementResource*`, the `scanOperatorConsoleHardcodedSinks` helper reused behind `function_exists`.
- **Tests**: enumeration green (a removed key fails); sink scan finds zero hardcoded strings in `…/Resources/Parties/ProducerAgreementResource*`. Run via `--filter`/full suite, not a bare path.

## 11. ProducerAgreement full closing-chain integration test (PG17)

- [ ] 11.1 Add `ProducerAgreementConsoleChainTest.php` — one `it()` driving the whole slice through the **pages**: seed a Producer event-free (`Producer::factory()`); `Livewire::test(CreateProducerAgreement::class)->fillForm([... producer A, no club ...])->call('create')` (agreement **A**, draft) → `callAction('activate')` on A (→active); create agreement **B** in the same scope (draft) → `callAction('activate')` on B (→active, A→superseded) → `callAction('terminate')` on B (→terminated). Assert: the emergent event set `->toEqualCanonicalizing(['ProducerAgreementCreated','ProducerAgreementActivated','ProducerAgreementCreated','ProducerAgreementActivated','ProducerAgreementSuperseded','ProducerAgreementTerminated'])` (create×2, activate×2, supersede×1, terminate×1); a `foreach` asserting every event `module === 'parties'`, `actor_role === ActorRole::NewcoOps`, `actor_id` non-null; the `ProducerAgreementSuperseded` carries B's `ProducerAgreementActivated` event id as `causation_id` (loose `toEqual`); a representative `actor_id` equals the operator.
- **Tests**: green on SQLite AND PG17 (preamble command); phpstan 0; pint clean; `openspec validate operator-console-parties-supply-side --strict` OK; composer diff vs `main` empty.
