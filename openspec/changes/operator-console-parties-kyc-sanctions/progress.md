# Progress — operator-console-parties-kyc-sanctions

## Codebase Patterns
(consolidated reusable patterns — read first each iteration)

- **Surface-extension prep: relax the prior slice's absence-guard FIRST (keep every iteration green).** When this slice adds a verb a shipped test asserted absent (`assertActionDoesNotExist('verb')`), the group-1 prep task drops that ONE assertion *before* the verb lands — otherwise the moment the verb is wired the old guard turns red. Drop only the now-stale id; KEEP the guards for verbs still out of scope (here `submit`/`reject`/`reopen` — the Customer FSM is not review-governed). Also fix the stale prose (file header + test title) that named the verb "deliberately ABSENT" so the file isn't self-contradictory, and re-point the reader to where the verb is now pinned (`CustomerKycSanctionsConsoleTest`).

- **i18n front-load discipline (group-1 prep, ahead of the surface).** When a console slice adds verbs/fields, author ALL their copy keys in BOTH `lang/en/operator_console.php` and `lang/it/operator_console.php` (every IT value DISTINCT from EN — the IT-differs guard) and add their dot-suffixes to the I18nTest's key contract (`customerConsoleKitKeys()` / sibling) in the prep task, BEFORE the resolving code lands. The `…ItDiffersKeys()` dataset derives from the kit-keys list via `array_diff(…, ['label','plural_label'])`, so a key added to the contract auto-flows into both the EN-baseline and the IT-differs datasets — never maintain two lists. Each slice's keys get an INLINE sub-group comment inside the existing `fields`/`actions`/`notifications` arrays (the Holds-slice precedent in the same `customer` block), leaving the per-block lead comments untouched. Net effect: the enumeration contract is green from iteration 1 and the surface tasks only wire behaviour. The I18nTest must run via `--filter` or the full suite (so `scanOperatorConsoleHardcodedSinks`, declared in `ProductMasterConsoleI18nTest`, is loaded — a bare path skips it).

- **Form-less, visibility-gated lifecycle verb (the cheap path for a bare-`int $id` Action).** When a domain Action takes only the entity `int $id` (no operands), the verb is the kit's `lifecycleAction($verb, $successKey, $invoke)` with `$form` left null — `$invoke = fn (Model $record, string $notes) => app(SomeAction::class)->handle($this->recordOf(Customer::class, $record)->id)` (`$notes` unused). To gate it to a legal from-state, chain `->visible(fn (): bool => $this->predicate($this->recordOf(Customer::class, $this->getRecord())))` onto the returned Action — the `->visible()` closure reads `$this->getRecord()` (the page record), evaluated per-record. Extract each predicate to a `private` helper so the magic-string token + the complement-of-guard rationale (design D4) live in ONE place. Predicate a STATE enum through its model CAST VALUE, NEVER an import (design D5): use `=== null || ->value === 'token'` when a prior `=== null` narrows the operand (non-nullsafe), or `?->value === 'token'` when the column is independently nullable (nullsafe — and PHPStan-max clean, no `nullsafe.neverNull`, because the cast is genuinely `Enum|null`). Because the predicate is the EXACT COMPLEMENT of the Action's domain from-state guard, a rejected transition is unreachable through the surface — prove its reject with a domain `toThrow` + `assertActionHidden`, never an `action_failed` the hidden verb can't raise (the Filament hidden-action landmine). The Action stays the sole writer; the console never re-checks the from-state.

- **Bespoke (multi-operand) form header action, built form-FIRST then write-through (the `placeHold` precedent).** When a verb carries operands the form-less `lifecycleAction()` cannot thread (a verdict + trigger-source; a Hold type/scope/profile), build it BESPOKE: a `private function fooAction(): Action` returning `Action::make('foo')->label(…)->schema([…Selects…])`, appended to `getHeaderActions()`. Split across two tasks: the FORM task ends the chain with a **no-op `->action(function (): void {})`** + the comment "write-through lands in task N.M" (a form-bearing Action must have an `->action()` to mount/render; the no-op keeps the iteration green while the test only `mountAction(...)` + `assertFormFieldExists(...)` — `getOptions()` resolves a `->options(Closure)` via `HasOptions::getOptions()`→`evaluate()`); the WRITE-THROUGH task swaps in `->action(fn (array $data) => …surfaceLifecycleOutcome(fn () => app(Action::class)->handle(…), $successTitle))`. Enum-backed Select options go through dedicated `private` helpers keyed `->value => ->value` (NEVER magic strings — the `holdTypeOptions`/`holdScopeOptions` precedent). A **record-dependent** option-set is a `->options(fn (): array => $this->fooOptions($this->recordOf(Customer::class, $this->getRecord())))` whose helper narrows by the page record's CAST VALUE — make the offered set the EXACT COMPLEMENT of the domain floor (e.g. drop `onboarding` once `last_screening_at !== null`, mirroring `RecordCustomerScreening`'s onboarding-already-screened guard), so a stale option simply drops while the domain still enforces (the same surface-hides + domain-enforces split as the visibility-gated verbs). Importing the operand enums (`Parties\Enums\*`) rides the OperatorPanel `{Models, Actions, Enums}` carve-out (namespace-prefix allow — `ModuleBoundariesTest` line 61; UNCHANGED); the STATE enum `KycStatus` is never imported.

- **Page header-action visibility-test API (Filament 5.6.7, pinned against installed source — NOT memory).** The header-action twin of the Holds table's `assertTableActionVisible/Hidden`. On `Livewire::test(ViewCustomer::class, ['record' => $id])`: `assertActionVisible('verb')` / `assertActionHidden('verb')` resolve a header action by name (`vendor/filament/actions/src/Testing/TestsActions.php` lines 217/231 → `assertActionExists` → `$action->isVisible()`/`isHidden()`) and evaluate its `->visible()` closure **per record** (empirically confirmed: same action class flips visible→hidden when the record's gated state changes). Mount-and-inspect a form action: `mountAction('verb')` + `assertFormFieldExists/Visible/Hidden` + `setActionData([…])` (the `placeHold` precedent; form assertions live in `vendor/filament/forms/src/Testing/TestsForms.php`). **D4 landmine (applies to header actions, confirmed):** `callAction()` (TestsActions line 84) asserts-visible-FIRST and `mountAction` is a no-op on a hidden action (server-side re-resolution drops it; `assertActionNotMounted()` holds) — so a hidden verb's reject branch is UNREACHABLE through the surface. Prove a verb's reject via a domain `expect(fn () => app(Action::class)->handle($id))->toThrow(...)` + `assertActionHidden('verb')`, NEVER an `assertNotified(action_failed)` the page can't raise. Confirmation method: a throwaway `ViewCustomer` subclass adding one `->visible()`-gated header action, run green then deleted (the permanent coverage lands in `CustomerKycSanctionsConsoleTest`).

---

## [2026-06-22 17:34] — 1.1 Relax the now-stale absence guard
- Dropped the terminal `->assertActionDoesNotExist('requireKyc')` from the "exposes the four form-less status verbs" test in `CustomerLifecycleConsoleTest.php`; kept the `submit`/`reject`/`reopen` catalog-governance guards (`reopen` is now the terminal assertion). No account/profile absence guard existed in this file to touch (those stay deferred — never added).
- Fixed two now-stale references that named the KYC verb absent: the file-header scope note (lines 7–8) and the test title (`… without the catalog governance or KYC verbs` → `… without the catalog governance verbs`). Re-pointed both to `CustomerKycSanctionsConsoleTest`, where the KYC + sanctions verbs land this slice.
- Files changed: `tests/Feature/Modules/OperatorPanel/Parties/CustomerLifecycleConsoleTest.php` (test-only; no production code, no spec/arch files).
- Quality loop: **green** — Pint clean; `--filter=CustomerLifecycleConsoleTest` 9/9 (90 assn); full suite 1442/1442 (7948 assn, was 7950 — the single removed assertion); PHPStan max 0 errors; `pint --test` clean; `openspec validate --strict` green.
- **Learnings for future iterations:**
  - The KYC verbs are **visibility-gated** (design D4): on a fresh factory Customer (`kyc_status` null) `requireKyc` becomes VISIBLE the moment task 2.1 wires it — which is exactly why this absence guard had to go now to keep the loop green.
  - Next (task 1.2): pin the Filament 5 **page header-action** visibility API against installed 5.6.7 — `assertActionVisible/Hidden`, plus the mount-and-inspect path (`mountAction`, `assertFormFieldExists`, `setActionData`) on `Livewire::test(ViewCustomer::class, ['record' => $id])`. Do NOT write it from memory (arch-from-memory ban); record the confirmed helpers in the `ViewCustomer` docblock.
---

## [2026-06-22 18:09] — 1.2 Pin the Filament 5 page header-action visibility-test API
- Pinned the page header-action visibility-test API against the **installed** Filament 5.6.7 source (not memory), then confirmed it empirically with a throwaway probe (deleted after green). Read `vendor/filament/actions/src/Testing/TestsActions.php` (`assertActionVisible`/`assertActionHidden` lines 217/231 → `assertActionExists`→`getAction()`→`isVisible()/isHidden()`; `callAction` asserts-visible-FIRST line 84; `mountAction` lines 29-49; `assertActionNotMounted` line 460) + `vendor/filament/forms/src/Testing/TestsForms.php` (`assertFormFieldExists`/`Visible`/`Hidden`, `fillForm`).
- Probe = a test-only `ViewCustomer` subclass exposing ONE `->visible()`-gated header action keyed on the record's `kyc_status` cast value; **2 tests / 8 assn green**: `assertActionVisible('probe')` on a null-`kyc_status` Customer + `assertActionHidden('probe')` on a `pending` one (same action class → per-record `->visible()` evaluation), and `mountAction('probe')`→`assertActionNotMounted()` on the hidden record (the D4 not-mountable landmine for header actions). Probe then deleted (throwaway; permanent coverage lands in group-2's `CustomerKycSanctionsConsoleTest`).
- Recorded the confirmed helpers + the D4 landmine in the `ViewCustomer` class docblock (a HEADER-ACTION VISIBILITY-TEST API note; comment-only — no production behavior change).
- Files changed: `app/Modules/OperatorPanel/Filament/Resources/Parties/CustomerResource/Pages/ViewCustomer.php` (docblock only).
- Quality loop: **green** — Pint clean; targeted (`CustomerLifecycleConsoleTest` + `CustomerHoldsConsoleTest`) 19/19 (211 assn); full suite 1442/1442 (7948 assn); PHPStan max 0 errors; `pint --test` clean; `openspec validate --strict` valid. Diff touches no `spec/` / `openspec/specs/` / `tests/Architecture/`, adds no composer dep or migration.
- **Learnings for future iterations:**
  - Consolidated the reusable API into `## Codebase Patterns` above. Task 2.1 can now write `assertActionVisible('requireKyc')` / `assertActionHidden('requireKyc')` and the reject-via-`toThrow`+`assertActionHidden` split with confidence — the helpers are confirmed, not assumed.
  - Empirical-probe recipe for a `->visible()`-gated header action without touching production: subclass the page in the test file, override `getHeaderActions()` to return one `Action::make('probe')->action(fn () => null)->visible(fn () => …record state…)`, drive via `Livewire::test(Subclass::class, ['record' => $id])`. The subclass inherits `$resource` so it resolves through the registered resource — renders fine in isolation.
  - Next (task 1.3): front-load i18n — extend the `customer` block in `lang/en/operator_console.php` AND `lang/it/operator_console.php` with the ten new KYC/sanctions keys (IT value MUST differ from EN), and add the suffixes to `customerConsoleKitKeys()` in `CustomerConsoleI18nTest`.
---

## [2026-06-22 17:57] — 1.3 Front-load i18n
- Authored the ten KYC/sanctions copy keys in the `customer` block of BOTH lang files: `actions.{require_kyc,record_kyc_verified,record_kyc_rejected,record_screening}`, `fields.{screening_verdict,screening_source}`, `notifications.{kyc_required,kyc_verified,kyc_rejected,screening_recorded}` (`notifications.action_failed` reused, not re-added). Every IT value is distinct from EN (the IT-differs guard) — verb labels Richiedi/Registra…, `screening_verdict`→`Esito screening`, `screening_source`→`Origine screening`, and full-sentence success titles. The Customer KYC verbs use a `record_*` label register (audit-recording) distinct from the Producer console's `verify_kyc`/`reject_kyc` — noted in the IT comment.
- Added the ten dot-suffixes to `customerConsoleKitKeys()` in `CustomerConsoleI18nTest` (grouped after the Holds sub-groups in `fields`/`actions`/`notifications`); they auto-flow into `customerConsoleItDiffersKeys()` via `array_diff`. Extended the function docblock with a KYC+sanctions bullet (front-loaded this change, task 1.3, ahead of tasks 2–3). Documented each new key group with an inline sub-group comment inside the EN+IT arrays (the Holds-slice precedent), lead comments untouched.
- Files changed: `lang/en/operator_console.php`, `lang/it/operator_console.php`, `tests/Feature/Modules/OperatorPanel/Parties/CustomerConsoleI18nTest.php`. No production source, no `spec/`/`openspec/specs/`/`tests/Architecture/`, no migration, no composer dep.
- Quality loop: **green** — Pint clean; `--filter=CustomerConsoleI18nTest` 108/108 (217 assn, +20 data cases from the 10 new keys × 2 guards); full suite **1462/1462** (7988 assn, was 1442/7948); PHPStan max 0 errors; `pint --test` clean; `openspec validate --strict` valid.
- **Learnings for future iterations:**
  - Consolidated the reusable **i18n front-load discipline** into `## Codebase Patterns` above (author EN+IT IT≠EN + the test key-contract in prep; the `…ItDiffersKeys()` dataset derives via `array_diff` so one list feeds both guards).
  - The i18n contract is now green ahead of the surface, so group 2 (KYC verbs) and group 3 (sanctions form) only wire behaviour — every label/notification they resolve is already authored. The success-title keys are `kyc_required`/`kyc_verified`/`kyc_rejected`/`screening_recorded`; the screening form field labels are `fields.screening_verdict`/`fields.screening_source`.
  - Next (task 2.1): append the three form-less KYC verbs to `ViewCustomer::getHeaderActions()` via `lifecycleAction($verb, $successKey, $invoke)` with chained `->visible()` from-state gates; add the `kycRequirable`/`kycPending` cast-value predicates (state enum, NEVER imported); import only the three Actions (name `IllegalKycTransition` in PROSE so Pint can't re-add an import — the 2026-06-20 landmine).
---

## [2026-06-22 18:40] — 3.1 The sanctions-screening form (verdict + record-dependent trigger_source)
- Added `private function recordScreeningAction(): Action` on `ViewCustomer`, appended to `getHeaderActions()` after `placeHoldAction()` (the two bespoke form actions now sit together). Built BESPOKE (NOT `lifecycleAction`) — the `placeHold` precedent: `Action::make('recordScreening')->label(actions.record_screening)->schema([verdict Select, trigger_source Select])->action(function (): void {})`. The no-op `->action()` mirrors the placeHold 3.1 step (the write-through into `RecordCustomerScreening` lands in 3.2); the comment names 3.2.
- `verdict` Select → `screeningVerdictOptions()` (new `private`): `SanctionsStatus::cases()` keyed `->value => ->value` (the `holdTypeOptions` mirror — all four states offerable). `trigger_source` Select → `->options(fn (): array => $this->screeningSourceOptions($this->recordOf(Customer::class, $this->getRecord())))`. `screeningSourceOptions(Customer): array` (new `private`) returns `['compliance_ad_hoc'…]`, prepending `['onboarding'…]` iff `last_screening_at === null` (design D6 — onboarding-is-first; `cadence`/`aml_threshold` never offered). Both helpers key off the `ScreeningTriggerSource`/`SanctionsStatus` operand-enum `->value` (no magic strings).
- Imports: added `Parties\Enums\{SanctionsStatus, ScreeningTriggerSource}` (operand enums — the carve-out; BOTH used, so no unused-import lint). Did NOT import `RecordCustomerScreening` (that's 3.2's write-through) nor `KycStatus` (state enum, never imported). Updated the class docblock (sanctions verb now lands) + the `getHeaderActions()` docblock (placeHold + recordScreening both bespoke).
- Test (`CustomerKycSanctionsConsoleTest`, +1 `it`): mount `recordScreening`; `assertFormFieldExists('verdict', …getOptions() === SanctionsStatus::cases()->value)`; never-screened Customer → `trigger_source` options `['onboarding','compliance_ad_hoc']`; `last_screening_at`-stamped Customer → `['compliance_ad_hoc']`. Added `Filament\Forms\Components\Select` import; updated the file-header scope clause.
- Files changed: `app/Modules/OperatorPanel/Filament/Resources/Parties/CustomerResource/Pages/ViewCustomer.php`, `tests/Feature/Modules/OperatorPanel/Parties/CustomerKycSanctionsConsoleTest.php`. No `spec/`/`openspec/specs/`/`tests/Architecture/`, no migration, no composer dep.
- Quality loop: **green** — Pint clean (no forbidden import re-added); `--filter=CustomerKycSanctionsConsoleTest` 27/27 (158 assn, was 26/149); full suite **1489/1489** (8146 assn, was 1488/8137 — +1 test / +9 assn); PHPStan max 0 errors; `pint --test` clean; `openspec validate --strict` valid. `ModuleBoundariesTest` UNCHANGED (operand-enum import rides the existing `Parties\Enums` carve-out — verified line 61 is a namespace-prefix allow).
- **Learnings for future iterations:**
  - Consolidated the reusable **bespoke form header action (form-first, write-through-next)** + **record-dependent Select option-set** pattern into `## Codebase Patterns` above.
  - Verified empirically that `Select::getOptions()` resolves a `->options(Closure)` (`HasOptions::getOptions()` line 36 `evaluate($this->options)`), so the record-dependent `trigger_source` options assert correctly via `array_keys($field->getOptions())`. The bare `CustomerFactory` sets none of the additive screening columns → a factory Customer is never-screened (`last_screening_at` NULL) for free.
  - Next (task 3.2): swap the no-op for the write-through — `->action(function (array $data): void { … })` narrowing each operand with `is_string` (the Holds form-data discipline), then `$this->surfaceLifecycleOutcome(fn () => app(RecordCustomerScreening::class)->handle($customer->id, SanctionsStatus::from($verdict), ScreeningTriggerSource::from($source)), (string) __('operator_console.customer.notifications.screening_recorded'))`. NO `$model->save()`. Import `RecordCustomerScreening` THEN (Actions carve-out). 3.2 asserts: passed/onboarding → `CustomerOnboardingScreeningPassed` + 12-month `next_rescreen_at`; failed/compliance_ad_hoc → `CustomerRescreeningFailed`; under_review → zero screening events; kyc_status untouched (independence).
---

## [2026-06-22 18:28] — 2.3 Reject floor + no-waive guard + KYC↔sanctions independence
- Appended five `it()` blocks to `CustomerKycSanctionsConsoleTest` (test-only — verbs shipped in 2.1):
  - **Three reject-floor blocks** (one per verb, design D4), each parametrised over its OUT-of-from-states and proving BOTH halves: the surface HIDES the verb (`assertActionHidden('verb')`) AND the domain INDEPENDENTLY rejects an out-of-band call (`expect(fn () => app(Action::class)->handle($id))->toThrow(IllegalKycTransition::class)`), with `kyc_status` unchanged (`->toBe($from)`) and the event log empty (`DomainEvent::count() === 0`). Bare-factory Customers → "unchanged" needs no baseline. Coverage: `requireKyc` from {pending, verified, rejected}; `recordKycVerified` + `recordKycRejected` each from {not_required, verified, rejected, NULL}. NOT `assertNotified(action_failed)` — a hidden verb can't raise it.
  - **No-waive guard** (design D8): on a `pending` Customer, `assertActionDoesNotExist('waiveKyc')` AND `assertActionDoesNotExist('waive')` — there is no Customer KYC waive Action (only `WaiveProducerKyc`).
  - **KYC↔sanctions independence** (design D7): an `active` Customer with `sanctions_status = passed` driven through a console require→verify cycle (two `Livewire::test` mounts — the second verb's visibility depends on the first having committed) keeps `kyc_status → verified` + `status → active`, but `sanctions_status` stays `passed`, `last_screening_at` stays NULL, and zero screening events (`where('name','like','Customer%creening%')->count() === 0` — all four screening event names contain "creening").
- Imports added (tests import Parties freely — carve-out is production-only): `Actions\{RecordKycRejected,RecordKycVerified}`, `Enums\SanctionsStatus`, `Exceptions\IllegalKycTransition`. Used per-verb literal Action class constants (NOT a `string`-variable `app($x)`) so PHPStan-max resolves the `->handle()` receiver — no closure-in-dataset (no repo precedent; three focused blocks instead). Tweaked the stale file-header clause to name 2.3 as added.
- Files changed: `tests/Feature/Modules/OperatorPanel/Parties/CustomerKycSanctionsConsoleTest.php` (test-only). No production source, no `spec/`/`openspec/specs/`/`tests/Architecture/`, no migration, no composer dep.
- Quality loop: **green** — Pint clean; `--filter=CustomerKycSanctionsConsoleTest` 26/26 (149 assn, was 13/75); full suite **1488/1488** (8137 assn, was 1475/8063 — +13 tests / +74 assn); PHPStan max 0 errors; `pint --test` clean; `openspec validate --strict` valid. `tests/Architecture/` UNCHANGED (`ModuleBoundariesTest` rides the carve-out).
- **Learnings for future iterations:**
  - `assertActionDoesNotExist('verb')` (action NOT registered) vs `assertActionHidden('verb')` (registered but `->visible()` false) are DIFFERENT helpers — use the former for an ABSENT verb (no-waive, design D8: there's nothing to evaluate a `->visible()` closure on), the latter for a gated verb out of its from-state. Both confirmed in installed Filament 5.6.7 `TestsActions`.
  - The reject-floor `toThrow` works under `DatabaseMigrations` because the Action's `DB::transaction` genuinely rolls back (no never-committed outer test transaction as with `RefreshDatabase`), so `kyc_status` + the event log are demonstrably unchanged after the throw.
  - `IllegalKycTransition` exposes `cannotRequire(KycStatus)` (non-nullable — NULL is a LEGAL require from-state), `cannotVerify(?KycStatus)` / `cannotReject(?KycStatus)` (nullable — NULL is illegal, rendered with the `unset` token). All extend `RuntimeException`.
  - Next (task 3.1): the sanctions form — `recordScreeningAction()` (bespoke, the `placeHoldAction()` precedent, NOT `lifecycleAction`); `verdict` Select over `SanctionsStatus::cases()`, record-dependent `trigger_source` (drop `onboarding` once `last_screening_at` set — design D6). Import the OPERAND enums `Enums\{SanctionsStatus,ScreeningTriggerSource}` in PRODUCTION (the carve-out — `ModuleBoundariesTest` stays unchanged); `KycStatus` still never imported.
---

## [2026-06-22 18:17] — 2.2 KYC write-through + auto-Hold coupling tests
- Appended three write-through `it()` blocks to `CustomerKycSanctionsConsoleTest` (test-only — the verbs shipped in 2.1):
  - **(a) require on an `active` Customer**: `callAction('requireKyc')` → `kyc_status=pending` + `kyc_required` true + a system-placed Customer-scope `kyc` Hold `active` (reason NULL) + Customer `suspended`. Asserted the EVENT-SILENT contract (design D7): exactly one `CustomerHoldPlaced` (`->sole()`, `entity_type=Hold`) + one `CustomerSuspended` (`->sole()`, `entity_type=Customer`), each `module=parties` / `actor_role=NewcoOps` / `actor_id->toEqual($operator->id)` (loose — PG bigint-as-string), and `where('name','like','CustomerKyc%')->count()===0`.
  - **(b) verify on a require-suspended Customer**: arranged the precondition THROUGH the real `RequireKyc` Action on an `active` Customer (never the bare factory — it skips the coupling), giving `pending`+`suspended`+`kyc`-Hold. `callAction('recordKycVerified')` → `verified`, the `kyc` Hold `lifted`, Customer `active`; exactly one `CustomerHoldLifted` + one `CustomerReactivated`, zero `CustomerKyc%` events.
  - **(c) reject on a `pending` Customer**: bare-factory `pending` Customer + active `kyc` Hold (no arrange events). `callAction('recordKycRejected')` → `rejected`, the `kyc` Hold STILL `active`, `DomainEvent::count()===0` (audit-only — RecordKycRejected touches no recorder).
  - Each asserts its success notification (`kyc_required` / `kyc_verified` / `kyc_rejected`).
- Imports added (tests import Parties freely — the carve-out governs production code only): `Actions\RequireKyc`, `Enums\{CustomerStatus,HoldScope,HoldStatus,HoldType}`, `Events\{CustomerHoldLifted,CustomerHoldPlaced,CustomerReactivated,CustomerSuspended}`, `Models\Hold`, `Platform\Events\{ActorRole,DomainEvent}`. Tweaked the stale file-header clause ("THIS task (2.1)…") to name 2.2 as added.
- Files changed: `tests/Feature/Modules/OperatorPanel/Parties/CustomerKycSanctionsConsoleTest.php` (test-only). No production source, no `spec/`/`openspec/specs/`/`tests/Architecture/`, no migration, no composer dep.
- Quality loop: **green** — Pint clean; `--filter=CustomerKycSanctionsConsoleTest` 13/13 (75 assn, was 10/30); full suite **1475/1475** (8063 assn, was 1472/8018 — +3 tests / +45 assn); PHPStan max 0 errors; `pint --test` clean; `openspec validate --strict` valid.
- **Learnings for future iterations:**
  - Verified the four coupling events' `NAME`/`ENTITY_TYPE` against source (zero-invention): `CustomerHoldPlaced`/`CustomerHoldLifted` → `entity_type=Hold`; `CustomerSuspended`/`CustomerReactivated` → `entity_type=Customer`. The chain test (4.1) re-proves this heterogeneous envelope in the full 5-event multiset.
  - Envelope idiom pinned by `CustomerHoldsConsoleTest` and reused verbatim: `Hold.scope_id`→`toBe($customer->id)` (int cast), but `DomainEvent.entity_id`→`toBe((string) …)` and `DomainEvent.actor_id`→`toEqual($operator->id)` (loose, PG bigint-as-string). Don't mix them.
  - To arrange a `suspended`+`kyc`-Hold Customer you MUST start `active` then call `RequireKyc` (require on a `pending` Customer throws — `pending` is not a require from-state). `RequireKyc` on a `pending` Customer would only place the Hold without suspending anyway.
  - Next (task 2.3): the reject FLOOR — `assertActionHidden('verb')` + a domain `expect(fn () => app(Action)->handle($id))->toThrow(IllegalKycTransition::class)` (imported freely in the test), state + event-log unchanged; the no-waive guard `assertActionDoesNotExist('waiveKyc')` (design D8); + the KYC↔sanctions independence check.
---

## [2026-06-22 18:07] — 2.1 KYC verbs — three form-less, visibility-gated header actions
- Appended `requireKyc` / `recordKycVerified` / `recordKycRejected` to `ViewCustomer::getHeaderActions()`, each built with the kit's form-less `lifecycleAction($verb, $successKey, $invoke)` (`$form` left null) and chaining `->visible(fn (): bool => …)` for its from-state gate. Success keys map to the 1.3 notification keys: `kyc_required` / `kyc_verified` / `kyc_rejected`; labels snake-case to `actions.require_kyc` / `record_kyc_verified` / `record_kyc_rejected`. Each invoke routes the customer `int $id` into its Action via `recordOf(Customer::class, $record)->id` — no Eloquent write.
- Added two `private` predicates predicating on the model CAST VALUE (state enum never imported — design D5): `kycRequirable(Customer)` = `kyc_status === null || ->value === 'not_required'` (non-nullsafe — the `=== null` short-circuit narrows); `kycPending(Customer)` = `kyc_status?->value === 'pending'` (nullsafe — NULL is un-screened). Each is the EXACT COMPLEMENT of its Action's domain from-state guard, so a rejected transition is unreachable through the surface (design D4 — the verb is simply hidden).
- Imports: added only the three Actions (`RecordKycRejected` / `RecordKycVerified` / `RequireKyc`, alphabetically between `ReactivateCustomer` and `SuspendCustomer`). Did NOT import `KycStatus` (state enum, cast-only) or `IllegalKycTransition` (named in PROSE in the docblock so Pint's `fully_qualified_strict_types` can't re-add it — the 2026-06-20 landmine). `ModuleBoundariesTest` UNCHANGED (no new operand-enum import this task).
- New test `CustomerKycSanctionsConsoleTest.php` (the slice's console-test file, `uses(DatabaseMigrations::class)`): pins the VISIBILITY contract only — `requireKyc` visible iff `kyc_status` NULL/`not_required` and hidden once `pending`/`verified`/`rejected`; `recordKycVerified` + `recordKycRejected` visible together iff `pending`, both hidden otherwise. (2.2/2.3 will append write-through + reject-floor to this file.)
- Files changed: `app/Modules/OperatorPanel/Filament/Resources/Parties/CustomerResource/Pages/ViewCustomer.php` (3 imports + 3 verbs + 2 predicates + docblocks), `tests/Feature/Modules/OperatorPanel/Parties/CustomerKycSanctionsConsoleTest.php` (new). No `spec/`/`openspec/specs/`/`tests/Architecture/`, no migration, no composer dep.
- Quality loop: **green** — Pint clean (no forbidden import re-added); `--filter=CustomerKycSanctionsConsoleTest` 10/10 (30 assn); full suite **1472/1472** (8018 assn, was 1462/7988 — +10 visibility data cases); PHPStan max 0 errors (no `nullsafe.neverNull`); `pint --test` clean; `openspec validate --strict` valid.
- **Learnings for future iterations:**
  - Consolidated the reusable **form-less visibility-gated verb** pattern into `## Codebase Patterns` above.
  - The Customer KYC verb ids (`requireKyc` / `recordKycVerified` / `recordKycRejected`) deliberately DIFFER from the Producer console's (`requireKyc` / `waiveKyc` / `verifyKyc` / `rejectKyc`) — the verb id is page-scoped, so the shared `requireKyc` id on both pages is no clash; there is NO Customer `waiveKyc` (design D8).
  - Next (task 2.2): test the write-through + auto-Hold coupling in the SAME file — `callAction('requireKyc')` on an `active` Customer → `pending` + a `kyc` Hold `active` + `suspended`, exactly one `CustomerHoldPlaced` + one `CustomerSuspended` and ZERO `CustomerKyc%` events (design D7); arrange the verify-path Customer THROUGH the real `RequireKyc` Action (never the bare factory — it skips the coupling).
---
