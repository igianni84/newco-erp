## 1. Prepare the surface (keep every iteration green)

- [x] 1.1 Relax the now-stale absence guard: in `tests/Feature/Modules/OperatorPanel/Parties/CustomerLifecycleConsoleTest.php` drop the terminal `->assertActionDoesNotExist('requireKyc')` (line ~202) and update the lead-in comment — `requireKyc` (and the other KYC + sanctions verbs) now land in this slice. **KEEP** the catalog-governance guards (`submit` / `reject` / `reopen` — the Customer FSM is not review-governed). There is **no** account/profile absence guard in this file to touch (those stay deferred and are simply never added). Acceptance: `php artisan test --filter=CustomerLifecycleConsoleTest` green; no `requireKyc` absence assertion remains.
- [x] 1.2 Pin the Filament 5 **page header-action** visibility-test API against the installed version (5.6.7) — `assertActionVisible('verb')` / `assertActionHidden('verb')` and the mount-and-inspect-form path (`mountAction`, `assertFormFieldExists`, `setActionData`) on a `Livewire::test(ViewCustomer::class, ['record' => $id])` — do NOT write it from memory (the repo's arch-from-memory ban). This is the header-action twin of the table API the Holds row-lift used (`assertTableActionVisible/Hidden`); confirm a hidden header action's reject is NOT mountable server-side (design D4 — the hidden-action landmine applies to header actions too). Record the confirmed helpers in a one-line note in the `ViewCustomer` docblock. Acceptance: a throwaway probe (or the first group-2 test) confirms `assertActionHidden` evaluates the `->visible()` closure per record; no API written speculatively.
- [x] 1.3 Front-load i18n. Extend the `customer` block in `lang/en/operator_console.php` AND `lang/it/operator_console.php` with: `actions.require_kyc`, `actions.record_kyc_verified`, `actions.record_kyc_rejected`, `actions.record_screening`; `fields.screening_verdict`, `fields.screening_source`; `notifications.kyc_required`, `notifications.kyc_verified`, `notifications.kyc_rejected`, `notifications.screening_recorded` (reuse the existing `notifications.action_failed`). Every new key's IT value MUST differ from EN (the IT-differs guard). Add the ten new suffixes to `customerConsoleKitKeys()` in `tests/Feature/Modules/OperatorPanel/Parties/CustomerConsoleI18nTest.php` (they auto-flow into `customerConsoleItDiffersKeys()` via `array_diff`). Acceptance: run via `--filter=CustomerConsoleI18nTest` OR the full suite (so the shared `scanOperatorConsoleHardcodedSinks` helper loads) — all five i18n guards green with the new keys.

## 2. KYC verbs — three form-less header actions (design D2/D4/D7/D8)

- [x] 2.1 On `ViewCustomer`, append three verbs to `getHeaderActions()` built with the kit's `lifecycleAction($verb, $successKey, $invoke)` (form-less, `$form = null`), each chaining `->visible(...)` for its from-state gate:
  - `lifecycleAction('requireKyc', 'kyc_required', fn (Model $record, string $notes) => app(RequireKyc::class)->handle($this->recordOf(Customer::class, $record)->id))->visible(fn (): bool => $this->kycRequirable($this->recordOf(Customer::class, $this->getRecord())))`
  - `lifecycleAction('recordKycVerified', 'kyc_verified', fn (Model $record, string $notes) => app(RecordKycVerified::class)->handle($this->recordOf(Customer::class, $record)->id))->visible(fn (): bool => $this->kycPending(...))`
  - `lifecycleAction('recordKycRejected', 'kyc_rejected', fn (Model $record, string $notes) => app(RecordKycRejected::class)->handle($this->recordOf(Customer::class, $record)->id))->visible(fn (): bool => $this->kycPending(...))`
  Add two `private` predicate helpers, predicating on the **cast value** (state enum, never imported — design D5): `kycRequirable(Customer $c): bool` = `$c->kyc_status === null || $c->kyc_status->value === 'not_required'`; `kycPending(Customer $c): bool` = `$c->kyc_status?->value === 'pending'`. Import `App\Modules\Parties\Actions\{RequireKyc,RecordKycVerified,RecordKycRejected}`; do NOT import `KycStatus` or `IllegalKycTransition` (name the exception in PROSE in the docblock so Pint's `fully_qualified_strict_types` cannot re-add the import — the lessons.md 2026-06-20 landmine). Acceptance + test (`CustomerKycSanctionsConsoleTest`): `assertActionVisible('requireKyc')` on a Customer with `kyc_status` null/`not_required` and `assertActionHidden('requireKyc')` once `pending`; `assertActionVisible('recordKycVerified')` + `('recordKycRejected')` iff `pending`, hidden otherwise. `vendor/bin/phpstan analyse` at max clean (no `nullsafe.neverNull`).
- [x] 2.2 Test the KYC write-through + the auto-Hold coupling (`CustomerKycSanctionsConsoleTest`): (a) `callAction('requireKyc')` on an **active** Customer → `kyc_status = pending`, `kyc_required` true, a Customer-scope `kyc` Hold `active`, the Customer now `suspended`; exactly one `CustomerHoldPlaced` and one `CustomerSuspended` recorded (envelope `module=parties`, `actor_role=ActorRole::NewcoOps`, `actor_id` loose `toEqual` the operator id) AND **zero** KYC-named events (`DomainEvent::query()->where('name','like','CustomerKyc%')->count() === 0` — design D7); (b) arrange a `pending`, `suspended`, `kyc`-Hold-covered Customer **through the real `RequireKyc` Action** (never the bare factory — the factory skips the coupling), then `callAction('recordKycVerified')` → `kyc_status = verified`, the `kyc` Hold `lifted`, the Customer `active`; one `CustomerHoldLifted` + one `CustomerReactivated`, no KYC event; (c) on a `pending` Customer with an active `kyc` Hold, `callAction('recordKycRejected')` → `kyc_status = rejected`, the `kyc` Hold **still `active`**, and **no** domain event recorded at all. Assert each success notification (`assertNotified((string) __('operator_console.customer.notifications.kyc_*'))`).
- [x] 2.3 Test the reject floor (design D4 — the hidden-action landmine) + the no-waive guard (`CustomerKycSanctionsConsoleTest`): for a verb out of its from-state, prove BOTH halves the console can show — the surface HIDES it (`assertActionHidden('recordKycVerified')` on a non-`pending` Customer) AND the domain INDEPENDENTLY rejects an out-of-band call (`expect(fn () => app(RecordKycVerified::class)->handle($id))->toThrow(IllegalKycTransition::class)`; `IllegalKycTransition` imported freely in the TEST), with `kyc_status` and the domain-event log unchanged — NOT an `assertNotified(action_failed)` the page can't raise. Repeat for `requireKyc` out of `not_required`/null and `recordKycRejected` out of `pending`. No-waive: `assertActionDoesNotExist('waiveKyc')` (and no waive id at all) — there is no Customer KYC waive Action (design D8). Independence (design D7): a Customer with `sanctions_status = passed` keeps it through a require→verify cycle, no sanctions event recorded. Cross-reference `ProducerKycConsoleTest` for the KYC-verb test idiom.

## 3. Sanctions screening — one bespoke form header action (design D3/D6/D7)

- [x] 3.1 Add a `recordScreeningAction(): Action` private method on `ViewCustomer` (the `placeHoldAction()` precedent — bespoke, multi-operand, NOT `lifecycleAction`) and append it to `getHeaderActions()`. Form: a `verdict` `Select` over `SanctionsStatus::cases()` keyed value→value, and a `trigger_source` `Select` whose options are **record-dependent** — `->options(fn (): array => $this->screeningSourceOptions($this->recordOf(Customer::class, $this->getRecord())))`. The helper `private function screeningSourceOptions(Customer $c): array` returns `['compliance_ad_hoc' => 'compliance_ad_hoc']`, prepending `'onboarding' => 'onboarding'` **iff** `$c->last_screening_at === null` (design D6 — onboarding first-screening-only; `cadence`/`aml_threshold` never offered). Import `App\Modules\Parties\Enums\{SanctionsStatus,ScreeningTriggerSource}` (operand enums — the carve-out, like `HoldType`/`HoldScope`). Labels resolve `fields.screening_verdict` / `fields.screening_source`. Acceptance + test (`CustomerKycSanctionsConsoleTest`): mount the action — `assertFormFieldExists('verdict', fn (Select $f) => array_keys($f->getOptions()) === array_map(fn ($c) => $c->value, SanctionsStatus::cases()))`; on a never-screened Customer `trigger_source` options `=== ['onboarding','compliance_ad_hoc']`; on a Customer with `last_screening_at` set, options `=== ['compliance_ad_hoc']`.
- [x] 3.2 Wire `recordScreening` write-through: `->action(function (array $data): void { … })` narrowing each operand with `is_string` (the Holds form-data discipline), then `$this->surfaceLifecycleOutcome(fn () => app(RecordCustomerScreening::class)->handle($customer->id, SanctionsStatus::from($verdict), ScreeningTriggerSource::from($source)), (string) __('operator_console.customer.notifications.screening_recorded'))`. NO `$model->save()`. Acceptance + test (`CustomerKycSanctionsConsoleTest`): (a) `callAction('recordScreening', data: ['verdict' => 'passed', 'trigger_source' => 'onboarding'])` on a never-screened Customer → `sanctions_status = passed`, `last_screening_at` stamped, `next_rescreen_at` exactly 12 months after `last_screening_at`, `screening_trigger_source = onboarding`, exactly one `CustomerOnboardingScreeningPassed` (envelope `module=parties`, `entity_type=Customer`, `actor_role=NewcoOps`); (b) on an already-screened Customer, `verdict=failed`+`trigger_source=compliance_ad_hoc` → `sanctions_status=failed`, exactly one `CustomerRescreeningFailed`; (c) `verdict=under_review` (or `pending`) → `sanctions_status` updated, **zero** screening events (`where('name','like','Customer%creening%')->count() === 0`); (d) independence — recording a screening leaves `kyc_status` unchanged.
- [x] 3.3 Test the onboarding-first floor (design D6, `CustomerKycSanctionsConsoleTest`): on an already-screened Customer the form no longer offers `onboarding` (covered by 3.1) AND the domain is the floor — `expect(fn () => app(RecordCustomerScreening::class)->handle($id, SanctionsStatus::Passed, ScreeningTriggerSource::Onboarding))->toThrow(IllegalSanctionsTransition::class)` (imported freely in the test), `sanctions_status` and the event log unchanged. (The console can't reach this through a hidden option — same surface-hides + domain-enforces split as D4.)

## 4. PG17 closing-chain integration test — the cross-slice proof (design D11)

- [x] 4.1 Add `tests/Feature/Modules/OperatorPanel/Parties/CustomerKycSanctionsChainTest.php` using `DatabaseMigrations` (NOT `RefreshDatabase` — each console action opens its own `DB::transaction`; the in-transaction event append must commit). Use a **profile-less** Customer (keeps the suspend/reactivate cascade silent → exact event count). Drive the chain through the `ViewCustomer` page **and** the shipped `CustomerHoldsTable` footer widget: on an `active` Customer, `callAction('requireKyc')` → `kyc_status = pending`; the `kyc` Hold row appears in the Holds table and exposes **no** per-row `lift` (`assertTableActionHidden('lift', record: $kycHold)` on the widget — auto-managed, the Holds slice's discipline); the Customer is `suspended`. Then `callAction('recordKycVerified')` → the `kyc` Hold row flips to `lifted`, the Customer is `active`. Then `callAction('recordScreening', data: ['verdict' => 'passed', 'trigger_source' => 'onboarding'])`. Assert the emergent `DomainEvent` set: `pluck('name')->toEqualCanonicalizing(['CustomerHoldPlaced','CustomerSuspended','CustomerHoldLifted','CustomerReactivated','CustomerOnboardingScreeningPassed'])` — a multiset carrying **no** KYC event (design D7) — `->toHaveCount(5)`; SET-WIDE `module=parties` / `actor_role=NewcoOps` / `actor_id` not null; representative per-event envelope with the **heterogeneous** `entity_type` (Hold events → `Hold`/`$hold->id`; status + screening events → `Customer`/`$customer->id`), `actor_id` loose `toEqual`. Acceptance: green on SQLite (`php artisan test --filter=CustomerKycSanctionsChainTest`).
- [ ] 4.2 Run the PG17 ritual (GUIDE §2.7): `docker run -d --name pg … postgres:17 -p 55432:5432` → poll `pg_isready` → `DB_CONNECTION=pgsql DB_HOST=127.0.0.1 DB_PORT=55432 DB_DATABASE=newco_test DB_USERNAME=newco DB_PASSWORD=newco php -d memory_limit=-1 vendor/bin/pest tests/Feature/Modules/OperatorPanel/Parties tests/Feature/Modules/OperatorPanel/Catalog/ProductMasterConsoleI18nTest.php` (the appended Catalog i18n test loads the shared `scanOperatorConsoleHardcodedSinks` helper) → `docker rm -f pg`. Acceptance: the Parties OperatorPanel folder is green under PostgreSQL 17 (loop = verify only; no push/merge).

## 5. Quality gates + memory

- [ ] 5.1 `vendor/bin/pint` clean; `vendor/bin/phpstan analyse` at max green — including `NoEloquentWriteInOperatorPanelRule` (the write routes only through the four domain Actions) and `ModuleBoundariesTest` **UNCHANGED** (the `Parties\Enums` operand-enum import rides the existing carve-out — do NOT widen the allowlist; `KycStatus` is never imported). `php -d memory_limit=-1 vendor/bin/pest` (full) green. Acceptance: all four quality commands pass; `git diff --name-only main | grep -E '^(spec/|openspec/specs/|tests/Architecture/)'` → NONE, and the diff adds no composer dependency and no migration.
- [ ] 5.2 `openspec validate operator-console-parties-kyc-sanctions --strict` green. Append a `log.md` entry via `scripts/memlog.sh "<op>" "<target>" "<outcome>"`; overwrite `hot.md` from current state; consolidate the durable patterns into the change's `progress.md` `## Codebase Patterns` (form-less verb = `lifecycleAction` + chained `->visible()` from-state gate; state-enum cast-value predicate vs operand-enum import; the record-dependent option-set drop for onboarding-first; the event-silent KYC verbs → assert the coupled Hold/status events + the absence of any KYC event; the cross-slice chain proven through the shipped Holds table).
