# Progress — parties-producer-approval-sod

## Codebase Patterns
(consolidated reusable patterns — read first each iteration)

- **Full suite = `php -d memory_limit=-1 vendor/bin/pest`, NOT `php artisan test`.** The `artisan test` path (laravel/pao driver) spawns the runner in a subprocess capped at 128 MB and OOMs during result collection on the full ~1956-test suite (fatal in `DemoSeeder.php`, misleading line number — it dies while autoloading a PHPUnit class, not in a test). `-d memory_limit=1G` on `artisan` does NOT propagate to the subprocess. Run Pest directly with uncapped memory; per-`--filter` runs are fine under `artisan test`.
- **Parties copy is per-key EN-fallback (DEC-127).** `lang/en/parties.php` is the authored baseline + final fallback; per-locale files (e.g. `lang/it/parties.php`) may cover a *subset* and fall back to EN per key (`config('app.fallback_locale') = 'en'`). A new IT file need only carry the keys it translates. **Invariant:** every authored `it` key MUST have an `en` counterpart (`array_diff(Arr::dot(it), Arr::dot(en)) === []`) — assert it or a typo'd IT key dangles without a fallback.
- **i18n copy test idiom (PHPStan-max-clean).** `trans($key, $replace, $locale)` is typed `array|string|null`; do NOT chain `->toBeString()->not->...` off it (PHPStan can't resolve `->not` on the union). Use `expect(trans(...))->not->toBe($key)->and(trans(...))->toContain(...)`, and for authored-IT proof `expect(Lang::has($key,'it',false))->toBeTrue()->and(trans($key,[],'it'))->not->toBe(trans($key,[],'en'))` (the ProductMasterConsoleI18nTest pattern). Unit copy tests must `uses(TestCase::class)` explicitly (Pest auto-binds TestCase only in `Feature`).
- **Copy keys mirror Catalog's `approval` group, minus the reviewer leg.** Catalog uses `requires_operator` / `self_approval_creator` / `self_approval_reviewer` / `insufficient_separation`; Parties (linear FSM, no reviewer) uses only `requires_operator_principal` / `creator_may_not_approve`. Keep `:entity` parameterized (the guard passes `'Producer'`), name only the violated rule, never PII.
- **The SoD guard resolves via `app(ProducerApprovalGovernance::class)` — no container binding.** It's a concrete class whose ctor injects the `ActorContext` **singleton**, so `app()`/constructor-injection just works (task 2.1 injects it into `ActivateProducer`). Test it directly by seeding the creator lineage through the **real `CreateProducer`** Action inside `ActorContext::runAs(ActorRole::NewcoOps, $creatorId, fn () => …)` (the RM-07 pattern; `runAs` restores the prior context on exit, so the system-actor case sees **no lingering auth** → resolves `(System, null)`), then approving inside a second `runAs`. A `Producer::factory()->create()` records no `ProducerCreated` → **null creator = vacuous** distinctness (fourth case). Assert the two reject legs by the resolved message (`(string) __('parties.approval.<key>', ['entity' => 'Producer'])`) to prove the RIGHT factory fired; pass legs via `->not->toThrow(SeparationOfDutiesViolation::class)`.
- **Cross-module `{@see}` in a module class docblock is a Pint→boundary landmine (lessons.md 2026-07-06).** A `{@see \App\Modules\Catalog\…}` FQCN in the guard/its ctor docblock makes Pint's `fully_qualified_strict_types` auto-add a `use` → Parties→Catalog import → `ModuleBoundariesTest` red (invariant 10). Name Catalog (and any other module) in **backticked prose**; keep only code-used `use`s. The arch test is ABSENT from a `--filter` run, so re-run Pint + `grep -n Catalog` the file before the full suite. **Standing trap for tasks 3.x** — `ViewProducer` docblock will describe the mirrored Catalog behavior. (In `ActivateProducer` the SoD paragraph avoided naming Catalog at all — it references the Parties `{@see ProducerApprovalGovernance}` and names the exception in backticked prose.)
- **The SoD floor's migration blast radius = EVERY `ActivateProducer` call site, not the task's named files (lessons.md 2026-07-06 floor-blast-radius).** Wiring the operator-principal floor into the shared Action broke SIX test files, only ONE named in tasks 2.1/2.2. Two migration shapes: **(1) real-lineage** — a Producer built by the real `CreateProducer` (as operator A) MUST be activated by a **distinct** operator B (`producerCreatedByOperator($a)` + `activateProducerAsOperator($id, $b)` in `ProducerLifecycleTest`; distinct `actingAs` for `ProducerConsoleChainTest`; else creator===approver self-approval, the F2 case). **(2) vacuous** — a `factory()->create()` Producer has a null creator, so a SINGLE `actingAs`/`runAs(operator)` clears the floor (`CatalogLifecycleChainTest`, the `ComplianceChainTest` matrix). And **SET-WIDE `->every(actor_role === System)` chain assertions must split**: only `ProducerActivated` is operator-gated (→ `NewcoOps`); creation/KYC-Holds/sanctions/agreements/clubs/retire stay `System`. Pest helpers are one shared global namespace but do NOT cross single-file runs — define per-file (unique name) or inline the `runAs`, never call another test file's helper. Applies again to `ActivateProfile`/`ApproveProfile` if a floor lands there.

---

## [2026-07-06 11:43] — 1.1 Parties SoD approval copy (EN + IT)
- **What:** Added the `approval` copy group (keys `requires_operator_principal`, `creator_may_not_approve`) to `lang/en/parties.php` and created `lang/it/parties.php` with authored Italian for the same two keys — the localized messages the task-1.2 `SeparationOfDutiesViolation` factories will resolve. Wording mirrors `lang/en/catalog.php`'s `approval` group minus the reviewer leg (Producer FSM is linear). Fixed the now-stale "there is no lang/it/parties.php" parenthetical in the `compliance_review` comment.
- **Files changed:** `lang/en/parties.php` (+`approval` group, comment fix), `lang/it/parties.php` (new, `approval` group only), `tests/Unit/Modules/Parties/PartiesApprovalCopyTest.php` (new, 5 tests / 13 assertions).
- **Quality loop:** green. Pint (touched + full `--test`) clean · filter test 5/5 · full suite **1956/1956** (10432 assn, was 1951 + 5) via `php -d memory_limit=-1 vendor/bin/pest` · PHPStan max **0 errors** · `openspec validate parties-producer-approval-sod --strict` green. SQLite only this task (no schema/PG-relevant code; PG17 run belongs to the close ritual per design Migration Plan).
- **Learnings for future iterations:**
  - The `artisan test` OOM landmine + the i18n idioms are consolidated in Codebase Patterns above — read them before the next task's quality loop.
  - Task 1.2 (`SeparationOfDutiesViolation`) resolves `parties.approval.requires_operator_principal` / `parties.approval.creator_may_not_approve` via `(string) __(...)`, exactly like `Catalog\Exceptions\ApprovalGovernanceViolation::build()` — mirror that class's factory shape (`requiresOperatorPrincipal(string $entity)`, `creatorMayNotApprove(string $entity)`).
  - Pre-existing uncommitted bookkeeping (`hot.md`, `log.md`, `docs/validation/Remediation_Tracker.md`) from the prior authoring/approval session rode into this commit as the memory envelope — the tracker still shows RM-08 🟡 (approved/building); task 6.1 flips it to ✅ at close.
---

## [2026-07-06 11:50] — 1.2 SeparationOfDutiesViolation exception
- **What:** Added `app/Modules/Parties/Exceptions/SeparationOfDutiesViolation.php` — a `RuntimeException` with two static factories, `requiresOperatorPrincipal(string $entity)` and `creatorMayNotApprove(string $entity)`, each resolving its localized message via a private `build()` that calls `(string) __("parties.approval.{$key}", ['entity' => $entity])`. Exact shape-mirror of `Catalog\Exceptions\ApprovalGovernanceViolation::build()` MINUS the reviewer leg (linear Producer FSM → no `reviewerMayNotApprove`/`insufficientSeparation`/block-gate). No `Catalog\*` symbol imported (invariant 10) — the docblock names Catalog only in prose, not via `use`/FQCN `{@see}`.
- **Files changed:** `app/Modules/Parties/Exceptions/SeparationOfDutiesViolation.php` (new), `tests/Unit/Modules/Parties/Exceptions/SeparationOfDutiesViolationTest.php` (new, 5 tests / 11 assertions).
- **Quality loop:** green. Pint (touched) clean · filter 5/5 · full suite **1961/1961** (10443 assn, was 1956 + 5) via `php -d memory_limit=-1 vendor/bin/pest` · PHPStan max **0** · Pint `--test` full clean · `openspec validate … --strict` green. SQLite only (no schema/PG-relevant code; PG17 belongs to the close ritual).
- **Learnings for future iterations:**
  - The two `parties.approval.*` keys were already landed by task 1.1, so the test's data-provider leg (`resolves every … lang key`) was GREEN even before the class existed — only the 3 factory/instance tests were red. TDD red→green still holds; just know the copy half of the contract is pre-satisfied.
  - Test entity token is the literal `'Producer'` (the guard passes it in task 1.3) — absent from both templates, so `->toContain('Producer')` proves `:entity` interpolation. The data-provider uses synthetic `'SomeEntity'`, mirroring the Catalog test.
  - **Next = task 1.3:** the guard `app/Modules/Parties/Governance/ProducerApprovalGovernance.php` (constructor injects `App\Platform\Events\ActorContext`), `guard(string $entityType, int|string $entityId): void`. Mirror `Catalog\Lifecycle\ApprovalGovernance` minus the reviewer leg: `operatorPrincipalOrFail()` (reject `role() !== ActorRole::NewcoOps` ∨ `actorId() === null` → `requiresOperatorPrincipal`, else return approver id); `creatorOf()` = earliest `DomainEvent` row for `(entity_type, entity_id)` by `id` via `orderBy('id')->value('actor_id')`, through a PRIVATE `normalizeActorId()` copy (PG numeric-string vs SQLite int, so `===` holds cross-engine); if `creator !== null && approver === creator` → `creatorMayNotApprove`. TDD Feature test first (`tests/Feature/Modules/Parties/ProducerApprovalGovernanceTest.php`). Read `Catalog\Lifecycle\ApprovalGovernance` + `App\Platform\Events\ActorContext`/`ActorRole` before writing; verify the `domain_events` column is `actor_id` and `entity_type`/`entity_id` names against a real row.
---

## [2026-07-06 12:04] — 1.3 ProducerApprovalGovernance guard
- **What:** Added `app/Modules/Parties/Governance/ProducerApprovalGovernance.php` — the Parties-local separation-of-duties guard, `guard(string $entityType, int|string $entityId): void`, ctor-injecting the `ActorContext` singleton. Two legs, in order: `operatorPrincipalOrFail()` (reject `role() !== ActorRole::NewcoOps` ∨ `actorId() === null` → `requiresOperatorPrincipal`, else return the approver id) then the distinct-actor check (`creator !== null && approver === creator` → `creatorMayNotApprove`; null creator vacuous). `creatorOf()` = earliest `DomainEvent` row for `(entity_type, entity_id)` by `id` (`orderBy('id')->value('actor_id')`) through a PRIVATE `normalizeActorId()` copy (numeric → int, else null, so `===` holds PG numeric-string ↔ SQLite int). Exact shape-mirror of Catalog's guard MINUS the reviewer leg + review-freshness block-gate (linear Producer FSM — no `reviewed` state / `.submitted` audit source). No `Catalog\*` import (invariant 10) — Catalog named in **prose only**; imports are `Parties\Exceptions\SeparationOfDutiesViolation` + `Platform\Events\{ActorContext,ActorRole,DomainEvent}`. No behavior change yet — task 2.1 wires it into `ActivateProducer`.
- **Files changed:** `app/Modules/Parties/Governance/ProducerApprovalGovernance.php` (new), `tests/Feature/Modules/Parties/ProducerApprovalGovernanceTest.php` (new, 4 tests / 7 assertions), `lessons.md` (+1 entry: the Pint cross-module-`{@see}` boundary trap generalized to lateral modules).
- **Quality loop:** green. Pint (touched) clean · filter 4/4 · `ModuleBoundariesTest` green (invariant 10) · full suite **1965/1965** (10450 assn, was 1961 + 4) via `php -d memory_limit=-1 vendor/bin/pest` · PHPStan max **0** · Pint `--test` full clean · `openspec validate parties-producer-approval-sod --strict` green. SQLite only (no schema/PG-relevant delta; the `normalizeActorId` cross-engine `===` is exercised on PG17 in the close ritual, task 6.1).
- **Learnings for future iterations:**
  - **LANDMINE HIT (fixed pre-commit):** wrote the mirrored-Catalog ref as `{@see \App\Modules\Catalog\Lifecycle\ApprovalGovernance}` → Pint auto-added `use App\Modules\Catalog\Lifecycle\ApprovalGovernance;` → invariant-10 violation. Caught it because `ModuleBoundariesTest` OOMs under `artisan test <file>` (128 MB, arch class-graph) so I ran it via Pest → then grepped. Full mechanics + generalized rule now in lessons.md (2026-07-06) and Codebase Patterns above. **Tasks 2.x/3.x face the same trap** (ActivateProducer/ViewProducer docblocks describe Catalog).
  - `ActorContext::runAs(NewcoOps, $creatorId, fn () => app(CreateProducer::class)->handle(...))` seeds a genuine creator lineage AND restores the empty context on exit — so the very next assertion (no `runAs`, no auth) resolves `(System, null)` and exercises the operator-principal reject without any logout plumbing. Cleaner than `actingAs` for a direct-guard test.
  - The two pass legs assert via `->not->toThrow(SeparationOfDutiesViolation::class)`; the two reject legs assert the resolved message (`(string) __('parties.approval.<key>', ['entity' => 'Producer'])`) to prove the RIGHT factory fired (both share the exception class). Test #1 (system actor + real creator lineage) also pins the in-guard ORDER: it must throw `requires_operator_principal`, not `creator_may_not_approve`.
  - **Next = task 2.1:** inject `ProducerApprovalGovernance` into `ActivateProducer`, invoke `guard('Producer', $producer->id)` inside the existing `DB::transaction`, AFTER the locked from-state assert and BEFORE the KYC gate + `update` (design D6 order: from-state → operator-principal → distinct-actor → KYC → write). Extend `ProducerLifecycleTest` (TDD): creator self-approval → `SeparationOfDutiesViolation` + stays `draft` + 0 `ProducerActivated`; `system` actor → rejected; distinct operator + KYC cleared → `active` + one `ProducerActivated` whose `actor_id` = approver; self-approval on a KYC-`pending` Producer rejected on the SoD floor (SoD before KYC). **Watch:** the EXISTING `ProducerLifecycleTest` activation tests run under the default `System` actor (`app(ActivateProducer)->handle()` with no `actingAs`/`runAs`) — the new floor breaks them; task 2.2 migrates them to distinct operator principals. Do 2.1's new assertions under operators; leave the 2.2 migration to its own task, but expect 2.1 to red some existing tests (note them, don't fix ahead).
---

## [2026-07-06 14:42] — 3.1 ViewProducer console: second-actor affordance + SoD→notification surface
- **What:** Surfaced the separation-of-duties floor on the Producer console's `activate` verb — "surface, not reimplement", exactly as the catalog consoles do. Added `confirmationKey: 'affordance.second_actor'` to the `activate` `lifecycleAction()` (the ONLY code change on `ViewProducer` — Filament's `->requiresConfirmation()->modalDescription(...)` via the shared trait). The SoD→notification half was ALREADY wired: `SeparationOfDutiesViolation extends RuntimeException`, so `SurfacesDomainActions::surfaceLifecycleOutcome()` catches it by base type → `action_failed` danger notification, and `ActivateProducer`'s transaction rollback (task 2.1) leaves state unchanged. Added the `operator_console.producer.affordance.second_actor` copy (EN + IT), fixed the now-stale "Producer has NO SoD activation / no affordance block" comments in both lang files and both `ViewProducer` docblocks + the console-test header. **No submit/reject/reopen added; KYC verbs untouched.**
- **Files changed (5):** `app/Modules/OperatorPanel/Filament/Resources/Parties/ProducerResource/Pages/ViewProducer.php` (confirmationKey + 2 docblocks); `lang/en/operator_console.php` + `lang/it/operator_console.php` (new `producer.affordance.second_actor` + comment fix); `tests/Feature/Modules/OperatorPanel/Parties/ProducerLifecycleConsoleTest.php` (flipped the affordance assertion `!isConfirmationRequired()`→`isConfirmationRequired() && modalDescription===copy`; +1 NEW test: creator self-approval via console → `action_failed`, stays draft, 0 ProducerActivated; header comment; `CreateProducer` import); `tests/Feature/Modules/OperatorPanel/Parties/ProducerConsoleI18nTest.php` (added `affordance.second_actor` to `producerConsoleKitKeys()` → flows into the EN-baseline + IT-distinct + IT⊆EN guards automatically, mirroring `ProductMasterConsoleI18nTest:183`).
- **Quality loop:** green. Pint (touched + full `--test`) clean · no Catalog import in `ViewProducer` (the `{@see}`-trap grep, CLEAN — Catalog named in prose only) · full suite **1972/1972** (10479 assn, was 1969 + 3: SoD console test + EN-baseline affordance + IT-distinct affordance) via `php -d memory_limit=-1 vendor/bin/pest` · PHPStan max **0** · `openspec validate parties-producer-approval-sod --strict` valid. SQLite only.
- **Learnings for future iterations:**
  - **`callAction('activate')` executes even with `requiresConfirmation()`** — the Livewire/Filament test helper auto-confirms, so the affordance does NOT break the existing happy-path `callAction('activate')` tests (proven across all 8 catalog console tests). Adding an affordance only breaks tests that ASSERT its absence (`!isConfirmationRequired()`) — exactly ONE: `ProducerLifecycleConsoleTest`'s "exposes only the two status verbs" test. The sibling Parties consoles (ProducerAgreement `activate`, Club `sunset`/`close`, Customer `activate`) assert their OWN verbs' absence and are untouched (Producer-only scope).
  - **Isolated-run false failure:** `ProducerConsoleI18nTest` alone fails its LAST test (`function_exists('scanOperatorConsoleHardcodedSinks')`) — that scanner is declared in `ProductMasterConsoleI18nTest` and only loaded suite-wide. Run the two i18n files together (or the full suite) to see it green. Not a regression; the file's own comment documents the cross-file dependency.
  - **Next = task 3.2:** its 2nd bullet (creator self-approval via console → notification + stays draft + 0 event) and 3rd bullet (affordance exposed) are ALREADY covered by 3.1's two tests. 3.2's genuine remaining work: the **distinct-operator happy path** (real `CreateProducer` lineage as op A → activate via console as DISTINCT op B → `active` + `ProducerActivated` w/ approver actor_id) + migrate the existing factory-based happy path (lines 38–67, null-creator vacuous) to genuine distinct-operator lineage. Use `actingAs($opA)` for create, switch to `actingAs($opB)` before the activate `Livewire::test` (a fresh component reads the guard at record time — the 2.2 `ProducerConsoleChainTest` pattern).
---

## [2026-07-06 14:26] — 2.1 + 2.2 SoD floor in ActivateProducer + activation-test migration
- **What:** Wired the separation-of-duties floor into `ActivateProducer` and migrated EVERY Producer-activation call site to authenticated operators. `ActivateProducer` now ctor-injects `ProducerApprovalGovernance` and calls `guard('Producer', $producer->id)` inside the existing `DB::transaction`, AFTER the locked from-state assert and BEFORE the KYC gate (design D6 order: from-state → operator-principal → distinct-actor → KYC → write) — a violation throws before any write, so `status`/event-log are unchanged. Docblock gained a SEPARATION-OF-DUTIES paragraph (Catalog NOT named — references the Parties `{@see ProducerApprovalGovernance}`; exception in backticked prose) + stale "System until real principals" clause corrected. **2.1 and 2.2 were done together — inseparable under the green-commit gate** (the floor rejects the System actor at activation, so the very tests that drove it under System can't stay green without migration).
- **Files changed (6 + memory):** `app/Modules/Parties/Actions/ActivateProducer.php` (inject + guard call + docblock); `tests/Feature/Modules/Parties/ProducerLifecycleTest.php` (two file-local helpers `producerCreatedByOperator`/`activateProducerAsOperator`; 4 breaking blocks migrated to distinct-operator lineage; 4 NEW SoD tests — self-approval reject, system/null reject on the operator-principal leg, distinct-operator happy path w/ approver actor_id, KYC-pending self-approval rejected on the SoD floor **before** KYC → 20 tests/100 assn); **collateral not named by any task** (blast radius): `ComplianceChainTest.php` (4 activations wrapped in `runAs(5100)`, the SET-WIDE `every(System)` split → ProducerActivated=NewcoOps), `SupplyLifecycleChainTest.php` (helper activation wrapped, `every(System)` split over 14 events), `CatalogLifecycleChainTest.php` (one `actingAs` before the factory-Producer activation — null creator = vacuous), `ProducerConsoleChainTest.php` (single-operator create+activate was SoD self-approval → distinct `$approver` for activate, re-`actingAs($operator)` for the non-gated retire so `ProducerCreated`/`ProducerRetired` stay the primary operator); `lessons.md` (+1 floor-blast-radius entry).
- **Quality loop:** green. Pint (touched + full `--test`) clean · every touched file per-file green · full suite **1969/1969** (10468 assn, was 1965 + 4 SoD tests) via `php -d memory_limit=-1 vendor/bin/pest` · PHPStan max **0** · `openspec validate parties-producer-approval-sod --strict` valid. SQLite only (the `normalizeActorId` cross-engine `===` and the split chain assertions ride the PG17 close, task 6.1). **Landmine that bit:** the first full run was 1968/1969 — `ProducerConsoleChainTest` (console `callAction('activate')`, invisible to a `grep ActivateProducer::class`) was the sole miss; per-file/`--filter` runs never surfaced it.
- **Learnings for future iterations:**
  - The migration blast radius (6 files, only 1 named) + the two migration shapes (real-lineage distinct operator vs. factory-null-creator vacuous) + the SET-WIDE `every(System)`→split rule are consolidated in Codebase Patterns above and lessons.md 2026-07-06. **Task 3.x console work will face the same — grep `callAction('activate')` sites, not just Action callers.**
  - `actingAs` can be switched MID-test between `Livewire::test(...)` calls to change the resolved operator (`ProducerConsoleChainTest`: create/KYC under `$operator`, activate under `$approver`, retire back under `$operator`). Each `Livewire::test` is a fresh component that reads `Auth::guard('operator')` at record time.
  - Pest global helpers do NOT survive a single-file run of a DIFFERENT file — `ProducerLifecycleTest`'s helpers can't be reused from the chain tests; those inline `app(ActorContext::class)->runAs(NewcoOps, 5100, …)`.
  - **Next = task 3.1:** `ViewProducer` console — surface the "second actor required" affordance on the `activate` verb + map a thrown `SeparationOfDutiesViolation` to a notification (state unchanged), the same "surface, not reimplement" contract Catalog uses; keep the KYC-rejection notification path; add no submit/reject/reopen. **Pint `{@see}` trap is LIVE here** — `ViewProducer`'s docblock describes Catalog's mirror; name Catalog in backticked prose, re-run Pint + `grep -n Catalog` before the full suite. The existing `ProducerLifecycleConsoleTest`/`ProducerKycConsoleTest` already activate factory Producers under `actingAs` (null creator = vacuous) so they stayed green — task 3.2 ADDS the distinct-operator + self-approval-via-console cases.
---
