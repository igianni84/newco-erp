# Progress — catalog-lifecycle-approval

## Codebase Patterns
(consolidated reusable patterns — read first each iteration)

- **Pint auto-imports docblock `{@see \FQCN}` refs (`fully_qualified_strict_types`).** If you `{@see \App\Modules\…\SomeClass}` a class a LATER task creates, Pint rewrites it into a real `use` import → PHPStan then breaks on the unknown class. **Worse, for a cross-module class:** auto-importing an EXISTING `App\Modules\<other>\…` FQCN adds a real cross-module `use` → **breaches invariant 10 and breaks `ModuleBoundariesTest`** (verified-by-reasoning task 2.1: a Catalog exception that wanted to cite Module K's `IllegalProducerTransition` analogue). **Rule:** reference a not-yet-existent class (a future task's Action/Consumer/Event) AND any other-module class as plain backticked text / prose — `` `ProducerLifecycleProjector` (task 1.2) ``, "Module K's `IllegalProducerTransition`" — never `{@see \FQCN}`. Same-namespace `{@see ClassName}` / `{@see Class::$prop}` are safe (Pint leaves them, no import needed).
- **PG-only CHECK test idiom** (template: `tests/Feature/Platform/ActorRoleConstraintTest.php`): a raw `DB::table('t')->insert([...])` bypasses the Eloquent enum cast (the app-layer floor), so on PG the DB CHECK is the SOLE gate. Wrap the probe insert in `DB::transaction(...)` (a SAVEPOINT, testing-rule #5) so a PG constraint-abort stays isolated and the row-state check after the throw is valid. Assert BOTH halves of the engine asymmetry, never a vacuous skip: `pgsql` → message `toContain('<named_constraint>')` + row absent; `sqlite` → message `''` + row present (the cast, not a DB CHECK, is the floor). NB: `captureConstraintViolation()` in that file is a top-level (GLOBAL) function — do NOT redeclare it in another test file (fatal on full-suite load); inline the try/catch or name a fresh helper.
- **New projection/read-model migration shape** (mirrors the spine): `$table->id()` + a plain `unsignedBigInteger('<fk>_id')` (NEVER a cross-module FK/relation — invariant 10) + a `string('status')` whose value-set is enforced by the enum cast on both engines PLUS a PG-only `CHECK (… IN (…))` derived from `Enum::cases()` (so it can never drift) + `timestampsTz()`. Explicit, <63-char index names. CHECK DDL guarded by `if (DB::getDriverName() === 'pgsql')`.
- **First-consumer registration seam** (`{Module}ServiceProvider::boot()`, design D4 — established by `CatalogServiceProvider`): register on the shared `ConsumerRegistry` via **method injection** — `public function boot(ConsumerRegistry $registry): void { $registry->register(EVENT_NAME, Consumer::class); }`. The registry is a singleton bound in `AppServiceProvider` (all `register()`s run before any `boot()`, so the binding exists), so the instance `boot()` gets is the same one `DomainEventRecorder` fans out against. **Cross-module consumers key off BARE event-name strings**, never the emitter's event class (invariant 10 + the stricter task rule): anchor the names as the consumer's own `public const` (single source of truth for the provider registration + the handler's `match`), docblock-noting they mirror the emitter's `::NAME` verbatim. A registration test asserts `consumersFor(NAME)->toContain(Consumer::class)` (without it, zero deliveries fan out — silent gate failure).
- **Idempotent + order-tolerant consumer (watermark upsert)**: read the row `->where(fk)->lockForUpdate()->first()` (real lock on PG serializes concurrent inline-vs-sweep deliveries for the same key; no-op on SQLite, where the assert carries correctness — design D2/D4). Null → `create(…last_event_id = $event->id)`; else apply **only when `$event->id` STRICTLY advances `last_event_id`** (`<=` → no-op), so replays + stale/out-of-order events never regress. The handler opens **no** transaction (the `InlineDeliveryExecutor` wraps it — DB-work-only contract). Test the guard by calling `app(Consumer::class)->handle($staleEvent)` **directly** — a re-`deliver()` is a no-op on a `done` row (ledger terminality), so it can't drive the watermark branch.
- **Reading a `mixed` jsonb payload value under phpstan-max**: `(int) $event->payload['k']` trips `cast.int` (can't blind-cast `mixed`). Narrow instead: `$v = $event->payload['k'] ?? null; if (! is_int($v)) throw new UnexpectedValueException(…);` — a malformed envelope then fails loudly (delivery retries → dead-letters), never silently coerced. (Verified empirically: Laravel's `array` cast round-trips a JSON integer back as a PHP `int`, so `is_int` holds on the happy path.)
- **Shared generic transition mechanism (the lifecycle FSM seam — design D1/D2).** `LifecycleTransition::transition()` is `@template TModel of Model&HasLifecycleState`. To thread `TModel` through `DB::transaction` under phpstan-max with NO forbidden `@var`/assert override, operate on the **passed `$model`** (typed `TModel`), never the `firstOrFail()` result — Larastan erases `$model->newQuery()->…->firstOrFail()` to base `Model`, dropping the generic AND the interface. So: `$model->newQuery()->whereKey($model->getKey())->lockForUpdate()->firstOrFail()` (result **discarded** — it only acquires the `FOR UPDATE` lock) **then `$model->refresh()`** loads the authoritative locked state onto the typed instance; `return $model` threads `TModel`, so a per-entity `handle(ProductMaster): ProductMaster` keeps its concrete return with zero casts. The from-state is read AFTER refresh → a stale caller snapshot is overwritten by DB truth before the assert (race-safe; the lock serialises on PG, no-op on SQLite).
- **Lifecycle read contract is a typed METHOD, not a magic `@property`.** `HasLifecycleState::lifecycleState(): LifecycleState` — because Larastan's Eloquent property extension **shadows an interface's `@property`** through a `Model&Interface` intersection (`→ "Access to undefined property TModel of …::$lifecycle_state"`), but resolves a real interface method cleanly. Spine models opt in with a one-line read accessor (a pure getter — NOT a transition method, so they stay persistence-only). Same trick will serve any future generic-over-models mechanism.
- **Audit action derived from the TABLE, never `Str::snake(classBasename)`.** `catalog.<entity>.<verb>` builds `<entity>` as `Str::singular(Str::after($model->getTable(), 'catalog_'))` — the table is canonical snake and survives the SKU acronyms (`catalog_sellable_skus → sellable_sku`), whereas `Str::snake('SellableSKU')` mangles to `sellable_s_k_u`. Verified across all 7 spine tables. (Audit `entity_type` stays the PascalCase canonical label `ProductMaster`/`SellableSKU`, passed explicitly by the Action — it must match the domain-event `entity_type`, and `class_basename` would give `SellableSku` ≠ the canonical `SellableSKU`.)
- **Pint method-name trap (sharpens Patterns #1).** `fully_qualified_strict_types` rewrites a docblock `{@see lowercaseMethod()}` to `{@see CasedClass()}` when an imported class matches it case-insensitively — it "corrected" `{@see lifecycleState()}` → `{@see LifecycleState()}` (the imported enum). Reference a same-class method in backticks (`` `lifecycleState()` ``), not `{@see}`, when a similarly-named class is imported. (Also re-confirmed: a `{@see \Other\Module\Action}` in the generic mechanism auto-imports the Action — keep cross-class/future examples in backticks.)
- **Reading an uncast `actor_id` (or any bigint) column cross-engine.** A `Builder::value('actor_id')` (or `->first()->actor_id`) on a column NOT in the model's `casts()` returns a PHP `int` on SQLite but a numeric **STRING** on PostgreSQL (PDO_PGSQL's text protocol). A `===` against an `ActorContext` int id (or any int) then silently fails ONLY on PG — SQLite-green hides it. Normalize every such read through one helper: `is_numeric($raw) ? (int) $raw : null` (covers int, numeric-string, and null/absent in one expression; `is_numeric` also satisfies phpstan-max so the subsequent `(int)` is not a `cast.int` on `mixed`). This is THE reason a guard that reads prior actors from `domain_events`/`audit_records` MUST be PG-verified. (Verified empirically on PG17 task 2.3.)
- **Asserting a multi-key jsonb snapshot (`before`/`after`) cross-engine.** PG `jsonb` does NOT preserve key insertion order (it reorders), so a whole-array `->toBe(['a'=>…,'b'=>…])` on a 2-plus-key snapshot is order-sensitive (`===`) and can pass on SQLite yet fail on PG. Assert keys INDIVIDUALLY and order-independently: `$after = $row->after ?? []; expect($after['k'] ?? null)->toBe(…)` (the `?? []` narrows the nullable jsonb to an array so phpstan-max is happy AND a null row fails loudly; the inner `?? null` tolerates a missing key without an offset error). A SINGLE-key snapshot is safe with whole-array `toBe` (no ordering to disturb).
- **Activate/Retire wiring onto the shared `transition()` (the seam tasks 4.x/5.x reuse).** `transition()` now takes two OPTIONAL trailing closures: `?Closure $gate` (`(TModel): void`) — an activation precondition evaluated AFTER the governance guard and BEFORE the write (throws a localized exception to reject); and `?Closure $event` (`(TModel): array{name: string, payload: array<string, mixed>}`) — the `*Activated`/`*Retired` intent recorded through `DomainEventRecorder` AFTER the write (so the payload reads the to-state), the mechanism owning the envelope (module `catalog`, `ActorContext` principal, entity type/id resolved once). Per-entity Actions stay thin: **Activate** passes BOTH (`gate: fn ($m) => $this->parentGate->assert(...)`, `event: fn ($m) => ['name' => XActivated::NAME, 'payload' => XActivated::payload($m)]`); **Retire** passes only `$event` (the within-catalog ref-integrity guard is separate — 5.2); **Submit/Reopen/Reject** pass neither (the bare call still serves the 2.3 governance tests). The single array-intent (NOT two paired `?string $name` + `?Closure` params) is atomic — name+payload can't desync — and phpstan-max-clean: the array-shape return resolves through the nested `DB::transaction` closure + `use ($gate, $event)`, and the closure's `(TModel)` param matches `$model` threaded the same way (Pattern #12). Use `XActivated::ENTITY_TYPE` as the `$entity` label in the Activate/Retire Actions (ties the audit + event `entity_type` to one source). The activation **gate is lock-free** (a read-time gate, design D3 risk note — an activation decided while the producer was active is valid even if a `ProducerRetired` commits just after; block-NEW, never cascade an in-flight activation), and ordered AFTER governance so a self-approval throws the governance error, not the gate error; the from-state assert is FIRST, so an out-of-state activate throws `IllegalLifecycleTransition`, not the gate.
- **Per-entity lifecycle recipe (the 4.x mirror — proven end-to-end on a STANDALONE entity, task 4.1).** Each of the six remaining spine entities needs, in one task: (1) two `final` `*Activated`/`*Retired` event classes (Pattern #19 shape); (2) the model opts into the FSM — add `implements HasLifecycleState` + a one-line `lifecycleState(): LifecycleState` accessor (the `Format`/`ProductMaster` shape), and refresh any stale "no transition out of draft" docblock line carried from the spine-creation change; (3) five thin Actions — `Submit*ForReview`/`Reopen*` (bare `transition(Submit|Reopen)`, audit-only), `Reject*Review` (`reject(...)`), `Activate*` and `Retire*` (each passing the `event:` intent, Activate ALSO passing `gate:` for the FOUR child entities — Format/CaseConfiguration are standalone and pass NO gate); (4) fix the matching `*Created` docblock's "No `*Activated`/`*Retired` sibling exists" line to same-namespace `{@see XActivated}`/`{@see XRetired}` cross-refs (bare same-namespace `{@see}` is Pint-safe; a FQCN would auto-import — Pattern #1). **A standalone Activate still runs approval governance** (the gate's absence ≠ no guard) — so its test asserts BOTH the distinct-actor positive path AND a self-approval (creator) negative, plus the two from-state guards (submit-on-non-draft, activate-on-non-reviewed). **Audit action segment** is table-derived (`catalog_formats → catalog.format.<verb>`, Pattern #14) — no per-Action wiring. **`CreateFormat`-style create (no dedup)** establishes the creator lineage the governance reads (records `*Created` with the creator's `actor_id`); `CreateProductMaster` additionally dedups (BR-Identity-1), so only the Master test needs distinct identities for two-under-one-parent.
- **Each `*LifecycleTest.php` feature file MUST name its create-helper UNIQUELY** (`lifecycleCreateDraftFormat` ≠ `lifecycleCreateDraftMaster`) — Pest shares ONE global namespace across all test files, so a second `function lifecycleCreateDraftX()` with a name already defined in a sibling lifecycle test is a fatal redeclare on full-suite load (Pattern #7). 4.2–4.6 each add another such file: pick a distinct helper name per entity. (Also: a docblock `{@see \App\Modules\Catalog\Lifecycle\LifecycleTransition}` in a TEST gets auto-imported by Pint's `fully_qualified_strict_types` but `no_unused_imports` does NOT then strip it when you later remove the `{@see}` — so reference the mechanism in backticked prose from the start, matching `ProductMasterLifecycleTest`.)
- **DB-free event-payload unit test (the reusable shape for every `*Activated`/`*Retired` class — tasks 4.1–4.6 each add two more).** Place it under `tests/Unit/.../Events/`, `uses(TestCase::class)` (NO `RefreshDatabase`/`DatabaseMigrations` — booting the app gives the model its enum casts; the **absence** of a migrated schema is itself the guard that the test runs zero queries). Build the fixture with `Model::factory()->make([...])` — `make()` never persists or queries and never fires `afterCreating`, and `$guarded = []` lets you override `id` so the payload's `*_id` mapping is assertable. A file-level `$fixture = fn (LifecycleState $s) => …->make([... 'lifecycle_state' => $s])` threaded into each `it(..., function () use ($fixture) {})` keeps it DRY without a top-level (GLOBAL) function (avoids the redeclare-on-full-suite-load fatal — Patterns above). Assert the payload BOTH ways: `array_keys($payload)->toBe([...])` for exact shape+order AND whole-array `->toBe([...])` for values — a **pure PHP array's** order is deterministic, so the jsonb-ordering caveat above does NOT apply here. Prove PII-free with `not->toHaveKey('name'|'product_type'|'producer')` + `array_values($payload)->not->toContain(<the descriptive name you set>)`. The Naming Cascade acceptance ("never `WineMaster*`") is already covered by `tests/Architecture/CatalogNamingCascadeTest.php` (it scans the whole Catalog subtree for `^Wine`/`^BottleReference`), so no per-event name assertion is needed — just keep that arch test green.

---

## [2026-06-16 10:47] — 1.1 catalog_producer_states projection — migration + model + enum
- **What:** Stood up the Catalog-owned producer-state projection (the codebase's first cross-module read model) — the persistence floor under task 1.2's consumer and task 3.2's gate. Three artifacts + tests, no behaviour yet (the consumer/gate land next).
- **Files changed:**
  - `app/Modules/Catalog/Enums/ProducerProjectionStatus.php` (NEW) — backed enum `Active='active'`/`Retired='retired'` (the two gate-relevant states; `draft`/`reviewed` never reach this read model — design D3).
  - `app/Modules/Catalog/Models/ProducerState.php` (NEW) — `$table='catalog_producer_states'`, `$guarded=[]`, casts `status`→enum, `producer_id`/`last_event_id`→`integer`. Persistence-only; no `HasFactory` (the projector is the sole writer; tests `create()` directly). `producer_id` is a plain id (no relation — invariant 10).
  - `database/migrations/2026_06_16_000001_create_catalog_producer_states_table.php` (NEW) — the change's ONE migration: `id`, `producer_id` (unique), `status` (string + PG CHECK from `cases()`), `last_event_id` (watermark, plain bigint — no FK into the platform event store), `timestampsTz()`.
  - `tests/Feature/Modules/Catalog/ProducerStateProjectionTest.php` (NEW, `DatabaseMigrations`) — 4 tests: schema/columns on both engines; cast round-trip (status→enum, ids→int); duplicate `producer_id` rejected (unique, both engines); status CHECK asserted both-halves (PG names the constraint / SQLite accepts the raw insert).
  - `tests/Unit/Modules/Catalog/Enums/EnumsTest.php` (EDIT) — pinned `ProducerProjectionStatus` case/value map verbatim + a `from()` rejection (protects the migration's `cases()`-derived CHECK from silent drift).
- **Quality loop: green.** Pint format ✓ · filtered tests 10/10 ✓ · full suite 481/481 (was 475; +6) ✓ · phpstan 0 errors ✓ · pint --test ✓. **PG17:** new tests 10/10 + `tests/Feature/Modules/Catalog` + `tests/Unit/Modules/Catalog` + `tests/Architecture` = 77/77 green (CHECK + unique proven on PG; migration applies cleanly in the full chain; `ModulePersistenceConventionsTest`/`ModuleBoundariesTest`/`CatalogNamingCascadeTest` green). `openspec validate --strict` ✓.
- **Acceptance:** all bullets met — table on both engines with status CHECK + unique `producer_id`; model casts status to the enum; `ModulePersistenceConventionsTest` green; quality green; verified on PG17.
- **Learnings for future iterations:**
  - Hit the Pint `{@see \FQCN}` → auto-import trap referencing the not-yet-existent `ProducerLifecycleProjector` (task 1.2) — fixed by using plain backticked text. See Codebase Patterns #1; task 1.2 will create that class, at which point a `{@see \…}` becomes safe.
  - The model deliberately has NO factory — the projection is consumer-written, and tests create rows directly. Task 1.2's consumer is the first real writer; 3.2's gate the first reader.
  - PG run cmd (avoids arch-OOM + pao stdout fatal): `DB_CONNECTION=pgsql DB_HOST=127.0.0.1 DB_PORT=55432 DB_DATABASE=newco_test DB_USERNAME=newco DB_PASSWORD=newco php -d memory_limit=512M vendor/bin/pest <paths>`.
---

## [2026-06-16 11:02] — 1.2 ProducerLifecycleProjector consumer + provider registration
- **What:** Wired the codebase's FIRST registered cross-module domain-event consumer. It consumes Module K's `ProducerActivated`/`ProducerRetired` into the 1.1 `catalog_producer_states` projection — `active`/`retired` per the event, idempotent + order-tolerant via the per-producer `last_event_id` watermark. This is the read-model writer the 3.2 Producer Activation Gate will read. No gate, no FSM yet (those land next).
- **Files changed:**
  - `app/Modules/Catalog/Consumers/ProducerLifecycleProjector.php` (NEW) — `implements DomainEventConsumer`. `handle()` maps `$event->name` (two `public const` anchors `PRODUCER_ACTIVATED`/`PRODUCER_RETIRED` = bare § 15.4 strings, NO Parties import — invariant 10) → enum, narrows `payload['producer_id']` to int (phpstan-max-safe, throws on a malformed envelope), `lockForUpdate()`-reads the row, and seeds-or-watermark-upserts (`$event->id` must STRICTLY advance `last_event_id`). DB-work-only; opens no transaction (the executor wraps it).
  - `app/Modules/Catalog/Providers/CatalogServiceProvider.php` (EDIT) — `boot(ConsumerRegistry $registry)` (method injection) registers the projector for both event names (inline mode). Establishes the production consumer-registration seam (the empty stub gained its first real wiring).
  - `tests/Feature/Modules/Catalog/ProducerLifecycleProjectorTest.php` (NEW, `DatabaseMigrations`) — 6 tests: registration on the shared registry; full record→inline-deliver path (`done` row + projection `active` + watermark + `ProductMaster::count()===0`); `ProducerRetired`→`retired` (watermark advances, one row, no Master touched); idempotent re-handle (one row, no re-advance); order-tolerant stale re-handle (no regression); first-event-is-retirement seeds the row. Helper `recordProducerLifecycleEvent()` simulates Module K's emit with bare strings + `{producer_id,status}` payload (no Parties class import).
- **Quality loop: green.** Pint format ✓ · filtered 6/6 ✓ · full suite **487/487** SQLite (was 481; +6) ✓ · PHPStan max **0** (after fixing a `cast.int` on the mixed payload → narrowed with `is_int`+throw) ✓ · Pint --test ✓. **PG17:** `tests/Feature/Modules/Catalog` + `tests/Unit/Modules/Catalog` + `tests/Architecture` = **83/83** (was 77; +6) — the `lockForUpdate` lock + watermark + projection writes proven on a real PG lock; `ModuleBoundariesTest`/`ModulePersistenceConventionsTest` green (payload-only coupling, conventions hold). `openspec validate --strict` ✓.
- **Acceptance:** all bullets met — delivered `ProducerActivated`→`done` delivery + `status=active`; `ProducerRetired`→`retired`; idempotent (one row); order-tolerant (watermark never regresses); writes ONLY the projection (no `ProductMaster` mutation); no `App\Modules\Parties\*` import; quality green; verified on PG17.
- **Learnings for future iterations:**
  - The four new Codebase Patterns above are the reusable distillate: the `boot(ConsumerRegistry)` registration seam, the bare-string event-name anchoring, the watermark-upsert consumer shape, and the phpstan-max mixed-payload narrowing. Every future cross-module gate (and the Phase-3 referencers) follows the same consume→project→read-locally shape.
  - `ConsumerRegistry` is an `AppServiceProvider` singleton, so `boot()` method-injection shares the recorder's instance — no manual `app()->make`. All `register()`s run before any `boot()`, so the binding always exists by the time the module provider boots.
  - The watermark guard is testable ONLY by calling `handle()` directly — the delivery ledger makes a `done` row terminal, so re-`deliver()` never re-invokes the handler. Direct `handle($staleEvent)` is the faithful order-tolerance probe.
  - Next: **task 2.1** — `IllegalLifecycleTransition` exception + `lang/en/catalog.php` `lifecycle` group (the FSM's localized rejection copy). First of the lifecycle-mechanism tasks; no DB, no PG run needed (pure exception + i18n), but quality commands still green.
---

## [2026-06-16 11:10] — 2.1 IllegalLifecycleTransition + localized copy
- **What:** The FSM's single, parameterized rejection exception + its localized copy — the first lifecycle-mechanism task (no DB; pure exception + i18n). One `IllegalLifecycleTransition extends RuntimeException` serves ALL seven spine entities (the FSM is uniform, so the entity name is a factory param — design D2; the faithful analogue of Module K's three DISTINCT transition exceptions). The shared transition mechanism (task 2.2+) throws it; the localized reason names the rule + offending state + entity.
- **Files changed:**
  - `app/Modules/Catalog/Exceptions/IllegalLifecycleTransition.php` (NEW) — 4 named factories `cannotSubmit/cannotActivate/cannotRetire/cannotReopen(LifecycleState $from, string $entity)`, each delegating to a private `build(key, from, entity)` helper (DRY across 4 identical bodies; centralizes the `(string) __()` coercion + the `{state,entity}` placeholder contract so no factory can drift). Mirrors `IllegalProducerTransition` — but cites it in PROSE, never `{@see \FQCN}` (auto-import would breach invariant 10; see Patterns #1).
  - `lang/en/catalog.php` (EDIT) — added a `lifecycle` group (4 keys `cannot_submit`/`cannot_activate`/`cannot_retire`/`cannot_reopen`), `:state` + `:entity` placeholders (neither PII). Each reason names the rule and the valid-from state (submit←draft, activate←reviewed, retire←active, reopen←retired) — matches the design-D2 transition map. Header comment extended to cover transition Actions, not just creation.
  - `tests/Unit/Modules/Catalog/Exceptions/IllegalLifecycleTransitionTest.php` (NEW, `uses(TestCase::class)`, NO `RefreshDatabase`) — 9 tests / 33 assertions, mirroring `tests/Unit/Modules/Parties/Exceptions/TransitionExceptionsTest.php`: 4 per-factory (instanceof + non-empty + state token + entity token); a 4-case key datatest proving BOTH placeholders interpolate (sentinel tokens absent from every template); a "preserves existing catalog groups" guard (the `product_master`/`composite_sku` keys still resolve after the addition).
- **Quality loop: green.** Pint format ✓ · filtered 9/9 ✓ · full suite **496/496** SQLite (was 487; +9) ✓ · PHPStan max **0** ✓ · Pint --test ✓. **No PG run** — pure exception + i18n, touches no DB (per task + hot.md). `openspec validate --strict` ✓. Guards: no new migration, no composer drift, no protected files.
- **Acceptance:** all bullets met — each factory builds an exception whose message names the rule + the offending `:state` + the `:entity`; copy lives in `lang/en/catalog.php` (invariant 12, no hardcoded strings); quality green. Test-hint `__('catalog.lifecycle.cannot_activate', …)` resolves (covered by the key datatest).
- **Learnings for future iterations:**
  - **Interpolation-proof test idiom** (reused from the parties template): pick a from-state token ABSENT from that key's own literal copy (the copy names a DIFFERENT valid-from state) — its presence in the message proves `:state` was interpolated, not coincidental wording. For the cross-key datatest where no single real enum token is absent from all 4 templates, use a synthetic sentinel (`somestate`/`SomeEntity`).
  - Extended **Patterns #1**: the `{@see \FQCN}` trap is sharper for cross-module refs — auto-import of an existing other-module FQCN breaks `ModuleBoundariesTest` (invariant 10), not just PHPStan. Cite other-module classes in prose only.
  - Next: **task 2.2** — the shared `LifecycleTransition` mechanism (`app/Modules/Catalog/Lifecycle/`) + Product Master submit/reopen Actions (audit-only, NO domain event). First DB-touching lifecycle task → **PG17 run required**. It THROWS this exception on out-of-state calls and records an `AuditRecorder` row per step (verify `AuditRecorder::record(action,module,actorRole,actorId,entityType,entityId,before,after,authorizationBasis,…)` + `ActorContext` before use).
---

## [2026-06-16 11:40] — 2.2 Shared FSM mechanism + Product Master submit / reopen
- **What:** Stood up the shared lifecycle-transition mechanism — the foundation every later lifecycle task (2.3 governance, 3.x activate/retire+events+gate, 4.x the other six entities, 5.x cascades) extends — and wired Product Master's two AUDIT-ONLY transitions onto it (submit `draft→reviewed`, reopen `retired→reviewed`). No domain event on either checkpoint (Module 0 PRD § 14.2); first real module use of `AuditRecorder`. First DB-touching lifecycle task → PG17 verified.
- **Files changed:**
  - `app/Modules/Catalog/Lifecycle/HasLifecycleState.php` (NEW) — marker interface with ONE typed read method `lifecycleState(): LifecycleState` (a real method, NOT a magic `@property` — Larastan shadows interface `@property` through a `Model&Interface` intersection). Lets the mechanism stay generic over all seven spine entities.
  - `app/Modules/Catalog/Lifecycle/LifecycleTransitionType.php` (NEW) — the four-edge FSM map as an enum (Submit/Activate/Retire/Reopen), each case knowing its `from()`/`to()`/`auditVerb()`/`rejection()`. Single source of truth for the transition map; Activate/Retire cases exist now (the map is uniform) though only Submit/Reopen are wired this task.
  - `app/Modules/Catalog/Lifecycle/LifecycleTransition.php` (NEW) — the shared service: `transition(Model&HasLifecycleState $model, LifecycleTransitionType, string $entity): TModel`. DB::transaction → `lockForUpdate` (lock acquired, result discarded) → `$model->refresh()` → from-state assert (else throws 2.1's `IllegalLifecycleTransition`) → in-place `lifecycle_state` update → one `AuditRecorder` row (before/after `{lifecycle_state}`, actor from `ActorContext`, action `catalog.<entity>.<verb>`, basis `catalog-lifecycle`). Sole `lifecycle_state` writer (design D1).
  - `app/Modules/Catalog/Actions/SubmitProductMasterForReview.php` + `ReopenProductMaster.php` (NEW) — thin wrappers calling the mechanism with `LifecycleTransitionType::Submit`/`Reopen` + entity label `'ProductMaster'`.
  - `app/Modules/Catalog/Models/ProductMaster.php` (EDIT) — `implements HasLifecycleState` + one-line `lifecycleState()` read accessor; docblock refreshed (the spine's "no transition" clause was stale — the mechanism, not the model, is now the writer; still persistence-only).
  - `tests/Feature/Modules/Catalog/ProductMasterLifecycleTest.php` (NEW, `DatabaseMigrations`, per-test `actingAs(Operator…)`) — 6 tests/38 assertions: submit happy-path (state + one audit row w/ full envelope + operator actor + no `%Activated%`/`%Reviewed%` event); reopen happy-path; submit-on-reviewed rejected naming the offending state, no audit row; reopen-on-draft rejected; the lock re-read guards a STALE caller instance; a draft→reviewed→(retired)→reviewed round-trip (one audit row per audit-only leg).
- **Quality loop: green.** Pint ✓ · filtered 6/6 ✓ · full suite **502/502** SQLite (was 496; +6) ✓ · PHPStan max **0** ✓ · Pint --test ✓. **PG17:** new test 6/6 + `tests/Feature/Modules/Catalog` + `tests/Unit/Modules/Catalog` + `tests/Architecture` = **98/98** (the real `FOR UPDATE` lock, audit writes + from-state guard proven on Postgres; `ModuleBoundariesTest`/`ModulePersistenceConventionsTest` green — the Lifecycle classes are Catalog-internal, no Parties import). `openspec validate --strict` ✓. Guards: no new migration, no composer drift, no protected files.
- **Acceptance:** all bullets met — submit moves draft→reviewed + one audit + no event; reopen moves retired→reviewed likewise; an out-of-state call throws + writes nothing; the `lockForUpdate` re-read guards the from-state; quality green; verified on PG17.
- **Learnings for future iterations:**
  - The four new Codebase Patterns above are the reusable distillate (generic-mechanism `$model`-threading + refresh; typed interface method over `@property`; table-derived audit action; the Pint method-name trap). **2.3/3.2 extend this same `transition()`** — add a governance guard + per-entity gate closure BEFORE the `update`, and a `DomainEventRecorder` event factory AFTER it (the docblock notes both seams). `LifecycleTransitionType::Activate`/`Retire` already encode their edges, so those Actions just pass the case.
  - The mechanism is a plain injectable service (deps `AuditRecorder` + `ActorContext`, both autowirable) — NO provider registration needed (unlike the consumer in 1.2).
  - Entity label `'ProductMaster'` is passed explicitly by the Action (matches the domain-event `entity_type`); do NOT derive it from `class_basename` — that yields `SellableSku` ≠ the canonical `SellableSKU` for the SKU entities (4.5/4.6).
  - Next: **task 2.3** — approval governance (separation-of-duties + role-count config + rejection) layered onto the shared mechanism's commercial-impact steps, + `RejectProductMasterReview` + `config/catalog.php`. Verify `audit_records` querying for prior governance actors (creator from the `*Created` event `actor_id`, reviewer from the `draft→reviewed` audit row).
---

## [2026-06-16 12:10] — 2.3 Approval governance — separation-of-duties, role-count, rejection
- **What:** Layered the Creator → Reviewer → Approver approval governance onto the shared mechanism's commercial-impact steps (design D5). A new `ApprovalGovernance` guard service runs INSIDE `LifecycleTransition::transition()` (after the from-state assert, before the write) for every commercial-impact type (`requiresApprovalGovernance()` = activate/retire/reopen): the operator-principal floor always, plus the separation-of-duties distinctness at the APPROVAL step (`reviewed → active`). Added the `reject()` decision (`reviewed → reviewed`, audit-only, `decision: rejected` + notes) + the `RejectProductMasterReview` Action, and the role-count config. The audit trail is the SoR — no governance columns (D5). DB-touching → PG17 verified.
- **Files changed:**
  - `config/catalog.php` (NEW) — `approval.role_count` (env-overridable, default 3; ∈ {2,3}). Outside phpstan's `app/database/routes/tests` scope, so the `env()` read is unanalysed (intentional).
  - `lang/en/catalog.php` (EDIT) — `approval` group (4 keys: `requires_operator`/`self_approval_creator`/`self_approval_reviewer`/`insufficient_separation`, `:entity` only — no PII, the actor lives on the audit row) + a `lifecycle.cannot_reject` key.
  - `app/Modules/Catalog/Exceptions/ApprovalGovernanceViolation.php` (NEW) — `extends RuntimeException`, 4 named factories → localized copy (mirrors `IllegalLifecycleTransition`; one parameterized class, the governance is uniform).
  - `app/Modules/Catalog/Exceptions/IllegalLifecycleTransition.php` (EDIT) — `cannotReject` factory (reject's from-state guard).
  - `app/Modules/Catalog/Lifecycle/ApprovalGovernance.php` (NEW) — the guard. `guard(type, entity, entityId)` (commercial-impact) + `requireOperator(entity)` (reject). Reads the **creator** from the entity's FIRST `domain_events` row (its `*Created` event) and the **reviewer** from the latest `%.submitted` `audit_records` row; null prior actors are vacuous. Role-count from config (non-numeric → 3). Takes entity_id as a VALUE (no model dep, no key-narrowing, no cross-module import — only platform models + ActorContext).
  - `app/Modules/Catalog/Lifecycle/LifecycleTransitionType.php` (EDIT) — `requiresApprovalGovernance()` (true: activate/retire/reopen; false: submit).
  - `app/Modules/Catalog/Lifecycle/LifecycleTransition.php` (EDIT) — inject `ApprovalGovernance`; call `guard()` on commercial-impact steps; add `reject()`; extract `lockAndRefresh()` + generalise `recordAudit()` (verb + arbitrary before/after, threads the once-narrowed entityId).
  - `app/Modules/Catalog/Actions/RejectProductMasterReview.php` (NEW) — thin wrapper over `lifecycle->reject(…, 'ProductMaster', notes)`.
  - `tests/Feature/Modules/Catalog/ProductMasterLifecycleTest.php` (EDIT) — +8 tests / +46 assertions (reviewer & creator self-approval; reviewer==creator under role_count 3 vs the two-actor pass under role_count 2 — one test, both configs on one Master; system actor on activate AND on reject; reject records decision+notes & preserves prior rows; reject from non-reviewed; flow completes after a rejection w/ rejection-pending derived→cleared). Two file-local helpers (`lifecycleCreateDraftMaster`, `latestGovernanceAction`).
- **Quality loop: green.** Pint ✓ · filtered 14/14 ✓ · full suite **510/510** SQLite (was 502; +8) ✓ · PHPStan max **0** ✓ · Pint --test ✓. **PG17:** `tests/Feature/Modules/Catalog` + `tests/Unit/Modules/Catalog` + `tests/Architecture` = **106/106** (was 98; +8) — the `actor_id` string-vs-int normalization (the critical PG-only path), the `FOR UPDATE` lock, the `LIKE '%.submitted'` reviewer read and the jsonb rejection snapshot all proven on real Postgres; `ModuleBoundariesTest`/`ModulePersistenceConventionsTest`/`CatalogNamingCascadeTest` green. `openspec validate --strict` ✓. Guards: no new migration, no composer drift, no protected files.
- **Acceptance:** all bullets met — self-approval (creator, or reviewer in three-step) rejected on the SoD floor, Master stays `reviewed`, no `*Activated`; distinct actors pass at role_count 2 (test 3) and 3 (test 8); a system/null actor cannot perform a governance step (activate or reject); `RejectProductMasterReview` keeps `reviewed`, records decision+notes, preserves prior rows; the flow restarts/completes after a rejection (rejection non-terminal; rejection-pending derived from the latest governance audit action, cleared by the superseding approval); quality green; verified on PG17.
- **Learnings for future iterations:**
  - **Approval step is `=== Activate`; the other commercial-impact steps carry only the operator floor.** `requiresApprovalGovernance()` is the operator-principal gate (activate/retire/reopen); the separation-of-duties distinctness is keyed on `$type === LifecycleTransitionType::Activate` inside `guard()`. Reopen now requires an operator principal (commercial-impact) — the 2.2 reopen tests stayed green (they all `actingAs`). Order matters: from-state assert FIRST, then governance — so an out-of-state reopen still throws `IllegalLifecycleTransition`, not a governance error.
  - **Governance reads prior actors as VALUES, not via the model** — `guard(type, entity, entityId)` keeps the service model-free (no key-narrowing duplication) and import-clean (platform models + ActorContext only; no Parties type — invariant 10 holds). The mechanism narrows `entityId` once and threads it to both the guard and `recordAudit`.
  - **The two cross-engine traps above (uncast bigint string-vs-int; multi-key jsonb ordering) are now in Codebase Patterns** — both are SQLite-invisible, so this class of guard/snapshot work is exactly what the PG17 gate exists to catch.
  - **"rejection-pending" is a derived read, not a flag** (D5): the latest audit action for the entity (`%.rejected` latest ⇒ pending). A literal "re-submit from reviewed" is NOT reachable (Submit is draft→reviewed; § 4.3 forbids revert-to-draft), so the reachable, faithful realization is: a rejection is non-terminal, and any superseding governance action (here the approval) clears the derived pending state. The future `catalog-operator-console` builds the queue UI on this derivation.
  - **3.2 builds `ActivateProductMaster` on the SAME `transition()`** — it passes the Producer-gate predicate (read `catalog_producer_states` for the Master's `producer_id`, require `active`) + the `ProductMasterActivated` event factory; the governance guard already runs. The 2.3 tests drive the bare mechanism's Activate edge directly (no gate/event yet) — they remain valid after 3.2 adds optional gate/event params (the bare call passes neither).
---

## [2026-06-16 12:19] — 3.1 ProductMasterActivated + ProductMasterRetired events
- **What:** Added the first two of the change's fourteen `*Activated`/`*Retired` lifecycle event classes — Product Master's. Pure event contracts (no DB, no behaviour): `final` classes mirroring `ProductMasterCreated` and the Parties `Producer{Activated,Retired}` analogue. Task 3.2 records them (via the shared `LifecycleTransition` + the Producer gate); this task only defines the contract facets so 3.2 stays thin and magic-string-free.
- **Files changed:**
  - `app/Modules/Catalog/Events/ProductMasterActivated.php` (NEW) — `NAME='ProductMasterActivated'`, `ENTITY_TYPE='ProductMaster'`, static `payload(ProductMaster)` → `{product_master_id, producer_id, lifecycle_state}` (§14.2: `reviewed → active` only; the audit-only `draft→reviewed`/`retired→reviewed` checkpoints emit nothing — no `*Reviewed`). PII-free, producer by id.
  - `app/Modules/Catalog/Events/ProductMasterRetired.php` (NEW) — same shape, `NAME='ProductMasterRetired'` (§14.2: `active → retired`); docblock notes the cascade writer (`RetireProductMasterCascade`, task 5.2) records it parent-before-child.
  - `app/Modules/Catalog/Events/ProductMasterCreated.php` (EDIT) — corrected the now-false "No `*Activated`/`*Retired` sibling exists" docblock line (this task creates exactly those siblings); replaced with same-namespace `{@see ProductMasterActivated}`/`{@see ProductMasterRetired}` cross-refs (Pint leaves bare same-namespace `{@see}` — no import, no boundary risk; Patterns #1). The other six `*Created` events keep that line (their siblings land in 4.x).
  - `tests/Unit/Modules/Catalog/Events/ProductMasterLifecycleEventsTest.php` (NEW, `uses(TestCase::class)`, no DB) — 4 tests / 18 assertions: verbatim NAME+ENTITY_TYPE + `isFinal()` for each class; `payload()` over an in-memory `factory()->make()` Master returns exactly the three keys (order-pinned) with the right values and is PII-free (`name`/`product_type`/`producer` absent, the descriptive name not leaked).
- **Quality loop: green.** Pint format ✓ · filtered 4/4 ✓ · full suite **514/514** SQLite (was 510; +4) ✓ · PHPStan max **0** ✓ · Pint --test ✓. **No PG run** — pure event classes + i18n-free, touch no DB (per task + design D9). `openspec validate --strict` ✓. Guards: no new migration (the change's single `catalog_producer_states` migration shipped in 1.1), no composer drift, no protected files.
- **Acceptance:** all bullets met — both classes expose the three contract facets (NAME/ENTITY_TYPE/static `payload()`); payloads PII-free (producer by id only); Naming Cascade holds (`ProductMaster*` neutral names — `CatalogNamingCascadeTest` green in the full run); quality green.
- **Learnings for future iterations:**
  - The new Codebase Pattern above (DB-free event-payload unit test) is the reusable distillate — tasks **4.1–4.6** each add two more `*Activated`/`*Retired` classes + an identical-shape payload test (Format/CaseConfiguration/ProductVariant/ProductReference/SellableSKU/CompositeSKU). The SKU events keep UPPER-case `SKU` in NAME/class (`SellableSKUActivated`), the models stay lower-`Sku` — the casing divergence the Naming Cascade test already pins; the `*_id` payload keys follow the table/identity (e.g. `ProductReferenceActivated` → `{product_reference_id, product_variant_id, format_id, lifecycle_state}` per design D9).
  - When you create a sibling event, the matching `*Created` docblock's "No `*Activated`/`*Retired` sibling exists" line goes stale — fix only the one whose sibling you just created (leave the others until their 4.x task).
  - Next: **task 3.2** — `ActivateProductMaster` (`reviewed→active`: governance guard already wired + the NEW Producer gate reading `catalog_producer_states` for `status=active`, else a localized gate exception added to `lang/en/catalog.php`) records `ProductMasterActivated`; `RetireProductMaster` (`active→retired`) records `ProductMasterRetired`. Both via the shared `transition()` (audit + event in one txn). **DB-touching → PG17 run required.** Extend `ProductMasterLifecycleTest.php` (project producer 7 `active` via a recorded `ProducerActivated`; three negative gate paths: absent/`draft`/`retired`).
---

## [2026-06-16 12:54] — 3.2 Activate / Retire Product Master — wired to the gate, governance & events
- **What:** Wired Product Master's two COMMERCIAL-IMPACT transitions onto the shared mechanism — activate (`reviewed→active`, gated on the linked Producer being `active` + governance + records `ProductMasterActivated`) and retire (`active→retired`, records `ProductMasterRetired`). This realizes the headline cross-module gate (AC-0-FSM-12, AC-0-EVT-20/21, AC-0-J-2/10) on the producer-state projection the 1.2 consumer maintains. The shared `LifecycleTransition::transition()` gained the two seams 4.x/5.x reuse: an optional activation-gate closure (after governance, before write) + an optional domain-event intent (after write, in-txn). DB-touching → PG17 verified.
- **Files changed:**
  - `app/Modules/Catalog/Exceptions/ProducerActivationGateViolation.php` (NEW) — `extends RuntimeException`, one factory `producerNotActive(string $entity)` → localized `catalog.gate.producer_not_active` (`:entity` only — PII-free; producer by id, never named). Mirrors `ApprovalGovernanceViolation`. (Pint auto-imported the same-module `{@see ProducerState}` — harmless, no boundary breach.)
  - `lang/en/catalog.php` (EDIT) — new `gate` group, key `producer_not_active` (the hard-gate reason; `:entity` placeholder).
  - `app/Modules/Catalog/Lifecycle/ProducerActivationGate.php` (NEW) — the guard: `assertProducerActive(int $producerId, string $entity)` reads ONLY `catalog_producer_states` (the Catalog projection), throws unless a row with `status = Active`. Fail-closed: no row (draft/unknown) ≡ retired → reject. Lock-free read (read-time gate). No `App\Modules\Parties\*` import (invariant 10).
  - `app/Modules/Catalog/Lifecycle/LifecycleTransition.php` (EDIT) — inject `DomainEventRecorder`; `transition()` gains `?Closure $gate` (run after governance, before the write) + `?Closure $event` (`(TModel): array{name, payload}`, recorded via the recorder AFTER the write, in the same txn — the mechanism owns the envelope: module catalog, ActorContext principal, entity type/id). Class docblock updated (the two seams are now plugged, no longer "task 3.x TODO").
  - `app/Modules/Catalog/Actions/ActivateProductMaster.php` (NEW) — passes the Producer gate (`$producerGate->assertProducerActive($m->producer_id, …)`) + the `ProductMasterActivated` intent; governance already runs inside `transition()`. Re-activation re-checks (same Action, no special-casing).
  - `app/Modules/Catalog/Actions/RetireProductMaster.php` (NEW) — single-entity retire; passes only the `ProductMasterRetired` intent (no gate; the within-catalog ref-integrity guard + the cascade are 5.2).
  - `tests/Feature/Modules/Catalog/ProductMasterLifecycleTest.php` (EDIT) — +8 tests / +60 assertions (22 total): activate-happy (state + one audit + one PII-free `*Activated` w/ full envelope + approver actor); gate negatives absent + retired; from-state guard precedes the gate (draft → FSM error, not gate); retire-happy (`*Retired` envelope); block-new/preserve (M1 active, retire producer 7, M2 rejected, M1 untouched, one `*Activated`); re-activation re-checks both ways (producer still active → 2nd `*Activated`; producer since-retired → blocked, one `*Activated`). New file-local helper `lifecycleProjectProducer()` (records a Parties `ProducerActivated`/`Retired` → inline consumer projects); `lifecycleCreateDraftMaster()` extended with name/appellation params (block-new needs two distinct identities on one producer).
- **Quality loop: green.** Pint ✓ · filtered 22/22 ✓ · full suite **522/522** SQLite (was 514; +8) ✓ · PHPStan max **0** ✓ · Pint --test ✓. **PG17:** lifecycle 22/22 + `tests/Feature/Modules/Catalog` + `tests/Unit/Modules/Catalog` + `tests/Architecture` = **118/118** (was 106 @2.3 +4 @3.1 +8) — the uncast `actor_id` string-vs-int on the recorded events, the jsonb payload, the `FOR UPDATE` lock, the producer-state CHECK during consumption, and the gate read all proven on real Postgres; `ModuleBoundariesTest`/`ModulePersistenceConventionsTest`/`CatalogNamingCascadeTest` green (the gate is event-payload + own-projection only). `openspec validate --strict` ✓. Guards: no new migration, no composer drift, no protected files.
- **Acceptance:** all bullets met — activation rejected on absent (=draft/unknown) + retired projections (AC-0-FSM-12), succeeds when active (AC-0-EVT-20); `ProducerRetired` blocks a new activation while the already-active Master is untouched (AC-0-EVT-21/FSM-13); re-activation re-checks (AC-0-J-10); a Master on an unprojected producer is held in reviewed but not activatable (AC-0-J-2); `*Activated`/`*Retired` recorded in the writing txn, module catalog, PII-free (AC-0-EVT-1); quality green; PG17 verified.
- **Learnings for future iterations:**
  - The new Codebase Pattern above (the `transition()` gate/event seam) is the reusable distillate — **tasks 4.1–4.6** each add an Activate Action passing a parent-active gate closure + the entity's `*Activated` intent, and a Retire Action passing the `*Retired` intent. 4.x gates read sibling `lifecycle_state` WITHIN module (Variant→Master, PR→Variant+Format, SKU→PR+CaseConfig, Composite→all constituents) — a different exception family from this cross-module Producer gate (separate spec Requirement). 5.2 adds the within-catalog ref-integrity guard onto the Retire path + the parent-before-child cascade.
  - **Gate-test gotcha:** `CreateProductMaster` runs the BR-Identity-1 dedup (producer + name + appellation, non-retired), so a test standing up TWO Masters under ONE producer (block-new) MUST give the second a distinct identity — `lifecycleCreateDraftMaster()` now takes name/appellation for exactly this.
  - **Projecting a producer in a lifecycle test:** record a Parties `ProducerActivated`/`ProducerRetired` inside `DB::transaction` and let the inline consumer project it (helper `lifecycleProjectProducer`); needs `DatabaseMigrations` (the post-commit hook fires at txn level 0). A first-seen `ProducerRetired` seeds the row retired (the 1.2 migration-plan behaviour), so a single event suffices for the retired-gate negative.
  - Next: **task 4.1** — Format lifecycle (standalone, no parent gate): `FormatActivated`/`FormatRetired` events + Submit/Activate/Retire/Reopen/RejectFormat Actions via the shared mechanism (Activate passes only the event intent + governance; NO gate). Mirror the 3.1 event shape + the 3.2 Action shape. PG17 (DB-touching).
---

## [2026-06-16 13:07] — 4.1 Format lifecycle (standalone — no parent gate)
- **What:** First of the six remaining spine entities to gain its full lifecycle, and the first STANDALONE one — a Format has no parent in the hierarchy, so its activation carries NO activation gate, only the approval governance already wired into `transition()` (task 2.3). Pure mirror of the 3.1 event shape + the 3.2 Action shape, minus the Producer gate. Establishes the per-entity recipe 4.2 (also standalone) reuses verbatim and 4.3–4.6 extend with a within-module parent-active gate. DB-touching → PG17 verified.
- **Files changed (11 — 9 new, 2 edited):**
  - `app/Modules/Catalog/Events/FormatActivated.php` + `FormatRetired.php` (NEW) — `final` classes mirroring `ProductMasterActivated`/`Retired`; `NAME`/`ENTITY_TYPE`/static `payload(Format)`. Standalone ⇒ the minimal PII-free pair `{format_id, lifecycle_state}` (no parent/party ids — design D9 `{<entity>_id, lifecycle_state, …ids}` with no "…ids"); the descriptive measure fields belong to `FormatCreated`, omitted from the transition snapshot.
  - `app/Modules/Catalog/Actions/SubmitFormatForReview.php` + `ReopenFormat.php` + `RejectFormatReview.php` + `ActivateFormat.php` + `RetireFormat.php` (NEW) — thin wrappers over the shared mechanism. **Activate passes ONLY the `event:` intent (NO `$gate`)** + governance; Retire passes only the `FormatRetired` intent; Submit/Reopen are bare audit-only `transition()`; Reject calls `reject(...)`. Entity label `'Format'` / `FormatActivated::ENTITY_TYPE`.
  - `app/Modules/Catalog/Models/Format.php` (EDIT) — `implements HasLifecycleState` + one-line `lifecycleState()` accessor; docblock refreshed (the spine-creation "no transition out of draft" line was stale — the mechanism is now the sole `lifecycle_state` writer; still persistence-only).
  - `app/Modules/Catalog/Events/FormatCreated.php` (EDIT) — fixed the now-false "No `*Activated`/`*Retired` sibling exists" line → same-namespace `{@see FormatActivated}`/`{@see FormatRetired}` cross-refs (Pint-safe, no import; mirrors the 3.1 `ProductMasterCreated` edit).
  - `tests/Unit/Modules/Catalog/Events/FormatLifecycleEventsTest.php` (NEW, `TestCase`, no DB) — 4 tests/24 assertions: verbatim NAME/ENTITY_TYPE + `isFinal()`; `payload()` over a `factory()->make()` Format returns exactly `{format_id, lifecycle_state}` (order-pinned, values), PII-free (no `name`/`size_label`/`volume_ml`, descriptive name not leaked).
  - `tests/Feature/Modules/Catalog/FormatLifecycleTest.php` (NEW, `DatabaseMigrations`, distinct operators) — 8 tests/63 assertions: submit→reviewed (audit row + event-silent); activate-happy (3 distinct operators → active + one `FormatActivated`, full envelope + PII-free payload); **self-approval (creator) rejected (governance applies with no parent gate)**; from-state guards (activate-on-draft + submit-on-reviewed); retire→retired + `FormatRetired`; reopen→reviewed (audit-only); reject→stays-reviewed + rejection row preserved. New uniquely-named global helper `lifecycleCreateDraftFormat()`.
- **Quality loop: green.** Pint ✓ · filtered 12/12 ✓ · full suite **534/534** SQLite (was 522; +12) ✓ · PHPStan max **0** ✓ · Pint --test ✓. **PG17:** Format lifecycle+events 12/12 + `tests/Feature/Modules/Catalog` + `tests/Unit/Modules/Catalog` + `tests/Architecture` = **130/130** (was 118; +12) — the audit/event `actor_id` string-vs-int, the jsonb payload, the `FOR UPDATE` lock, and the governance lineage reads all proven on real Postgres; `ModuleBoundariesTest`/`ModulePersistenceConventionsTest`/`CatalogNamingCascadeTest` green. `openspec validate --strict` ✓. Guards: no new migration, no composer drift, no protected files.
- **Acceptance:** all bullets met — a Format activates with NO parent gate (governance only: distinct-actor positive + self-approval negative both proven); records `FormatActivated`/`FormatRetired` (PII-free, in-txn, module catalog); from-state guarded (activate-on-draft, submit-on-reviewed); quality green; PG17 verified.
- **Learnings for future iterations:**
  - The two new Codebase Patterns above are the reusable distillate: the per-entity lifecycle recipe (proven on a standalone entity) and the per-file unique-helper-name rule. **Task 4.2 (Case Configuration) is the EXACT same shape** — standalone, no gate; swap Format→CaseConfiguration, payload `{case_configuration_id, lifecycle_state}`, helper `lifecycleCreateDraftCaseConfiguration`, assert `CaseConfigurationActivated`. **Tasks 4.3–4.6 add a `gate:` closure** reading sibling `lifecycle_state` WITHIN module (a new within-catalog gate exception family — separate from the cross-module Producer gate).
  - A standalone Activate is NOT ungated — it still runs approval governance. The test must prove governance applies (self-approval rejected), else "no parent gate" is mis-read as "no precondition."
  - Next: **task 4.2** — Case Configuration lifecycle (standalone, no parent gate): `CaseConfigurationActivated`/`Retired` events + the five Actions; mirror 4.1 verbatim (verify the `CaseConfiguration` model needs the `HasLifecycleState` accessor added, and check `CreateCaseConfiguration`'s signature for the test helper). PG17 (DB-touching).
---

## [2026-06-16 13:18] — 4.2 Case Configuration lifecycle (standalone — no parent gate)
- **What:** The SECOND of the six remaining spine entities, and the second STANDALONE one — a Case Configuration has no parent in the hierarchy, so its activation carries NO activation gate, only the approval governance already wired into `transition()` (task 2.3). A verbatim mirror of the 4.1 Format recipe: the per-entity lifecycle recipe (Codebase Patterns #19) applied with zero deviation. DB-touching → PG17 verified.
- **Files changed (11 — 9 new, 2 edited):**
  - `app/Modules/Catalog/Events/CaseConfigurationActivated.php` + `CaseConfigurationRetired.php` (NEW) — `final` classes mirroring `FormatActivated`/`Retired`; `NAME`/`ENTITY_TYPE`/static `payload(CaseConfiguration)`. Standalone ⇒ the minimal PII-free pair `{case_configuration_id, lifecycle_state}` (no parent/party ids); the descriptive packaging fields (`name`/`units_per_case`/`packaging_type`) belong to `CaseConfigurationCreated`, omitted from the transition snapshot.
  - `app/Modules/Catalog/Actions/SubmitCaseConfigurationForReview.php` + `ReopenCaseConfiguration.php` + `RejectCaseConfigurationReview.php` + `ActivateCaseConfiguration.php` + `RetireCaseConfiguration.php` (NEW) — thin wrappers over the shared mechanism. **Activate passes ONLY the `event:` intent (NO `$gate`)** + governance; Retire passes only the `CaseConfigurationRetired` intent; Submit/Reopen are bare audit-only `transition()`; Reject calls `reject(...)`. Entity label `'CaseConfiguration'` / `CaseConfigurationActivated::ENTITY_TYPE`.
  - `app/Modules/Catalog/Models/CaseConfiguration.php` (EDIT) — `implements HasLifecycleState` + import + one-line `lifecycleState()` accessor; docblock refreshed (the spine-creation "this change defines no transition out of it" line was stale — the mechanism is now the sole `lifecycle_state` writer; still persistence-only).
  - `app/Modules/Catalog/Events/CaseConfigurationCreated.php` (EDIT) — fixed the now-false "No `*Activated`/`*Retired` sibling exists" line → same-namespace `{@see CaseConfigurationActivated}`/`{@see CaseConfigurationRetired}` cross-refs (Pint-safe, no import; mirrors the 4.1 `FormatCreated` edit).
  - `tests/Unit/Modules/Catalog/Events/CaseConfigurationLifecycleEventsTest.php` (NEW, `TestCase`, no DB) — 4 tests/24 assertions: verbatim NAME/ENTITY_TYPE + `isFinal()`; `payload()` over a `factory()->make()` Case Config returns exactly `{case_configuration_id, lifecycle_state}` (order-pinned, values), PII-free (no `name`/`units_per_case`/`packaging_type`, descriptive name not leaked).
  - `tests/Feature/Modules/Catalog/CaseConfigurationLifecycleTest.php` (NEW, `DatabaseMigrations`, distinct operators) — 8 tests/63 assertions: submit→reviewed (audit row + event-silent); activate-happy (3 distinct operators → active + one `CaseConfigurationActivated`, full envelope + PII-free payload); **self-approval (creator) rejected (governance applies with no parent gate)**; from-state guards (activate-on-draft + submit-on-reviewed); retire→retired + `CaseConfigurationRetired`; reopen→reviewed (audit-only); reject→stays-reviewed + rejection row preserved. New uniquely-named global helper `lifecycleCreateDraftCaseConfiguration()`.
- **Quality loop: green.** Pint ✓ · filtered 12/12 ✓ · full suite **546/546** SQLite (was 534; +12) ✓ · PHPStan max **0** ✓ · Pint --test ✓. **PG17:** `tests/Feature/Modules/Catalog` + `tests/Unit/Modules/Catalog` + `tests/Architecture` = **142/142** (was 130; +12) — the audit/event `actor_id` string-vs-int, the jsonb payload, the `FOR UPDATE` lock, and the governance lineage reads all proven on real Postgres; `ModuleBoundariesTest`/`ModulePersistenceConventionsTest`/`CatalogNamingCascadeTest` green. `openspec validate --strict` ✓. Guards: no new migration, no composer drift, no protected files, no `App\Modules\Parties\*` import.
- **Acceptance:** all bullets met — a Case Configuration activates with NO parent gate (governance only: distinct-actor positive + self-approval negative both proven); records `CaseConfigurationActivated`/`CaseConfigurationRetired` (PII-free, in-txn, module catalog); from-state guarded (activate-on-draft, submit-on-reviewed); quality green; PG17 verified.
- **Learnings for future iterations:**
  - **The per-entity recipe (Patterns #19/#20) is now proven on TWO standalone entities (Format, Case Configuration) with zero deviation** — the recipe is stable. Audit segment auto-derived: `catalog_case_configurations → catalog.case_configuration.<verb>` (`Str::singular(Str::after(table,'catalog_'))`, Pattern #14, no per-Action wiring). No new lang keys: the standalone entity reuses the entity-parameterized `lifecycle`/`approval` groups (`:entity` placeholder); no `gate` key (no parent gate). The `'creator'` substring in the self-approval negative comes from `catalog.approval.self_approval_creator` (entity-independent), so it matches for any entity label.
  - **Pest dup-registration gotcha (harness, not code):** passing BOTH an explicit test file AND its containing folder to one `pest` invocation aborts with "already uses the test case [Tests\TestCase]". When a new file lives under a folder you're already running, pass ONLY the folder (it includes the file). The 4.1 run avoided this by listing the file separately from the broader folders; the simplest PG recipe is just the three folders (`Feature/Modules/Catalog` + `Unit/Modules/Catalog` + `Architecture`), which already include the new files.
  - Next: **task 4.3** — Product Variant lifecycle + the FIRST within-module activation gate (`gate:` closure requiring the parent `ProductMaster` `product_master_id` to be `active`). This is where the 4.x recipe DIVERGES from the two standalone entities: Activate passes a `gate:` reading the sibling `lifecycle_state` WITHIN module — a NEW within-catalog gate exception family (separate spec Requirement from the cross-module Producer gate). Verify the `ProductVariant` model needs `HasLifecycleState` + accessor; check `CreateProductVariant` signature + how a Variant binds its `product_master_id`; the positive test needs the FULL parent chain active (producer projected `active` → Master submit+approve → active) before the Variant can activate.
---
