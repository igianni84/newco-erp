## 1. ProfileResource read-only + approval-queue list + page scaffold + resource/infolist i18n

- [x] 1.1 Create `app/Modules/OperatorPanel/Filament/Resources/Parties/ProfileResource.php` — `extends OperatorConsoleResource`, `static i18nKey(): string => 'profile'` (reuse `getModelLabel`/`getPluralModelLabel`/`versionColumn()`). Read-bind `\App\Modules\Parties\Models\Profile`. List columns: customer (`fn (Profile $r) => $r->customer->email` + name) · club (`fn (Profile $r) => $r->club->display_name` — `Club` has `display_name`, lesson 2026-06-21) · a **`state`** badge `TextColumn` rendering `$r->state->value` via `getStateUsing` + `instanceof BackedEnum` (**no `Parties\Enums` import** — state via cast, design D2) · `versionColumn()`. Infolist: customer, club, the `state` badge, `tier`, `lapsed_at`, `cancellation_reason`, `version` — all read-only. **No** create/edit/delete mutating action on the table; `getPages()` → index/create/view. Do **NOT** call `lifecycleStateColumn()` (that is the catalog `lifecycle_state`, design D2).
- [x] 1.2 Create `ProfileResource/Pages/{ListProfiles,CreateProfile,ViewProfile}.php`. `ListProfiles`: the **approval queue** — Filament tabs `getTabs()` with a default **"Pending"** tab (`->modifyQueryUsing(fn ($q) => $q->where('state', 'applied'))` — string literal, no enum import) and an **"All"** tab. `ViewProfile`: `extends \Filament\Resources\Pages\ViewRecord` and `use SurfacesDomainActions` (NOT `OperatorConsoleViewRecord`, design D1), `i18nKey(): string => 'profile'`, `getHeaderActions(): []` for now (verbs land in groups 3–5). `CreateProfile`: `extends OperatorConsoleCreateRecord` (filled in group 2). Register the three pages in `ProfileResource::getPages()`.
- [x] 1.3 Add `operator_console.profile.{label, plural_label, columns.*, fields.*, tabs.*}` to `lang/en/operator_console.php` and `lang/it/operator_console.php` (DEC-127: IT omits `label`/`plural_label` for per-key EN fallback; all other keys authored in both).
- **Tests** (`tests/Feature/Modules/OperatorPanel/Parties/ProfileResourceTest.php`, `RefreshDatabase`): a seeded Profile renders in the list with its customer, club and `state`; the **Pending** tab shows only `applied` Profiles and the **All** tab shows every state; `ProfileResource::getPages()` keys == `['index','create','view']`; the table exposes no edit/delete/bulk action. Arch/boundary: `NoEloquentWriteInOperatorPanelRule` + `ModuleBoundariesTest` stay green over the new class (**no `Parties\Enums` import introduced** — every state read is `->state->value`). Typecheck passes; tests pass.

## 2. Profile create surface (write-through, int operands)

- [x] 2.1 Fill `ProfileResource/Pages/CreateProfile.php` — `extends OperatorConsoleCreateRecord`. **First read `app/Modules/Parties/Actions/CreateProfile.php`** to confirm the `handle()` signature and its gate(s) before wiring (never trust prose for identifiers, root CLAUDE.md §4). Form: a **Customer** `Select` (within-module read of `\App\Modules\Parties\Models\Customer`, label email+name) and a **Club** `Select` (`\App\Modules\Parties\Models\Club`, label `display_name`) — both Parties models, no cross-module import. `createViaAction(array $data): Model` → `app(CreateProfile::class)->handle((int) $data['customer_id'], (int) $data['club_id'])`. `createRejectionField(): string` → the form field to surface `DuplicateProfileForClub` on (e.g. `'club_id'`). Expose **no** `state`/`tier`/`role` field (born `applied`, single-tier DEC-062, design D6). Surface exactly the domain's rejection(s); if the Action gates on Club-`sunset`/Customer state, let that rejection surface — do **not** invent a console-side select filter (design D6 / Open Question). Reference `DuplicateProfileForClub` as **prose** in any docblock, never `{@see FQCN}` (Pint import-boundary trap, lesson 2026-06-20).
- [x] 2.2 Add `operator_console.profile.fields.{customer, club}` + create-surface labels to `lang/en` + `lang/it`.
- **Tests** (`ProfileCreateConsoleTest.php`): a valid Customer+Club submit → a Profile born `applied` for that pair **and** exactly one `ProfileCreated` event with `actor_role: newco_ops`, `actor_id` = operator; a duplicate non-terminal pair → `DuplicateProfileForClub` surfaced on the field, no second Profile, and **zero** new events (assert event-count delta 0); the create form exposes no `state`/`tier`/`role` field. Use the `Pest\Laravel\*` globals / Filament Livewire test helpers, never `$this->get()` (lesson 2026-06-13). Typecheck passes; tests pass.

## 3. Profile membership approval — approve / decline (ViewProfile)

- [x] 3.1 On `ViewProfile`, append two header actions via the kit's `lifecycleAction($verb, $successKey, $invoke)` (form-less, `$form = null`), each `->visible(...)` gated to `state == 'applied'`:
  - `approve` → `fn (Model $record, string $notes) => app(ApproveProfile::class)->handle($this->recordOf(Profile::class, $record)->id)`, `->visible(fn (): bool => $this->recordOf(Profile::class, $this->getRecord())->state->value === 'applied')`
  - `decline` → `app(DeclineProfile::class)->handle(...)`, same `->visible(...)`.
  Catch domain rejections by base `\RuntimeException` (the kit's `surfaceLifecycleOutcome`); reference `IllegalProfileTransition` only as **prose** (never `{@see FQCN}`/`@throws FQCN` — Pint adds a boundary-violating `use`, lesson 2026-06-20).
- [x] 3.2 Add `operator_console.profile.actions.{approve, decline}` + `notifications.{approved, declined}` to `lang/en` + `lang/it`.
- **Tests** (`ProfileApprovalConsoleTest.php`): approve an `Applied` Profile of a Customer whose `originating_club_id` is null → `approved` + exactly one `OriginatingClubLocked` (actor_role newco_ops) + Customer `originating_club_id` set to that Club; approve a **second** Club's `Applied` Profile for the same Customer → `approved` + **no further** `OriginatingClubLocked` (assert count unchanged — one-shot lock); decline an `Applied` → `rejected` + **zero** new domain events (audit-only); `assertActionVisible('approve'/'decline')` iff `Applied`, else `assertActionHidden`; an out-of-state approve/decline is a **domain throw** — `expect(fn () => app(ApproveProfile::class)->handle($nonApplied->id))->toThrow(IllegalProfileTransition::class)` **+** `assertActionHidden` (a hidden action is undriveable through the widget — assert the domain rejection directly, lesson 2026-06-22). Typecheck passes; tests pass.

## 4. Profile activation + suspension/restoration (ViewProfile)

- [x] 4.1 Append three header actions to `ViewProfile` (form-less, `lifecycleAction`, visibility-gated):
  - `activate` → `app(ActivateProfile::class)->handle($this->recordOf(Profile::class, $record)->id)`, visible iff `state == 'approved'`. **Uncapped** — drive `ActivateProfile` with no capacity check (the Hero-Package cap is a deferred Module-A seam, design Non-Goals); invent no cap.
  - `suspend` → `app(SuspendProfile::class)->handle(...)`, visible iff `state == 'active'`.
  - `reactivate` → `app(ReactivateProfile::class)->handle(...)`, visible iff `state == 'suspended'`.
- [x] 4.2 Add `operator_console.profile.actions.{activate, suspend, reactivate}` + `notifications.{activated, suspended, reactivated}` to `lang/en` + `lang/it`.
- **Tests** (`ProfileActivationConsoleTest.php`): activate an `Approved` Profile → `Active` + exactly one `ProfileActivated` (newco_ops); suspend an `Active` Profile → `Suspended` + one `ProfileSuspended`, and assert a co-existing `activeClubCredit` (seed via the ClubCredit factory) is **unchanged** — suspension changes only `state` (state-preservation, AC-K-FSM-2a); reactivate a `Suspended` Profile → `Active` + one `ProfileReactivated`; visibility complements via `assertActionVisible/Hidden`; illegal transitions are **domain-direct throws** + `assertActionHidden` (lesson 2026-06-22). Typecheck passes; tests pass.

## 5. Profile lapse / renew / terminal (ViewProfile)

- [x] 5.1 Append four header actions to `ViewProfile` (form-less, `lifecycleAction`, visibility-gated):
  - `lapse` → `app(LapseProfile::class)->handle(...)`, visible iff `state == 'active'`.
  - `renew` → `app(RenewProfile::class)->handle(...)`, visible iff `state == 'lapsed'`. **Design D5:** the 30-day-grace sub-gate is domain-internal — a past-grace renew is reachable through the surface and the domain rejects it → `surfaceLifecycleOutcome` sends the `action_failed` notification (the one UI-reachable reject in this slice).
  - `cancel` → `app(CancelProfile::class)->handle(...)`, visible iff `state == 'active' || state == 'lapsed'`. **Audit-only — no event.**
  - `deactivate` → `app(DeactivateProfile::class)->handle(...)`, visible iff `state == 'active'`.
- [x] 5.2 Add `operator_console.profile.actions.{lapse, renew, cancel, deactivate}` + `notifications.{lapsed, renewed, cancelled, deactivated}` to `lang/en` + `lang/it`.
- **Tests** (`ProfileLifecycleConsoleTest.php`): lapse an `Active` Profile → `Lapsed` + one `ProfileExpired`; renew a within-grace `Lapsed` Profile (`lapsed_at = now`) → `Active` + one `ProfileRenewed`; renew a **past-grace** `Lapsed` Profile (`travelTo` 31 days after `lapsed_at`) driven **through the page** → a danger `action_failed` notification (`assertNotified`) + state stays `Lapsed` + no event (design D5 — the sole UI-reachable reject); cancel an `Active`/`Lapsed` Profile → `Cancelled` + **zero** new events (audit-only) + the row stays queryable (soft-delete, AC-K-FSM-13); deactivate an `Active` Profile → `Inactive` + one `ProfileInactive`; visibility complements; the other illegal transitions are domain-direct throws + `assertActionHidden`. Typecheck passes; tests pass.

## 6. Account lifecycle on ViewCustomer (suspend / reactivate / close)

- [x] 6.1 On `CustomerResource/Pages/ViewCustomer.php` `getHeaderActions()`, append three form-less Account verbs via `lifecycleAction`, visibility-gated by the 1:1 Account's status (`$customer->account` is non-null by co-provisioning):
  - `suspendAccount` → `app(SuspendAccount::class)->handle($this->recordOf(Customer::class, $record)->account->id)`, visible iff `…->account->status->value === 'active'`.
  - `reactivateAccount` → `app(ReactivateAccount::class)->handle(...)`, visible iff `…->account->status->value === 'suspended'`.
  - `closeAccount` → `app(CloseAccount::class)->handle(...)`, visible iff status ∈ `{'active','suspended'}`.
  Reference `IllegalAccountTransition` as **prose** only (Pint trap, lesson 2026-06-20). No `Parties\Enums` import added (Account Actions take an int; status read via `->status->value`).
- [x] 6.2 Add `operator_console.customer.actions.{suspend_account, reactivate_account, close_account}` + `notifications.{account_suspended, account_reactivated, account_closed}` to `lang/en` + `lang/it`.
- **Tests** (`AccountLifecycleConsoleTest.php`): suspend an `active` Account → `suspended` + **zero** domain events (audit-only) + the Customer status and its Profiles **unchanged** (orthogonality, AC-K-FSM-9 / AC-K-BR-Customer-1); reactivate → `active`; close → `closed`; no Account activation verb exists; visibility complements via `assertActionVisible/Hidden`; an illegal Account transition is a domain-direct throw (`IllegalAccountTransition`) + `assertActionHidden` (lesson 2026-06-22). Typecheck passes; tests pass.

## 7. i18n kit-key completeness (EN/IT)

- [x] 7.1 Add `ProfileConsoleI18nTest.php` — **reuse** the suite-wide `scanOperatorConsoleHardcodedSinks` helper behind a `function_exists` guard (declared in the Catalog `ProductMasterConsoleI18nTest`; do **not** redeclare — a redeclaration fatals when both files load, lesson 2026-06-20). Assert: every `operator_console.profile.*` key **and** the new `operator_console.customer.*` Account keys resolve in `lang/en`; IT provides distinct values **except** the per-key EN-fallback keys (DEC-127 — `label`/`plural_label` omitted in IT); IT ⊆ EN (no dangling IT key); the new Filament classes route every user-facing string through i18n (the sink scan finds no hardcoded copy). **Verify via `php -d memory_limit=-1 vendor/bin/pest --filter=ProfileConsoleI18nTest`** (or the full suite) — **never** a bare single-file path, which leaves the shared helper undeclared → false red (lesson 2026-06-20). Typecheck passes; tests pass.

## 8. PG17 closing-chain integration + quality gates

- [x] 8.1 Add `ProfileMembershipChainTest.php` — a single self-contained PG17 integration test driving the **full** Profile FSM through the Filament pages: create (→ `Applied`) → approve (→ `Approved`, asserting `OriginatingClubLocked`) → activate (→ `Active`) → suspend (→ `Suspended`) → reactivate (→ `Active`) → lapse (→ `Lapsed`) → renew (→ `Active`) → cancel/deactivate (terminal); plus the Account suspend → reactivate → close chain on `ViewCustomer`. Assert the event sequence and final states. Run the PG17 ritual: a throwaway `postgres:17` Docker container, `DB_CONNECTION=pgsql … php -d memory_limit=-1 vendor/bin/pest --filter=ProfileMembershipChainTest` (GUIDE §2.7 recipe; `knowledge/testing/rules.md`). A self-contained chain test (no shared i18n helper) is safe to run via `--filter` alone.
- [ ] 8.2 Full-suite gate: `php -d memory_limit=-1 vendor/bin/pest` (NOT `artisan test` — OOMs at 128 MB, lesson 2026-06-20) green; `php -d memory_limit=-1 vendor/bin/phpstan analyse` (max, 0 errors); `vendor/bin/pint --test` clean; `openspec validate operator-console-parties-membership --strict` valid. Confirm **no new `Parties\Actions\*`** was added (this slice drives existing Actions) → `SupplyLifecycleChainTest` needs no whitelist change (grep `app/Modules/Parties/Actions/`; design D8). No new ADR (rides existing); no `decisions/INDEX.md` change expected. Consolidate the change's `progress.md` `## Codebase Patterns`. CHANGE_COMPLETE.

