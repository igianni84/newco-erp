# Progress — operator-console-parties-producer

## Codebase Patterns
(consolidated reusable patterns — read first each iteration)

- **The non-catalog console recipe (§1 confirmed) — reuse the kit where it is lifecycle-agnostic, diverge where the catalog FSM is encoded (ADR 2026-06-20).** `ProducerResource extends OperatorConsoleResource` for the label helpers (off `static i18nKey() => 'producer'`) + `versionColumn()`, but does **NOT** call `lifecycleStateColumn()` — Producer's state lives in `status` (a `ProducerStatus`) plus a separate nullable `kyc_status` (`KycStatus|null`), so the resource supplies its **own** `statusColumn()` + `kycStatusColumn()` (private static helpers) rendering the enum via `getStateUsing` + `instanceof BackedEnum ? (string) $state->value : ''` — type-clean at phpstan-max, **no `Parties\Enums` import** (the nullable kyc column renders `''` for NULL). List = name · region · country · status · kyc_status · `versionColumn()`; infolist = identity attrs + the translatable `description` + the operated-Clubs read. `ListProducers` = the header create-LINK page (copy `ListFormats`, NOT a `CreateAction`). The OperatorPanel import surface stays exactly **{`Parties\Models\Producer`}** on the Resource (the Create page adds `Parties\Actions\CreateProducer`).
- **`getPages()` coupling gotcha (drove the §1 slice shape).** A Filament Resource's `getPages()` eagerly references each page class (`Pages\CreateProducer::route()` autoloads the class), and the panel calls `getPages()` at **registration** — so the resource cannot boot until **all three** page classes (`ListProducers`, `CreateProducer`, `ViewProducer`) exist. The §1 test pins `getPages()` keys `== ['index','create','view']`. Therefore the read-surface section (§1) must **also scaffold** `CreateProducer` + `ViewProducer`, or the whole operator panel fails to boot and every Filament test reds. §1 created `CreateProducer` with its **real** `createViaAction` (the form on the Resource + the create test land in §2) and `ViewProducer` as a **bare read-only `ViewRecord`** (the `SurfacesDomainActions` trait + the activate/retire + KYC header actions land in §3/§4). `discoverResources` scans `Filament/Resources` recursively, so the new `Resources/Parties/` sub-namespace is auto-discovered with no panel-provider change.
- **Operated-Clubs read with NO `Club` import:** `fn (Producer $record): string => $record->clubs->pluck('display_name')->implode(', ')` — keeps the resource's cross-module surface at exactly `{Producer}`; phpstan-clean (`Collection<int,Club>::pluck → Collection<int,mixed>::implode → string`). The translatable description renders `fn (Producer $record): ?string => $record->description?->resolve(app()->getLocale())` (mirror catalog `tasting_notes`).
- **Create write-through (§1+§2 confirmed):** `CreateProducer extends OperatorConsoleCreateRecord`; page/action class-name collision → `use App\Modules\Parties\Actions\CreateProducer as CreateProducerAction` (D6). `createViaAction` narrows `array<string,mixed>` (string guards → `InvalidArgumentException` = LogicException, propagates past the base's `RuntimeException` catch), wraps `description` as `TranslatableText::of(['en' => $description])` when present else `null`, calls `app(CreateProducerAction::class)->handle(name, region, country, appellation, description, website)`. `createRejectionField() => 'name'` is the inert safety net (Producer ships no create guard — D6). The Parties Actions take args (not the model), unlike catalog. **The create FORM lives on `ProducerResource::form()`, NOT the page** — Filament's `CreateRecord` inherits the Resource form (mirrors catalog `FormatResource`); the page supplies only `createViaAction`/`createRejectionField`. The form is `name`/`region`/`country` required + `appellation`/`website` optional `TextInput` + `description` optional `Textarea`, all labelled `operator_console.producer.fields.*`. It exposes **neither `status` nor `kyc_status`** — both FSMs are born by the action (`draft` / NULL) and advance only through the view-page actions (D6). Tested in `ProducerCreateConsoleTest` (3 tests): valid submit → `draft`+NULL+1 PII-free `ProducerCreated` (payload keys are exactly the structural set — no party email/phone), `assertFormFieldDoesNotExist('status'|'kyc_status')`, two same-name Producers both succeed.
- **View-page lifecycle (§3 confirmed) — the non-catalog View page is the trait-reuse TEMPLATE the rest of Parties (Club/Agreement/Customer) reuses.** `ViewProducer extends \Filament\Resources\Pages\ViewRecord` + `use SurfacesDomainActions` (NOT the catalog `OperatorConsoleViewRecord`, whose five governance verbs are hard-coded — D1); it defines `i18nKey() => 'producer'` and assembles its OWN verbs in `getHeaderActions(): array`. Each verb is `$this->lifecycleAction(verb, successKey, invoke)` — **form-less, no `confirmationKey`** (Producer activation is KYC-gated, not Creator→Reviewer→Approver SoD, so no second-actor modal — D3). The invoke closure has the uniform `fn (Model $record, string $notes)` signature (`$notes` unused — Pint/phpstan-clean) and — because Parties Actions take `int $id`, not the model (the one shape difference vs catalog) — passes `$this->recordOf(Producer::class, $record)->id` (D4). Rejections (`IllegalProducerTransition` etc., all `RuntimeException`) surface as the `notifications.action_failed` danger title via the trait's `surfaceLifecycleOutcome` catch — name domain exceptions in PROSE backticks, never `{@see}` (Pint's `fully_qualified_strict_types` would re-add the forbidden `Parties\Exceptions` import — D5/lessons.md). **Pint DOES add a docblock `{@see}` import for SAME-module classes (`OperatorConsoleViewRecord`) — intra-OperatorPanel, NOT a boundary violation, so let it stand** (the rule polices only cross-module `{Models, Actions}`-violating imports). The retire→Club-sunset cascade is the domain action's; the console just invokes it. **§4 confirmed — the same recipe scales to a SECOND FSM on one page:** the four KYC verbs (`requireKyc`→`kyc_required`, `waiveKyc`→`kyc_waived`, `verifyKyc`→`kyc_verified`, `rejectKyc`→`kyc_rejected`) APPEND to the same `getHeaderActions()` array, form-less, each `app(<Action>::class)->handle($this->recordOf(Producer::class,$r)->id)`. KYC is AUDIT-ONLY (the actions record NO domain event and place NO Hold) and SEPARATE from the status FSM (a KYC verb never moves `status`); rejections throw `IllegalKycTransition` (also a `RuntimeException`) so the SAME base-type catch surfaces `action_failed` — no extra wiring. **Verb id ≠ catalog verb id** (`rejectKyc` ≠ `reject`), so §3's `assertActionDoesNotExist('reject')` scope-guard survives the append unchanged.
- **Console lifecycle test recipe (mirrors catalog `FormatLifecycleConsoleTest`):** `uses(DatabaseMigrations::class)` (the action opens its own real transaction → recorder commits for real; factories bypass the actions so record NO event — the only events are the console's). Drive verbs with `Livewire::test(ViewProducer::class, ['record' => $producer->id])->callAction('activate')->assertNotified((string) __('operator_console.producer.notifications.activated'))`; a domain rejection is caught by the trait (never re-thrown) so `callAction` completes and you assert the `action_failed` title. **PG17 (the §6 run covers this folder): `actor_id`/`causation_id` are `int|null` and UNCAST → PG returns numeric strings, so assert them with loose `toEqual`; `entity_id`/`correlation_id` are strings → strict `toBe` is safe.** Scope-guards: `assertActionExists('activate', fn (Action $a) => ! $a->isConfirmationRequired())` (no SoD affordance) + `assertActionDoesNotExist('submit'/'reject'/'reopen'/'retireCascade')`.
- **i18n incremental fill:** the `operator_console.producer.*` block grows per section. §1 added `label`/`plural_label` (EN; IT omits → per-key EN fallback), `columns.{name,region,country,status,kyc_status,version}`, `fields.{appellation,website,description,clubs}`, `actions.create`. §2 added `fields.{name,region,country}` (the required create inputs — re-using the same words as `columns.*`, like catalog). §3 added `actions.{activate,retire}` + `notifications.{activated,retired,action_failed}`. §4 adds the four KYC `actions.{require_kyc,waive_kyc,verify_kyc,reject_kyc}` + `notifications.{kyc_required,kyc_waived,kyc_verified,kyc_rejected}`. **§5 confirmed — the kit-key completeness recipe (the Parties i18n-guard template Club/Agreement/Customer reuse):** one `ProducerConsoleI18nTest` with five guards — (a) EN baseline `Lang::has("operator_console.<entity>.{$suffix}", 'en', false)` over the kit-contract suffix list (`label`/`plural_label` + `columns.*` + per-verb `actions.<Str::snake(verb)>` + `notifications.<successKey>` + `notifications.action_failed` — the concatenated keys invisible to a source scan, so enumeration is the ONLY guard); (b) IT rendering over the kit-minus-fallback set (`array_diff(kitKeys, ['label','plural_label'])`) asserting authored-in-`it` AND `__($key) !== trans($key,[],'en')`; (c) per-key EN fallback for the English-invariant `label`/`plural_label`; (d) IT ⊆ EN over the `<entity>.*` dotted block; (e) the suite-wide `scanOperatorConsoleHardcodedSinks` reused behind a `function_exists` guard, scoped to `<Entity>Resource*`. **Reuse, never redeclare, the scanner** (declared top-level in `ProductMasterConsoleI18nTest`) — and verify with `--filter=<TestName>`/full suite, NOT a bare file path (a path load misses the helper → the guard reds; lessons.md).

---

## [2026-06-20 21:57] — 1.1 / 1.2 / 1.3 Producer read-only Resource + list + resource i18n
- **What:** the FIRST Parties (Module K) operator console — the read surface over the shipped Producer backend, built as the non-catalog trait-reuse pattern (ADR 2026-06-20). `ProducerResource extends OperatorConsoleResource` read-binding `Parties\Models\Producer` (own `status` + nullable `kyc_status` badge columns — NOT `lifecycleStateColumn()`; list name/region/country/status/kyc_status/version; infolist identity attrs + translatable description + operated-Clubs read); `ListProducers` (header create-LINK); `operator_console.producer.*` EN (full) + IT (per-key; label/plural_label → EN fallback). To keep the panel booting under the `getPages()` coupling, the section also created `CreateProducer` (real `createViaAction` → `CreateProducerAction`; form + create test deferred to §2) and a bare read-only `ViewProducer` (trait + lifecycle/KYC actions deferred to §3/§4).
- **Files changed (new):** `app/Modules/OperatorPanel/Filament/Resources/Parties/ProducerResource.php` + `…/ProducerResource/Pages/{ListProducers,CreateProducer,ViewProducer}.php`; `tests/Feature/Modules/OperatorPanel/Parties/ProducerResourceTest.php`. **(modified):** `lang/{en,it}/operator_console.php` (+`producer` block). Plus the change's ADR `decisions/2026-06-20-operator-console-non-catalog-lifecycle-trait-reuse.md` + `decisions/INDEX.md` (authored with the change; committed here with the first implementation). No migration/composer/protected-file change.
- **Quality loop:** green — Pint ✓; filtered ✓ (`ProducerResourceTest` 5/5, 28 assertions); full suite ✓ **1143/1143** (6470 assertions, SQLite — +5 vs the 1138 `main` baseline); phpstan ✓ 0 (the `NoEloquentWriteInOperatorPanelRule` analysed the new Parties Filament classes — green proves no Eloquent write); pint --test ✓. `openspec validate operator-console-parties-producer --strict` ✓. composer diff vs `main` empty; no migrations; no protected files (only app code under `Filament/` + lang + tests + the change's ADR/INDEX).
- **Acceptance walked (§1):** seeded Producer renders in the list with its `status` (`active`) and `kyc_status` (`verified`) values, and a never-screened Producer (NULL `kyc_status`) renders with a blank KYC badge + `draft` status ✓; `ProducerResource::getPages()` keys `toEqualCanonicalizing(['index','create','view'])` ✓; table exposes no edit/delete/bulk action, create is a header navigation link to the write-through page (`assertActionHasUrl`) ✓; the view page renders the read-only identity attrs + the translatable description + the two operated Clubs (`Producer::clubs()`) ✓; EN `operator_console.producer.*` resolves, IT `columns.name` = `Nome` with `label` falling back to EN `Producer` (DEC-127) ✓; arch/boundary green — `ModuleBoundariesTest` (full suite) + `NoEloquentWriteInOperatorPanelRule` (phpstan) confirm the console imports only `Parties\Models\Producer` (Resource) + `Parties\Actions\CreateProducer` (Create page), the `{Models, Actions}` carve-out ✓.
- **Learnings for future iterations:**
  - The recipe + the `getPages()` coupling + the no-Club-import operated-clubs read + the create write-through shape are now in **Codebase Patterns** (top) — read them before §2. **Next (§2 — task 2.1/2.2):** add `ProducerResource::form()` (name/region/country required `TextInput`; appellation/website optional `TextInput`; description optional `Textarea`) + the create-form field labels to `lang/{en,it}/operator_console.producer.fields.*` + `ProducerCreateConsoleTest` (valid submit → `draft` + 1 `ProducerCreated`, PII-free, `actor_role: newco_ops`; form exposes no status/kyc field; two same-name Producers both succeed; no `$model->save()`). `CreateProducer` already exists with the real `createViaAction` — §2 wires the form + lang + test and flips 2.1/2.2.
  - `ViewProducer` is currently a bare `ViewRecord`; **§3 (task 3.1)** rewrites it to `use SurfacesDomainActions` + `i18nKey() => 'producer'` + `getHeaderActions()` with the form-less `activate`/`retire` `lifecycleAction`s (D3/D4 — no `confirmationKey`, no SoD affordance; `app(ActivateProducer::class)->handle($this->recordOf(Producer::class,$r)->id)`), and **§4 (task 4.1)** appends the four KYC verbs. Their notification/action lang keys land then; the §5 kit-key completeness test enumerates the full contract afterward.
---

## [2026-06-20 22:20] — 3.1 / 3.2 Producer status lifecycle (activate + retire on ViewProducer)
- **What:** rewrote `ViewProducer` from the bare read-only `ViewRecord` into the non-catalog lifecycle View page (ADR 2026-06-20, the trait-reuse template the rest of Parties reuses). It now `extends \Filament\Resources\Pages\ViewRecord` + `use SurfacesDomainActions`, defines `i18nKey() => 'producer'`, and assembles its OWN `getHeaderActions()` with the two **form-less, no-confirmation** status verbs — `activate`→`activated`→`app(ActivateProducer::class)->handle($this->recordOf(Producer::class,$r)->id)` and `retire`→`retired`→`RetireProducer` (D1/D3/D4). It does NOT extend the catalog `OperatorConsoleViewRecord` (whose five governance verbs don't fit Producer's `draft→active→retired`). Rejections surface as `notifications.action_failed` via the trait's `RuntimeException` catch (D5 — exception named in prose backticks; Pint added only a same-module `OperatorConsoleViewRecord` docblock import, not a boundary break). Retire's Club-sunset cascade is the domain action's; the console just invokes it.
- **Files changed (modified):** `app/Modules/OperatorPanel/Filament/Resources/Parties/ProducerResource/Pages/ViewProducer.php` (full rewrite); `lang/en/operator_console.php` + `lang/it/operator_console.php` (+`producer.actions.{activate,retire}` + `producer.notifications.{activated,retired,action_failed}`). **(new):** `tests/Feature/Modules/OperatorPanel/Parties/ProducerLifecycleConsoleTest.php` (8 tests / 85 assertions). No migration/composer/protected-file change.
- **Quality loop:** green — Pint ✓ (auto-added the same-module `{@see}` import); filtered ✓ (`ProducerLifecycleConsoleTest` 8/8, 85 assertions); full suite ✓ **1154/1154** (6596 assertions, SQLite — +8 vs the §2 1146 baseline); phpstan ✓ 0 (the no-Eloquent-write rule analysed the new `getHeaderActions()` — green proves the verbs route through the actions, no `$status` write); pint --test ✓. `openspec validate operator-console-parties-producer --strict` ✓. composer diff vs `main` empty; only `ViewProducer` + the two lang files + the new test touched.
- **Acceptance walked (§3):** activate a `draft` Producer with `kyc_status` NULL/`verified`/`not_required` → `active` + exactly one `ProducerActivated` (`actor_role` NewcoOps, `actor_id` = the acting operator) — the 3-case dataset ✓; retire an `active` Producer → `retired` + one `ProducerRetired` (same envelope) ✓; **cascade**: a Producer operating two `active` Clubs + one `closed` Club, retired THROUGH THE CONSOLE → both `active` Clubs → `sunset` with one `ClubSunset` each carrying the `ProducerRetired` event id as `causation_id` (loose `toEqual` — PG numeric string) + sharing its `correlation_id`, the `closed` Club unchanged ✓; out-of-state activate (from `active`) / retire (from `draft`) → `action_failed` danger notification, state unchanged + `DomainEvent::count() === 0` ✓; **scope-guards**: `assertActionExists('activate'/'retire')`, activate carries NO second-actor confirmation (`! isConfirmationRequired()`), `assertActionDoesNotExist('submit'/'reject'/'reopen'/'retireCascade')` ✓.
- **Learnings for future iterations:**
  - The view-page lifecycle recipe + the console-lifecycle test recipe (DatabaseMigrations, `callAction`, loose `toEqual` for the uncast `actor_id`/`causation_id` bigints on PG17) are consolidated in **Codebase Patterns** (top) — read them before §4. **Next (§4 — task 4.1/4.2):** APPEND the four KYC verbs to the same `ViewProducer::getHeaderActions()` array via `lifecycleAction` (form-less): `requireKyc`→`kyc_required`→`RequireProducerKyc`; `waiveKyc`→`kyc_waived`→`WaiveProducerKyc`; `verifyKyc`→`kyc_verified`→`RecordProducerKycVerified`; `rejectKyc`→`kyc_rejected`→`RecordProducerKycRejected` (each `->handle($this->recordOf(Producer::class,$r)->id)`). Add `actions.{require_kyc,waive_kyc,verify_kyc,reject_kyc}` + `notifications.{kyc_required,kyc_waived,kyc_verified,kyc_rejected}` to `lang/{en,it}`. Test `ProducerKycConsoleTest`: each KYC verb moves `kyc_status`, leaves `status` unchanged, records NO domain event (audit-only) and places no Hold; illegal KYC transition → `action_failed`; the KYC↔activation gate end-to-end (pending blocks activate → verify → activate succeeds).
  - **Verb id ≠ KYC verb id:** `assertActionDoesNotExist('reject')` stays true after §4 because the KYC reject verb is `rejectKyc` (distinct id) — §3's scope-guard test survives the §4 append unchanged.
---

## [2026-06-20 22:31] — 4.1 / 4.2 Producer KYC management (require / waive / verify / reject on ViewProducer)
- **What:** APPENDED the four KYC verbs to the existing `ViewProducer::getHeaderActions()` array — the same `SurfacesDomainActions::lifecycleAction()` recipe §3 used for the status verbs, proving it scales to a SECOND FSM on one page. `requireKyc`→`kyc_required`→`RequireProducerKyc` (`not_required`/NULL → `pending`); `waiveKyc`→`kyc_waived`→`WaiveProducerKyc` (any outstanding → `not_required`); `verifyKyc`→`kyc_verified`→`RecordProducerKycVerified` (`pending` → `verified`); `rejectKyc`→`kyc_rejected`→`RecordProducerKycRejected` (`pending` → `rejected`). All form-less, no confirmation (D3), each `app(...)->handle($this->recordOf(Producer::class,$r)->id)` (D4). KYC is AUDIT-ONLY (no domain event, no Hold) and SEPARATE from the status FSM; rejections throw `IllegalKycTransition` (a `RuntimeException`) → the trait's same base-type catch surfaces `action_failed` (D5) — zero extra wiring. Updated the class/method docblocks to describe the full six-verb set.
- **Files changed (modified):** `app/Modules/OperatorPanel/Filament/Resources/Parties/ProducerResource/Pages/ViewProducer.php` (+4 Action imports, +4 array entries, docblocks); `lang/en/operator_console.php` + `lang/it/operator_console.php` (+`producer.actions.{require_kyc,waive_kyc,verify_kyc,reject_kyc}` + `producer.notifications.{kyc_required,kyc_waived,kyc_verified,kyc_rejected}`). **(new):** `tests/Feature/Modules/OperatorPanel/Parties/ProducerKycConsoleTest.php` (13 tests / 124 assertions). No migration/composer/protected-file change.
- **Quality loop:** green — Pint ✓ (no `{@see}` import re-added — exceptions named in prose, D5); filtered ✓ (`ProducerKycConsoleTest` 13/13, 124 assertions); full suite ✓ **1167/1167** (6720 assertions, SQLite — +13 vs the §3 1154 baseline); phpstan ✓ 0 (the no-Eloquent-write rule analysed the grown `getHeaderActions()` — green proves the four KYC verbs route through the actions, no `$kyc_status` write); pint --test ✓. `openspec validate operator-console-parties-producer --strict` ✓. composer diff vs `main` empty; only `ViewProducer` + the two lang files + the new test touched.
- **Acceptance walked (§4):** require (`not_required`/NULL→`pending`), waive (`pending`/`rejected`/`verified`→`not_required`), verify (`pending`→`verified`), reject (`pending`→`rejected`) — each moves `kyc_status`, leaves `status` on `draft`, the `domain_events` count is unchanged (== baseline, audit-only), and `parties_holds` count stays 0 ✓; an illegal KYC transition (verify from `not_required`/`verified`/NULL, reject from `rejected`, waive from `not_required`) → `action_failed` danger notification with `kyc_status` + `status` + event-log unchanged ✓; **KYC↔activation gate end-to-end** — a `draft` Producer with `kyc_status` `pending`: activate blocked (`action_failed`, stays `draft`, 0 `ProducerActivated`) → verifyKyc (→ `verified`, still 0 `ProducerActivated`) → activate succeeds (`active` + exactly 1 `ProducerActivated`) ✓.
- **Learnings for future iterations:**
  - The recipe scaling to a second FSM (audit-only KYC verbs on the same page, shared `RuntimeException` catch, verb-id distinctness preserving §3's scope guard) is consolidated in **Codebase Patterns** (the view-page lifecycle bullet). **Next (§5 — task 5.1):** add `ProducerConsoleI18nTest` enumerating the kit-contract keys this console resolves by string concatenation (resource `label`/`plural_label`/`columns.{status,kyc_status,version}`; the six `actions.{activate,retire,require_kyc,waive_kyc,verify_kyc,reject_kyc}`; `notifications.{activated,retired,kyc_required,kyc_waived,kyc_verified,kyc_rejected,action_failed}`) via `Lang::has("operator_console.producer.{$suffix}", 'en', false)`, plus an IT-differs dataset and the suite-wide `scanOperatorConsoleHardcodedSinks` (with a `function_exists` guard) over `…/Resources/Parties/ProducerResource*`. Then §6 (PG17 closing-chain). The full kit-contract key list now exists in EN+IT — §5 only enumerates/asserts it.
---

## [2026-06-20 22:40] — 5.1 i18n kit-key completeness (EN/IT)
- **What:** added the console's capability-close i18n guard `ProducerConsoleI18nTest` — the only guard that catches a dropped *kit-contract* key, because the kit resolves those keys by string concatenation off `i18nKey()` in the base classes (so they never appear as literals in the per-entity source, and the lifecycle/create tests assert behaviour not label text — both pass even with a missing key, since `__()` renders the raw key). Five guards, mirroring the predecessors' ProductMaster/Spine tests: (1) EN baseline — every one of the 18 kit keys (`label`/`plural_label`, `columns.{status,kyc_status,version}`, the six `actions.*`, the seven `notifications.*`) is authored in `en` via `Lang::has(…, 'en', false)`; (2) IT rendering — the 16 IT-authored kit keys (kit minus the English-invariant `label`/`plural_label`) resolve to authored Italian distinct from EN; (3) per-key EN fallback — `label`/`plural_label` (the canonical term "Producer", CONTEXT.md) omitted from `it` yet still resolve to the EN value (DEC-127); (4) IT ⊆ EN over the `producer.*` block (a typo'd `it` key has no dangling fallback); (5) the suite-wide `scanOperatorConsoleHardcodedSinks` (reused from ProductMasterConsoleI18nTest behind a `function_exists` guard) finds zero hardcoded operator-facing literals across the Producer console source, scoped to `ProducerResource*`.
- **Files changed (new):** `tests/Feature/Modules/OperatorPanel/Parties/ProducerConsoleI18nTest.php` (38 tests / 77 assertions). No source/lang/migration/composer/protected-file change — §1–§4 already authored the full EN+IT key list, so §5 only enumerates and asserts it.
- **Quality loop:** green — Pint ✓; filtered ✓ (`--filter=ProducerConsoleI18nTest` 38/38, 77 assertions — see gotcha below on path-vs-filter); full suite ✓ **1205/1205** (6797 assertions, SQLite — +38 vs the §4 1167 baseline); phpstan ✓ 0; pint --test ✓. `openspec validate operator-console-parties-producer --strict` ✓. composer diff vs `main` empty; only the new test file touched.
- **Acceptance walked (§5):** the kit-contract enumeration is the exact 18-key set the task lists, each asserted with `Lang::has("operator_console.producer.{$suffix}", 'en', false)` ✓; non-vacuity proven OUT OF BAND (`Lang::has('…notifications.kyc_verified', 'en', false)` → true vs a `…_REMOVED` suffix → false, so a dropped key reds the test) ✓; the IT-differs dataset (16 keys) asserts authored-in-IT AND `__($key) !== trans($key, [], 'en')` ✓; the sink scan over `…/Resources/Parties/ProducerResource*` returns `[]` ✓.
- **Learnings for future iterations:**
  - **The i18n completeness recipe is now a Parties template** (consolidated in Codebase Patterns) — Club/Agreement/Customer reuse it: enumerate the kit-contract keys (resource `label`/`plural_label`/`columns.*` + per-verb `actions.*`/`notifications.*` + `notifications.action_failed`) via `Lang::has(…, 'en', false)`, derive the IT-differs dataset as kit-minus-fallback, scope the reused sink scan to `<Entity>Resource*`.
  - **Path-vs-`--filter` gotcha (cost one red):** running the test by FILE PATH (`vendor/bin/pest tests/…/ProducerConsoleI18nTest.php`) loads ONLY that file, so the shared `scanOperatorConsoleHardcodedSinks` (declared top-level in `ProductMasterConsoleI18nTest.php`) is absent → the `function_exists` guard fails by design. Verify these suite-wide-helper-reusing tests with `--filter=<TestName>` (or the full suite), which builds the suite from phpunit.xml and loads every file. The Spine test shares this property; it is the intended reuse pattern (do NOT redeclare the function — that would fatal on redeclaration in the full suite). Captured in lessons.md.
  - **Next (§6 — task 6.1):** the PG17 closing-chain `ProducerConsoleChainTest` driving a Producer through the console PAGES (`CreateProducer->create`, then `ViewProducer` `requireKyc`→`verifyKyc`→`activate`, seed two `active` Clubs, `retire`), asserting the emergent event set `toEqualCanonicalizing(['ProducerCreated','ProducerActivated','ProducerRetired','ClubSunset','ClubSunset'])` (KYC adds none), every event `module==='parties'` + `actor_role` NewcoOps + non-null `actor_id`, and the two `ClubSunset` carry the `ProducerRetired` id as `causation_id`. Green on SQLite AND PG17 (the docker postgres:17 invocation in hot.md / the task).
---

## [2026-06-20 22:08] — 2.1 / 2.2 Producer create surface (form + create-field i18n)
- **What:** wired the Producer write-through CREATE surface. §1 had already shipped the `CreateProducer` page with its real `createViaAction`/`createRejectionField`; §2 added the create FORM (which lives on `ProducerResource::form()`, inherited by the kit's `CreateRecord` — mirrors catalog `FormatResource`) and the create-field labels, then proved the whole flow. Form = `name`/`region`/`country` required `TextInput` + `appellation`/`website` optional `TextInput` + `description` optional `Textarea`, all `operator_console.producer.fields.*`. It exposes NEITHER `status` NOR `kyc_status`: a Producer is born `draft` with NULL KYC by the action, and both FSMs advance only through the §3/§4 view-page actions (design D6).
- **Files changed (modified):** `app/Modules/OperatorPanel/Filament/Resources/Parties/ProducerResource.php` (+`form()` + `Textarea`/`TextInput` imports); `lang/en/operator_console.php` + `lang/it/operator_console.php` (+`producer.fields.{name,region,country}`). **(new):** `tests/Feature/Modules/OperatorPanel/Parties/ProducerCreateConsoleTest.php` (3 tests / 41 assertions). No migration/composer/protected-file change.
- **Quality loop:** green — Pint ✓ (no reformat needed); filtered ✓ (`ProducerCreateConsoleTest` 3/3, 41 assertions); full suite ✓ **1146/1146** (6511 assertions, SQLite — +3 vs the §1 1143 baseline); phpstan ✓ 0 (the no-Eloquent-write rule analysed the new `form()` — green proves the create path still routes through the action, no `$model->save()`); pint --test ✓. `openspec validate operator-console-parties-producer --strict` ✓. composer diff vs `main` empty; no protected files (only `ProducerResource` + lang + the new test).
- **Acceptance walked (§2):** valid console submit (name/region/country/appellation/website/description) → a Producer `draft` with `kyc_status` NULL, the description captured as English-baseline `TranslatableText`, + exactly one `ProducerCreated` carrying the operator envelope (`actor_role` `NewcoOps`, `actor_id` = the acting operator, `entity_type` `Producer`, `entity_id` = the new id), the payload a pure structural-identity snapshot (keys exactly `producer_id,name,region,appellation,country,status` — no party email/phone) ✓; the form exposes name/region/country/appellation/description/website and `assertFormFieldDoesNotExist('status')`/`('kyc_status')` ✓; two same-name Producers both succeed (2 rows, 2 `ProducerCreated` — Producer ships no create dedup, D6) ✓; no `$model->save()` (phpstan no-Eloquent-write rule green) ✓.
- **Learnings for future iterations:**
  - The create write-through recipe (form on the Resource, page supplies only `createViaAction`/`createRejectionField`, no `status`/`kyc_status` inputs) is consolidated in **Codebase Patterns** (top) — the Club/Agreement/Customer create surfaces reuse it. **Next (§3 — task 3.1/3.2):** rewrite `ViewProducer` from the bare `ViewRecord` to `extends \Filament\Resources\Pages\ViewRecord` + `use SurfacesDomainActions` + `i18nKey() => 'producer'` + `getHeaderActions()` returning the two form-less status `lifecycleAction`s (`activate`→`activated`→`app(ActivateProducer::class)->handle($this->recordOf(Producer::class,$r)->id)`; `retire`→`retired`→`RetireProducer`) — D3/D4: no `confirmationKey`, no SoD affordance, uniform `(Model $record, string $notes)` closure. Add `actions.{activate,retire}` + `notifications.{activated,retired,action_failed}` to `lang/{en,it}`. Test `ProducerLifecycleConsoleTest` (activate draft→active, retire active→retired incl. the Club-sunset cascade, out-of-state → `action_failed`, scope-guards `assertActionDoesNotExist('submit'/'reject'/'reopen')`).
---
