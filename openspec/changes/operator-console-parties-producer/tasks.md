# Tasks — operator-console-parties-producer

> Pure operator surface over shipped Parties domain Actions. No new domain code, no migration, no composer dep. Read the predecessor `## Codebase Patterns` (`openspec/changes/archive/2026-06-20-operator-console-catalog-spine/progress.md`) and this change's `design.md` (D1–D8) before starting. Every task ends with the quality loop green: `vendor/bin/pint`, `php -d memory_limit=-1 vendor/bin/pest` (filtered + full), `php -d memory_limit=-1 vendor/bin/phpstan analyse`, `vendor/bin/pint --test`, `openspec validate operator-console-parties-producer --strict`, composer diff vs main empty, no protected-file edits. The ADR (`decisions/2026-06-20-operator-console-non-catalog-lifecycle-trait-reuse.md`) is authored with this change — do not re-decide it; implement to it.

## 1. Producer read-only Resource + list + resource i18n

- [x] 1.1 Create `app/Modules/OperatorPanel/Filament/Resources/Parties/ProducerResource.php` — `extends OperatorConsoleResource` (reuse `getModelLabel`/`getPluralModelLabel` off `static i18nKey(): string => 'producer'`, and `versionColumn()`). Read-bind `\App\Modules\Parties\Models\Producer`. Define a **`status`** badge `TextColumn` (render `ProducerStatus->value` via `getStateUsing` + `instanceof BackedEnum`, no `Parties\Enums` import) and a **`kyc_status`** badge column (nullable → `''` when NULL). List columns: name · region · country · status · kyc_status · `versionColumn()`. Infolist: name/region/country/appellation/website + the translatable `description` + the operated-Clubs read (`Producer::clubs()`). Add NO create/edit/delete mutating action. `getPages()` → index/create/view. Do **NOT** call `lifecycleStateColumn()` (D2). (D7)
- [x] 1.2 Create `…/ProducerResource/Pages/ListProducers.php` — the header create-LINK page (copy the shape from `ListFormats`, not a `CreateAction`).
- [x] 1.3 Add the `operator_console.producer.*` resource/column copy to `lang/en/operator_console.php` (full) and `lang/it/operator_console.php` (per-key; `label`/`plural_label` may fall back to EN): `label`, `plural_label`, `columns.{status,kyc_status,version}`, and the infolist field labels.
- **Tests** (`tests/Feature/Modules/OperatorPanel/Parties/ProducerResourceTest.php`): a seeded Producer renders in the list with its `status` and `kyc_status` values; `ProducerResource::getPages()` keys == `['index','create','view']`; the table exposes no edit/delete/bulk action. Arch/boundary: `NoEloquentWriteInOperatorPanelRule` (phpstan) + `ModuleBoundariesTest` stay green over the new class (imports only `Parties\Models\Producer`).

## 2. Producer create surface

- [x] 2.1 Create `…/ProducerResource/Pages/CreateProducer.php` — `extends OperatorConsoleCreateRecord`; alias the action import `use App\Modules\Parties\Actions\CreateProducer as CreateProducerAction;` (page/action name collision, D6). Form: `name`/`region`/`country` required `TextInput`; `appellation`/`website` optional `TextInput`; `description` optional `Textarea`. `createViaAction(array $data): Model` narrows the payload and calls `app(CreateProducerAction::class)->handle(...)`, wrapping `description` as `TranslatableText::of(...)` when present else `null`. `createRejectionField(): string => 'name'` (inert safety net — no create guard, so **no** `UniqueConstraintViolationException` catch).
- [x] 2.2 Add the create-field labels to `lang/{en,it}/operator_console.producer.fields.*`.
- **Tests** (`ProducerCreateConsoleTest.php`): submitting a valid Producer invokes the action → a Producer in `draft` with `kyc_status` NULL + exactly one `ProducerCreated` event (`actor_role: newco_ops`, `actor_id` = the acting operator, `entity_type` `Producer`, payload free of name/email/phone of any party); the form exposes name/region/country/appellation/description/website and **no** `status`/`kyc_status` field (`assertFormFieldDoesNotExist`); creating two Producers with the same name both succeed (2 rows, 2 `ProducerCreated`). No `$model->save()` (phpstan rule green).

## 3. Producer status lifecycle — activate + retire (ViewProducer)

- [ ] 3.1 Create `…/ProducerResource/Pages/ViewProducer.php` — `extends \Filament\Resources\Pages\ViewRecord` and `use SurfacesDomainActions` (NOT `OperatorConsoleViewRecord`, D1). `i18nKey(): string => 'producer'`. `getHeaderActions()` returns the two status actions via `lifecycleAction` — **form-less, no `confirmationKey`** (no SoD affordance, D3): `activate`→`activated`→`fn (Model $r) => app(ActivateProducer::class)->handle($this->recordOf(Producer::class,$r)->id)`; `retire`→`retired`→`app(RetireProducer::class)->handle(...->id)` (D4). Give each closure the uniform `(Model $record, string $notes)` signature (`$notes` unused).
- [ ] 3.2 Add `operator_console.producer.actions.{activate,retire}`, `notifications.{activated,retired,action_failed}` to `lang/{en,it}`.
- **Tests** (`ProducerLifecycleConsoleTest.php`): activate a `draft` Producer with `kyc_status` NULL/`verified`/`not_required` → `active` + exactly one `ProducerActivated` (`actor_role: newco_ops`, `actor_id` = operator); retire an `active` Producer → `retired` + one `ProducerRetired`; **cascade**: a Producer operating two `active` Clubs + one `closed` Club, retired through the console → both `active` Clubs become `sunset` with one `ClubSunset` each carrying the `ProducerRetired` event's id as `causation_id`, the `closed` Club unchanged; out-of-state activate (not `draft`) / retire (not `active`) → `action_failed` danger notification, state + event log unchanged; **scope-guards**: `assertActionExists('activate'/'retire')`, `assertActionDoesNotExist('submit'/'reject'/'reopen')`, and the activate action carries no "second actor" confirmation affordance and there is no separate cascade-retire action.

## 4. Producer KYC management — require / waive / verify / reject (ViewProducer)

- [ ] 4.1 Append the four KYC actions to `ViewProducer::getHeaderActions()` via `lifecycleAction` (form-less): `requireKyc`→`kyc_required`→`RequireProducerKyc`; `waiveKyc`→`kyc_waived`→`WaiveProducerKyc`; `verifyKyc`→`kyc_verified`→`RecordProducerKycVerified`; `rejectKyc`→`kyc_rejected`→`RecordProducerKycRejected` (each `->handle($this->recordOf(Producer::class,$r)->id)`).
- [ ] 4.2 Add `operator_console.producer.actions.{require_kyc,waive_kyc,verify_kyc,reject_kyc}` + `notifications.{kyc_required,kyc_waived,kyc_verified,kyc_rejected}` to `lang/{en,it}`.
- **Tests** (`ProducerKycConsoleTest.php`): require (`not_required`/NULL→`pending`), waive (`pending`/`rejected`/`verified`→`not_required`), verify (`pending`→`verified`), reject (`pending`→`rejected`) — for each: the `kyc_status` moves, the Producer's `status` is unchanged, the `domain_events` count is **unchanged** (audit-only, no event), and no Hold row is created; an illegal KYC transition (verify/reject from a non-`pending` state, or waive from `not_required`) → `action_failed` danger notification, `kyc_status` unchanged; **KYC↔activation gate end-to-end**: a `draft` Producer set to `kyc_status` `pending` → activate is blocked (`action_failed`, stays `draft`, 0 `ProducerActivated`) → verify → activate now succeeds (`active` + 1 `ProducerActivated`).

## 5. i18n kit-key completeness (EN/IT)

- [ ] 5.1 Add `ProducerConsoleI18nTest.php` enumerating the **kit-contract** keys this console resolves by string concatenation (so they never appear as literals in source): resource `label`/`plural_label`/`columns.{status,kyc_status,version}`; per the six verbs `actions.{activate,retire,require_kyc,waive_kyc,verify_kyc,reject_kyc}`; `notifications.{activated,retired,kyc_required,kyc_waived,kyc_verified,kyc_rejected,action_failed}`. Assert `Lang::has("operator_console.producer.{$suffix}", 'en', false)` for each. Add an IT-differs dataset (keys whose `it` value differs from `en` — translated labels, not proper nouns). Reuse the predecessor's suite-wide `scanOperatorConsoleHardcodedSinks` with a `function_exists` guard to assert the Producer console source carries no hardcoded operator-facing string.
- **Tests**: the enumeration above is green (a deliberately removed key fails it); the sink scan finds zero hardcoded strings in `…/Resources/Parties/ProducerResource*`.

## 6. Full closing-chain integration test (PG17)

- [ ] 6.1 Add `ProducerConsoleChainTest.php` — one `it()` driving a Producer through the console **pages** (not the raw Actions): `Livewire::test(CreateProducer::class)->fillForm([...])->call('create')`, then `Livewire::test(ViewProducer::class, ['record' => $id])->callAction('requireKyc')` → `callAction('verifyKyc')` → `callAction('activate')`; seed the Producer operating two `active` Clubs (event-free, via `Club::factory()` / `SunsetClub` is not used here) then `callAction('retire')`. Assert: the emergent event set `DomainEvent::pluck('name')->all()` `->toEqualCanonicalizing(['ProducerCreated','ProducerActivated','ProducerRetired','ClubSunset','ClubSunset'])` (the KYC steps add no event); a `foreach` asserting every event `module === 'parties'`, `actor_role === ActorRole::NewcoOps`, `actor_id` non-null; a representative `actor_id` equals the acting operator (loose `toEqual` — PG returns a numeric string); the two `ClubSunset` events carry the `ProducerRetired` event id as `causation_id`.
- **Tests**: green on SQLite AND on PG17 (`DB_CONNECTION=pgsql DB_HOST=127.0.0.1 DB_PORT=55432 DB_DATABASE=newco_test DB_USERNAME=newco DB_PASSWORD=newco php -d memory_limit=-1 vendor/bin/pest tests/Feature/Modules/OperatorPanel/Parties`); phpstan 0; pint clean; `openspec validate operator-console-parties-producer --strict` OK; composer diff vs main empty.
