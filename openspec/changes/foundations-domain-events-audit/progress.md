# Progress — foundations-domain-events-audit

## Codebase Patterns
(consolidated reusable patterns — read first each iteration)

- **Platform substrate code lives under `App\Platform`** (design D1). Enums sit in `App\Platform\Events` (`app/Platform/Events/`). PSR-4 `App\` → `app/` already covers it — no composer change.
- **Enum convention** (mirrors `App\Modules\Module`): string-backed, PascalCase case name → snake_case backing value; a `Module`-density class docblock citing the design/spec source. Enum tests live in `tests/Unit/Platform/`, assert the full case→value map verbatim with `->toBe([...])` (order-sensitive), and add one `from('<bad>')` → `ValueError` failure case. Mirror `tests/Unit/Modules/ModuleTest.php`.
- **Boundary-law amendment for a NEW platform root** (the only legitimate edit to the hardcoded `$platformNamespaces` array): add the root string to `$platformNamespaces` in `tests/Architecture/ModuleBoundariesTest.php` with a design-citing comment, then **prove non-vacuity with a red-proof** — a temp class under the new root type-referencing a module symbol must turn the suite RED naming the exact pair, then removal returns it GREEN with a clean `git status`. This is mandatory (lessons.md 2026-06-12; design Risk "vacuously-green arch tests"). F1 3/3 adds `App\Platform\Money` etc. *under the same already-listed `App\Platform` root* — no further amendment needed once `App\Platform` is in the array.
- **pest-plugin-arch RED failure message format**: `Expecting '<source>' not to use '<target>'.` — useful to grep for when asserting a red-proof named the right pair.
- **Test runner emits JSON** (one line per run: `{"tool":"pest","result":...,"tests":N,...}`). A failure includes a `failures[]` array with `test`, `file`, `line`, `message`. Tail/grep accordingly; don't expect Pest's pretty TTY output.
- **Per-file `RefreshDatabase`**: the global Pest binding stays commented — DB-touching test files opt in with `uses(RefreshDatabase::class)` per-file (tasks.md standing rule). 1.1 needed no DB so EnumsTest is a pure unit test.
- **Postgres-truthful migration idiom** (design D2): use the fluent column types as-is — on the SQLite dev/test lane they fall back behavior-preservingly (`jsonb()`→`text`, `timestampTz()`→datetime `text`, `uuid()`→`varchar`; all identical under the Eloquent array/datetime casts). A constraint only PostgreSQL can express (e.g. an `actor_role` CHECK) goes in an `if (DB::getDriverName() === 'pgsql')` branch via `DB::statement(...)` **after** `Schema::create()`, with the SQLite fallback (NOT NULL + the enum cast) documented inline. **Derive the CHECK's enumerated values from the enum's `::cases()`** so the DB constraint can never drift from the PHP enum. The PG branch is unexercised on SQLite — the `pgsql` CI lane (task 5.2 / first human push) is its proof, by design D8.
- **Portable schema assertions**: `Schema::hasColumns($t, [...])` → bool for column presence; `Schema::hasIndex($t, [col, col, ...], 'unique'|null)` matches on the **column array** (order-sensitive — verified in `Builder::hasIndex`, compares `$value['columns'] === $index`), so you assert composite/unique indexes **without** knowing Laravel's generated index name. Pair structural checks with behavioral ones: a complete-row `DB::table()->insert()` for happy path; `->throws(QueryException::class)` for NOT NULL / unique violations (never engine-specific SQLSTATEs); a self-referencing FK is proven by inserting a child row whose FK column points at a parent's `id`. A row-builder `function fooRow(array $overrides = [])` keeps the complete-valid-row DRY while each test drops/changes exactly one field.
- **PHPStan level-max in test files** (two recurring traps): (1) plain helper functions need iterable-value types — `@param array<string, mixed>` + `@return array<string, mixed>`; (2) never cast a DB-query result to a scalar — `->first()` is `stdClass|null` (a prior `expect()->not->toBeNull()` does NOT narrow it for PHPStan) and `->value()` is `mixed`, so `(int) $row->col` trips `cast.int`. Assert DB values with Pest `->toEqual(...)` (loose ==, accepts `mixed` with no cast) instead of casting.
- **Partial index = raw `DB::statement('CREATE INDEX <name> ON <t> (<col>) WHERE <predicate>')`** (design D2). The installed Laravel Blueprint has NO fluent partial-index predicate — `IndexDefinition` exposes only `algorithm/deferrable/initiallyImmediate/language/lock/nullsNotDistinct/online`, and both grammars' `compileIndex` emit a plain `create index … (cols)` with no `WHERE` (verified in vendor 2026-06-12). The raw `CREATE INDEX … WHERE` syntax is **identical and valid on BOTH SQLite and PostgreSQL**, so one statement serves both engines (unlike the PG-only `actor_role` CHECK). Give the index an **explicit stable name** and derive any literal in the predicate from the enum (`DeliveryStatus::Pending->value`) so it can't drift. **Assert existence by NAME**: `Schema::hasIndex($t, '<name>')` — `Builder::hasIndex` matches `$value['name'] === $index || $value['columns'] === $index`, so a name string works and is portable (both engines' `getIndexes()` return the index by name). Caveat: `getIndexes()` surfaces name+columns but **NOT the partial predicate** on either engine, so the name-existence assertion is the portable proof; partiality itself is carried by the migration DDL (a malformed predicate aborts the migration → every test in the file errors, so green ⇒ valid partial DDL on the running engine). The pgsql lane (5.2) re-proves the PG side.
- **Cross-engine immutability triggers** (design D7) — raw DDL via **`DB::statement`, NOT `DB::unprepared`**: Larastan types `Connection::unprepared()`'s `$query` as `literal-string` (an injection guard), so an interpolated clause (`"… ({$changed}) …"`, where `$changed` is `implode(array_map(...))`) trips `argument.type` at PHPStan max. `Connection::statement($query, $bindings=[])` is **not** literal-string-constrained (it carries a bindings param) — exactly why the table migrations (000001/000002 `actor_role` CHECK, 000003 partial index) already use it for interpolated driver-branched DDL. Match them: one statement per call, **no trailing semicolon** (a single command per call dodges PG's "cannot insert multiple commands into a prepared statement"; the body's own `;` are safe — inside SQLite `BEGIN … END` or PG `$$…$$`). Verified (prepare+execute installs a SQLite `CREATE TRIGGER` with internal semicolons and it fires). Engine dialects: **SQLite** `CREATE TRIGGER … BEFORE UPDATE/DELETE … BEGIN SELECT RAISE(ABORT, '…immutable…'); END` (single-event triggers → two for an append-only table; `RAISE(ABORT)` reverts only the offending statement, not the wrapping txn, so a test can read the row back unchanged after catching); **PostgreSQL** a plpgsql `RETURNS trigger` function (`RAISE EXCEPTION '…immutable…'`; `\$\$` is the PHP-escaped `$$` dollar-quote) fired by one combined `BEFORE UPDATE OR DELETE … EXECUTE FUNCTION`. A **partial-immutability** table (allow only some columns to change — e.g. audit `before`/`after` for GDPR redaction) uses **ONE authoritative structural-column list** (every other column) driving both branches: SQLite `WHEN (OLD.c IS NOT NEW.c OR …)` (null-safe `IS NOT`), PG `IF (OLD.c IS DISTINCT FROM NEW.c OR …) THEN RAISE; END IF; RETURN NEW`. Tests assert **behaviour only** — `QueryException` whose message `toContain('immutable')` + row unchanged/exists afterward — **never SQLSTATEs**, so one test file proves both lanes; they ARE the red-paths (no separate red-proof). `event_deliveries` gets NO trigger (mutable by design) — pin that with a positive "update succeeds" test so the trigger scope can't silently over-extend.
- **Platform Eloquent models** (design D1/D2): `$table` explicit, `protected $guarded = []` (the recorders are the only writers — no request-driven mass assignment on a substrate model), `casts()` method (Laravel 11+ style, `@return array<string, string>`). The two append-only tables (`domain_events`/`audit_records`) set `public $timestamps = false` — the `occurred_at` envelope clock is the only timestamp and the migrations have NO created/updated columns, so leaving timestamps on would make `create()` write non-existent columns (it's a correctness requirement, not a preference); the mutable `event_deliveries` KEEPS timestamps. Cast vocabulary: enum column → the enum FQCN (`ActorRole::class`/`DeliveryStatus::class`) = the application-side value-set floor on BOTH engines — reading an out-of-enum value throws `ValueError` via `BackedEnum::from()`, **uncaught by Eloquent** (`HasAttributes::getEnumCaseFromValue` → `::from()`), which IS the failure-case test; `jsonb` → `'array'`; `timestampTz` → `'immutable_datetime'` (→ `Carbon\CarbonImmutable`). **PHPStan level-max model-property access**: Larastan scans `database/migrations` by default (`databaseMigrationsPath: []` → `database_path('migrations')`) and its `SchemaAggregator` understands `jsonb`/`uuid`/`foreignid`/`unsigned*integer`/`id`/`string`/`text`/`timestamps` but **NOT `timestamptz`** (singular), so `occurred_at`/`available_at` are NOT schema-inferred — add explicit `@property` annotations (Larastan maps `immutable_datetime`→`CarbonImmutable`, annotate to match so there's no drift). `Eloquent\Model::__get` means an un-inferred property never trips `property.notFound`, but annotate anyway so `expect($x->prop)->toBeInstanceOf(...)` is a typed check, not `mixed`. **Pint's `fully_qualified_strict_types` fixer manages docblock types + imports together**: write `@property \Carbon\CarbonImmutable`, Pint adds the `use` and shortens to `CarbonImmutable` (and `no_unused_imports` keeps a docblock-only import) — don't fight it. An uncast column reads back as the raw DB type (`audit_records.occurred_at` is left uncast per the task's explicit cast list → reads as `string`, annotate it so).
- **Pest helper functions are GLOBAL across all test files** (one shared namespace) → prefix per-file helpers to avoid a redeclaration fatal: `domainEventRow` (schema test), `immutabilityDomainEventRow` (immutability), `modelsDomainEventAttributes` (models) all build a valid `domain_events` row but must NOT share a name.
- **Provider-seam singleton** (3.2, design D4): a shared registry/service is a plain class with instance state, bound `$this->app->singleton(Foo::class)` in `AppServiceProvider::register()` (bindings go in `register()`, runs before any provider `boot()`). The container singleton is what makes module-provider `boot()` registrations and the consuming path see ONE instance. `singleton($class)` with no closure is valid (resolves the concrete via reflection; works when the ctor has no args).
- **Unit tests have NO Laravel app** — `tests/Pest.php` binds `Tests\TestCase` (which boots the app) only `->in('Feature')`; `tests/Unit/**` is plain `PHPUnit\Framework\TestCase`, so `app()`/`$this->app` are unavailable there. Split accordingly: pure-logic assertions → Unit (new up the SUT directly); anything that resolves from the container (singleton identity, binding persistence) → a Feature test. DB-touching Feature tests still opt into `uses(RefreshDatabase::class)` per-file; a container-only Feature test needs no DB trait.
- **`is_subclass_of($class, Interface::class)` is a PHPStan type-guard**: after a negated guard `if (! is_subclass_of($x, Foo::class)) { throw; }`, PHPStan level-max narrows the `string $x` to `class-string<Foo>`, so it can be stored into a `list<class-string<Foo>>` with no cast (verified 2026-06-12). It accepts an anonymous-class name (`$anon::class`) and returns `false` (no fatal) for a missing class — so ONE guard covers both "doesn't implement the contract" and "class doesn't exist" failure cases. Type the validated param `string` (not `class-string<Foo>`) so a negative test can pass a non-conforming class without a static-analysis error.
- **A single-case enum is a compile-time gate, not a runtime branch**: with `enum DeliveryMode { case Inline; }` you CANNOT write `if ($mode !== DeliveryMode::Inline) throw …` — PHPStan max flags it always-false / the throw unreachable. Pin the gate behaviorally instead: a test asserting `DeliveryMode::cases() === [DeliveryMode::Inline]` with a comment naming exactly what flips (the registry gains a reject-branch; the test flips to assert rejection) when the queue-driver ADR (F4–F6) adds `Queued`. Carry the deferred mode as a defaulted param (`DeliveryMode $mode = DeliveryMode::Inline`) for forward-compatible call sites; PHPStan does not flag an unused method param.
- **Platform substrate speaks `string` module-ids, never the `Module` enum** (boundary law, 3.3 — also `knowledge/architecture/rules.md`): a platform API carrying a module identity types it `string` (the `Module::X->value`, or `'platform'` for the platform pseudo-module), NOT `App\Modules\Module` — importing `Module` under `App\Platform\**` trips the `it_forbids_platform_code_from_depending_on_any_module` arch test (proven RED this iter: `Expecting 'App\Platform' not to use 'App\Modules'.`). Module emitters pass `Module::X->value` (the registry stays their typed anchor); same shape as `event_deliveries.consumer` (string FQCN) and the plain-`string` `*.module` columns. **Binds 3.4's `DomainEventRecorder` (`string $module`, NOT `Module|string`)** and every F2+ emitter. Design D3 sketched `Module|string` but labels it "realization, loop may refine"; D1 (the canonical boundary) outranks that detail. Tests MAY import `Module` (the arch suite scans only `App\Platform` *source*, not `Tests\`).
- **A `DB::transactionLevel() === 0` guard is testable only under `DatabaseMigrations`** (3.3): `RefreshDatabase` wraps each test in a transaction (level ≥ 1) so the guard's negative path is unreachable (design D3: "satisfied trivially" under the wrapper). `DatabaseTruncation` is out — its per-table DELETE hits the append-only immutability triggers. `DatabaseMigrations` (migrate:fresh = DDL, which the triggers don't guard) leaves tests at level 0 → wrap write tests in explicit `DB::transaction()` (the hint's instruction becomes load-bearing, not cosmetic), call the guard test bare. Bonus: under DatabaseMigrations `DB::transaction()` actually COMMITS observably (and a rollback discards) — which **3.4's atomicity tests (commit-persists / rollback-discards-all-three) REQUIRE**; RefreshDatabase can't surface a real commit. Per-file `uses(DatabaseMigrations::class)`; the global Pest binding stays commented.
- **Shared substrate guard exception** `App\Platform\Events\NotInTransactionException` (3.3): ONE dedicated exception for the no-dual-write guard, used by BOTH recorders (3.4's `DomainEventRecorder` natively; 3.3's `AuditRecorder` by import — `App\Platform\Audit` already imports `App\Platform\Events\ActorRole`, so the direction is established). Static factory `forRecording(string $what)` for a precise per-caller message (`'an audit record'` / `'a domain event'`); extends `RuntimeException`. Lives in `Events/` (the substrate core namespace owning the recorder/contract/registry).
- **Recorder fan-out + the constructor-injected singleton registry** (3.4, design D3/D4): a platform service that needs the `ConsumerRegistry` declares `public function __construct(private readonly ConsumerRegistry $registry) {}` — the container auto-wires the constructor and resolves the AppServiceProvider-bound SINGLETON, so the injected instance IS the one tests/module-providers `register()` into; the service itself needs NO explicit binding (`app(DomainEventRecorder::class)` reflects the ctor and injects the singleton). The fan-out shape: `foreach ($this->registry->consumersFor($name) as $consumer) { EventDelivery::create([...status => DeliveryStatus::Pending, attempts => 0]) }`, inside the caller's transaction, AFTER `DomainEvent::create()` (so `$event->id` exists for the FK). The recorder NEVER opens its own transaction (it appends to the caller's) and NEVER calls `DB::afterCommit` — delivery EXECUTION + the post-commit hook are task 4.1; 3.4 only enqueues the `pending` ledger rows. Enum-cast columns take the enum INSTANCE on write (`DeliveryStatus::Pending`, `ActorRole::X`), not `->value`. `correlation_id` default DIFFERS between the two recorders: audit → a fresh independent `Str::uuid7()`; the event recorder → the event's OWN `event_id` (root event is its own correlation root, design D3) — capture `$eventId` once and reuse it for both `event_id` and the `?? $eventId` correlation default.
- **Atomicity tests need DatabaseMigrations + the `cache` table as the "state change"** (3.4): proving the recorder rides the caller's transaction requires a REAL commit/rollback, which only happens at `transactionLevel 0` (DatabaseMigrations, not RefreshDatabase — see the guard pattern above). The canonical co-committed state change is a `cache`-table write (design D9: a platform table, idempotent, no domain pollution): `DB::table('cache')->insert(['key' => '…', 'value' => '…', 'expiration' => 9999999999])` (its three columns). Commit test: wrap state-write + `record()` in `DB::transaction()`, then assert the cache row + the `domain_events` row + the `pending` `event_deliveries` rows all exist. Rollback test: `expect(fn () => DB::transaction(fn () => { state-write; record(); throw new RuntimeException(...); }))->toThrow(RuntimeException::class)` then assert ALL THREE tables are empty. Reused by 5.1's E2E pipeline + demo command.
- **Two distinct `new class` statements ⇒ two distinct anonymous-class FQCNs** (3.4): a PHP anonymous-class declaration is compiled once per SYNTACTIC occurrence — executing the same `new class {...}` statement twice yields two instances of the SAME class name, but two SEPARATE `new class {...}` statements yield two DIFFERENT `::class` names. A fan-out/multi-consumer test that needs N *distinct* registered consumers (so the unique `(domain_event_id, consumer)` pair gives N rows) must write N separate `new class implements DomainEventConsumer {...}` blocks (helper `registerTwoFakeConsumers` does exactly this and returns the two FQCNs in registration order). The container/registry singleton is rebuilt per test (fresh `createApplication()`), so re-registering the same FQCNs across tests never leaks. 4.1 reuses this for its throwing-vs-succeeding consumer pair.
- **Two PHPStan-max test-typing traps** (3.3): (a) spreading an `array<string, mixed>` as named args (`record(...$args)`) degrades EVERY parameter to `mixed` (`expects string, mixed given`) — replace with a **fully-typed helper function** with defaulted params (enum and array-literal defaults are valid) returning the model; Larastan infers `DB::transaction(fn () => helper())` and `app(Foo::class)` return types so `$record->id` stays typed. (b) `$this->travelTo()`/`$this->freezeTime()` are unresolvable inside a Pest closure (`$this` resolves to `Pest\PendingCalls\TestCall`, not the TestCase) → use the static `CarbonImmutable::setTestNow($frozen)` + an `afterEach(fn () => CarbonImmutable::setTestNow())` reset (Carbon and CarbonImmutable share the global mock). (c) Don't `{@see}` a not-yet-existent class — Pint's `fully_qualified_strict_types` auto-imports docblock FQCNs, yielding a `use` of a missing class; use prose for forward references (e.g. "the domain-event recorder (task 3.4)").
- **The D5 `afterCommit`-under-test landmine, RESOLVED (4.1, verified in vendor)**: the base `DatabaseTransactionsManager::addCallback` attaches the callback to the current pending transaction (or runs it immediately at level 0); `afterCommitCallbacksShouldBeExecuted($level)` returns `$level === 0`, so callbacks fire when the OUTERMOST `DB::transaction()` commits — exactly production. The testing override (`Illuminate\Foundation\Testing\DatabaseTransactionsManager`, used by `RefreshDatabase`) returns `$level === 1` and `callbackApplicableTransactions()` skips the wrapper, so the hook ALSO fires under RefreshDatabase (at the inner commit). **So the "afterCommit never fires under the wrapper" landmine is NOT present in this framework** — no D5 fallback needed. Still choose **DatabaseMigrations** for delivery tests: only there are commit/rollback OBSERVABLE (level 0), which the atomicity / exactly-once assertions require. `DatabaseTransactionRecord::executeCallbacks()` is a plain FIFO `foreach`, so callbacks fire in registration order.
- **Per-record `afterCommit` hook = the realization of "one hook per transaction" (4.1, design D5)**: each `record()` registers `DB::afterCommit(fn () => $executor->deliver([$event->id]))`. Multiple records in ONE transaction attach their callbacks to the SAME `DatabaseTransactionRecord` and fire FIFO in record (= id) order — so causal delivery order holds — and a rollback discards them ALL (Laravel never runs uncommitted after-commit callbacks → no delivery on rollback, automatically). This stateless per-record form was chosen over a transaction-scoped id-buffer + single hook: functionally identical at launch volumes, but with zero buffer state to clear on commit/rollback and no nested-transaction edge cases. (Design D3/D5 say "one hook per transaction"; that is the behavioral contract — deliver post-commit, in causal order, never on rollback — which per-record satisfies. If a high-fan-out emitter ever makes the per-record query count matter, batching is a drop-in behind the same contract.)
- **Reading typed config at PHPStan level max (4.1)**: `config('x', 5)` is typed `mixed`, so `(int) config(...)` trips `cast.int` at level max (do NOT cast mixed). Use **`Config::integer($key, $default)`** (`Illuminate\Support\Facades\Config`, Laravel 11+) — returns a real `int` (throws `InvalidArgumentException` if the resolved value isn't an int). Baking the default into its second arg makes a service self-sufficient BEFORE its `config/*.php` exists (`Config::get` returns the default when the key is unresolved; the later file just makes the numbers explicit + env-overridable). Siblings: `Config::string/boolean/float/array`.
- **Container-resolve a consumer by FQCN + `instanceof` narrowing (4.1, design D4)**: `$this->container->make($stringFqcn)` returns `mixed` (a plain `string`, not `class-string`, defeats the generic), so guard with `if (! $x instanceof Contract) { throw; }` — PHPStan-narrows to the contract AND is the runtime guard for a renamed/removed consumer class. Inject `Illuminate\Contracts\Container\Container`. **Empirically the container resolves ANONYMOUS classes too** (`make($anon::class)` builds + returns it) — so a fan-out's anonymous fake-consumer rows actually execute under the inline hook (they don't silently no-op by failing to resolve).
- **Execution-after-rollback model resync (4.1)**: when a consumer handler and its `done` flip share one `DB::transaction()` and the handler throws, the transaction rolls back; `$delivery->refresh()` in the catch resyncs the model to its committed (pre-attempt) state before the SEPARATE failure write, so `attempts` counts from the persisted value (not an in-memory mutation a rolled-back `update()` left behind, nor a stale read after a commit-time failure). In the common path the throw precedes the `update()` line so the model is already clean — `refresh()` is the belt-and-suspenders that also covers the rare commit-time failure.
- **Named test-double consumers under `Tests\Support\` (4.1)**: PSR-4 `Tests\ => tests/` autoloads `tests/Support/Platform/*.php` (namespace `Tests\Support\Platform`) with NO composer dump and NO Pest collection (`tests/Pest.php` binds `TestCase` only `->in('Feature')`, and they have no `it()`); the container resolves them by FQCN at delivery time (named, not anonymous, = how a real module consumer resolves). They do DB-work-only (write `cache` markers for observable effects) and append to a `public static array $handled` sink for invocation count/order — **reset the static in `beforeEach`** (the DB trait resets the DB, never PHP statics).
- **When a downstream behavior lands, re-point a prior task's tests that incidentally relied on its absence (4.1→3.4)**: 3.4's two fan-out tests asserted `pending`/`attempts 0` AFTER a committed `record()`; 4.1's `afterCommit` hook now delivers post-commit (the anonymous fake consumers resolve → rows go `done`). Fix = assert the recorder's REAL in-transaction contract INSIDE the `DB::transaction()` closure (immune to the post-commit hook), and relax the atomicity test from a `where('status','pending')->count()` to a status-agnostic `EventDelivery::count()` (atomicity is co-persistence, not delivery status). The tests get tighter, not weaker — they now pin exactly what each task owns.
- **Laravel 13 scheduling + explicit console-command registration (4.2, design D1/D6, all vendor-verified)**: a sub-minute schedule goes in `routes/console.php` via the `Illuminate\Support\Facades\Schedule` facade — `Schedule::command('events:sweep')->everyThirtySeconds()->withoutOverlapping()` (`everyThirtySeconds` → `repeatEvery(30)`, sub-minute is first-class). A CLASS-based command living OUTSIDE `app/Console/Commands` (here `App\Platform\Events\SweepCommand`) is NOT auto-discovered (`Kernel::shouldDiscoverCommands()` only loads `app/Console/Commands`, and only when `get_class($this) === Foundation\Console\Kernel` — true since this app has no custom `app/Console/Kernel.php`) — register it explicitly with `->withCommands([SweepCommand::class])` in `bootstrap/app.php` (the `ApplicationBuilder` partitions class-string args to `$kernel->addCommands()`). `bootstrap/` is NOT in the phpstan paths (`app`/`database`/`routes`/`tests` are) so that file isn't type-checked, but `routes/console.php` IS — the facade chain types clean (`command()` returns `Event`, the frequency/overlap methods return `$this`).
- **Introspecting the schedule in a Feature test (4.2)**: `app(\Illuminate\Contracts\Console\Kernel::class)->bootstrap()` forces the console kernel to `require routes/console.php` (via `discoverCommands()`'s `commandRoutePaths` loop), registering the entries on the `Schedule` singleton (bound shared in `FoundationServiceProvider`, so the facade in the routes file and `app(Schedule::class)` are the SAME instance). Then `collect(app(Schedule::class)->events())->sole(fn (Event $e): bool => is_string($e->command) && str_contains($e->command, 'events:sweep'))` — `events()` is typed `Event[]` so `collect()` is `Collection<int,Event>` and `sole(callable): Event` (non-null, **throws `ItemNotFoundException` if absent/duplicated → finding it IS the existence proof**). Assert the public introspectable props `$e->repeatSeconds` (30) and `$e->withoutOverlapping` (bool). Guard the mixed `$e->command` with `is_string()` (NOT `(string)` cast — mixed→string casts trip PHPStan max like `(int)` does).
- **`Artisan::call('cmd')` returns a clean `int` exit code; the Pest `artisan()` helper returns `PendingCommand|int` (4.2)**: `Pest\Laravel\artisan()` is PHPDoc'd `@return PendingCommand|int`, so chaining `->assertExitCode()` trips PHPStan max on the `int` arm — and `$this->artisan()` is unusable in a Pest closure (`$this` is `TestCall`, like `$this->travelTo`). When a test only needs execution + exit code (no output assertions), use `Illuminate\Support\Facades\Artisan::call('cmd')` → `int`, asserted `expect(Artisan::call('cmd'))->toBe(0)` — one line that runs the command (bootstrapping the console kernel + loading routes/console.php) and pins success. (Keep the Pest `artisan()`/`expectsOutputToContain` for 5.1's demo-command OUTPUT assertions.)
- **PHPStan-clean time assertions on a nullable Carbon cast (4.2)**: `event_deliveries.available_at` is `?CarbonImmutable`, so a direct `$d->available_at->equalTo(...)` trips "method on nullable" at level max. Use the null-safe operator: `expect($d->available_at?->equalTo($expected))->toBeTrue()` (and `?->greaterThan(...)`) — the chain is `bool|null`, `toBeTrue()` fails loudly on an unexpected null, and PHPStan is satisfied with no narrowing dance. Freeze the clock with `CarbonImmutable::setTestNow($t0)` + an `afterEach(fn () => CarbonImmutable::setTestNow())` reset (NOT `$this->travelTo()` in a Pest closure); `setTestNow` persists across `Artisan::call()` invocations (it is process-global), so a multi-sweep backoff/dead-letter test just advances the mock between calls.
- **Share the "due" predicate, not just the loop (4.2)**: two delivery entry points (`deliver(eventIds)` for the inline hook, `deliverDue()` for the sweep) differ ONLY in their `where`-scope/ordering, so extract the common `pending`-AND-backoff-elapsed filter into one private `dueDeliveries(): Builder<EventDelivery>` both call — the definition of "due" then cannot drift between inline and sweep (a real correctness risk, since a divergent predicate would silently deliver/skip differently on the two paths). The trivial `foreach(...) attempt()` loop is left inline in each (two lines, not worth the `iterable<EventDelivery>`-typed indirection).
- **Command OUTPUT assertions = `Artisan::call()` + `Artisan::output()`, NOT the Pest `artisan()` chain (5.1)** — this CORRECTS the 4.2 note that suggested keeping the Pest helper for output assertions. `Pest\Laravel\artisan()` is PHPDoc'd `@return PendingCommand|int`, so `artisan('cmd')->expectsOutputToContain(...)` trips PHPStan max on the `int` arm (an `int` has no such method) — the same family as the `->assertExitCode()` trap. The PHPStan-clean path for BOTH exit code and output: `expect(Artisan::call('cmd'))->toBe(0)` (clean `int`) then `expect(Artisan::output())->toContain(...)` — `Illuminate\Support\Facades\Artisan::output(): string` returns the buffered output of the last `call()` (verified → `Foundation\Console\Kernel::output()`). Net: the Pest `artisan()` helper is effectively unusable at PHPStan max in this repo (output assertions OR exit-code chaining both hit the `int` arm); always reach for the `Artisan` facade pair. `PendingCommand::expectsOutputToContain`/`assertExitCode` do exist — they're just unreachable through the union-typed helper.
- **Re-runnable operator command over append-only tables (5.1, design D9)**: a command that records events is naturally idempotent-friendly — each run APPENDS to the append-only tables (a fresh per-event UUIDv7 dodges the unique `event_id` and `(domain_event_id, consumer)` constraints → no collision), and any STATE write uses `DB::table(...)->updateOrInsert([keyCols], [valueCols])` (an idempotent upsert on the `cache` PK — `cache` is `key` string-PK · `value` mediumText · `expiration` bigInteger) so a second run upserts rather than duplicate-keys. The consumer keys its `cache` marker by `event_id` (fresh each run) and ALSO upserts — modelling the consumer idempotency obligation the docs preach. "A second run also exits 0" is the documented failure/edge test. **Demonstration-only consumers register on the shared `ConsumerRegistry` singleton INSIDE the command's `handle()`** (idempotent `register()`), keeping demo wiring out of always-on module providers — the same singleton the recorder fans out against, so no provider boot() is needed for demo-only code.
- **`DB::transaction(fn (): array => [...])` return shape at PHPStan max (5.1)**: returning a keyed array from the transaction closure and destructuring it (`$r = DB::transaction(...); $r['event']`) is clean IF you annotate the assignment `/** @var array{event: DomainEvent, audit: AuditRecord} $r */` — Larastan threads the closure return through `transaction`'s generic, but the explicit `@var` removes any doubt and survives Pint (`fully_qualified_strict_types` shortens the FQCNs + adds the `use`). For a SINGLE value just type the closure (`fn (): DomainEvent => …record(…)`) and Larastan infers it directly (no annotation), as the recorder/inline tests already rely on.
- **The `pgsql` CI lane = a SEPARATE job; real job-env beats phpunit.xml `<env>` (5.2, design D8, all vendor-verified + empirically probed)**: a second engine lane is a distinct `jobs.tests-pgsql` job, NOT a matrix on the `quality` job — lint/PHPStan are engine-independent and run ONCE (only `php artisan test` runs twice). The lane: `services.postgres.image: postgres:17` (the DB-ADR floor) with `--health-cmd pg_isready` + throwaway creds + `ports: ['5432:5432']` (the job runs on the runner HOST, not in a `container:`, so the service port must be published and reached at `127.0.0.1` — NOT `localhost`, which can resolve to IPv6 `::1` where PG listens on IPv4); setup-php `extensions` gains `pdo_pgsql, pgsql`; same checkout/cache/install/`.env`+`key:generate` steps; then `php artisan test` with the connection passed as **job-level `env:`** (`DB_CONNECTION: pgsql` + `DB_HOST/DB_PORT/DB_DATABASE/DB_USERNAME/DB_PASSWORD`). **The precedence chain that makes the real env win over `phpunit.xml`'s `<env name="DB_CONNECTION" value="sqlite"/>` (the D8 landmine, verified):** (1) PHPUnit `PhpHandler::handleEnvVariables` only `putenv`s when `$force || getenv($name) === false` — our `<env>` has no `force`, so an already-set OS var survives (`vendor/phpunit/.../PhpHandler.php:140`); (2) `php artisan test` (Collision `TestCommand`) spawns the runner via Symfony `Process` whose `getDefaultEnv()` (`+= getenv()`) makes the subprocess INHERIT all parent OS env vars, Collision's own vars merged on top, NOT replacing them (`vendor/symfony/process/Process.php:336/1715`) — Collision's `clearEnv()` does NOT defeat this; (3) Laravel boots immutable dotenv so `.env`'s `DB_CONNECTION=sqlite` does not override the real var. Net: `config('database.default') === 'pgsql'`, **no `--env` fallback needed**. **Empirically proven** (postgres-free probe): a throwaway test printing `getenv/env()/config('database.default')` run as `DB_CONNECTION=pgsql php artisan test --filter=…` resolved ALL THREE to `pgsql` (baseline run resolved to `sqlite`). Pin the contract in `CiWorkflowTest` with `toContain` (job name, `image: postgres:17`, `DB_CONNECTION: pgsql`, a DISTINCT pgsql step name) + **`substr_count` guards** (`run: vendor/bin/pint --test`/`phpstan analyse` each === 1, `run: php artisan test` === 2) so a careless duplication of the engine-independent gates into the second lane breaks the suite; keep `quality` FIRST so the existing `strpos`-based gate-order pins anchor on its SQLite test line. The loop never pushes → the lane's FIRST live run is the human push after review (design D8); the reviewer treats that CI run as part of acceptance.

---

## [2026-06-12 14:26] — 1.1 Platform root + substrate enums + boundary-law amendment
- **What:** Created the `App\Platform` root with the three substrate enums and extended the platform-direction boundary law to cover it (design D1).
- **Files changed:**
  - `app/Platform/Events/ActorRole.php` — string-backed enum: `NewcoOps=newco_ops`, `Producer=producer`, `Customer=customer`, `System=system` (invariant-8 actor provenance).
  - `app/Platform/Events/DeliveryStatus.php` — `Pending=pending`, `Done=done`, `Failed=failed` (the `event_deliveries` lifecycle).
  - `app/Platform/Events/DeliveryMode.php` — single case `Inline=inline`; `queued` deliberately unrepresentable until the queue-driver ADR (F4–F6), making "queued is gated" a compile-time guarantee.
  - `tests/Unit/Platform/EnumsTest.php` — 3 verbatim case/value maps + 1 failure case (`ActorRole::from('admin')` → `ValueError`). 4 tests / 4 assertions.
  - `tests/Architecture/ModuleBoundariesTest.php` — added `'App\Platform'` to `$platformNamespaces` with a D1-citing comment.
- **Quality loop:** green — format ✅ · EnumsTest 4/4 ✅ · full suite **64/64 (323 assertions)** ✅ · phpstan level max 0 errors ✅ · pint --test ✅ · `openspec validate --strict` ✅.
- **RED-PROOF (mandatory, design D1 / lessons.md 2026-06-12):**
  - Added temp fixture `app/Platform/TempBoundaryProbe.php` (`namespace App\Platform; use App\Modules\Module;` returning `Module::Catalog`).
  - Ran `tests/Architecture/ModuleBoundariesTest.php` → **RED**: `it_forbids_platform_code_from_depending_on_any_module` failed with `Expecting 'App\Platform' not to use 'App\Modules'.` (tests 2, passed 1, failed 1) — the exact (source, target) pair, so the amendment is non-vacuous.
  - `rm app/Platform/TempBoundaryProbe.php` → re-ran the suite → **GREEN** (2 tests, 152 assertions). `git status app/` shows only `?? app/Platform/` (the three enums); the probe is gone, tree clean.
- **Learnings for future iterations:**
  - The boundary suite has high assertion counts (152) because both directions loop the registry — a single dirty namespace surfaces as one named pair, easy to read in the JSON `failures[]`.
  - `DeliveryMode` was made string-backed (`inline`) for parity with the other two and to keep the eventual `Queued` case persistable; nothing stores it yet (registry-time only).
  - No DB, no migration in this task — the first migrations arrive at 2.1. Kept the enums method-free; casts/usage land in 3.1 (models) per design D2.
---

## [2026-06-12 15:20] — 2.1 Migration `create_domain_events_table`
- **What:** The first domain migration — the append-only `domain_events` log (transactional outbox + 10-year audit/financial event store), with the full ADR envelope, the two launch indexes, and the PostgreSQL-only `actor_role` CHECK (design D2).
- **Files changed:**
  - `database/migrations/2026_06_12_000001_create_domain_events_table.php` — new. `id` bigint PK · `event_id` uuid unique · `name` · `schema_version` unsignedSmallInteger default 1 · `module` · `occurred_at` timestampTz · `actor_role` string NOT NULL (+ PG CHECK derived from `ActorRole::cases()` via a `DB::getDriverName()==='pgsql'` branch; SQLite fallback = NOT NULL + the enum cast, documented inline) · `actor_id` nullable unsignedBigInteger · `entity_type`/`entity_id` strings · `correlation_id` uuid · `causation_id` nullable self-FK→`domain_events.id` · `payload` jsonb. Indexes `(entity_type, entity_id, id)` and `(name, id)`.
  - `tests/Feature/Platform/DomainEventsSchemaTest.php` — new, `uses(RefreshDatabase::class)`. 7 tests / 8 assertions: full envelope columns (`hasColumns`), unique `event_id` (`hasIndex … 'unique'`), the two composite indexes (`hasIndex` on column arrays), happy-path full-row insert + `schema_version` defaults to 1, causal chain through the self-FK, NOT-NULL floor (missing `actor_role` → `QueryException`), duplicate `event_id` → `QueryException`.
- **Quality loop:** green — format ✅ · DomainEventsSchemaTest 7/7 ✅ · full suite **71/71 (331 assertions)**, +7 vs the 64 baseline ✅ · phpstan level max 0 errors ✅ · pint --test ✅ · `openspec validate --strict` ✅. `git status`: only the two new files (no protected paths); `git diff main -- composer.json composer.lock` empty.
- **Vendor-verified before writing** (standing rule): `Blueprint::jsonb/timestampTz/uuid/foreignId/unsignedSmallInteger` exist; SQLite grammar fallbacks (`typeJsonb`→`text`, `typeTimestampTz`→`typeTimestamp`, `typeUuid`→`varchar`); `Schema::hasColumns`/`hasIndex`/`getIndexes` signatures and that `hasIndex` matches a column array exactly; `DB::getDriverName()` at `Connection.php:1443`; sqlite `foreign_key_constraints` defaults true (so the self-FK is actually enforced — happy path uses `causation_id = null`).
- **PHPStan caught two real test-typing gaps** (fixed properly, not suppressed): the row-builder needed `array<string, mixed>` param/return types; the schema_version read was switched from `(int) $row->schema_version` (`$row` is `stdClass|null`, the value `mixed`) to `->value('schema_version')` asserted with `->toEqual(1)`. Both promoted to Codebase Patterns above.
- **Learnings for future iterations:**
  - 2.2 (`audit_records`) reuses this exact shape — same envelope CORE (`occurred_at`/`module`/`actor_role`+PG-CHECK/`actor_id`/`entity_type`/`entity_id`/`correlation_id`) minus `event_id`/`name`/`schema_version`/`causation_id`, plus `action`/`before`/`after`/`authorization_basis`. Lift the driver-branched CHECK block and the `hasColumns`/`hasIndex` test idiom verbatim. Stamp it `2026_06_12_000002_…` (runs after this one).
  - The self-referencing FK (`foreignId('causation_id')->nullable()->constrained('domain_events')`) works inside the same `Schema::create()` on SQLite `:memory:` — no split ALTER needed; confirmed by the causal-chain test passing under enforced FKs.
  - `payload` was made NOT NULL (an event always has a payload, `{}` at minimum) — the recorder (3.4) always passes an array. The FX-string discipline (`'1.0842'` as a string) is only *placed* in the payload here; the round-trip-survives-as-string assertion is 3.4's acceptance, not this task's (DB-layer `json_encode` here, the model cast lands at 3.1).
---

## [2026-06-12 16:05] — 2.2 Migration `create_audit_records_table`
- **What:** The operator-action audit trail (design D2) — the second substrate table. Shares only the envelope CORE with `domain_events` (no `event_id`/`name`/`schema_version`/`causation_id`); adds `action`/`before`/`after`/`authorization_basis`. Satisfies CLAUDE.md invariant 8 (every operator action records `actor_role`; trails append-only).
- **Files changed:**
  - `database/migrations/2026_06_12_000002_create_audit_records_table.php` — new. `id` PK · `occurred_at` timestampTz · `module` · `actor_role` string NOT NULL (+ PG CHECK derived from `ActorRole::cases()` via the same `DB::getDriverName()==='pgsql'` branch as 2.1; constraint named `audit_records_actor_role_check`) · `actor_id` nullable unsignedBigInteger · `entity_type`/`entity_id` strings · `correlation_id` uuid (no FK — soft grouping key) · `action` string NOT NULL · `before`/`after` jsonb **nullable** (creation has no before; GDPR redaction nulls both — design D7) · `authorization_basis` string NOT NULL. Single index `(entity_type, entity_id, id)`. **No timestamps** (`occurred_at` is the envelope clock — task 3.1).
  - `tests/Feature/Platform/AuditRecordsSchemaTest.php` — new, `uses(RefreshDatabase::class)`. 8 tests / 11 assertions: envelope-core+audit columns present (`hasColumns`), the event-log-only columns ABSENT (`hasColumns(...)->toBeFalse()` for `event_id`/`name`/`schema_version`/`causation_id`), the composite index (`hasIndex`), happy-path full-row insert reading back `action`, before/after-nullable edge (insert with both null → row exists), and three NOT-NULL floors (`actor_role`, `authorization_basis`, `action` each → `QueryException`).
- **Quality loop:** green — format ✅ · AuditRecordsSchemaTest 8/8 ✅ · full suite **79/79 (342 assertions)**, +8 vs the 71 baseline ✅ · phpstan level max 0 errors ✅ · pint --test ✅ · `openspec validate --strict` ✅. `git status`: only the two new files (no protected paths); `git diff main -- composer.json composer.lock` empty.
- **Learnings for future iterations:**
  - The driver-branched `actor_role` CHECK block (2.1) lifted verbatim — only the table name and the constraint name (`audit_records_actor_role_check`) changed. The pattern is now proven twice; 2.4's trigger migration will branch the same `DB::getDriverName()` way for both engines.
  - `correlation_id` here is a bare `uuid()` with **no FK** (unlike `domain_events.causation_id`'s self-FK) — in the audit envelope it's a soft grouping key tying the operator action to the events it triggered, not a referential link. Matches `domain_events.correlation_id` (also FK-less).
  - Added a `hasColumns(...)->toBeFalse()` test pinning the *absence* of the event-log-only columns — cheap guard that audit stays envelope-CORE-only and never drifts toward duplicating the event log. Reusable idiom for "this table deliberately lacks column X".
  - `before`/`after` nullability is load-bearing for D7 redaction (the only mutation the immutability trigger will allow on an audit row) — proven nullable here so 2.4 can rely on it.
---

## [2026-06-12 16:45] — 2.3 Migration `create_event_deliveries_table`
- **What:** The third substrate table (design D2) — the per-consumer delivery ledger that makes domain-event delivery durable and at-least-once. One mutable row per (domain_event × consumer): lifecycle `pending → done | failed`, attempt count, exponential-backoff clock. R4 is resolved structurally (independent rows → independent retries). Unlike the two append-only tables this one is deliberately MUTABLE and carries timestamps.
- **Files changed:**
  - `database/migrations/2026_06_12_000003_create_event_deliveries_table.php` — new. `id` PK · `domain_event_id` foreignId→`domain_events.id` (NOT NULL) · `consumer` string (= the consumer class FQCN, design D4) · `status` string NOT NULL default `DeliveryStatus::Pending->value` · `attempts` unsignedSmallInteger default 0 · `available_at` timestampTz nullable (NULL = due now) · `last_error` text nullable · `timestamps()`. Unique `(domain_event_id, consumer)`. **Partial index** `event_deliveries_pending_index ON (available_at) WHERE status = 'pending'` via raw `DB::statement` (Blueprint has no fluent predicate — verified in vendor), the `'pending'` literal derived from the enum.
  - `tests/Feature/Platform/EventDeliveriesSchemaTest.php` — new, `uses(RefreshDatabase::class)`. 9 tests / 10 assertions: full columns (`hasColumns`), the unique pair (`hasIndex … 'unique'`), the partial index by NAME (`hasIndex($t, 'event_deliveries_pending_index')`), defaults (status=pending, attempts=0), happy-path full row, duplicate-pair → `QueryException`, the pair allows same-consumer/diff-event & diff-consumer/same-event (non-vacuity), FK orphan → `QueryException`, missing `consumer` → `QueryException`. Two helpers: `seedDomainEvent(): int` (FK parent via `insertGetId`, PHPDoc `@return int` so no cast) and `eventDeliveryRow(int, array): array`.
- **Quality loop:** green — format ✅ · EventDeliveriesSchemaTest 9/9 ✅ · full suite **88/88 (352 assertions)**, +9 vs the 79 baseline ✅ · phpstan level max 0 errors ✅ · pint --test ✅ · `openspec validate --strict` ✅. `git status`: only the two new files (no protected paths); `git diff main -- composer.json composer.lock` empty.
- **Vendor-verified before writing** (standing rule): `IndexDefinition` has no `where()`/partial-predicate method; SQLite & Postgres `compileIndex` emit plain `create index … (cols)`; `Builder::hasIndex` matches a NAME string OR a columns array (`$value['name'] === $index || $value['columns'] === $index`); `getIndexes()` returns name+columns+unique+primary (PG adds type) but **never the partial predicate**; `insertGetId` PHPDoc `@return int`; `Blueprint::timestamps()` makes `created_at`/`updated_at` **nullable**; `ForeignIdColumnDefinition::constrained()` defaults the referenced column to `'id'`.
- **Extra confidence check (not a committed test):** ran a throwaway `php -r` against `sqlite::memory:` creating the same partial index and read `sqlite_master.sql` — the stored DDL keeps the `WHERE status = '…'` predicate, confirming SQLite materializes a genuinely PARTIAL index (not a plain one). Documented because `getIndexes()` can't surface this.
- **Learnings for future iterations:**
  - 2.4 (immutability triggers) now has all three tables to guard — but note the trigger task targets `domain_events` (UPDATE+DELETE) and `audit_records` (DELETE + structural-UPDATE) ONLY; `event_deliveries` is deliberately MUTABLE (status/attempts churn during retries) and gets NO immutability trigger. Don't accidentally extend the trigger to it.
  - The FK `domain_event_id`→`domain_events.id` is enforced on SQLite `:memory:` (foreign_keys ON), so 3.4's recorder must insert the parent event BEFORE its delivery rows inside the same transaction (it does — the event is inserted, then `->id` feeds the fan-out rows).
  - Partial-index idiom is now in Codebase Patterns above (raw DDL, name-based portable assertion). It's the second "PG-truthful realization where SQLite needs a documented path" — but unlike the CHECK (PG-only), the partial index runs on BOTH engines.
---

## [2026-06-12 17:25] — 2.4 Migration `add_immutability_triggers`
- **What:** Immutability layer 1 — DB triggers with full SQLite/PostgreSQL parity (design D7), the last of the schema group. `domain_events` rejects every UPDATE+DELETE (append-only 10-year store); `audit_records` rejects every DELETE and any UPDATE that changes a *structural* column, allowing only `before`/`after` to change (the GDPR redaction seam — Module K's erasure job overwrites PII in place, never DELETE). `event_deliveries` is deliberately left mutable (no trigger). Enforces CLAUDE.md invariant 4 at the storage layer.
- **Files changed:**
  - `database/migrations/2026_06_12_000004_add_immutability_triggers.php` — new. Driver-branched in `up()`/`down()` on `DB::getDriverName()`, **both branches fully implemented**. SQLite: 4 `CREATE TRIGGER … RAISE(ABORT, '…immutable…')` (domain_events update+delete, audit delete, audit structural-update with a `WHEN (… IS NOT …)` guard). PostgreSQL: 2 plpgsql `RETURNS trigger` functions (`RAISE EXCEPTION '…immutable…'`) each fired by one `BEFORE UPDATE OR DELETE … EXECUTE FUNCTION`; the audit function checks `TG_OP='DELETE'` then `IS DISTINCT FROM` over the structural list and `RETURN NEW` for allowed redaction. ONE `$auditStructuralColumns` list (every column except before/after) drives both dialects.
  - `tests/Feature/Platform/ImmutabilityTest.php` — new, `uses(RefreshDatabase::class)`. 6 tests / 14 assertions: the five delta-spec scenarios (domain_events UPDATE & DELETE rejected; audit structural UPDATE rejected; audit before/after-only UPDATE allowed + structural columns untouched; audit DELETE rejected) + an `event_deliveries`-stays-mutable guard. Helper `captureImmutabilityError(Closure): string` returns the QueryException message ('' if nothing thrown → `toContain('immutable')` fails loudly = non-vacuous). Two row builders (`immutabilityDomainEventRow`/`immutabilityAuditRow`, names chosen to not collide with the sibling test helpers).
- **Quality loop:** green — format ✅ · ImmutabilityTest 6/6 ✅ · full suite **94/94 (366 assertions)**, +6 vs the 88 baseline ✅ · phpstan level max 0 errors ✅ · pint --test ✅ · `openspec validate --strict` ✅. `git status`: only the two new files (no protected paths); `git diff main -- composer.json composer.lock` empty.
- **Realization decision — `DB::statement` not `DB::unprepared`** (design D7/tasks.md 2.4 both name `unprepared`): `unprepared()`'s `$query` is Larastan-typed `literal-string`, so the interpolated audit structural clause (`{$changed}` from `implode`/`array_map`) trips PHPStan `argument.type` at level max. `DB::statement` carries no such constraint — and is already the codebase's established idiom for driver-branched interpolated DDL (000001/000002 CHECK, 000003 partial index). This is a realization detail (the ADR doesn't mandate a facade method), not an ADR re-decision — preserves driver-branching, both branches, the one authoritative list, and behaviour parity. No escalation. Documented in the migration docblock + Codebase Patterns above.
- **Verified before writing** (standing rule): throwaway `php -r` against `sqlite::memory:` proved (a) `RAISE(ABORT,'…immutable…')` surfaces the token in the PDOException message; (b) `IS NOT` is null-safe (NULL→value structural change caught); (c) a before/after-only UPDATE passes the `WHEN` guard untouched; (d) prepare+execute (= `DB::statement`) installs a `CREATE TRIGGER` with internal semicolons and it fires.
- **Learnings for future iterations:**
  - The schema group (2.x) is now COMPLETE — three tables + the immutability triggers. Next is 3.1 (Eloquent models). The triggers mean a model test that tries to `update()`/`delete()` a `domain_events`/`audit_records` row WILL throw — models for those two tables are effectively insert-only; only `event_deliveries` (mutable) round-trips update in tests.
  - The `RAISE(ABORT)`-reverts-statement-not-transaction semantics is what lets a test catch the QueryException and still read the (unchanged) row back inside the same RefreshDatabase wrapper transaction. If a future engine/path rolled back the whole txn on abort, those row-unchanged assertions would need a re-seed.
  - The audit structural-UPDATE trigger is the load-bearing one for 6.1's REVOKE-runbook docs (layer 2) and the additive-only policy: any new `audit_records` column (additive-only) MUST be added to `$auditStructuralColumns` or it becomes silently mutable — called out in the migration docblock and to be repeated in docs/event-substrate.md (task 6.1).
---

## [2026-06-12 18:05] — 3.1 Eloquent models (DomainEvent / EventDelivery / AuditRecord)
- **What:** The application layer on top of the three substrate tables (design D1/D2) — the casts that hydrate persisted rows into the typed envelope. The schema migrations (2.x) proved columns/indexes/triggers at the DB layer; these models prove the read/write casts: enum value-set floor, CarbonImmutable clocks, jsonb ↔ array, and the timestamps split (append-only tables carry none; the mutable delivery ledger keeps them).
- **Files changed (all new, no existing file modified):**
  - `app/Platform/Events/DomainEvent.php` — `$table='domain_events'`, `public $timestamps = false`, `$guarded = []`; casts `payload`→array, `occurred_at`→immutable_datetime, `actor_role`→`ActorRole::class`, `schema_version`→integer. Full `@property` block.
  - `app/Platform/Events/EventDelivery.php` — `$table='event_deliveries'`, timestamps KEPT, `$guarded = []`; casts `status`→`DeliveryStatus::class`, `available_at`→immutable_datetime.
  - `app/Platform/Audit/AuditRecord.php` — new `App\Platform\Audit\` namespace; `$table='audit_records'`, `public $timestamps = false`, `$guarded = []`; casts `before`/`after`→array, `actor_role`→`ActorRole::class`. `occurred_at` left UNCAST per the task's explicit cast list (reads as string) — documented in the model docblock.
  - `tests/Feature/Platform/ModelsTest.php` — new, `uses(RefreshDatabase::class)`. 5 tests / 20 assertions: 3 round-trips (one per model, create→re-fetch→assert cast types: `ActorRole`/`DeliveryStatus`/`CarbonImmutable` instances, payload array, `schema_version` int, before/after arrays), the timestamps split (`usesTimestamps()` false/false/true + created_at actually populated on a delivery row), and the failure case (an out-of-enum `status` planted via raw `DB::table` insert throws `ValueError` on model hydration).
- **Quality loop:** green — format ✅ · ModelsTest 5/5 ✅ · full suite **99/99 (386 assertions)**, +5 vs the 94 baseline ✅ · PHPStan level max 0 errors ✅ · Pint `--test` ✅ · `openspec validate --strict` ✅. `git status`: only the four new files (no protected paths); `git diff main -- composer.json composer.lock` empty.
- **Acceptance walk:** (1) round-trip create/read with correct cast types — the 3 round-trip tests re-fetch a fresh instance so the assertions exercise the READ casts, not create()'s in-memory values. (2) persistence-convention arch test stays green unchanged — it scans `App\Modules\**` only; platform models are out of scope; the full suite (which includes `ModulePersistenceConventionsTest`) is green and only +5 NEW tests were added (no pre-existing test changed). (3) happy + failure case both tested.
- **Verified before writing (standing rule):** `immutable_datetime` cast exists (`HasAttributes.php:127/896`) → `Carbon\CarbonImmutable`; enum cast `getEnumCaseFromValue`→`::from()` throws `ValueError` uncaught; Larastan default migration scan + `SchemaAggregator` column-type support (no `timestamptz`); `usesTimestamps()` is public on `Model`.
- **Learnings for future iterations:**
  - 3.3 (`AuditRecorder`) writes via `AuditRecord`, 3.4 (`DomainEventRecorder`) writes via `DomainEvent` + fans out `EventDelivery` rows — all now exist with `$guarded = []` so the recorders can `create([...])` the full attribute set directly. The enum casts accept enum INSTANCES on write (pass `ActorRole::System`, not its `->value`).
  - The immutability triggers make `domain_events`/`audit_records` INSERT-only: `create()` works (INSERT), but any later `->update()`/`->delete()` on those two models throws `QueryException` — recorders must never mutate a persisted event/audit row. Only `EventDelivery` round-trips an update (4.1's status flips).
  - The FX-string discipline is demonstrated here (payload `'1.0842'` survives as a string, `12000` survives as an int through the array cast) but 3.4 owns the authoritative round-trip-survives assertion inside a recorder transaction.
  - Patterns consolidated above (platform-model conventions + the Pest global-helper-name collision rule).
---

## [2026-06-12 18:40] — 3.2 Consumer contract + registry
- **What:** The provider seam for domain-event consumers (design D4) — the contract F2+ modules implement and the registry their service providers register into. Group 3 (substrate API) 2/4. With this, the recorder (3.4) can ask `consumersFor($name)` to fan out delivery rows; the executor (4.1) resolves each FQCN from the container.
- **Files changed (3 new, 1 edited):**
  - `app/Platform/Events/Contracts/DomainEventConsumer.php` — new `Contracts/` subdir. `interface DomainEventConsumer { public function handle(DomainEvent $event): void; }`; docblock carries the consumer obligations (DB-work-only, idempotent + order-tolerant, Module E § 7 intent-row shape for external I/O) for docs 6.1.
  - `app/Platform/Events/ConsumerRegistry.php` — plain class, `private array $consumers` (event name → `list<class-string<DomainEventConsumer>>` in registration order). `register($eventName, $consumerClass, DeliveryMode $mode = DeliveryMode::Inline)`: `is_subclass_of` guard → `InvalidArgumentException`; idempotent on the (event, consumer) pair (first wins). `consumersFor($eventName): array`.
  - `app/Providers/AppServiceProvider.php` — edited `register()`: `$this->app->singleton(ConsumerRegistry::class)` (the "singleton" of D4 — one shared instance across provider boots + delivery path).
  - `tests/Unit/Platform/ConsumerRegistryTest.php` — new, 7 tests / 9 assertions (anonymous-class consumers): registration order, empty-for-unknown, per-event isolation, idempotent duplicate, non-contract class throws (`stdClass`), missing class throws, the Inline-only compile-time gate.
  - `tests/Feature/Platform/ConsumerRegistryBindingTest.php` — new, 2 tests / 3 assertions: singleton identity from the container + registration persistence across resolutions (Feature because Unit has no app).
- **Quality loop:** green — Pint ✅ · ConsumerRegistry filter 9/9 ✅ · full suite **108/108 (398 assertions)**, +9 vs the 99 baseline ✅ · PHPStan level max 0 errors ✅ · Pint `--test` ✅ · `openspec validate --strict` ✅. `git status`: 1 modified (`AppServiceProvider`, not protected) + 4 new; `git diff main -- composer.json composer.lock` empty.
- **Decisions (all per task/design):**
  - **Duplicate policy = idempotent** (task said pick one + document + test). Reason beyond friendliness: a duplicate in `consumersFor()` would make the recorder (3.4) attempt two identical `event_deliveries` rows → the unique `(domain_event_id, consumer)` index rejects it → the whole emitting transaction fails. Idempotent registration makes that class of bug unreachable. Documented in the class docblock, tested.
  - **The queued-mode gate is compile-time** — a single-case `DeliveryMode` means `register()` can't be handed a non-inline mode; a runtime `if ($mode !== Inline) throw` would be PHPStan-max dead code (always-false). Pinned with a `DeliveryMode::cases() === [Inline]` test + a comment naming what flips when the queue ADR (F4–F6) adds `Queued`. `$mode` rides as a defaulted param for forward-compatible call sites.
  - **`consumersFor` returns `class-string<DomainEventConsumer>`** (not bare `string`) — `is_subclass_of` narrowing (PHPStan max) makes the validated string storable without a cast, and the precise type helps 3.4 (the FQCN goes straight into `event_deliveries.consumer`).
- **Verified before writing (standing rule):** `is_subclass_of` accepts an anonymous-class name (true), returns false for `stdClass` and for a missing class with no fatal (probe run); `tests/Pest.php` binds `TestCase` only `->in('Feature')` so Unit has no container; `$this->app->singleton($class)` single-arg form binds concrete-to-self shared.
- **Learnings for future iterations:**
  - 3.3 (`AuditRecorder`) + 3.4 (`DomainEventRecorder`) resolve the shared registry via `app(ConsumerRegistry::class)` (or ctor-inject) — same instance the module providers register into. 3.4's fan-out: `foreach (consumersFor($name) as $fqcn) { insert pending event_deliveries row }`; the idempotent registry guarantees no duplicate FQCN, so the unique index is never tripped by registration.
  - 4.1's executor does NOT need the registry — it reads `event_deliveries.consumer` (the FQCN) and `app($fqcn)` resolves the consumer at delivery time (design D4 "container-resolved at delivery time").
  - The `Contracts/` subdir is new under `App\Platform\Events\` — boundary law unaffected (imports only `App\Platform\Events\DomainEvent`, no module symbols); the full arch suite stayed green unchanged.
  - Four reusable patterns consolidated above (provider-seam singleton · Unit-has-no-app split · `is_subclass_of` narrowing/dual-failure-guard · single-case-enum compile-time gate).
---

## [2026-06-12 19:10] — 3.3 AuditRecorder
- **What:** The single write path for `audit_records` (design D3) — the operator/system action trail. Assembles the shared envelope core (app-set UTC `occurred_at`, `module`, `actor_role`, nullable `actor_id`, entity ref, `correlation_id` defaulting to a fresh UUIDv7) plus `action`/`before`/`after`/`authorization_basis`, writes one row via the `AuditRecord` model, and — unlike the event recorder — creates ZERO `event_deliveries` rows (audit is write-only w.r.t. the substrate). Enforces the no-dual-write guard. Group 3 (substrate API) 3/4.
- **Files changed (3 new):**
  - `app/Platform/Audit/AuditRecorder.php` — `record(string $action, string $module, ActorRole $actorRole, ?int $actorId, string $entityType, string $entityId, ?array $before, ?array $after, string $authorizationBasis, ?string $correlationId = null): AuditRecord`. Guard `DB::transactionLevel() === 0` → throw; `occurred_at` = `CarbonImmutable::now('UTC')`; `correlation_id` = `$correlationId ?? (string) Str::uuid7()`.
  - `app/Platform/Events/NotInTransactionException.php` — the shared no-dual-write guard exception (static `forRecording(string $what)`, extends `RuntimeException`), reused by 3.4.
  - `tests/Feature/Platform/AuditRecorderTest.php` — `uses(DatabaseMigrations::class)`; 6 tests / 20 assertions: read-back-complete (every envelope field), no-deliveries (`EventDelivery::count() === 0`), outside-transaction throws + writes nothing (level-0 precondition pinned), correlation_id default is a fresh UUID, the platform pseudo-module string, and `occurred_at` as the app-set UTC clock (frozen-time). Typed helper `recordTestAudit(...)` (defaulted params) + `afterEach` test-now reset.
- **Quality loop:** green — format ✅ · AuditRecorderTest 6/6 ✅ · full suite **114/114 (418 assertions)**, +6 vs the 108 baseline ✅ · PHPStan level max 0 errors ✅ · Pint `--test` ✅ · `openspec validate --strict` ✅. `git status`: only the 3 new files + the flipped checkbox (no protected paths); `git diff main -- composer.json composer.lock` empty.
- **Boundary-law decision (the iteration's one real fork — affects 3.4 + every F2+ emitter):** the recorder was first written `Module|string $module` (design D3's sketch) and the FULL SUITE went RED on `it_forbids_platform_code_from_depending_on_any_module` (`Expecting 'App\Platform' not to use 'App\Modules'.`). Importing `App\Modules\Module` under `App\Platform` violates the canonical boundary (design D1: "platform must never depend on modules"). Refined the signature to `string $module`: platform speaks the storage primitive, module emitters pass `Module::X->value`. D3 labels its signature "realization, loop may refine"; D1 + the arch test outrank it. NOT an escalation — the boundary law is unambiguous and canonical, and the fix RESPECTS invariant 10 rather than violating it. Captured as `knowledge/architecture/rules.md` (1st knowledge domain) + a Codebase Pattern.
- **Realization — DatabaseMigrations, not RefreshDatabase** (verified in vendor before choosing): a `transactionLevel() === 0` guard can't be tested under RefreshDatabase (wrapper keeps level ≥ 1; design D3 anticipates this). DatabaseTruncation is out (its per-table DELETE hits the append-only immutability triggers). DatabaseMigrations (migrate:fresh = DDL the triggers don't guard) runs each test at level 0 — so the write tests use explicit `DB::transaction()` (the hint's instruction made load-bearing) and the guard test runs bare. This also gives 3.4 the observable-commit / observable-rollback its atomicity scenarios require.
- **Verified before writing (standing rule):** `DatabaseMigrations` uses `migrate:fresh`/`migrate:rollback` (no wrapper txn); `Str::isUuid` (Str.php:645), `Str::uuid7` (returns a `Stringable` `UuidInterface`), `DB::transactionLevel()` (ManagesTransactions:341) all exist; `travelTo`/`freezeTime` are `InteractsWithTime` METHODS (so `$this->travelTo()` is unresolvable by PHPStan in a Pest closure — used `CarbonImmutable::setTestNow()` instead).
- **Learnings for future iterations:**
  - 3.4's `DomainEventRecorder` mirrors this exactly: same guard via `NotInTransactionException::forRecording('a domain event')`, same `string $module`, same DatabaseMigrations test trait. Its ONLY additions over audit: app-side `event_id` (UUIDv7), `correlation_id` defaults to the event's OWN `event_id` (not an independent fresh UUID), `causation_id`, `schema_version`, and the `event_deliveries` fan-out (one `pending` row per `consumersFor($name)`) + the `DB::afterCommit` hook.
  - `before`/`after` are required-but-nullable params (explicit `null` for a creation audit); only `correlationId` is defaulted. `actor_id` reads back as a bigint → assert with `->toEqual` (loose, spans engines), like `attempts` in ModelsTest.
  - Four reusable patterns consolidated above (platform-speaks-string-module-ids · transaction-guard-needs-DatabaseMigrations · shared NotInTransactionException · two PHPStan-max test-typing traps).
---

## [2026-06-12 19:55] — 3.4 DomainEventRecorder
- **What:** The single write path for `domain_events` (design D3) — the append-only log that is the transactional outbox + inter-module API record + 10-year audit/financial event store in one table. Assembles the full envelope (app-side UUIDv7 `event_id`, UTC `occurred_at`, `correlation_id` defaulting to the event's OWN `event_id` for a root event, nullable `causation_id`, `schema_version` 1, `string $module`) plus the caller's `payload`, persists one `DomainEvent`, and fans out one `pending` `event_deliveries` row per consumer registered for the event name — all inside the caller's transaction. Shares the no-dual-write transaction guard with the audit recorder. Group 3 (substrate API) **4/4 — COMPLETE**.
- **Files changed (2 new):**
  - `app/Platform/Events/DomainEventRecorder.php` — `record(string $name, string $module, ActorRole $actorRole, ?int $actorId, string $entityType, string $entityId, array $payload, ?string $correlationId = null, ?int $causationId = null): DomainEvent`. Ctor-injects the `ConsumerRegistry` singleton (`private readonly`). Guard `DB::transactionLevel() === 0` → `NotInTransactionException::forRecording('a domain event')`. Captures `$eventId = (string) Str::uuid7()` once → used for both `event_id` and the `?? $eventId` correlation default. NO own transaction, NO `DB::afterCommit` (4.1's job).
  - `tests/Feature/Platform/DomainEventRecorderTest.php` — `uses(DatabaseMigrations::class)`; 11 tests / 44 assertions. Typed helper `recordTestEvent(...)` (defaulted params) + `registerTwoFakeConsumers(string): list<string>` (two distinct `new class` blocks) + `afterEach` test-now reset.
- **Quality loop:** green — format ✅ · DomainEventRecorderTest 11/11 ✅ · full suite **125/125 (462 assertions)**, +11 vs the 114 baseline ✅ · PHPStan level max 0 errors ✅ · Pint `--test` ✅ · `openspec validate --strict` ✅. `git status`: only the 2 new files + the flipped checkbox (no protected paths); `git diff main -- composer.json composer.lock` empty.
- **Acceptance walk (every bullet demonstrably true):** atomic commit (state `cache` row + event + 2 pending rows) ✅ · rollback discards all three (cache/domain_events/event_deliveries all 0) ✅ · outside-transaction refused + writes nothing (failure case) ✅ · FX `'1.0842'` survives as exact string + `amount_minor` int ✅ · ids strictly increase in emission order + 3 distinct event_ids ✅ · provenance query by (entity_type, entity_id) in id order, filtering other entities ✅ · `correlation_id == event_id` for a root event ✅ · happy path reads back the full envelope (UUIDv7 `event_id`, `schema_version` 1, enum `actor_role`, CarbonImmutable `occurred_at`) ✅ · two fake consumers → two `pending` rows (FQCNs in registration order, status, attempts 0) + orphan name → 0 rows ✅. Bonus: causation threading + occurred_at UTC clock.
- **Scope discipline — the afterCommit hook is 4.1, NOT 3.4:** tasks.md 3.4 says "Delivery execution is NOT this task (4.1 wires the post-commit hook)" and 4.1 says "Wire the recorder: ONE DB::afterCommit hook…". `InlineDeliveryExecutor` doesn't exist yet, so the recorder can't reference it — 3.4 only durably enqueues the `pending` ledger rows; the at-least-once guarantee is the sweep, so nothing hangs on the hook. The recorder docblock flags exactly where 4.1 adds it.
- **Verified before writing (standing rule):** `Str::uuid7()` (Stringable UuidInterface; `(string)` cast) and `Str::isUuid()` accept a v7 UUID (AuditRecorderTest already relies on this); the `cache` table's three columns (`key`/`value`/`expiration`); `ConsumerRegistry` IS the AppServiceProvider singleton (read the provider — `$this->app->singleton(ConsumerRegistry::class)`), so the ctor-injected instance is the one the test registers into; `DeliveryStatus::Pending` enum instance is accepted on write by the cast (3.1).
- **Learnings for future iterations:**
  - 4.1 (`InlineDeliveryExecutor`) ADDS the `DB::afterCommit` hook to THIS recorder (modifying `DomainEventRecorder` — collect recorded event ids per transaction, hand them to the executor) and builds the executor that reads `event_deliveries` and `app($consumer)`-resolves each FQCN. The verify-in-vendor landmine is `Illuminate\Foundation\Testing\DatabaseTransactionsManager` afterCommit-under-RefreshDatabase semantics (design D5 fallback ladder).
  - The recorder returns the `DomainEvent` only (not the deliveries); a caller needing the recorded id for a `causation_id` chain reads `$event->id` (the monotonic PK). Three new Codebase Patterns consolidated above (recorder-fan-out/ctor-injected-singleton · atomicity-needs-DatabaseMigrations+cache-table · two-distinct-new-class-statements).
  - Group 3 is CLOSED. Next is **4.1** (delivery execution + post-commit wiring), then 4.2 (sweep), 5.1 (hello-world/demo), 5.2 (pgsql CI lane), 6.1 (docs), 6.2 (final sweep).
---

## [2026-06-12 20:45] — 4.1 InlineDeliveryExecutor + post-commit wiring
- **What:** The launch delivery engine (design D5) and the recorder's `DB::afterCommit` wiring — the inline fast path that runs an event's `event_deliveries` rows once the emitting transaction commits. Opens group 4 (delivery execution). `deliver(array $domainEventIds)` selects DUE `pending` deliveries (NULL or past `available_at`) ordered `domain_event_id, id`, and runs each through `attempt()`: the consumer's `handle()` AND the `done`/attempts+1 flip share ONE `DB::transaction()` (exactly-once for DB effects); a throw rolls that back and is recorded as a failure in a SEPARATE write (attempts+1, exponential backoff `available_at`, truncated `last_error`, `failed` at max attempts), the per-delivery try/catch isolating consumers (R4). Same path the sweep (4.2) will reuse over its own due selection.
- **Files changed (3 new, 2 modified):**
  - `app/Platform/Events/InlineDeliveryExecutor.php` — new. Ctor-injects `Illuminate\Contracts\Container\Container`; resolves each consumer by FQCN (`make()` + `instanceof DomainEventConsumer` narrowing/guard); loads the event via `DomainEvent::findOrFail($delivery->domain_event_id)`; tunables via `Config::integer('events.sweep.*', <default>)` (self-sufficient before 4.2's `config/events.php`).
  - `app/Platform/Events/DomainEventRecorder.php` — modified. Ctor now also injects `InlineDeliveryExecutor`; after the fan-out it registers `DB::afterCommit(fn () => $this->executor->deliver([$event->id]))` (one per `record()` call — FIFO on the shared txn record preserves causal order, discarded on rollback). Docblock updated (delivery EXECUTION is now wired here).
  - `tests/Support/Platform/RecordingConsumer.php` + `FailingConsumer.php` — new named test doubles (`Tests\Support\Platform`, PSR-4 autoloaded, container-resolvable). Recording: `$handled` static sink + `cache` marker. Failing: `cache` marker then throws (`FAILURE_MESSAGE`).
  - `tests/Feature/Platform/InlineDeliveryTest.php` — new, `uses(DatabaseMigrations::class)`. 6 tests / 27 assertions: inline happy path (done/attempts 1/handler ran/cache), R4 isolation + exactly-once failure-rollback + emitter intact, direct-deliver recovery (sweep foundation), done-is-terminal, causal order (FIFO), rollback-discards-hook.
  - `tests/Feature/Platform/DomainEventRecorderTest.php` — modified 2 assertions (see re-point pattern above): the atomicity test now uses status-agnostic `EventDelivery::count()`; the fan-out test asserts pending/attempts-0 INSIDE the `DB::transaction()` closure (before the new hook delivers).
- **Quality loop:** green — format ✅ · InlineDeliveryTest 6/6 + DomainEventRecorderTest 11/11 ✅ · full suite **131/131 (489 assertions)**, +6 vs the 125 baseline ✅ · PHPStan level max 0 errors ✅ · Pint `--test` ✅ · `openspec validate --strict` ✅. `git status`: 2 modified (recorder + its test, not protected) + 3 new paths; `git diff main -- composer.json composer.lock` empty.
- **Design-D5 afterCommit resolution (REQUIRED record, tasks.md 4.1 + 6.2):** verified in vendor BEFORE writing. The landmine ("afterCommit may never fire under `RefreshDatabase`'s wrapper") does NOT exist on this framework: the testing `DatabaseTransactionsManager` returns `afterCommitCallbacksShouldBeExecuted($level) === ($level === 1)` and skips the wrapper, so the hook fires at the inner `DB::transaction()` commit under RefreshDatabase, and at level 0 under the base manager (DatabaseMigrations / production). **No fallback was needed.** I still used `DatabaseMigrations` (not RefreshDatabase) for the delivery tests because only there are real commit/rollback OBSERVABLE — which the atomicity (exactly-once) and rollback-discards-hook assertions require. Full trace in the Codebase Patterns above.
- **Acceptance walk (every bullet demonstrably true):** inline happy path → done, attempts 1, handler ran, cache marker present ✅ · R4 fan-out isolation → first (failing) consumer's row Pending+attempts1+last_error+backoff, sibling Done+attempts1, emitter's event intact ✅ · done-is-terminal → re-running `deliver()` over a `done` row leaves `$handled` and attempts unchanged ✅ · causal order → 3 events in one txn delivered in ascending `id` order ✅ · exactly-once for DB effects → success half (cache write + `done` co-committed, Test 1) AND failure half (failing consumer's cache write rolled back, count 0, Test 2) ✅ · happy + failure tested ✅ · quality green ✅.
- **Learnings for future iterations:**
  - **4.2 (`events:sweep`)** reuses this exact engine: the command selects DUE rows ordered `(consumer, domain_event_id)` (note: different order than `deliver()`'s `(domain_event_id, id)`) and runs each through the SAME per-delivery path — extract/expose `attempt()` (currently private) or add a `deliverDue()` that shares it. It also adds `config/events.php` (`sweep.max_attempts` 5 / `backoff_base_seconds` 30 / `backoff_cap_seconds` 3600) — the executor already reads them via `Config::integer(..., <same default>)`, so the file just makes them explicit + env-overridable (no executor change needed). 4.2's crash-recovery test mirrors `seedPendingDelivery()` (rows committed WITHOUT the recorder hook → sweep delivers them); backoff/dead-letter tests freeze time with `CarbonImmutable::setTestNow()` (NOT `$this->travelTo()` in a Pest closure) and assert the growing `available_at` + the `failed` terminal state at `max_attempts`.
  - `available_at` is set on EVERY failure including the dead-letter (`failed`) one — moot there since the sweep filters `status = pending`, but harmless. `recordFailure` uses `max(1, …)`/`max(0, …)` on the tunables to stay safe against a misconfigured 0.
  - The executor injects the **container contract** (not the concrete) and resolves consumers lazily at delivery time — so a consumer with constructor dependencies resolves correctly. The recorder injects the executor (no circular dep: recorder→registry+executor, executor→container).
  - Seven reusable patterns consolidated above (afterCommit-under-test landmine resolved · per-record hook · `Config::integer` typed-config · container-resolve+instanceof · execution-after-rollback resync · `Tests\Support\` named doubles · re-point-prior-tests).
---

## [2026-06-12 21:25] — 4.2 events:sweep + schedule + tunables
- **What:** The at-least-once delivery sweep (design D6) — the durable guarantee behind 4.1's inline fast path. `php artisan events:sweep` drains DUE deliveries through the SAME executor core 4.1 built, so a crash between the emitting commit and the inline hook loses no event and a retryable failure eventually delivers (or dead-letters). Closes group 4 (delivery execution) **2/2**.
- **Files changed (3 new, 3 modified):**
  - `app/Platform/Events/InlineDeliveryExecutor.php` — modified. Extracted the shared `dueDeliveries(): Builder<EventDelivery>` (the `pending` AND backoff-elapsed predicate) from `deliver()`; added `deliverDue(): void` (ALL due deliveries, ordered `(consumer, domain_event_id)` per D6) reusing `dueDeliveries()` + the existing private `attempt()`. `deliver()` now calls `dueDeliveries()->whereIn(...)->orderBy('domain_event_id')->orderBy('id')` — behavior unchanged (InlineDeliveryTest stayed green). Class docblock updated (two entry points, one due-definition, config now in `config/events.php`).
  - `app/Platform/Events/SweepCommand.php` — new. `$signature = 'events:sweep'`; `handle(InlineDeliveryExecutor $executor): int` → `deliverDue()` → `self::SUCCESS`. Dense docblock citing D1 (explicit registration) + D6.
  - `config/events.php` — new, platform-owned tunables: `sweep.max_attempts` (5), `sweep.backoff_base_seconds` (30, ×2), `sweep.backoff_cap_seconds` (3600), each `(int) env(...)`. The executor already read these via `Config::integer(..., <same default>)`, so this file just makes them explicit + env-overridable — **no executor change for the numbers** (only the doc/extraction edits above).
  - `bootstrap/app.php` — modified. `->withCommands([SweepCommand::class])` (auto-discovery only scans `app/Console/Commands`; D1 keeps platform commands under `App\Platform`).
  - `routes/console.php` — modified. `Schedule::command('events:sweep')->everyThirtySeconds()->withoutOverlapping();`
  - `tests/Feature/Platform/SweepTest.php` — new, `uses(DatabaseMigrations::class)`. 5 tests / 37 assertions + the prefixed global helper `sweepSeedDelivery(consumer, overrides, entityId)` (commits an event + a delivery row in a chosen state WITHOUT the recorder hook = the crash state).
- **Quality loop:** green — format ✅ · SweepTest 5/5 ✅ · full suite **136/136 (526 assertions)**, +5 vs the 131 baseline ✅ · PHPStan level max 0 errors ✅ · Pint `--test` ✅ · `openspec validate --strict` ✅. `git status`: 3 new + 3 modified (none protected); `git diff main -- composer.json composer.lock` empty.
- **Acceptance walk (every bullet demonstrably true):** crash recovery → seed a committed pending delivery with no hook, `Artisan::call('events:sweep')` → row `done`, attempts 1, handler ran ✅ · exponential backoff → attempt1 `available_at`=T0+30, a same-instant re-sweep SKIPS the not-yet-due row (attempts stay 1), attempt2 (T0+31) `available_at`=T0+91 with the window grown 30→60 ✅ · dead-letter → 5 failing sweeps (time advanced past each capped window) → `failed`/attempts 5, a further sweep leaves attempts unchanged (a `failed` row is never selected) ✅ · poison-no-stall → a RecordingConsumer poison row in backoff + a due RecordingConsumer row for the same consumer → the due one `done`, the poison skipped/untouched, only the due event handled (no per-consumer FIFO) ✅ · schedule entry exists → `Schedule::events()` introspection finds `events:sweep` with `repeatSeconds` 30 + `withoutOverlapping` true ✅ · happy + failure tested ✅ · quality green ✅.
- **Vendor-verified before writing (standing rule):** `ManagesFrequencies::everyThirtySeconds()`→`repeatEvery(30)`; `Schedule` facade → `Illuminate\Console\Scheduling\Schedule` bound SINGLETON (`FoundationServiceProvider`); `Schedule::events()` → typed `Event[]`; `Event::$command/$repeatSeconds/$withoutOverlapping` public; `Collection::sole(callable): TValue` (throws if 0/≥2); `ApplicationBuilder::withCommands()` partitions class-strings → `addCommands()`; `Kernel::bootstrap()`→`discoverCommands()` requires `commandRoutePaths` (= `routes/console.php`); `Kernel::shouldDiscoverCommands()` true (no custom `app/Console/Kernel.php`); `Artisan::call()` → `int`; the Pest `artisan()` helper → `PendingCommand|int`.
- **Learnings for future iterations:**
  - **5.1 (events:demo)** is the next sibling command — register it the SAME way (`->withCommands([..., DemoCommand::class])` in `bootstrap/app.php`; class under `App\Platform\Events\` or a `Demo/` subdir per D9). For its OUTPUT assertions use the Pest `artisan()` helper's `->expectsOutputToContain(...)->assertExitCode(0)` (the `PendingCommand` arm) — but for plain run+exit-code checks `Artisan::call()` stays cleaner (see the new Codebase Pattern). The demo command reuses the recorder→inline-delivery→audit→immutability-probe flow; the sweep is NOT part of the demo (the inline hook delivers it).
  - The sweep is now the safety net the recorder's docblock promised: a committed `pending` row with no (or a crashed) inline hook is delivered on the next tick. 5.1's E2E test can rely on EITHER path; the inline hook is the one it asserts (the sweep is its own SweepTest).
  - Six reusable patterns consolidated above (Laravel 13 scheduling + explicit command registration · schedule introspection in a Feature test · `Artisan::call` vs Pest `artisan()` return types · `?->` null-safe Carbon assertions + `setTestNow` across `Artisan::call` · share-the-due-predicate).
---

## [2026-06-12 22:05] — 5.1 Hello-world `events:demo` + E2E pipeline test
- **What:** The Workplan Phase 1 hello-world (design D9, founder-confirmed) — an end-to-end demonstration of "DB + event bus + audit trail" as both an operator-runnable command and the suite's single full-stack acceptance scenario. Opens group 5. In ONE transaction the demo makes a `cache` state change + records a synthetic domain event (`PlatformDemoRecorded`) + an audit record (`platform.demo`), all committing atomically; the recorder's post-commit hook then delivers the event inline to a registered demo consumer (its DB effect visible, ledger `done`); then it probes the immutability triggers (UPDATE + DELETE on `domain_events`, both rejected) and prints the trail, exiting 0. Every identifier is clearly synthetic — verbatim spec event names stay reserved for F2+ real module events.
- **Files changed (4 new, 1 modified):**
  - `app/Platform/Events/Demo/DemoConsumer.php` — new. The demo's inline consumer (DB-work-only, design D4/D5); idempotent `updateOrInsert` of a `cache` marker keyed by `event_id` (public `MARKER_PREFIX`). Named so the container resolves it by FQCN at delivery time exactly as a real module consumer.
  - `app/Platform/Events/Demo/DemoCommand.php` — new. `events:demo`; injects `DomainEventRecorder`/`AuditRecorder`/`ConsumerRegistry`; registers `DemoConsumer` on the singleton (idempotent) then runs record → post-commit delivery → immutability probe → trail. Public consts `EVENT_NAME`/`AUDIT_ACTION`/`STATE_KEY` (test-pinned, no magic-string drift); `probeRejected(Closure)` catches `QueryException` and reports `rejected (immutable)`.
  - `tests/Feature/Platform/HelloWorldPipelineTest.php` — new, `uses(DatabaseMigrations::class)`. 2 tests: the single full-stack scenario (atomic state+event+audit → inline delivery + consumer DB effect → ledger `done`, orchestrated directly via the substrate APIs as an independent witness) + the immutability tail (UPDATE/DELETE on the recorded event both `toThrow(QueryException, 'immutable')`, row unchanged).
  - `tests/Feature/Platform/EventsDemoCommandTest.php` — new, `uses(DatabaseMigrations::class)`. 2 tests: full-trail run (exit 0 + `Artisan::output()` contains the event/event_id/actor_role, audit, `done (attempts 1)`, both probes `rejected`) + re-runnability (two runs both exit 0; append-only tables gain 2 rows each, the idempotent state row stays 1, 2 deliveries `done`).
  - `bootstrap/app.php` — modified. `DemoCommand::class` added to the existing `withCommands([...])` (design D1 explicit registration, same as `SweepCommand`).
- **Quality loop:** green — format ✅ · the two new files 4/4 ✅ · full suite **140/140 (555 assertions)**, +4 vs the 136 baseline ✅ · PHPStan level max 0 errors ✅ · Pint `--test` ✅ · `openspec validate --strict` ✅. `git status`: 1 modified (`bootstrap/app.php`, not protected) + the new `Demo/` dir + 2 new test files; `git diff main -- composer.json composer.lock` empty.
- **Acceptance walk (every bullet demonstrably true):** E2E test exercises atomic record → inline delivery w/ consumer DB effect → ledger `done` in ONE scenario (HelloWorldPipelineTest test 1) ✅ · `events:demo` exit 0 + key output lines — event (name/event_id/actor_role), audit, delivery `done`, both probes rejected (EventsDemoCommandTest test 1) ✅ · synthetic identifiers (`PlatformDemoRecorded`, `platform.demo`, module `'platform'`) — grep-able via the public consts; real spec event names reserved for F2+ ✅ · happy + failure/edge (re-runnability second-run-exits-0 + the immutability tail) ✅ · quality green ✅. **Also ran the real command manually** against a throwaway migrated SQLite file (dev DB untouched): printed the full trail (`event_id: 019ebcae-…` UUIDv7, `actor_role: system`, `done (attempts 1)`, both probes `rejected (immutable)`), exit 0, second run also exit 0.
- **Design decision — pipeline test orchestrates the substrate APIs directly, NOT via the command:** design D9 frames the E2E test as "inside one `DB::transaction()` — a state change, a recorded demo event, an audit record …", i.e. the TEST drives the recorder/audit-recorder directly while the command does "the same flow" for operators. So `HelloWorldPipelineTest` is an INDEPENDENT witness of the substrate (a command-printing bug can't mask a pipeline regression), and `EventsDemoCommandTest` covers the operator surface. The ~6-line orchestration parallel between command and test is the standard test-vs-production shape; extracting a shared `DemoPipeline` service would add a layer D9 doesn't ask for (demonstration code, kept simple).
- **Verified before writing (standing rule):** `Artisan::call(): int` + `Artisan::output(): string` (facade `@method` + `Kernel::output()`); `Pest\Laravel\artisan()` is `@return PendingCommand|int` (confirms the PHPStan trap → used the facade pair); `PendingCommand::expectsOutputToContain`/`assertExitCode` exist (but unreachable through the union); `Query\Builder::updateOrInsert(array, array|callable)`; `cache` table columns (`key`/`value`/`expiration`); `QueryException` = `Illuminate\Database\QueryException`.
- **Learnings for future iterations:**
  - **5.2 (pgsql CI lane)** is next: add the `tests-pgsql` job to `.github/workflows/ci.yml` (`postgres:17` service, `pgsql, pdo_pgsql` extensions, `DB_CONNECTION=pgsql` job env) + extend `tests/Feature/CiWorkflowTest.php` with the new pins + document the second lane in `docs/development.md`. Verify the PHPUnit `<env>` non-force precedence before relying on it (design D8 fallback if it clobbers). The lane's first live run is the human push (the loop never observes it green; design D8).
  - The post-commit hook fires synchronously WITHIN `DB::transaction()`'s return at transactionLevel 0 (DatabaseMigrations / real CLI), so the command reads a `done` delivery immediately after the transaction block — no polling, no sweep needed for the demo (the inline hook delivers; the sweep is its own SweepTest).
  - Three reusable patterns consolidated above (Artisan output assertions at PHPStan max · re-runnable operator command over append-only tables · `DB::transaction` array-return shape).
---

## [2026-06-12 22:45] — 5.2 The `pgsql` CI lane
- **What:** The second CI engine lane (design D8) — a separate `tests-pgsql` job re-running `php artisan test` against **PostgreSQL 17** (the production-DB ADR floor), so the Postgres-truthful migration branches the SQLite dev lane can't exercise — the `actor_role` CHECK, the partial `WHERE status='pending'` index, the plpgsql immutability trigger functions — are proven on the engine that ships. Closes group 5 (hello-world + CI) **2/2**. The whole 3.x/4.x/5.x substrate is engine-agnostic; this lane is what turns "should work on PG" into a CI gate.
- **Files changed (3 modified, none protected):**
  - `.github/workflows/ci.yml` — added the `tests-pgsql` job after the untouched `quality` job: `services.postgres.image: postgres:17` (health-check `pg_isready`, throwaway creds `newco/newco/newco_test`, `ports: ['5432:5432']`), job-level `env:` (`DB_CONNECTION: pgsql`, `DB_HOST: 127.0.0.1`, `DB_PORT/DB_DATABASE/DB_USERNAME/DB_PASSWORD`), setup-php `extensions` + `pdo_pgsql, pgsql`, same checkout/cache/install/`.env`+`key:generate` steps, then `php artisan test`. The `quality` job is byte-for-byte unchanged except a header comment documenting the two lanes. Lint/PHPStan are NOT duplicated here (engine-independent, D8).
  - `tests/Feature/CiWorkflowTest.php` — +3 tests (5→8): pins the lane (`tests-pgsql:`, `image: postgres:17`, `pdo_pgsql`, `DB_CONNECTION: pgsql`, the distinct `name: Tests (Pest on PostgreSQL 17)` step); `substr_count` guards (pint/phpstan run-lines === 1, `php artisan test` run-line === 2) = the failure-ish case (duplicating the engine-independent gates into the new lane breaks the suite); a scoping/order pin (`quality` SQLite step before `tests-pgsql:` before `DB_CONNECTION: pgsql`) so the pre-existing `strpos` gate-order pins keep anchoring on the SQLite lane. The 5 pre-existing pins are unmodified (one got a clarifying comment, no assertion change).
  - `docs/development.md` — rewrote the CI section: two lanes, what each proves, the PG-17 = ADR-floor pin, the job-env-beats-phpunit.xml mechanism, and the "first live run = the human push" review expectation.
- **Quality loop:** green — format ✅ · CiWorkflowTest+DevelopmentDocsTest 14/14 ✅ · full suite **143/143 (565 assertions)**, +3 vs the 140 baseline ✅ · PHPStan level max 0 errors ✅ · Pint `--test` ✅ · `openspec validate --strict` ✅. `git status`: 3 modified (none protected); `git diff main -- composer.json composer.lock` empty.
- **D8 landmine RESOLVED — phpunit.xml `<env>` precedence (REQUIRED record, tasks.md 5.2 + design D8):** verified BOTH in vendor AND empirically. (1) `PHPUnit\TextUI\Configuration\PhpHandler::handleEnvVariables` (`vendor/phpunit/phpunit/src/TextUI/Configuration/PhpHandler.php:140`) only `putenv`s when `$force || getenv($name) === false` — our `phpunit.xml` `<env name="DB_CONNECTION" value="sqlite"/>` has NO `force`, so an already-set real env var is NOT clobbered. (2) `php artisan test` (Collision `TestCommand`) spawns the runner via Symfony `Process`; `Process::getDefaultEnv()` (`vendor/symfony/process/Process.php:1715`, `$env = getenv()`) + the `$env += $this->getDefaultEnv()` union at `:336` mean the subprocess INHERITS every parent OS env var, Collision's own vars merged on top — `clearEnv()` does not defeat it. (3) Laravel's immutable dotenv won't override a real var with `.env`'s `sqlite`. **Empirical proof (postgres-free):** a throwaway `tests/Feature/TmpEnvProbeTest.php` printing `getenv/env()/config('database.default')`, run twice — baseline `php artisan test --filter=…` → all `sqlite`; `DB_CONNECTION=pgsql php artisan test --filter=…` → all **`pgsql`**. The real job-env wins end-to-end; **no `--env` fallback needed** (the D8 fallback ladder stays unused). Probe deleted before commit (`git status` clean).
- **Acceptance walk (every bullet demonstrably true):** `CiWorkflowTest` pins the new job — job name ✅, `postgres:17` image ✅, the pgsql test run line (distinct step name + the `=== 2` count proving both lanes run the test gate) ✅, `DB_CONNECTION: pgsql` ✅ · ALL pre-existing pins pass unmodified (suite green, no assertion edited) ✅ · full local suite green on SQLite (143/143) ✅ · `docs/development.md` documents the second lane ✅ · quality commands green ✅ · the lane's first live run = the human push, recorded here + in the doc per D8 ✅.
- **Learnings for future iterations:**
  - **6.1 (`docs/event-substrate.md`)** + the `docs/module-template.md` § 6 refresh are next: the template's "a `pgsql` CI lane lands with the first domain migration (the next foundations change)" sentence can now point at the **now-existing** lane (decided-conventions-only — no new rule). The two-lane CI story is documented in `docs/development.md` already; `event-substrate.md` should reference it for the immutability-trigger PG-proof.
  - **6.2 (final sweep)** must record the scenario→test mapping for BOTH delta specs and re-confirm the two required proofs (the 1.1 arch red-proof + the D5 afterCommit resolution) are in this file — both are (1.1 entry @ 2026-06-12 14:26; D5 in the 4.1 entry + the Codebase Pattern). The pgsql lane itself proves the PG branches of 2.1 (CHECK), 2.3 (partial index), 2.4 (triggers) at the human push — none of those need new tests (engine-agnostic behavior assertions already cover them).
  - The pattern is consolidated above (separate-job + real-env-beats-phpunit.xml + the substr_count non-vacuity guard). A future second-service lane (e.g. Redis once the queue ADR lands) follows the same separate-job shape, never a matrix on `quality`.
---
