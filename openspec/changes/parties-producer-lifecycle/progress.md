# Progress — parties-producer-lifecycle

## Codebase Patterns
(consolidated reusable patterns — read first each iteration)

- **Run the full suite with `php -d memory_limit=512M vendor/bin/pest`** — NOT `php artisan test`. The default 128M OOMs in the Pest arch plugin (`nikic/php-parser` scans every module class via `ModuleBoundariesTest`); the crash surfaces as a misleading `NoTestCaseObjectOnCallStackException` / `stream_filter_remove` shutdown trace, but the head of the output reveals `Allowed memory size … exhausted`. The 512M runner is green (441/441). PHPStan + Pint run fine at default memory.
- **Unit tests that call `__()` (or any container service) must `uses(Tests\TestCase::class)`** at the top of the file. `tests/Pest.php` binds `TestCase` (+ optional `RefreshDatabase`) to `Feature` ONLY; files under `tests/Unit/**` default to plain PHPUnit and DON'T boot Laravel, so the translator is absent. For a translator-only test add `uses(TestCase::class)` WITHOUT `RefreshDatabase` (no DB needed → fast, and honours "no DB" tasks).
- **PHPStan max rejects Pest closure-dataset typing.** Threading a `Closure $factory` + a `class-string` (declared `string`) through `->with([...])` loses type info: `$factory()` is `mixed` (so `->getMessage()` fails) and `toBeInstanceOf(string)` wants `class-string`. For factory/exception assertions prefer **explicit per-case `it()` blocks** where the concrete return type and `::class` are visible — type-clean and reads like the repo's `EnumsTest`. (Don't eager-build exceptions inside a plain-array dataset either: datasets evaluate at collection time, before the app boots, so `__()` won't resolve.)
- **Transition-guard exception shape** (this change): `class Illegal{Entity}Transition extends RuntimeException` (non-`final`, matching the parties-core creation guards) with named static factories `::cannotX({Entity}Status $from): self` that return `new self((string) __('parties.<group>.<key>', ['state' => $from->value]))`. The `:state` placeholder is the enum **backing value** (e.g. `'active'`), a business token — NOT PII — so it is interpolated (cf. `DuplicateCustomerEmail`, which omits the PII email). Lang lives in grouped keys in `lang/en/parties.php`; ADD keys to existing groups (`club`, `producer_agreement`) rather than replacing them, and a new `producer` group.

---

## [2026-06-15 22:07] — 1.1 Transition-guard exceptions + localized copy
- **Implemented** the three supply-side transition-guard exceptions and their localized operator copy (the foundation tasks 2.x–4.x throw on out-of-state transitions):
  - `App\Modules\Parties\Exceptions\IllegalProducerTransition` — `::cannotActivate(ProducerStatus)`, `::cannotRetire(ProducerStatus)`.
  - `App\Modules\Parties\Exceptions\IllegalProducerAgreementTransition` — `::cannotActivate(ProducerAgreementStatus)`, `::cannotTerminate(ProducerAgreementStatus)`.
  - `App\Modules\Parties\Exceptions\IllegalClubTransition` — `::cannotSunset(ClubStatus)`, `::cannotClose(ClubStatus)`.
  - Each `extends RuntimeException` with rich docblocks citing design L2 + the per-entity Lifecycle requirement + the FSM; each factory resolves a localized reason naming the rule and interpolating the offending `:state` (`$from->value`, not PII).
- **Lang:** added a new `producer` group (`cannot_activate`, `cannot_retire`) and appended `cannot_sunset`/`cannot_close` to `club` and `cannot_activate`/`cannot_terminate` to `producer_agreement` in `lang/en/parties.php`. The pre-existing `club`/`producer_agreement`/`customer`/`profile` keys are preserved (asserted by a test).
- **Files changed:** `app/Modules/Parties/Exceptions/{IllegalProducerTransition,IllegalProducerAgreementTransition,IllegalClubTransition}.php` (new); `lang/en/parties.php` (+3 groups' keys); `tests/Unit/Modules/Parties/Exceptions/TransitionExceptionsTest.php` (new, 13 cases / 36 assertions).
- **Quality loop: green** — Pint ✓, filter ✓ (13/13), full suite ✓ (441/441 SQLite, +13 vs the 428 baseline), PHPStan max ✓ (0), Pint --test ✓. `openspec validate --strict` ✓. No migration / no composer drift (verified `git diff main`). No PG17 run required — task 1.1 touches no database (pure exceptions + translator; the unit test boots the app but uses no DB).
- **Learnings for future iterations:** see the four Codebase Patterns above. Biggest gotcha resolved: the full suite needs the **512M** memory runner; the default `php artisan test` crashes inside the arch test with a misleading shutdown trace, not a real test failure. Also: a translator-only unit test must `uses(TestCase::class)` (Pest binds TestCase to `Feature` only). The PHPStan-max-friendly way to assert factory outputs is explicit `it()` blocks, not a closure dataset.
---
