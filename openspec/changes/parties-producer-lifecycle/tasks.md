# Tasks — parties-producer-lifecycle

Read `proposal.md`, `design.md` (L1–L9) and the spec delta `specs/party-registry/spec.md` before any task. **Standing rules per task:** the five Quality Commands green at the end (format · test_filter · test · type_check · lint); `openspec validate parties-producer-lifecycle --strict` green; **no protected files touched**; **no new migration** (the state columns + their `CHECK`s ship from `parties-core` — this change writes transitions over them, L3); **no new Composer dependency** (`git diff main -- composer.json composer.lock` stays empty). **Verify every framework/vendor API in `vendor/`/`app/` before writing it** (lessons.md) — in particular `DomainEventRecorder::record(...)` returns the `DomainEvent` and takes `?int $causationId` / `?string $correlationId`. **Every DB-touching task is verified on a real PostgreSQL 17 before it is marked done** — SQLite-green is necessary, never sufficient (`knowledge/testing/rules.md`). **Boundary & persistence law:** every reference is within Module K (no cross-module import/join/event-consume); the Models stay persistence-only — the **transition Action is the sole writer of `status`** (L1), and no Action writes `party_type` / `producer_id` / the Club↔Producer link. **Scope guard:** implement ONLY supply-side (Producer/ProducerAgreement/Club) transitions; add NO Customer/Account/Profile transition, NO `originating_club_id` mutation, NO demand-side event.

## 1. Foundations (no transitions yet)

- [x] 1.1 **Transition-guard exceptions + localized copy** (design L2; spec — Requirements: Producer/ProducerAgreement/Club Lifecycle) — `App\Modules\Parties\Exceptions\{IllegalProducerTransition, IllegalProducerAgreementTransition, IllegalClubTransition}`, each `extends RuntimeException` with named static factories for its illegal transitions (`IllegalProducerTransition::cannotActivate(ProducerStatus $from)` / `::cannotRetire(...)`; `IllegalProducerAgreementTransition::cannotActivate(...)` / `::cannotTerminate(...)`; `IllegalClubTransition::cannotSunset(ClubStatus $from)` / `::cannotClose(...)`), each resolving a localized reason. Append new groups (`producer`, `producer_agreement`, `club`) to `lang/en/parties.php` with dotted keys + a `:state` placeholder (the state token is not PII).
  - Acceptance: each factory builds an exception whose message names the rule and the offending `:state`; copy lives in `lang/en/parties.php` (no hardcoded strings — invariant 12); the existing `club`/`producer_agreement`/`customer`/`profile` lang groups are preserved; quality commands green.
  - Test hint: `tests/Unit/Modules/Parties/Exceptions/TransitionExceptionsTest.php` (no `RefreshDatabase`) — assert each factory returns the right class and a non-empty resolved message containing the state token; assert the lang keys resolve via `__('parties.producer.cannot_activate', ['state' => 'active'])`.

- [x] 1.2 **`Producer::clubs()` within-module relation** (design L6; spec — Requirement: Producer Lifecycle) — add a `clubs(): HasMany` to `app/Modules/Parties/Models/Producer.php` (`@return HasMany<Club, $this>` + `@property-read Collection<int, Club> $clubs`), the read the retirement cascade walks. Within-module (Club is Module K) — boundary-clean.
  - Acceptance: `$producer->clubs` returns the Producer's Clubs; arch tests (`ModuleBoundariesTest`) stay green unamended; PHPStan max clean (typed relation generic); quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/ProducerLifecycleTest.php` (`uses(RefreshDatabase::class)`) — factory a Producer with three Clubs and assert `$producer->clubs()->count() === 3`.

## 2. Club lifecycle (the cascade's single `ClubSunset` writer comes first)

- [ ] 2.1 **`SunsetClub` + `ClubSunset`** (design L1/L2/L4/L6; spec — Requirements: Club Lifecycle, Supply-Side Lifecycle Events) — `App\Modules\Parties\Events\ClubSunset` (`NAME='ClubSunset'`, `ENTITY_TYPE='Club'`, static `payload(Club): array` → `{club_id, producer_id, status}`); `App\Modules\Parties\Actions\SunsetClub` with `handle(int $clubId, ?int $causationId = null, ?string $correlationId = null): Club` — one `DB::transaction`: `lockForUpdate` re-read, assert `status === Active` (else `IllegalClubTransition::cannotSunset`), set `Sunset`, record `ClubSunset` (passing through `$causationId`/`$correlationId` so the cascade can thread it; standalone callers pass null → root event).
  - Acceptance: spec scenarios "Sunset an active Club" and (negative half of) "Illegal Club transitions are rejected" pass; the `ClubSunset` is recorded in the same transaction, module `parties`, PII-free; a sunset on a non-`active` Club throws and records nothing; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/ClubLifecycleTest.php` — factory an `active` Club, `SunsetClub`, assert `status === ClubStatus::Sunset` and one `ClubSunset` row in `domain_events` (assert payload keys = `{club_id, producer_id, status}`, no PII); factory a `sunset` Club and assert `SunsetClub` throws `IllegalClubTransition` and no second event is recorded.

- [ ] 2.2 **`CloseClub` + `ClubClosed`** (design L1/L2/L4/L8; spec — Requirement: Club Lifecycle) — `App\Modules\Parties\Events\ClubClosed` (`NAME='ClubClosed'`, `ENTITY_TYPE='Club'`, `payload(Club)` → `{club_id, producer_id, status}`); `App\Modules\Parties\Actions\CloseClub::handle(int $clubId): Club` — `DB::transaction`: `lockForUpdate` re-read, assert `status === Sunset` (else `IllegalClubTransition::cannotClose`), set `Closed`, record `ClubClosed`. NO all-members-gone gate (deferred seam — L8); document the seam in the class docblock.
  - Acceptance: spec scenarios "Close a sunset Club" and the `active`-Club half of "Illegal Club transitions are rejected" pass; closing an `active` Club directly throws (must pass through `sunset`); the seam (no Profile-state gate) is documented; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/ClubLifecycleTest.php` — factory a `sunset` Club, `CloseClub`, assert `status === ClubStatus::Closed` and one `ClubClosed` row; factory an `active` Club and assert `CloseClub` throws `IllegalClubTransition` (cannot skip `sunset`).

## 3. Producer lifecycle (retirement reuses `SunsetClub`)

- [ ] 3.1 **`ActivateProducer` + `ProducerActivated`** (design L1/L2/L4/L8; spec — Requirement: Producer Lifecycle) — `App\Modules\Parties\Events\ProducerActivated` (`NAME='ProducerActivated'`, `ENTITY_TYPE='Producer'`, `payload(Producer)` → `{producer_id, status}`); `App\Modules\Parties\Actions\ActivateProducer::handle(int $producerId): Producer` — `DB::transaction`: `lockForUpdate` re-read, assert `status === Draft` (else `IllegalProducerTransition::cannotActivate`), set `Active`, record `ProducerActivated`. NO KYC gate (deferred seam — L8); document the seam in the docblock.
  - Acceptance: spec scenarios "Activate a draft Producer", "Activation enforces no KYC gate in this slice" and the activate-half of "Illegal Producer transitions are rejected" pass; activation succeeds with no KYC verdict present; a non-`draft` activation throws and records nothing; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/ProducerLifecycleTest.php` — factory a `draft` Producer, `ActivateProducer`, assert `status === ProducerStatus::Active` and one `ProducerActivated` row (payload `{producer_id, status}`, no PII); factory an `active` Producer and assert `ActivateProducer` throws `IllegalProducerTransition`.

- [ ] 3.2 **`RetireProducer` + `ProducerRetired` + Club-sunset cascade** (design L1/L2/L4/L5/L6; spec — Requirements: Producer Lifecycle, Supply-Side Lifecycle Events) — `App\Modules\Parties\Events\ProducerRetired` (`NAME='ProducerRetired'`, `ENTITY_TYPE='Producer'`, `payload(Producer)` → `{producer_id, status}`); `App\Modules\Parties\Actions\RetireProducer::handle(int $producerId): Producer` — one `DB::transaction`: `lockForUpdate` re-read, assert `status === Active` (else `IllegalProducerTransition::cannotRetire`), set `Retired`, record `ProducerRetired` (capture `$retired`), then for each `Producer::clubs()` row with `status === Active` call `SunsetClub->handle($club->id, causationId: $retired->id, correlationId: $retired->correlation_id)`. Clubs already `sunset`/`closed` are skipped (idempotent). Profile leg NOT performed (deferred — L6).
  - Acceptance: spec scenarios "Retire an active Producer cascades Club sunset", the retire-half of "Illegal Producer transitions are rejected", and "Cascade events are causally linked to the retirement" pass; one `ProducerRetired` + one `ClubSunset` per `active` Club, each `ClubSunset.causation_id === $retired->id` and same `correlation_id`; an already-`closed` Club is untouched and emits nothing; the whole cascade is one transaction (rolls back together on failure); no Profile is mutated; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/ProducerLifecycleTest.php` — factory an `active` Producer operating two `active` Clubs + one `closed` Club; `RetireProducer`; assert Producer `Retired`, both `active` Clubs `Sunset`, the `closed` Club unchanged; query `domain_events` for one `ProducerRetired` and exactly two `ClubSunset` rows whose `causation_id` equals the `ProducerRetired` row's `id`; assert a non-`active` Producer retire throws and emits nothing.

## 4. ProducerAgreement lifecycle (scope-aware supersession)

- [ ] 4.1 **`ActivateProducerAgreement` + activation/supersession events** (design L1/L2/L4/L5/L7; spec — Requirements: ProducerAgreement Lifecycle, Supply-Side Lifecycle Events) — `App\Modules\Parties\Events\ProducerAgreementActivated` (`payload` → `{producer_agreement_id, producer_id, club_id, status, supersedes}`, `supersedes` = the superseded agreement id or null) and `App\Modules\Parties\Events\ProducerAgreementSuperseded` (`payload` → `{producer_agreement_id, producer_id, club_id, status, superseded_by}`); `App\Modules\Parties\Actions\ActivateProducerAgreement::handle(int $agreementId): ProducerAgreement` — `DB::transaction`: `lockForUpdate` re-read, assert `status === Draft` (else `IllegalProducerAgreementTransition::cannotActivate`); find the prior `active` in the SAME scope — `where('producer_id', …)` plus **NULL-safe** `club_id` (`whereNull('club_id')` when this agreement's `club_id` is null, else `where('club_id', $clubId)`) — `lockForUpdate`; if found, set it `Superseded`; set this agreement `Active`; record `ProducerAgreementActivated` first (capture `$activated`, `supersedes` = prior?->id), then (if a prior existed) `ProducerAgreementSuperseded` with `superseded_by` = this agreement id, `causationId: $activated->id`, `correlationId: $activated->correlation_id`.
  - Acceptance: spec scenarios "Activate a draft agreement with no prior active in scope", "Activating a replacement supersedes the prior active in the same scope", "Scope isolation between Producer-wide and Club-narrowed agreements", "Supersession events pair old and new", and the activate-half of "Illegal ProducerAgreement transitions are rejected" pass; the supersession query is NULL-safe (a second Producer-wide activation supersedes the first); the pair links in payload + causation; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/ProducerAgreementLifecycleTest.php` — (a) activate a lone `draft` → `Active`, one `ProducerAgreementActivated` (`supersedes` null), no superseded event; (b) two Producer-wide (`club_id` null) agreements — activate A, then activate B; assert A `Superseded`, B `Active`, `ProducerAgreementSuperseded.superseded_by === B.id`, `ProducerAgreementActivated(B).supersedes === A.id`, and the supersession's `causation_id` equals B's activation event id; **(c) scope isolation** — an `active` Producer-wide agreement + an `active` `club_id=C` agreement coexist; activate a new `club_id=C` draft; assert only the `club_id=C` prior is superseded and the Producer-wide stays `Active` (this is the PG NULL-distinctness trap — assert on PG17). Assert a non-`draft` activation throws.

- [ ] 4.2 **`TerminateProducerAgreement` + `ProducerAgreementTerminated`** (design L1/L2/L4; spec — Requirement: ProducerAgreement Lifecycle) — `App\Modules\Parties\Events\ProducerAgreementTerminated` (`payload` → `{producer_agreement_id, producer_id, club_id, status}`); `App\Modules\Parties\Actions\TerminateProducerAgreement::handle(int $agreementId): ProducerAgreement` — `DB::transaction`: `lockForUpdate` re-read, assert `status === Active` (else `IllegalProducerAgreementTransition::cannotTerminate`), set `Terminated`, record `ProducerAgreementTerminated`. NO cascade to Producer state.
  - Acceptance: spec scenarios "Terminate an active agreement without cascading" and the terminate-half of "Illegal ProducerAgreement transitions are rejected" pass; the Producer's status is unchanged; a non-`active` termination throws and records nothing; quality commands green; **verified on PG17**.
  - Test hint: `tests/Feature/Modules/Parties/ProducerAgreementLifecycleTest.php` — factory an `active` agreement under an `active` Producer; `TerminateProducerAgreement`; assert agreement `Terminated`, one `ProducerAgreementTerminated` row, Producer still `Active`; assert a `draft` agreement termination throws.

## 5. Integration, docs & cross-engine close

- [ ] 5.1 **Docs — CONTEXT.md terms + Parties lifecycle contract note** (proposal "What Changes"/Impact) — extend `CONTEXT.md` with the resolved supply-side lifecycle terms (Club sunset/close, Producer retire, agreement supersede, the `(producer_id, club_id)` agreement scope) and add a Parties contract note documenting the seven lifecycle event payloads (PII-free) and the two deferred seams (KYC-on-activation → `parties-compliance`; all-members-gone-on-close → demand-side). No code.
  - Acceptance: `CONTEXT.md` carries the new terms with verbatim spec anchors; the contract note lists the seven events + payload shapes + the two seams; no canonical-term drift from `CONTEXT.md`/CLAUDE.md; quality commands green.
  - Test hint: docs-only — no test; verify by `openspec validate parties-producer-lifecycle --strict` and a terminology re-read against the spec.

- [ ] 5.2 **Full supply-side lifecycle chain + cross-engine close** (spec — Requirements: all four, MODIFIED Birth States) — one feature test driving the whole supply-side: create (via `Create*`) → activate Producer → create/activate an agreement, then a renewal that supersedes it → sunset + close a Club → retire the Producer (cascading sunset on its remaining `active` Clubs); assert each transition recorded exactly its verbatim event with the right causation links, that the spine's **creation** chain still emits no lifecycle event (`SpineCreationChainTest` unamended and green), and that the **demand-side stays inert** (no Customer/Account/Profile transition exists; `originating_club_id` has no mutation surface). Then run the whole Parties suite + the two arch tests on PostgreSQL 17.
  - Acceptance: the chain records exactly the seven supply-side event names with correct causation/correlation on the two derived chains and **zero** demand-side lifecycle events; the MODIFIED requirement's "Supply-side transitions exist; demand-side transitions do not" scenario holds (assert via reflection that the Parties Actions expose supply-side transitions but no Customer/Account/Profile transition or `originating_club_id` setter); `ModuleBoundariesTest` + `ModulePersistenceConventionsTest` green (no amendment); the **entire** Parties test set is green on SQLite **and** on a local PostgreSQL 17 (`php -d memory_limit=512M vendor/bin/pest`; docker `postgres:17` on `:55432` per `knowledge/testing/rules.md`); record the PG run in `progress.md`.
  - Test hint: `tests/Feature/Modules/Parties/SupplyLifecycleChainTest.php` — assert `domain_events` contains the seven supply-side names with the expected counts, the cascade `ClubSunset` rows and the `ProducerAgreementSuperseded` row carry the right `causation_id`, and `where('name','like','OriginatingClubLocked')->count() === 0` and no `CustomerActivated`/`ProfileActivated`; reflect the Parties `Actions/` namespace and assert no `Customer`/`Profile` transition class exists in this change.
