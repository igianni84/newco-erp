# Tasks — foundations-modules-skeleton

> Every task: quality commands green at the end (format · test_filter · test · type_check · lint), `openspec validate foundations-modules-skeleton --strict` green, no protected files touched, zero composer.json/composer.lock churn (this change adds NO dependencies). Read `design.md` D1–D8 before starting any task.

## 1. Module Skeleton

- [x] 1.1 Create the canonical module registry `app/Modules/Module.php`: string-backed enum, nine cases (`Catalog, Parties, Allocation, Procurement, Commerce, Inventory, Fulfilment, Finance, OperatorPanel` — values `'catalog'` … `'operator_panel'`, snake_case), `letter(): string` returning the spec letter per the CLAUDE.md table (`0, K, A, D, S, B, C, E, Admin`) and `namespace(): string` returning `App\Modules\{CaseName}` (design D2).
  - Acceptance: enum exists, type-clean at PHPStan level max; unit tests cover happy path and at least one failure case; all quality commands green.
  - Test hint: `tests/Unit/Modules/ModuleTest.php` (Pest) — exactly 9 cases; full letter mapping asserted verbatim (Catalog→'0', Parties→'K', Allocation→'A', Procurement→'D', Commerce→'S', Inventory→'B', Fulfilment→'C', Finance→'E', OperatorPanel→'Admin'); `namespace()` shape for at least Catalog and OperatorPanel; failure case: `Module::from('warehouse')` throws `ValueError`.
- [ ] 1.2 Create the nine module directories `app/Modules/{Name}/Providers/`, each with `{Name}ServiceProvider extends ServiceProvider` (empty `register()`/`boot()` — the module wiring seam, design D1), and register all nine in `bootstrap/providers.php` (keep the existing `AppServiceProvider` and `AdminPanelProvider` lines; the panel moves in 1.3, not here).
  - Acceptance: `php artisan about` exits 0; all nine module providers loaded at boot; feature tests cover happy path and one failure case; quality commands green.
  - Test hint: `tests/Feature/Modules/ModuleProvidersTest.php` — iterate `Module::cases()`: each `{Name}ServiceProvider` FQCN is present among the loaded providers (verify the exact introspection API in vendor first — `Illuminate\Foundation\Application::getLoadedProviders()` returns an array keyed by provider FQCN) and the class file exists at `app/Modules/{Name}/Providers/`; failure case: a fabricated `App\Modules\Warehouse\Providers\WarehouseServiceProvider` is NOT among the loaded providers.
- [ ] 1.3 Relocate the Filament panel into its owning module (design D5): move `app/Providers/Filament/AdminPanelProvider.php` → `app/Modules/OperatorPanel/Providers/AdminPanelProvider.php` (namespace `App\Modules\OperatorPanel\Providers`; class body otherwise unchanged — panel id `admin`, path `admin`, `->login()`, default Dashboard), update `bootstrap/providers.php`, delete the now-empty `app/Providers/Filament/` directory.
  - Acceptance: the ENTIRE pre-existing suite passes UNMODIFIED — especially `tests/Feature/OperatorPanelTest.php` and `tests/Feature/OperatorSeederTest.php` (run them before and after the move); old FQCN gone; quality commands green.
  - Test hint: extend `tests/Feature/Modules/ModuleProvidersTest.php`: `class_exists(\App\Modules\OperatorPanel\Providers\AdminPanelProvider::class)` is true and the provider is loaded; `class_exists('App\Providers\Filament\AdminPanelProvider')` is false; the `admin` Filament panel is registered (verify the panel-introspection API in vendor — e.g. `Filament\Facades\Filament::getPanel('admin')` — before writing).

## 2. Boundary Enforcement (architecture tests)

- [ ] 2.1 Stand up the Architecture test suite (design D8): create `tests/Architecture/ModuleConformanceTest.php` (registry ↔ filesystem conformance: the set of directories directly under `app/Modules/` equals exactly the nine `Module::cases()` names, plus `Module.php` as the only loose file) and add an `Architecture` testsuite to `phpunit.xml` so `php artisan test` runs it by default. Check whether `tests/Pest.php` needs a binding for the new directory.
  - Acceptance: `php artisan test` output includes the Architecture tests; `php artisan test --filter={name}` still selects single tests (CLAUDE.md `test_filter`); set-equality assertion (not subset) so both a missing AND an extra tenth directory fail; quality commands green.
  - Test hint: build the expected set from `Module::cases()` (never hardcode names); compare against `scandir(app_path('Modules'))` filtered; failure path is proven by the set-equality itself — temporarily `mkdir app/Modules/Warehouse` → red, remove → green; record both outputs in `progress.md`.
- [ ] 2.2 Write the cross-module privacy arch test (design D3): for every module M, code in `App\Modules\{M}` must not use any other module's namespace EXCEPT the target's `Contracts\*` and `Events\*`. Iterate `Module::cases()` for both sides — no hardcoded lists. Verify the pest-plugin-arch expectation API against `vendor/pestphp/pest-plugin-arch/src/` before writing (e.g. `expect(...)->not->toUse([...])->ignoring([...])` — confirm names, don't assume).
  - Acceptance: suite green on current code; RED-PROOF demonstrated and recorded in `progress.md`: (a) temporary `app/Modules/Catalog/Tmp.php` referencing `App\Modules\Parties\Providers\PartiesServiceProvider` → arch test FAILS naming the pair; (b) replace the reference with a temporary `App\Modules\Parties\Contracts\TmpContract` interface → arch test PASSES (public surface allowed); both temp files removed afterward (`git status` clean); quality commands green.
  - Test hint: `tests/Architecture/ModuleBoundariesTest.php`; one expectation per source module (loop over the enum) so a failure names the offending module; the ignore-list for module M = every other module's `\Contracts` and `\Events` namespace.
- [ ] 2.3 Write the platform-direction arch test (design D3): the enumerated platform namespaces (`App\Providers`, `App\Models`, `App\Http`) must not use `App\Modules\*`. Keep the platform-namespace list a single named constant/array in the test so the template can cite where to extend it (`bootstrap/providers.php` is outside `app/` classes — naturally out of arch scope; do not special-case it).
  - Acceptance: green on current code; RED-PROOF recorded in `progress.md`: temporary class in `app/Providers/` importing `App\Modules\Catalog\Providers\CatalogServiceProvider` → FAILS; removed → green; quality commands green.
  - Test hint: extend `tests/Architecture/ModuleBoundariesTest.php` (same file, separate expectation block); target `App\Modules` as a whole — any module symbol is a violation from platform code.
- [ ] 2.4 Write the forward-binding persistence-convention test (design D6): every class under `App\Modules\**` extending `Illuminate\Database\Eloquent\Model` must declare an explicit `$table` starting with its owning module's prefix (the `Module` enum backing value + `_`, e.g. `catalog_`). Zero module models exist today: the test must prove the scan executed over the module tree and found 0 models — pass-by-proven-empty, never pass-by-error or skip.
  - Acceptance: green (proven-empty path); RED-PROOF ×3 recorded in `progress.md`: temp model in Catalog with `$table = 'product_masters'` → FAILS (missing prefix); temp model with no explicit `$table` → FAILS; temp model with `$table = 'catalog_product_masters'` → PASSES; all temp files removed (`git status` clean); quality commands green.
  - Test hint: `tests/Architecture/ModulePersistenceConventionsTest.php`; discover candidate classes by globbing `app/Modules/*/**/*.php`, map path→FQCN, filter `class_exists` + `ReflectionClass::isSubclassOf(Model::class)` (skip abstract); read the default via `ReflectionClass::getDefaultProperties()['table']`; derive the required prefix from the file's module segment through the `Module` enum.

## 3. Module-Build Template (docs)

- [ ] 3.1 Write `docs/module-template.md` — the falsariga for every F2+ module change — with exactly the nine sections enumerated in design D7: (1) modules + spec letters table; (2) canonical layout tree + lazy-creation rule; (3) public surface & boundary law + amendment protocol; (4) service-provider conventions; (5) operator-surface placement; (6) persistence conventions (incl. migration-policy reference to the DB ADR); (7) terminology & naming cascade (CONTEXT.md glossary of record, Module 0 § 18 source-of-truth); (8) test patterns; (9) add-a-module-slice checklist. Document DECIDED conventions only (design D1–D8) — invent nothing new. Add the `docs/INDEX.md` row and a one-line cross-reference in `docs/development.md`.
  - Acceptance: all nine sections present and consistent with design.md; INDEX row added; development.md cross-reference added; doc-pin tests cover happy path and one failure-ish case; quality commands green.
  - Test hint: `tests/Feature/ModuleTemplateDocsTest.php` mirroring the existing `DevelopmentDocsTest` style — pins: file exists; every `Module::cases()` name AND its spec letter appear (registry-completeness, iterate the enum — this is the failure-proof if a module were dropped from the doc); the strings `Contracts` + `Events` appear in the public-surface section; the literal example prefix `catalog_` appears in the persistence section; `CONTEXT.md` is referenced.

## 4. Final Quality Pass

- [ ] 4.1 Traceability + full-quality sweep: run the five Quality Commands in CLAUDE.md table order, all green; `openspec validate foundations-modules-skeleton --strict` green; walk every `#### Scenario:` in `specs/module-architecture/spec.md` and record a scenario → covering-test mapping table in `progress.md` (if any scenario lacks a covering test or recorded red-proof, add it on the spot); confirm `git status` shows no protected-file modification and `git diff main -- composer.json composer.lock` is empty (zero new dependencies).
  - Acceptance: five commands green in table order; validate --strict green; the scenario→test mapping table is in `progress.md` with no uncovered scenario; no protected files touched; composer.json/lock identical to main.
  - Test hint: this task adds no new tests unless the mapping finds a gap; the deliverable is the recorded mapping + the green sweep.


