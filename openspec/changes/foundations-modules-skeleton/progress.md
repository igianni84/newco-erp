# Progress — foundations-modules-skeleton

## Codebase Patterns
(consolidated reusable patterns — read first each iteration)

- **Module registry is the single source for "the nine modules".** `App\Modules\Module` (string-backed enum, `app/Modules/Module.php`): case name = namespace segment (`namespace()` → `App\Modules\{CaseName}`), backing value = snake_case table prefix (`->value`, e.g. `catalog` → table prefix `catalog_`, design D6), `letter()` = spec letter (`0,K,A,D,S,B,C,E,Admin`). Every architecture/conformance test MUST iterate `Module::cases()` — never a hardcoded module list — so module-set drift fails loudly.
- **Unit tests need no Laravel boot.** `tests/Pest.php` binds `Tests\TestCase` only to `Feature`; `tests/Unit/**` runs on plain PHPUnit `TestCase`. Pure-PHP tests (enums, value objects) belong in `tests/Unit/`. Pest `it()/expect()/->and()` globals work in both suites regardless of the binding.
- **Strong verbatim map assertion.** `expect($builtMap)->toBe([...])` uses strict `===` (order- + type-sensitive): asserts completeness, values, AND declaration order in one shot. Build the map by iterating `Module::cases()` keyed by `->name`, compare against the literal in spec order.
- **Quality loop is green-clean today:** `vendor/bin/pint <files>` → `php artisan test <path>` → `php artisan test` → `vendor/bin/phpstan analyse` (level max, 0 baseline) → `vendor/bin/pint --test`. App files follow Laravel idiom: no `declare(strict_types=1)`, Pint laravel preset (no spaces around `.` concatenation).
- **Providers are DERIVED from the registry, not listed.** `bootstrap/providers.php` spreads `array_map(fn (Module $m) => $m->providerClass(), Module::cases())` after the two platform providers — so adding/removing a module never edits the composition root by hand. `Module::providerClass()` owns the wiring-seam FQCN convention (`{namespace}\Providers\{Name}ServiceProvider`); both the composition root and the conformance test consume it (single source of truth). The composition root is boundary-exempt by design (D3) — it references module provider FQCNs on purpose. Each module's standard provider `extends Illuminate\Support\ServiceProvider` (NOT Filament's `PanelProvider` — that's the separate `AdminPanelProvider`, design D5) with empty `register()/boot()` = the seam F2+ fills.
- **Assert boot-time wiring via `app()->getLoadedProviders()`** — an FQCN-keyed array (`[Provider::class => true]`), Laravel API verified at `Application.php:1477` (`providerIsLoaded(string)` at 1488 is the single-key variant). Non-deferred providers (no `DeferrableProvider`) are eagerly registered, so empty seam providers appear there. Use `expect($loaded)->toHaveKeys($all)` for completeness and `->not->toHaveKey($fabricated)` for the negative (Pest `Mixins/Expectation.php:628/661`; `not->toHaveKey` via `OppositeExpectation::__call`). A non-existent provider FQCN in `bootstrap/providers.php` fails LOUD: the app can't boot → the whole Feature suite errors.
- **Relocating/renaming any `App\**` class → run `composer dump-autoload` in the same iteration.** The `App\` PSR-4 map resolves the *new* path at runtime for free, but Composer's non-authoritative classmap (`vendor/composer/autoload_classmap.php`) keeps a STALE entry pointing the OLD FQCN at the deleted file. `findFile()` checks the classmap before PSR-4 and returns the stale path without an existence check → `class_exists('Old\FQCN')` tries to `include` a missing file → PHP warning → PHPUnit/Pest escalates it to a test failure. `composer dump-autoload` regenerates the classmap (drops the stale entry); it touches only `vendor/composer/autoload_*.php` — never `composer.json`/`composer.lock`, and `vendor/` is gitignored, so zero commit/dependency churn. Verify with `php -r "require 'vendor/autoload.php'; var_dump(class_exists('Old\FQCN'));"` → must be `false` with no warning. Filament panel introspection for tests: `Filament\Facades\Filament::getPanel('admin')->getId()` (`FilamentManager.php:372` → `PanelRegistry::get($id, $isStrict=true)`; `Panel::getId()` via `HasId`).

---

## [2026-06-12 11:05] — 1.1 canonical module registry enum
- **Implemented:** `App\Modules\Module` string-backed enum (design D2) — nine cases `Catalog…OperatorPanel` with snake_case backing values (`catalog`…`operator_panel`), `letter()` (exhaustive `match($this)` → spec letters `0,K,A,D,S,B,C,E,Admin`), `namespace()` (`__NAMESPACE__.'\\'.$this->name` → `App\Modules\{CaseName}`, drift-proof vs the file's own namespace).
- **Files changed:** `app/Modules/Module.php` (new), `tests/Unit/Modules/ModuleTest.php` (new, 5 tests / 6 assertions), `tasks.md` (1.1 ✓).
- **Tests:** count=9; full letter map verbatim via `toBe`; full snake_case value map via `toBe` (pins the D6 table-prefix contract task 2.4 depends on); `namespace()` for Catalog + OperatorPanel; failure case `Module::from('warehouse')` throws `ValueError`.
- **Quality loop:** green — format ✅ · filtered 5/5 ✅ · full suite 41/41 (105 assertions, was 36/99) ✅ · phpstan 0 @ level max ✅ · pint --test ✅ · `openspec validate --strict` ✅. composer.json/lock vs main: empty diff (zero new deps). No protected files touched.
- **Learnings for future iterations:**
  - The enum uses an **exhaustive `match($this)`** for `letter()` (no `default` arm): adding a tenth case without its letter is a PHPStan-detectable hole, not a silent fallthrough — keep it exhaustive.
  - `namespace()` uses `__NAMESPACE__` rather than a hardcoded `'App\Modules'` literal, so it can't drift from where the registry physically lives; tests pin the concrete output for two cases.
  - Next: 1.2 creates the nine `Providers/` dirs + `{Name}ServiceProvider`s and registers them in `bootstrap/providers.php` (keep `AppServiceProvider` + `AdminPanelProvider`; panel relocates in 1.3). Use `Module::cases()` + `->namespace()` to drive that test too.
---

## [2026-06-12 10:57] — 1.2 nine module service providers + registration
- **Implemented:** the nine standard module wiring seams (design D1). Each `app/Modules/{Name}/Providers/{Name}ServiceProvider` `extends Illuminate\Support\ServiceProvider` with empty `register()/boot()` (matches `AppServiceProvider` idiom — no `declare(strict_types=1)`, class docblock naming it the seam). Registered all nine in `bootstrap/providers.php` **derived from the registry** — `...array_map(fn (Module $m) => $m->providerClass(), Module::cases())` after the kept `AppServiceProvider` + `AdminPanelProvider` (panel stays at `App\Providers\Filament` until 1.3). OperatorPanel gets the standard `OperatorPanelServiceProvider` (distinct from the Filament `AdminPanelProvider`, design D5).
- **Elegance choice:** added `Module::providerClass(): string` (`{namespace}\Providers\{Name}ServiceProvider`) so the composition root AND the conformance test both derive the provider FQCN from the registry — no duplicated FQCN-construction string, registry stays the single source of truth (D2). Pinned in the unit `ModuleTest` (Catalog + OperatorPanel, mirroring the `namespace()` pin).
- **Files changed:** `app/Modules/Module.php` (+`providerClass()`), `bootstrap/providers.php` (registry-driven spread), `tests/Unit/Modules/ModuleTest.php` (+1 pin), nine new `app/Modules/{Name}/Providers/{Name}ServiceProvider.php`, `tests/Feature/Modules/ModuleProvidersTest.php` (new, 3 tests), `tasks.md` (1.2 ✓).
- **Tests:** `ModuleProvidersTest` (Feature, booted) — (1) `app()->getLoadedProviders()` `toHaveKeys` all nine `providerClass()`; (2) every provider `class_exists` + file at `app/Modules/{Name}/Providers/`; (3) failure: fabricated `App\Modules\Warehouse\…\WarehouseServiceProvider` `not->toHaveKey`. All iterate `Module::cases()` — no hardcoded list.
- **Quality loop:** green — format ✅ · filtered 9/9 ✅ · full suite **45/45** (135 assertions, was 41/105) ✅ · phpstan 0 @ level max ✅ · pint --test ✅ · `php artisan about` exit 0 ✅ · `openspec validate --strict` ✅. `git diff main -- composer.json composer.lock` empty (zero new deps). No protected files touched.
- **Learnings for future iterations:**
  - `getLoadedProviders()` (`Application.php:1477`) returns `[FQCN => true]`; non-deferred providers are eagerly registered so empty seams show up. A bad FQCN in `bootstrap/providers.php` fails LOUD (app can't boot → whole Feature suite errors) — so the registry-driven spread can't silently mis-register.
  - 1.3 next: relocate `app/Providers/Filament/AdminPanelProvider.php` → `app/Modules/OperatorPanel/Providers/AdminPanelProvider.php` (namespace `App\Modules\OperatorPanel\Providers`, body unchanged), update the `use` in `bootstrap/providers.php`, delete the empty `app/Providers/Filament/`. The pre-existing `OperatorPanelTest`/`OperatorSeederTest` must pass UNMODIFIED — run before & after. `class_exists('App\Providers\Filament\AdminPanelProvider')` must become false; the `admin` Filament panel must still register (verify `Filament\Facades\Filament::getPanel('admin')` in vendor before asserting).
---

## [2026-06-12 11:05] — 1.3 relocate Filament panel into OperatorPanel module
- **Implemented:** moved the Filament panel provider into its owning module (design D5). New `app/Modules/OperatorPanel/Providers/AdminPanelProvider.php` (namespace `App\Modules\OperatorPanel\Providers`) — **class body byte-for-byte unchanged** (panel id `admin`, path `admin`, `->login()`, Amber primary, default `Dashboard`, the three string-based `discoverResources/Pages/Widgets` still pointing at the platform `app/Filament/*` / `App\Filament\*` location — strings are not symbol imports, allowed by D5/D3). Updated the `use` in `bootstrap/providers.php` (`App\Providers\Filament\AdminPanelProvider` → `App\Modules\OperatorPanel\Providers\AdminPanelProvider`; the `AdminPanelProvider::class` array entry is unchanged). Deleted the old file via `git rm` and removed the now-empty `app/Providers/Filament/` dir (`AppServiceProvider.php` stays in `app/Providers/`). Ran `composer dump-autoload` to drop the stale classmap entry (see Codebase Patterns).
- **Boundary note:** OperatorPanel now hosts BOTH providers — the standard `OperatorPanelServiceProvider` (wiring seam, registered via the `Module::cases()` spread) AND the Filament `AdminPanelProvider extends PanelProvider` (registered explicitly in `bootstrap/providers.php`). Both loaded; distinct roles (D5).
- **Files changed:** `app/Modules/OperatorPanel/Providers/AdminPanelProvider.php` (new), `app/Providers/Filament/AdminPanelProvider.php` (deleted) + empty dir removed, `bootstrap/providers.php` (use import), `tests/Feature/Modules/ModuleProvidersTest.php` (+3 tests / +import), `tasks.md` (1.3 ✓).
- **Tests:** extended `ModuleProvidersTest` with three pins (all in the booted Feature suite): (1) `class_exists(AdminPanelProvider::class)` true AND loaded via `getLoadedProviders()->toHaveKey(...)`; (2) `class_exists('App\Providers\Filament\AdminPanelProvider')` `toBeFalse` (old location gone); (3) `Filament::getPanel('admin')->getId()` `toBe('admin')` (panel still registered). **Regression net run before & after:** `OperatorPanelTest`+`OperatorSeederTest` = 7/7 (23 assertions) identical pre- and post-move; both files unmodified (`git status` blank for them).
- **Quality loop:** green — format ✅ (Pint auto-imported the FQCN → short `AdminPanelProvider::class`) · filtered 6/6 ✅ · full suite **48/48** (139 assertions, was 45/135) ✅ · phpstan 0 @ level max ✅ · pint --test ✅ · `php artisan about` exit 0 (boots clean post-relocation) ✅ · `openspec validate --strict` ✅. `git diff main -- composer.json composer.lock` empty. No protected files touched.
- **Learnings for future iterations:**
  - **The classmap gotcha is the only non-obvious step here** — promoted to Codebase Patterns: any `App\**` class move/rename needs `composer dump-autoload` or the OLD FQCN's `class_exists` triggers an include of a deleted file (warning → test failure). F2+ module work will hit this whenever Filament resources/models relocate.
  - The panel's `discoverResources/Pages/Widgets` point at `app/Filament/*` which **does not exist** — Filament tolerates missing discover dirs (no error). Left unchanged per "body unchanged"; F2+ adds per-module `->discoverResources(in: app_path('Modules/{X}/Filament/Resources'), ...)` lines here (D5 operator-surface pattern), each a string arg (arch-invisible by design).
  - Next: 2.1 — stand up `tests/Architecture/` + `phpunit.xml` `Architecture` testsuite (registry↔filesystem conformance, set-equality). RED-PROOF mandatory & recorded (temp `mkdir app/Modules/Warehouse` → red → remove → green). Verify whether `tests/Pest.php` needs a binding for the new dir; arch/conformance tests need no Laravel boot.
---
