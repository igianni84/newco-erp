# Progress — parties-anonymisation

## Codebase Patterns
(consolidated reusable patterns — read first each iteration)

- **Anonymisation Hold-precedence gate (tasks 3.2 / 4.1) = `compliance`-only, count-independent.** The gate blocks iff an active `compliance` Hold covers the Customer; **no other type blocks**. Key on `compliance` **only** — do NOT enumerate the whole Hold set, so the gate is immune to the RM-04 6→8 Hold-count debt. Read coverage via `PartyComplianceStatusReader` (contract) / `DatabaseComplianceStatusReader` — **never** touch the `Hold` Eloquent model directly (no-model-leak boundary). Canon MVP-DEC-015 (ADR `2026-07-02-adopt-dec-015-…`).
- **There is NO `sanctions` Hold type.** `HoldType` = 8 cases (`admin|kyc|payment|fraud|compliance|credit|chargeback_review|storage_payment_failed`), none `sanctions`. Sanctions is a **separate** `sanctions_status` FSM (`SanctionsStatus`: `pending|passed|failed|under_review`) on the Customer — do NOT wire it into the anonymisation gate. A sanctioned-customer retention case is a `compliance` Hold placed by Compliance.
- **Frozen-spec anonymisation precedence is stated 3 disagreeing ways** — `DEC-027` (compliance non-blocking) vs `§8.2` / `AC-K-J-9a` (compliance blocks). Canon collapses to compliance-only; the ADR is the reconciliation. Cite the ADR, not the raw spec, when implementing the gate.
- **Owned-child FK = CASCADE; referenced-shared-parent FK = RESTRICT (module rule).** The sibling parties_* FKs RESTRICT with the explicit rationale "a shared parent it references, NOT a row it owns" (profiles→customers, club_credits→profiles). `parties_addresses.customer_id` is the module's first genuinely-OWNED child (Customer hasMany Address, overwritten in place on anonymisation) → the same ownership rationale inverts to `cascadeOnDelete()`. Inert in practice (Customers are never hard-deleted; anonymisation preserves the row). Use this test when adding any future FK.
- **ISO codes are fixed-width `string(col, N)` columns, validated at the action boundary, NOT a DB enum/CHECK.** `amount_currency` = `string(_,3)` (ISO 4217); `parties_addresses.country_code` = `string(_,2)` (ISO 3166-1 alpha-2). No value-set CHECK for open code-sets (contrast the enum columns, which get the PG-only CHECK) — the guard lives in the Create* action. **Two boundary shapes by whether a launch-set exists:** a CLOSED launch-set (currency = 5, locale = 6) → a **typed enum param** (`Currency`/`SupportedLocale`), fail-closed by type (can't pass an invalid value). An OPEN set with no launch-enum (country — collectors are international) → a **format regex** in the action (`preg_match('/^[A-Z]{2}$/', …) !== 1`) throwing a **localized exception** (`InvalidAddressCountryCode`, task 2.1); the offending value IS interpolated (a country code is not PII, unlike an email — the `:producer`/`:club` id discipline).
- **A new non-`Create*` Action MUST be registered in `SupplyLifecycleChainTest` the SAME iteration it lands — never deferred.** The test globs `Actions/*.php`, `array_filter`s out `Create*`, and asserts an EXACT `toEqualCanonicalizing` set (line ~389). So the instant a non-`Create*` Action file exists, the glob picks it up and the suite reds unless it is in the expected set. The Quality-Loop "suite stays green before commit" rule therefore OVERRIDES any task-plan note that assigns "registration" to a later task (RM-01 task 3.4's note is the latest-possible reminder + covers the still-unbuilt `ExportCustomerData`, but `AnonymiseCustomer` had to be registered in 3.2 where it was created). Add a labelled group + spread it into the assertion; a same-iteration edit of a test file to accommodate the code you just wrote is NOT scope creep. An event-free Action (records no domain event) is fine here — the whitelist already holds audit-only entries (`CancelProfile`, the Account family, the ClubCredit writers).
- **Thin `Create*` action = no event, no transaction, filtered out of the exhaustive non-`Create*` allow-list.** A creation Action that records NO domain event and does a single `Model::create()` needs neither a `DB::transaction` (one Eloquent insert is atomic) nor the `DomainEventRecorder`/`ActorContext` ctor deps — contrast the spine `Create*` actions, which wrap a transaction ONLY because they emit an event (write+emit atomicity). `CreateCustomerAddress` is the reference. Naming it `Create*` keeps it out of `SupplyLifecycleChainTest`'s non-`Create*` whitelist (`array_filter(..., fn => ! str_starts_with($name, 'Create'))`, line ~389) — so a pure creation seam never needs registering there; only NON-`Create*` writers (`AnonymiseCustomer`/`ExportCustomerData`, task 3.4) do.
- **Address is a persistence-only within-module child (no `casts()`).** `Address belongsTo Customer` / `Customer hasMany Address` are WITHIN Module K (invariant 10 — `ModuleBoundariesTest` scans the whole `App\Modules\Parties` namespace, so a new model/action is auto-covered; boundary law is NOT re-asserted in feature tests). All-`string` columns + `timestampsTz` need NO `casts()` method (contrast `ClubCredit`/`Customer`, which cast Money/enum/date). AC-K-XM-25 (no B2C/B2B discriminator on Customer; company-billing on Address) is a **schema-shape** assertion (`Schema::hasColumn` present on `parties_addresses`, absent on `parties_customers`).
- **Schema-test idiom: read columns via `->value('col')`, never `->first()->prop`.** `DB::table()->first()` is `stdClass|null` → PHPStan-max rejects a property access on the null branch. `->value('col')` returns the scalar directly (the ClubCreditSchemaTest pattern). Raw `DB::table()->insert()` + a local `xRow()` array-builder helper is the pre-model schema-layer proof (the model lands in a later task).
- **An internal within-module value object goes in `app/Modules/{Module}/Support/` — pure, import-free, `::for()`/`::of()` static factory.** A class that is neither a cross-module contract, an Action, nor an Eloquent model (e.g. `AnonymisedPlaceholders`) has no type-folder, so it lands in a new `Support/` sub-namespace. Verified safe against all four arch gates: `ModuleConformanceTest` gates only the TOP-LEVEL `app/Modules/` dir (9 module dirs + `Module.php`), NOT sub-namespaces; `ModulePersistenceConventionsTest` skips non-`Model` classes; `SupplyLifecycleChainTest` globs `Actions/*.php` ONLY (a `Support/` class is never caught — so it needs no allow-list registration, unlike the non-`Create*` Actions in 3.4); `ModuleBoundariesTest` only forbids cross-module imports (a pure value object has none). **Do NOT use `Contracts/`** — that folder is the module's cross-module PUBLIC surface (other modules may import `Parties\Contracts`, e.g. `ComplianceStatus`); an internal helper there would (mis)advertise as cross-module API. House style: plain `class` (not `final`), promoted `public readonly` props, private ctor + static factory (`Money::of` / `ComplianceStatus`). Mirror value-object tests on `Platform/Money/MoneyTest` — a pure unit test with NO `uses(TestCase::class)` and NO `RefreshDatabase` (`tests/Pest.php` binds `TestCase` to `Feature` only, so `tests/Unit` is boot-free).
- **A constant written into a fixed-width `string(N)` column must be ≤ N chars or it's a cross-engine trap** (extends the ISO-code pattern above). SQLite ignores `varchar(N)` length; Postgres ENFORCES it. So a placeholder/sentinel destined for a `string(N)` column (e.g. the `country_code` `string(2)` → the `ZZ` "unknown" ISO-3166 sentinel, 2 chars) MUST fit N, else it passes the `:memory:` suite and breaks at the PG17 close. Pin it with a `mb_strlen(...) <= N` + format-regex test on the placeholder — the module's Postgres-truthful-by-construction discipline made a unit assertion.

---

## [2026-07-02 15:32] — 1.1 Mini-ADR: adopt canon MVP-DEC-015
- Wrote `decisions/2026-07-02-adopt-dec-015-anonymisation-hold-block-set.md` (mirrors the DEC-018 ADR shape) recording all four required points: (a) `compliance`-only block-set + resolution of the compound frozen-spec contradiction; (b) sanctions-retention via a `compliance` Hold (no `sanctions` Hold type — sanctions is the `sanctions_status` FSM); (c) J-9b minimal/synchronous/in-memory export scope (D5); (d) PII-free `CustomerAnonymised` event over frozen §15.1's event-free anonymisation (D3). Added the newest-first `decisions/INDEX.md` row.
- **Verification-first, not design-trusting:** an Explore subagent quoted the three frozen-spec precedence statements against source — richer than the design summarized. The contradiction is **two-layered**, both folded into the ADR: (1) DEC-027 (`compliance` NON-blocking; non-block list `{payment,fraud,kyc,compliance,admin}`) vs §8.2 (`compliance` blocks; non-block `{…,credit}`) vs AC-K-J-9a (blocks `{sanctions-OFAC,compliance}`) — they disagree on `compliance` AND `credit`, and DEC-027 even cites §8.2 as its anchor; (2) all three name a "sanctions Hold" that is not in the §4.8 six-type enum — sanctions is the separate `sanctions_status` FSM. Canon MVP-DEC-015 = `compliance`-only resolves both. Code reality confirmed: `HoldType` = 8 cases, none `sanctions`; `SanctionsStatus` separate; `PartyComplianceStatusReader`/`DatabaseComplianceStatusReader` present.
- Accurate anchors pinned before writing (no invented line numbers): `Module_K_Verdict_v0.3-MVP.md:152` = the authoritative DEC-015 canon-overlay row ("only `compliance` blocks; no separate `sanctions` Hold", 🔴 floor); `:34` = AC-K-J-9a "See Canon Overlay (DEC-015)"; DEC-027 `decisions.md:198`; §8.2 `Module_K_PRD:454`; §4.8 `:301`; §9.2 `:479`; §12 `:599`; §15.1 `:750`.
- Files changed: `decisions/2026-07-02-adopt-dec-015-anonymisation-hold-block-set.md` (new), `decisions/INDEX.md` (+1 row), `openspec/changes/parties-anonymisation/{tasks.md,progress.md}`. Date = 2026-07-02 → filename matches; delta-spec `_Source:_` citations already point at it (no rename/citation edit needed).
- Quality loop: green — `openspec validate parties-anonymisation --strict` valid; INDEX grep = 1; Pint `--test` passed. Doc-only, zero PHP touched → PHPStan/Pest baseline (1807/1807) unaffected; full-suite + PG17 close is task 7.1.
- **Learnings for future iterations:**
  - The gate is `compliance`-only and **count-independent** — see the Codebase Patterns block at top. Cite the ADR (not the raw, self-contradictory spec) when building tasks 3.2 (gate) and 4.1 (precedence matrix).
  - `sanctions_status` is NOT a Hold type; never wire it into the anonymisation gate.
---

## [2026-07-02 15:43] — 1.2 Migrations: `anonymised_at` + `parties_addresses`
- Two additive, Postgres-truthful + SQLite-compatible migrations (no PG extension, no backfill):
  - `2026_07_02_000001_add_anonymised_at_to_parties_customers` — nullable `timestampTz('anonymised_at')`, no default, no CHECK (mirrors `2026_06_18_000002`'s onboarding timestamps). It's a flag+timestamp, orthogonal to the status FSM (task 3.2 stamps it; the model cast lands there too).
  - `2026_07_02_000002_create_parties_addresses_table` — `id`, `customer_id` FK→`parties_customers` (`cascadeOnDelete`, indexName `parties_addresses_customer_fk`), `line1`/`line2?`/`locality`/`region?`/`postal_code`/`country_code(2)`, optional `company_name`/`vat_id`, `timestampsTz`. No `version` column (mutable child — the club_credits precedent, not the versioned identity spine).
- **FK on-delete design call:** CASCADE, not the sibling RESTRICT — Address is the module's first genuinely-OWNED child (see the new Codebase Pattern). Documented thoroughly in the migration PHPDoc so it reads as deliberate.
- **`country_code` = `string(_,2)`** (ISO 3166-1 alpha-2, the ISO-4217 `amount_currency(_,3)` precedent); no country enum/CHECK — validated at the CreateCustomerAddress boundary (task 2.1).
- Files: 2 migrations (new) + `tests/Feature/Modules/Parties/AnonymisationSchemaTest.php` (new, 7 `it()` → 10 cases: `anonymised_at` present/nullable/accepts-a-timestamp; `parties_addresses` all-columns; full row; optional-fields-NULL; FK-orphan reject; required-field NOT-NULL dataset). `tasks.md` checkbox flipped.
- Quality loop: **green** — `migrate:fresh` (SQLite) clean; filtered 10/10; full suite **1817/1817** (9866 assertions, +10 vs the 1807 baseline); PHPStan max 0; Pint clean; `openspec validate --strict` valid.
- **Learnings for future iterations:**
  - PHPStan-max bit the `->first()->prop` read (`stdClass|null`) — fixed to `->value('col')` on the first attempt; captured as a Codebase Pattern above.
  - PG17 truthfulness is asserted by construction (portable constructs only); the actual cross-engine run is task 7.1 (the close ritual), matching the module's schema-test convention.
  - Task 2.1 builds the `Address` Eloquent model (`parties_addresses`) + `Customer hasMany Address` + `CreateCustomerAddress` (`Create*`-named → stays out of the exhaustive non-`Create*` Action allow-list). Task 1.3 (next) = localized reasons + `CONTEXT.md` Address term.
---

## [2026-07-02 15:56] — 1.3 Localized reasons + CONTEXT.md terminology
- Added the sole anonymisation rejection copy + brought `CONTEXT.md` to the change's end-state.
- **`lang/en/parties.php`** — new `anonymisation` group with the single key `blocked_by_compliance_hold` (the localized exception reason task 3.2's gate throws). `:customer` id interpolated (operator-facing reference, NOT PII — the `duplicate_for_club`/`club_credit` id discipline); copy names the rule + interpolates no name/email/phone/dob so it is log-safe (the `DuplicateCustomerEmail` PII-free discipline). **Only one key**: `AnonymiseCustomer` is orthogonal to the status FSM + idempotent + has no illegal-state edge, so the Hold-precedence block is its *only* rejection — there is no illegal-state reason to author (the "(and any illegal-state reason)" in the task resolves to none).
- **`CONTEXT.md`** — (a) new **Address** canonical term placed after Account (Customer-cluster): `parties_addresses`, Customer `hasMany` within-module, personal fields + `country_code` string(2)/ISO-3166 (no CHECK, boundary-validated), optional `company_name`/`vat_id` (DEC-068/AC-K-XM-25), overwrite-in-place-row-preserved on anonymisation, billing-only at launch; (b) **flipped both anonymisation seam entries** from *deferred → future change* to **now landed** — the *Deferred Hold seams* precedence bullet (the `compliance`-only gate, reader, ADR, lang key) and the *Deferred suspension seams* Anonymisation clause (overwrite + `anonymised_at` orthogonal to `status` + audit redaction + PII-free `CustomerAnonymised`, idempotent).
- **Test** — new `tests/Unit/Modules/Parties/Exceptions/AnonymisationExceptionsTest.php` (2 cases, 9 assertions): the key resolves with `:customer` wired + PII-free (`@`-free), and a pre-existing-groups regression guard. Co-located in `Exceptions/` per the module convention (the `ClubCreditExceptionsTest` pattern — lang tests live beside the exception factory tests); task 3.2 extends this file with the `AnonymiseCustomer` blocked-exception factory tests.
- Quality loop: **green** — Pint format+`--test` clean; filtered 2/2; full suite **1819/1819** (9875 assertions, +2 vs the 1817 baseline); PHPStan max 0; `openspec validate --strict` valid. Only-EN authored (`lang/{it,fr,de,ja,zh_Hans}/parties.php` don't exist — per-key EN fallback, DEC-127).
- **Learnings for future iterations:**
  - **Documentation-forward flip (deliberate, per the task):** `CONTEXT.md` now says the anonymisation seams are "now landed" naming `AnonymiseCustomer`/`CustomerAnonymised`, but those artifacts land in tasks 3.2/3.4 (only `anonymised_at` exists today, from 1.2). This matches the repo's front-loading pattern (the `CustomerConsoleI18nTest` front-loaded i18n keys "ahead of the resolving code") and CONTEXT.md's truth-time is archive. tasks.md + progress.md remain the authoritative build-state, not CONTEXT.md.
  - Domain-rejection reasons live in `lang/en/parties.php` **only** (other locales fall back per-key, DEC-127); their resolution tests co-locate in `tests/Unit/Modules/Parties/Exceptions/`. The exact key task 3.2 must throw: `parties.anonymisation.blocked_by_compliance_hold` with `['customer' => $id]`.
  - PHPStan-max Pest chain shape (lessons 108/111): re-anchor each `->toContain` with `->and($x)`, put `->not->toContain('@')` on its own `expect()` — mirrored from the green `ClubCreditExceptionsTest`.
---

## [2026-07-02 16:10] — 2.1 `Address` model + `Customer hasMany Address` + `CreateCustomerAddress`
- Built the Address entity from the task 1.2 `parties_addresses` migration: the `Address` Eloquent model, the within-module `Customer hasMany Address` / `Address belongsTo Customer` relation pair, and the thin `CreateCustomerAddress` action — plus the deferred country-code boundary validation task 1.2's migration committed to.
- **`Address` model** (`app/Modules/Parties/Models/Address.php`) — `$table = 'parties_addresses'`, `$guarded = []`, `HasFactory` + `newFactory()`, `belongsTo(Customer)` within-module. **No `casts()`** — every column is a plain string + `timestampsTz` (nothing to cast); `@property` annotations carry the PHPStan-max shape.
- **`Customer::addresses()`** — a within-module `HasMany<Address, $this>` + `@property-read Collection<int, Address> $addresses`, mirroring the existing `profiles()` hasMany.
- **`CreateCustomerAddress` action** — THIN: no event (§ 15.1 names no Address event), no transaction (single atomic `Address::create()`), no ctor deps. ONE boundary guard: `country_code` must match `/^[A-Z]{2}$/` (ISO 3166-1 alpha-2, fail-closed) else throws the localized `InvalidAddressCountryCode`. Named `Create*` so `SupplyLifecycleChainTest`'s non-`Create*` whitelist filters it out (verified — that test stayed green unamended).
- **`InvalidAddressCountryCode`** (mirrors `DuplicateCustomerEmail`) + **`parties.address.invalid_country_code`** lang key (interpolates `:country` — not PII). **`AddressFactory`** (`forCompany()` state for the DEC-068 company case).
- Files changed: 6 new (`Address`, `CreateCustomerAddress`, `InvalidAddressCountryCode`, `AddressFactory`, `CustomerAddressTest`, `AddressExceptionsTest`), 3 modified (`Customer.php` +addresses relation, `lang/en/parties.php` +address group, `tasks.md` checkbox). Pint auto-imported the `@see Currency` FQCN (cosmetic).
- Quality loop: **green** — filtered 12/12; full suite **1831/1831** (9926 assertions, +12 vs the 1819 baseline); PHPStan max 0; Pint clean; `openspec validate --strict` valid. Arch gates (`ModuleBoundariesTest`, `ModulePersistenceConventionsTest`, `SupplyLifecycleChainTest`) all green unamended.
- **Learnings for future iterations:**
  - The two new **Codebase Patterns** at top: thin-`Create*`-no-event-no-transaction, and open-code-set → format-regex boundary (vs closed-set → typed-enum param). Task 3.2's `AnonymiseCustomer` will overwrite `Address` personal fields (`line1/line2/locality/region/postal_code/country_code` + `company_name/vat_id`) via `AnonymisedPlaceholders` (task 3.1) — the Address field list is exactly those 8.
  - Task 3.4 (not here) registers the NON-`Create*` `AnonymiseCustomer`/`ExportCustomerData` in `SupplyLifecycleChainTest`. `CreateCustomerAddress` needs NO registration (confirmed by a green run).
  - The `belongsTo`/`@see` in `Address` deliberately references `AnonymiseCustomer` only in **prose** (not an import/`@see` tag) — that class lands in 3.2; importing it now would break PHPStan (unknown class).
---

## [2026-07-02 16:40] — 3.2 `AnonymiseCustomer` action — gate + overwrite + `anonymised_at`
- Built the GDPR right-to-erasure core action (design D1/D2/D4). `app(AnonymiseCustomer::class)->handle(int $customerId): Customer` in ONE `DB::transaction`: lock-re-read → **(d) idempotent early-return** if `anonymised_at` already set → **(gate)** compliance Hold-precedence → **(a)** overwrite Customer PII + every scoped Address → **(b)** stamp `anonymised_at`. Records NO event, NO audit redaction (those are the deferred legs 3.4/3.3 — the action GROWS across the three tasks).
- **Gate = canon MVP-DEC-015 `compliance`-only:** reads `PartyComplianceStatusReader::forCustomer($id)` (the contract, never the `Hold` model), blocks iff `in_array(HoldType::Compliance, $coverage->activeHoldTypes, true)` → throws the new `AnonymisationBlockedByComplianceHold::forCustomer($id)` BEFORE any write (rollback). Count-independent (immune to RM-04 6→8 debt); sanctions NOT wired (separate `sanctions_status` FSM).
- **Overwrite** reads task 3.1's `AnonymisedPlaceholders::for($customer->id)`: `$customer->update([...$p->customerAttributes(), 'anonymised_at' => CarbonImmutable::now()])`, then EACH `$customer->addresses()->lockForUpdate()->get()` `->update($p->addressAttributes())`. Rows PRESERVED (overwrite-in-place). **Orthogonal to status** — writes no `status`, records no `CustomerClosed`.
- **`lockForUpdate()->firstOrFail()`, NOT `lockAndRefresh`** — the hot-cache term was imprecise; `lockAndRefresh` exists only in `Catalog\Lifecycle\LifecycleTransition`. The Parties convention (`CloseCustomer`/`SuspendCustomer`) is `Customer::query()->whereKey($id)->lockForUpdate()->firstOrFail()` + a child-set re-read `->lockForUpdate()->get()` (the SuspendCustomer profile-cascade precedent, mirrored for addresses).
- **Ctor is reader-only** (`PartyComplianceStatusReader $compliance`) — NOT the `DomainEventRecorder`/`ActorContext` of the event-emitting transition Actions: 3.2 records no event, so injecting them now would be dead code. Task 3.4 adds them with `CustomerAnonymised`.
- **Model:** added `@property CarbonImmutable|null $anonymised_at` + `'anonymised_at' => 'immutable_datetime'` cast + a docblock paragraph (Customer.php). Pint auto-imported the `{@see AnonymiseCustomer}`/`{@see CustomerReactivated}` FQCNs (cosmetic).
- **`SupplyLifecycleChainTest` registration (REQUIRED in 3.2, not deferrable to 3.4):** adding `AnonymiseCustomer` (a non-`Create*` Action) makes it appear in the `array_filter(!str_starts_with 'Create')` glob → the exact `toEqualCanonicalizing` set reds UNLESS registered. Added a `$anonymisationWriters = ['AnonymiseCustomer']` group + spread. The task-3.4 note is the LATEST-possible reminder (and still covers `ExportCustomerData`, task 5.1) — but the Quality-Loop "suite stays green" rule forces registering `AnonymiseCustomer` the same iteration it lands.
- Files: 3 new (`Actions/AnonymiseCustomer`, `Exceptions/AnonymisationBlockedByComplianceHold`, `tests/Feature/.../CustomerAnonymisationTest` — 6 `it()`), 3 modified (`Models/Customer` +cast/+property/+docblock, `SupplyLifecycleChainTest` +registration, `tests/Unit/.../AnonymisationExceptionsTest` +1 factory test), 1 checkbox.
- Quality loop: **green** — filtered 14/14 (155 assertions); full suite **1846/1846** (9994 assertions, +7 vs the 1839 baseline); PHPStan max 0; Pint clean; `openspec validate --strict` valid. Arch gates green (`SupplyLifecycleChainTest` amended-and-green; boundaries/persistence/conformance untouched).
- **Learnings for future iterations:**
  - **Task 3.3 (audit redaction) + 3.4 (event) EXTEND this same `AnonymiseCustomer::handle`** inside the SAME transaction. 3.4 adds `DomainEventRecorder $recorder` + `ActorContext $actor` to the ctor and records `CustomerAnonymised` (PII-free: id + `anonymised_at`) after the overwrite; it also registers nothing NEW in `SupplyLifecycleChainTest` (the Action is already whitelisted — only `ExportCustomerData` from 5.1 still needs adding to `$anonymisationWriters`). 3.3 nulls the Customer's `audit_records` before/after within the tx.
  - **The idempotency + event-count-0 assertions in `CustomerAnonymisationTest` FLIP in 3.4:** the happy/closed/idempotent tests currently assert `DomainEvent::count() === 0`; once 3.4 records `CustomerAnonymised`, update them to assert exactly one `CustomerAnonymised` (happy/closed) and no SECOND event (idempotent). Grep the test for `DomainEvent::query()->count())->toBe(0)`.
  - **Placing an isolated Hold in a gate test:** `Hold::factory()->create(['hold_type' => HoldType::Compliance, 'scope_type' => HoldScope::Customer, 'scope_id' => $customer->id])` — bypasses `PlaceHold`'s status-coupling suspend, and `DatabaseComplianceStatusReader` reads `parties_holds` directly, so the gate sees it identically. Use this for the task-4.1 precedence matrix.
---

## [2026-07-02 16:23] — 3.1 `AnonymisedPlaceholders` deterministic helper
- Built the pure value object task 3.2's `AnonymiseCustomer` overwrite reads. `AnonymisedPlaceholders::for(int $customerId)` (private ctor + static factory, mirroring `Money::of`), holding two id-derived `public readonly` props (`email`, `name`) + two projection methods that centralise the field→placeholder maps.
- **Placeholders (design D1):** `email → anonymised+{id}@anonymised.invalid` (id-keyed → UNIQUE-safe on the global `parties_customers.email` index; `.invalid` = RFC 6761 non-routable), `name → "Anonymised Customer {id}"` (id-keyed PII-free breadcrumb), `phone`/`date_of_birth → null` (both nullable). `customerAttributes()` returns that 4-col map. `addressAttributes()` returns the 8 `parties_addresses` fields: NOT-NULL free-text (`line1`/`locality`/`postal_code`) → `'Anonymised'` sentinel; `country_code` → `'ZZ'` (ISO 3166-1 alpha-2 "unknown", 2-char → Postgres `varchar(2)`-safe + passes the `/^[A-Z]{2}$/` law); the 4 nullable fields (`line2`/`region`/`company_name`/`vat_id`) → null. Address placeholders are CONSTANT (not id-keyed — no unique constraint), so they carry no re-identification vector.
- **Placement:** new `app/Modules/Parties/Support/` sub-namespace — pure/import-free, prose-only refs to Customer/Address/`AnonymiseCustomer` (no `use`, no `@see` tag → no PHPStan coupling to the not-yet-existing 3.2 class). Not `Contracts/` (cross-module surface) nor `Actions/` (the `SupplyLifecycleChainTest` glob). See the new Codebase Pattern.
- Files: `app/Modules/Parties/Support/AnonymisedPlaceholders.php` (new), `tests/Unit/Modules/Parties/Support/AnonymisedPlaceholdersTest.php` (new — 8 `it()`, 15 assertions: exact email/name, two-ids-distinct UNIQUE-safe, byte-identical determinism, the 2 overwrite-map shapes + counts, address-constant-across-ids, `country_code` varchar(2)-safe + format, `.invalid` TLD), `tasks.md` checkbox.
- Quality loop: **green** — red proof first (8/8 class-not-found), then filtered 8/8; full suite **1839/1839** (9941 assertions, +8 vs the 1831 baseline); PHPStan max 0; Pint clean; `openspec validate --strict` valid. Arch gates untouched (a `Support/` value object trips none).
- **Learnings for future iterations:**
  - Two new Codebase Patterns at top: **internal value object → `{Module}/Support/`** (pure, import-free, safe vs all four arch gates), and **fixed-width `string(N)`-column sentinel must be ≤ N chars** (cross-engine trap; `country_code`/`ZZ`).
  - **Task 3.2 wiring (`AnonymiseCustomer`):** `$p = AnonymisedPlaceholders::for($customer->id);` → overwrite the Customer with `$p->customerAttributes()` and EACH scoped Address with `$p->addressAttributes()`, inside one `DB::transaction` + `lockAndRefresh`. The maps are DB-column-keyed, so they drop straight into an Eloquent update/`forceFill`. `date_of_birth` is `immutable_date`-cast — a `null` write is fine (nullable col).
---
