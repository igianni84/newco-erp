# NewCo ERP — Launch-MVP Decisions Register v0.1 (thin index)

- **Version**: v0.1 (living — accretes through Phase D; **frozen into the Phase E handoff**).
- **Date**: 2026-06-07
- **Status**: OPEN — **Phase-D re-baseline drafting COMPLETE + BATCH-RATIFIED 2026-06-08** (all artefacts #1–#14 drafted + ratified; the still-⏳ set batch-ratified by Paolo 2026-06-08 — §7; the gate into Phase E is CLEARED). Living through Phase E; **frozen into the Phase-E handoff.** Established by Paolo's decision 2026-06-07 (a **thin index**, not a full restatement).
- **Owner**: Paolo (decides). Claude maintains.
- **Purpose**: a single greppable map of **every decision the launch-MVP re-scoping has made** — for the dev/build phase, coding agents, and the Phase E handoff. The MVP's decisions were otherwise spread across the method docs, the 8 cut-sheets (§6 Q's), Phase C (§5 RECONCILEs + notes), and the PRD headers; this file indexes them in one place.

---

## §1 How to read this register

- **This is an INDEX, not a source of truth.** Each row points to the **authoritative doc** where the decision is specified in full and self-contained (DEC-074). If this register ever conflicts with the cut-sheet / Phase C / PRD it points to, **that doc wins.** Do not restate or re-litigate decisions here — add/update a one-line row and point.
- **ID scheme** (reuses the handles already in play; does **not** invent a parallel numbering that competes with greenfield):
  - **GOV-1..3 / P1–P2** — the governing decisions + binding principles (plan §2 / §4.1).
  - **D1–D25 / L-PP** — the macro dials (`Dials_Worksheet_v0.1.md` §5).
  - **Module Q's** — the per-module cut-sheet ratifications (`01-triage/Module_X_CutSheet_v0.1.md` §6), cited as e.g. *Mod0-Q1*, *K-Q4*.
  - **A–N / R1–R4 / N1–N3** — the Phase C register items / RECONCILEs / editorial notes (`01-triage/Phase_C_Reconciliation_v0.1.md`).
  - **MVP-DEC-NNN** — a small **new** stream for net-new Phase-C/D decisions with no prior handle (§6).
  - **DEC-NNN** — **greenfield's frozen register** (`greenfield/04-decisions/decisions.md`, DEC-001..196). **Never extended here** — this register only *bridges* to it ("touches / supersedes-at-launch").
- **Status legend**: ✅ RATIFIED (Paolo, date) · ⏳ DRAFTED (awaiting batch ratification) · ⛔ DEFERRED-to-roadmap · 🔁 SUPERSEDED-at-launch (greenfield DEC reframed for the MVP; restores post-launch).
- **The deferred set + its seams (P1) lives in** `04-roadmap/Post_Launch_Roadmap_v0.1.md`; this register flags *that* a thing is deferred and points there.

---

## §2 Governing decisions & binding principles (locked 2026-06-05)

| ID | Decision | Status | Authoritative doc |
|---|---|---|---|
| **GOV-1** | **Lean MVP** — aggressive defer + manual-first where safe; KEEP only what the core loop or the compliance/data-integrity floor requires. | ✅ 2026-06-05 | `00-method/MVP_Restructure_Plan_v0.1.md` §2.1 |
| **GOV-2** | **One coherent re-baseline, then a single handoff** — strip + reconcile the whole MVP in `mvp/`; promote to `handoff/` once, coherent. **No piecemeal handoff.** | ✅ 2026-06-05 | plan §2.2 |
| **GOV-3** | **High-touch governance** — Paolo ratifies; Claude recommends. | ✅ 2026-06-05 | plan §2.3 |
| **P1** | **Defer without burning bridges** — every deferred/simplified item names the **seam** that makes the post-launch build additive (+ points to the roadmap). | ✅ 2026-06-05 | plan §4.1 |
| **P2** | **Admin-first, self-serve-later** — producer/back-office writes are operator-driven via the Admin Panel (admin-parity DEC-083); consumer storefront exempt. | ✅ 2026-06-05 | plan §4.1; bridges **DEC-083** |

---

## §3 Macro dials (decided 2026-06-05; `Dials_Worksheet_v0.1.md` §5)

The **final ratified** decision per dial (where it diverges from the §1 Lean recommendation — e.g. D1, D2, D21 — the §5 decision / Paolo override governs).

| Dial | Final decision | Status | Greenfield DEC bridge |
|---|---|---|---|
| **D1** Currencies | **KEEP all 5** (dual-record machinery is fixed-cost) | ✅ | — |
| **D2** Locales | **KEEP** (6 locales; i18n infra reportedly built; content may stagger) | ✅ | DEC-031, DEC-064 |
| **D3** Geography | **SIMPLIFY (hybrid)** — automate low-friction; manual operator quote for complex (US/high-excise/state-alcohol); **OFAC retained (floor)**; automated US-state/excise engines deferred | ✅ | 🔁 DEC-148/149/150 (deferred) |
| **D4** Payments | **KEEP card + SEPA; DEFER saved-card auto-escalation (INV3 dunning)** — timing-driven (storage billing months out) | ✅ / ⛔(escalation) | — |
| **D5** Gifting | **DEFER** — seam: voucher ownership-transfer capability (no hard single-permanent-owner) | ⛔ | — |
| **D6** Cancellation/refund | **KEEP 14-day (legal) + basic refund; SIMPLIFY the matrix** (goodwill/partial-voucher → manual) | ✅ | — |
| **D7** Discovery composites | **SIMPLIFY → single-producer at launch**; defer multi-producer atomic composites — seam: Composite SKU + N-constituent retained (Mod 0) | ✅ / ⛔(multi-producer) | DEC-019, DEC-061 |
| **D8** Club / membership | **KEEP (full — core VP)** | ✅ | — |
| **D9** Catalog LWIN + bulk | **KEEP** + basic bulk; **manual-first enrichment** (LWIN pluggable, off critical path — Mod0-Q3) | ✅ | — |
| **D10** Allocation sub-pools | **KEEP** (core supply primitive) | ✅ | — |
| **D11** Procurement variants | **KEEP both consignment (V1/V2); DEFER Direct Purchase** — seam: uniform flow parameterized | ✅ / ⛔(Direct Purchase) | DEC-011, DEC-063, **DEC-093** |
| **D12** NFT / blockchain + NFC | **KEEP (core VP), DECOUPLED parallel workstream** — on-chain off the critical path; serialization stays launch-ready | ✅ / 🔁(on-chain decoupled) | DEC-050, DEC-052, DEC-134 |
| **D13** Late-binding pick | **SIMPLIFY → FIFO + manual tiebreak** | ✅ | — |
| **D14** Returns / replacements | **SIMPLIFY → manual-first (Admin Panel)**; defer the FSM automation | ✅ / ⛔(automation) | — |
| **D15** Producer recall | **KEEP minimal / manual** | ✅ | — |
| **D16** Stocktake / quarantine / adjustments | **KEEP integrity core (floor); SIMPLIFY the workflows → manual-first** (delicate — joint B/D review) | ✅ / ⛔(automation) | DEC-194, DEC-195 |
| **D17** Cellar render | **SIMPLIFY → basic view**; defer richest aggregation (in-transit ETA precision, granular storage) | ✅ / ⛔(richness) | — |
| **D18** Multi-currency dual-recording | **KEEP (floor)** — FX-correct refunds; unsafe to simplify | ✅ | — |
| **D19** Supplier settlement | **DEFER → operator-run first cycle(s)**; the engine builds after — first close is months post-launch | ⛔ | — |
| **D20** Italian SDI connector | **already deferred (EXT-2)** — no action | ⛔ | — |
| **D21** Chargeback | **KEEP chargeback automation** — **Paolo override** of the Lean "manual" rec (payment automation is floor) | ✅ | DEC-014, DEC-028; see [[keep-payment-automation]] |
| **D22** Services / experiences | **KEEP storage-only** (already lean) | ✅ | — |
| **D23** Producer reporting | **KEEP full self-serve** (all 7 sections, real-time) — core to the producer promise | ✅ | — |
| **D24** Admin Panel | **KEEP (scoped to MVP ops)** — *more* load-bearing in a manual-first MVP (→ the 9th PRD, Phase C item L) | ✅ | DEC-083 |
| **D25** Support tooling | **KEEP email + admin lookup** (already lean) | ✅ | — |
| **L-PP** | **Producer portal = full read, ops-driven writes**; exactly **ONE producer write** retained: membership approve/decline (K) | ✅ | DEC-083; bridges K-Q4 |

---

## §4 Phase B cut-sheet verdicts (RATIFIED 2026-06-07; `01-triage/Module_X_CutSheet_v0.1.md`)

One row per module (the §6 Q's are in each cut-sheet). Verdicts per the master `Phase_D_Kickoff_Prompt.md` §5.

| Module | Verdict | Authoritative doc |
|---|---|---|
| **0 (PIM)** | KEEP-in-full **+ Wine→Product generalisation**; ~0 net-new deferrals | `Module_0_CutSheet_v0.1.md` |
| **K (Parties)** | KEEP-in-full + naming cascade (compliance floor + D8 club spine intact) | `Module_K_CutSheet_v0.1.md` |
| **A (Allocations)** | KEEP-in-full + cascade (supply primitive + Layer-1 no-oversell + Direct-Purchase enum/FSM seam) | `Module_A_CutSheet_v0.1.md` |
| **D (Procurement)** | KEEP-heavy + **Direct Purchase deferred** (idle + seam) + R1 + E-emits consumer | `Module_D_CutSheet_v0.1.md` |
| **S (Sales)** | Cut-heavy (D7 composite / D5 gifting / D8 club-credit peripherals / D6 refund-matrix) + R2 | `Module_S_CutSheet_v0.1.md` |
| **B (Bottle/Stock)** | Cut-heavy (**D12 DECOUPLE on-chain**; serialization launch-ready; **D16 SIMPLIFY** Stage-8 → manual-first) + E-emits ownership + N1 | `Module_B_CutSheet_v0.1.md` |
| **C (Fulfilment)** | Cut-heavy (D3 geography-hybrid / D13 pick / D14 returns / D17 cellar / D15 recall) + R3 + in-transit redemption-block floor | `Module_C_CutSheet_v0.1.md` |
| **E (Finance)** | Cut-heavy (**D19 DEFER** settlement engine; **D4 DEFER** INV3 auto-escalation; **D21 KEEP** chargeback automation; **D18 FLOOR**) + R4 | `Module_E_CutSheet_v0.1.md` |

**Load-bearing cross-cutting ratifications (called out; full set in each §6):**
- **Mod0-Q1** Composite SKU KEPT (single-producer bundles + D7 seam) · **Mod0-Q2** approval role-count admin-configurable (no spec change) · **Mod0-Q3** manual-first enrichment (LWIN pluggable, off critical path). ✅
- **K-Q4** exactly one producer write platform-wide = membership approve/decline (= L-PP). ✅
- **A-Q1** D7 multi-producer-composite cut **forwarded to Module S** — Module A takes no D7 cut, ships its full supply primitive as the seam (mirrors K→S for D8). · **A-Q2** Direct Purchase deferred, A keeps the `direct_purchase` enum + uniform-FSM seam (substantive defer is D's). · **A-Q3** all A ops operator-driven via Admin Panel, **zero producer writes, no backend cut**. ✅
- **D21 override** — chargeback automation KEPT (Paolo). ✅ → [[keep-payment-automation]]

---

## §5 Phase C reconciliation outcomes (RATIFIED 2026-06-07; `01-triage/Phase_C_Reconciliation_v0.1.md`)

**Verdict: the 8 trimmed scopes compose into one coherent system; R1 (cross-module incoherence) discharged.** No new scope cuts. The four RECONCILEs are the concrete Phase-D PRD edits (naming/contract only, zero behaviour change).

| ID | Decision | Status | Authoritative locus / greenfield bridge |
|---|---|---|---|
| **A** | Naming cascade (BR→PR; Wine*→Product*); Module 0 v0.3-MVP §18 = source of truth; carve-outs (E neutral; B/C physical-unit) | ✅ CONSISTENT | Phase C §2-A; Module 0 PRD §18 |
| **R1** | Module D — `SupplierPaymentCompleted` is **financial-event-only** (no Direct-Purchase FSM-activation role); activation is uniform operator-publish | ✅ RECONCILE | Phase C §5-R1; 🔁 **DEC-183** |
| **R2** | Module S — storage is **Module-S-internal** (single D→S read of `InboundEventPhysicallyAccepted`; no bidirectional S↔E at INV2) | ✅ RECONCILE | Phase C §5-R2; 🔁 **DEC-118→DEC-119** |
| **R3** | Module C — **4-fulfilment-stream** Logilize contract (storage-location migrated to Module B's Stream B1) | ✅ RECONCILE | Phase C §5-R3; 🔁 **DEC-188** |
| **R4** | **`SupplierPaymentCompleted` = E-emits** (flipped at ratification, Q2) → Module D consumes (settle/close PO) + Module B consumes (`ownership_flag` PRODUCER→CRURATED). Corrects the cut-sheets' "D-emits". | ✅ RECONCILE | Phase C §2-C/§5-R4; 🔁 **DEC-091 / DEC-119** |
| **N1** | D16 manual-first-automation posture landed **identically** in B + D PRDs (integrity core = floor; only automated round-trips defer) | ✅ note | Phase C §5-N1; DEC-194 |
| **N2** | Finance Hold triggers: **chargeback automated (D21)**, **storage-payment manual-first (D4 deferred)**; K's Hold registry trigger-agnostic — align K.28 + E prose | ✅ note | Phase C §5-N2 |
| **N3** | Party naming: PO-level enum `NEWCO` (DEC-085) vs inventory `ownership_flag` `CRURATED` (DEC-185) — same party; make `OwnershipTransitioned` cascade prose unambiguous | ✅ note | Phase C §5-N3; DEC-085, DEC-185 |
| **I / Q4** | **Direct Purchase deferred at launch** — confirmed no launch deal; idles across A/D/B/E/S with retained enum/FSM seam | ⛔ | Phase C item I / Q4; DEC-093 (seam) |
| **L / Q1** | **The 9th, thin Admin-Panel MVP PRD** (operator-surface scope); the *full* Admin-Panel surface → roadmap; Architecture records it as first-class | ✅ | Phase C item L / Q1 |
| **G / Q5** | No-overselling **build-sequencing** (B's floor artefacts integration-ready by the integrated launch) → carried to the dev-team sizing exercise | ✅ (sequencing flag) | Phase C item G / Q5 |
| **M** | **The end-to-end floor is whole across the composed system** (no-oversell · KYC/sanctions/OFAC/Hold · tax-correct invoicing · dual-record FX · committed-inventory · audit/retention) | ✅ CONSISTENT | Phase C §6; DEC-181, DEC-099 |
| **Q3 (P-C)** | D12 owned action items (EXT-1 blockchain-review scheduling; DEC-124 tag-stock lead-time) → Paolo-track, time-sensitive | ✅ (Paolo-track) | Phase C Q3; DEC-124 |

*(Phase C items D/E/F/H/J/K/N were CONSISTENT — no change needed; see Phase C §2–§4 for the verification.)*

---

## §6 Phase D net-new decisions (MVP-DEC stream)

Decisions with no prior handle, made during Phase C ratification / Phase D.

| ID | Decision | Status | Authoritative doc |
|---|---|---|---|
| **MVP-DEC-001** | **Production cadence** — one artefact per fresh context window; write the next sub-session kickoff before moving (Phase B + D) | ✅ | [[mvp-triage-cadence]]; `00-method/*_Kickoff_Prompt.md` |
| **MVP-DEC-002** | **Phase D acceptance rides alongside each PRD** — write `Module_X_Acceptance_v0.3-MVP.md` in the same session as the PRD | ✅ 2026-06-07 | [[mvp-triage-cadence]]; master kickoff §5 |
| **MVP-DEC-003** | **Phase D ratification is batched** — Paolo ratifies several artefacts at checkpoints he chooses; production continues; nothing to `handoff/` until Phase E | ✅ 2026-06-07 | [[mvp-triage-cadence]] |
| **MVP-DEC-004** | **The Module 0 generalisation is folded into the v0.3-MVP PRD** (not a separate greenfield v0.3); **no new greenfield DEC minted** (greenfield frozen, R4) — bridges the generalisation brief's proposed DEC substance + DEC-065 | ✅ 2026-06-07 | `Module_0_CutSheet_v0.1.md` §3; brief; Module 0 PRD §16; bridges **DEC-065** |
| **MVP-DEC-005** | **Q3 manual-first enrichment acceptance scheduling** — LWIN-adapter criteria are "verified when the adapter lands" (not launch-blocking); the manual baseline is launch-critical | ✅ 2026-06-07 | Module 0 Acceptance §0.1 |
| **MVP-DEC-006** | **This MVP decisions register established** — a thin index in `mvp/04-decisions/`; reuse handles + MVP-DEC stream; bridges (never extends) greenfield DEC-NNN; **frozen into the Phase E handoff** | ✅ 2026-06-07 | this file |
| **MVP-DEC-007** | **The Admin-Panel surface is role-agnostic at the PRD layer** — the authority-tier / RBAC / persona-gating policy is admin-configurable + downstream (`feedback_prd_rr_approval`); the **only** PRD-level discipline is the spec-mandated multi-actor separation-of-duties floor (3-step approval; supervisor-override; single-supervisor-approval — self-approval never allowed). Applies Mod0-Q2 / K-Q3 to the consolidated Admin-Panel surface. | ✅ 2026-06-08 (ratified with the Admin-Panel PRD) | Admin-Panel PRD §1.4 / §5.2; `feedback_prd_rr_approval` |
| **MVP-DEC-008** | **Module K Hold-type enum completed to 8 (Q-AD-3 follow-through).** Canonical `Hold.type` = the six base types + `CHARGEBACK_REVIEW` (DEC-168) + `STORAGE_PAYMENT_FAILED` (DEC-160) = **8 first-class coordinate values** (Q-AD-3 **Option B**, Paolo-ratified; sub-type/cause-discriminator reading **rejected**). Erratum fix: §4.8 / §4.8.1 / AC-K-FSM-10 / AC-K-FSM-11 said "six" (the Q-AD-3 §4.8 enum delta had only landed in §15.8); aligned to 8 + the per-type lift discipline for both finance-driven types. Casing **non-normative** (dev's call, DEC-073). **No new DEC** — DEC-160 + DEC-168 lock the substance. | ✅ 2026-06-18 (Phase-E erratum) | Module K PRD §4.8/§4.8.1 + Acceptance AC-K-FSM-10/11; bridges **Q-AD-3** (`reference/v1.1/03-qa/qa.integration.md`) + **N2** (§5) |
| **MVP-DEC-009** | **ProducerAgreement per-Club scope requires an `active` Club (new-agreement scoping guard).** A new ProducerAgreement scoped to a specific Club requires that Club to be `active`; `sunset` and `closed` Clubs are not selectable. Producer-wide scope is ungated; supersession/renewal (BR-K-Agreement-3) inherits scope and is exempt, so wind-down amendments on a since-`sunset` Club are unaffected. Erratum of omission: DEC-070 admitted per-Club scoping with **no** Club-lifecycle constraint, carried verbatim into §4.6 + BR-K-Agreement-{1,2,3}, and the QA integration probe verified ProducerAgreement-lifecycle clean without raising it. Distinct from BR-K-Club-2 / §6.2 (deref of an **already-locked** Club reference for historical settlement, regardless of state). Added **BR-K-Agreement-4** + §4.6 clause + **AC-K-BR-Agreement-4**. **No new DEC** — editorial completion of DEC-070. | ✅ 2026-06-18 (tech-team Q&A; Phase-E erratum) | Module K PRD §4.6 + §14.6 (new **BR-K-Agreement-4**) + Acceptance **AC-K-BR-Agreement-4**; bridges **DEC-070** + tech-team GitHub question (Module K ProducerAgreement × Club lifecycle) |
| **MVP-DEC-010** | **ProducerAgreement `settlement_cadence` closed to 3 at launch, enforced server-side.** Launch value set = `quarterly` (default) + `monthly` + `semi-annual` — the only producer-settlement cadences the business model contemplates (BMD §3.10; `annual` / sub-monthly named nowhere). Closed + enforced at **API + DB** (not UI-only) because the value times settlement (Module E read) + V1 PO issuance (Module D), so an out-of-set value would silently mis-time money movement and corrupt the D19 seam. **Extensible post-launch** (a new cadence is a deliberate, business-approved config addition, not arbitrary free-text). Literal representation (enum vs lookup, casing, string form) is the dev team's call (DEC-073). DEC-042 framework (quarterly default, agreement-configurable) is **unchanged**; the BMD stays open ("e.g.") — launch closes the set as an MVP-scoping delta (no BMD revision). Placeholder fields (minimum commitment, payment terms, special-clause refs) remain deferred to the 24-month template (Q-OQ-9 / DEC-058) — **none promoted**. **No new DEC** — bridges DEC-042. | ✅ 2026-06-18 (tech-team Q&A) | Module K PRD §4.6 + §14.6 (BR-K-Agreement-2) + Acceptance AC-K-BR-Agreement-2; bridges **DEC-042** + tech-team GitHub question (Module K ProducerAgreement settlement cadence) |
| **MVP-DEC-011** | **Hero Package capacity × renewal: renewing members are grandfathered; reductions draw down by attrition (never eviction).** Clarifies the year-boundary case the mid-year §13.4 floor + AC-K-J-15 left implicit. (i) A renewal continuing an in-good-standing `Active` membership does **not** newly consume capacity, so it is **not cap-gated** (grandfathered); the cap gates only new approvals, waitlist conversions, and `Lapsed`/`Cancelled` re-activations (§13.1). (ii) The §13.4 anti-orphan floor applies at the renewal boundary — the renewing cohort floors the new period's `qty`; a Producer cannot set capacity below members entitled to renew. (iii) To shrink, a Producer sets a lower **target** and lets attrition draw the count down (no-backfill); `qty` tracks down but never below current `Active`; the invariant holds throughout. (iv) A renewal is **never** auto-cancelled to force a reduction (would break the auto-renew + stable-price model, DEC-007/DEC-033). Sophisticated priority/FIFO eviction stays **deferred** (§13.5/§17). Cross-module seam: invariant defined in K §13; `qty` + anti-orphan floor owned by Module A (§5.3.4 / BR-A-Mutability-1, DEC-079); enforced at order completion by Module S three-gate (DEC-114, referenced generically — the refinement flows without an S edit, but S must honor the renewal-exemption when built). **No new DEC** — editorial completion of DEC-069 (capacity = current-state value) + §13.4 anti-orphan intent. | ✅ 2026-06-18 (tech-team Q&A) | Module K PRD §13.1 + §13.4 + Acceptance **AC-K-J-15a**; bridges **DEC-069** + tech-team GitHub question (Module K Hero Package capacity × auto-renewal) |
| **MVP-DEC-012** | **Intrinsic + Composite SKU may share one physical representation; the two event families and Intrinsic-only Case-Config are the spec guardrails (DEC-073 applied).** Tech-team Phase-1 architecture question (pre-implementation, nothing built): implement the two PRD SKU shapes as two parallel entities/event lanes, or one unified `SellableSKU` with a constituent collection? Ruling: the **physical entity/table shape is the dev team's call** (DEC-073 — representation is out of PRD scope), so a single representation with a constituent collection (Intrinsic = 1 constituent; Composite = N ≥ 2) **is permitted** and is the better D7 seam (composite stays latent + additively restorable). **But** three things stay in the spec layer (DEC-073 reserves "domain event names" + "business invariants") and a unified representation MUST preserve them: **(i)** the two distinct event families `SellableSKU*` / `CompositeSKU*` remain the emitted contract — §14.2 fixes **family-based dispatch, not a payload discriminator**; the issue's proposed single family + `constituent_count` discriminator is **declined** (it inverts §14.2 and erodes the deliberately-KEPT D7 composite event lane, Mod0-Q1); **(ii)** Case Configuration attaches to the **Intrinsic shape only** (§3.7) — composite constituents are bare PRs, so the proposed uniform per-constituent `case_configuration_id` is **declined**; **(iii)** BR-SKU-2..5 (atomicity, post-Offer immutability, producer-agnostic composition) scope to N ≥ 2, BR-SKU-1 to the single-PR shape. Recorded as clarifying implementation notes in §3.8 + §13.5; **no PRD semantics, BR text, event set, or acceptance assertion change** (AC-0-FSM-4/FSM-5 unchanged + reinforced; AC-0-BR-Identity-3 already asserts the PR carries no `case_configuration_id`). **No new DEC** — DEC-073 governs; bridges Mod0-Q1. BMD unchanged. | ✅ 2026-06-18 (tech-team Q&A) | Module 0 PRD §3.8 + §13.5 (implementation notes), §14.1/§14.2, §3.7; Acceptance AC-0-FSM-4/FSM-5 (unchanged); bridges **DEC-073** + **Mod0-Q1** + tech-team GitHub question (Module 0 Intrinsic/Composite SKU unification) |
| **MVP-DEC-013** | **Club config field-set ratified for launch (the four config blobs); structured forms OK to build; conflicts vs the v2.23 Producer Agreement logged as known-open for the consistency review.** Build-layer field ratification (DEC-073: the semantic field set is spec, storage/encoding is the dev's call). §4.3 / §11 **conceptual prose unchanged — no PRD edit**. **`fee_model`**: `amount` (number, per-Club, Producer-set, stable YoY) · `currency` (EUR at launch) · `billing_frequency` (`annual` only at launch) · `waiver_rules` = **none at launch** (no membership-fee waiver mechanic; welcome-window = deferred K.18) → leave empty/free, no form. Fee is collected via the Module S Hero Package purchase; `generates_credit` is separate. **`tier_definitions`**: single implicit tier — **hide the field at launch** (DEC-062); structure carried; no per-tier fields. **`credit_policy`**: `generates_credit` = separate flag; **credit amount is COMPUTED (= membership fee − Hero Package value; DEC-007 / agreement Cl.2.6(b)), NOT a config field** — the "full-fee→full-credit" phrasing in §11/K.18 means *no welcome-window proration*, not "credit = full fee"; configurable = `valid_to` rule + `cancellation_grace_days` (§11.3); **scope fixed** (issuing-club-only, not configurable). **`renewal_policy`**: `auto_renew` (bool, default true) · `grace_period_days` · `lapse_action` (enum, per the Profile FSM). **ProducerAgreement `minimum_commitment` / `payment_terms` / `special_clause_refs`**: confirmed **deferred for v1** (no forms) — the v2.23 agreement now supplies the content (Q-OQ-9 effectively landed), fillable in the consistency review. **KNOWN-OPEN — deferred to the agreement↔PRD consistency review (with legal); NOT resolved here:** (1) Club Credit validity default — spec §11 = **31 Dec** of issuance year vs agreement = **end of the rolling Membership Year** (anniversary); (2) renewal grace — DEC-034/spec = **30 days** vs agreement Cl.4.6 = **none** (immediate lapse); (3) fee/credit currency — DEC-037 = **5 currencies** vs agreement Cl.19.7 = **EUR-only** for club economics; (4) the agreement pins **Hero Package = 70–100%** of the coupon and **Residual Credit = 0–30%**, bands the spec leaves open. For v1 these fields stay **configurable at current spec defaults**; defaults/constraints are decided in the review. **No new DEC** — build-layer ratification. | ✅ 2026-06-18 (tech-team Q&A) | Module K PRD §4.3 + §11 (prose unchanged); MVP register only; bridges **DEC-062 / DEC-034 / DEC-037 / DEC-007** + tech-team GitHub question (Club config field set) + `greenfield/06-producer-agreement/CRCLES_Producer_Club_Collaboration_Agreement_v2.23.docx` |
| **MVP-DEC-014** | **Customer-side KYC + sanctions-screening launch posture ratified (tech-team Module K §9 build questions).** Build-layer ratification (DEC-073: the screening *outcome/state* + the *order-completion gate* are spec; vendor wiring + field representation are the dev's call). §9 conceptual prose **unchanged — no PRD/acceptance edit** (the spec is silent/under-specified on these refinements, not wrong). Seven answers: **(1) Launch mode = manual-first / operator-entry behind a pluggable screening adapter** — Compliance runs the check externally and records `passed`/`failed`/`under_review`; the automated vendor wires in behind the same adapter post-launch (re-confirms §9.5: "the screen is non-negotiable; only the integration is deferrable"). **No vendor chosen — TBD by Compliance** (BMD §10.7); ComplyAdvantage / Onfido / World-Check are examples, not a selection — build vendor-agnostic. Screened lists fixed = **EU consolidated + Italian UIF + OFAC** (DEC-030 + DEC-041). **(2) Minimum match record on `failed`/`under_review`** (elaborates §9.2 "match metadata"): matched **list/regime** {EU / UIF / OFAC} + **reviewer identity** + **timestamp** + a **required free-text reason/note**; **document/evidence attachment is OPTIONAL at launch** (a note suffices; attachments ride with the automated vendor later). Field *set* is spec; storage/encoding is the dev's (DEC-073). **(3) Re-screen-due = FLAG, not block** — the previous `passed` stands until the re-screen completes; the order-completion gate (§9.3 / Module S S.15) blocks only on `sanctions_status ≠ passed`, and a due date alone does not flip the status. A hard-stop on *egregiously* overdue re-screens is a configurable post-launch tightening, not launch scope. **(4) `under_review`/`failed` = purely manual at launch** — no auto-timeout/escalation; a Customer may remain `under_review` indefinitely. Fail-closed: while not `passed` they cannot transact, so indefinite review = indefinitely blocked (the safe direction). SLA/escalation timers are post-launch (cf. §17 enhanced-KYC document-workflow deferral). **(5) Enhanced-KYC "cumulative annual" = ROLLING 12 MONTHS** (trailing-12-month sum, recomputed by the daily scan §9.2), **not** calendar-year — closes the year-boundary structuring gap (€49k Dec + €49k Jan would never trip €50k under a calendar reset) and is the more AML-conservative reading. (DEC-035 window basis fixed; the €10k/€50k *values* unchanged.) **(6) Threshold measure = GROSS order value** (total the Customer pays, incl. shipping + VAT) **converted to EUR at the order-time FX rate** already recorded per order, aggregated in EUR (thresholds are EUR-denominated; reuses the D18 dual-currency machinery). Net-ex-VAT was the considered alternative; gross chosen as most auditable + conservative. **(7) Crossing the threshold does NOT auto-block** — enhanced-KYC review is operational at launch: the only automatic effects are the enhanced-KYC **flag + timestamp + Compliance review-queue entry** (§9.1) and the **lightweight DEC-030 sanctions re-check** (§9.2(i)). Any block is a *downstream Compliance decision*: setting `kyc_required` transitions KYC `not_required → pending` and auto-places a `kyc` Hold (§9.1) — an operator action, not an automatic consequence of crossing. Matches §9.1 "handled operationally at launch — no separate enhanced-KYC state machine beyond the trigger flag + timestamp." **Build-note (not a decision):** the €10k/€50k values should be **admin-configurable platform constants** (regulatory thresholds evolve), not hard-coded — consistent with "configurable within rules" ([[erp-configurable-within-rules]]); they are **platform-level, not per-customer/club**. The **window basis (5)** + the **measure (6)** are the semantics ratified here. **No new DEC** — bridges DEC-035 + DEC-030 + DEC-041 + DEC-071 + §9.5. | ✅ 2026-06-19 (tech-team Q&A) | Module K PRD §9.1 + §9.2 + §9.3 + §9.5 (prose unchanged); MVP register only; bridges **DEC-035 / DEC-030 / DEC-041 / DEC-071** + tech-team GitHub question (Module K Customer KYC/sanctions screening) + BMD §10.7 (vendor TBD) |
| **MVP-DEC-015** | **Module K erasure × Hold blocking-set completed to the 8-type taxonomy (tech-team Q&A).** The GDPR right-to-erasure × active-Hold precedence (DEC-027, "Stage 6.5"): across the **eight** Hold types (§4.8 / MVP-DEC-008), **exactly one blocks anonymisation — the `compliance` Hold**; the **seven** others (`payment`, `fraud`, `kyc`, `admin`, `credit`, `CHARGEBACK_REVIEW`, `STORAGE_PAYMENT_FAILED`) are non-regulatory and **do not block** (their metadata anonymises alongside the PII; the structural Hold flag is preserved for blocking-state continuity). `compliance` is the regulatory-record-retention type and is **where a sanctions / OFAC / UIF screening finding is recorded — there is no separate `sanctions` Hold type** (the sanctions lifecycle lives on `Customer.sanctions_status`, §9.2; DEC-071). **Confirms the tech team's "only `compliance` blocks" build default.** Erratum of omission — the **same 6→8 gap MVP-DEC-008 fixed in §4.8**: §8.2 + acceptance AC-K-J-9a enumerated only the six base types, leaving the two finance-driven types (DEC-168 / DEC-160) unclassified, and the phrase "sanctions Holds" implied a 9th type that does not exist. Fix: §8.2 reworded to classify all 8 explicitly (1 blocks / 7 don't) + the sanctions-recorded-as-`compliance` clarification; AC-K-J-9a synced verbatim. **No semantics change** — the blocking set stays `{compliance}`, the metadata-anonymisation behaviour is unchanged; this is documentation completeness. **No new DEC** — DEC-027 governs; bridges MVP-DEC-008 + DEC-168 + DEC-160 + DEC-071. | ✅ 2026-06-19 (tech-team Q&A; Phase-E erratum) | Module K PRD §8.2 + Acceptance **AC-K-J-9a**; bridges **DEC-027** + **MVP-DEC-008** + tech-team GitHub question (Module K erasure × Hold blocking set) |
| **MVP-DEC-016** | **Membership payment flow corrected: charge-on-approval mandate at application; producer approval = charge = activation, atomically** (the spec previously documented the wrong "approve → invoice → pay-later → Active" flow). At **application** (all three onboarding flows) the Customer supplies a payment method **+ a charge-on-approval mandate** — consent to be charged the Hero Package fee **when the producer approves**. This is a **save-card-plus-mandate that holds NO funds** (like a subscription sign-up), **not** a held pre-authorisation (which would not survive an approval that arrives after arbitrary delay or a waitlist wait). **Producer approval atomically triggers the charge (capture) AND activation** — approval = charge = `Active`; `Approved` is the **transient approve-and-charge action, not a durable unpaid state**; there is **no separate post-approval "go pay" step**. **A charge that fails at approval → member NOT activated, NO seat consumed**, the Profile stays `Applied`, and the Customer is re-prompted (approval re-attemptable). The mandate **persists through waitlisting** (no funds held) until conversion = approval (§13.5). **Payment instrument = pull-capable: a card (one-step authorize+capture per DEC-101) or a SEPA Direct Debit mandate; a one-off bank/wire transfer is NOT a membership method** (a push transfer cannot be auto-charged on approval) — bank transfer remains available for Discovery (DEC-101/159 unchanged); the instrument set is configurable within that rule, and the mechanism (saved-card vs. mandate token) is the dev team's call (DEC-073). **Coherence correction folded in:** the v1.1-inherited Module K framing "`MembershipFeePaid` emitted by **Module E** for an **INV0** charge" was **stale** — `MembershipFeePaid` is **Module-S-emitted** (Module E records; Module K consumes — DEC-173), and there is **no INV0** (Hero Package fires **INV1** — DEC-157). Module S §5.2, Module E §3.4, and the Architecture event contract already recorded Module S as the emitter; only Module K (§1 / §4.2.1 / §11.1 / §15.2 / §15.8 / §18 + AC-K-EVT-15 / J-16 / MVP-1) carried the stale framing — now aligned. **Dissolves the tech team's "approved-but-not-yet-paid" premise** (approval IS the charge IS activation). **No new DEC** — bridges DEC-101 + DEC-007 + DEC-033 + DEC-114 + DEC-158 + DEC-159; aligns the stale Module K framing to DEC-173 + DEC-157. | ✅ 2026-06-21 (tech-team Q&A) | Module K PRD §4.2.1 + §7 + §11.1 + §15.8; Module S PRD §5 + §9.4 + §10.2; Module E PRD §3.4; Architecture §3.1; + Acceptance AC-K-J-2/3/16 + AC-K-FSM-2 + AC-K-EVT-15; bridges **DEC-101 / DEC-007 / DEC-033 / DEC-114 / DEC-158 / DEC-159** + aligns **DEC-173 / DEC-157** + tech-team GitHub question (Hero Package capacity → corrected membership flow) |
| **MVP-DEC-017** | **Hero Package capacity invariant: the seat-occupying set is `Active` + `Suspended`, enforced at the approval = charge = activation moment** (answers the tech team's Q1/Q2 → both **Option A**). **(Q1)** A `Suspended` membership **keeps** its Hero Package seat; `Suspended → Active` on Hold-lift is **never capacity-re-checked and never blocked** — only an explicit **Cancellation** frees a seat. Rationale = invariant soundness: freeing a seat on suspension would let a returning member exceed the cap or be evicted by a *temporary* Hold, contradicting §10.1 (suspension preserves state) + MVP-DEC-011 (never evict a member over capacity). The seat-occupying set is therefore `Active` + `Suspended`; seats are **freed only by `Lapsed` / `Cancelled` / `Inactive`** and are **never held by `Applied` / `WaitingList` / `Rejected`**. (Corrects the §13.1 "only `Active` consume" phrasing, which misled on suspension.) **(Q2)** The cap is enforced **at approval** — which, post-MVP-DEC-016, **is** the atomic approve = charge = activation instant; with no "approved-but-unpaid" gap, *reserve-at-approval* and *count-only-seat-occupying* are the **same instant** (a charge that fails at approval consumes no seat). **Renewal of an in-force membership is grandfathered, not re-gated** (MVP-DEC-011 — unchanged). The **§13.4 decrease-floor counts the seat-occupying set (`Active` + `Suspended`)** so suspended members can't be orphaned. Cross-module seam: the invariant is defined in Module K §13; the `qty` + anti-orphan floor are owned by Module A (DEC-079); enforcement at order completion / approval is Module S's (§5.1 Gate 3 / §10.2 — count `Active` + `Suspended`). **No new DEC** — editorial completion of DEC-069 (cap = current-state value) + MVP-DEC-011 (grandfathering); bridges DEC-069 + MVP-DEC-011. | ✅ 2026-06-21 (tech-team Q&A) | Module K PRD §13.1 + §13.4 + §10.1 + §4.2.1 + Acceptance **AC-K-J-13 / AC-K-J-15 / AC-K-FSM-2a**; Module S PRD §5.1 + §10.2; bridges **DEC-069** + **MVP-DEC-011** + tech-team GitHub question (Module K Hero Package Capacity Invariant — Q1 Suspended seat / Q2 enforcement point) |
| **MVP-DEC-018** | **Club Credit / Store Credit event vocabulary reconciled to the DEC-174 three-actor split: canonical issuance event = `ClubCreditAccrued` (NOT `ClubCreditIssued`); the application events are Module-S-emitted; Module K owns the balance + consumes from BOTH S and E.** The v1.1-inherited Module K framing — "**Module E emits `ClubCredit{Issued,Applied,Restored,Forfeited}`**; Module K consumes" (§11.4 / §15.8) — was **stale on two counts** against DEC-166 + DEC-174: an **emitter-ownership** mismatch (the customer-facing application events are **Module-S-emitted**, not Module E's) **and** a **naming** mismatch (the issuance event is **`ClubCreditAccrued`**, not `ClubCreditIssued`). Canonical three-actor split: **Module E emits the financial accrual / reversal events** — `ClubCreditAccrued` (creation) + `ClubCreditRestored` (cancellation-window restoration, K §11.1) + `ClubCreditForfeited` (lapse / replacement / cancellation, K §11.3) + the store-credit analog `StoreCreditIssued`; **Module S emits the customer-facing application events** — `ClubCreditAutoApplied` / `ClubCreditRemovedByCustomer` / `StoreCreditApplied` at checkout-render / customer action (DEC-111); **Module K owns the credit-balance entity** (DEC-166) and **consumes from BOTH** S (application) and E (accrual / reversal); Module E **also** consumes the S application events to route the prepayment offset to Xero (DEC-072). **`ClubCreditForfeited` + `ClubCreditRestored` are NOT renamed or reassigned** — DEC-174 enumerates only the accrual + application events, so the two it leaves unnamed are **preserved as Module-E-emitted** (both back real launch behaviour — the K §11.3 forfeiture job + the K §11.1 `redeemed → active` cancellation path); minimal-invention, no new greenfield semantics (Paolo-ratified). **Same class as MVP-DEC-016** (which corrected the parallel `MembershipFeePaid` "Module E emits" framing to "Module S emits" per DEC-173) — deliberately left out of scope there as orthogonal to the membership payment flow. **Full sweep (Paolo-ratified):** Module S PRD / Acceptance + the BMD (§2.3 / §4.12) **already used** `ClubCreditAccrued` + the S-side application names — **no change**; **stale surfaces corrected** = Module K PRD §0 / §11.4 / §15.8 / §18 + Acceptance AC-K-EVT-16 / AC-K-XM-21, and Module E PRD §5.3 / §8.1 / §8.2 + Acceptance AC-E-BR-Cred-2 / AC-E-EVT-21 / AC-E-EVT-22 / AC-E-EVT-28 / AC-E-XM-5 / AC-E-XM-10 (Module E was stale **in lockstep with K**, not authoritative); **plus the contradictory "(resolved)" digest note** in Architecture §8.3 item 6 + MVP Release Index §5.2 item 6, which had standardised K + E onto `ClubCreditIssued` — the **opposite** of the frozen DEC-174 — and was never ratified as an MVP-DEC. **Follow-up completion (same-day, same erratum):** the Architecture **§8.1 event-contract table** (the emitter↔consumer spine — distinct from the §8.3 digest note) carried the same stale row (`ClubCredit{Issued,Applied,Restored,Forfeited}` / `StoreCredit{Issued,Applied}` attributed to Module E) and was missed in the first pass; corrected to the three-actor model — the E-emitted `ClubCreditAccrued` / `ClubCreditRestored` / `ClubCreditForfeited` / `StoreCreditIssued` (consumed by K) stay on the Module E row, and a **new Module S row** carries `ClubCreditAutoApplied` / `ClubCreditRemovedByCustomer` / `StoreCreditApplied` (consumed by K for the balance + E for the Xero offset, DEC-072). **No behaviour change** — emitter-ownership + event names only. **No new DEC** — bridges DEC-166 + DEC-174. | ✅ 2026-06-21 (tech-team Q&A; Phase-E erratum) | Module K PRD §11.4 + §15.8 (+ §0 + §18) + Acceptance **AC-K-EVT-16 / AC-K-XM-21**; Module E PRD §5.3 + §8.1 + §8.2 + Acceptance **AC-E-BR-Cred-2 / EVT-21 / EVT-22 / EVT-28 / XM-5 / XM-10**; Architecture §8.1 (event-contract table) + §8.3 item 6; MVP Release Index §5.2 item 6; bridges **DEC-166 + DEC-174** + tech-team GitHub erratum (Module K §11.4/§15.8 "Module E emits ClubCredit*") |
| **MVP-DEC-019** | **PIM approval gate after reject/edit made explicit (review-freshness invariant).** Tech-team question (CML-21 `PimLifecycleService`): §4.3 / BR-Lifecycle-6 described the reject *behaviour* (entity stays `reviewed`, Creator edits in place + re-submits, "the flow restarts from the review step") but never stated the enforceable *gate* — so the literal FSM (where `reviewed` = "review passed, submitted for approval", §4.1) let an Approver activate a just-rejected or edited-since-review entity. Ruling (faithful to intent; **no `draft` revert** — the PRD deliberately excludes it): **(i)** a pending un-remediated rejection **blocks** `reviewed → active` — an Approver must not activate a flagged entity; **(ii)** "re-submission" is an **explicit, audited Creator action** (not an implicit effect of editing — required fields may be filled across sessions), which clears the **live gate-flag** (the rejection **history** is never cleared, §4.8) and restarts from review (a **distinct** re-Reviewer, BR-Lifecycle-1 floor); **(iii)** **review freshness** — editing the review-governed (identity/quality) content of a `reviewed` entity invalidates the prior review and re-arms the review step; purely **observational enrichment** edits (`EnrichmentDataUpdated`) do **not** gate. Mental model: lifecycle **state** (`reviewed`, never reverts to `draft`) is orthogonal to the approval-workflow **position**; `reviewed → active` gates on "the current content has a fresh passing review." Representation (flag / sub-status / version-stamped review record) is the dev team's call (DEC-073). **Cross-module:** the Creator→Reviewer→Approver workflow exists only in Module 0 + Module K (§4.4 "analogous to the PIM lifecycle in Module 0") + the Admin Panel (§3.0 / §5.2 reference Module 0); both **inherit** the gate via their existing Module-0 references — **no edit needed** (confirmed by a read-only cross-module consistency sweep before commit). **No new DEC** — erratum of omission completing the v17-inherited lifecycle (Q-AD-1 / DEC-060); the MVP-DEC-007 separation-of-duties floor is unchanged. BMD unchanged. | ✅ 2026-06-22 (tech-team Q&A) | Module 0 PRD §4.3 + §13.2 (**BR-Lifecycle-6**) + Acceptance **AC-0-J-7 / AC-0-BR-Lifecycle-6** (verbatim-synced); bridges **Q-AD-1 / DEC-060** + **MVP-DEC-007** + tech-team GitHub question (Module 0 PIM rejection/approval gate, CML-21) |
| **MVP-DEC-020** | **Hero Package capacity ownership clarified: Module A's allocation `qty` stays the single source of truth; *how* Module K / Module S enforce the cap (live read vs. a Module-K read-model) is a DEC-073 implementation choice.** The tech team proposed flipping club-capacity ownership from Module A to Module K (K stores + owns capacity, updated by an A signal; S reads K). **Declined as a product/ownership change (Paolo):** club capacity **cannot diverge** from the Hero Package allocation `qty` — capacity *is* the allocation `qty` (BMD §2.3 / DEC-007 / DEC-069 / DEC-079) — so Module A's `qty` **remains the single source of truth**, and a K-owned capacity number would be a drift-prone mirror with no independent meaning. Module K keeps owning + enforcing the **invariant** (§13); Module A owns the **number** — this **reaffirms** the A-owns-the-number seam recorded in MVP-DEC-011 / MVP-DEC-017 (their substance — seat-occupying set, grandfathering, attrition floor — unchanged). **The real (small) issue = the spec over-specified the enforcement *mechanism*:** Module K §16 said K "does not duplicate the value" and Module S §5.1 said S reads the `qty` "at validation time (no caching)" — two phrases that, read literally, **forbid a legitimate DEC-073 implementation** the dev team is free to choose: Module K maintaining a derived, **reconciling read-model** of `qty` fed by A's `AllocationCapacity*` events, with Module S querying a Module K capacity view rather than reading Module A directly (the same pattern the spec already blesses for the Module A↔B ATP cache, A §11.5.1 / DEC-187). Fix (**mechanism-only; zero product / ownership / behaviour change**): softened those two sentences + their verbatim-synced acceptance mirrors (**AC-K-XM-18 / AC-S-XM-6**) to state the *requirement* (A's `qty` = single source of truth; capacity = the allocation `qty`; strongly-consistent at the validation boundary) while making the read mechanism (live vs. read-model; S→A vs. S→K) explicitly the dev's call (DEC-073). The Layer-1 no-oversell read of `qty − issued` (AC-S-XM-8) is a **different** contract — untouched. Pre-commit read-only sweep (2 agents) found no contradiction; flagged one **pre-existing, non-contradicting** gap — Architecture §8 event-contract row (~line 355) omitted Module K from the `AllocationCapacity{Increased,Decreased}` consumer list though K §13.4 consumes it — **fixed in a Paolo-approved follow-up (added Module K to the consumer row)**. **No new DEC** — DEC-073 governs the spec-vs-physical line. | ✅ 2026-06-22 (tech-team Q&A) | Module K PRD §16 + Module S PRD §5.1 + Acceptance **AC-K-XM-18 / AC-S-XM-6** + Architecture §8.1 (event-contract consumer row); bridges **DEC-073 / DEC-069 / DEC-079 / DEC-007** + reaffirms **MVP-DEC-011 / MVP-DEC-017** + tech-team GitHub question (Hero Package capacity ownership — Module A vs Module K) |
| **MVP-DEC-021** | **CML-88 Customer & Party data-model completion — five Module K business-rule clarifications ratified** (one already-decided confirm, three errata-of-omission, one genuine-open MVP-scope call). Tech-team question (CML-88): the PRD covered most of the Customer & Party data model but left five rule-areas ambiguous or silent. Rulings (all Paolo-ratified). **(1) Third-Party Owner subtype = seam-only (already-decided; no doc change).** The `third_party_owner` value stays a dormant, behaviourless seam in the immutable party-type enum — no agency/consignment behaviour, no `AgencyAgreement` — kept dormant by DEC-011 (no active consignment) + DEC-017 (consumer-only); reintroduction (subtype + AgencyAgreement go live) stays the §17.2 #9 post-launch item. Does **not** un-defer active consignment. Already covered by §4.4 / §4.5 / §16 + BR-K-Identity-5 + AC-K-XM-24 / AC-K-BR-Identity-5 — no edit. **(2) Email-change workflow = new blocking / old non-blocking notice; Customer stays `active`.** BR-K-Identity-1 said only "verification + old-email confirmation workflow" (the same one-liner in frozen v1.1 — the blocking semantics + lifecycle behaviour were **never specified**, not compressed out). Ruling: the **new** address completes a **blocking** click-verification (the change commits only on its confirmation); the **old** address gets a **non-blocking** security notice (with a revoke link), **not** a second blocking confirmation — a blocking old-leg would let a lost old mailbox strand the change (often the very reason for the change). The Customer **stays `active`** and transacts on the existing verified email throughout — an email change is a **field-scoped re-verification, never an `active → pending` revert** (the lifecycle has no such edge; reverting would wrongly re-gate KYC/T&C/transactions). Rewrote BR-K-Identity-1 + AC-K-BR-Identity-1 (verbatim-synced). **(3) Company-billing + Address = exactly `company_name` + `vat_id`, free-text, no VIES; no address-type model.** Confirmed (DEC-068): launch billing field set = **exactly `company_name` + `vat_id`** (free-text, echoed onto invoices, **no VAT/VIES validation**); the fuller B2B cluster (registration number, legal-entity type, billing/primary contact) stays dropped. **No billing-vs-shipping address-type taxonomy and no stored default-billing** at launch — Address is a Customer-scoped record (an address book Module K owns; Module C reads it read-only + copies it onto the SO `destination`), a **referenced field-cluster, not one of Module K's eight first-class entities**; the "billing" role is just the Address selected at purchase. Physical `address_type` / default flag = dev's call (DEC-073). Also closes a **pre-existing v1.1-inherited gap** (Modules C/S cite a "Module K §4 Address entity" the §4 entity list never defined). Appended a §4.1 clarity clause + enriched AC-K-XM-25 verification. **(4) Suspension review-flag fires on the last-`Active`-leaves transition (not pure-Discovery); auto-clears; informational.** BR-K-Customer-3 ("zero active Profiles → flagged for review") was literally ambiguous. Ruling: the flag fires on the **≥1 → 0** transition — a previously-active Customer's **last `Active` Profile** moving to `Lapsed` / `Cancelled` / `Inactive`; it does **not** fire for a never-active Customer (pure-Discovery with no Profile, or Waiting-list-only — both normal states per §5; flagging the whole Discovery base would be pure noise). It **auto-clears** when an `Active` Profile returns (derived condition). It is **informational at launch** (ops follow-up); downstream re-activation outreach rides the **already-specced `CustomerTransitionedToLegacy` / `CustomerSegmentChanged`** events (§5 / §15, AC-K-EVT-13), **not** the flag. Rewrote BR-K-Customer-3 + AC-K-BR-Customer-3 (verbatim-synced). **(5) GDPR right of access / data export = automate-OK but operator-triggered (not self-serve) at launch; Module-K-PII slice with the cross-module bundle phased; `closed` = full right, anonymised = nothing.** §12 left right-of-access "operationally executed … not modelled as a state machine"; Architecture is silent; **no AC mirrored it.** Ruling: the floor **stays operator-executed**; an **automated structured-export endpoint may serve it** (format — JSON or equivalent — + mechanism = dev's call, DEC-073; JSON meets the GDPR Art. 20 machine-readable bar), but at launch it is **operator-triggered + access-controlled, NOT customer self-serve** (self-serve adds an identity-proofing / PII-egress surface the manual verification provides — clean post-launch upgrade). **Launch export scope = Module-K-owned PII** (Customer, Account, Profiles, consent, Holds, Addresses, Club Credits, audit); the **cross-module transactional bundle** (Orders/Invoices — S/E; serialized bottles — B/C; shipments — C) is **phased as a dependency** + assembled by the manual ops process until then (**no data debt** — the Module-K slice composes with the phased export). A **`closed` (not-yet-anonymised) Customer retains the full right** (PII intact — BR-K-Customer-2); an **anonymised Customer has nothing to return** — the right is extinguished by the erasure itself (§8.2); surviving history is keyed only to the opaque identifier. Rewrote the §12 right-of-access bullet + **added AC-K-J-9b** (the §12 verbatim-sync gap meant a new AC, not an amend). **No new DEC** — confirms / errata-of-omission + one MVP-scope call; bridges the frozen decisions + DEC-073 for the spec-vs-physical lines. BMD unchanged. | ✅ 2026-06-24 (tech-team Q&A) | Module K PRD §14.1 (**BR-K-Identity-1**) + §14.2 (**BR-K-Customer-3**) + §4.1 (company-billing / Address) + §12 (right of access) + Acceptance **AC-K-BR-Identity-1 / AC-K-BR-Customer-3** (verbatim-synced) + new **AC-K-J-9b** + **AC-K-XM-25** (enriched); confirms **AC-K-XM-24 / AC-K-BR-Identity-5** (Q1, no edit); bridges **DEC-068 / DEC-011 / DEC-017 / DEC-027 / DEC-026 / DEC-040 / DEC-073** + tech-team GitHub question (CML-88 Customer & Party data-model completion) |
| **MVP-DEC-022** | **CML-89 Membership, Onboarding & Club-Governance completion — seven Module K clarifications ratified** (three confirm-only, two errata-of-omission, two BMD-grounded reversals of the dev's stated default). Tech-team question (CML-89): the dev proposed defaults on seven residual product ambiguities. Rulings (all Paolo-ratified; **one MVP-DEC, structured by sub-question** — the MVP-DEC-014 / MVP-DEC-021 batch precedent). **(1) Waitlist conversion on capacity increase = manual, producer-discretionary, NO FIFO (confirm; no edit).** The CML-89 task text ("auto-convert `waiting_list → approved` FIFO") is **loose** — §13.5 + DEC-069 + §17.2 #5 + AC-K-J-14 all mandate manual producer approval up to N (the one retained producer write, L-PP); FIFO / auto-conversion stays deferred (§17). PRD wins (handoff read-order rule). **(2) `registration_flow` vs `invite_only` = collapse to the enum; it is an entry channel, not an approval bypass (erratum).** §4.3 carried two overlapping / contradictable fields. Ruling: **drop the `invite_only` boolean** (subsumed: `invite_only=true` ⟺ `invitation_only`); `registration_flow` selects the onboarding **entry channel only** — producer approval (atomic approve=charge=activation, §4.2.1) is mandatory for **every** value, **no value auto-approves** (DEC-069). Launch-live: `application_with_approval` (open self-application §7.1, default) / `invitation_only` (§7.3) / `link_onboarding` (§7.2); **`open` (auto-join) carried latent, not selectable at launch** (would contradict the mandatory producer write). Rewrote §4.3 + added **BR-K-Club-6** / **AC-K-BR-Club-6**; representation = dev's (DEC-073). **(3a) `credit_grace_days` = the PRD's per-Club cancellation grace = `cancellation_grace_days` (confirm; no edit).** Same field (align to the established token, MVP-DEC-013); it gates **only Profile-cancellation forfeiture** — not year-end `valid_to` lapse, renewal-replacement, or closure (§11.3:584). **(3b) Build the credit-validity fields configurable at current spec defaults (confirm; no edit).** 31-Dec `valid_to` + 30-day grace are the build defaults; the 31-Dec-vs-anniversary / 30d-vs-none conflicts vs the v2.23 Producer Agreement stay **deferred to the legal consistency review** (MVP-DEC-013 + the 2026-06-24 assessment). **(3c) Suspension does NOT pause the `valid_to` clock — credit forfeits at `valid_to` even while suspended (genuine-open; ruled).** §10.1 freezes the credit *balance* (no accrual / redemption) but not the calendar; the daily lapse job runs regardless. Faithful to the literal §11.3 FSM, simplest build; the member self-cures by lifting the Hold and redeeming before `valid_to`. A suspension-aware clock pause / extension is a post-launch option folded into the deferred credit-validity review. Edited §11.3 + AC-K-FSM-17. **(4) Producer content edits RE-ARM review — the dev's "operator-driven, no re-review" default is REJECTED (already-decided by inheritance).** **MVP-DEC-019** established review-freshness and recorded that Module K §4.4 ("analogous to the PIM lifecycle") inherits it; producer name / description is the customer-facing Bottle-Page narrative the gate protects. Ruling: editing review-governed producer content (name / description / region / website) of an `active` Producer re-enters the Creator → Reviewer → Approver workflow and does not publish until it passes (the Producer stays `active` serving last-approved content meanwhile); review **depth** stays admin-configurable (a light 2-step is fine — keeps re-review lightweight). The §4.4 cross-reference alone was too implicit (the dev read the opposite) → made explicit. Edited §4.4 + added **BR-K-Producer-5** / **AC-K-BR-Producer-5**; re-arm representation = dev's (DEC-073). **(5a) Onboarding token lifecycle (erratum-of-omission).** §7 described the flows but not the tokens. Ruling: **Club-link token = reusable / long-lived / operator-revocable** (a broadly-shareable link, §7.2); **producer-invite token = single-use (one invitee email) / expiring / operator-revocable before acceptance** (§7.3). Exact mechanics (TTL, single-use enforcement) = dev's (DEC-073). Edited §7.2 / §7.3 + AC-K-J-2 / AC-K-J-3. **(5b) Registration age-gate ADDED — the dev's "no age-gate" default is REVERSED; the BMD already mandates it (erratum-of-omission).** **BMD §2.8** mandates *"Age verification at registration. Mandatory; gating signup … self-attest plus card-on-file … no physical-document verification at launch"* — Module K's PRD had **dropped** it. Restored: a **hard 18+ gate at registration** (block if self-attested `date_of_birth` < min at registration date; no Customer created), min-age an **admin-configurable platform constant default 18** (EU alcohol-purchase baseline; cf. MVP-DEC-014 threshold constants); launch mechanism = self-attest + payment-method-bound check, no documents; per-jurisdiction higher floors (e.g. US 21) downstream. Edited §7.1 + added **BR-K-Identity-6** / **AC-K-BR-Identity-6** + AC-K-J-1. **(6) Profile `role` = bounded inherited set, single-role + inert at launch (confirm; no edit).** The v17 role set (Member / IO / Admin) — **not** free-text; single-role + no eligibility / permission effect at launch (the DEC-062 latent-tier analog); representation = dev's (DEC-073). §4.2 already adequate. **(7) Per-Profile `auto_renew` inherits the Club default at creation AND is customer self-toggleable — the dev's "operator-only" half is REVERSED (BMD-mandated).** **BMD §2.4 / B2** mandates customer self-serve auto-renewal cancellation via the Consumer Portal; trapping a member in auto-renewal is a consumer-protection problem. Ruling: `auto_renew` default-inherits `renewal_policy.auto_renew` at creation **and** the Customer may self-toggle it via the Consumer Portal (an Operator may also set it) — a Consumer-Portal write is in launch scope. Edited §4.2 + added **BR-K-Profile-5** / **AC-K-BR-Profile-5**. **No new DEC** — confirms + errata-of-omission + two BMD-grounded reversals; bridges DEC-069 / DEC-073 + MVP-DEC-013 / MVP-DEC-014 / MVP-DEC-019 + BMD §2.4 / §2.8. BMD unchanged (it was the authority). | ✅ 2026-06-24 (tech-team Q&A) | Module K PRD §2 (Consumer-Portal self-service-write carve-out) + §4.2 + §4.3 + §4.4 + §7.1 / §7.2 / §7.3 + §11.3 + §14.1 (**BR-K-Identity-6**) + §14.3 (**BR-K-Profile-5**) + §14.4 (**BR-K-Club-6**) + §14.5 (**BR-K-Producer-5**) + Acceptance **AC-K-BR-Identity-6 / AC-K-BR-Profile-5 / AC-K-BR-Club-6 / AC-K-BR-Producer-5** (verbatim-synced) + AC-K-J-1 / J-2 / J-3 / J-10 + AC-K-FSM-17; bridges **DEC-069 / DEC-073 / MVP-DEC-013 / MVP-DEC-014 / MVP-DEC-019 + BMD §2.4 / §2.8** + tech-team GitHub question (CML-89 Membership, Onboarding & Club Governance Completion) |
| **MVP-DEC-023** | **CML-92 PIM type-generalisation clarifications — seven Module 0 product-rule rulings ratified** (two confirm/already-decided, three errata-of-omission, two genuine-open forward-seam calls). Tech-team question (CML-92): mapping the neutral-core + Product-Type seam surfaced seven under-specified / potentially-contradictory product points. Rulings (all Paolo-ratified; **one MVP-DEC, structured by sub-question** — the MVP-DEC-014 / 021 / 022 batch precedent; physical representation is out of scope, DEC-073). **(1) LWIN persistence & dedup (genuine-open + confirm).** The LWIN-7/11/16 code **is persisted** on the `WINE` attribute set as an external reference code for re-enrichment / cross-reference (capture-then-own, §5.5 — v1.1 persisted the *data* Liv-ex returns but never the *code*); it is **NOT part of the identity/dedup key** (the key stays `producer + product name + appellation`, BR-Identity-1, so dedup behaves identically on the manual path where no LWIN exists) — an advisory matching-LWIN warning at data entry is permitted but not an enforced constraint. Edited §3.9 + §5.1 + §5.5. **(2) Appellation-null wines & the key (genuine-open; under-specified in v1.1 too, not a regression).** **Appellation is optional**; a **null appellation is a significant key value** — two null-appellation wines with the same producer + name collide as duplicates, an appellation-bearing and a null-appellation wine with the same producer + name are distinct (the key degrades to producer + name when absent). Dedup matching is **normalisation-insensitive** (case / accent / whitespace-folded for the comparison; stored values keep the operator's casing; the algorithm is DEC-073). Amended BR-Identity-1 + §3.1 + §3.9 + AC-0-BR-Identity-1 + AC-0-GEN-5 (verbatim-synced). **(3) `region` ownership PIM vs Module K (erratum-of-omission).** Module K Producer carries `region` (§4.4) **and** the Module 0 `WINE` attribute set carries the wine's region (§3.9) — a genuine overlap that was never disambiguated. Ruling: **conceptually distinct, not a mirror** (so — unlike MVP-DEC-020 — both legitimately exist; neither is derived from the other): Producer.region = the producer's home region (producer-level narrative); `WINE`.region = the wine's region tied to its appellation, and is **authoritative for the wine's region on the Bottle Page**. Edited Module 0 §9.1 + §3.9 + AC-0-XM-3 + a disambiguation note in Module K §4.4 (the Producer identity bullet also carries `optional appellation` — same producer-vs-wine split). **(4) `product_type` immutable after creation (erratum-of-omission; intent implied).** Confirmed **immutable for the life of the Master** — Product Type selects the identity key, attribute set, variant axis, adapter, and Format vocab, so an in-place change would invalidate identity + every type-driven binding; a reclassification is **retire + register a new Master**. Added **BR-Identity-5** + §3.1 clause + **AC-0-BR-Identity-5** (a forward-seam guardrail — at launch `WINE` is the only type, so no reachable cross-type transition, but the immutability constraint is testable now). **(5) Format ↔ Product Type binding (genuine-open forward seam; moot at launch).** A Format **may be admissible for one *or more* Product Types** (physical measures can be shared; not scoped to exactly one) — but **not built at launch**: with a single type every Format is implicitly `WINE`-admissible and no Format→Type binding structure is required; the explicit per-Format admitting-type set is additive when a 2nd type lands (consistent with the §16 don't-over-build guard). Light §3.5 note; register only otherwise (nothing testable at launch beyond WINE-admits-all). **(6) Catalog RBAC at launch (already-decided; MVP-DEC-007 + Mod0-Q2 / §4.2 / §2).** Ship the **configurable separation-of-duties primitive** (no self-approval, distinct actors on every configured step, audited; 2-or-3-step by config), **NOT** three hard-coded first-class roles (the named Operator / Reviewer / Approver personas are the governance description / 3-step default; the full RBAC / authority-tier model is admin-configurable + downstream). **Operational input (Paolo): the launch catalog team runs the 2-step (Creator → Approver) config** — the build supports both depths regardless; no spec change. Register only (no PRD edit). **(7) Future Service / Experience SKU: new event family, no seam field now (already-decided by the MVP-DEC-012 pattern).** When the deferred §17.1 subtype lands it will be a **new, distinct event family** (e.g. `ServiceSKU*`), **not** a `kind` / subtype discriminator on the Intrinsic `SellableSKU*` family (§14.2 fixes family-based dispatch, never a payload flag — the same rule that keeps Intrinsic vs Composite as two families, MVP-DEC-012); and **no seam field is reserved on the Intrinsic family now** — the subtype is purely additive later (a new family + its own attribute / pricing surface), so a pre-emptive field would violate the §16 don't-over-build guard and buys nothing (§17.1 "additive later"). Forward-direction §17.1 note; nothing built at launch. **Process note (not a decision):** the non-behavioural-for-wine sign-off **AC-0-GEN-9** (MIXED) is scheduled toward the end of CML-92 — unaffected here; nothing in this MVP-DEC changes wine behaviour (dedup for appellation-bearing wines, lifecycle, events, enrichment flow all identical — the additions are the optional-appellation edge, LWIN persistence, region disambiguation, and forward-seam guardrails). **No new DEC** — confirms + errata-of-omission + two forward-seam MVP-scope calls; bridges DEC-073 (spec-vs-physical) + DEC-065 (generalisation) + MVP-DEC-004 / MVP-DEC-007 / MVP-DEC-012. BMD unchanged. | ✅ 2026-07-01 (tech-team Q&A) | Module 0 PRD §3.1 + §3.5 + §3.9 + §5.1 + §5.5 + §9.1 + §13.1 (**BR-Identity-1** amended, new **BR-Identity-5**) + §17.1; Module 0 Acceptance §4.1 header + **AC-0-BR-Identity-1** + new **AC-0-BR-Identity-5** + **AC-0-GEN-5** + **AC-0-XM-3** + §1.1/§4 counts; Module K PRD §4.4 (region/appellation disambiguation note); bridges **DEC-073 / DEC-065 / MVP-DEC-004 / MVP-DEC-007 / MVP-DEC-012** + tech-team GitHub question (CML-92 PIM type-generalisation) |
| **MVP-DEC-024** | **Retirement approval-gating made explicit — PIM entities (already-gated; erratum at the BR/AC layer) + Producer offboarding (net-new dual-control gate).** Tech-team question: is `active → retired` approval-gated for PIM entities and for Producers, and is it never stated explicitly? Findings split cleanly by module. **PIM entities (Q1–Q3) = already-decided; the gap was one layer down.** Module 0 §4.2 **already** enumerates `active → retired` (alongside `reviewed → active` and `retired → active`) as a commercial-impact transition that passes the Creator → Reviewer → Approver workflow — so retirement of all seven PIM entities (Product Master / Variant / Reference, Sellable + Composite SKU, Format, Case Configuration) **is** approval-gated, and it **uses the same configurable role-count (Q2 — the lighter 2-step Creator → Approver is available), separation-of-duties floor preserved** (Q2). The dev's agent was right that it read as *inferred*: the **BR/acceptance layer under-mirrored §4.2** — **BR-Lifecycle-2** named only *re-activation* approval (not retirement), and **no acceptance criterion asserted retirement-through-workflow** (AC-0-FSM-9 tests re-activation approval; AC-0-J-9 tests retire-blocked-by-open-refs; AC-0-FSM-11 tests the retirement *cascade* — none tested the gate). *Expressio unius*: naming re-activation but not retirement implied retirement wasn't gated. **Erratum of omission at the BR/AC layer (no semantics change, no new DEC):** made **BR-Lifecycle-2** state that both retirement (`active → retired`) and re-activation pass the same workflow at the configured depth — "retiring a PIM entity requires approval" — and verbatim-synced **AC-0-BR-Lifecycle-2** + upgraded its test to exercise the retirement gate (self-approval rejected; distinct actors succeed; `*Retired` fires; at 3-step and 2-step depth). §4.2 prose left as the authority (Paolo declined a standalone §4.2 line — minimal churn). **Producer (Q4) = genuine-open; ruled: dual-control approval (Paolo).** Unlike PIM, Producer offboarding was explicitly **operator-initiated, un-gated** (§10.2 step 1 "Operator initiates Producer retirement"; §4.6.1 "a separate operator action"); Producer *activation/content* is gated (§4.4 Creator → Reviewer → Approver, BR-K-Producer-5) but retirement wasn't. The asymmetry is real: offboarding is the **highest-blast-radius action in Module K** — it cascades to `sunset` every Club the Producer operates, winds down all member Profiles, and triggers Club-Credit conversion + refunds (§10.2 steps 2–6), and the Producer sits *upstream* of PIM. Ruling: the `active → retired` transition **requires dual-control approval** — a **distinct second operator** (initiator ≠ approver) must approve before the Producer retires and `ProducerRetired` fires; audited (initiator, approver, timestamp, reason). It **reuses the configurable separation-of-duties primitive (MVP-DEC-007), NOT the content Creator → Reviewer → Approver lane** (§4.4) — offboarding is a commercial-relationship wind-down authorisation, not a data-quality review, so it is a two-actor authorisation with no "review" step; role-count/gating mechanism/RBAC tier = dev's (DEC-073), no-self-approval floor holds at any depth. Edited Module K §10.2 step 1 + §4.6.1 cross-ref + added **BR-K-Producer-6** + verbatim-synced **AC-K-BR-Producer-6** + cross-ref on **AC-K-FSM-7**. **No new DEC** — PIM half = erratum of omission completing §4.2; Producer half = a launch-scope governance addition bridging MVP-DEC-007. BMD unchanged. | ✅ 2026-07-02 (tech-team Q&A) | Module 0 PRD §13.2 (**BR-Lifecycle-2**) + Module 0 Acceptance **AC-0-BR-Lifecycle-2** (verbatim-synced, test upgraded); Module K PRD §10.2 + §4.6.1 + §14.5 (new **BR-K-Producer-6**) + Module K Acceptance §4.5 header + new **AC-K-BR-Producer-6** + **AC-K-FSM-7** (cross-ref); cites Module 0 §4.2 (already-authoritative); bridges **DEC-073 / MVP-DEC-007 / MVP-DEC-019 / Mod0-Q2 / Mod K-Q3** + tech-team GitHub question (PIM + Producer retirement approval-gating) |
| **MVP-DEC-025** | **Case Configuration shape & identity specified — five Module 0 clarifications ratified** (all genuine-open; the shape was never specified in v1.1 either — not a re-baseline regression). Tech-team question: the PRD listed three Case Configuration attributes (units per case, packaging type, physical form/attributes; no breakability flag) but never pinned the *shape* — the `packagingType` vocabulary, what `physicalAttributes` are, the uniqueness rule, or the `LOOSE` invariant. Rulings (all Paolo-ratified; value-sets + invariants = spec, representation = dev's per DEC-073). **(1) `packagingType` = a controlled base-code vocabulary, bounded / validated / approval-extensible — NOT free text, NOT a hard-coded constant** (the [[erp-configurable-within-rules]] house rule; Case Configuration is already approval-governed reference data per §2, so the vocabulary extends via that same standard workflow). Launch **seed set = `OWC`, `OC`, `CARTON`, `LOOSE`** (named as seed, set stays open/extensible — the Format "WINE = bottle sizes" pattern). Human labels (`OWC` → "Original Wooden Case") = presentation metadata. **(2) Derived display name = granted; dev's call (DEC-073), no stored name required** — the canonical label derives from `packagingType + unitsPerCase` (`OWC6` / `OWC1` / `CARTON12` / `loose`); stays unique because the identity key is `(packagingType, unitsPerCase)` (Q4). **(3) `physicalAttributes` = optional descriptive operator metadata, display / audit only, NON-identifying** (PIM owns identity + is silent on operating-model concerns; these are light notes — wooden vs cardboard, presentation — NOT shipping dimensions / weight, which are fulfilment's; free-text is the §16 minimal floor; free-text-vs-structured = dev's, DEC-073); **excluded from the uniqueness key**. **(4) Uniqueness = `(packagingType, unitsPerCase)`** — two entries differing only in `physicalAttributes` are the **same** config with different notes, not distinct (business invariant → spec). **(5) `LOOSE` invariants** — `packagingType = LOOSE` **requires** `unitsPerCase = 1`; the converse does **not** hold (`unitsPerCase = 1` does not imply `LOOSE` — `OWC1` is a legitimate distinct config); `unitsPerCase ≥ 1` always; no minimum forced on `OWC` / `OC` / `CARTON` (don't over-constrain). **Fix scope:** enriched Module 0 §3.6 prose (harmonised "physical form" → "physical attributes") + new **BR-RefData-3** (Case Configuration shape & identity) + verbatim-synced **AC-0-BR-RefData-3** (AUTO) + tally sweep (Acceptance §4 header `29 → 30 BRs`; §1.1 distribution `~101 → ~102`, `98 → 99 AUTO`; §4.6 header `BR-RefData-1..2 → ..3`). **Cross-module clean** — uniqueness + vocabulary are internal to the entity; Modules A (Layer 2), B (StockPosition dimension), S (Offer→iSKU) reference the *entity*, not its `packagingType` field (confirmed by pre-commit read-only sweep). **No new DEC** — genuine-open spec completions; bridges **DEC-073** (spec-vs-physical) + **MVP-DEC-012** (Intrinsic-only Case-Config) + [[erp-configurable-within-rules]]. BMD unchanged (§736 uses `OWC6`/`OWC12` as shorthand — validates the naming convention, mandates nothing). **Follow-up (same day, engineer clarification on point 1): the Case Configuration approval flow IS the introduction mechanism for new `packagingType` base codes.** An operator may **select an existing code or introduce a new one inline**; the new code is minted as a controlled value **only on approval** of that Case Configuration (rejected ⇒ no code minted), as an **explicit, approver-visible, normalised, near-duplicate-warned** act (not a bare free-text box) — riding the **Approver-level reference-data approval §2 already defines** ("entries that expand the reference set"), so no separate vocabulary-governance workflow is stood up. Standalone-lookup-with-own-lifecycle vs. derived-from-approved-Case-Configs + the entry UX = dev's (DEC-073). Sharpened §3.6 + BR-RefData-3 + AC-0-BR-RefData-3 (governance made explicit; the fragile-implicit-made-explicit pattern, MVP-DEC-022 Q4). **Follow-up 2 (same day, engineer proposed deferring self-serve creation): ACCEPTED — launch ships a FIXED predefined (seeded) `packagingType` set; self-serve runtime creation of new codes is deferred post-launch (new §17.7).** The defer is of the **management surface only** — capability + data model ship in full (KEEP-in-full holds), and the vocabulary stays controlled + extensible: **guardrail** = seed the set as **reference data** (code + label + normalised form), extensible by a cheap config addition, **NOT a hard-coded enum constant** (keeps [[erp-configurable-within-rules]] + averts the fixed-set-too-small hard-block → **vet the seed against the real launch catalogue** with the Catalog Lead / Ops). The engineer's label observation is correct and reinforces the defer: a fixed set ships code→label as seed data (no label-capture UX at launch); label capture becomes part of the deferred create flow. Reframed §3.6 + BR-RefData-3 (launch = select-from-seed; create-new = post-launch) + annotated the AC-0-BR-RefData-3 create/normalise/label assertions "verified when the flow lands" (§0.1 deferred-slice pattern) + **new §17.7** (sole MVP-scope surface-deferral). No cross-module impact; uniqueness / LOOSE / `physicalAttributes` / derived-name unchanged. **Supersedes Follow-up 1's launch-inline-create framing** — that mechanism is now the post-launch spec. **Follow-up 3 (same day, engineer flagged missing i18n on the label): the `packagingType` human-readable label is TRANSLATABLE — MVP scope (erratum-of-omission).** It rides the §8 i18n model (six launch locales, DEC-031; content-on-entity; English fallback §8.3, no hard locale-completeness gate) **and carries singular + plural forms per locale** — it renders with a unit count ("1 Original Wooden Case" / "6 Original Wooden Cases"); of the six launch locales EN/DE/IT/FR carry both forms, JA/zh a single form; post-launch locales with additional CLDR plural categories = representation layer (DEC-073). Seed data ships **per-locale singular/plural labels** (refines Follow-up 2's "code + label" → "code + per-locale singular/plural labels + normalised form"). The **derived code shorthand** (`OWC6` / `loose`) stays **untranslated**. The §8 model + the label both already existed; the translate-and-pluralise requirement was never stated → erratum. Edited §3.6 + BR-RefData-3 + AC-0-BR-RefData-3 + a **§8.1 carve-out** (the label is the one targeted translatable-field addition since v1.1 + the first translatable field needing plural forms). No new BR/AC → no tally change; label i18n is a Module-0-internal reference-data attribute consumed via the existing §8 content contract (no new cross-module seam). | ✅ 2026-07-02 (tech-team Q&A) | Module 0 PRD §3.6 + §8.1 + §13.6 (new **BR-RefData-3**) + §17.7 + Module 0 Acceptance §4.6 (new **AC-0-BR-RefData-3**, verbatim-synced) + §4 header (30 BRs) + §1.1 distribution; bridges **DEC-073 / MVP-DEC-012** + tech-team GitHub question (Case Configuration shape) |
| **MVP-DEC-026** | **Producer-Portal (L-PP) disclosure scope pinned for two cards — ProducerAgreement terms + Club Credit — both to the contract-scoped aggregate (CML-168).** Tech-team question: the wiki documents the underlying data but not what the *producer self-service view* should disclose. Two cards, both governed by the same authority pair — **BMD §3.11 (Producer Reporting Visibility)** + the **DEC-162 info-disclosure discipline** (producer surfaces expose only the contractually-scoped aggregate; internal / other-party detail never crosses). Both ruled **(A)** (Paolo). **Q1 — ProducerAgreement placeholder fields = HIDE (erratum of omission).** The launch-real terms — Producer reference, optional Club narrowing (scope), term window (`term_start`/`term_end`), settlement cadence — are producer-portal-visible (the Producer's own operative contract). The **placeholder fields — minimum-commitment, payment terms, special-clause references (deferred per Q-OQ-9 / DEC-058) — are operator-only and MUST NOT surface on the Producer Portal**: they are unfinalised placeholders carrying internal operator-authored working notes ("*Use the signed agreement wording*", "*net 30 from a valid invoice date*"), and ProducerAgreement drafting is **back-office by definition** (Module K §2: "*Operator action by definition … not a producer surface*"). The read-boundary was never written but is implied by the back-office classification + placeholder nature → made explicit. **Q2 — Club Credit card = aggregate only (confirm; the dev's current card over-discloses).** BMD §3.11 authoritatively scopes the producer KPI to "*club credit outstanding*" — a single aggregate, not a per-state ledger; §7 restates it (per-Profile **issuance count** + **total outstanding per currency**). The current card's per-state breakdown — `redeemed`/`applied` and `forfeited` amounts — is **operator / ERP-only** (redemption behaviour + lost/expired credit are internal ledger states beyond the "outstanding" contract). Per-state detail stays in the Admin Panel / ERP. **Fix scope:** two prose notes — Module K PRD **§4.6** (ProducerAgreement producer-portal read scope) + **§11** (Club Credit producer-portal read scope). No new BR/AC (§3.11 producer-visibility carries as prose + has no AC mirror — house pattern kept; no tally change). No BMD edit (§3.11 already authoritative + correct). **No new DEC** — Q1 = erratum of omission (producer-facing read boundary); Q2 = confirm-to-BMD-§3.11; both bridge **DEC-162** disclosure discipline + BMD §3.11. BMD unchanged. | ✅ 2026-07-03 (tech-team Q&A) | Module K PRD **§4.6** (ProducerAgreement producer-portal read scope) + **§11** (Club Credit producer-portal read scope); cites **BMD §3.11** (already-authoritative) + **DEC-162** + Module K §2 (back-office classification); bridges **DEC-058 / Q-OQ-9 / DEC-162** + tech-team GitHub question (CML-168 Producer Portal disclosure) |
| **MVP-DEC-027** | **Two-layer no-oversell guard: Layer-2's SALE-gate is sourcing-model-scoped — `passive_v1` sells on Layer 1 alone (erratum of omission; surfaced at the Module A engineering briefing).** The guard was written UNIVERSALLY — "both layers must pass at hold placement / voucher issuance" (A §7.1 / B §10.5 / S §8.6 + AC mirrors) — but Module B StockPosition exists only from `InboundEventPhysicallyAccepted` (DEC-195), and for `passive_v1` (BMD §3.7/§5.1: stock stays at the producer until a customer order — per-order inbound) no position exists at sale time → a literal reading (AC-A-XM-14's {L1 pass + L2 fail}→reject corner; AC-B-XM-8's "missing layer returns conservatively"; the S lesser-of displaying 0) **rejects every `passive_v1` sale** — contradicting **DEC-081** ("sales can proceed; physical receipt is a downstream operational dependency for shipment, not for sellability"), **DEC-183** (ACTIVE = sellable uniformly at operator-publish), **Phase C items I/K** ("a voucher-before-physical-receipt window persists; the V1-per-order window survives"), and Modules **C/D/E**, which already implement sale-before-receipt (C §11 shipment gate + in-transit redemption-block; D §12.1 PI auto-fires at voucher issuance; E INV1 at payment-cleared). **Ruling (Paolo, option a): Layer 2 gates the SALE only where the allocation's sellable stock is warehouse-resident** — `passive_v2` always; `direct_purchase` once received (pre-receipt DP sells on Layer 1 alone, DEC-081; idle at launch — roadmap seam note added). **`passive_v1` sells on Layer 1 alone, ALWAYS — scoped by SOURCING MODEL, not pool-existence** (pool-existence scoping would re-block a V1 allocation after its first per-order arrival, whose units are committed to already-issued vouchers). Physical receipt gates shipment (Module C), never the sale. **No protection lost:** warehouse-resident stock keeps the full two-layer gate; arrived `passive_v1` stock keeps committed-inventory protection (Q-CL-6) + no-oversell-at-pick. **Fix = prose + amended-in-place existing BR/ACs, NO new BR/AC (zero tally ripple):** A §7.1 (sale-gate scope block + per-sub-pool + discipline notes) + §11.5.1 (cache never gates `passive_v1` holds) + AC-A-XM-14/15 (scoped four-corner test + a `passive_v1` accept-on-L1 leg); B §8.2 + §10.4 + §10.5 + BR-B-NoOversell-1/2 (§18.3) + §20.2 + AC-B-BR-NoOversell-1/2 + AC-B-XM-8 (V1 = by-design absence, not "missing layer") + AC-B-XM-22; S §0 + §1.1 + §4.4 + §8.6 + AC-S-XM-9/10; Architecture §3.1 step 5 + §5.5 (S↔B) + §8.2 chain 1; Release Index §3.2 chain 1; Post-Launch Roadmap §3 (DP-restore inherits the carve-out). Modules C/D/E/0/K + Admin Panel verified already-consistent (no edit). **No new greenfield DEC** — bridges DEC-081 + DEC-183 (sellability decoupling, frozen) + DEC-185/187/196 (the guard — its universal wording never contemplated the per-order case; v1.1 + v17 carried the same gap, Q-CL-5 was entity-unification) + DEC-195 + Phase C items G/I/K; BMD §3.7/§5.1 unchanged (already correct). | ✅ 2026-07-03 (tech-team Q&A / Module A briefing) | Module A PRD **§7.1** + **§11.5.1**; Module A Acceptance **AC-A-XM-14/15**; Module B PRD **§8.2 + §10.4 + §10.5 + §18.3 (BR-B-NoOversell-1/2) + §20.2**; Module B Acceptance **AC-B-BR-NoOversell-1/2 + AC-B-XM-8 + AC-B-XM-22**; Module S PRD **§0 + §1.1 + §4.4 + §8.6**; Module S Acceptance **AC-S-XM-9/10**; Architecture **§3.1 + §5.5 + §8.2 chain 1**; Release Index **§3.2 chain 1**; Roadmap **§3**; bridges **DEC-081 / DEC-183 / DEC-185 / DEC-187 / DEC-195 / DEC-196** + **Phase C items G/I/K** + **BMD §3.7/§5.1** |
| **MVP-DEC-028** | **Inventory `ownership_flag` value harmonized `CRURATED` → `NEWCO` — the un-swept DEC-185 leftover (naming-only; non-behavioural; surfaced at the Module D engineering-kickoff briefing).** DEC-085 renamed the PurchaseOrder-title `ownership` enum's middle value `CRURATED` → `NEWCO` (the greenfield NewCo rename), but Module B's parallel **inventory `ownership_flag`** (DEC-185, keyed to `SupplierPaymentCompleted`; the R4 E-emits consumer side) was left `CRURATED` — an un-swept leftover that Phase-C note **N3** flagged ("PO-level enum `NEWCO` [DEC-085] vs inventory `ownership_flag` `CRURATED` [DEC-185] — same party") but left divergent. Harmonized the inventory-ownership value to **`NEWCO`** across the **13 build-contract files** (136 true occurrences): Module A/B/D/E/S PRD + Acceptance, Architecture, Post-Launch Roadmap, Build Workplan. **Two-ledger distinction PRESERVED (behaviour unchanged):** the PO-title ledger (Module D, keyed to `VoucherIssued`) and the inventory `ownership_flag` ledger (Module B, keyed to `SupplierPaymentCompleted`) remain two DISTINCT ledgers for the same real-world party — the ~35 N3 "two-ledger clarity" contrast lines were **reworded to distinguish by module + trigger** (not by label, which would read "NEWCO vs NEWCO"); no keying / emitter / consumer change, no FSM change. **DEC-185 citations retained** (the citation stands; only the value it introduced flips — annotated "DEC-185; `NEWCO` per MVP-DEC-028"). The prose gloss "bottles owned by Crurated" → "owned by NewCo" (Module B §2.2). Mixed-case "Crurated" (the external Discovery supplier BMD §13.11/§5.2 / the "Crurated v17" lineage / the company name) left untouched throughout — a case-sensitive rename of the all-caps enum value only. **Deliberate non-edits (Paolo-ruled "build-contract only"):** the **BMD** (9 all-caps `CRURATED` — business-narrative + DEC-index rows) and the **MVP-register N3/R4 rows** (§5 — the frozen-Phase-C mirror) are left as historical record (this row supersedes the N3 label divergence; PRDs win per README). Frozen `_provenance/**` + `decisions.md` (the greenfield DEC-085/185 record) untouched. **Pure value relabel — zero tally ripple** (no BR/AC counts, no §-range headers). Independent multi-agent re-discovery (2 CRURATED sweeps cross-checked + my grep) confirmed the 136/13 set + the ~35 contrast loci; post-edit grep confirms 0 in the 13 files. **No new greenfield DEC** — completes Phase-C note N3; bridges **DEC-085 / DEC-185 / Phase-C R4 / N3**. BMD unchanged. | ✅ 2026-07-06 (tech-team Q&A / Module D briefing) | Module A/B/D/E/S PRD + Acceptance + Architecture + Post-Launch Roadmap + Build Workplan (136 occ → `NEWCO`); supersedes Phase-C **N3** (register §5); bridges **DEC-085 / DEC-185 / Phase-C R4 / N3** + tech-team Module-D engineering-kickoff briefing |
| **MVP-DEC-029** | **Inbound receiving QC: "PO manifest" → "producer's manifest" — the consignment-correct term (naming-only; erratum-of-omission; surfaced at the Module D engineering-kickoff briefing).** Module D's documents-side 3-gate inbound QC (§7 / §7.1, mirrored at AC-D-XM-9) checked the delivery against the "PO manifest" / "PO-manifest match". But at launch (consignment `passive_v1` / `passive_v2`) there is **NO purchase order at arrival** — the PO is the quarter-end **settlement** artefact (issued at voucher time, keyed to `VoucherIssued`; DEC-085 / N3), so "PO manifest" is wrong for the launch path. The **BMD** (`NewCo_BusinessModel_v0.9.md:964`) already uses the authoritative term — "the shipment matches **the producer's manifest**" — so this aligns the PRD to the established business term (the grep-the-BMD discipline). Corrected to the **both-flows** wording ("the producer's manifest — the PO for direct purchase") at Module D **§7 Quantity gate** (`units received match the producer's manifest`), **§7 Matching gate** (the adjacent `identity declared on the PO` line — the identical consignment erratum one line below the manifest gate; **Paolo ruled include it**), **§7.1 documents-side description** (`producer's manifest match`), and the **verbatim-synced acceptance mirror AC-D-XM-9**. Also harmonized a pre-existing PRD/AC spelling drift (PRD "PO-manifest match" vs AC "PO manifest match" → both "producer's manifest match"). **Context-sensitive, NOT a blind replace:** `direct_purchase` (deferred) DOES carry a PO upfront, so the both-flows form keeps that path correct; the unrelated "manifest" uses (the Release-Index launch manifest, the D12 decouple manifest, the Module B FSM "manifest registered") were considered + ruled out. **Deliberate non-edits:** Architecture carries no receiving-manifest reference (no edit); the BMD is already correct (no edit); frozen `_provenance/Module_D_CutSheet` "PO-manifest match" left as historical record. **No new BR/AC → zero tally ripple.** Independent read-only agent sweep confirmed the 3 loci + the adjacent :307, verbatim-sync, and the BMD anchor. **No new greenfield DEC** — an erratum-of-omission aligning the PRD to the BMD's established term; bridges **BMD (the producer's-manifest inbound term) / DEC-081 / DEC-085 / DEC-093**. BMD unchanged. | ✅ 2026-07-06 (tech-team Q&A / Module D briefing) | Module D PRD §7 (Quantity + Matching gates) + §7.1 (documents-side QC); Module D Acceptance **AC-D-XM-9** (verbatim-synced); anchors **BMD §-inbound (`NewCo_BusinessModel_v0.9.md:964` — already-authoritative "producer's manifest")**; bridges **DEC-081 / DEC-085 / DEC-093** + tech-team Module-D engineering-kickoff briefing |
| **MVP-DEC-030** | **Case configuration captured/verified at the inbound check — capture now, verification-as-seam (genuine gap; new Module B receiving BR/AC; surfaced at the Module D engineering-kickoff briefing).** The inbound 3-gate receiving QC (quantity / damage / matching) checks PR-identity + total quantity only; **BR-Identity-3 makes Case Configuration definitionally not part of PR identity, so the "matching" gate structurally excludes packaging** — yet Case Configuration (packaging type + units-per-case) is a load-bearing 5-dimension StockPosition dimension ("cannot be dropped", Module B §8.1) with real whole-original-wooden-case provenance/value stakes. Independent multi-agent investigation (file:line-grounded) confirmed a **genuine, narrow gap**: neither the producer/PO manifest (PR + qty only) nor either receiving gate captures or verifies the received packaging, and no inbound step populates the StockPosition case-config dimension from actual receipt — a producer could ship 60 bottles as 20×3 when 10×OWC6 was expected and **every gate passes silently**. **Ruling (Paolo): capture now + verification-as-seam.** Launch = **capture** — at the Module B physical-match check the operator records the **received Case Configuration** onto the Case / StockPosition case-config dimension (from the Logilize receipt), manual-first, so the ledger reflects *actual* receipt with no data debt. **Seam (P1, deferred post-launch):** the **automated expected-vs-actual verification** — comparing received packaging against a derivable expected (the ordering Offer's Sellable-SKU Case Configuration, or a producer's-manifest-declared packaging) + a **new `case_config_mismatch` `InboundBatchDiscrepancy` variance type** — with the `InboundBatchDiscrepancy` event + the DISCREPANCY state as the retained seam (no `case_config_mismatch` variance emitted at launch). Lands on the **Module B physical-side** (NOT the PR-keyed Module D manifest — loading case config onto the PO/manifest would fight BR-Identity-3; the producer's manifest, MVP-DEC-029, is the natural future carrier of a *declared* expected packaging). **Fix scope:** new **BR-B-Reconcile-5** (Module B PRD §18.8) + verbatim-synced **AC-B-BR-Reconcile-5** (Module B Acceptance §4.8) + a §8.1 Case-Configuration-dimension cross-ref + a Post-Launch Roadmap §2 verification-seam row. **Tally ripple (contained):** Module B Acceptance §4.8 header `(BR-B-Reconcile-1..4 → ..5)` + the distribution line `~176 → ~177 total criteria` (+1 AUTO). Module B PRD §18 uses named BRs (no numeric BR-count header → no PRD ripple); no §1.1-style PRD criteria tally exists. **Cross-module clean** — the Case Configuration entity is Module 0's (referenced, not modified); the `case_config_mismatch` variance is deferred, so Module D's discrepancy consumption is unchanged at launch. Pre-commit read-only sweep confirmed. **No new greenfield DEC** — a genuine-gap launch-scope BR/AC addition; bridges **BR-Identity-3 / MVP-DEC-025 (Case Configuration shape) / DEC-194 (receiving split) / DEC-195 / MVP-DEC-029 (producer's-manifest term)**. BMD unchanged. | ✅ 2026-07-06 (tech-team Q&A / Module D briefing) | Module B PRD §8.1 + §18.8 (new **BR-B-Reconcile-5**); Module B Acceptance §4.8 (new **AC-B-BR-Reconcile-5**, verbatim-synced) + §4.8 header range + the distribution tally (`~176 → ~177`); Post-Launch Roadmap §2 (verification-seam row); bridges **BR-Identity-3 / MVP-DEC-025 / DEC-194 / DEC-195 / MVP-DEC-029** + tech-team Module-D engineering-kickoff briefing |
| **MVP-DEC-031** | **SKU taxonomy prose relabel: "Sellable SKU (Intrinsic)" → "Intrinsic SKU"; the `SellableSKU*` / `CompositeSKU*` event families are RETAINED (naming-only; prose erratum-of-omission + already-decided event contract; tech-team docs question).** The docs used "Sellable SKU" three ways — as the umbrella class ("two Sellable SKU shapes"), as a **synonym for Intrinsic** ("Sellable SKU (Intrinsic)"; "Sellable SKU / Composite SKU" sibling lists), and as the **ratified event-family name** for the Intrinsic shape (`SellableSKU*`). The synonym use reads for a coding agent as "only Intrinsic is sellable / Composite sits outside the sellable-SKU model" → risks wrong schemas / events / APIs / tests. **Premise confirmed:** both shapes ARE sellable (Architecture "Sellable SKU (Intrinsic + Composite)"; Module K/S boundary lists; MVP-DEC-012); §14.2 already calls the family "Intrinsic SKU events" and §17.1 already writes "the Intrinsic `SellableSKU*` family" → the intended concept is **Intrinsic SKU**, the prose merely drifted. **Ruling (Paolo — keep event names + thorough prose scope):** **(1)** the two sellable SKU shapes are **Intrinsic SKU** + **Composite SKU**, both sellable — "sellable" is a property, not a synonym; retire "Sellable SKU" as a prose class label (→ "Intrinsic SKU" where it meant Intrinsic; → "Intrinsic + Composite SKUs" / lowercase "sellable SKU shapes" where it was the umbrella). **(2)** the domain-event families **`SellableSKU*` (Intrinsic) / `CompositeSKU*` (Composite) are NOT renamed** — they are the MVP-DEC-012 cross-module contract (DEC-073 reserves event-names to the spec layer), and keeping them holds the live docs in sync with the frozen `decisions.md` (which permanently records `SellableSKU`); a rename to `IntrinsicSKU*` would reverse MVP-DEC-012's "unchanged", ripple every Module-S consumer + AC EVT/FSM, and desync from the frozen layer — **declined**. **(3)** a **disambiguation note** added at Module 0 §3.7 (the definitional home) + §14.2 (the family-dispatch paragraph) makes the concept↔event-name mapping explicit so no future agent "fixes" the family name; the §18 naming-cascade table gains a relabel row + a **carve-out bullet** instructing every sibling PRD "prose = Intrinsic SKU; event families `SellableSKU*` / `CompositeSKU*` retained". **Fix scope (pure relabel, ~40 prose loci across 8 build-contract files):** Module 0 PRD (§0/§1/§3.6/§3.7 header + note/§3.8/§4.1/§4.4/§4.5/§6/§7.1/§13.5 header + BR-SKU-1/§13.7/§14.1 table label/§14.2 note/§14.3/§17.1/§18 table + carve-out) + Module 0 Acceptance (bucket summary + AC-0-J-4/11/13, AC-0-FSM-4/5, AC-0-BR-Identity-3, AC-0-BR-Lifecycle-3, §4.5 header, AC-0-BR-SKU-1, AC-0-BR-BulkImport-1, AC-0-EVT-5, AC-0-XM-9 — all verbatim-synced with the PRD) + Architecture (entity-model + §3.1 catalog-identity flow step 1 + data-owner table) + Module K PRD (§13.3 + §16 + §18 mapping-table v0.3-MVP cell) + Module K Acceptance (AC-K-XM-19 + AC-K-XM-20) + Module B PRD (§1.3 boundary) + Module S PRD (boundary) + Build Workplan (Module 0 subtasks). **Event tokens preserved everywhere** (`SellableSKU*` at §14.1/§14.5 + Architecture §8.1 event-contract row + AC-0-EVT-4 unchanged). **Zero tally ripple** — no BR/AC added or removed; pure wording (contrast MVP-DEC-025's BR add). **Deliberate non-edits:** frozen `_provenance/Module_0_CutSheet_v0.1.md:36` (tech-team listed it; never-edit set — records the pre-fix state) + `decisions.md:481` ("SellableSKU subtype", frozen); **historical register rows MVP-DEC-012 / MVP-DEC-023 (137/148) left as-is** (their `SellableSKU*` is the code-form event-family name — correct + a historical record). **Validation-loop note:** Mod 0 + Mod K were under dev-team validation when this landed (AC report received + under Paolo's assessment 2026-07-09; not yet signed off) — the relabel is naming-only (all **AC IDs stable**; event tokens `SellableSKU*` / `CompositeSKU*` unchanged; no test-logic / pass-criteria / tally change), so the dev team's per-criterion verdicts + evidence stand line-for-line and the relabelled wording folds into the current adjustment round (no re-test). **No new greenfield DEC** — prose erratum-of-omission with the MVP-DEC-012 event contract upheld; bridges **DEC-073 / MVP-DEC-012**. BMD unchanged. **⚠️ SUPERSEDED (event-name half) by MVP-DEC-033 (2026-07-09):** on tech-team request (zero code cost pre-implementation) the Intrinsic event family `SellableSKU*` was **renamed to `IntrinsicSKU*`** — the prose relabel (Sellable SKU → Intrinsic SKU) in this row **stands**; only the keep-the-event-name half (2) is reversed, and the §3.7/§14.2 notes + §18 carve-out written here are rewritten/removed accordingly. | ✅ 2026-07-09 (tech-team Q&A) | Module 0 PRD §3.7 + §14.2 (disambiguation notes) + §18 (naming-cascade relabel row + carve-out bullet) + ~20 prose loci; Module 0 Acceptance (~13 loci, verbatim-synced); Architecture (entity-model + catalog-flow + owner table); Module K PRD §13.3 + §16 + §18; Module K Acceptance **AC-K-XM-19 / AC-K-XM-20**; Module B PRD §1.3; Module S PRD boundary; Build Workplan (Module 0 subtasks); event families `SellableSKU*` / `CompositeSKU*` **RETAINED** (§14.1/§14.5, Architecture §8.1, AC-0-EVT-4); bridges **DEC-073 / MVP-DEC-012** + tech-team GitHub question (SKU taxonomy wording) |
| **MVP-DEC-032** | **Module D inbound entities (CML-114) — six clarifications ratified in one batch (tech-team Q&A; 2 errata + 4 genuine-opens).** **(1) Receiving 3-gate QC = Quantity / Damage / Matching — confirmed; the "ProducerAgreement validity" wording was a v0.2 drift from DEC-194's "provenance"** (frozen DEC-194: "paperwork + provenance + physical-condition-on-arrival"; BMD §5.1 the same trio; v17 §B.9 + qa.modD Q-AD-4 agreement-free) — deleted at all 5 loci (Module D §7.1; AC-D-J-9; AC-D-XM-9; Module B §11.1 — whose "PO-line conformance" also aligned to the MVP-DEC-029 producer's-manifest term; Admin Panel §3.D inbound row). Agreement gating stays at PO issuance (DEC-094 Level 1 + the §11.1 override); no agreement check at the dock (a V1 arrival is often already customer-sold wine). **(2) DiscrepancyResolution cost-impact payload shape ratified (the AC-D-J-10 MIXED "Paolo confirms" checkpoint, exercised early):** optional signed `quantity_delta` (units) + optional signed `value_delta` (amount + explicit currency — the affected line's settlement currency; Module E owns FX, DEC-169) + required free-text note, **uniform across the six paths**; sign convention positive = increases NewCo's cost / payable; qty-adjusting resolutions (Accept Shortage; a qty-flavoured Adjustment) carry `quantity_delta` (feeds §13.3); zero-impact resolutions admitted; representation = DEC-073. **(3) Phase-2 landed-cost base scoped by sourcing model (erratum — the "PO unit price × accepted qty" formula was v17 direct-purchase-shaped; no PO exists at arrival for the two exercised launch flows):** base = **the accepted units' supplier-share value per `Allocation.commercial_terms`** (DEC-092) — the PO line value for direct purchase; the voucher-resolved PI values for V1; the commercial-terms evaluation against the allocation's set selling price for V2 pre-positioning (BMD §5.1: cost finalization "typically immediate for passive consignment since the producer's price is set at allocation creation" — the dev's "percent has no unit value until sell-through" premise dissolves; percent-shape *settlement* truth stays sell-through-keyed, drift reconciles at Module E, DEC-072). Base **provisional from Phase 1** (the payload field Module B §3.2 already reads at InboundBatch creation, DEC-195 — now added to the D-side payload enumeration §7/§16.1/AC-D-EVT-6/7) and confirmed at Phase 2; **SLA uniform**; pre-fill/override = DEC-073. **BR-D-Identity-3 inbound anchor scoped by sourcing model** (was "every InboundEvent references exactly one PO" — impossible for V2): direct purchase = PO at creation; V1 = the settlement-cadence PO binds when issued (arrivals may precede it); V2 = the allocation via the paired ConsignmentReceipt, no PO at arrival (aligns Module D to DEC-195/BMD "PO or consignment receipt"). **(4) ConsignmentReceipt producer-notification shape** = notified-at timestamp + optional free-text note at launch (operator-recorded — zero producer writes, §3.6; the manifest substance is the CR's own qty + PR-identity fields; the BMD §9.8 EXPECTED-state analog). **(5) CR transitions operator-recorded at launch** (manual-first) **with the invariant AVAILABLE requires the paired InboundEvent `PHYSICALLY_ACCEPTED`** (AVAILABLE is *defined* as post-acceptance — an ungated operator flip could contradict the definition) **+ an explicit late-bound paired-InboundEvent reference** (null at PENDING_RECEIPT — notification precedes arrival; set from RECEIVED; required for AVAILABLE; makes Module B's V2 compose-into-one-InboundBatch pairing deterministic); operator-button-under-validation vs auto-derivation = DEC-073. **(6) 5-WD SLA working days = Mon–Fri, no holiday calendar at launch** (the SLA is a soft alert, BR-D-Inbound-2; the value is already configurable; holiday-awareness = post-launch config if ever needed; DEC-169's holiday-aware FX "business day" is FX-specific, not a platform calendar). **No new BR/AC → zero tally ripple; no new greenfield DEC; BMD unchanged (it is the authority on (1) and (3)).** Bridges **DEC-194 / DEC-094 / DEC-092 / DEC-195 / DEC-090 / DEC-086 / DEC-183 / DEC-169 / MVP-DEC-029 / MVP-DEC-030 / BMD §5.1 + §3.10 + §9.8**. | ✅ 2026-07-09 (tech-team Q&A, CML-114) | Module D PRD §7 (base derivation + event payloads + SLA calendar) + §7.1 (QC trio) + §8 (CR notification / pairing / transitions) + §13.2 (payload shape) + §15.1 **BR-D-Identity-3** + §16.1 (event payloads); Module D Acceptance **AC-D-J-9 / AC-D-J-10 / AC-D-XM-9 / AC-D-BR-Identity-3 / AC-D-FSM-11 / AC-D-EVT-6 / AC-D-EVT-7** (verbatim-synced); Module B PRD §11.1; Admin Panel PRD §3.D inbound row |
| **MVP-DEC-033** | **Intrinsic SKU event family RENAMED `SellableSKU*` → `IntrinsicSKU*` (supersedes the event-name half of MVP-DEC-031; tech-team request).** Hours after MVP-DEC-031 shipped "keep `SellableSKU*` + disambiguation note", the tech team (the event-contract consumer) asked to rename the Intrinsic family to `IntrinsicSKU*` **now** — "basically zero-cost in the codebase (nothing is built yet), clear disambiguation, start clean." **Paolo accepted** (a decision reversal on new information). **Re-evaluation of MVP-DEC-031's three keep-arguments — all weaken:** (a) *codebase ripple* — consumers confirm ~zero cost pre-implementation, and this is the cheapest moment it will ever be (every build-day makes it dearer, so "not now" ≈ "keep the wart forever"); (b) *"reverses MVP-DEC-012"* — only the family **name** moves; MVP-DEC-012's substance (two **distinct** families, dispatch-by-family not a payload discriminator) is fully preserved; (c) *"desync from the frozen `decisions.md`"* — **retracted**: the frozen layer is by design a historical/superseded record (live already diverges across the whole Wine→Product cascade + the CRURATED→NEWCO relabel MVP-DEC-028), so "sync with frozen" was never a real goal. **End state is strictly cleaner** — `IntrinsicSKU*` / `CompositeSKU*` / (future) `ServiceSKU*` all match their concepts, and the "why does Intrinsic emit `SellableSKU`?" residual vanishes (the MVP-DEC-031 §3.7/§14.2 disambiguation notes are rewritten to a plain two-family statement; the §18 carve-out bullet is removed — the families are no longer a do-not-rename item). **The prose relabel from MVP-DEC-031 (Sellable SKU → Intrinsic SKU) STANDS; this row reverses only the keep-the-event-name half.** **Work split (Paolo):** the **spec** rename runs through `/devqa` direct-to-main (this row) — preserving the DEC-074 PRD↔acceptance verbatim-sync + the §14.1/§18 rename records; the **tech team** does the **code + test** rename in lockstep. Their offer to open a **docs-repo PR** was **declined** in favour of the settled direct-to-main workflow (the 2026-06-19 PR experiment stays reverted). **Fix scope (event tokens, ~11 loci; `SellableSKU*`→`IntrinsicSKU*`):** Module 0 PRD §3.7 note (rewritten) + §3.8 impl-note + §14.1 table row ("unchanged"→"renamed from `SellableSKU*`") + §14.2 note (rewritten) + §14.5 Module-S consumer + §17.1 Service-SKU seam + §18 relabel-row (now records the event rename) + §18 carve-out bullet **removed**; Architecture §8.1 event-contract row (+ "renamed from `SellableSKU*`" note); Module 0 Acceptance **AC-0-FSM-4 / AC-0-EVT-4 / AC-0-EVT-14 / AC-0-GEN-6** (GEN-6 moves the Intrinsic family from its "unchanged" list to a "renamed" clause + a no-`SellableSKU*`-remaining assertion). **Composite stays `CompositeSKU*`** (already matches its concept). **Zero BR/AC added/removed → zero tally ripple.** **Validation-loop impact (Mod 0 mid-validation):** unlike MVP-DEC-031 (tokens kept → tests untouched), this **does change the event-name string asserted by AC-0-EVT-4 / AC-0-FSM-4 / AC-0-EVT-14** — but the **AC IDs are stable** and the tech team renames **code + tests** in lockstep, so the dev's verdicts move coherently (`SellableSKUActivated`→`IntrinsicSKUActivated` in both criterion and test); folds into the same adjustment round. **Deliberate non-edits:** frozen `decisions.md:481` + `_provenance/Module_0_CutSheet_v0.1.md:36` (historical); **historical register rows MVP-DEC-012 / 023 / 031 event-name tokens left as-is** (they record what was decided *then*; 031 additionally carries an inline "⚠️ superseded (event-name half) by MVP-DEC-033" flag). **No new greenfield DEC** — supersedes the event-name half of MVP-DEC-031; MVP-DEC-012's two-distinct-families substance upheld. Bridges **DEC-073 / MVP-DEC-012 / MVP-DEC-031**. BMD unchanged. | ✅ 2026-07-09 (tech-team Q&A) | Module 0 PRD §3.7 + §3.8 + §14.1 + §14.2 + §14.5 + §17.1 + §18; Module 0 Acceptance **AC-0-FSM-4 / AC-0-EVT-4 / AC-0-EVT-14 / AC-0-GEN-6**; Architecture §8.1 event-contract row; annotates + supersedes the event-name half of **MVP-DEC-031**; Composite `CompositeSKU*` unchanged; bridges **DEC-073 / MVP-DEC-012 / MVP-DEC-031** + tech-team request (event rename) |
| **MVP-DEC-034** | **Module K validation-round rulings (five-part batch — Paolo's dispositions on the dev acceptance report; report snapshot `cmless@31fcb55a` 2026-07-07, validation pinned `8bc9bddf`, docs `360df0b`).** **(a) Hero-capacity interim authority (CML-79).** The build stores capacity as an independent `clubs` attribute (`TODO(CML-79)`) — letter-fails AC-K-XM-18 (and AC-K-XM-20's capacity clause) and is exactly the K-owned-number design MVP-DEC-020 declined; but Module A does not yet exist to bind to. Ruling: accepted as **INTERIM** authority pre-A; MUST convert to the reconciling read-model (or live read) of A's `qty` at Module-A integration, where XM-18 **re-arms** and is re-verified; until then XM-18/19 + the XM-20 capacity clause reconcile as **Defer, not Fail**. MVP-DEC-020 substance preserved (A = source of truth; timing accommodation, not an ownership flip). **(b) Account-scope Holds = FIX-NOW.** The service layer rejects Account-scope placement (`hold-service.ts:23-26` at the pin) → AC-K-FSM-9's "Account-level Holds drive suspension" is unsatisfiable and AC-K-FSM-10 runs 2-of-3 scopes; K-internal, no dependency excuse. Paolo **declined** the 1-Customer-=-1-Account redundancy deferral → **spec unchanged (FSM-9 / §4.8 stand as written); dev work item** alongside the CML-45 consumer cluster. **(c) AC-K-J-7a enhanced-KYC signed deferral.** The scanner is a stub returning `[]` (CML-77) — a retained FLOOR control rendered inert; silent deferral refused. Ruling: scanner delivery **time-boxed to before Module S go-live** (pre-S no orders exist → no threshold can trip) + compensating manual control (periodic Compliance review of trailing-12-month gross-EUR per Customer, MVP-DEC-014 aggregation semantics); criterion retained unchanged, **re-arms at S integration**; FLOOR intact — the control is not cut, only the automation's delivery moves. **(d) AC-K-XM-24 relaxed to absent-or-dormant.** The build omits AgencyAgreement entirely; the AC letter demanded present-in-schema. Absence over-satisfies dormancy; the operative future-flexibility seam is the Third-Party-Owner Party subtype (MVP-DEC-021 Q1) and forcing a dead table would contradict DEC-073 (schema = dev's). PRD boundary loci (§1 scope list + §16) softened in lockstep. **(e) Profile `role` visibility note (MVP-DEC-022(f) completion).** 022(f) ruled confirm-only/no-AC; the validation found the ruling invisible in the acceptance doc → added a §4.3 confirm-note (bounded single-valued enum, zero permission effect; closes via a dev-confirmation answer; NO new AC, zero tally ripple). **Also reconciled:** the doc's Status line still read "DRAFT — awaiting batch ratification" though this register's §7 log records the batch ratification on **2026-06-08** — the stamp never landed on the doc; header + §8.1 updated to match the register (per-AC §8.2 sign-off remains OPEN until module close). **Deliberate exclusion:** the transactional-email delivery ruling (BR-K-Contract-3 / AC-K-XM-17 / PRD §7.5 "HubSpot owns outbound delivery; K never sends") is **spun out to a dedicated cross-module pass** — blast radius spans K/E/S/Admin-Panel/Architecture/Workplan/BMD (103 `hubspot` + 20 transactional-email loci across 16 files) and the approved direction (ERP-owned operational sends via a single email service + governed template catalog; HubSpot owns marketing/lifecycle) reverses a categorical PRD invariant → own MVP-DEC (**landed same-day — MVP-DEC-035**). **Validation context:** parts (a)/(c)/(d) convert report ❌/⚠️ rows to clean Defers/Accepts; (b) + the CML-45 cluster + dual-control offboarding (MVP-DEC-024 build) + the K.18 seam (AC-K-MVP-3) + the EVT-20 order assertion remain the launch-blocking build set. Zero BR/AC added or removed. | ✅ 2026-07-09 (Module K validation round) | Module K Acceptance: Status header + §0 item 6 note + new §0 item 7 + AC-K-J-7a + §4.3 role note + AC-K-XM-18/19/20/24 + §8.1 milestone row; Module K PRD: §1 boundary bullet + §9.1 launch-delivery note + §13.2 interim note + §16 boundary bullet; account-scope Holds + CML-45 = dev work items (no spec edit); bridges **MVP-DEC-014 / 020 / 021 / 022 / 024** + CML-45 / CML-77 / CML-79; transactional-email ruling → **MVP-DEC-035** |
| **MVP-DEC-035** | **Transactional-email architecture — purpose split: the ERP owns operational/transactional email through ONE internal email service; HubSpot owns marketing/lifecycle only (reverses the v1.1-inherited "HubSpot owns ALL outbound delivery; Module K never sends a Customer communication directly").** Spun out of the Module K validation round per MVP-DEC-034's deliberate-exclusion clause; Paolo approved 2026-07-09. **Why the old rule fell:** (i) **unimplementable at launch** — the HubSpot integration is not built, while registration verification (K §7.1) + the email-change dual-token mail (BR-K-Identity-1 / MVP-DEC-021) are hard launch gates; (ii) **wrong on consent** — operational mail is consent-independent (DEC-026 single-opt-in; AC-K-FSM-15 "transactional emails independent of this state"), so routing it through the consent-gated marketing platform inverts the legal basis; (iii) wrong on latency for token-bearing flow mail (the email IS the flow's delivery vehicle); (iv) the docs were already internally inconsistent (AC-K-J-6 "Module K sends confirmation email" + §8.1 vs §7.5/BR-K-Contract-3 "K never sends"); (v) the dev build already sends the email-change mail directly — the ⚠️ at BR-K-Contract-3 / AC-K-XM-17 in the dev acceptance report (snapshot `31fcb55a`, pin `8bc9bddf`) — those two rows now reconcile as **Accept-with-refactor** (consolidate the existing direct sends behind the service; refactor, not rip-out). **The ruling (7 parts):** (1) **purpose split** — ERP = operational/transactional; HubSpot = marketing/lifecycle (campaigns, newsletters, nurture, win-back, welcome-journey content), driven by the existing event sync, marketing-consent-gated; (2) **ERP-sent classes** — security/identity (registration verification, email-change dual-token verify+notice, password reset), token-bearing flow mail (club-link + producer invitations, waitlist/approval confirmations, the §8.1 double-opt-in confirmation), money mail (receipts, failed-charge notices — Module E side, joins when E lands; Module S order/shipment mail joins when S lands); (3) **ONE email service** — a single internal adapter every module calls; no module integrates the mail provider directly; one send log/audit, one suppression + locale policy; (4) **governed template layer outside code** — one base layout on the CRCLES design-system tokens; templates owned by design/marketing; engineering injects variables only; (5) **registered email catalog** — every ERP-sent email = ID + trigger event + audience + template + locales; adding an email = product decision; **spec home = Module K PRD §14.9.1** (new subsection under the §14.9 cross-module-contract group, so existing sibling cites of "Module K §14.9" keep resolving) + one Admin-Panel §3.K operator row (catalog/template management — a bounded list, not a CMS); (6) **separate sending subdomains** transactional vs marketing; (7) **marketing consent gates ONLY the HubSpot class** (aligns §8.1 + AC-K-FSM-15). **Provider behind the adapter = dev team's call (DEC-073** — dedicated ESP vs HubSpot transactional API; BMD §11.5 already contemplated either → **no BMD revision**). **Fix scope (~40 loci / 11 build-contract files + this register):** Module K PRD §2 HubSpot actor + §7.3 step 2 + §7.5 bullet (the reversed rule) + §8.1 ×2 + **BR-K-Contract-3 rewritten + NEW §14.9.1** + §15.6 ×2 + §16 boundary bullet; Module K Acceptance header + §0 item 7 note + AC-K-J-3 + AC-K-J-6 + **AC-K-BR-Contract-3 (verbatim-sync)** + AC-K-EVT-11 + AC-K-XM-17 + §8.1 log; Module E PRD §3.3 Stage-1 + §5.1 step 3 + §8.3 HubSpot bullet + §10 item 19; Module E Acceptance AC-E-J-8 + AC-E-XM-25 + AC-E-XM-27..41; Module S PRD §13 gift-seam line + §17.8 (retitled: the ERP email service + HubSpot) + BR-S-CrossModule-5; Module S Acceptance AC-S-BR-CrossModule-5 + §6.8/AC-S-XM-24; Module A PRD §13 boundary bullet; Architecture §4 HubSpot row + **NEW transactional-email-provider row** + §7.2 notification-delivery boundary line; Admin-Panel §3.K **NEW email-catalog row**; Build Workplan Phase-2 integrations + vendor-onboarding lines; Roadmap EXT-4 row (premise updated). **Zero BR/AC added or removed → zero tally ripple** (BR-K-Contract-3 + mirrors rewritten in place). **Deliberate non-edits:** the BMD (all 10 hubspot loci — business-level stack statements; §11.5's "may be routed through HubSpot or a dedicated transactional provider" is the provider-choice TBD this DEC resolves at PRD layer); frozen `decisions.md` + `_provenance/` cut-sheets (historical); every HubSpot-as-CRM/event-consumer locus (K §15.1/§15.6 sync + segment consumers, AC-K-EVT-3/4/10/13, BR-K-Contract-1 / BR-K-Profile-4 + mirrors, S §16.12 consumer list + acceptance consumer notes, Architecture §1.2 execution-arms line, Admin-Panel GDPR/consent rows, Workplan "Parties ↔ email/HubSpot" test line) — HubSpot's CRM-sync + marketing/lifecycle role is UNCHANGED. **Dev work items:** consolidate the existing direct sends behind the single adapter (refactor); stand up the template layer + registered catalog; configure the transactional/marketing sending subdomains; select the provider behind the adapter (DEC-073). Bridges **DEC-014 / DEC-026 / DEC-073 / MVP-DEC-021 / MVP-DEC-034**. | ✅ 2026-07-09 (Module K validation-round spin-off) | Module K PRD **§14.9.1** (the email-service seam — the authoritative home) + BR-K-Contract-3; Module K Acceptance AC-K-BR-Contract-3 / XM-17 / J-3 / J-6 / EVT-11 / §0 item 7 / §8.1; Module E + Module S PRD/Acceptance + Module A PRD + Architecture §4 + §7.2 + Admin-Panel §3.K + Workplan + Roadmap EXT-4 (short cross-refs to §14.9.1 — no restated copies); dev-report reconciliation: BR-K-Contract-3 + AC-K-XM-17 ⚠️ → **Accept-with-refactor**; bridges **DEC-014 / 026 / 073 / MVP-DEC-021 / 034** |
| **MVP-DEC-036** | **PO-level title transition — launch-flow evaluation moment pinned: the PRODUCER → NEWCO flip applies at PO issuance for V1/V2 (CML-115; erratum of omission + two confirms).** Tech-team question (implementing the `VoucherIssued` consumer): Module D §6 keys the PO title transition to `VoucherIssued` (item F), but in both launch flows every covering voucher PRECEDES its settlement-cadence PO (voucher → PI → cadence PO, §12.1/§12.2), so a literal arrival-driven flip would never fire — while AC-D-J-1/J-2 issue the PO `ownership = PRODUCER`. **Q1 (evaluation moment — ruled option a):** Module D's PO `ownership` reflects the current title-bearing state of the PO's covered units, evaluated **at PO issuance and on `VoucherIssued` events covering an open PO's units** — in the launch cadence flow the PO is issued `PRODUCER` (the derivation rule; `PurchaseOrderIssued` carries the at-issuance value — AC-D-EVT-3 unchanged) and the title transition applies **immediately upon issuance, in the same operation, as its own audited transition** citing the already-issued covering vouchers; the event-driven flip stays wired for vouchers covering a title-bearing open PO's units (**scoped to the PO's bound PIs, NOT any voucher on the allocation** — a later voucher belongs to the next cadence PO), unexercised at launch (the covered PI set is fixed at issuance). Matches frozen **DEC-085's own "transitions … at sell-through-settlement" wording** (at launch the settlement moment IS the cadence PO) + §6's current-title-state invariant; the rejected literal-arrival-only reading would leave every launch V1/V2 PO `PRODUCER` for life (a permanently false attribute) and dead-code AC-D-J-2's transition assertion. The flip is an audited attribute transition, **no new domain event** (§16.1 unchanged; audit-record representation = DEC-073). **Q2 (confirm): the flip applies to BOTH V1 and V2** — §6 + DEC-085 bind `passive_v1 \| passive_v2` jointly; AC-D-J-1's silence was journey-scoping (the MVP-DEC-024 under-mirrored-AC pattern) → the transition assertion added to AC-D-J-1. **Q3 (confirm): atomic per PO** — `ownership` is a PO-level attribute; atomicity is exact at launch (a cadence PO covers only the period's SOLD PIs — no unsold unit is ever PO-covered), mirroring the atomic-per-PO settlement (§16.4 / OQ-20); per-line / per-voucher partial title deferred (D19 territory). **No accounting position (DEC-072)** — this pins the product title-ledger behaviour only; the GL keying stays folded into the deferred settlement recording (D19). **N3's two-ledger separation untouched** (Module B `ownership_flag` stays keyed to `SupplierPaymentCompleted`; sibling N3 mirrors in Architecture / S / B / E state the signal only — inherit, no edit). **No new BR/AC → zero tally ripple; no new greenfield DEC; BMD unchanged.** Bridges **DEC-085 / DEC-072 / DEC-073 / DEC-086 / Phase C item F / N3 / MVP-DEC-028**. | ✅ 2026-07-10 (tech-team Q&A, CML-115) | Module D PRD **§6** (ownership-derivation rule — the evaluation-moment clause) + **§14.4** (`VoucherIssued` launch-timing note); Module D Acceptance **AC-D-J-1** (title-transition assertion added) + **AC-D-J-2** (evaluation moment pinned) + **AC-D-FSM-5** (launch-timing aligned; the post-issuance event flip marked wired-but-unexercised — all aligned with §6); bridges **DEC-085 / DEC-072 / DEC-073 / DEC-086 / item F / N3 / MVP-DEC-028** + tech-team GitHub question (CML-115 `VoucherIssued` consumer) |
| **MVP-DEC-037** | **Allocation committed-vs-received — the per-allocation receipt-position read + the two reporting definitions (internal audit Option B; design decision).** An independent 2026-07-10 spec audit (from a Module D briefing data-modeling concern) established: the Allocation stores **committed only** (`qty` + sub-pools — DEC-079/080; the DEC-187 ATP cache is an operational guard input, not a receipt record); **no flow updates Module A on an inbound divergence** (divergences settle in Module D's DISCREPANCY + 6-path enum, Module B's InboundBatch/StockPosition, Module E's cost events; Module A is an enumerated audit-only consumer; the D→A event crossing is empty; realignment = the operator qty decrease, A §5.3.4, anti-orphan-bounded); the **received figure exists first-class per allocation in Module B** (InboundBatch `source allocation` + `received_quantity`, DEC-194/195 — the deterministic Σ the NS-ATP formula already computes; **the PO is NOT the attribution route** — PO lines carry no per-allocation split; BR-D-Identity-3 anchors instead); but **no allocation-level received/available READ, no committed-vs-received comparison, and no sell-through denominator is defined anywhere in the baseline** — the only 7-section enumeration is pre-baseline v1.1 `Producer_Portal_Reporting_v0.1` §4 (`qty_remaining = qty − sold − held`, committed-denominated; v1.1 has the identical shape — inherited, nothing MVP-cut). **Ruling (Option B — transparency, zero A-side state):** **(1)** Module B **§8.3 NEW** exposes **`received_to_date` per allocation** = Σ InboundBatch `received_quantity` (cumulative intake — never decremented by shipment/adjustment/quarantine/destruction; reflects DiscrepancyResolution qty adjustments; new **BR-B-Ledger-6** + **AC-B-FSM-22**); **(2)** the **canonical "received" = Module B's physical-match figure** (DEC-194) — never Module D's documents-side accepted qty; **(3)** **sell-through per allocation is measured against `received_to_date` with the committed `qty` always shown alongside** ("sold X of Y arrived, committed Z"; `passive_v1` reads as commitment-denominated with arrivals as fulfilment progress — composes with MVP-DEC-027); **(4)** the **Admin-Panel allocation-position view + the Producer Portal show the same four-figure strip** — committed / `received_to_date` / issued / available-to-sell (the S §8.6 lesser-of read) — plus a **soft under-receipt flag** (non-blocking; the A §5.2 soft-alert pattern) at allocation close/retire and on an Accept-Shortage resolution; **(5) Module A UNCHANGED** — the Allocation stays commitment-only; **no new event, no new entity** (materialise-vs-compute is tech, DEC-073). Alternatives: A (definitions-only) subsumed; **C (mirror received into Module A) REJECTED** (duplicate truth/drift; would open a load-bearing D/B→A contract where the routing is deliberately empty); **D (prompted commitment true-up) DEFERRED-not-designed** (revisit post-launch with §8.3's data; the manual lever exists). Riding along, **two wording alignments**: ConsignmentReceipt §8 Shape now lists the source-allocation reference (already normative — BR-D-Identity-3 + the `ConsignmentReceiptRecorded` payload + AC-D-BR-Identity-3); InboundBatch §3.1 expected-quantity immutability qualified (mutable solely via the DiscrepancyResolution qty adjustment, §3.4 / D §13.3 — a v1.1-inherited internal tension). **No new greenfield DEC; BMD unchanged** (BMD §3.8's commitment-vs-released view is the distinct agreement-level comparison, untouched). Bridges **DEC-073/077/079/080/083/187/194/195/196 · MVP-DEC-027/030/032 · D23**. | ✅ 2026-07-10 (internal audit; Option B ratified in-session) | Module B PRD **§1.1 + §3.1 + §8.3 (NEW) + §18.2 BR-B-Ledger-6 (NEW)**; Module B Acceptance **AC-B-FSM-22 (NEW) + AC-B-BR-Ledger-6 row (covered-by AC-B-FSM-22) + AC-B-FSM-10 (expected-qty carve-out) + §1.1 tally ~178 + §4.2 range label 1..6**; Module D PRD **§8** (ConsignmentReceipt Shape wording); Admin Panel PRD **§0 item 3 + §2.2** (read list + the two definitions) **+ §3.A** (allocation-position read row); Admin Panel Acceptance **AC-AP-PWB-4 + AC-AP-INV-A**; Module K PRD **§3.3** (D23 read-list); Architecture **§1.2** (inventory-authority list) + the producer-write-boundary D23 read list; bridges DEC-079/187/194/195 · MVP-DEC-027/032 · D23 |

---

## §7 Phase D artefact ratification log (accretes)

| Artefact | Status | Date |
|---|---|---|
| **Module 0 PRD v0.3-MVP** (`02-prd/`) | ✅ RATIFIED | 2026-06-07 |
| **Module 0 Acceptance v0.3-MVP** (`03-acceptance/`) | ✅ RATIFIED | 2026-06-07 |
| **Module K PRD v0.3-MVP** (`02-prd/`) | ✅ RATIFIED | 2026-06-08 |
| **Module K Acceptance v0.3-MVP** (`03-acceptance/`) | ✅ RATIFIED | 2026-06-08 |
| **Module A PRD v0.3-MVP** (`02-prd/`) | ✅ RATIFIED | 2026-06-08 |
| **Module A Acceptance v0.3-MVP** (`03-acceptance/`) | ✅ RATIFIED | 2026-06-08 |
| **Module D PRD v0.3-MVP** (`02-prd/`) | ✅ RATIFIED | 2026-06-08 |
| **Module D Acceptance v0.3-MVP** (`03-acceptance/`) | ✅ RATIFIED | 2026-06-08 |
| **Module S PRD v0.3-MVP** (`02-prd/`) | ✅ RATIFIED | 2026-06-08 |
| **Module S Acceptance v0.3-MVP** (`03-acceptance/`) | ✅ RATIFIED | 2026-06-08 |
| **Module B PRD v0.3-MVP** (`02-prd/`) | ✅ RATIFIED | 2026-06-08 |
| **Module B Acceptance v0.3-MVP** (`03-acceptance/`) | ✅ RATIFIED | 2026-06-08 |
| **Module C PRD v0.3-MVP** (`02-prd/`) | ✅ RATIFIED | 2026-06-08 |
| **Module C Acceptance v0.3-MVP** (`03-acceptance/`) | ✅ RATIFIED | 2026-06-08 |
| **Module E PRD v0.3-MVP** (`02-prd/`) | ✅ RATIFIED | 2026-06-08 |
| **Module E Acceptance v0.3-MVP** (`03-acceptance/`) | ✅ RATIFIED | 2026-06-08 |
| **Admin-Panel PRD v0.3-MVP** (`02-prd/`) | ✅ RATIFIED | 2026-06-08 |
| **Admin-Panel Acceptance v0.3-MVP** (`03-acceptance/`) | ✅ RATIFIED | 2026-06-08 |
| **Architecture v0.3-MVP** (`02-prd/`) | ✅ RATIFIED | 2026-06-08 |
| **Post-Launch Roadmap v0.1** (`04-roadmap/`) | ✅ RATIFIED | 2026-06-08 |
| **MVP Release Index v0.1** (`05-release/`) — #13 | ✅ RATIFIED | 2026-06-08 |
| **Build Workplan v0.3-MVP** (`05-release/`) — #13 | ✅ RATIFIED | 2026-06-08 |

> **The full launch-MVP spec is ratified and coherent.** All 9 PRDs + 9 acceptance docs + the Architecture (#10) + the roadmap (#12) + the two #13 release artefacts (the Release Index + the Build Workplan) are drafted, ratified, and coherent; the §7 rows above are the authoritative ratification log (it wins on any interim drift).
>
> **Maintenance:** when an artefact ratifies, add/flip its row here (and add any net-new MVP-DEC the session produced). Keep rows to one line + a pointer — this stays an index.

---

## §8 Cross-references

- **Method / dials** — `00-method/MVP_Restructure_Plan_v0.1.md`, `Dials_Worksheet_v0.1.md`, `Dials_Grounding_v1.1_reference.md`, `Phase_D_Kickoff_Prompt.md`.
- **Triage** — `01-triage/Module_X_CutSheet_v0.1.md` (×8), `Phase_C_Reconciliation_v0.1.md`.
- **PRDs / acceptance** — `02-prd/`, `03-acceptance/`.
- **Roadmap (the deferred set + seams)** — `04-roadmap/Post_Launch_Roadmap_v0.1.md`.
- **Greenfield frozen register (bridged, never extended)** — `greenfield/04-decisions/decisions.md` (DEC-001..196).

---

*End of Launch-MVP Decisions Register v0.1 — a **thin index** (one row per decision → its authoritative doc), established 2026-06-07. Reuses the existing handles (GOV / P / D-dials / module-Q / Phase-C A–N·R1–R4·N1–N3) + a small MVP-DEC stream; bridges but never extends greenfield's frozen DEC-001..196. Living through Phase D; frozen into the Phase E handoff.*
