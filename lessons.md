# Lessons

> Mistake → Correction → Rule triples. Added by the agent after every correction from the user and after every self-discovered mistake pattern. **Read at session start** (and at ralph iteration start via progress.md pointers). Keep entries short and prescriptive; when a lesson reveals a deeper domain pattern, promote the insight to `knowledge/` and keep the lesson as quick-reference.

Format:

```
## YYYY-MM-DD — short title
- **Mistake:** what went wrong
- **Correction:** what the user/reality corrected it to
- **Rule:** the prescriptive rule that prevents recurrence
```

---

## 2026-06-11 — Protected files need explicit per-file authorization
- **Mistake:** Treated a generic "procedi" as sufficient authorization to edit protected files (.claude/settings.json, .claude/hooks/, ralph.sh).
- **Correction:** The permission classifier blocked the edits; explicit per-file confirmation (AskUserQuestion) was required before retrying.
- **Rule:** Before touching anything in the CLAUDE.md "Protected Files" list, name the exact files and obtain an explicit per-file OK in that same exchange.

## 2026-06-11 — git-guardrails hook matches bare "rm"/"mv" letter sequences inside Bash prose
- **Mistake:** Appended a progress entry to `openspec/changes/.../progress.md` (a legitimate, writable target) via a Bash heredoc whose prose contained "platform spec"; the PreToolUse hook regex `(rm|mv)[^|;&]*[[:space:]](spec|openspec/specs)…` is unanchored, matched the `rm` inside "platfo**rm**" + " spec", and blocked the whole command as "deleting the immutable spec layers".
- **Correction:** Wrote the identical content with the Edit/Write file tools — the hook matches Bash commands only, and file-tool writes to non-protected paths are the intended channel.
- **Rule:** Write memory/progress files (progress.md, log.md, hot.md, lessons.md) with the Edit/Write tools, never Bash heredocs/redirects — any prose word ending in -rm/-mv followed by "spec…" (platform spec, confirm spec, …) trips the hook. (Suggested human-side fix, hook is protected: anchor the verb — `(^|[;&|[:space:]])(rm|mv)[[:space:]]`.)

## 2026-06-12 — pest-plugin-arch: `not` over an ARRAY *source* inverts the aggregate (silently masks a violation)
- **Mistake:** Wrote the platform-direction arch test (task 2.3) as `expect(['App\Providers','App\Models','App\Http'])->not->toUse('App\Modules')`, assuming an array source means "NONE of these may use the target". The suite stayed GREEN even with a deliberate violating fixture (`app/Providers/` class importing a module) present — a vacuously-passing test.
- **Correction:** Empirically probed the API (per design D4): `expect([sources])->not->toUse(X)` negates the *aggregate* positive `toUse` ("ALL sources use X"), so it passes the moment ONE source is clean — `App\Models` not using `App\Modules` satisfied the `not`, masking the dirty `App\Providers`. The fix is to LOOP and assert one single-source `expect($ns)->not->toUse(X)` per source (mirrors the 2.2 per-module loop): reads "this source uses none of X", fails on any one dirty source.
- **Rule:** In pest-plugin-arch, put arrays on the **dependency** side only (`not->toUse([d1,d2,…])` = "uses ANY of these" — safe). NEVER put an array on the **source** side under `not`; loop instead, one `expect($source)->not->toUse(…)` per source. The mandatory red-proof is what caught this — a boundary test never seen red does not count as done (design Risk "vacuously green arch tests").

## 2026-06-12 — Launch `./ralph.sh` only AFTER committing the change scaffolding + APPROVED (else the integrity gate trips on exit 5)
- **Mistake:** Ran `./ralph.sh --change foundations-domain-events-audit` while the change's scaffolding (`proposal.md`/`design.md`/`tasks.md`/delta specs/`.openspec.yaml`) and the human `APPROVED` marker were still **untracked** in the working tree. The loop fixes `BASELINE_SHA=$(git rev-parse HEAD)` at launch (= `64b6e5c`), which contained none of them (verified: APPROVED `ABSENT in baseline`). Iteration 1's task-1.1 commit `0582628` staged everything with `git add -A` and swept all 15 untracked files in — including the protected 0-byte `APPROVED`. The integrity gate (`git diff --name-only "$BASELINE_SHA" HEAD | grep -E '(^|/)APPROVED$'`, ralph.sh:136) then saw APPROVED appear in `baseline..HEAD` and halted with `exit 5 — protected layer modified since loop start`.
- **Correction:** Benign sweep-in, not tampering (APPROVED content never changed). Did **NOT** follow the script's "git revert the offending commit" hint — that would destroy the green task-1.1 work. The established, correct pattern is a dedicated pre-loop `approve:` commit (precedent `3fba1c7 approve: bootstrap-laravel-app` = `APPROVED` + hot.md + log.md, committed *before* launching the loop), so the baseline already contains the protected marker. Recorded the lesson + memory, committed them; re-run is safe because HEAD already contains APPROVED → the next `BASELINE_SHA` includes it → the gate cannot re-fire (task 2.1 won't touch any protected path).
- **Rule:** Before `./ralph.sh`, ALWAYS commit the change scaffolding **and** the `APPROVED` marker as one `approve: <change>` commit (mirror `3fba1c7`). The integrity baseline is HEAD-at-launch; ANY protected path left uncommitted at launch (`APPROVED`, `spec/**`, `openspec/specs/**`, `CLAUDE.md`, `RALPH.md`, `ralph.sh`, `.claude/**`) will trip exit 5 the moment the first task commit's `git add -A` sweeps it in. Verify a clean `git status` with no untracked protected paths before launching. (Hardening option, ralph.sh is protected → needs explicit per-file OK: at preflight, auto-stage the active change's own untracked `openspec/changes/<change>/**` into an `approve:` commit, or exclude `openspec/changes/<active>/APPROVED` from the gate grep.)
- **Recurred 2026-06-13** — identical benign exit 5 on `foundations-money-i18n-flags`: launched `./ralph.sh` with the change scaffolding + `APPROVED` still untracked; task-1.1 commit `866e92a` swept all 14 artifacts in via `git add -A`, and the gate flagged the `APPROVED` path appearing in `baseline(06aa494)..HEAD`. Verified the protected-set diff was otherwise empty (only APPROVED matched) → benign sweep, not tampering. Re-run is safe: HEAD now tracks APPROVED, so the next `BASELINE_SHA`=`866e92a` already contains it and the gate cannot re-fire (task 1.2 touches no protected path). **Two strikes on the doc-only rule → ✅ hardening implemented 2026-06-13** (Giovanni-OK'd the protected `ralph.sh` edit): the preflight, right before `BASELINE_SHA` is fixed, runs `git status --porcelain -- "$CHANGE_DIR"` and, if non-empty, `git add -- "$CHANGE_DIR"` + `git commit -m "approve: <change> (ralph preflight auto-stage…)"`. Scoped strictly to the change folder (`.last-output` is gitignored; the immutable `spec/` + `openspec/specs/` layers are never auto-committed), and idempotent on a clean tree (porcelain hides ignored files, so no spurious commit). Verified by an isolated 3-case bash test (untracked → exactly 1 commit / clean → 0 commits / post-baseline task commit no longer surfaces APPROVED in the gate) + `bash -n`. Manual discipline is no longer the only safeguard.

## 2026-06-13 — The second brain degrades silently unless enforced at the source (not just documented)
- **Mistake:** `hot.md` and `log.md` drifted out of their documented shape — `log.md` grew to ~104KB of giant single-line entries (one outcome field held 6,350 chars), timestamps were model-*estimated* (drift up to 4h39m, false chronology), and the "rotate past ~5000 lines" rule never fired because entries are mono-line (it would have hit ~8MB first). The format lived only as prose in `.claude/CLAUDE.md`/`RALPH.md`, with nothing validating writes. Giovanni flagged it: fixing the files isn't enough, fix the source so the second brain stays healthy.
- **Correction:** Built enforcement, not just better prose: `scripts/memlog.sh` (stamps the timestamp from `date` — never estimated — and rejects an outcome >280 chars), a non-blocking `.claude/hooks/memory-health.sh` Stop hook (warns on hot.md >550 words / log.md >200KB / any line >500 chars), rotation re-specified **by size (~200KB)** not line count, and rules updated in both CLAUDE.md files. Rotated the bloated log to `log-archive-2026-H1.md` (verbatim) and restarted slim.
- **Rule:** Memory writes that must hold a shape get a tool + a hook, not just a documented convention — a model will not reliably self-enforce free-form prose. Timestamps come from the real clock (`scripts/memlog.sh` / `date`), never estimated. Append to `log.md` only via `scripts/memlog.sh`; keep entries one line (narrative → the change's `progress.md`); rotate by size. When a doc-only rule keeps being violated, that's the signal to move it into tooling.

## 2026-06-13 — Pint `fully_qualified_strict_types` turns a `{@see \Namespace}` docblock ref into a broken `use`
- **Mistake:** A `TranslatableText` docblock referenced the `lang/` mechanism as `{@see \App\Platform\I18n}` — but `App\Platform\I18n` is a **namespace, not a class**. Pint's `fully_qualified_strict_types` fixer rewrote it to short form `{@see I18n}` and auto-added `use App\Platform\I18n;` (importing a namespace as if it were a class) — a broken, meaningless import the fixer happily produced.
- **Correction:** Reworded the docblock to prose ("the `lang/` group files hold…") and removed the import. The fixer behaves correctly for a real class ref (`{@see \App\Platform\Money\MoneyCast}` → `{@see MoneyCast}` + a valid `use`), so the rule is specifically about namespace refs.
- **Rule:** In a docblock `{@see …}`, reference a **concrete class/method** (`{@see SupportedLocale}`, `{@see Foo::bar()}`) or use plain prose for a namespace/concept — never `{@see \Some\Namespace}`. Pint will otherwise emit a `use` that imports the namespace as a class. After any iteration that adds rich docblocks, eyeball the post-Pint `use` block for an import that points at a namespace rather than a class.
- **Promoted 2026-06-16** → `knowledge/laravel/rules.md` (the pattern recurred across 6 changes — forward-refs, FQN form, cross-module breach). This entry stays as the quick-ref correction; the rule is the durable applied-by-default form.

## 2026-06-13 — `$this->get()` in a Pest test reds PHPStan max — use the `Pest\Laravel\*` typed globals
- **Mistake:** Wrote the first HTTP Feature test (`WelcomePageTest`, task 2.4) with `$this->get('/')->assertOk()->assertSee(...)`. The full suite was green, but PHPStan max threw 11 errors: `Call to an undefined method Pest\PendingCalls\TestCall::get()` (`method.notFound`) + a cascade of `Cannot call method assertOk()/assertSee()/assertDontSee() on mixed` (`method.nonObject`). Inside a Pest `it()`/`test()` closure, `$this` is **statically** `Pest\PendingCalls\TestCall`; the runtime rebinding to `Tests\TestCase` (where `MakesHttpRequests::get()` lives) is invisible to static analysis, and no Pest→PHPStan binding plugin is installed (`pestphp/pest-plugin-laravel` ships runtime globals, not PHPStan rules). Every earlier Feature test passed only because it never called a TestCase *instance* method — facades (`App::`, `Lang::`, `Schema::`), `expect()`, `trans()`/`__()` are all global/static.
- **Correction:** `use function Pest\Laravel\get;` and call the **global** `get('/')` (likewise `post`, `getJson`, `actingAs`, `from`, … in `vendor/pestphp/pest-plugin-laravel/src/*.php`). These are documented `@return TestResponse`, so the whole `get('/')->assertOk()->assertSee($v)->assertDontSee($v)` chain resolves clean (the assertion methods take an untyped `$value` and `return $this`). No new dependency (plugin already installed); did NOT add a Pest PHPStan plugin (this change is dep-bounded to Pennant).
- **Rule:** In Pest tests, reach for a TestCase instance method (HTTP `get/post/getJson`, `actingAs`, `withSession`, `travelTo`, …) **only** via the `Pest\Laravel\*` global function of the same name (`use function Pest\Laravel\<fn>;`) — never `$this-><fn>()`, which is unresolvable at PHPStan max. Facades/`expect()`/helpers stay as-is. This is the standing convention for ALL future route/controller/Livewire/Filament Feature tests (Module S, Admin Panel, …). (Captured as the "HTTP/instance-method Feature test under PHPStan max" Codebase Pattern in the change's progress.md.)

## 2026-06-16 — RALPH.md directives constrain the ralph agent, not Giovanni's own workflow
- **Mistake:** While scoping a cloud-scheduled "Dreaming" routine, I told Giovanni the repo would first need to be "made reachable remotely" — inferring `origin` was stale/local-only from RALPH.md's "Never `git push` — the loop commits locally; humans push."
- **Correction:** That rule binds the autonomous loop, not Giovanni. He pushes after every loop; `git remote -v` + `git status -sb` confirmed `origin → github.com/igianni84/newco-erp.git`, `main` 0-ahead/0-behind `origin/main`, HEAD == `origin/main` (public repo). The constraint I raised didn't exist.
- **Rule:** RALPH.md governs the ralph *agent's* per-iteration behaviour, not the human's operating procedure. Never infer real-world state (remote currency, branch sync, what's pushed) from an agent-facing directive — verify with git (`git remote -v`, `git status -sb`, `git rev-list --left-right --count A...B`) before asserting a constraint built on it.

## 2026-06-16 — The scheduled Dreaming routine runs on Opus 4.8 1M, not the `/schedule` skill's Sonnet default
- **Mistake:** Created the weekly cloud Dreaming routine with the `/schedule` skill's documented default model (`claude-sonnet-4-6`), reasoning the PR gate + the skill's conservative design made the cheaper model acceptable.
- **Correction:** Giovanni: the routine must use **Opus 4.8 1M** (`claude-opus-4-8[1m]`). Memory curation is judgment-heavy (promote/demote, never-invent-a-confirmation) and runs only weekly — curation quality outweighs the per-run cloud cost.
- **Rule:** For low-frequency, high-leverage, judgment-heavy automation (memory curation / Dreaming, ADR synthesis, review/judge panels), default to the **most capable** model, not the cheap default — cost is bounded by the low cadence. Decided in `decisions/2026-06-16-scheduled-memory-dreaming.md`.

## 2026-06-16 — Knowledge confirmations are dated by the archive-directory date, not progress.md's work-timestamp
- **Mistake:** Across two dreaming passes I dated `catalog-product-spine` confirmations by its internal `progress.md` work-timestamp (2026-06-14) in 2 entries, while 6 others — and `parties-producer-lifecycle` (5/5) — used the archive-directory date. The same change carried two dates across domains, which can silently split a confirmation count (the same pattern looking like two weaker ones).
- **Correction:** A `progress.md` clock and the archive-dir date can legitimately differ (work precedes/spans the archive day): `2026-06-15-catalog-product-spine` was worked 06-14; `2026-06-16-parties-producer-lifecycle` was worked 06-15. The directory date is the stable, canonical identifier. Standardized all 8 catalog-product-spine citations to 2026-06-15.
- **Rule:** Cite a change as a confirmation by its **archive-directory date** (`openspec/changes/archive/YYYY-MM-DD-<name>/`), never the inner `progress.md` clock — one change = one date, in every domain. Codified in `.claude/CLAUDE.md` → Knowledge System.

## 2026-06-18 — Two chained `->not->…` assertions red PHPStan max — re-anchor with `->and($x)`
- **Mistake:** In `ComplianceReadApiTest` (task 5.1) wrote `expect($values)->not->toContain($a)->not->toContain($b)` — one `expect` with two consecutive negatives. The full suite was green, but PHPStan max threw `property.notFound` (`Access to an undefined property Pest\Mixins\Expectation<mixed>::$not`) + `method.nonObject` (`Cannot call method toContain() on mixed`): after the first `->not->toContain()` the chain's static type degrades to `Expectation<mixed>`, so a second `->not` is unresolvable.
- **Correction:** Re-anchor between the two negatives with `->and($x)`: `expect($values)->not->toContain($a)->and($values)->not->toContain($b)` — the established repo idiom (already used in `HoldLifecycleTest`'s PII-absence assertions). `->and(...)` returns a fresh, well-typed `Expectation`, so the second `->not` resolves clean.
- **Rule:** A single `expect()` chain carries at most ONE `->not->…` step. For a second negative on the same (or another) value, start a new clause with `->and($value)->not->…`. Same family as the Pest+PHPStan-max traps (`$this->get()`, the arch-array-under-`not`): the runtime chains fine, but static analysis only narrows one `not` proxy per chain.

## 2026-06-18 — A handoff's "affected files" list is a hint; grep the blast radius from the code
- **Mistake:** The `parties-holds` 3.x handoffs (`hot.md` + `progress.md`) named exactly two shipped tests to flip when wiring the `kyc`-Hold coupling into `RequireKyc`/`RecordKycVerified` (`CustomerKycLifecycleTest`, `ComplianceIndependenceTest`). A third — `ComplianceChainTest` — also drives both Actions via its `runComplianceChain()` helper and asserts an exact event-count set (`count()===3`, `pluck('name')->unique()`, `%Hold%===0`); it would have broken on the full-suite step had I trusted the list.
- **Correction:** `grep -rn 'RequireKyc\b\|RecordKycVerified\b' app tests` before touching anything surfaced all callers — including the helper-function indirection a name-only handoff missed — so all three flipped together in one pass and the suite never went red.
- **Rule:** When modifying a widely-used symbol (Action/event/model/migration), re-derive the blast radius by grepping for every caller and every assertion that observes its side effects — never trust a hand-listed "affected files" set in a handoff. The handoff narrows attention; the grep is the truth. (Cheap insurance: the full-suite gate catches a miss eventually, but a wasted iteration is the cost.)

## 2026-06-18 — "Phase complete" is judged across ALL workplan columns, not the backend domain alone
- **Mistake:** Building a "next slice" question on the claim that Phase 2 (Catalog + Parties) was "complete" — judging completeness from the backend domain + green tests (783/783) only. Two whole columns of the Phase-2 workplan were unbuilt: the **demand-side Parties** cluster (Profile activation FSM, OC one-shot lock, membership approve/decline, Hero Package, Club Credit entity) — which I had *myself* cited as deferred two turns earlier — and the **entire Admin Panel UI** (0 Filament Resources over the existing backend), despite the Build Workplan making the Admin Panel a "first-class build target at every phase" (§1 Position 3; the Frontend column of Phase 2, lines 108/132/154).
- **Correction:** Giovanni: "non è vero che la fase 2 è completa — se dici 'chiudere Module K demand-side' già di per sé significa che non lo è; e manca la parte UI dell'Admin Panel, verifica bene." Verified from code, not the handoff: `find app -path '*Filament*Resource*'` → **0**; `party-registry` truth spec explicitly defers every demand-side status FSM; **no ADR** ratifies the UI deferral — it was implicit ralph drift (backend-first), not a decision.
- **Rule:** Before calling a phase/module "done", enumerate EVERY column of its Build-Workplan phase — backend domain + integrations + **frontend/Admin Panel** + tests/signoff — AND the supply-side vs demand-side split, then grep the codebase for each (Filament Resource count; deferred-seam markers in `openspec/specs/**`). A capability with green domain tests but **no operator surface** and a **deferred seam in the truth spec** is NOT complete. Backend-green ≠ phase-complete. When the gap is a silent drift (UI never built, no ADR), name it as a drift and propose ratifying it, don't paper over it as "done".

## 2026-06-18 — Spreading a bare-`array` Pest dataset param into a string-keyed literal reds PHPStan max
- **Mistake:** In `CustomerOnboardingActivationTest` (task 2.3) drove a "single unmet gate" dataset as `function (array $override)` then `Customer::factory()->create(['email_verified_at' => now(), …, ...$override])` with rows like `['email_verified_at' => null]`. Suite green, but PHPStan max threw `argument.type`: `create()` expects `array<string, mixed>`, `non-empty-array given`. A native `array` hint is `array<array-key, mixed>` (keys could be `int`), and spreading it into a string-keyed literal **widens the whole array's key type to `array-key`** — so the literal no longer satisfies `array<string, mixed>`.
- **Correction:** Dropped the spread for scalar dataset params (the sibling idiom — `ProfileActivationTest`'s `(ProfileState $state)`, `ComplianceIndependenceTest`'s `(KycStatus, SanctionsStatus)`): a single-field override became `function (string $field, mixed $value)` with `…, $field => $value` in the literal (a `string` dynamic key keeps it `array<string,mixed>`) and rows `['email_verified_at', null]`; the two-field KYC case split into its own `function (KycStatus $kyc)` test with literal keys. No inline `@var`, no ignore, no cast.
- **Rule:** Never `...$spread` a bare-`array`-typed Pest dataset param into a string-keyed array literal you pass to a `array<string,mixed>` sink (`Factory::create`, `update`, …) — it fails PHPStan max on the key type. Pass dataset overrides as **scalar** params (a `string $field` + `mixed $value` dynamic key for one field; a typed enum/scalar for fixed fields), the repo's scalar-dataset convention. Same family as `$this->get()` / arch-array-under-`not` / two-chained-`not`: the runtime is fine, static analysis is not.

## 2026-06-20 — The full suite via `php artisan test` blows the box's 128M memory limit; run `vendor/bin/pest` with `-d memory_limit=-1`
- **Mistake:** Ran the step-3 full-suite quality command as documented (`php artisan test`) and got a `Fatal error: Allowed memory size of 134217728 bytes exhausted` in `vendor/laravel/pao/.../TestResultParsable.php` during result aggregation over the ~950-test suite — which reads like a hard failure, not a tooling limit. Re-running as `php -d memory_limit=1G artisan test` did NOT help: the `-d` flag applies to the parent process, but `laravel/pao` (the `artisan test` runner) aggregates results in a worker that re-reads php.ini's 128M.
- **Correction:** Bypassed the pao wrapper — `php -d memory_limit=-1 vendor/bin/pest` ran the full suite clean (953/953). Same for static analysis: `php -d memory_limit=-1 vendor/bin/phpstan analyse`. A *filtered* run (`php artisan test --filter=Foo`) stays under 128M and is fine for step 2. The box default is `memory_limit=128M` (`php -r "echo ini_get('memory_limit');"`); not a project/phpunit.xml setting, so don't "fix" config.
- **Rule:** For the **full** test suite and PHPStan, invoke the binary directly with an unlimited memory flag — `php -d memory_limit=-1 vendor/bin/pest` and `php -d memory_limit=-1 vendor/bin/phpstan analyse` — never bare `php artisan test`/`composer test` (pao worker ignores `-d`, OOMs at 128M, false-reds the suite). A 128M-exhausted fatal in `laravel/pao`/`TestResultParsable` is an env limit, not a test failure — don't diagnose it as a regression.

## 2026-06-18 — A user picking a multiple-choice option endorses the CHOICE, not every clause of my description
- **Mistake:** Authored an `AskUserQuestion` option "Backend demand-side Mod K" whose description bundled the primary decision (build the demand-side Parties backend next) with a secondary inference *I* added — "la UI resta differita (da ratificare con un ADR)." Giovanni picked it for the primary thrust; I then treated the whole description, UI-deferral clause included, as his ratified intent and built the next-session prompt + `hot.md` + `log.md` on a "UI deferred + ratifying ADR" plan he never asked for.
- **Correction:** Giovanni: "non ho mai detto che la UI è differita, semplicemente la faremo come prossimo step dopo Demand-side Parties… perché nel prompt dici diversamente?" His actual intent: demand-side Parties backend first, **then** the Admin Panel UI as the immediate next step — no deferral, no ratifying ADR. Removed the ADR step; corrected the prompt + `hot.md` + the `log.md` line.
- **Rule:** When the user selects one of my authored options, they endorse the **labelled choice**, not necessarily every sub-claim in my prose. Never bundle an inferred *secondary* decision into an option's description and then treat the click as ratifying it. If a secondary decision matters, ask it separately or surface it explicitly for confirmation before building anything on it. (Same family as "mai assumere": an inference I wrote is still my inference, not the user's word — even after they click near it.)
