# Testing — Knowledge (observed facts)

> Raw observations not yet a rule. Promote to `hypotheses.md` (then `rules.md` after 3 dated confirmations).

(none yet — the engine-portability insight was strong enough to enter `rules.md` directly, derived from the 2026-06-12 `tests-pgsql` CI finding rather than via the 3-confirmation path.)
