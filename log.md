# Operations Log

> Append-only ledger. One entry per significant operation: `## [YYYY-MM-DD HH:MM] {op} | {target} | {outcome}`.
> Rotate to `log-archive-YYYY.md` past ~5000 lines. Chronology lives here; current state lives in `hot.md`.

---

## [2026-06-11 08:30] scaffold | repository | infrastructure created (CLAUDE.md, RALPH.md, ralph.sh, hooks, skills, memory files)
## [2026-06-11 08:30] scaffold | spec/ | immutable copy of handoff v0.3-MVP (45 files, source commit e55dfc8)
## [2026-06-11 08:35] openspec | init | structure + 5 opsx skills/commands generated (CLI 1.4.1)
## [2026-06-11 08:40] openspec | bootstrap-laravel-app | change drafted (9 tasks) — awaiting human approval
## [2026-06-11 09:05] push | origin/main | initial scaffold published by Giovanni (85881df)
## [2026-06-11 09:20] docs | GUIDE.md | operator playbook added (fase 0→go-live); ralph.sh/README verify references aligned (no /opsx:verify in CLI 1.4.1 core profile)
## [2026-06-11 09:41] harden | guardrails | P0 enforcement live: protected-paths hook (Edit/Write), git-guardrails write-verbs + loop-only rules (push/APPROVED/archive), ralph.sh integrity gate (exit 5) — 60/60 hook tests green
## [2026-06-11 10:20] review | bootstrap-laravel-app | fase-0 rilievi Giovanni: pinned Laravel 13.x + Filament 5.x (ADR superseded → stack-versions-and-filament-ai-tooling), task 3.2 Boost aggiunto (ora 10 task, CI→3.3 docs→3.4), direzione frontend TanStack registrata (.claude/memory + ADR; gate Module S invariato) — strict valid, NOT approved; edit CLAUDE.md in attesa di OK esplicito
## [2026-06-11 10:25] config | CLAUDE.md | edit autorizzato esplicitamente da Giovanni (AskUserQuestion, entrambe le righe): tech-stack pinnata a Laravel 13.x + Filament 5.x; riga gate storefront riscritta con direzione TanStack
## [2026-06-11 10:27] approve | bootstrap-laravel-app | APPROVED creato su istruzione esplicita di Giovanni ("mi tornano, procedi") dopo review fase-0 — change pronto per il loop (10 task)
## [2026-06-11 10:40] config | ralph.sh | --effort max di default su ogni iterazione (richiesta esplicita Giovanni; override per-lancio via RALPH_EFFORT) — flag validato su CLI installata, bash -n ok, commit db386d8
## [2026-06-11 10:51] ralph | bootstrap-laravel-app 1.1 | green | 21 files (Laravel 13.15.0 skeleton merged at root, .gitignore union, quality loop green, no pre-existing file touched)
## [2026-06-11 10:59] ralph | bootstrap-laravel-app 1.2 | green | 7 files (Pest 4.7 installed + tests/Pest.php, OPERATOR_* env contract in .env.example, /up health + :memory: environment Pest tests, fresh-file migrate verified)
## [2026-06-11 12:00] ralph | bootstrap-laravel-app 2.1 | green | 5 files (pint.json laravel-preset, composer format/lint aliases mirroring CLAUDE.md table, QualityToolingTest pins scripts+preset vs composer-require rewrites; full suite 9/16, both aliases green)
## [2026-06-11 12:30] ralph | bootstrap-laravel-app 2.2 | green | 5 files (both skeleton ExampleTests → Pest idiom, QualityToolingTest +test-script pin, composer test confirmed not re-added, test_filter verified --filter='that true is true'→1; full suite 10/18)
## [2026-06-11 12:19] ralph | bootstrap-laravel-app 2.3 | green | 8 files (Larastan v3.10.0 + PHPStan 2.2.2; phpstan.neon level=max no-baseline over app/database/routes/tests; phpstan-bootstrap.php ini_set 1G fixes 128M Homebrew OOM on the bare vendor/bin/phpstan analyse, no --memory-limit needed; composer analyse alias; 6 own-code level-max type errors fixed no-suppression; QualityToolingTest 5th pin; full suite 11/21)
## [2026-06-11 12:27] ralph | bootstrap-laravel-app 2.4 | green | 5 files (all 5 CLAUDE.md Quality Commands run in table order, all green: format/test_filter/test/type_check/lint; canonical version snapshot recorded in progress.md — PHP 8.5.2, Composer 2.9.2, Laravel 13.15.0, Filament n/a, Pest 4.7.2 (+plugin-laravel 4.1.0), PHPStan 2.2.2, Larastan 3.10.0, Pint 1.29.1; PlatformRequirementsTest pins PHP≥8.4 + Laravel ^13 floors; full suite 13/25)
## [2026-06-11 12:40] ralph | bootstrap-laravel-app 3.1 | green | 51 files (Filament v5.6.7 + Livewire 4.3.1 installed --no-interaction; AdminPanelProvider id=admin path=/admin generated+registered; User implements FilamentUser (403-gate outside local env, revisit at Module K ADR); config/operator.php + OperatorSeeder standalone idempotent w/ RuntimeException guards; phpstan stubFiles=filament/forms/.stubs.php for typed fillForm at level max; OperatorPanelTest 4 + OperatorSeederTest 3 incl. wrong-password + missing-password edges; 37 published assets committed; full suite 20/48, phpstan 0 @ max)
## [2026-06-11 12:58] ralph | bootstrap-laravel-app 3.2 | green | 8 files (laravel/boost v2.4.10 --dev; boost:install --guidelines --no-interaction driven deterministically via pre-seeded boost.json agents=[claude_code] packages=[filament/filament]; config/boost.php redirects claude_code guidelines_path CLAUDE.md→AGENTS.md (protected-file guard, vendor default would append to CLAUDE.md); AGENTS.md = 9 guidelines incl. laravel/core + filament/filament + enforce-tests; no .mcp.json, nothing under .claude/; AiToolingTest 5 pins incl. executable guidelines_path guard; full suite 25/59, phpstan 0 @ max)
## [2026-06-11 13:05] ralph | bootstrap-laravel-app 3.3 | green | 7 files (.github/workflows/ci.yml: single quality job, bare push+pull_request triggers, setup-php@v2 php 8.5 + explicit extensions incl. undeclared pdo_sqlite/sqlite3, actions/cache@v4 keyed on composer.lock, cp .env.example+key:generate because skeleton phpunit.xml ships no APP_KEY, gates pint --test → phpstan analyse → artisan test on :memory: sqlite, no memory flag thanks to committed phpstan-bootstrap.php; CiWorkflowTest 5 pins on run:-prefixed lines; YAML parse-validated via vendored symfony/yaml; full suite 30/70, phpstan 0 @ max; lesson: git-guardrails unanchored rm|mv regex matches "platform spec" prose in heredocs — use file tools for memory writes)
## [2026-06-11 13:15] ralph | bootstrap-laravel-app 3.4 | green | 7 files (docs/development.md: setup clone→install→env→migrate→serve + composer setup/dev alternatives, five Quality Commands verbatim + aliases + tool notes, CI section w/ PHP-pin policy (8.5 local minor, bump together, floor ≥8.4), ralph.sh run/monitor full reference incl. RALPH_EFFORT/CLAUDE_FLAGS model pin + exit codes 0–5 + GUIDE.md §2.7 ritual, AI tooling w/ boost:install --guidelines -n regeneration + filamentphp.com/docs/llms.txt agent index, exact-version table incl. Boost v2.4.10 w/ composer.json-verified constraints; docs/INDEX.md row updated; DevelopmentDocsTest 6 tests incl. live doc↔composer.lock version guard; full suite 36/99, phpstan 0 @ max — ALL 10 TASKS DONE → CHANGE_COMPLETE)
## [2026-06-11 14:23] close | bootstrap-laravel-app | rituale GUIDE.md §2.7 completato: pre-merge gates tutti verdi (10/10 task, validate --strict, pint, 36/36 test, phpstan 0) → merge --no-ff concluso (5d474ba, il merge era stato avviato e lasciato in pending; tree staged == tip branch, main era antenato) → push → PRIMA RUN CI REMOTA SUCCESS (30s, run 27346064961) → branch ralph/bootstrap-laravel-app cancellato → verifica semantica via subagent: CLEAN, 0 CRITICAL / 3 WARNING (W1 prosa requirement health promette check DB ma /up risponde 200 a solo boot, nessun listener DiagnosingHealth; W2 DatabaseSeeder crea test@example.com con password di fabbrica "password" + canAccessPanel()=true → login funzionante su /admin da seed, bonificare prima di staging/Module K; W3 composer.json php "^8.3" vs floor progetto ≥8.4 — fallisce a test-time invece che a install-time) / 3 SUGGESTION (S1 commento phpstan-bootstrap.php dice "raise" ma forza 1G anche su limiti maggiori; S2 CI mai provata live — RISOLTA dalla run reale; S3 welcome.blade.php copy hardcoded, invariante i18n la vieterà su superfici reali) → openspec archive → changes/archive/2026-06-11-bootstrap-laravel-app, openspec/specs/platform/spec.md creato (3 requirement). FASE 0 CHIUSA. Prossimo: ADR 1 (DB engine) + ADR 2 (event substrate) poi /spec-to-change per F1 foundations.
## [2026-06-12 08:59] adr | production-db-engine | ADR 1 decisa via grill-with-docs: **PostgreSQL** in produzione (floor 17, managed EU; dev/test resta SQLite). Istruttoria: 2 subagent sui PRD (architettura/NFR + A/B/E) — spec engine-agnostic by design (DEC-073, zero menzioni engine), vincoli reali = contesa transazionale ≤200ms p99 (B §22.1), i18n-keyed JSON (Arch §1.2/§5.1), payload eventi interrogabili (E §4.7), append-only 10y (E §7.6), EU residency. Argomenti decisivi: parità SQLite del vocabolario invarianti (CHECK/partial/expression index — MySQL non ha partial index), JSONB+GIN, DDL transazionale, FK su tabelle partizionate. Grill 4 domande: Q1 nessun bias operativo · Q2 hosting probabilmente hyperscaler EU-region (input non vincolante per gate F7) · Q3 baseline UTF-8/C.UTF-8 + ICU opt-in, zero estensioni · Q4 policy "Postgres-truthful, SQLite-compatible" + lane CI pgsql nel primo change F1 con migration di dominio. Scritti: decisions/2026-06-12-production-db-engine.md + INDEX.md. CLAUDE.md protetto → 2 righe dettate a Giovanni per edit manuale (riga tech-stack "Production engine: OPEN" + riga tabella gate). Gate "first Module 0 migration" SBLOCCATO (a valle dell'edit umano). Prossimo: ADR 2 (event substrate + audit store) poi /spec-to-change foundations.
## [2026-06-12 09:58] adr | event-substrate-and-audit-store | ADR 2 decisa via grill-with-docs, UN file che chiude ENTRAMBI i gate (substrate + audit/financial store): **transactional outbox su app DB — `domain_events` append-only scritto nella transazione emittente È insieme outbox, audit log decennale e store eventi finanziari**. Istruttoria: 4 subagent (Architecture §1.2/§5.3/§8.1, Workplan Phase 1+5, Module A §12.4 + B §22.1/§18, Module E §7–8); finding chiave: R4 da solo falsifica il dispatch sincrono nudo (consumer accoppiati alla txn dell'emitter) e le cose che DEVONO essere sincrone non sono eventi (L1 rifiuta senza emettere, A §10.5). Grill 8 domande, tutte confermate: Q1 telaio outbox (vs sync nudo e broker, respinti) · Q2 guardie fuori dal substrato, `inline` default lancio (mai I/O esterno), `queued` post-ADR-queue, I/O esterno = intent + processor di modulo, ATP B→A inline con porta `transactional` dichiarata non costruita (Phase 5 resta aperta) · Q3 topologia: `domain_events` unico (finanziari dentro, no dual-home R4) + `audit_records` (before/after, authorization_basis) + `event_deliveries` per-consumer prunabile + ledger di processo nei moduli (FSM Xero di E) · Q4 envelope (id bigint causale + event_id UUIDv7 + actor_role NOT NULL + correlation/causation + payload JSONB; FX come stringhe decimali), versioning monolite-style, PII rule: eventi PII-free (immutabilità assoluta), audit records con redazione strutturale via ruolo `redactor` (grant column-level su before/after) · Q5 tre strati immutabilità (trigger PG+SQLite identici, REVOKE prod, migrations additive-only), no hash-chaining, NO partizioni al lancio (opzione conservata, revisit trigger dichiarato) · Q6 at-least-once + exactly-once su effetti DB (handler+ledger stessa txn), consumer idempotenti/order-tolerant, no FIFO bloccante, dead-letter in loco · Q7 gate queue NON triggerato (atteso F4–F6, requisiti pre-fissati: at-least-once + per-job delay), object storage intatto · Q8 un solo file ADR. Scritti: decisions/2026-06-12-event-substrate-and-audit-store.md + INDEX.md (riga + open list) + CONTEXT.md (sezione Events & Audit: Domain Event, Audit Record, Financial Event). CLAUDE.md protetto → 3 edit dettati a Giovanni (2 righe gate da cancellare + riga Architecture da sostituire; opzionale raffinamento riga queue). Gate `foundations-domain-events-audit` SBLOCCATO (a valle dell'edit umano). Prossimo: /spec-to-change foundations (F1).
## [2026-06-12 10:31] spec-to-change | foundations-modules-skeleton | change F1 1/3 autorato + `openspec validate --strict` ✅ + **APPROVED da Giovanni** ("Si approvo"; marker creato dalla skill su autorizzazione esplicita). Capability nuova `module-architecture`: 6 requirement / 15 scenari, tutti con provenienza (ADR monolite 2026-06-11, Architecture §0.5/§1.2/§2.1/§2.2/§2.3, Workplan Phase 1, invariante 10) — skeleton+registry, boundary law (superficie pubblica = solo `Contracts\*`+`Events\*`; piattaforma mai → moduli; composition root esente), persistenza module-prefixed, OperatorPanel placement, enforcement nel default suite, module-build template. 9 task / 4 gruppi: 1.1 enum `App\Modules\Module` (9 case + lettere spec) → 1.2 nove dir+provider+registrazione → 1.3 move AdminPanelProvider in OperatorPanel (suite esistente verde NON modificata) → 2.1–2.4 arch test Pest (conformance set-equality, privacy cross-module, direzione piattaforma, convenzione `$table` forward-binding) con **red-proof obbligatorio** documentato in progress.md → 3.1 docs/module-template.md (9 sezioni, naming cascade Module 0 §18) → 4.1 sweep + mappa scenario→test. Intervista: 2 domande, entrambe le Recommended accettate — (1) panel → `App\Modules\OperatorPanel\Providers` (design D5); (2) tabelle dominio `{modulo}_*` con `$table` esplicito (design D6). Zero nuove dipendenze (pest-plugin-arch già in lock con Pest 4.7.2; composer.json/lock devono restare intatti). Esclusioni dichiarate nel proposal: substrato eventi + lane CI pgsql → foundations-domain-events-audit; Money/i18n/flags/actor_role → foundations-money-i18n-flags; frontend shell e auth → gate ADR. Prossimo: `caffeinate -i ./ralph.sh --change foundations-modules-skeleton 15` da terminale puro.
## [2026-06-12 10:40] config | ralph.sh | model pinnato di default su ogni iterazione: `--model "${RALPH_MODEL:-claude-opus-4-8[1m]}"` (Opus 4.8, contesto 1M) a ralph.sh:188 + header doc :24 (richiesta esplicita Giovanni). Effort già `max` (invariato). Prima ralph non aveva `--model` e ereditava il default account (silenzioso). Override per-lancio via `RALPH_MODEL`; `CLAUDE_FLAGS` resta in coda e vince (ultimo `--model` prevale). Stringa validata con chiamata reale (`claude --model 'claude-opus-4-8[1m]' --print` → exit 0); `bash -n` ok; brackets `[1m]` = glob shell → valore sempre fra doppi apici. Precedente analogo: `--effort max` db386d8 (2026-06-11). Memoria `ralph-model-and-effort-resolution` + indice MEMORY.md aggiornati. Commit+push su main (convenzione repo per tweak tooling/meta).
## [2026-06-12 11:05] ralph | foundations-modules-skeleton 1.1 | green | 3 files (App\Modules\Module string-backed enum, 9 case Catalog…OperatorPanel con backing snake_case catalog…operator_panel; letter() match esaustivo → 0,K,A,D,S,B,C,E,Admin; namespace() = __NAMESPACE__.'\\'.name → App\Modules\{CaseName}, drift-proof; tests/Unit/Modules/ModuleTest.php 5 test/6 assert — count=9, mappa lettere verbatim via toBe, mappa value snake_case via toBe (pin prefisso tabella D6 per task 2.4), namespace() Catalog+OperatorPanel, failure Module::from('warehouse')→ValueError; Unit suite gira su plain PHPUnit TestCase, no boot Laravel; full suite 41/41 105 assert (era 36/99), phpstan 0 @ max, pint+lint ✅, validate --strict ✅, composer.json/lock diff vs main vuoto; primo task del change)
## [2026-06-12 10:57] ralph | foundations-modules-skeleton 1.2 | green | 14 files (nove `app/Modules/{Name}/Providers/{Name}ServiceProvider extends Illuminate\Support\ServiceProvider`, register()/boot() vuoti = wiring seam D1, idiom AppServiceProvider no strict_types; OperatorPanel ha il provider standard `OperatorPanelServiceProvider` distinto dal Filament `AdminPanelProvider` D5; `bootstrap/providers.php` registra i nove **derivati dal registry** via `...array_map(fn (Module $m) => $m->providerClass(), Module::cases())` dopo i due provider piattaforma tenuti — panel resta in App\Providers\Filament fino a 1.3; aggiunto `Module::providerClass()` = `{namespace}\Providers\{Name}ServiceProvider` così composition root E test conformance derivano l'FQCN dal registry, single source of truth — pin in ModuleTest Catalog+OperatorPanel; tests/Feature/Modules/ModuleProvidersTest.php 3 test che iterano Module::cases() — toHaveKeys su app()->getLoadedProviders() (Application.php:1477, array [FQCN=>true], non-deferred eager), class_exists+file path, failure not->toHaveKey su Warehouse fabbricato; full suite 45/45 135 assert (era 41/105), phpstan 0 @ max, pint+lint ✅, `php artisan about` exit 0, validate --strict ✅, composer.json/lock diff vs main vuoto; next 1.3 move AdminPanelProvider in OperatorPanel)
## [2026-06-12 11:05] ralph | foundations-modules-skeleton 1.3 | green | 5 files (relocate Filament panel nel modulo proprietario D5: nuovo `app/Modules/OperatorPanel/Providers/AdminPanelProvider.php` namespace `App\Modules\OperatorPanel\Providers`, **body byte-for-byte invariato** — panel id/path `admin`, `->login()`, Amber, Dashboard, i tre `discoverResources/Pages/Widgets` string-based ancora su `app/Filament/*` (stringhe ≠ import di simboli, ammesse D5/D3; quelle dir non esistono, Filament tollera); `bootstrap/providers.php` use aggiornato (`App\Providers\Filament\AdminPanelProvider`→`App\Modules\OperatorPanel\Providers\AdminPanelProvider`, entry `::class` invariata); vecchio file `git rm` + dir `app/Providers/Filament/` rimossa (AppServiceProvider resta); **`composer dump-autoload`** per eliminare la entry stantia del classmap non-authoritative (vecchio FQCN → file cancellato: `class_exists` includerebbe un file mancante → warning → fallimento Pest) — tocca solo vendor/composer/autoload_*.php, vendor gitignored, zero churn composer.json/lock; OperatorPanel ora ospita ENTRAMBI i provider, standard seam via spread + Filament PanelProvider esplicito, ruoli distinti. ModuleProvidersTest +3 pin: class_exists(AdminPanelProvider::class)+toHaveKey su getLoadedProviders, class_exists('App\Providers\Filament\AdminPanelProvider')→toBeFalse, Filament::getPanel('admin')->getId()→'admin' (FilamentManager.php:372, Panel::getId via HasId). Rete regressione `OperatorPanelTest`+`OperatorSeederTest` 7/7 23 assert IDENTICA prima/dopo, file NON modificati; full suite 48/48 139 assert (era 45/135), phpstan 0 @ max, pint+lint ✅, `php artisan about` exit 0, validate --strict ✅, composer diff vs main vuoto; next 2.1 arch suite tests/Architecture/ + phpunit.xml con red-proof)
## [2026-06-12 11:17] ralph | foundations-modules-skeleton 2.1 | green | 4 files (stand up Architecture suite D8: nuovo `tests/Architecture/ModuleConformanceTest.php` 2 test — conformance registry↔filesystem (scenario "No stray entries at the modules root"): le directory direttamente sotto `app/Modules/` == esattamente i nove nomi `Module::cases()` (set-equality via `sort()`+`toBe` strict, **simmetrica**: manca-una E dieci-esima-dir entrambe falliscono) + `Module.php` unico file sciolto; `phpunit.xml` +testsuite `Architecture` → `php artisan test` la esegue di default (48→50 test, 141 assert — il delta È la prova "suite runs by default"); `tests/Pest.php` **INVARIATO** — conformance boot-free, gira sul PHPUnit `TestCase` default, nessun bind `Tests\TestCase` per la nuova dir. Path modules root via reflection `dirname((string)(new ReflectionClass(Module::class))->getFileName())` NON `app_path()` (D8 no-boot + immune al flush del container fatto da un Feature test precedente); false-union guardate per phpstan max su tests/ (`(string)` su getFileName, `?: []` su scandir); dot-entries filtrate (`! str_starts_with($e,'.')` → `.DS_Store` gitignored = no false-red su macOS). **RED-PROOF**: `mkdir app/Modules/Warehouse` → suite RED (il test directories fallisce `Failed asserting that two arrays are identical … +9 => 'Warehouse'`, exit 1; il test "only loose file" resta verde perché dir≠file) → `rmdir` → 2/2 GREEN exit 0, `git status` pulito (nessun fixture residuo). Caso manca-una garantito dalla stessa `toBe` simmetrica. filtered 2/2 + `--filter="only loose file"` 1/1 (test_filter ok), full 50/50, phpstan 0 @ max, pint+lint ✅, validate --strict ✅, composer.json/lock diff vs main vuoto, nessun file protetto toccato; next 2.2 privacy cross-module `ModuleBoundariesTest` con pest-plugin-arch — verificare l'API expectation in `vendor/pestphp/pest-plugin-arch/src/` PRIMA di scrivere, D4)
## [2026-06-12 11:27] ralph | foundations-modules-skeleton 2.2 | green | 5 files (privacy cross-module arch test D3: nuovo `tests/Architecture/ModuleBoundariesTest.php` 1 test/144 assert — per ogni modulo M (`foreach Module::cases()`) un `expect($M->namespace())->not->toUse($forbidden)->ignoring($publicSurface)` dove `$forbidden`=namespace degli altri otto moduli e `$publicSurface`=`\Contracts`+`\Events` di ciascun altro; entrambe le liste derivate da `Module::cases()` (source filtrato fuori), zero hardcode → un decimo modulo coperto appena entra nel registry. **API verificata in `vendor/pestphp/pest-plugin-arch/src/` (v4.0.2) PRIMA di scrivere (D4)** — ToUse.php, PendingArchExpectation::not(), SingleArchExpectation::ignoring(), LayerFactory::make (`str_starts_with` = ignoring è match per PREFISSO di namespace), Blueprint::expectToUse; **provato empiricamente** il punto sottile (semantica multi-arg `not->toUse`): scratch `Catalog\ScratchAlpha` che usa SOLO Parties → `->not->toUse(['…Parties','…Allocation'])` FALLISCE → `not->toUse([list])` fallisce su QUALSIASI namespace usato (non tutti) e **nomina la coppia** ("Expecting 'App\Modules\Catalog' not to use 'App\Modules\Parties'."); scratch rimossi. **RED-PROOF** (fixture nuove = autoload PSR-4 `App\` live, niente dump-autoload, gotcha solo su classi MOSSE): (a) `app/Modules/Catalog/Tmp.php` → `App\Modules\Parties\Providers\PartiesServiceProvider` (INTERNO) → RED exit 1 nominando la coppia Catalog→Parties; (b) aggiunto temp `interface App\Modules\Parties\Contracts\TmpContract` e cambiato SOLO l'import di Tmp.php a quel contract (interno→superficie-pubblica unica variabile) → GREEN 1 test/144 assert exit 0 (`->ignoring([…\Contracts,…\Events])` ammette la superficie pubblica, flagga gli interni); rimossi entrambi + dir `Contracts/` temp, `git status` solo il nuovo test (dir moduli tornano a solo `Providers/`). Copre delta-spec scenari "Cross-module internal import fails the suite" (riga 32) + "Public-surface import is allowed" (riga 37). format ✅ · filtered `--filter="keeps each module private"` 1/1 (test_filter ok) · full 51/51 285 assert (era 50/141) · phpstan 0 @ max · pint --test ✅ · validate --strict ✅ · `git diff main -- composer.json composer.lock` vuoto · nessun file protetto toccato; next 2.3 direzione piattaforma — STESSO file, blocco `App\Providers|Models|Http` not->toUse `App\Modules` (lista piattaforma come singola costante/array), red-proof temp class in `app/Providers/` che importa `App\Modules\Catalog\Providers\CatalogServiceProvider`)
## [2026-06-12 11:40] ralph | foundations-modules-skeleton 2.3 | green | 3 files (direzione-piattaforma boundary law D3, seconda riga della tabella; delta-spec scenario "Platform never imports modules" righe 42-45: ESTESO `tests/Architecture/ModuleBoundariesTest.php` con un SECONDO blocco `it('forbids platform code from depending on any module')` separato dal blocco privacy 2.2 — namespace piattaforma enumerati in un singolo array nominato `$platformNamespaces=['App\Providers','App\Models','App\Http']` (punto di emendamento citato dal template) e il test ITERA l'array con `expect($ns)->not->toUse('App\Modules')` per ciascuno; **nessun ->ignoring()** — il codice piattaforma NON ha esenzione superficie-pubblica (D3 riga 2: Contracts/Events=NO, internals=NO), target `App\Modules` come intero; `bootstrap/providers.php` NON special-cased — sta sotto bootstrap/, fuori app/, in nessun layer `App\*`. **API verificata in vendor PRIMA + provata empiricamente (D4)**: ToUse.php (`Expectation<array|string>` → source array accettati), Targets::fromExpectation (splitta array source), Blueprint::expectToUse (`foreach target->value`, `$failure($targetValue,…)` nomina il source colpevole), ObjectsRepository (globba le dir PSR-4 live via Symfony Finder → fixture NUOVE scansionate senza dump-autoload), ObjectDependenciesDescription::make+ServiceContainer::init (uses = `Node\Name` AST che passano class_exists dopo NameResolver). **TRAPPOLA REALE intercettata dal red-proof**: la prima formulazione `expect([tutti e 3])->not->toUse('App\Modules')` era vacuously GREEN anche col fixture violante presente — un array sul lato SOURCE sotto `not` nega l'AGGREGATO "TUTTI i source usano X" (semantica di expectToUse), quindi passa appena UN source è pulito (`App\Models` pulito mascherava `App\Providers` sporco); tre scratch test l'hanno isolata (single-source not=RED, array-source positivo=fallisce su App\Models, array-source not=GREEN spurio); riscritto come loop per-source. **REGOLA → lessons.md + Codebase Patterns: array sul lato DEPENDENCY (safe='usa QUALSIASI'), loop sul lato SOURCE.** RED-PROOF: temp `app/Providers/TmpPlatformProbe.php` (typed property `public CatalogServiceProvider $provider` — un type reference è raccolto in modo affidabile; `::class` da solo NON registra) → RED exit 1 `Expecting 'App\Providers' not to use 'App\Modules'.` nominando il namespace piattaforma colpevole, test privacy 2.2 resta verde → rimosso → 2/2 GREEN 150 assert exit 0, `git status` solo il test modificato. format ✅ · filtered `--filter="forbids platform code from depending on any module"` 1/1 (test_filter ok) · full 52/52 291 assert (era 51/285) · phpstan 0 @ max · pint --test ✅ · validate --strict ✅ · `git diff main -- composer.json composer.lock` vuoto · nessun file protetto toccato; next 2.4 persistence-convention test `ModulePersistenceConventionsTest` reflection-over-models D6, pass-by-proven-empty + red-proof ×3)
## [2026-06-12 11:53] ralph | foundations-modules-skeleton 2.4 | green | 2 files (persistence-convention test D6 reflection-over-models, pass-by-proven-empty; delta-spec "Module Persistence Conventions" scenari "Module model declares its prefixed table" + "Empty model set is handled honestly": nuovo `tests/Architecture/ModulePersistenceConventionsTest.php` 1 test/2 assert — per ogni modulo (`foreach Module::cases()`) cammina ricorsivamente la dir del modulo (`RecursiveIteratorIterator`+`RecursiveDirectoryIterator(SKIP_DOTS)`, guard `$item instanceof SplFileInfo && isFile()` + `getExtension()==='php'`), mappa path→FQCN **radicato su `$module->namespace()`** (no `App\` hardcoded; un modulo per volta → modulo proprietario e prefisso richiesto `$module->value.'_'` noti strutturalmente, zero parsing del path), classifica `class_exists` → `isSubclassOf(Model::class) && ! isAbstract()` (solo modelli Eloquent concreti), e per ciascuno asserisce che il default `$table` inizi col prefisso modulo. NON un test pest-plugin-arch — la regola è il VALORE di una proprietà (`$table`), non un grafo di import → scan a riflessione fatto a mano. **Vendor verificato PRIMA di scrivere (D4/CLAUDE.md verify-before-use)**: base `Illuminate\Database\Eloquent\Model` dichiara `protected $table;` (`Model.php:70`) → probe empirico (solo in /tmp, mai nel repo) di `getDefaultProperties()`: chiave `'table'` SEMPRE presente (ereditata), valore = stringa dichiarata o **`null`** se la sottoclasse non l'override (nessun warning undefined-key); letto `…['table'] ?? null` + guard `is_string($table) && str_starts_with($table,$prefix)` → modello senza `$table` legge `null` → non stringa → violazione. **PROVEN-EMPTY non vacuo**: `expect($scannedFiles)->not->toBeEmpty()` PRIMA (lo scan ha visto codice modulo reale — oggi i 10 file provider; un path rotto troverebbe nulla → falso-verde) POI `expect($violations)->toBe([])` (la legge sul set modelli oggi vuoto); entrambe assertion reali ogni run (no flag risky/no-assertion), la legge si vincola in avanti appena arriva il primo modello. Boot-free modules root via reflection (`dirname((string)(new ReflectionClass(Module::class))->getFileName())`, come 2.1). **RED-PROOF ×3** (file temp NUOVO = PSR-4 live, no dump-autoload; `app/Modules/Catalog/Models/TmpProductMaster.php` extends Model, ciclato su `php artisan test tests/Architecture/`, gli altri 4 arch test sempre verdi): (a) `$table='product_masters'` → RED 4/5 `…must declare $table starting with "catalog_" (got "product_masters")` nomina offender+prefisso; (b) nessun `$table` → RED 4/5 `(got none)` (null preso dal guard is_string); (c) `$table='catalog_product_masters'` → GREEN 5/5 154 assert (modello classificato → scan raggiunge Catalog/Models/, $table letto, prefisso ok, forward-binding dimostrato); rimossi modello+dir `Catalog/Models/` vuota, `git status` solo il nuovo test (Catalog torna a solo Providers/). Creare `Catalog/Models/` NON fa scattare il conformance 2.1 (scansiona solo dir DIRETTAMENTE sotto app/Modules/). format ✅ · filtered `--filter="requires every module Eloquent model to declare its module-prefixed table"` 1/1 2 assert (test_filter ok) · full 53/53 293 assert (era 52/291) · phpstan 0 @ max · pint --test ✅ · validate --strict ✅ · `git diff main -- composer.json composer.lock` vuoto · nessun file protetto toccato; gruppo 2 (arch suite) COMPLETO 4/4; next 3.1 docs/module-template.md 9 sezioni D7 + INDEX/development.md + doc-pin test)
## [2026-06-12 12:04] ralph | foundations-modules-skeleton 3.1 | green | 5 files (module-build template docs D7: nuovo `docs/module-template.md` — la falsariga che ogni change modulo F2+ segue, con ESATTAMENTE le nove sezioni di design D7, documentando SOLO convenzioni DECISE (D1–D8, nulla inventato): (1) tabella nove moduli+lettere-spec (registry-driven via `Module::cases()`); (2) albero layout canonico + regola lazy-creation (`Providers/` sola allo skeleton, no `.gitkeep`); (3) superficie pubblica (`Contracts\*`+`Events\*`) & tabella boundary-law D3 + protocollo di emendamento (edit arch-test nello stesso change + giustificazione nel design.md); (4) convenzioni service-provider (routes/translations/listeners/bindings che il seam riempie); (5) operator-surface placement (D5 `->discoverResources` string-based); (6) persistence (D6 `$table` module-prefixed, esempio letterale `catalog_`, migrations nel `database/migrations/` globale, policy "Postgres-truthful, SQLite-compatible" per riferimento a `decisions/2026-06-12-production-db-engine.md`); (7) terminologia & naming cascade (CONTEXT.md glossary of record, Module 0 §18 source-of-truth + `spec/02-prd/Module_0_PRD_v0.3-MVP.md`); (8) test patterns (Architecture suite always-on, per-module `tests/{Feature,Unit}/Modules/{X}`, SQLite `:memory:`, PHPStan max, red-proof discipline); (9) checklist add-a-module-slice. Aggiunti riga `docs/INDEX.md` + una riga cross-ref in `docs/development.md`. Doc-pin test `tests/Feature/ModuleTemplateDocsTest.php` (7 test/24 assert, sullo stile di `DevelopmentDocsTest`): file esiste; nove sezioni numerate `## 1.`…`## 9.` via `range(1,9)`; **registry-complete** `foreach Module::cases()` esige una riga che accoppia `->name` E `->letter()` (same-line `str_contains` entrambi — codifica lo scenario delta-spec "every module appears with its spec letter"; il NOME distintivo è la prova-di-fallimento se una riga viene tolta — `toContain($letter)` da solo è quasi-vacuo per le lettere singole A/D/S/B/C/E/K); `Contracts`+`Events` presenti; letterale `catalog_` presente; `CONTEXT.md` referenziato; **failure-ish non-vacuo** — lo STESSO helper di pairing torna vuoto per un nome non-modulo (`Warehouse`,`W`), provando che le assertion registry-complete possono davvero fallire. format ✅ · filtered `--filter="is registry-complete"` 1/1 9 assert (test_filter ok) · full suite 60/60 317 assert (era 53/293, +7 test/+24 assert esattamente il nuovo file) · phpstan 0 @ max · pint --test ✅ · validate --strict ✅ · `git diff main -- composer.json composer.lock` vuoto (zero nuove dep) · nessun file protetto toccato; gruppo 3 (docs) COMPLETO 1/1, 8/9 task fatti; next 4.1 sweep traceability+qualità finale: scenario→test mapping in progress.md, cinque comandi in ordine CLAUDE.md, validate --strict, composer diff vuoto → su tutto-verde `<promise>CHANGE_COMPLETE</promise>`)
## [2026-06-12 12:21] ralph-finish | foundations-modules-skeleton 4.1 | green (interattivo) | il loop si era fermato dopo 3.1 (8/9, ultimo commit 6f7c7f6) lasciando 4.1 non eseguita — completata interattivamente nel rituale di chiusura. Deliverable = tabella tracciabilità scenario→test in progress.md (6 requirement / 15 scenari, copertura PIENA già da 1.1–3.1 → ZERO nuovi test, come da regola del task) + sweep dei cinque comandi in ordine CLAUDE.md: format pint no-op ✅ · test_filter (filtro is-registry-complete) 1/1 9 assert ✅ · test 60/60 317 assert ✅ · type_check phpstan 0 @ max ✅ · lint pint --test ✅; validate --strict ✅; `git diff main -- composer.json composer.lock` vuoto; 27 file vs main, nessun file protetto. tasks.md 4.1 ✓ → 9/9. Commit 059c5e6 sul branch.
## [2026-06-12 12:25] close | foundations-modules-skeleton | rituale GUIDE §2.7 eseguito interattivamente (Giovanni ha delegato i 4 step; scelte via AskUserQuestion: completa-4.1-interattivo + push-su-origin). Verifica semantica pre-merge (operatore, non subagent): CLEAN — 0 CRITICAL / 0 WARNING sull'implementazione (6/6 requirement implementati+testati, 15/15 scenari coperti, ogni scenario arch con red-proof registrato in progress.md); unico rilievo = di PROCESSO (loop fermo prima di 4.1, bonificato sopra). Suite verde su main post-merge 60/60 317 assert. merge --no-ff **dc36dfc** (storia 9 commit del branch preservata) → push origin main (0b65865..dc36dfc, sync 0 0) → `openspec archive foundations-modules-skeleton --yes`: creata spec di verità `openspec/specs/module-architecture/spec.md` (6 requirement), change spostato in `openspec/changes/archive/2026-06-12-foundations-modules-skeleton/`, Task status ✓ Complete → commit archive + push. Branch `ralph/foundations-modules-skeleton` NON cancellato (lasciato per ora). Rilievo minore: la spec di verità ha `## Purpose: TBD` (placeholder del tool; invariante 11 → non editato a mano, tidy-up opzionale). **F1 1/3 CHIUSA.** Prossimo: /spec-to-change per F1 2/3 `foundations-domain-events-audit` (substrato eventi, ADR 2026-06-12-event-substrate-and-audit-store + prima lane CI pgsql).
## [2026-06-12 14:18] spec-to-change | foundations-domain-events-audit | AUTHORED + APPROVED (validate --strict ✅, 4/4 artifact). F1 2/3: implementa l'ADR transactional-outbox `decisions/2026-06-12-event-substrate-and-audit-store.md` as-decided. Grounding: ADR per intero + ADR PG + module-template + archivio F1 1/3 letti diretti; sezioni spec (Architecture §0.5/§1.2/§5.3/§8.1, Workplan Phase 1+5, A §7.1/§10.5/§11.5.1/§12.4, B §10.2/§18/§22.1, E §4.7/§7/§8, K §8.2/§12, R4 via spec/README.md "single most important thing") estratte via 4 subagent Explore. Delta specs: capability NUOVA `event-substrate` (8 requirement / 26 scenari: transactional recording con guard fuori-txn; envelope ADR completo con FX decimal-string e PII-free; audit records col floor = unione due tabelle; delivery ledger per-consumer R4-meccanizzato; inline+sweep at-least-once / exactly-once-DB / dead-letter-in-place / queued gated; ordering A §12.4 verbatim + obblighi idempotenza/watermark; immutabilità trigger ENTRAMBI gli engine + seam redazione before/after + REVOKE runbook; hello-world E2E + events:demo) + MODIFIED `platform` Quality Pipeline (lane CI pgsql, postgres:17 floor, da DB-ADR "stesso change della prima migrazione domain"). Design D1–D10 + 10 rischi/landmine per il loop (afterCommit sotto RefreshDatabase con scala di fallback D5; SQLite no ALTER-ADD-CHECK → fallback policy su actor_role; precedenza env phpunit.xml; lista API vendor da verificare; lane pgsql vista verde solo al primo push umano). Tasks: 15 in 6 gruppi dependency-ordered (1.1 enums+amendment legge arch `App\Platform` con red-proof → 2.1–2.4 PRIME migrazioni domain: 3 tabelle + trigger immutabilità → 3.1–3.4 model / contract+registry / AuditRecorder / DomainEventRecorder → 4.1–4.2 InlineDeliveryExecutor+hook post-commit / events:sweep+schedule+config → 5.1–5.2 hello-world+events:demo / lane pgsql+CiWorkflowTest → 6.1–6.2 docs/event-substrate.md / sweep tracciabilità). INTERVISTA founder (le 2 sole scelte non-ADR, recepite D1/D9): root piattaforma `App\Platform\*` (Events+Audit; respinto Laravel-flat: App\Events fuorvia sulla semantica del bus nativo) + hello-world = test E2E + comando `php artisan events:demo`. Fuori fetta dichiarato in proposal: queued/driver coda (F4–F6), object storage (INV1), Money/i18n/flags/helper actor_role (F1 3/3), eventi modulo reali (F2+), job redazione GDPR K, superficie operator failed-deliveries, pruning ledger, modo transactional. APPROVED creato su OK esplicito di Giovanni (stessa exchange, AskUserQuestion). Next: `./ralph.sh --change foundations-domain-events-audit`.
## [2026-06-12 14:26] ralph | foundations-domain-events-audit 1.1 | green | 5 files (root piattaforma `App\Platform` + tre enum substrate + emendamento legge boundary, design D1. NUOVI: `app/Platform/Events/ActorRole.php` (string-backed `NewcoOps=newco_ops`/`Producer`/`Customer`/`System` — provenance attore invariante 8), `DeliveryStatus.php` (`Pending`/`Done`/`Failed` — ciclo `event_deliveries`), `DeliveryMode.php` (caso unico `Inline=inline`; `queued` NON rappresentabile → "queued gated" garanzia a compile-time fino all'ADR coda F4–F6); convenzione enum mirror di `App\Modules\Module` (PascalCase→snake_case, docblock citante design/spec). Test `tests/Unit/Platform/EnumsTest.php` 4/4: tre mappe case→value verbatim `->toBe([...])` order-sensitive (mirror `ModuleTest`) + failure `ActorRole::from('admin')`→`ValueError`. EMENDAMENTO legge: aggiunto `'App\Platform'` a `$platformNamespaces` in `tests/Architecture/ModuleBoundariesTest.php` con commento citante D1 (unico edit legittimo all'array hardcoded; F1 3/3 `App\Platform\Money` ricade sotto la STESSA radice → nessun ulteriore emendamento). **RED-PROOF (obbligatorio, design D1 + lessons.md 2026-06-12)**: temp `app/Platform/TempBoundaryProbe.php` (`namespace App\Platform; use App\Modules\Module;` → `Module::Catalog`) → suite arch **RED** `it_forbids_platform_code_from_depending_on_any_module` con `Expecting 'App\Platform' not to use 'App\Modules'.` (2 test, 1 pass 1 fail) nominando la coppia esatta → `rm` probe → **GREEN** 2/2 152 assert, `git status app/` solo `?? app/Platform/` (i tre enum), probe sparito, albero pulito. Nessun DB/migrazione (primi migration a 2.1); enum senza metodi (cast/uso a 3.1). format ✅ · test_filter EnumsTest 4/4 ✅ · full **64/64 323 assert** (era 60/317, +4 esatti) ✅ · phpstan 0 @ max ✅ · pint --test ✅ · validate --strict ✅; nessun file protetto toccato, `composer.json/lock` invariati. tasks 1/15. Next 2.1 migrazione `create_domain_events_table` (envelope ADR + CHECK actor_role solo PG / fallback SQLite documentato, indici compositi, FK self-ref causation_id; verificare in vendor `jsonb()`/`timestampTz()` su SQLite + API `Schema::getIndexes()`).
## [2026-06-12 14:52] fix | foundations-domain-events-audit — gate integrità ralph (exit 5) | RISOLTO. Il loop si è fermato dopo task 1.1 (verde) NON per un task fallito ma sul gate integrità di `ralph.sh` (riga 136 `git diff --name-only "$BASELINE_SHA" HEAD | grep -E '(^|/)APPROVED$'`). Causa provata: al lancio le impalcature del change + il marker `APPROVED` erano UNTRACKED — assenti in `BASELINE_SHA=64b6e5c` (`git cat-file -e …:…/APPROVED` → ABSENT in baseline) — e il `git add -A` del commit task-1.1 `0582628` le ha risucchiate tutte (15 file, incl. `…/APPROVED|0`), facendo comparire `APPROVED` nel range `baseline..HEAD`. BENIGNO: marker 0-byte, contenuto invariato, non manomissione; il gate non sa distinguere "marker umano risucchiato" da "loop fabbrica approvazione" → blocca per prudenza (giusto). Branch locale, non pushato (no upstream, 0582628 su nessun remoto), 1 solo commit oltre main. NON seguito il "Recover: git revert" dello script (avrebbe distrutto il lavoro verde di 1.1). Fix: lezione Mistake→Correction→Rule in lessons.md (pattern di riferimento = precedente `3fba1c7 approve: bootstrap-laravel-app`, commit dedicato APPROVED+hot+log PRIMA del loop) + hot.md + questa riga, committati come `chore:`. Re-run sicuro: HEAD ora CONTIENE già `APPROVED` → il prossimo `BASELINE_SHA` (=HEAD al lancio) lo include → `git diff baseline..HEAD | grep APPROVED` vuoto → gate non riscatta; task 2.1 (migrazione) non tocca path protetti. Prossimo: `./ralph.sh --change foundations-domain-events-audit` riprende da 2.1. Opzioni offerte a Giovanni: (A) re-run così com'è [raccomandato]; (B) split storia in `approve:`+`feat 1.1`; (C) hardening ralph.sh (file protetto, serve OK).
## [2026-06-12 15:20] ralph | foundations-domain-events-audit 2.1 | green | 2 files (PRIMA migrazione domain: `database/migrations/2026_06_12_000001_create_domain_events_table.php` — il log append-only `domain_events` (outbox transazionale + event store audit/finanziario 10-anni), envelope ADR completo + indici di lancio + CHECK actor_role solo-PG, design D2. Colonne: `id` bigint PK (sequence-backed = ordine d'inserzione monotòno, ancora causal-order A §12.4) · `event_id` uuid unique (UUIDv7 app-side dal recorder 3.4) · `name` · `schema_version` unsignedSmallInteger default 1 · `module` (Module->value, o 'platform' per il demo) · `occurred_at` timestampTz (UTC app-set) · `actor_role` string NOT NULL · `actor_id` nullable unsignedBigInteger · `entity_type`/`entity_id` string · `correlation_id` uuid · `causation_id` nullable **self-FK→domain_events.id** · `payload` jsonb (NOT NULL; money=minor-units, FX=stringhe decimali). Indici `(entity_type,entity_id,id)` e `(name,id)` (id-suffissati → letture in ordine causale gratis; no GIN su payload finché non serve, ADR). **CHECK actor_role**: solo PostgreSQL via `if (DB::getDriverName()==='pgsql') DB::statement('ALTER TABLE … ADD CONSTRAINT … CHECK (actor_role IN (…)))'` con i valori DERIVATI da `ActorRole::cases()` (non può driftare dall'enum); fallback SQLite = NOT NULL + cast enum (3.1), documentato inline — policy documented-fallback, design D2; il branch PG è provato dalla lane pgsql 5.2/primo push umano (D8). **Verify-vendor-first** (regola lessons.md) PRIMA di scrivere: fallback grammar SQLite `typeJsonb`→`text`/`typeTimestampTz`→`typeTimestamp`/`typeUuid`→`varchar`, firme `Schema::hasColumns`/`hasIndex`/`getIndexes` + `hasIndex` matcha l'array di colonne esatto, `DB::getDriverName()` a Connection.php:1443, sqlite `foreign_key_constraints` default true → self-FK davvero enforced (happy path usa `causation_id=null`). Test `tests/Feature/Platform/DomainEventsSchemaTest.php` (`uses(RefreshDatabase::class)`) 7/8: colonne envelope (`hasColumns`), unique `event_id` (`hasIndex …'unique'`), due indici compositi (`hasIndex` su array colonne), happy-path inserimento riga completa + `schema_version` default 1 (`->value()->toEqual(1)`), catena causale via self-FK, NOT-NULL floor (manca `actor_role` → `QueryException`), duplicato `event_id` → `QueryException`. **PHPStan max ha pescato 2 gap reali nel test** (fixati alla radice, non soppressi): helper-row serviva `array<string,mixed>` param/return; lettura schema_version da `(int) $row->col` (`$row` è `stdClass|null`, valore `mixed` → `cast.int`) → spostata su `->value('schema_version')` asserita con `->toEqual(1)`. format ✅ · test_filter 7/7 ✅ · full **71/71 331 assert** (era 64/323, +7 esatti) ✅ · phpstan 0 @ max ✅ · pint --test ✅ · validate --strict ✅; solo 2 file nuovi (nessun path protetto), `git diff main -- composer.json composer.lock` vuoto. tasks 2/15; gruppo 2 (schema) 1/4. Next 2.2 `create_audit_records_table`: stesso envelope CORE meno event_id/name/schema_version/causation_id, più action/before/after/authorization_basis; riusa verbatim il blocco CHECK branch-ato e l'idiom test hasColumns/hasIndex, stamp `2026_06_12_000002_…`.
## [2026-06-12 16:05] ralph | foundations-domain-events-audit 2.2 | green | 2 files (SECONDA tabella substrato: `database/migrations/2026_06_12_000002_create_audit_records_table.php` — il trail di audit delle azioni operatore, design D2, invariante 8 "ogni azione operatore registra actor_role; trail append-only". Condivide SOLO l'envelope CORE con `domain_events` (no `event_id`/`name`/`schema_version`/`causation_id` — concerns del log eventi); aggiunge le colonne audit-specifiche. Colonne: `id` bigint PK (id-suffisso dell'indice → trail in append-order gratis) · `occurred_at` timestampTz (UTC, UNICO timestamp — nessun created/updated_at, task 3.1) · `module` · `actor_role` string NOT NULL · `actor_id` nullable · `entity_type`/`entity_id` string · `correlation_id` uuid (**no FK** — chiave di raggruppamento soft che lega l'azione agli eventi che ha innescato, come `domain_events.correlation_id`) · `action` string NOT NULL (il verbo, es. 'voucher.cancel') · `before`/`after` jsonb **nullable** (creazione non ha before; redazione GDPR azzera entrambi — l'UNICA mutazione che il trigger immutabilità 2.4 permetterà, design D7) · `authorization_basis` string NOT NULL (la "by what right"). Indice singolo `(entity_type,entity_id,id)`. **CHECK actor_role** solo-PG lift verbatim da 2.1 (solo cambiati nome tabella + constraint `audit_records_actor_role_check`; valori da `ActorRole::cases()`, no drift); fallback SQLite = NOT NULL + cast enum, documentato inline. Test `tests/Feature/Platform/AuditRecordsSchemaTest.php` (`uses(RefreshDatabase::class)`) 8/11: colonne envelope-core+audit (`hasColumns`), **assenza** colonne event-log-only (`hasColumns(...)->toBeFalse()` per event_id/name/schema_version/causation_id — guard anti-drift), indice composito (`hasIndex`), happy-path inserimento riga completa che rilegge `action`, edge before/after-nullable (insert con entrambi null → riga esiste), TRE floor NOT-NULL (`actor_role`, `authorization_basis`, `action` ciascuno → `QueryException`). format ✅ · test_filter 8/8 ✅ · full **79/79 342 assert** (era 71/331, +8 esatti) ✅ · phpstan 0 @ max ✅ · pint --test ✅ · validate --strict ✅; solo 2 file nuovi (nessun path protetto), `git diff main -- composer.json composer.lock` vuoto. tasks 3/15; gruppo 2 (schema) 2/4. Next 2.3 `create_event_deliveries_table`: ledger per-consumer, unique `(domain_event_id, consumer)`, **partial index su `status='pending'`** (fluent se il Blueprint installato esprime il predicato — verificare in vendor — altrimenti raw `CREATE INDEX … WHERE` valido su entrambi gli engine).
