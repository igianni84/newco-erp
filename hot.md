---
type: meta
description: Hot cache — repo-state digest (~500 words), overwritten on every significant operation. Not a journal (chronology lives in log.md).
updated: 2026-06-12
---

# Hot Cache

> Auto-injected at session start and post-compaction via hooks. OVERWRITTEN completely, never appended.
> Updated by: every ralph iteration (mandatory), and any interactive session that materially changes the repo.

## Last Updated
**2026-06-12 22:45 (ralph iter — task 5.2/15 ✅ green: la lane CI pgsql)** — Gruppo 5 (hello-world + CI) **COMPLETO 2/2**. MODIFICATO `.github/workflows/ci.yml`: aggiunto un job SEPARATO `tests-pgsql` (DOPO il job `quality`, lasciato invariato salvo un commento header) che ri-esegue `php artisan test` contro `postgres:17` (il floor del DB-ADR) — `services.postgres` con health-check `pg_isready` + creds usa-e-getta + `ports: ['5432:5432']` (il job gira sull'HOST runner, non in `container:`, quindi porta pubblicata + `DB_HOST: 127.0.0.1` non `localhost`/`::1`); `env:` a livello-job (`DB_CONNECTION: pgsql` + host/port/db/user/password); setup-php `extensions` + `pdo_pgsql, pgsql`; stessi step checkout/cache/install/`.env`+`key:generate`; poi SOLO la gate `php artisan test` (lint/PHPStan NON duplicati — engine-independent, D8). MODIFICATO `tests/Feature/CiWorkflowTest.php` (+3 test, 5→8): pin lane (job name, `image: postgres:17`, `pdo_pgsql`, `DB_CONNECTION: pgsql`, step distinto `Tests (Pest on PostgreSQL 17)`) + guard `substr_count` (run-line pint/phpstan===1, `php artisan test`===2 = il caso failure-ish) + pin scoping/ordine (lo step SQLite del `quality` PRIMA di `tests-pgsql:` PRIMA di `DB_CONNECTION: pgsql`, così i pin gate-order pre-esistenti `strpos` restano ancorati su SQLite); i 5 pin pre-esistenti INVARIATI. MODIFICATO `docs/development.md`: riscritta la sezione CI (due lane, cosa prova ciascuna, pin PG-17=floor-ADR, meccanismo job-env-batte-phpunit.xml, "prima run live = push umano").

## Build & Quality Status
- Stack invariato: PHP 8.5.2 · Laravel 13.x · Filament v5 · Pest 4.7.2 · PHPStan 2.2.2 · Larastan 3.10.0 · Pint 1.29.1. SQLite dev; tests `:memory:`.
- Quality su `ralph/foundations-domain-events-audit`: full suite **143/143 (565 assertioni)** ✅ (era 140/555, +3 = i 3 nuovi pin `CiWorkflowTest`) · type_check 0 @ level max ✅ · lint ✅ · `openspec validate foundations-domain-events-audit --strict` ✅. `composer.json/lock` invariati vs main (zero nuove dep). Gruppi: schema (2) CHIUSO 4/4 · substrate API (3) CHIUSO 4/4 · delivery (4) CHIUSO 2/2 · hello-world+CI (5) **CHIUSO 2/2**. Restano: docs+sweep (6) 0/2.

## Active Change & Next Task
- **Attivo: `foundations-domain-events-audit` — APPROVED, loop in corso. 13/15 task fatti.**
- **Prossimo task: 6.1** — `docs/event-substrate.md` (design D10): how-to-emit (recorder API + envelope + payload discipline: minor-units money, FX decimal-string con razionale D18, regola PII-free), how-to-consume (contract, registrazione sul provider-seam, obblighi idempotency + order-tolerance incl. il pattern watermark per-entità, regola DB-work-only + shape intent-row Module E §7 per I/O esterno), delivery semantics (inline, sweep, backoff/dead-letter, tunables), immutability (tre layer; il REVOKE runbook con SQL esatto `CREATE ROLE redactor` / `GRANT`; policy additive-only incl. la nota "estendi il trigger" per nuove colonne audit), nota consumer-rename ledger. Aggiungi la riga `docs/INDEX.md` + refresh `docs/module-template.md` §6 (la frase pgsql-lane ora punta alla lane ESISTENTE — decided-conventions-only, nessuna nuova regola). Test `tests/Feature/EventSubstrateDocsTest.php` mirror dell'idiom doc-pin sibling (reader condiviso, pin `toContain` per: i 3 nomi-tabella, `redactor`, `immutable`, la menzione del pattern watermark, `decimal string`); prova non-vacua (il pin helper fallisce forte su file assente). `ModuleTemplateDocsTest` deve restare verde dopo il refresh §6.
- Ordine successivo: 6.2 sweep finale (i 5 quality cmd in ordine tabella + validate --strict + walk di OGNI `#### Scenario:` in `specs/event-substrate/spec.md` E `specs/platform/spec.md` con tabella mapping scenario→test in progress.md, qualunque scenario scoperto prende il test sul posto + conferma che il red-proof arch 1.1 e la risoluzione D5 afterCommit sono registrati in progress.md — entrambi lo sono).

## Blockers & Decisions Needed
- **Nessun blocker.**
- **Job-env reale batte phpunit.xml `<env>` non-force, verificato + provato (5.2, D8)**: il chain `PhpHandler::handleEnvVariables` (putenv solo se `$force || getenv===false`) → Symfony `Process::getDefaultEnv()` (subprocess eredita le env OS del parent) → Laravel immutable dotenv significa che un `DB_CONNECTION=pgsql` a livello-job VINCE end-to-end (`config('database.default')==='pgsql'`); provato empiricamente con un probe throwaway poi cancellato. NESSUN `--env` fallback necessario.
- La lane CI pgsql (5.2) + i branch PG (CHECK 2.1/2.2, partial index 2.3, trigger 2.4) sono visti verdi solo al push umano pre-merge (D8). Il codice 3.x/4.x/5.x è engine-agnostic.
- Carry-over: gate ADR aperti (identity/auth K · queue driver F4–F6 · object storage INV1 · hosting EU F7 · frontend TanStack S); debiti semantic-verify W1/W2/W3/S1/S3 dal bootstrap (gate Module K).

## Open Patterns
- **Lane CI per un secondo engine = un JOB SEPARATO, mai una matrix (5.2, D8)**: lint/PHPStan engine-independent girano UNA volta (job `quality`), solo `php artisan test` gira due volte (job `tests-pgsql`). Service `postgres:17` con `pg_isready` + `ports: ['5432:5432']` (job sull'host runner → porta pubblicata, `DB_HOST: 127.0.0.1` non `localhost`); `DB_*` a livello-job; setup-php `extensions` + `pdo_pgsql, pgsql`. Pin in `CiWorkflowTest` con `toContain` (job name, image, `DB_CONNECTION: pgsql`, step-name distinto) + `substr_count` (le gate engine-independent === 1, il test gate === 2) come guard non-vacuo; tieni `quality` PRIMO così i pin gate-order `strpos` ancorano sulla lane SQLite. Per un futuro service-lane (Redis al gate queue) stessa shape separate-job.
- **Asserzioni OUTPUT comando a PHPStan max = `Artisan::call()` + `Artisan::output()`** (5.1, corregge 4.2): il Pest `artisan()` è `@return PendingCommand|int` → inusabile a max sul ramo `int`.
- **Comando operator re-runnable su tabelle append-only (5.1, D9)**: ogni run APPENDE (UUIDv7 fresco), ogni STATE-write è `updateOrInsert` idempotente; "2° run anche exit 0" = il failure/edge test. Consumer demo-only registrati sul singleton DENTRO `handle()` (no provider always-on).
- **Scheduling Laravel 13 (4.2)**: sub-minute in `routes/console.php` via `Schedule::command(...)->everyThirtySeconds()->withoutOverlapping()`; comandi-classe fuori da `app/Console/Commands` registrati via `withCommands([Cmd::class])` in `bootstrap/app.php`; introspezione test via `app(Kernel::class)->bootstrap()` → `collect(app(Schedule::class)->events())->sole(...)`.
