---
type: meta
description: Hot cache — repo-state digest (~500 words), overwritten on every significant operation. Not a journal (chronology lives in log.md).
updated: 2026-06-16
---

# Hot Cache

> Auto-injected at session start and post-compaction via hooks. OVERWRITTEN completely, never appended.
> Updated by: every ralph iteration (mandatory), and any interactive session that materially changes the repo.

## Last Updated
**2026-06-16 (ralph iter 13 — task 4.6 of `catalog-lifecycle-approval` DONE).** Composite SKU lifecycle — the LAST CHILD and the N-CONSTITUENT case. The gate `foreach`-LOOPS the SAME `ActivationCascadeGate::assertParentActive` primitive over `$sku->constituents` (the `catalog_composite_sku_constituents` junction), each constituent a `ProductReference` — ZERO change to the per-PARENT primitive (Patterns #22/#23 confirmed across single/two/N-parent). **All SEVEN spine entities now have their full lifecycle.** Events `CompositeSKUActivated`/`Retired` (UPPER `SKU` NAME, `ENTITY_TYPE='CompositeSku'`); **payload = LEAN transition shape** `{composite_sku_id, constituent_product_reference_ids, lifecycle_state}` — the constituent ids ride (the gated parents, `(int)`-cast → clean `list<int>` cross-engine, Pattern #16) but `constituent_count` is OMITTED (a `*Created` convenience; the 12 sibling transition events carry no derived enrichment; spec says "entity ids + lifecycle/enum values only"). 11 files (9 new, 2 edited): 2 events + 5 thin Actions + `CompositeSku` FSM opt-in + the `CompositeSKUCreated` sibling-docblock fix (all 7 `*Created` now cross-ref siblings).

## Build & Quality Status
- Stack: PHP 8.5.2 · Laravel 13.15 · Filament 5.6.7 · Pest 4.7.2 · PHPStan 2.2.2 · Pint 1.29.1. SQLite dev (`:memory:`); prod PG17.
- **Full suite 600/600 on SQLite** (was 587; +13), phpstan max 0, pint clean. **PG17 verified:** `tests/Feature/Modules/Catalog` + `tests/Unit/Modules/Catalog` + `tests/Architecture` = **196/196** (was 183; +13) — the `(int)`-cast constituent-id list, jsonb array payload, loop-over-constituents sibling reads, per-entity governance lineage, `actor_id` string-vs-int, `FOR UPDATE` lock all proven; boundary/conventions/naming arch tests green. `openspec validate --strict` ✓. Guards: no new migration, no composer drift, no protected files, no Parties import in production.

## Active Change & Next Task
- **`catalog-lifecycle-approval` — 13/17 tasks.** Done: producer projection+consumer (1.x), shared FSM+governance+rejection (2.x), Master events+activate/retire+gate (3.x), all 6 other entities (4.1 Format, 4.2 CaseConfig, 4.3 Variant, 4.4 PR, 4.5 SellableSKU, 4.6 CompositeSKU). Remaining: cascades (5.x), docs+e2e (6.x).
- **Next: task 5.1** — activation cascade INTEGRATION test (+ any small ordering glue): build Master→Variant→PR→SellableSKU (+Format/CaseConfig), project producer `active`, activate each in order with distinct operators, assert the four `*Activated` `domain_events.id` are in parent-before-child (hierarchy) order (§14.3 / AC-0-FSM-10); assert activating a child before its parent is `active` is rejected at every level. `tests/Feature/Modules/Catalog/ActivationCascadeTest.php` (`DatabaseMigrations`). **DB-touching → PG17 required.** Likely test-mostly (the gate composition already works — this proves the ORDERING falls out). Reusable: the 4.x `lifecycle*ActiveParent`/`compositeSkuLifecycleActiveReferences` full-chain helpers.

## Blockers & Decisions Needed
- **None blocking.** Standing design calls (stable across ralph): (1) standalone entities (Format/CaseConfig) pass NO `$gate` but STILL run approval governance; (2) child entities add a within-module parent-active `gate:` via the shared `ActivationCascadeGate` — REUSE it, no new gate family (single→once, multi→once per parent, N-constituent→loop the junction); (3) child `*Activated`/`*Retired` payload = `{<entity>_id, <parent ids…>, lifecycle_state}` (lifecycle_state LAST; lean — no derived enrichments) per design D9.
- Deferred follow-ons (named): `catalog-operator-console` (Filament approval queue UI), `parties-compliance` (KYC tightens `ActivateProducer` — D6), Phase-3 referencers (cross-module retirement-blocking refs — D8, extends 5.2).

## Open Patterns
- **Full suite = `php -d memory_limit=512M vendor/bin/pest`** (NOT `php artisan test` — 128M OOMs in arch plugin).
- **Per-entity lifecycle recipe — proven on ALL 7 spine entities, stable:** two `final` events + model `HasLifecycleState` opt-in + five thin Actions + fix the `*Created` "no sibling" docblock. Standalone → Activate passes NO `$gate`; children → Activate passes a `gate:` calling `ActivationCascadeGate::assertParentActive` (single: once; multi: once per parent, ordered, each named; N-constituent: `foreach` the junction). SKU naming split: event NAME/class UPPER `SKU`, model/Actions/`ENTITY_TYPE` use `Sku`. Audit verb table-derived (Pattern #14). See progress.md Codebase Patterns.
- **Gate ordering (test fixtures):** from-state FIRST → governance SECOND → gate THIRD. Self-approval negative needs ACTIVE parents (isolate governance); from-state negative needs DRAFT child (prove FSM precedes gate); N-constituent negative holds the FIRST constituent active + the inactive one SECOND (proves the loop checks past the first). Factory-`lifecycle_state` parents are legit for negatives (gate reads only `lifecycleState()`); reserve genuinely-active immediate parents for the flagship positive (full chain w/ event ordering = task 5.1).
- **Each `*LifecycleTest.php` names helpers UNIQUELY** (one shared Pest namespace — a redeclare is a fatal full-suite load error). Don't pass BOTH a file AND its folder to one `pest` run. For PG pass the three folders (`Feature/Modules/Catalog` + `Unit/Modules/Catalog` + `Architecture`).
- **Uncast bigint cross-engine:** int on SQLite, numeric STRING on PG — assert with `->toEqual()` (loose), or `(int)`-cast at the source for a clean contract (works when the `@property int` makes the cast phpstan-clean). THE reason event/audit/governance-lineage work is PG-gated.
- **PG17 gate:** `docker run -d --name pg -e POSTGRES_DB=newco_test -e POSTGRES_USER=newco -e POSTGRES_PASSWORD=newco -p 55432:5432 postgres:17`; wait `docker exec pg bash -c 'for i in $(seq 1 40); do pg_isready -U newco -q && exit 0; sleep 0.5; done'`; env `DB_CONNECTION=pgsql DB_HOST=127.0.0.1 DB_PORT=55432 DB_DATABASE=newco_test DB_USERNAME=newco DB_PASSWORD=newco php -d memory_limit=512M vendor/bin/pest <folders>`; `docker rm -f pg`. Lifecycle tests use `DatabaseMigrations`, not `RefreshDatabase`.
