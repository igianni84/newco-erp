---
type: meta
description: Hot cache — repo-state digest (~500 words), overwritten on every significant operation. Not a journal (chronology lives in log.md).
updated: 2026-06-12
---

# Hot Cache

> Auto-injected at session start and post-compaction via hooks. OVERWRITTEN completely, never appended.
> Updated by: every ralph iteration (mandatory), and any interactive session that materially changes the repo.

## Last Updated
**2026-06-12 11:40 (ralph — foundations-modules-skeleton 2.3)** — **Task 2.3 green: the platform-direction half of the boundary law is now mechanically enforced (design D3, the table's second row).** Extended `tests/Architecture/ModuleBoundariesTest.php` with a SECOND `it()` block (`forbids platform code from depending on any module`): the enumerated platform namespaces live in one named array `$platformNamespaces = ['App\Providers','App\Models','App\Http']` (the template's amendment point) and the test LOOPS it asserting `expect($ns)->not->toUse('App\Modules')` per namespace. **No `->ignoring()`** — platform code gets NO public-surface exemption (D3 row 2: Contracts/Events = NO, internals = NO); the dependency target is `App\Modules` as a whole. `bootstrap/providers.php` is NOT special-cased (it's under `bootstrap/`, outside `app/`, so in no `App\*` layer). **A real trap was caught by the mandatory red-proof** (see lessons.md + Codebase Patterns): the first formulation `expect([all 3])->not->toUse('App\Modules')` was vacuously GREEN even with a violating fixture present — an array on the SOURCE side under `not` negates the AGGREGATE "ALL sources use X", so it passes the moment one source is clean. Rewrote as the per-source loop. **RED-PROOF recorded** in progress.md: temp `app/Providers/TmpPlatformProbe.php` (typed property `public CatalogServiceProvider $provider` — a type reference is reliably collected; `::class` alone was observed NOT to register) → RED `Expecting 'App\Providers' not to use 'App\Modules'.` naming the offender; remove → GREEN 2/2 (150 assertions); `git status` only the test file. Progress **6/9 tasks**.

## Build & Quality Status
- **Stack invariato** (zero composer churn — this change adds NO deps): PHP 8.5.2 · Laravel 13.15.0 (`^13.8`) · Filament v5.6.7 + Livewire v4.3.1 · Pest 4.7.2 incl. **pest-plugin-arch v4.0.2** (already in lock) · PHPStan 2.2.2 · Larastan 3.10.0 · Pint 1.29.1. SQLite dev; tests `:memory:`.
- Quality loop (post-2.3): format ✅ · filtered `--filter="forbids platform code from depending on any module"` 1/1 ✅ · **full suite 52/52 (291 assertions)** ✅ (was 51/285 — the new platform `it()` adds 1 test) · type_check 0 @ level max ✅ · lint ✅ · `openspec validate foundations-modules-skeleton --strict` ✅. `git diff main -- composer.json composer.lock` empty. No protected files touched.

## Active Change & Next Task
- **`foundations-modules-skeleton` (F1 1/3), branch `ralph/foundations-modules-skeleton`, 6/9 tasks done.**
- **Next task: 2.4** — forward-binding persistence-convention test (design D6): `tests/Architecture/ModulePersistenceConventionsTest.php`. Every class under `App\Modules\**` extending `Illuminate\Database\Eloquent\Model` must declare an explicit `$table` starting with its owning module's prefix (`Module` enum backing value + `_`, e.g. `catalog_`). **Zero module models exist today → pass-by-PROVEN-EMPTY** (prove the scan ran over the module tree and found 0 models; never pass-by-error or skip). Discover candidates by globbing `app/Modules/*/**/*.php` → path→FQCN → `class_exists` + `ReflectionClass::isSubclassOf(Model::class)` (skip abstract) → read default via `ReflectionClass::getDefaultProperties()['table']` → derive required prefix from the file's module segment through the `Module` enum. **RED-PROOF ×3 mandatory:** temp Catalog model `$table='product_masters'` → FAIL (missing prefix); temp model with no explicit `$table` → FAIL; temp model `$table='catalog_product_masters'` → PASS; all temp files removed (`git status` clean); record all three outputs in progress.md.
- **Then:** 3.1 `docs/module-template.md` (9 sections, D7) + INDEX/development.md rows → 4.1 traceability sweep + scenario→test map. On `CHANGE_COMPLETE`: human reviews/merges/archives (ralph never pushes).

## Blockers & Decisions Needed
- None for this change. All names verified against repo/vendor; no protected files touched; no open ADR gate stepped into by the skeleton.
- **Carry-over (not this change):** human edits to CLAUDE.md from ADR-1/ADR-2 sessions (if not yet applied); semantic-verify debts W1/W2/W3/S1/S3 from bootstrap (bonify before staging / Module K gate). Open ADR gates: identity/auth (K) · queue driver (F4–F6) · object storage (INV1) · hosting EU (F7) · frontend TanStack (Module S).

## Open Patterns
- **Registry is the single source of "the nine".** Every conformance/arch test AND the composition root iterate `Module::cases()` — never a hardcoded list. `->namespace()` FQCN root, `->value` table prefix, `->letter()` spec letter, `->providerClass()` standard provider FQCN. The ONE legitimate hardcoded list is `$platformNamespaces` (2.3) — the *complement* of the registry (non-module `App\*` roots), which `Module::cases()` cannot derive.
- **Import-boundary arch tests — arrays on the DEPENDENCY side, loops on the SOURCE side.** `not->toUse([deps])` = "source uses ANY of these" (safe; 2.2's `$forbidden`). But `expect([sources])->not->toUse(X)` negates the AGGREGATE "ALL sources use X" → passes when ANY one source is clean → **silently masks a violation** (proven in 2.3 red-proof). So loop: one `expect($source)->not->toUse(X)` per source. `->ignoring([...])` excludes by namespace PREFIX (so the Contracts/Events allow-list works); platform direction uses NO ignore (no public-surface exemption). A detected "use" = an AST `Node\Name` passing `class_exists` after NameResolver → use a TYPE reference (typed prop/param/extends/new) in fixtures; `Foo::class` alone may not register. Repository globs PSR-4 dirs live → NEW fixtures need no dump-autoload.
- **The Architecture suite is boot-free, registry-located, set-equality.** `tests/Architecture/` = own `<testsuite>` in `phpunit.xml` (→ default `php artisan test`; count delta is the proof) but NOT bound to `Tests\TestCase` in `tests/Pest.php` (arch/conformance/convention need no container). Root via reflection on `Module::class`, never `app_path()`. Type-clean at level max: guard `getFileName(): string|false` with `(string)`, `scandir(): array|false` with `?: []`. Filter dot-entries.
- **Each arch task (2.1–2.4) REQUIRES a red-proof** (temp violating fixture → suite red → remove → green, both outputs in progress.md). It is the only thing that catches a vacuously-green arch test (2.3 proved this). NEW app/ fixtures need no `composer dump-autoload` (PSR-4 resolves new paths live); dump-autoload is only for MOVED/RENAMED `App\**` classes (stale classmap → deleted-file include → Pest failure). Vendor gitignored → zero churn.
- **App-file idiom:** no `declare(strict_types=1)`; Pint laravel preset (no spaces around `.`); exhaustive `match($this)` over the enum (no `default`).
- Full prior-phase patterns: `openspec/changes/archive/2026-06-11-bootstrap-laravel-app/progress.md` → `## Codebase Patterns`.
