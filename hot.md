---
type: meta
description: Hot cache — repo-state digest (~500 words), overwritten on every significant operation. Not a journal (chronology lives in log.md).
updated: 2026-06-17
---

# Hot Cache

> Auto-injected at session start and post-compaction via hooks. OVERWRITTEN completely, never appended.
> Updated by: every ralph iteration (mandatory), and any interactive session that materially changes the repo.

## Last Updated
**2026-06-17 (interactive — spec/ linked to the shared documentation repo + 3 ADRs from Paolo's 2026-06-16 call).** `spec/` is now a **vendored mirror of `c-mless/documentation:handoff/`**, regenerated by new `scripts/sync-spec.sh` and pinned in `spec.lock` (chosen over a submodule — preserves structure, zero breakage to the ~360 `spec/` citations; `reference/v1.1/` stays out). First sync pinned `cmless/main` @ `4f48277`, which brought the **producer-KYC clarification**. Two call-note clarifications captured as ADRs (+ INDEX): **producer-KYC** `not_required` clears the gate like `verified` (already ratified upstream → lands in deferred `parties-compliance`); **approval SoD** "same person may create+approve if role-qualified" — contradicts spec + shipped code, so **erratum + Slack message drafted for Paolo** (`docs/spec-errata/2026-06-17-approval-sod-same-actor.md`), no code change yet.

## Build & Quality Status
- Stack: PHP 8.5.2 · Laravel 13.15 · Filament 5.6.7 · Pest 4.7.2 · PHPStan 2.2.2 · Pint 1.29.1. SQLite dev (`:memory:`); prod PG17.
- **Green on `main`: 613/613 SQLite AND 613/613 PG17.** This session is **docs/tooling/ADR-only — no app code touched.**
- **Committed to `main` + pushed (2026-06-17):** spec-link tooling (`sync-spec.sh` + `spec.lock`), the `spec/` refresh (8 files), 3 ADRs, `decisions/INDEX.md`, the erratum, memory.

## Active Change & Next Task
- **NONE active** (`openspec list` → none).
- **Next (ERP):** `/spec-to-change` for the next Build_Workplan slice. Deferred: `catalog-operator-console`, **`parties-compliance`** (will implement producer-KYC `cleared = verified ∨ not_required` + operator-waive), Phase-3 cross-module referencers (D8).
- **SoD change is BLOCKED** on Paolo ratifying the erratum — do not implement against the current spec.

## Blockers & Decisions Needed
- **Erratum to Paolo (SoD)** — Slack message drafted (in the erratum file); Giovanni to send. His answer gates the SoD change.
- **Note:** `scripts/sync-spec.sh` fetches the doc repo via the `GiovanniCrurated` gh account; the ai-dev remote `igianni84/newco-erp` uses `igianni84` (active).

## Open Patterns
- **Refresh spec/ = run `scripts/sync-spec.sh`** (ff the `../documentation` clone from `cmless`, mirror `handoff/`→`spec/`, repin `spec.lock`); review diff, commit refresh + lock together.
- **Dreaming routine live** (weekly cloud, Mon 06:00 UTC, first auto-run 2026-06-22; propose-only via `dream/<date>` PR).
- **`knowledge/`: architecture 2 · data-model 2+1 · laravel 3+1 · testing 4+1.** Promotion bar ≥3 dated cross-change confirmations.
- **Full suite = `php -d memory_limit=512M vendor/bin/pest`** (not `php artisan test` — 128M OOMs).
- **PG17 gate** before a DB task is done: `docker run -d --name pg … postgres:17`; `DB_CONNECTION=pgsql … -p 55432`; `docker rm -f pg`.
- **Heredoc `cat <<EOF` mentioning "spec" trips the git-guardrails Bash hook** — write via Edit/Write or `scripts/memlog.sh`.
