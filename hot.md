---
type: meta
description: Hot cache — repo-state digest (~500 words), overwritten on every significant operation. Not a journal (chronology lives in log.md).
updated: 2026-06-12
---

# Hot Cache

> Auto-injected at session start and post-compaction via hooks. OVERWRITTEN completely, never appended.
> Updated by: every ralph iteration (mandatory), and any interactive session that materially changes the repo.

## Last Updated
**2026-06-12 12:04 (ralph — foundations-modules-skeleton 3.1)** — **Task 3.1 green: the module-build template is written.** New `docs/module-template.md` — the falsariga every F2+ module change follows — with EXACTLY the nine sections of design D7, documenting **DECIDED conventions only (D1–D8), inventing nothing**: (1) nine modules + spec-letters table (registry-driven); (2) canonical layout tree + lazy-creation rule (`Providers/` only, no `.gitkeep`); (3) public surface `Contracts\*`+`Events\*` & the D3 boundary-law table + amendment protocol; (4) service-provider conventions (routes/translations/listeners/bindings); (5) operator-surface placement (D5 string-based `->discoverResources`); (6) persistence (D6 module-prefixed `$table`, literal `catalog_`, migrations in global `database/migrations/`, "Postgres-truthful, SQLite-compatible" policy → `decisions/2026-06-12-production-db-engine.md`); (7) terminology & naming cascade (CONTEXT.md glossary of record, Module 0 §18 source-of-truth); (8) test patterns (Architecture suite always-on, per-module `tests/{Feature,Unit}/Modules/{X}`, SQLite `:memory:`, PHPStan max, red-proof); (9) add-a-module-slice checklist. Added the `docs/INDEX.md` row + a one-line `docs/development.md` cross-ref. Doc-pin test `tests/Feature/ModuleTemplateDocsTest.php` (7 tests/24 assertions, `DevelopmentDocsTest`-style): file exists; nine numbered sections via `range(1,9)`; **registry-complete** — `foreach Module::cases()` requires a line pairing `->name` AND `->letter()` (same-line `str_contains` both); `Contracts`+`Events`, literal `catalog_`, `CONTEXT.md` present; **non-vacuous** — the same pairing helper returns empty for a non-module name. **Group 3 (docs) COMPLETE 1/1. Progress 8/9 tasks** — only 4.1 (final sweep) remains.

## Build & Quality Status
- **Stack invariato** (zero composer churn — this change adds NO deps): PHP 8.5.2 · Laravel 13.15.0 (`^13.8`) · Filament v5.6.7 + Livewire v4.3.1 · Pest 4.7.2 incl. pest-plugin-arch v4.0.2 · PHPStan 2.2.2 · Larastan 3.10.0 · Pint 1.29.1. SQLite dev; tests `:memory:`.
- Quality loop (post-3.1): format ✅ · filtered `--filter="is registry-complete"` 1/1 (9 assertions) ✅ · **full suite 60/60 (317 assertions)** ✅ (was 53/293 — the new doc-pin test adds 7 tests / 24 assertions) · type_check 0 @ level max ✅ · lint ✅ · `openspec validate foundations-modules-skeleton --strict` ✅. `git diff main -- composer.json composer.lock` empty. No protected files touched.

## Active Change & Next Task
- **`foundations-modules-skeleton` (F1 1/3), branch `ralph/foundations-modules-skeleton`, 8/9 tasks done.** Groups 1 (skeleton 3/3) + 2 (arch suite 4/4) + 3 (docs 1/1) complete.
- **Next task: 4.1** — Traceability + full-quality sweep (the LAST task). Steps: (a) run the five Quality Commands in CLAUDE.md table order, all green; (b) `openspec validate foundations-modules-skeleton --strict` green; (c) **walk EVERY `#### Scenario:` in `openspec/changes/foundations-modules-skeleton/specs/module-architecture/spec.md`** and record a scenario→covering-test mapping table in `progress.md` — if any scenario lacks a covering test/recorded red-proof, ADD it on the spot (expected: full coverage already exists — the 14 scenarios map to ModuleProvidersTest [providers load, panel served/unchanged/no-special-rights], ModuleConformanceTest [no stray entries], ModuleBoundariesTest [internal-import-fails, public-surface-allowed, platform-never-imports, violations-caught via recorded red-proofs], ModulePersistenceConventionsTest [prefixed-table, empty-set-honest], the phpunit `Architecture` testsuite [suite runs by default], ModuleTest [registry matches mapping], ModuleTemplateDocsTest [template exists+indexed, registry-complete]); (d) confirm `git status` shows no protected-file mod and `git diff main -- composer.json composer.lock` empty. This task adds **no new tests** unless the mapping finds a gap; the deliverable is the recorded mapping + green sweep.
- **On all-green → reply exactly `<promise>CHANGE_COMPLETE</promise>`** (do NOT archive/merge — humans review/merge/archive; ralph never pushes).

## Blockers & Decisions Needed
- None for this change. All names verified against repo/vendor/spec; no protected files touched; no open ADR gate stepped into by the skeleton.
- **Carry-over (not this change):** human edits to CLAUDE.md from ADR-1/ADR-2 sessions (if not yet applied); semantic-verify debts W1/W2/W3/S1/S3 from bootstrap (bonify before staging / Module K gate). Open ADR gates: identity/auth (K) · queue driver (F4–F6) · object storage (INV1) · hosting EU (F7) · frontend TanStack (Module S).

## Open Patterns
- **Registry is the single source of "the nine".** Every conformance/arch/doc-pin test AND the composition root iterate `Module::cases()` — never a hardcoded list. `->namespace()` FQCN root, `->value` table prefix, `->letter()` spec letter, `->providerClass()` standard provider FQCN. The ONE legitimate hardcoded list is `$platformNamespaces` (2.3) — the *complement* of the registry.
- **Three arch-test SHAPES in `tests/Architecture/`** (boot-free, registry-located via reflection on `Module::class`, own `<testsuite>` in `phpunit.xml`): (1) **conformance** (2.1, `scandir` set-equality); (2) **import-boundary** (2.2/2.3, pest-plugin-arch `not->toUse([deps])->ignoring([allowed])` — **arrays on the DEPENDENCY side, loops on the SOURCE side**); (3) **property-convention** (2.4, hand-rolled reflection scan, proven-empty). Each arch task carries a mandatory **red-proof** (temp violating fixture → red → remove → green, recorded in progress.md).
- **Doc-pin test idiom** (`tests/Feature/{X}DocsTest.php`, mirrors `DevelopmentDocsTest`, lives in Feature for `base_path()`): helper reads the doc; happy-path `toContain` pins; the non-vacuous proof is ONE shared mechanism exercised both ways (non-empty real input / empty absent input). Module-enumerating docs → same-line `str_contains(name) && str_contains(letter)`; the NAME is the real "row dropped" guard.
- **App-file idiom:** no `declare(strict_types=1)`; Pint laravel preset (no spaces around `.`); exhaustive `match($this)` over the enum (no `default`). MOVED/RENAMED `App\**` classes need `composer dump-autoload` (stale classmap); NEW files don't.
- Full prior-phase patterns: `openspec/changes/foundations-modules-skeleton/progress.md` → `## Codebase Patterns` and `openspec/changes/archive/2026-06-11-bootstrap-laravel-app/progress.md`.
