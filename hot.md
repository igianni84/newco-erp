---
type: meta
description: Hot cache — repo-state digest (~500 words), overwritten on every significant operation. Not a journal (chronology lives in log.md).
updated: 2026-06-12
---

# Hot Cache

> Auto-injected at session start and post-compaction via hooks. OVERWRITTEN completely, never appended.
> Updated by: every ralph iteration (mandatory), and any interactive session that materially changes the repo.

## Last Updated
**2026-06-12 21:25 (ralph iter — task 4.2/15 ✅ green: events:sweep + schedule + tunables)** — Gruppo 4 (delivery) **COMPLETO 2/2**. MODIFICATO `InlineDeliveryExecutor`: estratto `dueDeliveries(): Builder<EventDelivery>` (predicato `pending` AND backoff-elapsed) condiviso da `deliver()` (path hook inline, `whereIn` event-id + ordine `domain_event_id,id`) e NUOVO `deliverDue(): void` (path sweep, TUTTE le due, ordine `(consumer, domain_event_id)` per D6 → no per-consumer FIFO); entrambi riusano `attempt()`; `deliver()` invariato comportamentalmente. NUOVO `SweepCommand` (`$signature='events:sweep'`, `handle(InlineDeliveryExecutor): int` → `deliverDue()` → SUCCESS). NUOVO `config/events.php` (`sweep.max_attempts` 5 / `backoff_base_seconds` 30 ×2 / `backoff_cap_seconds` 3600, `(int) env(...)`) — l'executor li leggeva già via `Config::integer(..., <stesso default>)`, il file rende solo espliciti + env-overridable. MODIFICATO `bootstrap/app.php` `->withCommands([SweepCommand::class])` (D1: auto-discovery scansiona solo `app/Console/Commands`). MODIFICATO `routes/console.php` `Schedule::command('events:sweep')->everyThirtySeconds()->withoutOverlapping()`. Test `tests/Feature/Platform/SweepTest.php` 5/37 (crash-recovery · backoff+skip-not-due · dead-letter · poison-no-stall · schedule-entry introspettata).

## Build & Quality Status
- Stack invariato: PHP 8.5.2 · Laravel 13.x · Filament v5 · Pest 4.7.2 · PHPStan 2.2.2 · Larastan 3.10.0 · Pint 1.29.1. SQLite dev; tests `:memory:`.
- Quality su `ralph/foundations-domain-events-audit`: full suite **136/136 (526 assertioni)** ✅ (era 131/489, +5 = SweepTest) · type_check 0 @ level max ✅ · lint ✅ · `openspec validate foundations-domain-events-audit --strict` ✅. `composer.json/lock` invariati vs main (zero nuove dep). Gruppi: schema (2) CHIUSO · substrate API (3) CHIUSO 4/4 · delivery (4) **CHIUSO 2/2** (executor+wiring 4.1 ✅; sweep+schedule 4.2 ✅).

## Active Change & Next Task
- **Attivo: `foundations-domain-events-audit` — APPROVED, loop in corso. 11/15 task fatti.**
- **Prossimo task: 5.1** — hello-world `php artisan events:demo` + E2E pipeline test (design D9, founder-confirmed). Comando operator-runnable: in UNA `DB::transaction()` un cambio stato `cache` + un evento demo sintetico (`PlatformDemoRecorded`, module `'platform'`, actor `system`) + un audit record (`platform.demo`, con `before`/`after` + `authorization_basis`); post-commit il consumer demo (registrato per l'evento demo, DB-work-only) gira; poi probe immutabilità (UPDATE e DELETE su `domain_events`, catturati) — stampa il trail (evento con `event_id`/`actor_role`, audit id, delivery `done`, probe rifiutati) ed esce 0. Plus l'E2E feature test che copre lo stesso flusso come unico scenario full-stack. **Registra `DemoCommand::class` nello STESSO `withCommands([...])` di `bootstrap/app.php`** (classe sotto `App\Platform\Events\` o sottodir `Demo/`); identificatori SINTETICI (mai nomi-evento spec verbatim). Per le asserzioni OUTPUT usa il Pest `artisan()->expectsOutputToContain(...)->assertExitCode(0)` (il ramo `PendingCommand`); per run+exit-code puro `Artisan::call()` resta più pulito. Lo sweep NON fa parte del demo (l'hook inline consegna). Test `HelloWorldPipelineTest.php` + `EventsDemoCommandTest.php`; il demo deve lasciare il DB ri-eseguibile (2° run anch'esso exit 0 = failure/edge case).
- Ordine successivo: 5.2 lane CI pgsql (job `tests-pgsql`, `postgres:17`, estende `CiWorkflowTest`) · 6.1 `docs/event-substrate.md` + INDEX + refresh module-template §6 · 6.2 sweep finale + mapping scenari→test in progress.md.

## Blockers & Decisions Needed
- **Nessun blocker.**
- **Registrazione comandi platform = `withCommands()` in `bootstrap/app.php`** (4.2, design D1): `Kernel::shouldDiscoverCommands()` carica solo `app/Console/Commands` (e solo perché nessun `app/Console/Kernel.php` custom esiste → `get_class($this)===Foundation\Console\Kernel`). I comandi sotto `App\Platform\Events\` (Sweep ora, Demo a 5.1) si registrano esplicitamente lì. `bootstrap/` NON è nei path PHPStan, ma `routes/console.php` SÌ.
- La lane CI pgsql (5.2) + i branch PG (CHECK 2.1/2.2, partial index 2.3, trigger 2.4) sono visti verdi solo al push umano pre-merge (il loop non pusha; design D8). Il codice 3.x/4.x è engine-agnostic.
- Carry-over: gate ADR aperti (identity/auth K · queue driver F4–F6 · object storage INV1 · hosting EU F7 · frontend TanStack S); debiti semantic-verify W1/W2/W3/S1/S3 dal bootstrap (gate Module K).

## Open Patterns
- **Scheduling Laravel 13 + registrazione comando esplicita (4.2)**: schedule sub-minute in `routes/console.php` via facade `Schedule::command('name')->everyThirtySeconds()->withoutOverlapping()`; comandi-classe fuori da `app/Console/Commands` registrati via `withCommands([Cmd::class])` in `bootstrap/app.php`. Introspezione test: `app(\Illuminate\Contracts\Console\Kernel::class)->bootstrap()` (carica routes/console.php) → `collect(app(Schedule::class)->events())->sole(fn (Event $e) => is_string($e->command) && str_contains($e->command,'name'))` (`Schedule` è singleton FoundationServiceProvider; `events()` tipato `Event[]`; `sole()` lancia se assente = prova esistenza); asserisci `$e->repeatSeconds`/`$e->withoutOverlapping` (props pubbliche).
- **`Artisan::call('cmd')` → `int` pulito vs Pest `artisan()` → `PendingCommand|int`** (4.2): per run+exit-code usa `expect(Artisan::call('cmd'))->toBe(0)` (il ramo int di `artisan()` fa scattare PHPStan sul chaining `->assertExitCode`, e `$this->artisan()` è inusabile in closure Pest = `$this` è `TestCall`). Tieni il Pest `artisan()->expectsOutputToContain(...)` per le asserzioni OUTPUT (5.1 demo).
- **`?->` null-safe su cast Carbon nullable** (4.2): `expect($model->available_at?->equalTo($exp))->toBeTrue()` → `bool|null`, PHPStan-clean senza narrowing; `setTestNow($t0)` + `afterEach` reset, persiste attraverso `Artisan::call()` (process-global). `is_string()` guard NON `(string)` cast su mixed (entrambi i cast mixed→scalar fanno scattare max).
- **Condividi il predicato, non solo il loop** (4.2): due entry point di delivery (`deliver(eventIds)`/`deliverDue()`) che differiscono solo per scope/ordine → estrai il filtro "due" comune in UN `dueDeliveries(): Builder<EventDelivery>` (la definizione di "due" non può driftare tra inline e sweep = rischio di correttezza reale).
- **`Config::integer($key, $default)`** per int da config a PHPStan max (`(int) config()` trippa `cast.int`); il default nel 2° arg = servizio self-sufficient prima del `config/*.php`. **Container-resolve per FQCN + `instanceof`** narrowing+guard runtime. **Named test-doubles sotto `Tests\Support\`** (autoloaded, non raccolti da Pest, sink statico resettato in `beforeEach`). Convenzioni F1 legge: verify-vendor-first, runner JSON, per-file `uses(...)`, `DatabaseMigrations` per commit/rollback osservabili + tabella `cache` come state-change, platform parla `string` non l'enum `Module`.
