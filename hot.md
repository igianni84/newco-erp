---
type: meta
description: Hot cache — repo-state digest (~500 words), overwritten on every significant operation. Not a journal (chronology lives in log.md).
updated: 2026-06-11
---

# Hot Cache

> Auto-injected at session start and post-compaction via hooks. OVERWRITTEN completely, never appended.
> Updated by: every ralph iteration (mandatory), and any interactive session that materially changes the repo.

## Last Updated
**2026-06-11 12:40 (ralph iteration — task 3.1 green)** — **Filament v5.6.7 installed** (`composer require filament/filament:"^5.0" --no-interaction` + `filament:install --panels --no-interaction`). Panel id `admin` at `/admin` (generated `AdminPanelProvider`, committed as-generated). `User` now implements `FilamentUser` (`canAccessPanel(): true` — bootstrap scope; vendor middleware 403s any non-local env without it; revisit at Module K auth ADR). `config/operator.php` + standalone idempotent `OperatorSeeder` (env contract `OPERATOR_*` via config, RuntimeException on missing values, NOT in DatabaseSeeder). 7 new Pest tests (panel redirect/login-flow/actingAs-dashboard/wrong-password + seeder happy/idempotent/missing-password). 37 Filament assets under `public/` committed (regenerated by `filament:upgrade` post-autoload-dump hook).

## Build & Quality Status
- **Version snapshot:** PHP **8.5.2** · Composer **2.9.2** · Laravel **13.15.0** · **Filament v5.6.7** + Livewire **v4.3.1** · Pest **4.7.2** (+plugin-laravel 4.1.0) · PHPStan **2.2.2** · Larastan **3.10.0** · Pint **1.29.1**. SQLite dev DB; tests on sqlite `:memory:`.
- Quality loop (last run, 12:40): format ✅ · test_filter ✅ · full test **20/20 (48 assertions)** ✅ · type_check **0 errors @ level max** ✅ · lint ✅ · `openspec validate --strict` ✅. `QualityToolingTest` 5 pins survived the Filament composer-require rewrite.
- `phpstan.neon` now has `stubFiles: vendor/filament/forms/.stubs.php` (Filament's own typings for Livewire `Testable` macros — configuration, not suppression). PHPStan memory fix (`phpstan-bootstrap.php` ini_set 1G) unchanged and still needed.
- Quality-command output is shell-wrapped JSON (`{"tool":"pest","result":"passed",...}`), not vanilla CLI output.
- CI: not configured yet (task 3.3).

## Active Change & Next Task
- `openspec/changes/bootstrap-laravel-app/` — APPROVED, strict-valid, **7/10 tasks done** (1.1–2.4 ✅, 3.1 ✅).
- **Next task: 3.2** — Laravel Boost: `composer require laravel/boost --dev` then `php artisan boost:install` selecting Laravel + Filament guidelines (manual fallback per Boost/Filament docs if prompts block non-interactive use). Acceptance: Boost guideline files / `AGENTS.md` present + committed; `git status` confirms NO protected-file drift (`CLAUDE.md`, `RALPH.md`, `ralph.sh`, `.claude/**` except `.claude/memory/`); all five Quality Commands still green. Reference https://filamentphp.com/docs/llms.txt in docs notes.
- Then: 3.3 CI (`.github/workflows/ci.yml`) → 3.4 docs (`docs/development.md` + INDEX) — final task re-runs everything.
- Branch: `ralph/bootstrap-laravel-app`. Pinned per ADR 2026-06-11: laravel ^13.0, filament ^5.0, boost --dev.

## Blockers & Decisions Needed
- None active for the bootstrap change.
- Open ADR gates (none block bootstrap): production DB engine, identity/auth (now also owns `User::canAccessPanel()`), queue driver, event substrate, audit store, object storage, EU hosting, frontend stack.
- External sandbox credentials (Airwallex/Xero/HubSpot) needed before F6 changes — human-procured.

## Open Patterns
- **3.2 baseline to preserve:** suite **20 tests / 48 assertions** · phpstan **0 @ level max** · `QualityToolingTest` re-run after Boost's `composer require`; verify no protected-file drift via `git status`.
- **Filament 5 FQCNs:** auth pages at `Filament\Auth\Pages\*` (Login etc.) — verify in vendor, v3-era paths are gone. Failed login = ValidationException on `data.email`. Routes: `filament.admin.pages.dashboard`, `filament.admin.auth.login`.
- **Panel testing idiom:** `Livewire::test(Login::class)->fillForm([...])->call('authenticate')` (no pest-plugin-livewire; stubs make it type-clean). Pest `toThrow(X::class, 'substring')` = contains-check, the level-max-clean form.
- Versions: read from `composer show <pkg>` flat list, not the `versions :` constraint line. Pest `--filter` is regex over the description.
- Full list: `openspec/changes/bootstrap-laravel-app/progress.md` → `## Codebase Patterns`.
