---
type: meta
description: Hot cache — repo-state digest (~500 words), overwritten on every significant operation. Not a journal (chronology lives in log.md).
updated: 2026-06-12
---

# Hot Cache

> Auto-injected at session start and post-compaction via hooks. OVERWRITTEN completely, never appended.
> Updated by: every ralph iteration (mandatory), and any interactive session that materially changes the repo.

## Last Updated
**2026-06-12 23:20 (ralph iter — task 6.1/15 ✅ green: docs/event-substrate.md)** — Gruppo 6 (docs + final sweep) **1/2**. NUOVO `docs/event-substrate.md` (design D10): il single developer reference del substrato. §1 dove vive (`App\Platform` tree + le tre tabelle platform unprefixed con ruolo+mutabilità). §2 EMIT — firma esatta `DomainEventRecorder::record()`, regola must-run-in-caller's-transaction + `NotInTransactionException`, tabella colonne envelope, payload discipline (money minor-units + ISO 4217, **FX decimal string** col razionale D18 = refund all'exact-locked-rate quindi mai float, regola **PII-free** → immutabilità assoluta). §3 AUDIT (`AuditRecorder::record()`, default correlation UUID fresco, before/after può avere PII). §4 CONSUME — contract `DomainEventConsumer`, `ConsumerRegistry::register()` sul seam provider `boot()`, idempotency + order-tolerance + esempio codice **watermark per-entità**, **DB-work-only** + shape **intent-row Module E §7**, nota **consumer-rename ledger** (FQCN identity → un rename migra `event_deliveries.consumer` nello stesso change). §5 DELIVERY — hook inline post-commit, `events:sweep` = garanzia at-least-once + schedule everyThirtySeconds, exactly-once-for-DB-effects, isolamento R4, no-FIFO-per-consumer, backoff/dead-letter + tabella tunables `config/events.php` (5/30/3600). §6 IMMUTABILITÀ tre layer (trigger token `immutable`; **runbook REVOKE layer-2 con SQL esatto `CREATE ROLE redactor` / `GRANT INSERT, SELECT` / `GRANT UPDATE (before, after)`**; layer-3 additive-only + **nota extend-`$auditStructuralColumns`**). §7 proof pgsql-lane + `events:demo`. MODIFICATO `docs/INDEX.md` (riga) + `docs/module-template.md` §6 (frase pgsql-lane ora "esiste", decided-only, `catalog_` intoccato). NUOVO `tests/Feature/EventSubstrateDocsTest.php` (7 test) col reader-throwing + pin verbatim.

## Build & Quality Status
- Stack invariato: PHP 8.5.2 · Laravel 13.x · Filament v5 · Pest 4.7.2 · PHPStan 2.2.2 · Larastan 3.10.0 · Pint 1.29.1. SQLite dev; tests `:memory:`.
- Quality su `ralph/foundations-domain-events-audit`: full suite **150/150 (592 assertioni)** ✅ (era 143/565, +7 = i 7 nuovi pin `EventSubstrateDocsTest`) · type_check 0 @ level max ✅ · lint ✅ · `openspec validate foundations-domain-events-audit --strict` ✅. `composer.json/lock` invariati vs main (zero nuove dep). Gruppi: schema (2) CHIUSO 4/4 · substrate API (3) CHIUSO 4/4 · delivery (4) CHIUSO 2/2 · hello-world+CI (5) CHIUSO 2/2 · docs+sweep (6) **1/2**.

## Active Change & Next Task
- **Attivo: `foundations-domain-events-audit` — APPROVED, loop in corso. 14/15 task fatti.**
- **Prossimo task: 6.2 (L'ULTIMO)** — final traceability + quality sweep. NESSUN codice di produzione nuovo: (1) i cinque Quality Commands in ordine tabella CLAUDE.md tutti verdi; (2) `openspec validate foundations-domain-events-audit --strict` verde; (3) **walk di OGNI `#### Scenario:` in `specs/event-substrate/spec.md` E `specs/platform/spec.md`** — registra la tabella mapping scenario→test-che-lo-copre in `progress.md`; qualunque scenario scoperto prende il suo test sul posto; (4) conferma che i DUE proof richiesti sono già in `progress.md`: il red-proof arch 1.1 (entry @ 2026-06-12 14:26) E la risoluzione D5 afterCommit (entry 4.1 @ 2026-06-12 20:45 + Codebase Pattern) — entrambi presenti; (5) conferma `git status` nessun file protetto + `git diff main -- composer.json composer.lock` vuoto. Acceptance = i 5 cmd verdi in ordine + validate --strict + la tabella mapping senza scenari scoperti + i due proof presenti. Il deliverable è la mappa registrata + lo sweep verde (nessun test nuovo salvo gap scoperti).
- Dopo che 6.2 flippa → TUTTI i 15 task `[x]` → emetti `<promise>CHANGE_COMPLETE</promise>` (NON archiviare/mergiare — lo fa l'umano dopo review).

## Blockers & Decisions Needed
- **Nessun blocker.**
- **Idiom doc-pin (6.1)**: reader-helper che THROW `RuntimeException` su file mancante (mirror `lockedPackageVersion`) = la prova di non-vacuità (failure-ish = `toThrow(..., 'docs/missing.md')`); throwa PRIMA di `file_get_contents` quindi nessun warning E_WARNING→ErrorException (il `phpunit.xml` non setta `failOnWarning` ma non fidarsi). I pin sono substring VERBATIM: occhio al whitespace di allineamento dentro SQL fenced (pin `UPDATE (before, after)`, non `GRANT UPDATE (...)` che ha doppio spazio) e `immutable`≠substring-di-`immutability`. Quando rinfreschi una frase in un sibling-doc, preserva i suoi token pinnati (module-template §6 pinna `catalog_`).
- La lane CI pgsql (5.2) + i branch PG (CHECK 2.1/2.2, partial index 2.3, trigger 2.4) sono visti verdi solo al push umano pre-merge (D8). Il codice 3.x/4.x/5.x è engine-agnostic; i docs 6.x non toccano runtime.
- Carry-over: gate ADR aperti (identity/auth K · queue driver F4–F6 · object storage INV1 · hosting EU F7 · frontend TanStack S); debiti semantic-verify W1/W2/W3/S1/S3 dal bootstrap (gate Module K).

## Open Patterns
- **Doc-pin test idiom (6.1)**: sibling `*DocsTest.php` con reader globale (nome non-collidente — esistono `developmentGuide`/`moduleTemplate`/`developerDoc`/`eventSubstrateDoc`) + `toContain` per ogni fatto deciso; non-vacuità = reader-throwing o secondary-empty-on-bad-key. Pin verbatim (whitespace SQL + `immutable`≠`immutability`). Refresh di un sibling-doc preserva i suoi token pinnati.
- **Lane CI per un secondo engine = un JOB SEPARATO, mai una matrix (5.2, D8)**: lint/PHPStan engine-independent UNA volta (`quality`), solo `php artisan test` due volte (`tests-pgsql`). Service `postgres:17` + `pg_isready` + `ports:['5432:5432']` + `DB_HOST:127.0.0.1`; `DB_*` job-level; setup-php + `pdo_pgsql,pgsql`. Job-env reale batte `phpunit.xml <env>` non-force (verificato+provato). Per un futuro Redis-lane (gate queue) stessa shape separate-job.
- **Asserzioni OUTPUT comando a PHPStan max = `Artisan::call()` + `Artisan::output()`** (5.1): il Pest `artisan()` è `PendingCommand|int` → inusabile a max sul ramo `int`.
- **Comando operator re-runnable su tabelle append-only (5.1, D9)**: ogni run APPENDE (UUIDv7 fresco), ogni STATE-write è `updateOrInsert` idempotente; "2° run anche exit 0" = il failure/edge test. Consumer demo-only registrati sul singleton DENTRO `handle()`.
- **Scheduling Laravel 13 (4.2)**: sub-minute in `routes/console.php` via `Schedule::command(...)->everyThirtySeconds()->withoutOverlapping()`; comandi-classe fuori da `app/Console/Commands` registrati via `withCommands([Cmd::class])` in `bootstrap/app.php`; introspezione test via `app(Kernel::class)->bootstrap()` → `collect(app(Schedule::class)->events())->sole(...)`.
